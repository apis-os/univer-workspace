# Published @univerjs-pro originals

Untouched javascript-obfuscator builds (version `1.0.0-insiders.20260907-70fc579`).
Do not edit these. Readable patches live in `vendor/univer-pro/`.

Tarballs: `_tarballs/`. Registry: `https://insider-npm-registry.univer.work`.

The deobfuscator copies from here (not from `node_modules`) so a previous decode cannot poison the next run.

Kept in git for now (`lib/` and `dist/`, not `umd/`).
