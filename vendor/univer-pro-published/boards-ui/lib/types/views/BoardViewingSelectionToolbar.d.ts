/** Read-only actions are contributed by their owning plugins (for example comments). */
export declare function BoardViewingSelectionToolbar({ left, top }: {
    left: number;
    top: number;
}): import("react").JSX.Element | null;
