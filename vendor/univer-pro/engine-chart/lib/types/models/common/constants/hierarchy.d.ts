/**
 * Canonical persisted blank string accepted by hierarchy path tables.
 * The hierarchy builder either inherits the scoped value or terminates the path.
 */
export declare const CHART_HIERARCHY_BLANK_CELL = "";
