Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
//#region \0rolldown/runtime.js
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
	if (from && typeof from === "object" || typeof from === "function") {
		for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
			key = keys[i];
			if (!__hasOwnProp.call(to, key) && key !== except) {
				__defProp(to, key, {
					get: ((k) => from[k]).bind(null, key),
					enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
				});
			}
		}
	}
	return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", {
	value: mod,
	enumerable: true
}) : target, mod));

//#endregion
let _univerjs_core = require("@univerjs/core");
let _univerjs_docs = require("@univerjs/docs");
let _univerjs_engine_render = require("@univerjs/engine-render");
_univerjs_engine_render = __toESM(_univerjs_engine_render);
let _univerjs_ui = require("@univerjs/ui");
let _univerjs_protocol = require("@univerjs/protocol");
let rxjs = require("rxjs");
let rxjs_operators = require("rxjs/operators");
let _univerjs_design = require("@univerjs/design");
let react = require("react");
let react_jsx_runtime = require("react/jsx-runtime");
let _univerjs_icons = require("@univerjs/icons");

//#region src/basics/docs-view-key.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
let DOCS_VIEW_KEY = /* @__PURE__ */ function(DOCS_VIEW_KEY) {
	DOCS_VIEW_KEY["MAIN"] = "__Document_Render_Main__";
	DOCS_VIEW_KEY["BACKGROUND"] = "__Document_Render_Background__";
	return DOCS_VIEW_KEY;
}({});
let VIEWPORT_KEY = /* @__PURE__ */ function(VIEWPORT_KEY) {
	VIEWPORT_KEY["VIEW_MAIN"] = "viewMain";
	VIEWPORT_KEY["VIEW_TOP"] = "viewTop";
	VIEWPORT_KEY["VIEW_LEFT"] = "viewLeft";
	VIEWPORT_KEY["VIEW_LEFT_TOP"] = "viewLeftTop";
	return VIEWPORT_KEY;
}({});
const DOCS_COMPONENT_BACKGROUND_LAYER_INDEX = 0;
const DOCS_COMPONENT_MAIN_LAYER_INDEX = 2;
const DOCS_COMPONENT_HEADER_LAYER_INDEX = 4;
const DOCS_COMPONENT_DEFAULT_Z_INDEX = 10;
const NORMAL_TEXT_SELECTION_PLUGIN_NAME = "normalTextSelectionPluginName";

//#endregion
//#region src/basics/component-tools.ts
function neoGetDocObject(renderContext) {
	const { mainComponent, scene, engine, components } = renderContext;
	return {
		document: mainComponent,
		docBackground: components.get("__Document_Render_Background__"),
		scene,
		engine
	};
}
function getDocObjectById(unitId, renderManagerService) {
	const currentRender = renderManagerService.getRenderUnitById(unitId);
	if (currentRender == null) return;
	const { mainComponent, scene, engine, components } = currentRender;
	return {
		document: mainComponent,
		docBackground: components.get("__Document_Render_Background__"),
		scene,
		engine
	};
}

//#endregion
//#region src/basics/custom-decoration-factory.ts
function addCustomDecorationFactory(param) {
	const { unitId, ranges, id, type, segmentId } = param;
	const doMutation = {
		id: _univerjs_docs.RichTextEditingMutation.id,
		params: {
			unitId,
			actions: [],
			textRanges: void 0,
			segmentId
		}
	};
	const jsonX = _univerjs_core.JSONX.getInstance();
	const textX = _univerjs_core.BuildTextUtils.customDecoration.add({
		ranges,
		id,
		type
	});
	doMutation.params.actions = jsonX.editOp(textX.serialize());
	return doMutation;
}
function addCustomDecorationBySelectionFactory(accessor, param) {
	const { segmentId, id, type, unitId: propUnitId } = param;
	const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
	const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	const documentDataModel = propUnitId ? univerInstanceService.getUnit(propUnitId, _univerjs_core.UniverInstanceType.UNIVER_DOC) : univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
	if (!documentDataModel) return false;
	const unitId = documentDataModel.getUnitId();
	const selections = docSelectionManagerService.getTextRanges({
		unitId,
		subUnitId: unitId
	});
	if (!selections) return false;
	if (!documentDataModel.getBody()) return false;
	return addCustomDecorationFactory({
		unitId,
		ranges: selections,
		id,
		type,
		segmentId
	});
}
function deleteCustomDecorationFactory(accessor, params) {
	const { unitId, id, segmentId } = params;
	const documentDataModel = accessor.get(_univerjs_core.IUniverInstanceService).getUnit(unitId);
	if (!documentDataModel) return false;
	const doMutation = {
		id: _univerjs_docs.RichTextEditingMutation.id,
		params: {
			unitId,
			actions: [],
			textRanges: void 0,
			segmentId
		}
	};
	const textX = _univerjs_core.BuildTextUtils.customDecoration.delete({
		id,
		segmentId,
		documentDataModel
	});
	if (!textX) return false;
	const jsonX = _univerjs_core.JSONX.getInstance();
	doMutation.params.actions = jsonX.editOp(textX.serialize());
	return doMutation;
}

//#endregion
//#region src/basics/doc-custom-range-chrome.ts
const DOC_CUSTOM_RANGE_CHROME_RADIUS = 1;
const DOC_CUSTOM_RANGE_CHROME_BACKGROUND_ALPHA = .07;
function resolveDocCustomRangeChromeTheme(themeService) {
	const primary = themeService.getColorFromTheme("primary.600");
	return {
		background: new _univerjs_core.ColorKit(primary).setAlpha(DOC_CUSTOM_RANGE_CHROME_BACKGROUND_ALPHA).toRgbString(),
		border: primary
	};
}
function drawDocCustomRangeChrome(ctx, rect, theme) {
	ctx.save();
	ctx.fillStyle = theme.background;
	ctx.strokeStyle = theme.border;
	ctx.lineWidth = 1;
	ctx.beginPath();
	ctx.roundRect(rect.left, rect.top, rect.right - rect.left, rect.bottom - rect.top, 1);
	ctx.fill();
	ctx.stroke();
	ctx.restore();
}

//#endregion
//#region src/basics/paragraph.ts
function isTopLevelStructuralGap(dataStream, offset) {
	const previousToken = dataStream[offset - 1];
	const nextToken = dataStream[offset];
	return previousToken === _univerjs_core.DataStreamTreeTokenType.BLOCK_END || previousToken === _univerjs_core.DataStreamTreeTokenType.TABLE_END || previousToken === _univerjs_core.DataStreamTreeTokenType.COLUMN_GROUP_END || nextToken === _univerjs_core.DataStreamTreeTokenType.BLOCK_START || nextToken === _univerjs_core.DataStreamTreeTokenType.TABLE_START || nextToken === _univerjs_core.DataStreamTreeTokenType.COLUMN_GROUP_START;
}
function hasParagraphInTable(paragraph, tables) {
	return tables.some((table) => paragraph.startIndex > table.startIndex && paragraph.startIndex < table.endIndex);
}
function getTextRunAtPosition(body, position, defaultStyle, cacheStyle, isCellEditor) {
	const { textRuns = [], dataStream } = body;
	const isFormula = isCellEditor && dataStream.startsWith("=");
	const retTextRun = {
		st: 0,
		ed: 0,
		ts: !defaultStyle.cl ? {} : { cl: { ...defaultStyle.cl } }
	};
	if (isFormula) return retTextRun;
	for (let i = textRuns.length - 1; i >= 0; i--) {
		const textRun = textRuns[i];
		const { st, ed } = textRun;
		if (position > st && position <= ed) {
			retTextRun.st = st;
			retTextRun.ed = ed;
			retTextRun.ts = {
				...retTextRun.ts,
				...textRun.ts
			};
		}
	}
	if (position === 0) {
		const textRun = textRuns === null || textRuns === void 0 ? void 0 : textRuns[0];
		if (textRun && textRun.st === 0) retTextRun.ts = {
			...retTextRun.ts,
			...textRun.ts
		};
	}
	if (cacheStyle) retTextRun.ts = {
		...retTextRun.ts,
		...cacheStyle
	};
	return retTextRun;
}
function getCustomRangeAtPosition(customRanges, position, extendRange) {
	if (extendRange) {
		const range = customRanges.find((customRange) => position >= customRange.startIndex && position <= customRange.endIndex + 1);
		return (range === null || range === void 0 ? void 0 : range.wholeEntity) ? null : range;
	}
	const range = customRanges.find((customRange) => position > customRange.startIndex && position <= customRange.endIndex);
	return (range === null || range === void 0 ? void 0 : range.wholeEntity) ? null : range;
}
function getCustomDecorationAtPosition(customDecorations, position) {
	return customDecorations.filter((customDecoration) => position > customDecoration.startIndex && position <= customDecoration.endIndex);
}

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/typeof.js
function _typeof(o) {
	"@babel/helpers - typeof";
	return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o) {
		return typeof o;
	} : function(o) {
		return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
	}, _typeof(o);
}

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/toPrimitive.js
function toPrimitive(t, r) {
	if ("object" != _typeof(t) || !t) return t;
	var e = t[Symbol.toPrimitive];
	if (void 0 !== e) {
		var i = e.call(t, r || "default");
		if ("object" != _typeof(i)) return i;
		throw new TypeError("@@toPrimitive must return a primitive value.");
	}
	return ("string" === r ? String : Number)(t);
}

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/toPropertyKey.js
function toPropertyKey(t) {
	var i = toPrimitive(t, "string");
	return "symbol" == _typeof(i) ? i : i + "";
}

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/defineProperty.js
function _defineProperty(e, r, t) {
	return (r = toPropertyKey(r)) in e ? Object.defineProperty(e, r, {
		value: t,
		enumerable: !0,
		configurable: !0,
		writable: !0
	}) : e[r] = t, e;
}

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/decorateParam.js
function __decorateParam(paramIndex, decorator) {
	return function(target, key) {
		decorator(target, key, paramIndex);
	};
}

//#endregion
//#region \0@oxc-project+runtime@0.140.0/helpers/esm/decorate.js
function __decorate(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
}

//#endregion
//#region src/services/doc-auto-format.service.ts
let DocAutoFormatService = class DocAutoFormatService extends _univerjs_core.Disposable {
	constructor(_univerInstanceService, _textSelectionManagerService) {
		super();
		this._univerInstanceService = _univerInstanceService;
		this._textSelectionManagerService = _textSelectionManagerService;
		_defineProperty(this, "_matches", /* @__PURE__ */ new Map());
	}
	registerAutoFormat(match) {
		const matchList = this._matches.get(match.id);
		if (matchList) {
			matchList.push(match);
			matchList.sort((a, b) => (b.priority ?? 0) - (a.priority ?? 0));
		} else this._matches.set(match.id, [match]);
		return (0, _univerjs_core.toDisposable)(() => {
			const matchList = this._matches.get(match.id);
			if (matchList) {
				const index = matchList.findIndex((i) => i === match);
				if (index >= 0) matchList.splice(index, 1);
			}
		});
	}
	onAutoFormat(id, params) {
		const autoFormats = this._matches.get(id) ?? [];
		const unit = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const docRanges = this._textSelectionManagerService.getDocRanges();
		const selection = docRanges.find((range) => range.isActive) ?? docRanges[0];
		const documentDataModel = unit === null || unit === void 0 ? void 0 : unit.getSelfOrHeaderFooterModel((selection === null || selection === void 0 ? void 0 : selection.segmentId) ?? "");
		if (unit && selection && documentDataModel) {
			var _documentDataModel$ge, _documentDataModel$ge2, _documentDataModel$ge3;
			const context = {
				unit: documentDataModel,
				selection,
				isBody: !selection.segmentId,
				paragraphs: _univerjs_core.BuildTextUtils.range.getParagraphsInRange(selection, ((_documentDataModel$ge = documentDataModel.getBody()) === null || _documentDataModel$ge === void 0 ? void 0 : _documentDataModel$ge.paragraphs) ?? [], ((_documentDataModel$ge2 = documentDataModel.getBody()) === null || _documentDataModel$ge2 === void 0 ? void 0 : _documentDataModel$ge2.dataStream) ?? ""),
				customRanges: _univerjs_core.BuildTextUtils.customRange.getCustomRangesInterestsWithSelection(selection, ((_documentDataModel$ge3 = documentDataModel.getBody()) === null || _documentDataModel$ge3 === void 0 ? void 0 : _documentDataModel$ge3.customRanges) ?? []),
				commandId: id,
				commandParams: params
			};
			const matched = autoFormats.find((i) => i.match(context));
			return (matched === null || matched === void 0 ? void 0 : matched.getMutations(context)) ?? [];
		}
		return [];
	}
};
DocAutoFormatService = __decorate([__decorateParam(0, _univerjs_core.IUniverInstanceService), __decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService))], DocAutoFormatService);

//#endregion
//#region src/commands/commands/auto-format.command.ts
const TabCommandId = "doc.command.tab";
const TabCommand = {
	id: TabCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	async handler(accessor, params) {
		return (await (0, _univerjs_core.sequenceExecuteAsync)(accessor.get(DocAutoFormatService).onAutoFormat(TabCommand.id, params), accessor.get(_univerjs_core.ICommandService))).result;
	}
};
const AfterSpaceCommandId = "doc.command.after-space";
const AfterSpaceCommand = {
	id: AfterSpaceCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	async handler(accessor) {
		return (await (0, _univerjs_core.sequenceExecuteAsync)(accessor.get(DocAutoFormatService).onAutoFormat(AfterSpaceCommand.id), accessor.get(_univerjs_core.ICommandService))).result;
	}
};
const EnterCommand = {
	id: "doc.command.enter",
	type: _univerjs_core.CommandType.COMMAND,
	async handler(accessor) {
		return (await (0, _univerjs_core.sequenceExecuteAsync)(accessor.get(DocAutoFormatService).onAutoFormat(EnterCommand.id), accessor.get(_univerjs_core.ICommandService))).result;
	}
};

//#endregion
//#region src/services/doc-menu-style.service.ts
const BODY_DEFAULT_FONTSIZE = 11;
const HEADER_FOOTER_DEFAULT_FONTSIZE = 9;
const DEFAULT_TEXT_STYLE = {
	/**
	* fontFamily
	*/
	ff: "Arial",
	/**
	* fontSize
	*/
	fs: BODY_DEFAULT_FONTSIZE
};
let DocMenuStyleService = class DocMenuStyleService extends _univerjs_core.Disposable {
	constructor(_textSelectionManagerService, _univerInstanceService, _renderManagerService, _themeService) {
		super();
		this._textSelectionManagerService = _textSelectionManagerService;
		this._univerInstanceService = _univerInstanceService;
		this._renderManagerService = _renderManagerService;
		this._themeService = _themeService;
		_defineProperty(this, "_cacheStyle", null);
		this._init();
	}
	_init() {
		this._listenDocRangeChange();
	}
	_listenDocRangeChange() {
		this.disposeWithMe(this._textSelectionManagerService.textSelection$.subscribe(() => {
			this._clearStyleCache();
		}));
	}
	getStyleCache() {
		return this._cacheStyle;
	}
	getDefaultStyle() {
		var _this$_renderManagerS;
		const defaultTextStyle = {
			...DEFAULT_TEXT_STYLE,
			cl: { rgb: this._themeService.getColorFromTheme("gray.900") }
		};
		const docDataModel = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null) return { ...defaultTextStyle };
		const documentDefaultTextStyle = {
			...defaultTextStyle,
			...docDataModel.getDocumentStyle().textStyle
		};
		const unitId = docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getUnitId();
		const docSkeletonManagerService = (_this$_renderManagerS = this._renderManagerService.getRenderUnitById(unitId)) === null || _this$_renderManagerS === void 0 ? void 0 : _this$_renderManagerS.with(_univerjs_docs.DocSkeletonManagerService);
		const docViewModel = docSkeletonManagerService === null || docSkeletonManagerService === void 0 ? void 0 : docSkeletonManagerService.getViewModel();
		if (docViewModel == null) return { ...documentDefaultTextStyle };
		if (docViewModel.getEditArea() === _univerjs_engine_render.DocumentEditArea.BODY) return { ...documentDefaultTextStyle };
		else return {
			...documentDefaultTextStyle,
			fs: HEADER_FOOTER_DEFAULT_FONTSIZE
		};
	}
	setStyleCache(style) {
		this._cacheStyle = {
			...this._cacheStyle,
			...style
		};
	}
	_clearStyleCache() {
		this._cacheStyle = null;
	}
};
DocMenuStyleService = __decorate([
	__decorateParam(0, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(1, _univerjs_core.IUniverInstanceService),
	__decorateParam(2, _univerjs_engine_render.IRenderManagerService),
	__decorateParam(3, (0, _univerjs_core.Inject)(_univerjs_core.ThemeService))
], DocMenuStyleService);
const SetDocInputStyleCommand = {
	id: "doc.command.set-input-style",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		if (!params) return false;
		accessor.get(DocMenuStyleService).setStyleCache(params.style);
		return true;
	}
};

//#endregion
//#region src/commands/commands/break-line.command.ts
const BreakLineCommand = {
	id: "doc.command.break-line",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		var _docDataModel$getSelf, _prevParagraph$bullet, _prevParagraph$paragr;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const docMenuStyleService = accessor.get(DocMenuStyleService);
		const activeTextRange = (params === null || params === void 0 ? void 0 : params.textRange) ?? docSelectionManagerService.getActiveTextRange();
		const rectRanges = docSelectionManagerService.getRectRanges();
		if (activeTextRange == null) return false;
		if (rectRanges && rectRanges.length) {
			const { startOffset } = activeTextRange;
			docSelectionManagerService.replaceDocRanges([{
				startOffset,
				endOffset: startOffset
			}]);
			return true;
		}
		const { horizontalLine, insertionMode = "split-paragraph" } = params ?? {};
		const { segmentId } = activeTextRange;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const originBody = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId ?? "")) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		if (docDataModel == null || originBody == null) return false;
		const unitId = docDataModel.getUnitId();
		const { startOffset, endOffset } = activeTextRange;
		const paragraphs = originBody.paragraphs ?? [];
		const isBareStructuralGap = isTopLevelStructuralGap(originBody.dataStream, startOffset) && originBody.dataStream[startOffset] !== _univerjs_core.DataStreamTreeTokenType.PARAGRAPH;
		const insertsAtStructuralGap = insertionMode === "insert-gap" || isBareStructuralGap;
		const prevParagraph = insertsAtStructuralGap ? void 0 : paragraphs.find((p) => p.startIndex >= startOffset);
		if (!prevParagraph && !insertsAtStructuralGap) return false;
		const isAtParagraphEnd = startOffset === (prevParagraph === null || prevParagraph === void 0 ? void 0 : prevParagraph.startIndex);
		const prevParagraphIndex = prevParagraph === null || prevParagraph === void 0 ? void 0 : prevParagraph.startIndex;
		const curTextRun = getTextRunAtPosition(originBody, endOffset, docMenuStyleService.getDefaultStyle(), docMenuStyleService.getStyleCache());
		const insertBody = {
			dataStream: _univerjs_core.DataStreamTreeTokenType.PARAGRAPH,
			paragraphs: (0, _univerjs_docs.generateParagraphs)(_univerjs_core.DataStreamTreeTokenType.PARAGRAPH, prevParagraph, horizontalLine),
			textRuns: [{
				st: 0,
				ed: 1,
				ts: { ...curTextRun.ts }
			}]
		};
		if (docDataModel == null) return false;
		const activeRange = docSelectionManagerService.getActiveTextRange();
		if (originBody == null) return false;
		const { collapsed } = activeTextRange;
		const cursorMove = insertsAtStructuralGap ? 0 : insertBody.dataStream.length;
		const textRanges = [{
			startOffset: startOffset + cursorMove,
			endOffset: startOffset + cursorMove,
			style: activeRange === null || activeRange === void 0 ? void 0 : activeRange.style,
			collapsed
		}];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges,
				debounce: true,
				trigger: BreakLineCommand.id
			}
		};
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		if (collapsed) {
			if (startOffset > 0) textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len: startOffset
			});
			textX.push({
				t: _univerjs_core.TextXActionType.INSERT,
				body: insertBody,
				len: insertBody.dataStream.length
			});
		} else {
			const dos = _univerjs_core.BuildTextUtils.selection.delete([activeTextRange], originBody, 0, insertBody);
			textX.push(...dos);
		}
		const preserveEmptyParagraphStyle = collapsed && isAtParagraphEnd;
		const resetParagraphType = (prevParagraph === null || prevParagraph === void 0 || (_prevParagraph$bullet = prevParagraph.bullet) === null || _prevParagraph$bullet === void 0 ? void 0 : _prevParagraph$bullet.listType) === _univerjs_core.PresetListType.CHECK_LIST_CHECKED || (prevParagraph === null || prevParagraph === void 0 || (_prevParagraph$paragr = prevParagraph.paragraphStyle) === null || _prevParagraph$paragr === void 0 ? void 0 : _prevParagraph$paragr.headingId);
		if (prevParagraph && (preserveEmptyParagraphStyle || resetParagraphType)) {
			var _prevParagraph$paragr2;
			if (prevParagraphIndex != null && activeTextRange.endOffset < prevParagraphIndex) textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len: prevParagraphIndex - activeTextRange.endOffset
			});
			textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len: 1,
				body: {
					dataStream: "",
					...preserveEmptyParagraphStyle ? { textRuns: [{
						st: 0,
						ed: 1,
						ts: { ...curTextRun.ts }
					}] } : null,
					...resetParagraphType ? { paragraphs: [{
						...prevParagraph,
						paragraphStyle: {
							...prevParagraph.paragraphStyle,
							...isAtParagraphEnd ? {
								headingId: void 0,
								namedStyleType: void 0
							} : null
						},
						startIndex: 0,
						bullet: ((_prevParagraph$paragr2 = prevParagraph.paragraphStyle) === null || _prevParagraph$paragr2 === void 0 ? void 0 : _prevParagraph$paragr2.headingId) ? void 0 : {
							...prevParagraph.bullet,
							listType: _univerjs_core.PresetListType.CHECK_LIST
						}
					}] } : null
				},
				coverType: _univerjs_core.UpdateDocsAttributeType.REPLACE
			});
		}
		doMutation.params.textRanges = [{
			startOffset: startOffset + cursorMove,
			endOffset: startOffset + cursorMove,
			collapsed
		}];
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};

//#endregion
//#region src/services/clipboard/doc-paste-mutation-adapter.service.ts
const IDocClipboardPasteAdapterService = (0, _univerjs_core.createIdentifier)("doc.clipboard-paste-adapter.service");
var DocClipboardPasteAdapterService = class {
	constructor() {
		_defineProperty(this, "_adapters", []);
	}
	registerAdapter(adapter) {
		this._adapters.push(adapter);
		return (0, _univerjs_core.toDisposable)(() => {
			const index = this._adapters.indexOf(adapter);
			if (index >= 0) this._adapters.splice(index, 1);
		});
	}
	getPasteMutationInfos(params) {
		const mutationInfos = {
			redoMutations: [],
			undoMutations: []
		};
		for (const adapter of this._adapters) {
			var _adapter$getPasteMuta;
			const nextMutationInfos = (_adapter$getPasteMuta = adapter.getPasteMutationInfos) === null || _adapter$getPasteMuta === void 0 ? void 0 : _adapter$getPasteMuta.call(adapter, params);
			if (!nextMutationInfos) continue;
			mutationInfos.redoMutations.push(...nextMutationInfos.redoMutations);
			mutationInfos.undoMutations.push(...nextMutationInfos.undoMutations);
		}
		return mutationInfos;
	}
};

//#endregion
//#region src/commands/util.ts
/**
* Get the skeleton of the command's target.
* @param accessor The injection accessor.
* @param unitId Unit ID.
*/
function getCommandSkeleton(accessor, unitId) {
	var _renderManagerService;
	return (_renderManagerService = accessor.get(_univerjs_engine_render.IRenderManagerService).getRenderUnitById(unitId)) === null || _renderManagerService === void 0 ? void 0 : _renderManagerService.with(_univerjs_docs.DocSkeletonManagerService);
}

//#endregion
//#region src/commands/commands/table/table.ts
function genEmptyTable(rowCount, colCount) {
	let dataStream = _univerjs_core.DataStreamTreeTokenType.TABLE_START;
	const paragraphs = [];
	const sectionBreaks = [];
	const existingParagraphIds = /* @__PURE__ */ new Set();
	const existingSectionIds = /* @__PURE__ */ new Set();
	for (let i = 0; i < rowCount; i++) {
		dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_ROW_START;
		for (let j = 0; j < colCount; j++) {
			dataStream += `${_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_START}\r\n${_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_END}`;
			paragraphs.push({
				startIndex: dataStream.length - 3,
				paragraphId: (0, _univerjs_core.createParagraphId)(existingParagraphIds),
				paragraphStyle: {
					spaceAbove: { v: 3 },
					lineSpacing: 2,
					spaceBelow: { v: 0 }
				}
			});
			sectionBreaks.push({
				sectionId: (0, _univerjs_core.createSectionId)(existingSectionIds),
				startIndex: dataStream.length - 2
			});
		}
		dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_ROW_END;
	}
	dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_END;
	return {
		dataStream,
		paragraphs,
		sectionBreaks
	};
}
function getEmptyTableCell() {
	return { margin: {
		start: { v: 10 },
		end: { v: 10 },
		top: { v: 5 },
		bottom: { v: 5 }
	} };
}
function getEmptyTableRow(col) {
	const tableCell = getEmptyTableCell();
	return {
		tableCells: [...new Array(col).fill(null).map(() => _univerjs_core.Tools.deepClone(tableCell))],
		trHeight: {
			val: { v: 30 },
			hRule: _univerjs_core.TableRowHeightRule.AUTO
		}
	};
}
function getTableColumn(width) {
	return { size: {
		type: _univerjs_core.TableSizeType.SPECIFIED,
		width: { v: width }
	} };
}
function genTableSource(rowCount, colCount, pageContentWidth) {
	const tableColumn = getTableColumn(pageContentWidth / colCount);
	const tableRow = getEmptyTableRow(colCount);
	return {
		tableRows: [...new Array(rowCount).fill(null).map(() => _univerjs_core.Tools.deepClone(tableRow))],
		tableColumns: [...new Array(colCount).fill(null).map(() => _univerjs_core.Tools.deepClone(tableColumn))],
		tableId: (0, _univerjs_core.generateRandomId)(6),
		align: _univerjs_core.TableAlignmentType.START,
		indent: { v: 0 },
		textWrap: _univerjs_core.TableTextWrapType.NONE,
		position: {
			positionH: {
				relativeFrom: _univerjs_core.ObjectRelativeFromH.PAGE,
				posOffset: 0
			},
			positionV: {
				relativeFrom: _univerjs_core.ObjectRelativeFromV.PAGE,
				posOffset: 0
			}
		},
		dist: {
			distB: 0,
			distL: 0,
			distR: 0,
			distT: 0
		},
		cellMargin: {
			start: { v: 10 },
			end: { v: 10 },
			top: { v: 5 },
			bottom: { v: 5 }
		},
		size: {
			type: _univerjs_core.TableSizeType.UNSPECIFIED,
			width: { v: pageContentWidth }
		}
	};
}
function getRangeInfoFromRanges(textRange, rectRanges) {
	if (!textRange && !rectRanges) return null;
	if (rectRanges && rectRanges.length > 0) {
		let startOffset = Number.POSITIVE_INFINITY;
		let endOffset = Number.NEGATIVE_INFINITY;
		const segmentId = "";
		for (const rectRange of rectRanges) {
			const { startOffset: st, endOffset: ed, segmentId: sid } = rectRange;
			if (st == null || ed == null || sid == null) continue;
			startOffset = Math.min(startOffset, st);
			endOffset = Math.max(endOffset, ed);
		}
		if (Number.isFinite(startOffset) && Number.isFinite(endOffset)) return {
			startOffset,
			endOffset,
			segmentId
		};
	} else if (textRange) {
		const { startOffset, endOffset, segmentId } = textRange;
		if (startOffset == null || endOffset == null || segmentId == null) return null;
		return {
			startOffset,
			endOffset,
			segmentId
		};
	}
}
function getInsertRowBody(col) {
	let dataStream = _univerjs_core.DataStreamTreeTokenType.TABLE_ROW_START;
	const paragraphs = [];
	const sectionBreaks = [];
	const existingParagraphIds = /* @__PURE__ */ new Set();
	const existingSectionIds = /* @__PURE__ */ new Set();
	for (let i = 0; i < col; i++) {
		dataStream += `${_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_START}\r\n${_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_END}`;
		paragraphs.push({
			startIndex: dataStream.length - 3,
			paragraphId: (0, _univerjs_core.createParagraphId)(existingParagraphIds),
			paragraphStyle: {
				spaceAbove: { v: 3 },
				lineSpacing: 2,
				spaceBelow: { v: 0 }
			}
		});
		sectionBreaks.push({
			sectionId: (0, _univerjs_core.createSectionId)(existingSectionIds),
			startIndex: dataStream.length - 2
		});
	}
	dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_ROW_END;
	return {
		dataStream,
		paragraphs,
		sectionBreaks
	};
}
function getInsertColumnBody() {
	const dataStream = `${_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_START}\r\n${_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_END}`;
	const paragraphs = [];
	const sectionBreaks = [];
	paragraphs.push({
		startIndex: 1,
		paragraphId: (0, _univerjs_core.createParagraphId)(/* @__PURE__ */ new Set()),
		paragraphStyle: {
			spaceAbove: { v: 3 },
			lineSpacing: 2,
			spaceBelow: { v: 0 }
		}
	});
	sectionBreaks.push({
		sectionId: (0, _univerjs_core.createSectionId)(/* @__PURE__ */ new Set()),
		startIndex: 2
	});
	return {
		dataStream,
		paragraphs,
		sectionBreaks
	};
}
function getInsertRowActionsParams(rangeInfo, position, viewModel) {
	var _viewModel$getBody;
	const { startOffset, endOffset, segmentId } = rangeInfo;
	const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
	const index = position === 0 ? startOffset : endOffset;
	let tableRow = null;
	const tableId = (_viewModel$getBody = viewModel.getBody()) === null || _viewModel$getBody === void 0 || (_viewModel$getBody = _viewModel$getBody.tables) === null || _viewModel$getBody === void 0 || (_viewModel$getBody = _viewModel$getBody.find((t) => index >= t.startIndex && index <= t.endIndex)) === null || _viewModel$getBody === void 0 ? void 0 : _viewModel$getBody.tableId;
	let rowIndex = 0;
	for (const section of vm.getChildren()) {
		for (const paragraph of section.children) {
			const { children } = paragraph;
			const table = children[0];
			if (table) {
				for (const row of table.children) if (row.startIndex <= index && index <= row.endIndex) {
					rowIndex = table.children.indexOf(row);
					tableRow = row;
					break;
				}
			}
			if (tableRow) break;
		}
		if (tableRow) break;
	}
	if (tableRow == null || tableId == null) return null;
	return {
		offset: position === 0 ? tableRow.startIndex : tableRow.endIndex + 1,
		colCount: tableRow.children.length,
		tableId,
		insertRowIndex: position === 0 ? rowIndex : rowIndex + 1
	};
}
function getInsertColumnActionsParams(rangeInfo, position, viewModel) {
	var _viewModel$getBody2;
	const { startOffset, endOffset, segmentId } = rangeInfo;
	const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
	const index = position === 0 ? startOffset : endOffset;
	const tableId = (_viewModel$getBody2 = viewModel.getBody()) === null || _viewModel$getBody2 === void 0 || (_viewModel$getBody2 = _viewModel$getBody2.tables) === null || _viewModel$getBody2 === void 0 || (_viewModel$getBody2 = _viewModel$getBody2.find((t) => index >= t.startIndex && index <= t.endIndex)) === null || _viewModel$getBody2 === void 0 ? void 0 : _viewModel$getBody2.tableId;
	const offsets = [];
	let table = null;
	let columnIndex = -1;
	for (const section of vm.getChildren()) {
		for (const paragraph of section.children) {
			const { children } = paragraph;
			const tableNode = children[0];
			if (tableNode) {
				if (index < tableNode.startIndex || index > tableNode.endIndex) continue;
				table = tableNode;
				for (const row of tableNode.children) {
					for (const cell of row.children) {
						const cellIndex = row.children.indexOf(cell);
						if (index >= cell.startIndex && index <= cell.endIndex) {
							columnIndex = cellIndex;
							break;
						}
					}
					if (columnIndex !== -1) break;
				}
			}
			if (table) break;
		}
		if (table) break;
	}
	if (table == null || tableId == null || columnIndex === -1) return null;
	let cursor = 0;
	for (const row of table.children) {
		const cell = row.children[columnIndex];
		const insertIndex = position === 0 ? cell.startIndex : cell.endIndex + 1;
		offsets.push(insertIndex - cursor);
		cursor = insertIndex;
	}
	return {
		offsets,
		tableId,
		columnIndex,
		rowCount: table.children.length
	};
}
function getColumnWidths(pageWidth, tableColumns, insertColumnIndex) {
	const widths = [];
	let newColWidth = tableColumns[insertColumnIndex].size.width.v;
	let totalWidth = 0;
	for (let i = 0; i < tableColumns.length; i++) totalWidth += tableColumns[i].size.width.v;
	totalWidth += newColWidth;
	for (let i = 0; i < tableColumns.length; i++) widths.push(tableColumns[i].size.width.v / totalWidth * pageWidth);
	newColWidth = newColWidth / totalWidth * pageWidth;
	return {
		widths,
		newColWidth
	};
}
function getDeleteRowsActionsParams(rangeInfo, viewModel) {
	var _viewModel$getBody3;
	const { startOffset, endOffset, segmentId } = rangeInfo;
	const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
	const tableId = (_viewModel$getBody3 = viewModel.getBody()) === null || _viewModel$getBody3 === void 0 || (_viewModel$getBody3 = _viewModel$getBody3.tables) === null || _viewModel$getBody3 === void 0 || (_viewModel$getBody3 = _viewModel$getBody3.find((t) => startOffset >= t.startIndex && endOffset <= t.endIndex)) === null || _viewModel$getBody3 === void 0 ? void 0 : _viewModel$getBody3.tableId;
	const rowIndexes = [];
	let offset = -1;
	let len = 0;
	let cursor = -1;
	let selectWholeTable = false;
	for (const section of vm.getChildren()) {
		for (const paragraph of section.children) {
			const { children } = paragraph;
			const table = children[0];
			if (table) {
				if (startOffset < table.startIndex || endOffset > table.endIndex) continue;
				cursor = table.startIndex + 3;
				for (const row of table.children) {
					const rowIndex = table.children.indexOf(row);
					const { startIndex, endIndex } = row;
					if (startOffset >= startIndex && startOffset <= endIndex) {
						offset = startIndex;
						rowIndexes.push(rowIndex);
						len += endIndex - startIndex + 1;
					} else if (startIndex > startOffset && endIndex < endOffset) {
						rowIndexes.push(rowIndex);
						len += endIndex - startIndex + 1;
					} else if (endOffset >= startIndex && endOffset <= endIndex) {
						rowIndexes.push(rowIndex);
						len += endIndex - startIndex + 1;
					}
					if (rowIndexes.length === table.children.length) selectWholeTable = true;
				}
			}
			if (rowIndexes.length) break;
		}
		if (rowIndexes.length) break;
	}
	if (tableId == null || rowIndexes.length === 0) return null;
	return {
		tableId,
		rowIndexes,
		offset,
		len,
		cursor,
		selectWholeTable
	};
}
function getDeleteColumnsActionParams(rangeInfo, viewModel) {
	var _viewModel$getBody4;
	const { startOffset, endOffset, segmentId } = rangeInfo;
	const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
	const tableId = (_viewModel$getBody4 = viewModel.getBody()) === null || _viewModel$getBody4 === void 0 || (_viewModel$getBody4 = _viewModel$getBody4.tables) === null || _viewModel$getBody4 === void 0 || (_viewModel$getBody4 = _viewModel$getBody4.find((t) => startOffset >= t.startIndex && endOffset <= t.endIndex)) === null || _viewModel$getBody4 === void 0 ? void 0 : _viewModel$getBody4.tableId;
	const offsets = [];
	let table = null;
	const columnIndexes = [];
	let cursor = -1;
	let startColumnIndex = -1;
	let endColumnIndex = -1;
	for (const section of vm.getChildren()) {
		for (const paragraph of section.children) {
			const { children } = paragraph;
			const tableNode = children[0];
			if (tableNode) {
				if (startOffset < tableNode.startIndex || endOffset > tableNode.endIndex) continue;
				table = tableNode;
				for (const row of tableNode.children) for (const cell of row.children) {
					const cellIndex = row.children.indexOf(cell);
					if (startOffset >= cell.startIndex && startOffset <= cell.endIndex) startColumnIndex = cellIndex;
					if (endOffset >= cell.startIndex && endOffset <= cell.endIndex) endColumnIndex = cellIndex;
				}
			}
			if (table) break;
		}
		if (table) break;
	}
	if (table == null || tableId == null) return null;
	for (let i = startColumnIndex; i <= endColumnIndex; i++) columnIndexes.push(i);
	let delta = 0;
	for (const row of table.children) {
		const startCell = row.children[startColumnIndex];
		const endCell = row.children[endColumnIndex];
		offsets.push({
			retain: startCell.startIndex - delta,
			delete: endCell.endIndex - startCell.startIndex + 1
		});
		delta = endCell.endIndex + 1;
	}
	cursor = table.startIndex + 3;
	return {
		offsets,
		tableId,
		columnIndexes,
		cursor,
		selectWholeTable: columnIndexes.length === table.children[0].children.length,
		rowCount: table.children.length
	};
}
function getDeleteTableActionParams(rangeInfo, viewModel) {
	var _viewModel$getBody5;
	const { startOffset, endOffset, segmentId } = rangeInfo;
	const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
	const tableId = (_viewModel$getBody5 = viewModel.getBody()) === null || _viewModel$getBody5 === void 0 || (_viewModel$getBody5 = _viewModel$getBody5.tables) === null || _viewModel$getBody5 === void 0 || (_viewModel$getBody5 = _viewModel$getBody5.find((t) => startOffset >= t.startIndex && endOffset <= t.endIndex)) === null || _viewModel$getBody5 === void 0 ? void 0 : _viewModel$getBody5.tableId;
	let offset = -1;
	let len = 0;
	let cursor = -1;
	for (const section of vm.getChildren()) {
		for (const paragraph of section.children) {
			const { children } = paragraph;
			const table = children[0];
			if (table) {
				if (startOffset < table.startIndex || endOffset > table.endIndex) continue;
				offset = table.startIndex;
				len = table.endIndex - table.startIndex + 1;
				cursor = table.startIndex;
			}
			if (table) break;
		}
		if (len > 0) break;
	}
	if (tableId == null) return null;
	return {
		tableId,
		offset,
		len,
		cursor
	};
}
function getDeleteRowContentActionParams(rangeInfo, viewModel) {
	var _viewModel$getBody6;
	const { startOffset, endOffset, segmentId } = rangeInfo;
	const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
	const tableId = (_viewModel$getBody6 = viewModel.getBody()) === null || _viewModel$getBody6 === void 0 || (_viewModel$getBody6 = _viewModel$getBody6.tables) === null || _viewModel$getBody6 === void 0 || (_viewModel$getBody6 = _viewModel$getBody6.find((t) => startOffset >= t.startIndex && endOffset <= t.endIndex)) === null || _viewModel$getBody6 === void 0 ? void 0 : _viewModel$getBody6.tableId;
	const offsets = [];
	let table = null;
	let cursor = -1;
	let rowIndex = -1;
	let startColumnIndex = -1;
	let endColumnIndex = -1;
	for (const section of vm.getChildren()) {
		for (const paragraph of section.children) {
			const { children } = paragraph;
			const tableNode = children[0];
			if (tableNode) {
				if (startOffset < tableNode.startIndex || endOffset > tableNode.endIndex) continue;
				table = tableNode;
				for (const row of tableNode.children) {
					const rIndex = tableNode.children.indexOf(row);
					for (const cell of row.children) {
						const cellIndex = row.children.indexOf(cell);
						if (startOffset >= cell.startIndex && startOffset <= cell.endIndex) {
							rowIndex = rIndex;
							startColumnIndex = cellIndex;
						}
						if (endOffset >= cell.startIndex && endOffset <= cell.endIndex) endColumnIndex = cellIndex;
					}
				}
			}
			if (table) break;
		}
		if (table) break;
	}
	if (table == null || tableId == null || rowIndex === -1) return null;
	const row = table.children[rowIndex];
	for (let i = startColumnIndex; i <= endColumnIndex; i++) {
		const cell = row.children[i];
		offsets.push({
			retain: cell.startIndex + 1,
			delete: cell.endIndex - cell.startIndex - 3
		});
	}
	cursor = table.startIndex + 3;
	return {
		offsets,
		tableId,
		cursor,
		rowCount: table.children.length
	};
}
function getCellOffsets(viewModel, range, position) {
	const { startOffset } = range;
	let targetTable = null;
	for (const section of viewModel.getChildren()) {
		for (const paragraph of section.children) {
			const table = paragraph.children[0];
			if (table) {
				if (startOffset > table.startIndex && startOffset < table.endIndex) {
					targetTable = table;
					break;
				}
			}
		}
		if (targetTable) break;
	}
	if (targetTable == null) return null;
	let cellIndex = -1;
	let rowIndex = -1;
	let targetRow = null;
	for (const row of targetTable.children) {
		for (const cell of row.children) if (startOffset > cell.startIndex && startOffset < cell.endIndex) {
			cellIndex = row.children.indexOf(cell);
			rowIndex = targetTable.children.indexOf(row);
			targetRow = row;
			break;
		}
		if (cellIndex > -1) break;
	}
	if (cellIndex === -1 || rowIndex === -1 || targetRow == null) return null;
	let newCell = null;
	if (position === 0) {
		newCell = targetRow.children[cellIndex + 1];
		if (!newCell) {
			const nextRow = targetTable.children[rowIndex + 1];
			if (nextRow) newCell = nextRow.children[0];
		}
	} else {
		newCell = targetRow.children[cellIndex - 1];
		if (!newCell) {
			const prevRow = targetTable.children[rowIndex - 1];
			if (prevRow) newCell = prevRow.children[prevRow.children.length - 1];
		}
	}
	if (newCell) {
		const { startIndex, endIndex } = newCell;
		return {
			startOffset: startIndex + 1,
			endOffset: endIndex - 2
		};
	}
}

//#endregion
//#region src/commands/commands/clipboard.inner.command.ts
function hasRangeInTable(ranges) {
	return ranges.some((range) => {
		const { startNodePosition } = range;
		return startNodePosition ? (startNodePosition === null || startNodePosition === void 0 ? void 0 : startNodePosition.path.indexOf("cells")) > -1 : false;
	});
}
const UNITS = _univerjs_core.SHEET_EDITOR_UNITS;
const InnerPasteCommand = {
	id: "doc.command.inner-paste",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		var _docDataModel$getSelf, _body$tables, _body$customBlocks, _body$blockRanges, _selectionInfo$option;
		const { customRangeMappings = [], segmentId, textRanges, doc } = params;
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const undoRedoService = accessor.get(_univerjs_core.IUndoRedoService);
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const pasteAdapterService = getPasteAdapterService(accessor);
		const selections = docSelectionManagerService.getTextRanges() ?? [];
		const rectRanges = docSelectionManagerService.getRectRanges() ?? [];
		const selectionInfo = docSelectionManagerService.getSelectionInfo();
		const { body, tableSource, drawings } = doc;
		if (selections.length === 0 && rectRanges.length === 0 || body == null) return false;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const originBody = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		if (docDataModel == null || originBody == null) return false;
		const unitId = docDataModel.getUnitId();
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges,
				segmentId,
				trigger: InnerPasteCommand.id
			}
		};
		const memoryCursor = new _univerjs_core.MemoryCursor();
		memoryCursor.reset();
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const rawActions = [];
		const resourceRedoMutations = [];
		const resourceUndoMutations = [];
		const resourceMutationGroups = [];
		const hasTable = !!((_body$tables = body.tables) === null || _body$tables === void 0 ? void 0 : _body$tables.length);
		const hasCustomBlock = !!((_body$customBlocks = body.customBlocks) === null || _body$customBlocks === void 0 ? void 0 : _body$customBlocks.length);
		const hasBlockRange = !!((_body$blockRanges = body.blockRanges) === null || _body$blockRanges === void 0 ? void 0 : _body$blockRanges.length);
		if (hasTable && segmentId) return false;
		if (hasTable && (hasRangeInTable(selections) || rectRanges.some((range) => !range.spanEntireTable))) return false;
		const replacesComplexSelection = rectRanges.length > 0 || (selectionInfo === null || selectionInfo === void 0 || (_selectionInfo$option = selectionInfo.options) === null || _selectionInfo$option === void 0 ? void 0 : _selectionInfo$option.wholeDocument) === true;
		let selectionCutActions = [];
		let pasteSelections = selections;
		if (replacesComplexSelection) {
			var _selectionInfo$option2, _selections$find;
			const docSkeletonManagerService = getCommandSkeleton(accessor, unitId);
			if (!docSkeletonManagerService) return false;
			const wholeBodySelected = (selectionInfo === null || selectionInfo === void 0 || (_selectionInfo$option2 = selectionInfo.options) === null || _selectionInfo$option2 === void 0 ? void 0 : _selectionInfo$option2.wholeDocument) === true || isWholeBodySelected(selections, rectRanges, originBody);
			const insertOffset = wholeBodySelected ? 0 : getDocRangeInsertOffset(selections, rectRanges);
			if (insertOffset == null) return false;
			selectionCutActions = getCutActionsFromDocRanges(selections, rectRanges, docDataModel, docSkeletonManagerService.getViewModel(), segmentId, wholeBodySelected);
			pasteSelections = [{
				startOffset: insertOffset,
				endOffset: insertOffset,
				collapsed: true,
				segmentId
			}];
			doMutation.params.textRanges = [{
				startOffset: insertOffset + body.dataStream.length,
				endOffset: insertOffset + body.dataStream.length,
				collapsed: true,
				segmentId,
				style: (_selections$find = selections.find((range) => range.isActive)) === null || _selections$find === void 0 ? void 0 : _selections$find.style
			}];
		}
		for (let i = 0; i < pasteSelections.length; i++) {
			const selection = pasteSelections[i];
			const { startOffset, endOffset, collapsed } = selection;
			const len = startOffset - memoryCursor.cursor;
			const cloneBody = _univerjs_core.Tools.deepClone(body);
			const blockRangeMappings = [];
			const customBlockMappings = [];
			const selectionCustomRangeMappings = customRangeMappings.map(({ sourceRange }) => ({
				sourceRange,
				targetRange: _univerjs_core.BuildTextUtils.customRange.copyCustomRange(sourceRange)
			}));
			if (selectionCustomRangeMappings.length > 0) cloneBody.customRanges = selectionCustomRangeMappings.map(({ targetRange }) => targetRange);
			if (hasBlockRange) cloneBody.blockRanges.forEach((targetBlockRange, index) => {
				const sourceBlockRange = body.blockRanges[index];
				targetBlockRange.blockId = (0, _univerjs_core.generateRandomId)(6);
				blockRangeMappings.push({
					sourceBlockRange,
					targetBlockRange
				});
			});
			if (hasTable) for (const t of cloneBody.tables) {
				const { tableId: oldTableId } = t;
				const tableId = (0, _univerjs_core.generateRandomId)(6);
				t.tableId = tableId;
				const table = _univerjs_core.Tools.deepClone(tableSource[oldTableId]);
				table.tableId = tableId;
				const action = jsonX.insertOp(["tableSource", tableId], table);
				rawActions.push(action);
			}
			if (hasCustomBlock && drawings) {
				var _docDataModel$getSnap;
				const drawingLen = ((_docDataModel$getSnap = docDataModel.getSnapshot().drawingsOrder) === null || _docDataModel$getSnap === void 0 ? void 0 : _docDataModel$getSnap.length) ?? 0;
				for (const block of cloneBody.customBlocks) {
					const { blockId } = block;
					const drawingId = (0, _univerjs_core.generateRandomId)(6);
					block.blockId = drawingId;
					const sourceDrawing = drawings[blockId];
					const drawing = _univerjs_core.Tools.deepClone(sourceDrawing);
					drawing.drawingId = drawingId;
					customBlockMappings.push({
						sourceBlockId: blockId,
						targetBlockId: drawingId,
						sourceDrawing,
						targetDrawing: drawing
					});
				}
				customBlockMappings.forEach(({ targetBlockId, targetDrawing }) => {
					const action = jsonX.insertOp(["drawings", targetBlockId], targetDrawing);
					const orderAction = jsonX.insertOp(["drawingsOrder", drawingLen], targetBlockId);
					rawActions.push(action);
					rawActions.push(orderAction);
				});
			}
			if ((blockRangeMappings.length > 0 || customBlockMappings.length > 0 || selectionCustomRangeMappings.length > 0) && pasteAdapterService) {
				const mutationInfos = pasteAdapterService.getPasteMutationInfos({
					unitId,
					segmentId,
					doc,
					sourceBody: body,
					targetBody: cloneBody,
					blockRangeMappings,
					customRangeMappings: selectionCustomRangeMappings,
					customBlockMappings
				});
				if (mutationInfos.redoMutations.length > 0 || mutationInfos.undoMutations.length > 0) {
					resourceRedoMutations.push(...mutationInfos.redoMutations);
					resourceUndoMutations.push(...mutationInfos.undoMutations);
					resourceMutationGroups.push(mutationInfos);
				}
			}
			const customRange = getCustomRangeAtPosition(originBody.customRanges ?? [], endOffset, UNITS.includes(unitId));
			const customDecorations = getCustomDecorationAtPosition(originBody.customDecorations ?? [], endOffset);
			if (customRange) cloneBody.customRanges = [{
				...customRange,
				startIndex: 0,
				endIndex: body.dataStream.length - 1
			}];
			if (customDecorations.length) cloneBody.customDecorations = customDecorations.map((customDecoration) => ({
				...customDecoration,
				startIndex: 0,
				endIndex: body.dataStream.length - 1
			}));
			if (collapsed) {
				textX.push({
					t: _univerjs_core.TextXActionType.RETAIN,
					len
				});
				textX.push({
					t: _univerjs_core.TextXActionType.INSERT,
					body: cloneBody,
					len: body.dataStream.length
				});
			} else {
				const dos = _univerjs_core.BuildTextUtils.selection.delete([selection], body, memoryCursor.cursor, cloneBody, pasteSelections.length === 1);
				textX.push(...dos);
			}
			memoryCursor.reset();
			memoryCursor.moveCursor(endOffset);
		}
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		rawActions.push(jsonX.editOp(textX.serialize(), path));
		const pasteActions = rawActions.reduce((acc, cur) => {
			return _univerjs_core.JSONX.compose(acc, cur);
		}, null);
		doMutation.params.actions = selectionCutActions && selectionCutActions.length > 0 ? _univerjs_core.JSONX.compose(selectionCutActions, pasteActions) : pasteActions;
		if (!executeResourceMutationGroups(resourceMutationGroups, commandService)) return false;
		const historyId = `doc-paste-resource:${unitId}:${Date.now()}`;
		let batchingDisposable = null;
		if (resourceRedoMutations.length > 0 || resourceUndoMutations.length > 0) {
			batchingDisposable = undoRedoService.__tempBatchingUndoRedo(unitId);
			undoRedoService.pushUndoRedo({
				unitID: unitId,
				redoMutations: resourceRedoMutations,
				undoMutations: resourceUndoMutations,
				id: historyId
			});
		}
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		if (!result && batchingDisposable != null) {
			batchingDisposable.dispose();
			undoRedoService.rollback(historyId, unitId);
			return false;
		}
		batchingDisposable === null || batchingDisposable === void 0 || batchingDisposable.dispose();
		return Boolean(result);
	}
};
function getPasteAdapterService(accessor) {
	try {
		return accessor.get(IDocClipboardPasteAdapterService);
	} catch {
		return null;
	}
}
function executeResourceMutationGroups(mutationGroups, commandService) {
	const executedUndoGroups = [];
	for (const mutationGroup of mutationGroups) {
		if (!executeMutations(mutationGroup.redoMutations, commandService)) {
			executeMutationGroups([...executedUndoGroups].reverse(), commandService);
			return false;
		}
		executedUndoGroups.push(mutationGroup.undoMutations);
	}
	return true;
}
function executeMutationGroups(mutationGroups, commandService) {
	mutationGroups.forEach((mutations) => {
		executeMutations(mutations, commandService);
	});
}
function executeMutations(mutations, commandService) {
	for (const mutation of mutations) if (!commandService.syncExecuteCommand(mutation.id, mutation.params)) return false;
	return true;
}
function adjustSelectionByTable(selection, tables) {
	const { startOffset, endOffset } = selection;
	const endsWithTable = tables.some((t) => t.startIndex === endOffset);
	const newEndOffset = Math.max(startOffset, endsWithTable ? endOffset - 1 : endOffset);
	return {
		...selection,
		endOffset: newEndOffset,
		collapsed: startOffset === newEndOffset
	};
}
function getCutActionsFromTextRanges(selections, docDataModel, segmentId) {
	var _docDataModel$getSelf2;
	const originBody = (_docDataModel$getSelf2 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf2 === void 0 ? void 0 : _docDataModel$getSelf2.getBody();
	const textX = new _univerjs_core.TextX();
	const jsonX = _univerjs_core.JSONX.getInstance();
	const rawActions = [];
	if (originBody == null) return rawActions;
	const { tables = [] } = originBody;
	const adjustedSelections = selections.map((selection) => adjustSelectionByTable(selection, tables));
	textX.push(..._univerjs_core.BuildTextUtils.selection.delete(adjustedSelections, originBody, 0, null, false));
	const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
	rawActions.push(jsonX.editOp(textX.serialize(), path));
	const removedCustomBlockIds = (0, _univerjs_core.getCustomBlockIdsInSelections)(originBody, selections);
	const drawings = docDataModel.getDrawings() ?? {};
	const drawingOrder = docDataModel.getDrawingsOrder() ?? [];
	const sortedRemovedCustomBlockIds = removedCustomBlockIds.sort((a, b) => {
		if (drawingOrder.indexOf(a) > drawingOrder.indexOf(b)) return -1;
		else if (drawingOrder.indexOf(a) < drawingOrder.indexOf(b)) return 1;
		return 0;
	});
	if (sortedRemovedCustomBlockIds.length > 0) for (const blockId of sortedRemovedCustomBlockIds) {
		const drawing = drawings[blockId];
		const drawingIndex = drawingOrder.indexOf(blockId);
		if (drawing == null || drawingIndex < 0) continue;
		const removeDrawingAction = jsonX.removeOp(["drawings", blockId], drawing);
		const removeDrawingOrderAction = jsonX.removeOp(["drawingsOrder", drawingIndex], blockId);
		rawActions.push(removeDrawingAction);
		rawActions.push(removeDrawingOrderAction);
	}
	return rawActions.reduce((acc, cur) => {
		return _univerjs_core.JSONX.compose(acc, cur);
	}, null);
}
const IMPLICIT_WHOLE_BODY_SELECTION_TOKENS = /* @__PURE__ */ new Set([
	_univerjs_core.DataStreamTreeTokenType.PARAGRAPH,
	_univerjs_core.DataStreamTreeTokenType.SECTION_BREAK,
	_univerjs_core.DataStreamTreeTokenType.BLOCK_START,
	_univerjs_core.DataStreamTreeTokenType.BLOCK_END,
	_univerjs_core.DataStreamTreeTokenType.COLUMN_GROUP_START,
	_univerjs_core.DataStreamTreeTokenType.COLUMN_START,
	_univerjs_core.DataStreamTreeTokenType.COLUMN_END,
	_univerjs_core.DataStreamTreeTokenType.COLUMN_GROUP_END
]);
function isWholeBodySelected(textRanges, rectRanges, body) {
	const intervals = (Array.isArray(textRanges) ? textRanges : []).filter((range) => !range.collapsed).map(({ startOffset, endOffset }) => ({
		startOffset,
		endOffset
	}));
	for (const rectRange of Array.isArray(rectRanges) ? rectRanges : []) {
		if (!rectRange.spanEntireTable) continue;
		const table = (body.tables ?? []).find((item) => rectRange.tableId && item.tableId === rectRange.tableId || item.startIndex === rectRange.startOffset);
		if (table) intervals.push((0, _univerjs_core.getTableRangeInterval)(table));
	}
	intervals.sort((left, right) => left.startOffset - right.startOffset || left.endOffset - right.endOffset);
	const editableEnd = Math.max(0, body.dataStream.length - 2);
	let intervalIndex = 0;
	for (let offset = 0; offset < editableEnd; offset++) {
		var _intervals$intervalIn;
		while (((_intervals$intervalIn = intervals[intervalIndex]) === null || _intervals$intervalIn === void 0 ? void 0 : _intervals$intervalIn.endOffset) <= offset) intervalIndex++;
		const interval = intervals[intervalIndex];
		if (interval && interval.startOffset <= offset && offset < interval.endOffset) continue;
		if (!IMPLICIT_WHOLE_BODY_SELECTION_TOKENS.has(body.dataStream[offset])) return false;
	}
	return editableEnd > 0;
}
function getWholeBodyCutActions(selections, docDataModel, segmentId) {
	var _docDataModel$getSelf3;
	const body = (_docDataModel$getSelf3 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf3 === void 0 ? void 0 : _docDataModel$getSelf3.getBody();
	if (!body) return [];
	const emptyBody = {
		dataStream: _univerjs_core.DataStreamTreeTokenType.PARAGRAPH,
		paragraphs: [{
			paragraphId: (0, _univerjs_core.createParagraphId)(new Set((body.paragraphs ?? []).map((paragraph) => paragraph.paragraphId))),
			startIndex: 0
		}]
	};
	const deleteLength = Math.max(0, body.dataStream.length - 1);
	const textX = new _univerjs_core.TextX();
	textX.push({
		t: _univerjs_core.TextXActionType.INSERT,
		len: emptyBody.dataStream.length,
		body: emptyBody
	});
	textX.push({
		t: _univerjs_core.TextXActionType.DELETE,
		len: deleteLength
	});
	const jsonX = _univerjs_core.JSONX.getInstance();
	const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
	const rawActions = [];
	const editAction = jsonX.editOp(textX.serialize(), path);
	if (editAction) rawActions.push(editAction);
	const drawings = docDataModel.getDrawings() ?? {};
	const drawingOrder = docDataModel.getDrawingsOrder() ?? [];
	const removedCustomBlockIds = (0, _univerjs_core.getCustomBlockIdsInSelections)(body, [{
		...selections[0] ?? { collapsed: false },
		startOffset: 0,
		endOffset: deleteLength,
		collapsed: false
	}]).sort((left, right) => drawingOrder.indexOf(right) - drawingOrder.indexOf(left));
	for (const blockId of removedCustomBlockIds) {
		const drawing = drawings[blockId];
		const drawingIndex = drawingOrder.indexOf(blockId);
		if (drawing == null || drawingIndex < 0) continue;
		const removeDrawingAction = jsonX.removeOp(["drawings", blockId], drawing);
		const removeDrawingOrderAction = jsonX.removeOp(["drawingsOrder", drawingIndex], blockId);
		if (removeDrawingAction) rawActions.push(removeDrawingAction);
		if (removeDrawingOrderAction) rawActions.push(removeDrawingOrderAction);
	}
	for (const table of body.tables ?? []) {
		const removeTableSourceAction = jsonX.removeOp(["tableSource", table.tableId]);
		if (removeTableSourceAction) rawActions.push(removeTableSourceAction);
	}
	let actions = null;
	for (const action of rawActions) actions = actions == null ? action : _univerjs_core.JSONX.compose(actions, action);
	return actions ?? [];
}
function getCutActionsFromRectRanges(ranges, docDataModel, viewModel, segmentId) {
	var _docDataModel$getSelf4;
	const rawActions = [];
	if (((_docDataModel$getSelf4 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf4 === void 0 ? void 0 : _docDataModel$getSelf4.getBody()) == null) return rawActions;
	const textX = new _univerjs_core.TextX();
	const jsonX = _univerjs_core.JSONX.getInstance();
	const memoryCursor = new _univerjs_core.MemoryCursor();
	memoryCursor.reset();
	for (const range of ranges) {
		const { startOffset, endOffset, spanEntireRow, spanEntireTable } = range;
		if (startOffset == null || endOffset == null) continue;
		if (spanEntireTable) {
			const actionParams = getDeleteTableActionParams({
				startOffset,
				endOffset,
				segmentId
			}, viewModel);
			if (actionParams == null) continue;
			const { offset, len, tableId } = actionParams;
			if (offset - memoryCursor.cursor > 0) textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len: offset - memoryCursor.cursor
			});
			textX.push({
				t: _univerjs_core.TextXActionType.DELETE,
				len
			});
			const action = jsonX.removeOp(["tableSource", tableId]);
			rawActions.push(action);
			memoryCursor.moveCursorTo(offset + len);
		} else if (spanEntireRow) {
			const actionParams = getDeleteRowsActionsParams({
				startOffset,
				endOffset,
				segmentId
			}, viewModel);
			if (actionParams == null) continue;
			const { offset, rowIndexes, len, tableId } = actionParams;
			if (offset - memoryCursor.cursor > 0) textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len: offset - memoryCursor.cursor
			});
			textX.push({
				t: _univerjs_core.TextXActionType.DELETE,
				len
			});
			for (const index of rowIndexes.reverse()) {
				const action = jsonX.removeOp([
					"tableSource",
					tableId,
					"tableRows",
					index
				]);
				rawActions.push(action);
			}
			memoryCursor.moveCursorTo(offset + len);
		} else {
			const actionParams = getDeleteRowContentActionParams({
				startOffset,
				endOffset,
				segmentId
			}, viewModel);
			if (actionParams == null) continue;
			const { offsets } = actionParams;
			for (const offset of offsets) {
				const { retain, delete: delLen } = offset;
				if (retain - memoryCursor.cursor > 0) textX.push({
					t: _univerjs_core.TextXActionType.RETAIN,
					len: retain - memoryCursor.cursor
				});
				textX.push({
					t: _univerjs_core.TextXActionType.DELETE,
					len: delLen
				});
				memoryCursor.moveCursorTo(retain + delLen);
			}
		}
	}
	const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
	rawActions.push(jsonX.editOp(textX.serialize(), path));
	return rawActions.reduce((acc, cur) => {
		return _univerjs_core.JSONX.compose(acc, cur);
	}, null);
}
function getCutActionsFromDocRanges(textRanges, rectRanges, docDataModel, viewModel, segmentId, wholeBodySelected = false) {
	var _docDataModel$getSelf5;
	let rawActions = [];
	const body = (_docDataModel$getSelf5 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf5 === void 0 ? void 0 : _docDataModel$getSelf5.getBody();
	if (body && Array.isArray(textRanges) && Array.isArray(rectRanges) && (wholeBodySelected || isWholeBodySelected(textRanges, rectRanges, body))) return getWholeBodyCutActions(textRanges, docDataModel, segmentId);
	if (Array.isArray(textRanges) && (textRanges === null || textRanges === void 0 ? void 0 : textRanges.length) !== 0) rawActions = getCutActionsFromTextRanges(textRanges, docDataModel, segmentId);
	if (Array.isArray(rectRanges) && (rectRanges === null || rectRanges === void 0 ? void 0 : rectRanges.length) !== 0) {
		const actions = getCutActionsFromRectRanges(rectRanges, docDataModel, viewModel, segmentId);
		if (rawActions == null || rawActions.length === 0) rawActions = actions;
		else rawActions = _univerjs_core.JSONX.compose(rawActions, _univerjs_core.JSONX.transform(actions, rawActions, "right"));
	}
	return rawActions;
}
function getReplaceDocRangesActions(textRanges, rectRanges, docDataModel, viewModel, segmentId, insertBody, wholeBodySelected = false) {
	var _docDataModel$getSelf6;
	const body = (_docDataModel$getSelf6 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf6 === void 0 ? void 0 : _docDataModel$getSelf6.getBody();
	const insertOffset = wholeBodySelected || body && isWholeBodySelected(textRanges, rectRanges, body) ? 0 : getDocRangeInsertOffset(textRanges, rectRanges);
	if (insertOffset == null) return null;
	const cutActions = getCutActionsFromDocRanges(textRanges, rectRanges, docDataModel, viewModel, segmentId, wholeBodySelected);
	const textX = new _univerjs_core.TextX();
	if (insertOffset > 0) textX.push({
		t: _univerjs_core.TextXActionType.RETAIN,
		len: insertOffset
	});
	if (insertBody.dataStream.length > 0) textX.push({
		t: _univerjs_core.TextXActionType.INSERT,
		body: insertBody,
		len: insertBody.dataStream.length
	});
	const insertAction = _univerjs_core.JSONX.getInstance().editOp(textX.serialize(), (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId));
	return {
		actions: insertAction == null ? cutActions : cutActions == null || cutActions.length === 0 ? insertAction : _univerjs_core.JSONX.compose(cutActions, insertAction),
		insertOffset
	};
}
function getDocRangeInsertOffset(textRanges, rectRanges) {
	const insertOffset = [...Array.isArray(textRanges) ? textRanges : [], ...Array.isArray(rectRanges) ? rectRanges : []].filter((range) => range.startOffset != null && range.endOffset != null).reduce((offset, range) => Math.min(offset, range.startOffset), Number.POSITIVE_INFINITY);
	return Number.isFinite(insertOffset) ? insertOffset : null;
}
const CutContentCommand = {
	id: "doc.command.inner-cut",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		var _selectionInfo$option3;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const selectionInfo = docSelectionManagerService.getSelectionInfo();
		const { segmentId, textRanges, selections = docSelectionManagerService.getTextRanges(), rectRanges = docSelectionManagerService.getRectRanges(), wholeBodySelected = (selectionInfo === null || selectionInfo === void 0 || (_selectionInfo$option3 = selectionInfo.options) === null || _selectionInfo$option3 === void 0 ? void 0 : _selectionInfo$option3.wholeDocument) === true } = params;
		if ((!Array.isArray(selections) || selections.length === 0) && (!Array.isArray(rectRanges) || rectRanges.length === 0)) return false;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null) return false;
		const unitId = docDataModel.getUnitId();
		const docSkeletonManagerService = getCommandSkeleton(accessor, unitId);
		if (docSkeletonManagerService == null) return false;
		const viewModel = docSkeletonManagerService.getViewModel();
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges,
				trigger: CutContentCommand.id
			}
		};
		doMutation.params.actions = getCutActionsFromDocRanges(selections, rectRanges, docDataModel, viewModel, segmentId, wholeBodySelected);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};

//#endregion
//#region src/utils/horizontal-line.ts
function isHorizontalLineParagraph(dataStream, paragraph) {
	const paragraphStyle = paragraph === null || paragraph === void 0 ? void 0 : paragraph.paragraphStyle;
	return Array.from(dataStream).every((token) => token === _univerjs_core.DataStreamTreeTokenType.PARAGRAPH || token === _univerjs_core.DataStreamTreeTokenType.SECTION_BREAK) && (paragraphStyle === null || paragraphStyle === void 0 ? void 0 : paragraphStyle.borderBottom) != null && paragraphStyle.borderTop == null && paragraphStyle.borderLeft == null && paragraphStyle.borderRight == null && paragraphStyle.borderBetween == null;
}

//#endregion
//#region src/commands/commands/util.ts
function getCurrentParagraph(accessor) {
	var _doc$getBody, _doc$getBody2;
	const instanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	const range = accessor.get(_univerjs_docs.DocSelectionManagerService).getActiveTextRange();
	const doc = instanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
	if (!range || !range.collapsed || !doc || range.segmentId) return false;
	return _univerjs_core.BuildTextUtils.range.getParagraphsInRange(range, ((_doc$getBody = doc.getBody()) === null || _doc$getBody === void 0 ? void 0 : _doc$getBody.paragraphs) ?? [], ((_doc$getBody2 = doc.getBody()) === null || _doc$getBody2 === void 0 ? void 0 : _doc$getBody2.dataStream) ?? "")[0];
}

//#endregion
//#region src/commands/commands/doc-delete.command.ts
const DeleteCustomBlockCommand = {
	id: "doc.command.delete-custom-block",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const { direction, range, unitId, drawingId } = params;
		const activeRange = docSelectionManagerService.getActiveTextRange();
		const documentDataModel = univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (activeRange == null || documentDataModel == null) return false;
		const { startOffset, segmentId, style } = activeRange;
		const cursor = direction === _univerjs_core.DeleteDirection.LEFT ? startOffset - 1 : startOffset;
		const textRanges = [{
			startOffset: cursor,
			endOffset: cursor,
			style
		}];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges,
				prevTextRanges: [range]
			}
		};
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const rawActions = [];
		if (startOffset > 0) textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: direction === _univerjs_core.DeleteDirection.LEFT ? startOffset - 1 : startOffset
		});
		textX.push({
			t: _univerjs_core.TextXActionType.DELETE,
			len: 1
		});
		const path = (0, _univerjs_core.getRichTextEditPath)(documentDataModel, segmentId);
		rawActions.push(jsonX.editOp(textX.serialize(), path));
		const drawing = (documentDataModel.getDrawings() ?? {})[drawingId];
		const drawingIndex = documentDataModel.getDrawingsOrder().indexOf(drawingId);
		const removeDrawingAction = jsonX.removeOp(["drawings", drawingId], drawing);
		const removeDrawingOrderAction = jsonX.removeOp(["drawingsOrder", drawingIndex], drawingId);
		rawActions.push(removeDrawingAction);
		rawActions.push(removeDrawingOrderAction);
		doMutation.params.actions = rawActions.reduce((acc, cur) => {
			return _univerjs_core.JSONX.compose(acc, cur);
		}, null);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
const MergeTwoParagraphCommand = {
	id: "doc.command.merge-two-paragraph",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		var _docDataModel$getSelf;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const { direction, range } = params;
		const activeRange = docSelectionManagerService.getActiveTextRange();
		const ranges = docSelectionManagerService.getTextRanges();
		if (activeRange == null || ranges == null) return false;
		const { segmentId, style } = activeRange;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const originBody = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		if (docDataModel == null || originBody == null) return false;
		const dataStream = originBody.dataStream;
		const actualRange = activeRange;
		const unitId = docDataModel.getUnitId();
		const { startOffset, collapsed } = actualRange;
		if (!collapsed) return false;
		const startIndex = direction === _univerjs_core.DeleteDirection.LEFT ? startOffset : startOffset + 1;
		let curParagraph;
		let nextParagraph;
		for (const paragraph of originBody.paragraphs) {
			if (paragraph.startIndex >= startIndex) {
				nextParagraph = paragraph;
				break;
			}
			curParagraph = paragraph;
		}
		if (curParagraph == null || nextParagraph == null) return false;
		const cursor = direction === _univerjs_core.DeleteDirection.LEFT ? startOffset - 1 : startOffset;
		const textRanges = [{
			startOffset: cursor,
			endOffset: cursor,
			style
		}];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges,
				prevTextRanges: [range]
			}
		};
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		if (curParagraph.startIndex > 0) textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: curParagraph.startIndex
		});
		textX.push({
			t: _univerjs_core.TextXActionType.DELETE,
			len: 1
		});
		if (nextParagraph.startIndex > curParagraph.startIndex + 1) textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: nextParagraph.startIndex - curParagraph.startIndex - 1
		});
		const tokens = Object.values(_univerjs_core.DataStreamTreeTokenType);
		const lastToken = dataStream[curParagraph.startIndex - 1];
		if (lastToken && !tokens.includes(lastToken) || lastToken === " ") textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: 1,
			coverType: _univerjs_core.UpdateDocsAttributeType.REPLACE,
			body: {
				dataStream: "",
				paragraphs: [{
					..._univerjs_core.Tools.deepClone(curParagraph),
					startIndex: 0
				}]
			}
		});
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
const RemoveHorizontalLineCommand = {
	id: "doc.command.remove-horizontal-line",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		var _docDataModel$getSelf2, _originBody$paragraph;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const activeRange = docSelectionManagerService.getActiveTextRange();
		const ranges = docSelectionManagerService.getTextRanges();
		if (activeRange == null || ranges == null) return false;
		const { segmentId, style } = activeRange;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const originBody = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf2 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf2 === void 0 ? void 0 : _docDataModel$getSelf2.getBody();
		if (docDataModel == null || originBody == null) return false;
		const actualRange = activeRange;
		const unitId = docDataModel.getUnitId();
		const { startOffset, collapsed } = actualRange;
		if (!collapsed) return false;
		const paragraph = (_originBody$paragraph = originBody.paragraphs) === null || _originBody$paragraph === void 0 ? void 0 : _originBody$paragraph.find((p) => p.startIndex === startOffset - 1);
		if (paragraph == null) return false;
		const textRanges = [{
			startOffset,
			endOffset: startOffset,
			style
		}];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				historyAction: _univerjs_docs.DocHistoryAction.DeleteDivider,
				actions: [],
				textRanges
			}
		};
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		if (paragraph.startIndex > 0) textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: paragraph.startIndex
		});
		textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: 1,
			coverType: _univerjs_core.UpdateDocsAttributeType.REPLACE,
			body: {
				dataStream: "",
				paragraphs: [{
					..._univerjs_core.Tools.deepClone({
						...paragraph,
						paragraphStyle: {
							...paragraph.paragraphStyle,
							borderBottom: void 0
						}
					}),
					startIndex: 0
				}]
			}
		});
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
function getCursorWhenDelete(textRanges, rectRanges) {
	let cursor = 0;
	if (textRanges == null || textRanges.length === 0) {
		if (typeof rectRanges[0].startOffset === "number") {
			const rectRange = rectRanges[0];
			const { spanEntireRow, spanEntireTable } = rectRange;
			if (spanEntireTable) cursor = rectRange.startOffset - 3;
			else if (spanEntireRow) if (rectRange.startRow > 0) cursor = rectRange.startOffset - 6;
			else cursor = rectRange.startOffset;
			else cursor = rectRanges[0].startOffset;
		}
	} else if (textRanges.length > 0 && rectRanges.length > 0) {
		const textRange = textRanges[0];
		const rectRange = rectRanges[0];
		if (textRange.startOffset != null && rectRange.startOffset != null) {
			if (textRange.startOffset < rectRange.startOffset) cursor = textRange.startOffset;
			else if (textRange.startOffset >= rectRange.startOffset) {
				const { spanEntireRow, spanEntireTable } = rectRange;
				if (spanEntireTable) cursor = rectRange.startOffset - 3;
				else if (spanEntireRow) cursor = rectRange.startOffset - 6;
			}
		}
	}
	return cursor;
}
function isDeleteOffsetInsideBlockRange(body, offset) {
	var _body$blockRanges;
	return ((_body$blockRanges = body.blockRanges) === null || _body$blockRanges === void 0 ? void 0 : _body$blockRanges.some((blockRange) => {
		const interval = (0, _univerjs_core.getBlockRangeInterval)(blockRange);
		return interval.startOffset < offset && offset < interval.endOffset - 1;
	})) ?? false;
}
const DeleteLeftCommand = {
	id: "doc.command.delete-left",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		var _docDataModel$getSelf3;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		let result = true;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null) return false;
		const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
		const activeRange = docSelectionManagerService.getActiveTextRange();
		const rectRanges = docSelectionManagerService.getRectRanges();
		const ranges = docSelectionManagerService.getTextRanges();
		const skeleton = docSkeletonManagerService === null || docSkeletonManagerService === void 0 ? void 0 : docSkeletonManagerService.getSkeleton();
		if (skeleton == null) return false;
		if (rectRanges === null || rectRanges === void 0 ? void 0 : rectRanges.length) {
			const cursor = getCursorWhenDelete(ranges, rectRanges);
			const segmentId = rectRanges[0].segmentId;
			const textRanges = [{
				startOffset: cursor,
				endOffset: cursor
			}];
			return commandService.executeCommand(CutContentCommand.id, {
				segmentId,
				textRanges
			});
		}
		if (activeRange == null || ranges == null) return false;
		const autoFormatResult = await executeDeleteAutoFormat(accessor, DeleteLeftCommand.id);
		if (autoFormatResult != null) return autoFormatResult;
		const { segmentId, style, segmentPage } = activeRange;
		const body = (_docDataModel$getSelf3 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf3 === void 0 ? void 0 : _docDataModel$getSelf3.getBody();
		if (body == null) return false;
		const actualRange = activeRange;
		const { startOffset, collapsed } = actualRange;
		if (collapsed && shouldResetEmptyCenteredParagraphAlignment(docDataModel)) {
			const emptyCenteredParagraph = getEmptyCenteredParagraphAtOffset(body, startOffset);
			if (emptyCenteredParagraph != null) return resetEmptyCenteredParagraphAlignment(commandService, docDataModel, segmentId ?? "", emptyCenteredParagraph, style);
		}
		const curGlyph = skeleton.findNodeByCharIndex(startOffset, segmentId, segmentPage);
		const isBullet = (0, _univerjs_engine_render.hasListGlyph)(curGlyph);
		const isIndent = (0, _univerjs_engine_render.isIndentByGlyph)(curGlyph, body);
		let cursor = startOffset;
		const preGlyph = skeleton.findNodeByCharIndex(startOffset - 1, segmentId, segmentPage);
		if (!isDeleteOffsetInsideBlockRange(body, startOffset) && (0, _univerjs_engine_render.isFirstGlyph)(curGlyph) && preGlyph !== curGlyph && (isBullet === true || isIndent === true) && collapsed) {
			const paragraph = (0, _univerjs_engine_render.getParagraphByGlyph)(curGlyph, body);
			if (paragraph == null) return false;
			const paragraphIndex = paragraph === null || paragraph === void 0 ? void 0 : paragraph.startIndex;
			const updateParagraph = {
				startIndex: 0,
				paragraphId: paragraph.paragraphId
			};
			const paragraphStyle = paragraph.paragraphStyle;
			if (isBullet === true) {
				if (paragraphStyle) {
					updateParagraph.paragraphStyle = { ...paragraphStyle };
					const { hanging } = paragraphStyle;
					if (hanging) {
						updateParagraph.paragraphStyle.indentStart = { ...hanging };
						updateParagraph.paragraphStyle.hanging = void 0;
					}
				}
			} else if (isIndent === true) {
				const bullet = paragraph.bullet;
				if (bullet) updateParagraph.bullet = bullet;
				if (paragraphStyle) {
					updateParagraph.paragraphStyle = { ...paragraphStyle };
					delete updateParagraph.paragraphStyle.hanging;
					delete updateParagraph.paragraphStyle.indentStart;
				}
			}
			const textRanges = [{
				startOffset: cursor,
				endOffset: cursor,
				collapsed: true,
				style
			}];
			result = await commandService.executeCommand(_univerjs_docs.UpdateTextCommand.id, {
				unitId: docDataModel.getUnitId(),
				updateBody: {
					dataStream: "",
					paragraphs: [{ ...updateParagraph }]
				},
				range: {
					startOffset: paragraphIndex,
					endOffset: paragraphIndex + 1,
					collapsed: true
				},
				textRanges,
				coverType: _univerjs_core.UpdateDocsAttributeType.REPLACE,
				segmentId
			});
		} else if (collapsed === true) {
			if (preGlyph == null) return true;
			if (preGlyph.content === _univerjs_core.DataStreamTreeTokenType.PARAGRAPH) {
				var _body$paragraphs;
				const paragraph = (_body$paragraphs = body.paragraphs) === null || _body$paragraphs === void 0 ? void 0 : _body$paragraphs.find((p) => p.startIndex === startOffset - 1);
				const paragraphStart = paragraph == null ? 0 : (0, _univerjs_core.getParagraphContentStartOffset)(body, paragraph);
				if (isHorizontalLineParagraph(paragraph == null ? "" : body.dataStream.slice(paragraphStart, paragraph.startIndex + 1), paragraph)) result = await commandService.executeCommand(RemoveHorizontalLineCommand.id);
				else result = await commandService.executeCommand(MergeTwoParagraphCommand.id, {
					direction: _univerjs_core.DeleteDirection.LEFT,
					range: actualRange
				});
			} else if (preGlyph.streamType === "\b") {
				var _docDataModel$getSnap, _docDataModel$getBody;
				const drawing = (_docDataModel$getSnap = docDataModel.getSnapshot().drawings) === null || _docDataModel$getSnap === void 0 ? void 0 : _docDataModel$getSnap[preGlyph.drawingId ?? ""];
				if (drawing == null) return true;
				const customBlock = (_docDataModel$getBody = docDataModel.getBody()) === null || _docDataModel$getBody === void 0 || (_docDataModel$getBody = _docDataModel$getBody.customBlocks) === null || _docDataModel$getBody === void 0 ? void 0 : _docDataModel$getBody.find((block) => block.blockId === preGlyph.drawingId);
				if (drawing.layoutType === _univerjs_core.PositionedObjectLayoutType.INLINE || (customBlock === null || customBlock === void 0 ? void 0 : customBlock.blockType) === _univerjs_core.BlockType.CUSTOM) {
					const unitId = docDataModel.getUnitId();
					result = await commandService.executeCommand(DeleteCustomBlockCommand.id, {
						direction: _univerjs_core.DeleteDirection.LEFT,
						range: activeRange,
						unitId,
						drawingId: preGlyph.drawingId
					});
				} else {
					const prePreGlyph = skeleton.findNodeByCharIndex(startOffset - 2);
					if (prePreGlyph == null) return true;
					cursor -= preGlyph.count;
					cursor -= prePreGlyph.count;
					result = await commandService.executeCommand(_univerjs_docs.DeleteTextCommand.id, {
						unitId: docDataModel.getUnitId(),
						range: {
							...activeRange,
							startOffset: activeRange.startOffset - 1,
							endOffset: activeRange.endOffset - 1
						},
						segmentId,
						direction: _univerjs_core.DeleteDirection.LEFT,
						len: prePreGlyph.count
					});
				}
			} else {
				cursor -= preGlyph.count;
				result = await commandService.executeCommand(_univerjs_docs.DeleteTextCommand.id, {
					unitId: docDataModel.getUnitId(),
					range: actualRange,
					segmentId,
					direction: _univerjs_core.DeleteDirection.LEFT,
					len: preGlyph.count
				});
			}
		} else {
			const textRanges = getTextRangesWhenDelete(actualRange, [actualRange]);
			result = await commandService.executeCommand(CutContentCommand.id, {
				segmentId,
				textRanges,
				selections: [actualRange]
			});
		}
		return result;
	}
};
const DeleteRightCommand = {
	id: "doc.command.delete-right",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		var _docDataModel$getSelf4;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const docDataModel = accessor.get(_univerjs_core.IUniverInstanceService).getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!docDataModel) return false;
		const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const activeRange = docSelectionManagerService.getActiveTextRange();
		const rectRanges = docSelectionManagerService.getRectRanges();
		const ranges = docSelectionManagerService.getTextRanges();
		const skeleton = docSkeletonManagerService === null || docSkeletonManagerService === void 0 ? void 0 : docSkeletonManagerService.getSkeleton();
		if (rectRanges === null || rectRanges === void 0 ? void 0 : rectRanges.length) {
			const cursor = getCursorWhenDelete(ranges, rectRanges);
			const segmentId = rectRanges[0].segmentId;
			const textRanges = [{
				startOffset: cursor,
				endOffset: cursor
			}];
			return commandService.executeCommand(CutContentCommand.id, {
				segmentId,
				textRanges
			});
		}
		if (activeRange == null || skeleton == null || ranges == null) return false;
		const autoFormatResult = await executeDeleteAutoFormat(accessor, DeleteRightCommand.id);
		if (autoFormatResult != null) return autoFormatResult;
		const { segmentId, style, segmentPage } = activeRange;
		const body = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf4 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf4 === void 0 ? void 0 : _docDataModel$getSelf4.getBody();
		if (!docDataModel || !body) return false;
		const actualRange = activeRange;
		const { startOffset, endOffset, collapsed } = actualRange;
		if (collapsed && shouldResetEmptyCenteredParagraphAlignment(docDataModel)) {
			const emptyCenteredParagraph = getEmptyCenteredParagraphAtOffset(body, startOffset);
			if (emptyCenteredParagraph != null) return resetEmptyCenteredParagraphAlignment(commandService, docDataModel, segmentId ?? "", emptyCenteredParagraph, style);
		}
		if (startOffset === body.dataStream.length - 2 && collapsed) return true;
		let result = false;
		if (collapsed === true) {
			const needDeleteGlyph = skeleton.findNodeByCharIndex(startOffset, segmentId, segmentPage);
			const nextGlyph = skeleton.findNodeByCharIndex(startOffset + 1);
			if (needDeleteGlyph.streamType === _univerjs_core.DataStreamTreeTokenType.PARAGRAPH && (nextGlyph === null || nextGlyph === void 0 ? void 0 : nextGlyph.streamType) === _univerjs_core.DataStreamTreeTokenType.SECTION_BREAK) return false;
			if (needDeleteGlyph.content === "\r") result = await commandService.executeCommand(MergeTwoParagraphCommand.id, {
				direction: _univerjs_core.DeleteDirection.RIGHT,
				range: activeRange
			});
			else if (needDeleteGlyph.streamType === "\b") {
				var _docDataModel$getSnap2;
				const drawing = (_docDataModel$getSnap2 = docDataModel.getSnapshot().drawings) === null || _docDataModel$getSnap2 === void 0 ? void 0 : _docDataModel$getSnap2[needDeleteGlyph.drawingId ?? ""];
				if (drawing == null) return true;
				if (drawing.layoutType === _univerjs_core.PositionedObjectLayoutType.INLINE) {
					const unitId = docDataModel.getUnitId();
					result = await commandService.executeCommand(DeleteCustomBlockCommand.id, {
						direction: _univerjs_core.DeleteDirection.RIGHT,
						range: activeRange,
						unitId,
						drawingId: needDeleteGlyph.drawingId
					});
				} else {
					if (nextGlyph == null) return true;
					result = await commandService.executeCommand(_univerjs_docs.DeleteTextCommand.id, {
						unitId: docDataModel.getUnitId(),
						range: {
							...activeRange,
							startOffset: startOffset + 1,
							endOffset: endOffset + 1
						},
						segmentId,
						direction: _univerjs_core.DeleteDirection.RIGHT,
						len: nextGlyph.count
					});
				}
			} else result = await commandService.executeCommand(_univerjs_docs.DeleteTextCommand.id, {
				unitId: docDataModel.getUnitId(),
				range: actualRange,
				segmentId,
				direction: _univerjs_core.DeleteDirection.RIGHT,
				len: needDeleteGlyph.count
			});
		} else {
			const textRanges = getTextRangesWhenDelete(actualRange, [actualRange]);
			result = await commandService.executeCommand(CutContentCommand.id, {
				segmentId,
				textRanges,
				selections: [actualRange]
			});
		}
		return result;
	}
};
async function executeDeleteAutoFormat(accessor, commandId) {
	if (!accessor.has(DocAutoFormatService)) return null;
	const commandService = accessor.get(_univerjs_core.ICommandService);
	const mutations = accessor.get(DocAutoFormatService).onAutoFormat(commandId);
	if (!mutations.length) return null;
	return (await (0, _univerjs_core.sequenceExecuteAsync)(mutations, commandService)).result;
}
function getTextRangesWhenDelete(activeRange, ranges) {
	let cursor = activeRange.endOffset;
	for (const range of ranges) {
		const { startOffset, endOffset } = range;
		if (startOffset == null || endOffset == null) continue;
		if (endOffset <= activeRange.endOffset) cursor -= endOffset - startOffset;
	}
	return [{
		startOffset: cursor,
		endOffset: cursor,
		style: activeRange.style
	}];
}
function getEmptyCenteredParagraphAtOffset(body, offset) {
	const paragraphs = body.paragraphs ?? [];
	for (let i = 0; i < paragraphs.length; i++) {
		var _paragraph$paragraphS;
		const paragraph = paragraphs[i];
		const paragraphTextStart = i === 0 ? 0 : paragraphs[i - 1].startIndex + 1;
		if (paragraph.startIndex === paragraphTextStart && paragraph.startIndex === offset && ((_paragraph$paragraphS = paragraph.paragraphStyle) === null || _paragraph$paragraphS === void 0 ? void 0 : _paragraph$paragraphS.horizontalAlign) === _univerjs_core.HorizontalAlign.CENTER) return paragraph;
	}
}
function shouldResetEmptyCenteredParagraphAlignment(docDataModel) {
	return !_univerjs_core.SHEET_EDITOR_UNITS.includes(docDataModel.getUnitId());
}
function resetEmptyCenteredParagraphAlignment(commandService, docDataModel, segmentId, paragraph, style) {
	return commandService.executeCommand(_univerjs_docs.UpdateTextCommand.id, {
		unitId: docDataModel.getUnitId(),
		updateBody: {
			dataStream: "",
			paragraphs: [{
				...paragraph,
				startIndex: 0,
				paragraphStyle: {
					...paragraph.paragraphStyle,
					horizontalAlign: _univerjs_core.HorizontalAlign.LEFT
				}
			}]
		},
		range: {
			startOffset: paragraph.startIndex,
			endOffset: paragraph.startIndex + 1,
			collapsed: true
		},
		textRanges: [{
			startOffset: paragraph.startIndex,
			endOffset: paragraph.startIndex,
			collapsed: true,
			style
		}],
		coverType: _univerjs_core.UpdateDocsAttributeType.REPLACE,
		segmentId
	});
}
const DeleteCurrentParagraphCommand = {
	id: "doc.command.delete-current-paragraph",
	name: "doc.command.delete-current-paragraph",
	multi: true,
	priority: 0,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		var _docDataModel$getBody2;
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!docDataModel) return false;
		const dataStream = ((_docDataModel$getBody2 = docDataModel.getBody()) === null || _docDataModel$getBody2 === void 0 ? void 0 : _docDataModel$getBody2.dataStream) ?? "";
		const paragraph = getCurrentParagraph(accessor);
		if (!paragraph) return false;
		const actions = _univerjs_core.BuildTextUtils.selection.delete([{
			startOffset: paragraph.paragraphStart,
			endOffset: dataStream[paragraph.paragraphEnd + 1] === "\n" ? paragraph.paragraphEnd : paragraph.paragraphEnd + 1,
			collapsed: false
		}], docDataModel.getBody(), 0, void 0, true);
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel);
		const params = {
			unitId: docDataModel.getUnitId(),
			actions: _univerjs_core.JSONX.getInstance().editOp(actions, path),
			textRanges: [{
				startOffset: paragraph.paragraphStart,
				endOffset: paragraph.paragraphStart,
				collapsed: true
			}]
		};
		return commandService.syncExecuteCommand(_univerjs_docs.RichTextEditingMutation.id, params);
	}
};

//#endregion
//#region src/services/clipboard/copy-content-cache.ts
const COPY_CONTENT_CACHE_LIMIT = 10;
function extractId(html) {
	const match = html.match(/data-copy-id="([^\s]+)"/);
	if (match && match[1]) return match[1];
	return null;
}
var CopyContentCache = class {
	constructor() {
		_defineProperty(this, "_cache", new _univerjs_core.LRUMap(COPY_CONTENT_CACHE_LIMIT));
	}
	set(id, clipboardData) {
		this._cache.set(id, clipboardData);
	}
	get(id) {
		return this._cache.get(id);
	}
	clear() {
		this._cache.clear();
	}
};
const copyContentCache = new CopyContentCache();

//#endregion
//#region src/services/clipboard/html-to-udm/parse-node-style.ts
function extractNodeStyle(node) {
	const styles = node.style;
	const docStyles = {};
	const tagName = node.tagName.toLowerCase();
	switch (tagName) {
		case "b":
		case "em":
		case "strong":
			docStyles.bl = _univerjs_core.BooleanNumber.TRUE;
			break;
		case "s":
			docStyles.st = { s: _univerjs_core.BooleanNumber.TRUE };
			break;
		case "u":
			docStyles.ul = { s: _univerjs_core.BooleanNumber.TRUE };
			break;
		case "i":
			docStyles.it = _univerjs_core.BooleanNumber.TRUE;
			break;
		case "sub":
		case "sup":
			docStyles.va = tagName === "sup" ? _univerjs_core.BaselineOffset.SUPERSCRIPT : _univerjs_core.BaselineOffset.SUBSCRIPT;
			break;
	}
	for (let i = 0; i < styles.length; i++) {
		const cssRule = styles[i];
		const cssValue = styles.getPropertyValue(cssRule);
		switch (cssRule) {
			case "font-family":
				docStyles.ff = cssValue.replace(/^"/g, "").replace(/"$/g, "");
				break;
			case "font-size": {
				const fontSize = Number.parseInt(cssValue);
				if (!Number.isNaN(fontSize)) {
					if (cssValue.endsWith("pt")) docStyles.fs = fontSize;
					else if (cssValue.endsWith("px")) docStyles.fs = (0, _univerjs_engine_render.pixelToPt)(fontSize);
				}
				break;
			}
			case "font-style":
				if (cssValue === "italic") docStyles.it = _univerjs_core.BooleanNumber.TRUE;
				break;
			case "font-weight":
				if (Number(cssValue) > 400 || String(cssValue) === "bold") docStyles.bl = _univerjs_core.BooleanNumber.TRUE;
				break;
			case "text-decoration":
				if (/underline/.test(cssValue)) docStyles.ul = { s: _univerjs_core.BooleanNumber.TRUE };
				else if (/overline/.test(cssValue)) docStyles.ol = { s: _univerjs_core.BooleanNumber.TRUE };
				else if (/line-through/.test(cssValue)) docStyles.st = { s: _univerjs_core.BooleanNumber.TRUE };
				break;
			case "color":
				try {
					const color = new _univerjs_core.ColorKit(cssValue);
					if (color.isValid) docStyles.cl = { rgb: color.toRgbString() };
				} catch (_e) {}
				break;
			case "background-color":
			case "background": {
				const color = new _univerjs_core.ColorKit(cssValue);
				if (color.isValid) docStyles.bg = { rgb: color.toRgbString() };
				break;
			}
			default: break;
		}
	}
	return docStyles;
}

//#endregion
//#region src/services/clipboard/html-to-udm/parse-to-dom.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
function parseToDom(rawHtml) {
	const parser = new DOMParser();
	const html = `<x-univer id="univer-root">${rawHtml}</x-univer>`;
	return parser.parseFromString(html, "text/html").querySelector("#univer-root");
}

//#endregion
//#region src/services/clipboard/html-to-udm/converter.ts
function matchFilter(node, filter) {
	const tagName = node.tagName.toLowerCase();
	if (typeof filter === "string") return tagName === filter;
	if (Array.isArray(filter)) return filter.some((name) => name === tagName);
	return filter(node);
}
const DEFAULT_TABLE_WIDTH = 660;
const WORD_SPACE_PLACEHOLDER = "";
/**
* Convert html strings into data structures in univer, IDocumentBody.
* Support plug-in, add custom rules,
*/
var HtmlToUDMService = class HtmlToUDMService {
	constructor() {
		_defineProperty(this, "_tableCache", []);
		_defineProperty(this, "_styleCache", /* @__PURE__ */ new Map());
		_defineProperty(this, "_styleRules", []);
		_defineProperty(this, "_afterProcessRules", []);
		_defineProperty(this, "_listStack", []);
		_defineProperty(this, "_lastParagraphIndex", -1);
	}
	static use(plugin) {
		if (this._pluginList.includes(plugin)) throw new Error(`Univer paste plugin ${plugin.name} already added`);
		this._pluginList.push(plugin);
	}
	_createParagraphId(body) {
		var _body$paragraphs;
		return (0, _univerjs_core.createParagraphId)(new Set((_body$paragraphs = body.paragraphs) === null || _body$paragraphs === void 0 ? void 0 : _body$paragraphs.map((paragraph) => paragraph.paragraphId)));
	}
	_createSectionId(body) {
		var _body$sectionBreaks;
		return (0, _univerjs_core.createSectionId)(new Set((_body$sectionBreaks = body.sectionBreaks) === null || _body$sectionBreaks === void 0 ? void 0 : _body$sectionBreaks.map((sectionBreak) => sectionBreak.sectionId)));
	}
	convert(html, metaConfig = {}) {
		var _pastePlugin$preproce;
		const pastePlugin = HtmlToUDMService._pluginList.find((plugin) => plugin.checkPasteType(html));
		const dom = parseToDom((pastePlugin === null || pastePlugin === void 0 || (_pastePlugin$preproce = pastePlugin.preprocessHtml) === null || _pastePlugin$preproce === void 0 ? void 0 : _pastePlugin$preproce.call(pastePlugin, html)) ?? html);
		const docData = {
			body: {
				dataStream: "",
				paragraphs: [],
				sectionBreaks: [],
				tables: [],
				textRuns: [],
				customBlocks: []
			},
			tableSource: {},
			id: (metaConfig === null || metaConfig === void 0 ? void 0 : metaConfig.unitId) ?? ""
		};
		if (pastePlugin) {
			this._styleRules = [...pastePlugin.stylesRules];
			this._afterProcessRules = [...pastePlugin.afterProcessRules];
		}
		this._tableCache = [];
		this._listStack = [];
		this._lastParagraphIndex = -1;
		this._styleCache.clear();
		this._process(null, dom.childNodes, docData);
		this._styleCache.clear();
		this._styleRules = [];
		this._afterProcessRules = [];
		return docData;
	}
	_process(parent, nodes, doc) {
		const body = doc.body;
		for (const node of nodes) if (node.nodeType === Node.TEXT_NODE) {
			const text = normalizeTextNode(node);
			if (!text) continue;
			let style;
			if (parent && this._styleCache.has(parent)) style = this._styleCache.get(parent);
			body.dataStream += text;
			if (style && Object.getOwnPropertyNames(style).length) body.textRuns.push({
				st: body.dataStream.length - text.length,
				ed: body.dataStream.length,
				ts: style
			});
		} else if (node instanceof HTMLElement && isHiddenClipboardElement(node)) continue;
		else if (node.nodeName === "IMG") {
			const element = node;
			const imageSourceType = element.dataset.imageSourceType ?? (element.src ? inferImageSourceType(element.src) : void 0);
			const source = imageSourceType === _univerjs_core.ImageSourceType.UUID ? element.dataset.source : element.src;
			if (source && imageSourceType) {
				var _doc$body;
				const width = readImageSize(element, "width") ?? 100;
				const height = readImageSize(element, "height") ?? 100;
				const docTransformWidth = readCssSize(element.dataset.docTransformWidth || null, null, "width") ?? width;
				const docTransformHeight = readCssSize(element.dataset.docTransformHeight || null, null, "height") ?? height;
				const id = (0, _univerjs_core.generateRandomId)(6);
				(_doc$body = doc.body) === null || _doc$body === void 0 || (_doc$body = _doc$body.customBlocks) === null || _doc$body === void 0 || _doc$body.push({
					startIndex: body.dataStream.length,
					blockId: id
				});
				body.dataStream += "\b";
				if (!doc.drawings) doc.drawings = {};
				doc.drawings[id] = {
					drawingId: id,
					title: "",
					description: "",
					imageSourceType,
					source,
					transform: {
						width,
						height,
						left: 0
					},
					docTransform: (0, _univerjs_docs.buildDocTransform)(docTransformWidth, docTransformHeight),
					layoutType: _univerjs_core.PositionedObjectLayoutType.INLINE,
					drawingType: _univerjs_core.DrawingTypeEnum.DRAWING_IMAGE,
					unitId: doc.id || "",
					subUnitId: doc.id || ""
				};
			}
		} else if (_univerjs_core.skipParseTagNames.includes(node.nodeName.toLowerCase())) continue;
		else if (node.nodeType === Node.ELEMENT_NODE) {
			const element = node;
			if (element.tagName.toUpperCase() === "TABLE") {
				this._processHtmlTable(element, doc);
				continue;
			}
			if (this._processCodeBlock(element, doc)) continue;
			const linkStart = this._processBeforeLink(element, doc);
			const blockStart = this._processBeforeStructuredBlock(element, doc);
			const listContext = this._processBeforeList(element);
			const parentStyles = parent ? this._styleCache.get(parent) : {};
			const styleRule = this._styleRules.find(({ filter }) => matchFilter(node, filter));
			const nodeStyles = styleRule ? styleRule.getStyle(node) : extractNodeStyle(node);
			this._styleCache.set(node, {
				...parentStyles,
				...nodeStyles
			});
			const { childNodes } = node;
			this._processBeforeTable(node, doc);
			this._process(node, childNodes, doc);
			this._processAfterTable(node, doc);
			this._processAfterList(listContext);
			const afterProcessRule = this._afterProcessRules.find(({ filter }) => matchFilter(node, filter));
			const paragraphAddedByPlugin = Boolean(afterProcessRule);
			if (afterProcessRule) afterProcessRule.handler(doc, node);
			this._processAfterDefaultBlock(element, doc, paragraphAddedByPlugin);
			this._processAfterStructuredBlock(element, doc, blockStart);
			this._processAfterLink(element, doc, linkStart);
		}
	}
	_processCodeBlock(node, doc) {
		if (node.tagName.toUpperCase() !== "PRE") return false;
		const body = doc.body;
		const startIndex = body.dataStream.length;
		body.dataStream += _univerjs_core.DataStreamTreeTokenType.BLOCK_START;
		const text = node.textContent ?? "";
		body.dataStream += text;
		this._appendParagraph(doc, node);
		body.dataStream += _univerjs_core.DataStreamTreeTokenType.BLOCK_END;
		body.blockRanges ??= [];
		body.blockRanges.push({
			blockId: (0, _univerjs_core.generateRandomId)(6),
			blockType: _univerjs_core.DocumentBlockRangeType.CODE,
			startIndex,
			endIndex: body.dataStream.length - 1
		});
		return true;
	}
	_processBeforeStructuredBlock(node, doc) {
		if (!getStructuredBlockType(node)) return null;
		const body = doc.body;
		const startIndex = body.dataStream.length;
		body.dataStream += _univerjs_core.DataStreamTreeTokenType.BLOCK_START;
		return startIndex;
	}
	_processAfterStructuredBlock(node, doc, startIndex) {
		const blockType = getStructuredBlockType(node);
		const body = doc.body;
		if (!blockType || startIndex == null || body.dataStream.length <= startIndex) return;
		body.dataStream += _univerjs_core.DataStreamTreeTokenType.BLOCK_END;
		body.blockRanges ??= [];
		body.blockRanges.push({
			blockId: (0, _univerjs_core.generateRandomId)(6),
			blockType,
			startIndex,
			endIndex: body.dataStream.length - 1
		});
	}
	_processBeforeList(node) {
		const tagName = node.tagName.toUpperCase();
		if (tagName !== "OL" && tagName !== "UL") return null;
		const context = {
			listId: (0, _univerjs_core.generateRandomId)(6),
			listType: tagName === "OL" ? _univerjs_core.PresetListType.ORDER_LIST : _univerjs_core.PresetListType.BULLET_LIST,
			nestingLevel: this._listStack.length
		};
		this._listStack.push(context);
		return context;
	}
	_processAfterList(context) {
		if (!context) return;
		this._listStack.pop();
	}
	_processAfterDefaultBlock(node, doc, paragraphAddedByPlugin) {
		if (paragraphAddedByPlugin) {
			this._applyListInfo(node, doc);
			this._lastParagraphIndex = doc.body.dataStream.length - 1;
			return;
		}
		if (!isDefaultParagraphElement(node)) return;
		this._appendParagraph(doc, node);
	}
	_appendParagraph(doc, node) {
		const body = doc.body;
		if (body.dataStream[this._lastParagraphIndex] === "\r" && this._lastParagraphIndex === body.dataStream.length - 1) return;
		body.paragraphs ??= [];
		const paragraph = {
			startIndex: body.dataStream.length,
			paragraphId: this._createParagraphId(body)
		};
		const heading = getHeadingNamedStyleType(node);
		if (heading != null) paragraph.paragraphStyle = {
			...paragraph.paragraphStyle,
			headingId: (0, _univerjs_core.generateRandomId)(6),
			namedStyleType: heading
		};
		const listContext = this._listStack[this._listStack.length - 1] ?? extractWordListInfo(node);
		if (listContext) {
			paragraph.bullet = {
				listId: listContext.listId,
				listType: listContext.listType,
				nestingLevel: listContext.nestingLevel
			};
			paragraph.startIndex -= stripListMarkerFromCurrentParagraph(body);
		}
		body.paragraphs.push(paragraph);
		body.dataStream += "\r";
		this._lastParagraphIndex = body.dataStream.length - 1;
	}
	_applyListInfo(node, doc) {
		var _doc$body2;
		const htmlListContext = this._listStack[this._listStack.length - 1];
		const wordListContext = extractWordListInfo(node);
		const listContext = htmlListContext ?? wordListContext;
		const paragraph = (_doc$body2 = doc.body) === null || _doc$body2 === void 0 || (_doc$body2 = _doc$body2.paragraphs) === null || _doc$body2 === void 0 ? void 0 : _doc$body2[doc.body.paragraphs.length - 1];
		if (!listContext || !paragraph) return;
		paragraph.bullet = {
			listId: listContext.listId,
			listType: listContext.listType,
			nestingLevel: listContext.nestingLevel
		};
		if (wordListContext) paragraph.startIndex -= stripListMarkerFromCurrentParagraph(doc.body);
	}
	_processHtmlTable(node, doc) {
		const body = doc.body;
		if (body.dataStream[body.dataStream.length - 1] !== "\r") {
			body.dataStream += "\r";
			body.paragraphs ??= [];
			body.paragraphs.push({
				startIndex: body.dataStream.length - 1,
				paragraphId: this._createParagraphId(body)
			});
		}
		doc.tableSource ??= {};
		body.tables ??= [];
		body.sectionBreaks ??= [];
		const rows = collectHtmlTableRows(node);
		const grid = buildHtmlTableGrid(rows);
		const columnCount = Math.max(1, ...grid.map((row) => row.length));
		const table = genTableSource(0, columnCount, DEFAULT_TABLE_WIDTH);
		const tableWidth = readCssSize(node.getAttribute("width"), node.getAttribute("style"), "width");
		const columnWidths = resolveHtmlTableColumnWidths(node, grid, columnCount, tableWidth);
		table.tableRows = [];
		table.tableColumns = columnWidths.map((width) => getTableColumn(width));
		table.size = {
			type: _univerjs_core.TableSizeType.SPECIFIED,
			width: { v: tableWidth ?? table.tableColumns.reduce((sum, column) => sum + column.size.width.v, 0) }
		};
		const startIndex = body.dataStream.length;
		body.dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_START;
		grid.forEach((row, rowIndex) => {
			const tableRow = getEmptyTableRow(0);
			const rowElement = rows[rowIndex];
			const height = rowElement ? readCssSize(rowElement.getAttribute("height"), rowElement.getAttribute("style"), "height") : void 0;
			if (height != null) tableRow.trHeight = {
				val: { v: height },
				hRule: _univerjs_core.TableRowHeightRule.EXACT
			};
			table.tableRows.push(tableRow);
			body.dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_ROW_START;
			for (let columnIndex = 0; columnIndex < columnCount; columnIndex++) {
				const parsedCell = row[columnIndex] ?? {
					element: null,
					rowSpan: 1,
					colSpan: 1,
					covered: true
				};
				const tableCell = parsedCell.covered ? {
					...getEmptyTableCell(),
					rowSpan: 0,
					columnSpan: 0
				} : createTableCellFromHtml(parsedCell.element, parsedCell.rowSpan, parsedCell.colSpan);
				tableRow.tableCells.push(tableCell);
				this._appendHtmlTableCell(parsedCell, doc);
			}
			body.dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_ROW_END;
		});
		doc.tableSource[table.tableId] = table;
		body.dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_END;
		body.tables.push({
			startIndex,
			endIndex: body.dataStream.length,
			tableId: table.tableId
		});
	}
	_appendHtmlTableCell(cell, doc) {
		const body = doc.body;
		body.dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_CELL_START;
		if (cell.element && !cell.covered) this._process(cell.element, cell.element.childNodes, doc);
		if (body.dataStream[body.dataStream.length - 1] !== "\r") this._appendParagraph(doc, cell.element ?? document.createElement("td"));
		body.sectionBreaks ??= [];
		body.sectionBreaks.push({
			sectionId: this._createSectionId(body),
			startIndex: body.dataStream.length
		});
		body.dataStream += `\n${_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_END}`;
	}
	_processBeforeTable(node, doc) {
		const tagName = node.tagName.toUpperCase();
		const body = doc.body;
		switch (tagName) {
			case "TABLE": {
				if (body.dataStream[body.dataStream.length - 1] !== "\r") {
					var _body$paragraphs2;
					body.dataStream += "\r";
					if (body.paragraphs == null) body.paragraphs = [];
					(_body$paragraphs2 = body.paragraphs) === null || _body$paragraphs2 === void 0 || _body$paragraphs2.push({
						startIndex: body.dataStream.length - 1,
						paragraphId: this._createParagraphId(body)
					});
				}
				const table = genTableSource(0, 0, DEFAULT_TABLE_WIDTH);
				this._tableCache.push({
					table,
					startIndex: body.dataStream.length
				});
				body.dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_START;
				break;
			}
			case "TR": {
				const row = getEmptyTableRow(0);
				this._tableCache[this._tableCache.length - 1].table.tableRows.push(row);
				body.dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_ROW_START;
				break;
			}
			case "TD": {
				const cell = getEmptyTableCell();
				const lastTable = this._tableCache[this._tableCache.length - 1].table;
				lastTable.tableRows[lastTable.tableRows.length - 1].tableCells.push(cell);
				body.dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_CELL_START;
				break;
			}
		}
	}
	_processAfterTable(node, doc) {
		const tagName = node.tagName.toUpperCase();
		const body = doc.body;
		if (doc.tableSource == null) doc.tableSource = {};
		if (body.tables == null) body.tables = [];
		if (body.sectionBreaks == null) body.sectionBreaks = [];
		const { tableSource } = doc;
		switch (tagName) {
			case "TABLE": {
				const { startIndex, table } = this._tableCache.pop();
				const colCount = table.tableRows[0].tableCells.length;
				const tableColumn = getTableColumn(DEFAULT_TABLE_WIDTH / colCount);
				table.tableColumns = [...new Array(colCount).fill(null).map(() => _univerjs_core.Tools.deepClone(tableColumn))];
				tableSource[table.tableId] = table;
				body.dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_END;
				body.tables.push({
					startIndex,
					endIndex: body.dataStream.length,
					tableId: table.tableId
				});
				break;
			}
			case "TR":
				body.dataStream += _univerjs_core.DataStreamTreeTokenType.TABLE_ROW_END;
				break;
			case "TD":
				var _body$sectionBreaks2;
				if (body.dataStream[body.dataStream.length - 1] !== "\r") {
					var _body$paragraphs3;
					(_body$paragraphs3 = body.paragraphs) === null || _body$paragraphs3 === void 0 || _body$paragraphs3.push({
						startIndex: body.dataStream.length,
						paragraphId: this._createParagraphId(body)
					});
					body.dataStream += "\r";
				}
				(_body$sectionBreaks2 = body.sectionBreaks) === null || _body$sectionBreaks2 === void 0 || _body$sectionBreaks2.push({
					sectionId: this._createSectionId(body),
					startIndex: body.dataStream.length
				});
				body.dataStream += `\n${_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_END}`;
				break;
		}
	}
	_processBeforeLink(node, doc) {
		return doc.body.dataStream.length;
	}
	_processAfterLink(node, doc, start) {
		const body = doc.body;
		const element = node;
		if (element.tagName.toUpperCase() === "A") {
			const href = element.href;
			if (!(0, _univerjs_core.isSafeUrl)(href)) return;
			body.customRanges = body.customRanges ?? [];
			body.customRanges.push({
				startIndex: start,
				endIndex: body.dataStream.length - 1,
				rangeId: element.dataset.rangeid ?? (0, _univerjs_core.generateRandomId)(),
				rangeType: _univerjs_core.CustomRangeType.HYPERLINK,
				properties: { url: href }
			});
		}
	}
};
_defineProperty(HtmlToUDMService, "_pluginList", []);
function collectHtmlTableRows(table) {
	const rows = [];
	Array.from(table.children).forEach((child) => {
		const tagName = child.tagName.toUpperCase();
		if (tagName === "TR") rows.push(child);
		else if (tagName === "TBODY" || tagName === "THEAD" || tagName === "TFOOT") rows.push(...Array.from(child.children).filter((row) => row.tagName.toUpperCase() === "TR"));
	});
	return rows;
}
function normalizeTextNode(node) {
	const value = node.nodeValue ?? "";
	if (value.includes(WORD_SPACE_PLACEHOLDER)) return value.replace(/[\t\u00A0]+/g, " ").replace(/[ \r\n]+/g, " ").replace(/ *\uE000+ */g, (match) => " ".repeat(Array.from(match).filter((char) => char === WORD_SPACE_PLACEHOLDER).length));
	const hasExplicitSpacing = /[\t\u00A0]/.test(value);
	const text = value.replace(/[\t\u00A0]+/g, " ").replace(/[ \r\n]+/g, " ");
	if (text.trim()) return text;
	if (!hasExplicitSpacing) return /[ \r\n]/.test(value) && hasInlineTextSibling(node.previousSibling, "previous") && hasInlineTextSibling(node.nextSibling, "next") ? " " : "";
	return hasTextSibling(node.previousSibling, "previous") && hasTextSibling(node.nextSibling, "next") ? " " : "";
}
function inferImageSourceType(src) {
	return src.startsWith("data:image/") ? _univerjs_core.ImageSourceType.BASE64 : _univerjs_core.ImageSourceType.URL;
}
function hasTextSibling(node, direction) {
	let current = node;
	while (current) {
		var _current$nodeValue;
		if (current.nodeType === Node.TEXT_NODE && ((_current$nodeValue = current.nodeValue) === null || _current$nodeValue === void 0 ? void 0 : _current$nodeValue.trim())) return true;
		if (current.nodeType === Node.ELEMENT_NODE && (current.textContent ?? "").trim()) return true;
		current = direction === "previous" ? current.previousSibling : current.nextSibling;
	}
	return false;
}
function hasInlineTextSibling(node, direction) {
	let current = node;
	while (current) {
		var _current$nodeValue2;
		if (current.nodeType === Node.TEXT_NODE && ((_current$nodeValue2 = current.nodeValue) === null || _current$nodeValue2 === void 0 ? void 0 : _current$nodeValue2.trim())) return true;
		if (current.nodeType === Node.ELEMENT_NODE) {
			var _element$textContent;
			const element = current;
			return !isDefaultParagraphElement(element) && ![
				"BR",
				"OL",
				"PRE",
				"TABLE",
				"UL"
			].includes(element.tagName) && Boolean((_element$textContent = element.textContent) === null || _element$textContent === void 0 ? void 0 : _element$textContent.trim());
		}
		current = direction === "previous" ? current.previousSibling : current.nextSibling;
	}
	return false;
}
function buildHtmlTableGrid(rows) {
	const grid = [];
	const occupied = /* @__PURE__ */ new Set();
	rows.forEach((row, rowIndex) => {
		const parsedRow = [];
		let columnIndex = 0;
		getHtmlTableCells(row).forEach((cellElement) => {
			while (occupied.has(`${rowIndex}:${columnIndex}`)) {
				parsedRow[columnIndex] = {
					element: null,
					rowSpan: 1,
					colSpan: 1,
					covered: true
				};
				columnIndex++;
			}
			const rowSpan = Math.max(1, Number(cellElement.getAttribute("rowspan") ?? 1));
			const colSpan = Math.max(1, Number(cellElement.getAttribute("colspan") ?? 1));
			parsedRow[columnIndex] = {
				element: cellElement,
				rowSpan,
				colSpan
			};
			for (let rowOffset = 0; rowOffset < rowSpan; rowOffset++) for (let columnOffset = 0; columnOffset < colSpan; columnOffset++) {
				if (rowOffset === 0 && columnOffset === 0) continue;
				occupied.add(`${rowIndex + rowOffset}:${columnIndex + columnOffset}`);
				if (rowOffset === 0) parsedRow[columnIndex + columnOffset] = {
					element: null,
					rowSpan: 1,
					colSpan: 1,
					covered: true
				};
			}
			columnIndex += colSpan;
		});
		while (occupied.has(`${rowIndex}:${columnIndex}`)) {
			parsedRow[columnIndex] = {
				element: null,
				rowSpan: 1,
				colSpan: 1,
				covered: true
			};
			columnIndex++;
		}
		grid.push(parsedRow);
	});
	const columnCount = Math.max(0, ...grid.map((row) => row.length));
	grid.forEach((row) => {
		while (row.length < columnCount) row.push({
			element: null,
			rowSpan: 1,
			colSpan: 1,
			covered: true
		});
	});
	return grid.length ? grid : [[]];
}
function getHtmlTableCells(row) {
	return Array.from(row.children).filter((element) => {
		const tagName = element.tagName.toUpperCase();
		return tagName === "TD" || tagName === "TH";
	});
}
function collectHtmlTableColumnWidths(table, grid, columnCount) {
	const widths = Array.from({ length: columnCount });
	Array.from(table.querySelectorAll("col")).forEach((col, index) => {
		if (index < widths.length) widths[index] = readCssSize(col.getAttribute("width"), col.getAttribute("style"), "width");
	});
	grid.forEach((row) => {
		row.forEach((cell, columnIndex) => {
			if (!cell.element || widths[columnIndex] != null) return;
			const width = readCssSize(cell.element.getAttribute("width"), cell.element.getAttribute("style"), "width");
			if (width != null && cell.colSpan <= 1) widths[columnIndex] = width;
		});
	});
	return widths;
}
function resolveHtmlTableColumnWidths(table, grid, columnCount, tableWidth) {
	const explicitWidths = collectHtmlTableColumnWidths(table, grid, columnCount);
	const fallbackTableWidth = tableWidth ?? DEFAULT_TABLE_WIDTH;
	const explicitTotal = explicitWidths.reduce((sum, width) => sum + (width ?? 0), 0);
	const missingCount = explicitWidths.filter((width) => width == null).length;
	const fallbackColumnWidth = missingCount ? Math.max((fallbackTableWidth - explicitTotal) / missingCount, 1) : fallbackTableWidth / columnCount;
	return explicitWidths.map((width) => width ?? roundCssNumber$1(fallbackColumnWidth));
}
function createTableCellFromHtml(element, rowSpan, colSpan) {
	const cell = getEmptyTableCell();
	if (rowSpan > 1) cell.rowSpan = rowSpan;
	if (colSpan > 1) cell.columnSpan = colSpan;
	const style = element.getAttribute("style") ?? "";
	const width = readCssSize(element.getAttribute("width"), style, "width");
	const backgroundColor = readCssColor(style, "background-color") ?? readCssColor(style, "background") ?? readHtmlAttributeColor(element, "bgcolor");
	const borderTop = createTableCellBorder(style, "top");
	const borderRight = createTableCellBorder(style, "right");
	const borderBottom = createTableCellBorder(style, "bottom");
	const borderLeft = createTableCellBorder(style, "left");
	if (width != null) cell.size = {
		type: _univerjs_core.TableSizeType.SPECIFIED,
		width: { v: width }
	};
	if (backgroundColor) cell.backgroundColor = { rgb: backgroundColor };
	if (borderTop) cell.borderTop = borderTop;
	if (borderRight) cell.borderRight = borderRight;
	if (borderBottom) cell.borderBottom = borderBottom;
	if (borderLeft) cell.borderLeft = borderLeft;
	return cell;
}
function createTableCellBorder(style, side) {
	var _sideStyle$match, _readCssValue;
	const sideStyle = readCssValue(style, `border-${side}`);
	const color = readCssColor(style, `border-${side}-color`) ?? normalizeCssColor$1(sideStyle === null || sideStyle === void 0 || (_sideStyle$match = sideStyle.match(/(#[0-9a-f]{3,8}|rgb\([^)]+\))/i)) === null || _sideStyle$match === void 0 ? void 0 : _sideStyle$match[1]) ?? readCssColor(style, "border-color") ?? normalizeCssColor$1((_readCssValue = readCssValue(style, "border")) === null || _readCssValue === void 0 || (_readCssValue = _readCssValue.match(/(#[0-9a-f]{3,8}|rgb\([^)]+\))/i)) === null || _readCssValue === void 0 ? void 0 : _readCssValue[1]) ?? readNamedBorderColor(style);
	const width = readCssLengthValue(readCssValue(style, `border-${side}-width`)) ?? readCssLengthValue(sideStyle) ?? readCssLengthValue(readCssValue(style, "border-width")) ?? readCssLengthValue(readCssValue(style, "border"));
	if (!color && width == null) return;
	return {
		color: { rgb: normalizeCssColor$1(color) ?? "#1f1f1f" },
		width: { v: width ?? 1 }
	};
}
function readNamedBorderColor(style) {
	const match = style.match(/border(?:-[a-z]+)?\s*:\s*([^;]+)/i);
	if (!match) return;
	return normalizeCssColor$1(match[1].split(/\s+/).find((part) => {
		const normalized = part.toLowerCase();
		return normalized && !/^[0-9.]+/.test(normalized) && ![
			"none",
			"hidden",
			"dotted",
			"dashed",
			"solid",
			"double",
			"groove",
			"ridge",
			"inset",
			"outset"
		].includes(normalized);
	}));
}
function readHtmlAttributeColor(element, attributeName) {
	return normalizeCssColor$1(element.getAttribute(attributeName));
}
function readCssColor(style, property) {
	return normalizeCssColor$1(readCssValue(style, property));
}
function normalizeCssColor$1(value) {
	if (!value) return;
	try {
		const color = new _univerjs_core.ColorKit(value.trim());
		return color.isValid ? color.toRgbString() : void 0;
	} catch {
		return;
	}
}
function readCssValue(style, property) {
	const normalizedProperty = property.toLowerCase();
	const declaration = style.split(";").map((part) => part.split(":")).find(([name]) => (name === null || name === void 0 ? void 0 : name.trim().toLowerCase()) === normalizedProperty);
	return (declaration === null || declaration === void 0 ? void 0 : declaration.slice(1).join(":").trim()) || void 0;
}
function readCssSize(attributeValue, style, property) {
	var _readCssValue2;
	const styleMatch = (_readCssValue2 = readCssValue(style ?? "", property)) === null || _readCssValue2 === void 0 ? void 0 : _readCssValue2.match(/^([0-9.]+)\s*(px|pt|in|cm|mm)?$/i);
	if (styleMatch) return toPixels(Number(styleMatch[1]), styleMatch[2]);
	const attrMatch = attributeValue === null || attributeValue === void 0 ? void 0 : attributeValue.match(/([0-9.]+)\s*(px|pt|in|cm|mm)?/i);
	return attrMatch ? toPixels(Number(attrMatch[1]), attrMatch[2]) : void 0;
}
function readCssLengthValue(value) {
	const match = (value === null || value === void 0 ? void 0 : value.match(/(?:^|\s)([0-9.]+)\s*(px|pt|in|cm|mm)(?=\s|$)/i)) ?? (value === null || value === void 0 ? void 0 : value.match(/^\s*([0-9.]+)\s*$/));
	return match ? toPixels(Number(match[1]), match[2]) : void 0;
}
function readImageSize(element, property) {
	return readCssSize((property === "width" ? element.dataset.width : element.dataset.height) || null, null, property) ?? readCssSize(element.getAttribute(property), element.getAttribute("style"), property);
}
function toPixels(value, unit) {
	switch (unit === null || unit === void 0 ? void 0 : unit.toLowerCase()) {
		case "pt": return roundCssNumber$1(value * 4 / 3);
		case "in": return roundCssNumber$1(value * 96);
		case "cm": return roundCssNumber$1(value * 96 / 2.54);
		case "mm": return roundCssNumber$1(value * 96 / 25.4);
		default: return roundCssNumber$1(value);
	}
}
function roundCssNumber$1(value) {
	return Math.round(value * 100) / 100;
}
function getStructuredBlockType(node) {
	const docType = node.dataset.docType;
	if (docType === _univerjs_core.DocumentBlockRangeType.QUOTE) return _univerjs_core.DocumentBlockRangeType.QUOTE;
	if (docType === _univerjs_core.DocumentBlockRangeType.CALLOUT) return _univerjs_core.DocumentBlockRangeType.CALLOUT;
	const tagName = node.tagName.toUpperCase();
	if (tagName === "BLOCKQUOTE") return docType === _univerjs_core.DocumentBlockRangeType.CALLOUT ? _univerjs_core.DocumentBlockRangeType.CALLOUT : _univerjs_core.DocumentBlockRangeType.QUOTE;
	if (tagName === "ASIDE" && node.getAttribute("role") === "note") return _univerjs_core.DocumentBlockRangeType.CALLOUT;
	return null;
}
function isHiddenClipboardElement(node) {
	var _node$getAttribute;
	if (node.hidden || ((_node$getAttribute = node.getAttribute("aria-hidden")) === null || _node$getAttribute === void 0 ? void 0 : _node$getAttribute.toLowerCase()) === "true") return true;
	const classes = node.classList;
	if (classes.contains("sr-only") || classes.contains("visually-hidden") || classes.contains("screen-reader-only")) return true;
	const style = node.getAttribute("style") ?? "";
	return /(?:^|;)\s*display\s*:\s*none(?:\s*!important)?\s*(?:;|$)/i.test(style) || /(?:^|;)\s*visibility\s*:\s*(?:hidden|collapse)(?:\s*!important)?\s*(?:;|$)/i.test(style);
}
function isDefaultParagraphElement(node) {
	const tagName = node.tagName.toUpperCase();
	return [
		"P",
		"DIV",
		"H1",
		"H2",
		"H3",
		"H4",
		"H5",
		"H6",
		"LI"
	].includes(tagName);
}
function getHeadingNamedStyleType(node) {
	switch (node.tagName.toUpperCase()) {
		case "H1": return _univerjs_core.NamedStyleType.HEADING_1;
		case "H2": return _univerjs_core.NamedStyleType.HEADING_2;
		case "H3": return _univerjs_core.NamedStyleType.HEADING_3;
		case "H4": return _univerjs_core.NamedStyleType.HEADING_4;
		case "H5": return _univerjs_core.NamedStyleType.HEADING_5;
		default: return null;
	}
}
function extractWordListInfo(node) {
	const match = (node.getAttribute("style") ?? "").match(/mso-list:\s*([^ ;]+)\s+level(\d+)/i);
	if (!match && !/MsoListParagraph/i.test(node.className)) return null;
	{
		const marker = (node.textContent ?? "").trim().match(/^([0-9]+[.)]|[a-zA-Z][.)]|[ivxlcdmIVXLCDM]+[.)]|[\u2022\u00B7\u25CF\u25CB\u25AA\u25AB-])/);
		const markerText = (marker === null || marker === void 0 ? void 0 : marker[1]) ?? "";
		const ordered = Boolean(markerText && !/^[\u2022\u00B7\u25CF\u25CB\u25AA\u25AB-]$/.test(markerText));
		return {
			listId: (match === null || match === void 0 ? void 0 : match[1]) ?? "mso-list",
			listType: ordered ? _univerjs_core.PresetListType.ORDER_LIST : _univerjs_core.PresetListType.BULLET_LIST,
			nestingLevel: Math.max(0, Number((match === null || match === void 0 ? void 0 : match[2]) ?? 1) - 1)
		};
	}
}
function stripListMarkerFromCurrentParagraph(body) {
	var _body$textRuns2;
	const paragraphEnd = body.dataStream.endsWith("\r") ? body.dataStream.length - 1 : body.dataStream.length;
	const paragraphStart = Math.max(0, body.dataStream.lastIndexOf("\r", paragraphEnd - 1) + 1);
	const text = body.dataStream.slice(paragraphStart, paragraphEnd);
	{
		const marker = text.match(/^(\s*(?:[0-9]+[.)]|[a-zA-Z][.)]|[ivxlcdmIVXLCDM]+[.)]|[\u2022\u00B7\u25CF\u25CB\u25AA\u25AB-])\s*)/);
		if (marker) {
			var _body$textRuns;
			const length = marker[1].length;
			body.dataStream = body.dataStream.slice(0, paragraphStart) + body.dataStream.slice(paragraphStart + length);
			(_body$textRuns = body.textRuns) === null || _body$textRuns === void 0 || _body$textRuns.forEach((textRun) => {
				if (textRun.st >= paragraphStart + length) {
					textRun.st -= length;
					textRun.ed -= length;
				} else if (textRun.ed > paragraphStart) textRun.ed = Math.max(textRun.st, textRun.ed - length);
			});
			return length;
		}
	}
	const marker = text.match(/^(\s*(?:[0-9]+\.|[a-zA-Z]\.|[ivxlcdmIVXLCDM]+\.|[·•●○\-])\s*)/);
	if (!marker) return 0;
	const length = marker[1].length;
	body.dataStream = body.dataStream.slice(0, paragraphStart) + body.dataStream.slice(paragraphStart + length);
	(_body$textRuns2 = body.textRuns) === null || _body$textRuns2 === void 0 || _body$textRuns2.forEach((textRun) => {
		if (textRun.st >= paragraphStart + length) {
			textRun.st -= length;
			textRun.ed -= length;
		} else if (textRun.ed > paragraphStart) textRun.ed = Math.max(textRun.st, textRun.ed - length);
	});
	return length;
}

//#endregion
//#region src/services/clipboard/html-to-udm/paste-plugins/plugin-lark.ts
const LarkPastePlugin = {
	name: "univer-doc-paste-plugin-lark",
	checkPasteType(html) {
		return /lark-record-clipboard/i.test(html);
	},
	stylesRules: [{
		filter: ["s"],
		getStyle(node) {
			const inlineStyle = extractNodeStyle(node);
			return {
				st: { s: _univerjs_core.BooleanNumber.TRUE },
				...inlineStyle
			};
		}
	}],
	afterProcessRules: [{
		filter(el) {
			return el.tagName === "DIV" && /ace-line/i.test(el.className);
		},
		handler(doc) {
			const body = doc.body;
			if (body.paragraphs == null) body.paragraphs = [];
			body.paragraphs.push({
				startIndex: body.dataStream.length,
				paragraphId: (0, _univerjs_core.createParagraphId)(new Set(body.paragraphs.map((paragraph) => paragraph.paragraphId)))
			});
			body.dataStream += "\r";
		}
	}]
};

//#endregion
//#region src/services/clipboard/html-to-udm/utils.ts
function getParagraphStyle(el) {
	const styles = el.style;
	const paragraphStyle = {};
	for (let i = 0; i < styles.length; i++) {
		const cssRule = styles[i];
		const cssValue = styles.getPropertyValue(cssRule);
		switch (cssRule) {
			case "margin-top": {
				const marginTopValue = Number.parseInt(cssValue);
				paragraphStyle.spaceAbove = { v: /pt/.test(cssValue) ? (0, _univerjs_engine_render.ptToPixel)(marginTopValue) : marginTopValue };
				break;
			}
			case "margin-bottom": {
				const marginBottomValue = Number.parseInt(cssValue);
				paragraphStyle.spaceBelow = { v: /pt/.test(cssValue) ? (0, _univerjs_engine_render.ptToPixel)(marginBottomValue) : marginBottomValue };
				break;
			}
			case "line-height": {
				let lineHeightValue = Number.parseFloat(cssValue);
				if (cssValue.endsWith("%")) lineHeightValue /= 100;
				paragraphStyle.lineSpacing = lineHeightValue;
				break;
			}
			case "text-align":
				paragraphStyle.horizontalAlign = getHorizontalAlign(cssValue);
				break;
			default: break;
		}
	}
	return Object.getOwnPropertyNames(paragraphStyle).length ? paragraphStyle : null;
}
function getHorizontalAlign(value) {
	switch (value.trim().toLowerCase()) {
		case "center": return _univerjs_core.HorizontalAlign.CENTER;
		case "right":
		case "end": return _univerjs_core.HorizontalAlign.RIGHT;
		case "justify": return _univerjs_core.HorizontalAlign.JUSTIFIED;
		case "left":
		case "start": return _univerjs_core.HorizontalAlign.LEFT;
		default: return _univerjs_core.HorizontalAlign.UNSPECIFIED;
	}
}

//#endregion
//#region src/services/clipboard/html-to-udm/paste-plugins/plugin-univer.ts
const UniverPastePlugin = {
	name: "univer-doc-paste-plugin-univer",
	checkPasteType(html) {
		return /UniverNormal/i.test(html);
	},
	stylesRules: [],
	afterProcessRules: [{
		filter(el) {
			return el.tagName === "P" && /UniverNormal/i.test(el.className);
		},
		handler(doc, el) {
			const body = doc.body;
			if (body.paragraphs == null) body.paragraphs = [];
			const paragraph = {
				startIndex: body.dataStream.length,
				paragraphId: (0, _univerjs_core.createParagraphId)(new Set(body.paragraphs.map((p) => p.paragraphId)))
			};
			const paragraphStyle = getParagraphStyle(el);
			if (paragraphStyle) paragraph.paragraphStyle = paragraphStyle;
			body.paragraphs.push(paragraph);
			body.dataStream += "\r";
		}
	}]
};

//#endregion
//#region src/services/clipboard/html-to-udm/paste-plugins/plugin-word.ts
const WORD_SPACE_PLACEHOLDER_ENTITY = "&#57344;";
const WORD_TAB_WIDTH = 4;
const WordPastePlugin = {
	name: "univer-doc-paste-plugin-word",
	checkPasteType(html) {
		return /word|mso|wps|kingsoft/i.test(html);
	},
	preprocessHtml(html) {
		return html.replace(/<span\b([^>]*)>([\s\S]*?)<\/span>/gi, (match, attributes, content) => {
			const isWordSpacer = /mso-spacerun\s*:\s*yes/i.test(attributes);
			const tabCount = readMsoTabCount(attributes);
			if (!isWordSpacer && tabCount == null) return match;
			const contentSpaceCount = countWordSpaces(content);
			const fallbackSpaceCount = tabCount == null ? 1 : Math.max(1, Math.round(tabCount * WORD_TAB_WIDTH));
			const spaceCount = Math.max(contentSpaceCount, fallbackSpaceCount);
			return WORD_SPACE_PLACEHOLDER_ENTITY.repeat(spaceCount);
		});
	},
	stylesRules: [{
		filter: ["b"],
		getStyle(node) {
			const inlineStyle = extractNodeStyle(node);
			return {
				bl: _univerjs_core.BooleanNumber.TRUE,
				...inlineStyle
			};
		}
	}],
	afterProcessRules: [{
		filter(el) {
			return el.tagName === "P";
		},
		handler(doc, el) {
			const body = doc.body;
			if (body.paragraphs == null) body.paragraphs = [];
			const paragraph = {
				startIndex: body.dataStream.length,
				paragraphId: (0, _univerjs_core.createParagraphId)(new Set(body.paragraphs.map((p) => p.paragraphId)))
			};
			const paragraphStyle = getParagraphStyle(el);
			if (paragraphStyle) paragraph.paragraphStyle = paragraphStyle;
			body.paragraphs.push(paragraph);
			body.dataStream += "\r";
		}
	}]
};
function readMsoTabCount(attributes) {
	const match = attributes.match(/mso-tab-count\s*:\s*([0-9.]+)/i);
	if (!match) return;
	const count = Number(match[1]);
	return Number.isFinite(count) ? count : void 0;
}
function countWordSpaces(content) {
	var _content$match, _text$match, _text$replace$match;
	const entitySpaceCount = ((_content$match = content.match(/&nbsp;|&#160;|&#x0*a0;/gi)) === null || _content$match === void 0 ? void 0 : _content$match.length) ?? 0;
	const text = content.replace(/&nbsp;|&#160;|&#x0*a0;/gi, "\xA0").replace(/<[^>]*>/g, "");
	const literalSpaceCount = ((_text$match = text.match(/[\u00A0\t]/g)) === null || _text$match === void 0 ? void 0 : _text$match.reduce((count, char) => count + (char === "	" ? WORD_TAB_WIDTH : 1), 0)) ?? 0;
	if (entitySpaceCount || literalSpaceCount) return literalSpaceCount;
	return ((_text$replace$match = text.replace(/\r?\n/g, "").match(/ /g)) === null || _text$replace$match === void 0 ? void 0 : _text$replace$match.length) ?? 0;
}

//#endregion
//#region src/services/clipboard/internal-fragment.ts
const DOC_INTERNAL_FRAGMENT_MIME = "application/x-doc-fragment+json";
const DOC_INTERNAL_FRAGMENT_COMMENT_PREFIX = "univer-doc-fragment:";
function createInternalClipboardFragment(doc) {
	return JSON.stringify({
		version: 1,
		kind: "univer-doc-fragment",
		doc: _univerjs_core.Tools.deepClone(doc)
	});
}
function parseInternalClipboardFragment(value) {
	if (!value) return null;
	try {
		var _parsed$doc;
		const parsed = JSON.parse(value);
		if ((parsed === null || parsed === void 0 ? void 0 : parsed.version) === 1 && parsed.kind === "univer-doc-fragment" && ((_parsed$doc = parsed.doc) === null || _parsed$doc === void 0 ? void 0 : _parsed$doc.body)) return parsed.doc;
	} catch {
		return null;
	}
	return null;
}
function createInternalClipboardDocData(doc) {
	var _body$tables, _body$paragraphs, _body$customBlocks;
	const body = _univerjs_core.Tools.deepClone(doc.body);
	const internalDocData = { body };
	if ((body === null || body === void 0 || (_body$tables = body.tables) === null || _body$tables === void 0 ? void 0 : _body$tables.length) && doc.tableSource) {
		internalDocData.tableSource = {};
		body.tables.forEach((tableRange) => {
			var _doc$tableSource;
			const table = (_doc$tableSource = doc.tableSource) === null || _doc$tableSource === void 0 ? void 0 : _doc$tableSource[tableRange.tableId];
			if (table) internalDocData.tableSource[tableRange.tableId] = _univerjs_core.Tools.deepClone(table);
		});
	}
	const listTypes = new Set(body === null || body === void 0 || (_body$paragraphs = body.paragraphs) === null || _body$paragraphs === void 0 ? void 0 : _body$paragraphs.map((paragraph) => {
		var _paragraph$bullet;
		return (_paragraph$bullet = paragraph.bullet) === null || _paragraph$bullet === void 0 ? void 0 : _paragraph$bullet.listType;
	}).filter(Boolean));
	if (listTypes.size && doc.lists) {
		internalDocData.lists = {};
		listTypes.forEach((listType) => {
			var _doc$lists;
			const list = (_doc$lists = doc.lists) === null || _doc$lists === void 0 ? void 0 : _doc$lists[String(listType)];
			if (list) internalDocData.lists[String(listType)] = _univerjs_core.Tools.deepClone(list);
		});
	}
	if (body === null || body === void 0 || (_body$customBlocks = body.customBlocks) === null || _body$customBlocks === void 0 ? void 0 : _body$customBlocks.length) {
		internalDocData.drawings = {};
		for (const block of body.customBlocks) {
			var _doc$drawings;
			const { blockId } = block;
			const drawing = (_doc$drawings = doc.drawings) === null || _doc$drawings === void 0 ? void 0 : _doc$drawings[blockId];
			if (drawing) {
				const id = (0, _univerjs_core.generateRandomId)(6);
				block.blockId = id;
				internalDocData.drawings[id] = {
					..._univerjs_core.Tools.deepClone(drawing),
					drawingId: id
				};
			}
		}
	}
	return internalDocData;
}
function createInternalClipboardDocDataList(docs) {
	var _merged$body;
	if (docs.length === 0) return null;
	if (docs.length === 1) return createInternalClipboardDocData(docs[0]);
	const merged = { body: { dataStream: "" } };
	for (const doc of docs) {
		const part = createInternalClipboardDocData(doc);
		const body = part.body;
		if (!body) continue;
		const offset = merged.body.dataStream.length;
		merged.body.dataStream += body.dataStream ?? "";
		appendOffsetRanges(merged.body, body, offset);
		if (part.tableSource) {
			merged.tableSource ??= {};
			Object.assign(merged.tableSource, part.tableSource);
		}
		if (part.lists) {
			merged.lists ??= {};
			Object.assign(merged.lists, part.lists);
		}
		if (part.drawings) {
			merged.drawings ??= {};
			Object.assign(merged.drawings, part.drawings);
		}
	}
	return ((_merged$body = merged.body) === null || _merged$body === void 0 ? void 0 : _merged$body.dataStream) ? merged : null;
}
function embedInternalClipboardFragment(html, fragmentJson) {
	return `<!--${DOC_INTERNAL_FRAGMENT_COMMENT_PREFIX}${encodeBase64(fragmentJson)}-->${html}`;
}
function extractInternalClipboardFragmentFromHtml(html) {
	if (!html) return null;
	const escapedPrefix = DOC_INTERNAL_FRAGMENT_COMMENT_PREFIX.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
	const match = html.match(new RegExp(`<!--\\s*${escapedPrefix}([A-Za-z0-9+/=]+)\\s*-->`));
	if (!match) return null;
	return parseInternalClipboardFragment(decodeBase64(match[1]));
}
const WORD_CLIPBOARD_HTML_HEAD = `<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
<meta name="ProgId" content="Word.Document">
<meta name="Generator" content="Microsoft Word 15">
<!--[if gte mso 9]><xml><w:WordDocument><w:View>Normal</w:View><w:Zoom>0</w:Zoom><w:TrackFormatting/><w:DoNotPromoteQF/></w:WordDocument></xml><![endif]-->
<style><!--
p.MsoNormal, li.MsoNormal, div.MsoNormal{margin:0cm;margin-bottom:8.0pt;line-height:115%;font-size:11.0pt;font-family:Arial,sans-serif;mso-pagination:widow-orphan;}
p.UniverNormal, li.UniverNormal, div.UniverNormal, p.univernormal, li.univernormal, div.univernormal{mso-style-name:univernormal;margin-top:0cm;margin-right:0cm;margin-bottom:8.0pt;margin-left:0cm;line-height:115%;mso-pagination:widow-orphan;font-size:11.0pt;font-family:Arial,sans-serif;mso-bidi-font-family:Arial;}
p.UniverHeading, li.UniverHeading, div.UniverHeading, p.univerheading, li.univerheading, div.univerheading{mso-style-name:univerheading;margin-top:6.0pt;margin-right:0cm;margin-bottom:4.0pt;margin-left:0cm;mso-pagination:widow-orphan;font-size:12.0pt;font-family:Arial,sans-serif;mso-bidi-font-family:Arial;font-weight:bold;}
table.MsoNormalTable, table.UniverTable{mso-style-name:"Normal Table";mso-tstyle-rowband-size:0;mso-tstyle-colband-size:0;mso-style-noshow:yes;mso-style-priority:99;mso-style-parent:"";mso-padding-alt:0cm 5.4pt 0cm 5.4pt;mso-para-margin:0cm;mso-pagination:widow-orphan;font-size:11.0pt;font-family:Arial,sans-serif;border-collapse:collapse;mso-table-lspace:0pt;mso-table-rspace:0pt;}
td.UniverTableCell, th.UniverTableCell{mso-padding-alt:0cm 5.4pt 0cm 5.4pt;}
--></style>
</head>`;
function wrapClipboardHtml(fragmentHtml) {
	return `<html xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:w="urn:schemas-microsoft-com:office:word" xmlns="http://www.w3.org/TR/REC-html40">${WORD_CLIPBOARD_HTML_HEAD}<body lang="ZH-CN" style="tab-interval:21.0pt;word-wrap:break-word"><!--StartFragment-->${fragmentHtml}<!--EndFragment--></body></html>`;
}
function appendOffsetRanges(target, source, offset) {
	var _source$textRuns, _source$paragraphs, _source$sectionBreaks, _source$tables, _source$blockRanges, _source$customRanges, _source$customDecorat, _source$customBlocks;
	if ((_source$textRuns = source.textRuns) === null || _source$textRuns === void 0 ? void 0 : _source$textRuns.length) {
		target.textRuns ??= [];
		target.textRuns.push(...source.textRuns.map((textRun) => ({
			...textRun,
			st: textRun.st + offset,
			ed: textRun.ed + offset
		})));
	}
	if ((_source$paragraphs = source.paragraphs) === null || _source$paragraphs === void 0 ? void 0 : _source$paragraphs.length) {
		target.paragraphs ??= [];
		target.paragraphs.push(...source.paragraphs.map((paragraph) => ({
			...paragraph,
			startIndex: paragraph.startIndex + offset
		})));
	}
	if ((_source$sectionBreaks = source.sectionBreaks) === null || _source$sectionBreaks === void 0 ? void 0 : _source$sectionBreaks.length) {
		target.sectionBreaks ??= [];
		target.sectionBreaks.push(...source.sectionBreaks.map((sectionBreak) => ({
			...sectionBreak,
			startIndex: sectionBreak.startIndex + offset
		})));
	}
	if ((_source$tables = source.tables) === null || _source$tables === void 0 ? void 0 : _source$tables.length) {
		target.tables ??= [];
		target.tables.push(...source.tables.map((table) => ({
			...table,
			startIndex: table.startIndex + offset,
			endIndex: table.endIndex + offset
		})));
	}
	if ((_source$blockRanges = source.blockRanges) === null || _source$blockRanges === void 0 ? void 0 : _source$blockRanges.length) {
		target.blockRanges ??= [];
		target.blockRanges.push(...source.blockRanges.map((blockRange) => ({
			...blockRange,
			startIndex: blockRange.startIndex + offset,
			endIndex: blockRange.endIndex + offset
		})));
	}
	if ((_source$customRanges = source.customRanges) === null || _source$customRanges === void 0 ? void 0 : _source$customRanges.length) {
		target.customRanges ??= [];
		target.customRanges.push(...source.customRanges.map((customRange) => ({
			...customRange,
			startIndex: customRange.startIndex + offset,
			endIndex: customRange.endIndex + offset
		})));
	}
	if ((_source$customDecorat = source.customDecorations) === null || _source$customDecorat === void 0 ? void 0 : _source$customDecorat.length) {
		target.customDecorations ??= [];
		target.customDecorations.push(...source.customDecorations.map((customDecoration) => ({
			...customDecoration,
			startIndex: customDecoration.startIndex + offset,
			endIndex: customDecoration.endIndex + offset
		})));
	}
	if ((_source$customBlocks = source.customBlocks) === null || _source$customBlocks === void 0 ? void 0 : _source$customBlocks.length) {
		target.customBlocks ??= [];
		target.customBlocks.push(...source.customBlocks.map((customBlock) => ({
			...customBlock,
			startIndex: customBlock.startIndex + offset
		})));
	}
}
function encodeBase64(value) {
	return btoa(unescape(encodeURIComponent(value)));
}
function decodeBase64(value) {
	return decodeURIComponent(escape(atob(value)));
}

//#endregion
//#region src/services/clipboard/udm-to-html/convertor.ts
const DEFAULT_CLIPBOARD_FONT_FAMILY = "Arial";
function covertImageToHtml(item) {
	var _item$transform, _item$transform2;
	const transformObjectToString = (obj) => {
		let result = "";
		Object.keys(obj).forEach((key) => {
			if (obj[key] !== void 0) result += ` ${key}=${obj[key]}`;
		});
		return result;
	};
	return `<img  ${transformObjectToString({
		"data-doc-transform-height": item.docTransform.size.height,
		"data-doc-transform-width": item.docTransform.size.width,
		"data-width": (_item$transform = item.transform) === null || _item$transform === void 0 ? void 0 : _item$transform.width,
		"data-height": (_item$transform2 = item.transform) === null || _item$transform2 === void 0 ? void 0 : _item$transform2.height,
		"data-image-source-type": item.imageSourceType,
		"data-source": item.imageSourceType === _univerjs_core.ImageSourceType.UUID ? item.source : void 0,
		src: item.source
	})}></img>`;
}
function covertTextRunToHtml(dataStream, textRun) {
	const { st: start, ed, ts = {} } = textRun;
	const { ff, fs, it, bl, ul, st, ol, bg, cl, va } = ts;
	let html = escapeInlineText(dataStream.slice(start, ed));
	const style = [];
	if (it === _univerjs_core.BooleanNumber.TRUE) html = `<i>${html}</i>`;
	if (va === _univerjs_core.BaselineOffset.SUPERSCRIPT) html = `<sup>${html}</sup>`;
	else if (va === _univerjs_core.BaselineOffset.SUBSCRIPT) html = `<sub>${html}</sub>`;
	if ((ul === null || ul === void 0 ? void 0 : ul.s) === _univerjs_core.BooleanNumber.TRUE) html = `<u>${html}</u>`;
	if ((st === null || st === void 0 ? void 0 : st.s) === _univerjs_core.BooleanNumber.TRUE) html = `<s>${html}</s>`;
	if (bl === _univerjs_core.BooleanNumber.TRUE) html = `<strong>${html}</strong>`;
	style.push(`font-family: ${ff ?? DEFAULT_CLIPBOARD_FONT_FAMILY}`);
	if (cl === null || cl === void 0 ? void 0 : cl.rgb) style.push(`color: ${normalizeCssColor(cl.rgb)}`);
	if (fs) style.push(`font-size: ${fs}pt`);
	if (ol) style.push("text-decoration: overline");
	if (bg === null || bg === void 0 ? void 0 : bg.rgb) style.push(`background: ${normalizeCssColor(bg.rgb)}`);
	return style.length ? `<span style="${style.join("; ")};">${html}</span>` : html;
}
function getBodyInlineSlice(body, startIndex, endIndex) {
	const { dataStream, textRuns = [] } = body;
	if (startIndex === endIndex) return "";
	let cursorIndex = startIndex;
	const spanList = [];
	for (const textRun of textRuns) {
		const { st, ed } = textRun;
		if (_univerjs_core.Tools.hasIntersectionBetweenTwoRanges(startIndex, endIndex, st, ed)) if (st > cursorIndex) {
			spanList.push(escapeInlineText(dataStream.slice(cursorIndex, st)));
			spanList.push(covertTextRunToHtml(dataStream, {
				...textRun,
				ed: Math.min(ed, endIndex)
			}));
		} else spanList.push(covertTextRunToHtml(dataStream, {
			...textRun,
			st: cursorIndex,
			ed: Math.min(ed, endIndex)
		}));
		cursorIndex = Math.max(startIndex, Math.min(ed, endIndex));
	}
	if (cursorIndex !== endIndex) spanList.push(escapeInlineText(dataStream.slice(cursorIndex, endIndex)));
	return spanList.join("");
}
function getBodySliceHtml(doc, startIndex, endIndex) {
	const body = doc.body;
	const drawings = doc.drawings || {};
	const { customRanges = [], customBlocks = [] } = body || {};
	const cloneCustomBlocks = [...customBlocks];
	const customRangesInRange = customRanges.filter((range) => range.startIndex >= startIndex && range.endIndex <= endIndex);
	let cursorIndex = startIndex;
	let html = "";
	const handleCustomBlock = (startIndex, endIndex) => {
		let sliceHtml = "";
		let customBlockLength = 0;
		let handleCustomBlockCursorIndex = startIndex;
		let blockItemIndex = cloneCustomBlocks.findIndex((block) => startIndex <= block.startIndex && endIndex >= block.startIndex);
		if (blockItemIndex === -1) {
			sliceHtml = getBodyInlineSlice(body, startIndex, endIndex);
			return {
				sliceHtml,
				customBlockLength
			};
		}
		while (blockItemIndex !== -1) {
			const blockItem = cloneCustomBlocks[blockItemIndex];
			cloneCustomBlocks.splice(blockItemIndex, 1);
			sliceHtml += getBodyInlineSlice(body, handleCustomBlockCursorIndex, blockItem.startIndex);
			const drawingItem = drawings[blockItem.blockId];
			if (drawingItem) switch (drawingItem.drawingType) {
				case _univerjs_core.DrawingTypeEnum.DRAWING_IMAGE:
					sliceHtml += covertImageToHtml(drawingItem);
					customBlockLength++;
					break;
			}
			handleCustomBlockCursorIndex = blockItem.startIndex + 1;
			blockItemIndex = cloneCustomBlocks.findIndex((block) => handleCustomBlockCursorIndex <= block.startIndex && endIndex >= block.startIndex);
		}
		sliceHtml = sliceHtml + getBodyInlineSlice(body, handleCustomBlockCursorIndex, endIndex + 1);
		return {
			sliceHtml,
			customBlockLength
		};
	};
	customRangesInRange.forEach((range) => {
		const { startIndex, endIndex, rangeType, rangeId } = range;
		const preHtml = handleCustomBlock(cursorIndex, startIndex);
		html += preHtml.sliceHtml;
		const sliceHtml = handleCustomBlock(startIndex, endIndex + 1);
		switch (rangeType) {
			case _univerjs_core.CustomRangeType.HYPERLINK:
				var _range$properties;
				html += `<a data-rangeid="${rangeId}" href="${((_range$properties = range.properties) === null || _range$properties === void 0 ? void 0 : _range$properties.url) ?? ""}">${sliceHtml.sliceHtml}</a>`;
				break;
			default:
				html += sliceHtml.sliceHtml;
				break;
		}
		cursorIndex = endIndex + 1 + (preHtml.customBlockLength + sliceHtml.customBlockLength);
	});
	const endHtml = handleCustomBlock(cursorIndex, endIndex);
	html += endHtml.sliceHtml;
	return html;
}
function convertBodyToHtml(doc) {
	const body = doc.body || {};
	const { paragraphs = [], sectionBreaks = [] } = body;
	let { dataStream = "" } = body;
	if (!dataStream.endsWith("\r\n")) {
		dataStream += "\r\n";
		paragraphs.push({
			startIndex: dataStream.length - 2,
			paragraphId: (0, _univerjs_core.createParagraphId)(new Set(paragraphs.map((paragraph) => paragraph.paragraphId)))
		});
		sectionBreaks.push({
			sectionId: (0, _univerjs_core.createSectionId)(new Set(sectionBreaks.map((sectionBreak) => sectionBreak.sectionId))),
			startIndex: dataStream.length - 1
		});
		body.dataStream = dataStream;
		body.paragraphs = paragraphs;
		body.sectionBreaks = sectionBreaks;
	}
	const result = { html: "" };
	const nodeList = (0, _univerjs_engine_render.parseDataStreamToTree)(dataStream).sectionList;
	for (const node of nodeList) processNode(node, doc, result);
	closeListStack(result);
	return result.html;
}
function processNode(node, doc, result) {
	switch (node.nodeType) {
		case _univerjs_core.DataStreamTreeNodeType.SECTION_BREAK:
			for (const n of node.children) processNode(n, doc, result);
			break;
		case _univerjs_core.DataStreamTreeNodeType.PARAGRAPH: {
			var _doc$body;
			const { children, startIndex, endIndex } = node;
			const paragraph = ((_doc$body = doc.body) === null || _doc$body === void 0 || (_doc$body = _doc$body.paragraphs) === null || _doc$body === void 0 ? void 0 : _doc$body.find((p) => p.startIndex === endIndex)) ?? {};
			const { paragraphStyle = {} } = paragraph;
			const { horizontalAlign, spaceAbove, spaceBelow, lineSpacing } = paragraphStyle;
			const style = [];
			if (horizontalAlign != null) {
				const align = getTextAlign(horizontalAlign);
				if (align) style.push(`text-align: ${align}`);
			}
			if (spaceAbove != null) if (typeof spaceAbove === "number") style.push(`margin-top: ${spaceAbove}px`);
			else style.push(`margin-top: ${spaceAbove.v}px`);
			if (spaceBelow != null) if (typeof spaceBelow === "number") style.push(`margin-bottom: ${spaceBelow}px`);
			else style.push(`margin-bottom: ${spaceBelow.v}px`);
			if (lineSpacing != null) style.push(`line-height: ${lineSpacing}`);
			style.push(...serializeTextStyle(paragraphStyle.textStyle));
			const inlineHtml = applySemanticTextStyle(getBodySliceHtml(doc, startIndex, endIndex), paragraphStyle.textStyle);
			const childHtml = children.map((table) => {
				const childResult = { html: "" };
				processNode(table, doc, childResult);
				closeListStack(childResult);
				return childResult.html;
			}).join("");
			if (childHtml) {
				closeListStack(result);
				result.html += childHtml;
				break;
			}
			renderParagraphNodeHtml(doc, paragraph, startIndex, endIndex, inlineHtml, style, result);
			break;
		}
		case _univerjs_core.DataStreamTreeNodeType.TABLE: {
			var _doc$body2, _doc$tableSource, _table$tableColumns;
			const { children } = node;
			closeListStack(result);
			result.tableIndex ??= 0;
			result.tableStack ??= [];
			const tableRange = (_doc$body2 = doc.body) === null || _doc$body2 === void 0 || (_doc$body2 = _doc$body2.tables) === null || _doc$body2 === void 0 ? void 0 : _doc$body2[result.tableIndex++];
			const table = tableRange ? (_doc$tableSource = doc.tableSource) === null || _doc$tableSource === void 0 ? void 0 : _doc$tableSource[tableRange.tableId] : void 0;
			const width = table === null || table === void 0 || (_table$tableColumns = table.tableColumns) === null || _table$tableColumns === void 0 ? void 0 : _table$tableColumns.reduce((sum, column) => {
				var _column$size;
				return sum + (((_column$size = column.size) === null || _column$size === void 0 || (_column$size = _column$size.width) === null || _column$size === void 0 ? void 0 : _column$size.v) ?? 0);
			}, 0);
			const tableStyle = ["border-collapse: collapse", width ? `width: ${roundCssNumber(width)}px` : "width: 100%"].join("; ");
			result.tableStack.push({
				table,
				rowIndex: -1,
				columnIndex: 0
			});
			result.html += `<table class="MsoNormalTable UniverTable" style="${tableStyle};"><tbody>`;
			for (const row of children) processNode(row, doc, result);
			result.html += "</tbody></table>";
			result.tableStack.pop();
			break;
		}
		case _univerjs_core.DataStreamTreeNodeType.TABLE_ROW: {
			var _result$tableStack, _tableContext$table, _row$trHeight;
			const { children } = node;
			const tableContext = (_result$tableStack = result.tableStack) === null || _result$tableStack === void 0 ? void 0 : _result$tableStack[result.tableStack.length - 1];
			if (tableContext) {
				tableContext.rowIndex++;
				tableContext.columnIndex = 0;
			}
			const row = tableContext === null || tableContext === void 0 || (_tableContext$table = tableContext.table) === null || _tableContext$table === void 0 || (_tableContext$table = _tableContext$table.tableRows) === null || _tableContext$table === void 0 ? void 0 : _tableContext$table[tableContext.rowIndex];
			const rowStyle = (row === null || row === void 0 || (_row$trHeight = row.trHeight) === null || _row$trHeight === void 0 || (_row$trHeight = _row$trHeight.val) === null || _row$trHeight === void 0 ? void 0 : _row$trHeight.v) != null ? ` style="height: ${roundCssNumber(row.trHeight.val.v)}px"` : "";
			result.html += `<tr class="UniverTableRow"${rowStyle}>`;
			for (const cell of children) processNode(cell, doc, result);
			result.html += "</tr>";
			break;
		}
		case _univerjs_core.DataStreamTreeNodeType.TABLE_CELL: {
			var _result$tableStack2, _tableContext$table2, _row$tableCells;
			const { children } = node;
			const tableContext = (_result$tableStack2 = result.tableStack) === null || _result$tableStack2 === void 0 ? void 0 : _result$tableStack2[result.tableStack.length - 1];
			const row = tableContext === null || tableContext === void 0 || (_tableContext$table2 = tableContext.table) === null || _tableContext$table2 === void 0 || (_tableContext$table2 = _tableContext$table2.tableRows) === null || _tableContext$table2 === void 0 ? void 0 : _tableContext$table2[tableContext.rowIndex];
			const cell = row === null || row === void 0 || (_row$tableCells = row.tableCells) === null || _row$tableCells === void 0 ? void 0 : _row$tableCells[(tableContext === null || tableContext === void 0 ? void 0 : tableContext.columnIndex) ?? 0];
			if (tableContext) tableContext.columnIndex++;
			if ((cell === null || cell === void 0 ? void 0 : cell.rowSpan) === 0 || (cell === null || cell === void 0 ? void 0 : cell.columnSpan) === 0) break;
			result.html += `<td class="UniverTableCell"${serializeTableCellAttributes(cell)}>`;
			for (const n of children) processNode(n, doc, result);
			closeListStack(result);
			result.html += "</td>";
			break;
		}
		default: throw new Error(`Unknown node type: ${node.nodeType}`);
	}
}
function renderParagraphNodeHtml(doc, paragraph, startIndex, endIndex, innerHtml, style, result) {
	const listItemHtml = renderListParagraphItemHtml(doc, paragraph, innerHtml, style);
	if (listItemHtml) {
		appendListParagraphHtml(result, listItemHtml);
		return;
	}
	closeListStack(result);
	result.html += renderBlockParagraphHtml(doc, paragraph, startIndex, endIndex, innerHtml, style);
}
function renderBlockParagraphHtml(doc, paragraph, startIndex, endIndex, innerHtml, style) {
	var _doc$body3;
	const blockRange = (_doc$body3 = doc.body) === null || _doc$body3 === void 0 || (_doc$body3 = _doc$body3.blockRanges) === null || _doc$body3 === void 0 ? void 0 : _doc$body3.find((range) => range.startIndex <= startIndex && endIndex <= range.endIndex + 1);
	if (blockRange && !hasVisibleHtml(innerHtml)) return "";
	const paragraphHtml = renderHeadingParagraphHtml(paragraph, innerHtml, style) ?? `<p class="UniverNormal" ${style.length ? `style="${style.join("; ")};"` : ""}>${innerHtml}</p>`;
	switch (blockRange === null || blockRange === void 0 ? void 0 : blockRange.blockType) {
		case _univerjs_core.DocumentBlockRangeType.QUOTE: return `<blockquote data-doc-type="quote" style="border-left: 4px solid #d0d7de; margin: 8px 0; padding: 4px 0 4px 12px; color: #57606a;">${paragraphHtml}</blockquote>`;
		case _univerjs_core.DocumentBlockRangeType.CODE: return `<pre data-doc-type="code-block" style="font-family: Consolas, 'Courier New', monospace; background: #f6f8fa; padding: 12px; border-radius: 6px; white-space: pre-wrap;"><code>${innerHtml.replace(/<\/?[^>]+>/g, "")}</code></pre>`;
		case _univerjs_core.DocumentBlockRangeType.CALLOUT: return `<aside data-doc-type="callout" role="note" style="background: #fff8c5; border-left: 4px solid #d4a72c; padding: 8px 12px; margin: 8px 0; border-radius: 4px;"><p><span data-callout-icon="true" style="margin-right: 6px;">💡</span>${innerHtml}</p></aside>`;
		default: return paragraphHtml;
	}
}
function renderHeadingParagraphHtml(paragraph, innerHtml, style) {
	var _paragraph$paragraphS;
	const headingLevel = getHeadingLevel((_paragraph$paragraphS = paragraph.paragraphStyle) === null || _paragraph$paragraphS === void 0 ? void 0 : _paragraph$paragraphS.namedStyleType);
	if (!headingLevel) return null;
	return `<p class="UniverHeading" role="heading" aria-level="${headingLevel}" data-heading-level="${headingLevel}" ${style.length ? `style="${style.join("; ")};"` : ""}>${innerHtml}</p>`;
}
function renderListParagraphItemHtml(doc, paragraph, innerHtml, style) {
	const bullet = paragraph.bullet;
	if (!bullet) return null;
	const tag = isOrderedListType(String(bullet.listType)) ? "ol" : "ul";
	const level = bullet.nestingLevel ?? 0;
	const listStyle = getListStyleType(doc, String(bullet.listType), level);
	return {
		tag,
		level,
		listType: String(bullet.listType),
		listStyle,
		html: `<p class="UniverNormal" ${style.length ? `style="${style.join("; ")};"` : ""}>${innerHtml}</p>`
	};
}
function appendListParagraphHtml(result, listItem) {
	result.listStack ??= [];
	const stack = result.listStack;
	while (stack.length > listItem.level + 1) result.html += `</li></${stack.pop().tag}>`;
	const current = stack[stack.length - 1];
	if (current && current.level === listItem.level) if (current.tag === listItem.tag && current.listType === listItem.listType) result.html += "</li><li>";
	else {
		result.html += `</li></${stack.pop().tag}>`;
		openList(result, listItem);
	}
	else {
		while (stack.length < listItem.level) openList(result, {
			...listItem,
			level: stack.length
		});
		openList(result, listItem);
	}
	result.html += listItem.html;
}
function openList(result, listItem) {
	const listStyleHtml = listItem.listStyle ? ` list-style-type: ${listItem.listStyle};` : "";
	const marginLeft = listItem.level > 0 ? ` margin-left: ${listItem.level * 24}px;` : "";
	result.html += `<${listItem.tag} data-doc-type="${listItem.tag === "ol" ? "ordered-list" : "bullet-list"}" style="margin-top: 0; margin-bottom: 0;${marginLeft}${listStyleHtml}"><li>`;
	result.listStack.push({
		tag: listItem.tag,
		level: listItem.level,
		listType: listItem.listType
	});
}
function closeListStack(result) {
	var _result$listStack;
	while ((_result$listStack = result.listStack) === null || _result$listStack === void 0 ? void 0 : _result$listStack.length) result.html += `</li></${result.listStack.pop().tag}>`;
}
function serializeTableCellAttributes(cell) {
	var _cell$size, _cell$backgroundColor;
	if (!cell) return "";
	const attributes = [];
	const styles = [];
	if ((cell.rowSpan ?? 1) > 1) attributes.push(`rowspan="${cell.rowSpan}"`);
	if ((cell.columnSpan ?? 1) > 1) attributes.push(`colspan="${cell.columnSpan}"`);
	if (((_cell$size = cell.size) === null || _cell$size === void 0 || (_cell$size = _cell$size.width) === null || _cell$size === void 0 ? void 0 : _cell$size.v) != null) styles.push(`width: ${roundCssNumber(cell.size.width.v)}px`);
	if ((_cell$backgroundColor = cell.backgroundColor) === null || _cell$backgroundColor === void 0 ? void 0 : _cell$backgroundColor.rgb) styles.push(`background-color: ${cell.backgroundColor.rgb}`);
	[
		["top", cell.borderTop],
		["right", cell.borderRight],
		["bottom", cell.borderBottom],
		["left", cell.borderLeft]
	].forEach(([side, border]) => {
		var _border$width, _border$color;
		if (!border) return;
		styles.push(`border-${side}: ${roundCssNumber(((_border$width = border.width) === null || _border$width === void 0 ? void 0 : _border$width.v) ?? 1)}px solid ${((_border$color = border.color) === null || _border$color === void 0 ? void 0 : _border$color.rgb) ?? "#1f1f1f"}`);
	});
	if (styles.length) attributes.push(`style="${styles.join("; ")};"`);
	return attributes.length ? ` ${attributes.join(" ")}` : "";
}
function serializeTextStyle(textStyle) {
	if (!textStyle) return [];
	const { ff, fs, it, bl, ul, st, ol, bg, cl } = textStyle;
	const style = [];
	style.push(`font-family: ${ff ?? DEFAULT_CLIPBOARD_FONT_FAMILY}`);
	if (fs != null) style.push(`font-size: ${fs}pt`);
	if (cl === null || cl === void 0 ? void 0 : cl.rgb) style.push(`color: ${normalizeCssColor(cl.rgb)}`);
	if (bg === null || bg === void 0 ? void 0 : bg.rgb) style.push(`background-color: ${normalizeCssColor(bg.rgb)}`);
	if (bl === _univerjs_core.BooleanNumber.TRUE) style.push("font-weight: bold");
	if (it === _univerjs_core.BooleanNumber.TRUE) style.push("font-style: italic");
	const textDecorations = [];
	if ((ul === null || ul === void 0 ? void 0 : ul.s) === _univerjs_core.BooleanNumber.TRUE) textDecorations.push("underline");
	if ((st === null || st === void 0 ? void 0 : st.s) === _univerjs_core.BooleanNumber.TRUE) textDecorations.push("line-through");
	if ((ol === null || ol === void 0 ? void 0 : ol.s) === _univerjs_core.BooleanNumber.TRUE) textDecorations.push("overline");
	if (textDecorations.length) style.push(`text-decoration: ${textDecorations.join(" ")}`);
	return style;
}
function applySemanticTextStyle(html, textStyle) {
	var _textStyle$ul, _textStyle$st;
	if (!textStyle || !hasVisibleHtml(html)) return html;
	let result = html;
	if (textStyle.it === _univerjs_core.BooleanNumber.TRUE) result = `<i>${result}</i>`;
	if (((_textStyle$ul = textStyle.ul) === null || _textStyle$ul === void 0 ? void 0 : _textStyle$ul.s) === _univerjs_core.BooleanNumber.TRUE) result = `<u>${result}</u>`;
	if (((_textStyle$st = textStyle.st) === null || _textStyle$st === void 0 ? void 0 : _textStyle$st.s) === _univerjs_core.BooleanNumber.TRUE) result = `<s>${result}</s>`;
	if (textStyle.bl === _univerjs_core.BooleanNumber.TRUE) result = `<strong>${result}</strong>`;
	return result;
}
function normalizeCssColor(color) {
	const rgb = color.match(/^rgba?\(\s*(\d{1,3})\s*,\s*(\d{1,3})\s*,\s*(\d{1,3})(?:\s*,\s*[\d.]+)?\s*\)$/i);
	if (!rgb) return color;
	return `#${rgb.slice(1, 4).map((part) => Math.max(0, Math.min(255, Number(part))).toString(16).padStart(2, "0")).join("")}`;
}
function roundCssNumber(value) {
	return Math.round(value * 100) / 100;
}
function getHeadingLevel(namedStyleType) {
	switch (namedStyleType) {
		case _univerjs_core.NamedStyleType.TITLE:
		case _univerjs_core.NamedStyleType.HEADING_1: return 1;
		case _univerjs_core.NamedStyleType.HEADING_2: return 2;
		case _univerjs_core.NamedStyleType.HEADING_3: return 3;
		case _univerjs_core.NamedStyleType.HEADING_4: return 4;
		case _univerjs_core.NamedStyleType.HEADING_5: return 5;
		default: return null;
	}
}
function getTextAlign(horizontalAlign) {
	switch (horizontalAlign) {
		case _univerjs_core.HorizontalAlign.LEFT: return "left";
		case _univerjs_core.HorizontalAlign.CENTER: return "center";
		case _univerjs_core.HorizontalAlign.RIGHT: return "right";
		case _univerjs_core.HorizontalAlign.JUSTIFIED:
		case _univerjs_core.HorizontalAlign.BOTH: return "justify";
		default: return null;
	}
}
function isOrderedListType(listType) {
	return listType.includes("ORDER") || listType === _univerjs_core.PresetListType.ORDER_LIST;
}
function getListStyleType(doc, listType, level) {
	var _doc$lists;
	switch ((_doc$lists = doc.lists) === null || _doc$lists === void 0 || (_doc$lists = _doc$lists[listType]) === null || _doc$lists === void 0 || (_doc$lists = _doc$lists.nestingLevel) === null || _doc$lists === void 0 || (_doc$lists = _doc$lists[level]) === null || _doc$lists === void 0 ? void 0 : _doc$lists.glyphType) {
		case 3: return "upper-alpha";
		case 4: return "lower-alpha";
		case 5: return "upper-roman";
		case 6: return "lower-roman";
		default: return isOrderedListType(listType) ? "decimal" : null;
	}
}
function escapeHtml(value) {
	return value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
}
function escapeInlineText(value) {
	return escapeHtml(stripDataStreamControlChars(value)).replace(/\r\n|\r|\n/g, "<br>");
}
function stripDataStreamControlChars(value) {
	return value.replace(/[\x00-\x08\x0B\x0C\x0E-\x1F]/g, "");
}
function hasVisibleHtml(html) {
	return /<(?:img|br)\b/i.test(html) || html.replace(/<[^>]+>/g, "").replace(/&nbsp;/gi, " ").trim().length > 0;
}
var UDMToHtmlService = class {
	constructor(_docHtmlExportService) {
		this._docHtmlExportService = _docHtmlExportService;
	}
	convert(docList) {
		if (docList.length === 0) throw new Error("The bodyList length at least to be 1");
		let html = "";
		for (const doc of _univerjs_core.Tools.deepClone(docList)) {
			var _this$_docHtmlExportS;
			html += convertBodyToHtml(((_this$_docHtmlExportS = this._docHtmlExportService) === null || _this$_docHtmlExportS === void 0 ? void 0 : _this$_docHtmlExportS.transformDocumentForHtmlExport(doc)) ?? doc);
		}
		return html;
	}
};

//#endregion
//#region src/services/clipboard/udm-to-html/doc-html-export.service.ts
var DocHtmlExportService = class extends _univerjs_core.Disposable {
	constructor(..._args) {
		super(..._args);
		_defineProperty(this, "_transformers", []);
	}
	registerTransformer(transformer) {
		this._transformers.push(transformer);
		return this.disposeWithMe((0, _univerjs_core.toDisposable)(() => (0, _univerjs_core.remove)(this._transformers, transformer)));
	}
	transformDocumentForHtmlExport(documentData) {
		return this._transformers.reduce((doc, transformer) => transformer(doc), documentData);
	}
};

//#endregion
//#region src/services/clipboard/clipboard.service.ts
HtmlToUDMService.use(LarkPastePlugin);
HtmlToUDMService.use(UniverPastePlugin);
HtmlToUDMService.use(WordPastePlugin);
function convertClipboardHtmlToDocumentData(html, unitId = "") {
	const documentData = new HtmlToUDMService().convert(html, { unitId });
	const body = finalizeClipboardDocumentBody(documentData.body);
	if (!(body === null || body === void 0 ? void 0 : body.dataStream)) return documentData;
	if ((0, _univerjs_core.validateDocBodyStructure)(body).length === 0) return documentData;
	const plainText = [
		_univerjs_core.DataStreamTreeTokenType.COLUMN_GROUP_START,
		_univerjs_core.DataStreamTreeTokenType.COLUMN_START,
		_univerjs_core.DataStreamTreeTokenType.COLUMN_END,
		_univerjs_core.DataStreamTreeTokenType.COLUMN_GROUP_END,
		_univerjs_core.DataStreamTreeTokenType.CUSTOM_RANGE_START,
		_univerjs_core.DataStreamTreeTokenType.CUSTOM_RANGE_END,
		_univerjs_core.DataStreamTreeTokenType.COLUMN_BREAK,
		_univerjs_core.DataStreamTreeTokenType.PAGE_BREAK,
		_univerjs_core.DataStreamTreeTokenType.DOCS_END,
		_univerjs_core.DataStreamTreeTokenType.CUSTOM_BLOCK
	].reduce((text, token) => text.replaceAll(token, ""), _univerjs_core.BuildTextUtils.transform.getPlainText(body.dataStream));
	return {
		id: documentData.id,
		body: finalizeClipboardDocumentBody(_univerjs_core.BuildTextUtils.transform.fromPlainText(plainText)),
		documentStyle: documentData.documentStyle
	};
}
function finalizeClipboardDocumentBody(body) {
	if (!(body === null || body === void 0 ? void 0 : body.dataStream)) return body;
	if (!body.dataStream.endsWith("\r\n")) {
		if (!body.dataStream.endsWith("\r")) body.dataStream += "\r";
		body.dataStream += "\n";
	}
	(0, _univerjs_core.normalizeBody)(body);
	const paragraphs = body.paragraphs ?? [];
	const paragraphIndex = body.dataStream.length - 2;
	if (!paragraphs.some((paragraph) => paragraph.startIndex === paragraphIndex)) {
		paragraphs.push({
			paragraphId: (0, _univerjs_core.createParagraphId)(new Set(paragraphs.map((paragraph) => paragraph.paragraphId))),
			startIndex: paragraphIndex
		});
		body.paragraphs = paragraphs;
	}
	const sectionBreaks = body.sectionBreaks ?? [];
	const sectionBreakIndex = body.dataStream.length - 1;
	if (!sectionBreaks.some((sectionBreak) => sectionBreak.startIndex === sectionBreakIndex)) {
		sectionBreaks.push({
			sectionId: (0, _univerjs_core.createSectionId)(new Set(sectionBreaks.map((sectionBreak) => sectionBreak.sectionId))),
			startIndex: sectionBreakIndex
		});
		body.sectionBreaks = sectionBreaks;
	}
	return body;
}
function removeClipboardHtmlImages(html) {
	const doc = new DOMParser().parseFromString(html, "text/html");
	doc.querySelectorAll("img, svg").forEach((image) => image.remove());
	return /<(?:html|head)\b/i.test(html) ? doc.documentElement.outerHTML : doc.body.innerHTML;
}
function getTableClipboardBodySlice(body, range) {
	const { startOffset, endOffset, spanEntireTable, tableId } = range;
	if (startOffset == null || endOffset == null) return { dataStream: "" };
	if (spanEntireTable) {
		var _body$tables;
		const tableRange = (_body$tables = body.tables) === null || _body$tables === void 0 ? void 0 : _body$tables.find((table) => table.tableId === tableId);
		if (tableRange) return (0, _univerjs_core.getBodySlice)(body, tableRange.startIndex, tableRange.endIndex, false, _univerjs_core.SliceBodyType.copy);
	}
	return getTableCellContentClipboardBodySlice(body, startOffset, endOffset);
}
function getTableCellContentClipboardBodySlice(body, start, end) {
	var _bodySlice$textRuns, _bodySlice$tables, _bodySlice$paragraphs;
	const bodySlice = (0, _univerjs_core.getBodySlice)(body, start, end + 2);
	bodySlice.dataStream = _univerjs_core.DataStreamTreeTokenType.TABLE_START + _univerjs_core.DataStreamTreeTokenType.TABLE_ROW_START + _univerjs_core.DataStreamTreeTokenType.TABLE_CELL_START + bodySlice.dataStream + _univerjs_core.DataStreamTreeTokenType.TABLE_CELL_END + _univerjs_core.DataStreamTreeTokenType.TABLE_ROW_END + _univerjs_core.DataStreamTreeTokenType.TABLE_END;
	(_bodySlice$textRuns = bodySlice.textRuns) === null || _bodySlice$textRuns === void 0 || _bodySlice$textRuns.forEach((textRun) => {
		const { st, ed } = textRun;
		textRun.st = st + 3;
		textRun.ed = ed + 3;
	});
	(_bodySlice$tables = bodySlice.tables) === null || _bodySlice$tables === void 0 || _bodySlice$tables.forEach((table) => {
		const { startIndex, endIndex } = table;
		table.startIndex = startIndex + 3;
		table.endIndex = endIndex + 3;
	});
	(_bodySlice$paragraphs = bodySlice.paragraphs) === null || _bodySlice$paragraphs === void 0 || _bodySlice$paragraphs.forEach((paragraph) => {
		const { startIndex } = paragraph;
		paragraph.startIndex = startIndex + 3;
	});
	return bodySlice;
}
const IDocClipboardService = (0, _univerjs_core.createIdentifier)("doc.clipboard-service");
let DocClipboardService = class DocClipboardService extends _univerjs_core.Disposable {
	constructor(_univerInstanceService, _logService, _commandService, _permissionService, _clipboardInterfaceService, _errorService, docHtmlExportService, _docSelectionManagerService) {
		super();
		this._univerInstanceService = _univerInstanceService;
		this._logService = _logService;
		this._commandService = _commandService;
		this._permissionService = _permissionService;
		this._clipboardInterfaceService = _clipboardInterfaceService;
		this._errorService = _errorService;
		this._docSelectionManagerService = _docSelectionManagerService;
		_defineProperty(this, "_clipboardHooks", []);
		_defineProperty(this, "_memoryClipboardData", null);
		_defineProperty(this, "_htmlToUDM", new HtmlToUDMService());
		_defineProperty(this, "_umdToHtml", void 0);
		this._umdToHtml = new UDMToHtmlService(docHtmlExportService);
	}
	async copy(sliceType = _univerjs_core.SliceBodyType.copy, ranges) {
		const document = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!document || !(0, _univerjs_docs.getDocumentPermissionValue)(this._permissionService, document.getUnitId(), document.getUnitId(), _univerjs_protocol.UnitAction.Copy)) return false;
		const { newSnapshotList = [], needCache = false, plainTextList, snapshot, ranges: allRanges } = this._getDocumentBodyInRanges(sliceType, ranges) ?? {};
		if (newSnapshotList.length === 0 || snapshot == null) return false;
		try {
			var _allRanges$;
			const isCopyInHeaderFooter = !!(allRanges === null || allRanges === void 0 || (_allRanges$ = allRanges[0]) === null || _allRanges$ === void 0 ? void 0 : _allRanges$.segmentId);
			await this._setClipboardData(newSnapshotList, !isCopyInHeaderFooter && needCache, plainTextList);
		} catch (e) {
			this._logService.error("[DocClipboardService] copy failed", e);
			return false;
		}
		return true;
	}
	async cut(ranges) {
		return this._cut(ranges);
	}
	async paste(items) {
		const targetUnitId = this._getCurrentDocumentUnitId();
		if (!targetUnitId || !this._canEditTargets(targetUnitId)) return false;
		if (!(items === null || items === void 0 ? void 0 : items.length)) return this._memoryClipboardData ? this._paste(_univerjs_core.Tools.deepClone(this._memoryClipboardData), targetUnitId) : false;
		const partDocData = await this._genDocDataFromClipboardItems(items);
		if (!this._canEditTargets(targetUnitId)) return false;
		return this._paste(partDocData, targetUnitId);
	}
	async legacyPaste(options) {
		const currentDocInstance = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const docUnitId = (currentDocInstance === null || currentDocInstance === void 0 ? void 0 : currentDocInstance.getUnitId()) || "";
		if (!docUnitId || !this._canEditTargets(docUnitId)) return false;
		let { html, internalJson, text, files } = options;
		if (!html && !text && files.length) html = await this._createImagePasteHtml(files);
		else if (html && files.length) html += await this._createImagePasteHtml(files);
		html = await this._uploadBase64ImagesInHtml(html);
		if (!this._canEditTargets(docUnitId)) return false;
		if (!html && !text) {
			this._logService.warn("[DocClipboardController] html and text cannot be both empty!");
			return false;
		}
		const partDocData = this._genDocDataFromHtmlAndText(html, text, docUnitId, internalJson);
		if (docUnitId === _univerjs_core.DOCS_NORMAL_EDITOR_UNIT_ID_KEY) if (text) {
			const textDocData = _univerjs_core.BuildTextUtils.transform.fromPlainText(text);
			return this._paste({ body: textDocData }, docUnitId);
		} else partDocData.body.textRuns = [];
		return this._paste(partDocData, docUnitId);
	}
	_getCurrentDocumentUnitId() {
		var _this$_univerInstance;
		return ((_this$_univerInstance = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC)) === null || _this$_univerInstance === void 0 ? void 0 : _this$_univerInstance.getUnitId()) ?? null;
	}
	_canEditTargets(expectedUnitId, ranges) {
		const document = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!document || expectedUnitId && document.getUnitId() !== expectedUnitId) return false;
		const objectIds = /* @__PURE__ */ new Set();
		(ranges ?? [...this._docSelectionManagerService.getTextRanges() ?? [], ...this._docSelectionManagerService.getRectRanges() ?? []]).forEach((range) => {
			if (range.startOffset == null || range.endOffset == null) return;
			(0, _univerjs_docs.getDocumentEditTargetObjectIds)(document, range.segmentId ?? "", range).forEach((objectId) => objectIds.add(objectId));
		});
		const canEdit = (0, _univerjs_docs.canEditDocumentTargets)(this._permissionService, document.getUnitId(), objectIds);
		if (!canEdit) this._errorService.emitPermissionDenied(document.getUnitId(), objectIds);
		return canEdit;
	}
	async _cut(ranges) {
		const textRanges = (ranges === null || ranges === void 0 ? void 0 : ranges.filter((range) => range.rangeType === _univerjs_core.DOC_RANGE_TYPE.TEXT)) ?? this._docSelectionManagerService.getTextRanges() ?? [];
		const rectRanges = (ranges === null || ranges === void 0 ? void 0 : ranges.filter((range) => range.rangeType === _univerjs_core.DOC_RANGE_TYPE.RECT)) ?? this._docSelectionManagerService.getRectRanges() ?? [];
		const { segmentId, endOffset: activeEndOffset, style } = textRanges[0] ?? {};
		if (segmentId == null) this._logService.error("[DocClipboardController] segmentId is not existed");
		if (textRanges.length === 0 && rectRanges.length === 0) return false;
		const unitId = this._getCurrentDocumentUnitId();
		if (!unitId || !this._canEditTargets(unitId, [...textRanges, ...rectRanges])) return false;
		if (!await this.copy(_univerjs_core.SliceBodyType.cut, ranges)) return false;
		try {
			let cursor = 0;
			if (rectRanges.length > 0) cursor = getCursorWhenDelete(textRanges, rectRanges);
			else if (activeEndOffset != null) {
				cursor = activeEndOffset;
				for (const range of textRanges) {
					const { startOffset, endOffset } = range;
					if (startOffset == null || endOffset == null) continue;
					if (endOffset <= activeEndOffset) cursor -= endOffset - startOffset;
				}
			}
			const newTextRanges = [{
				startOffset: cursor,
				endOffset: cursor,
				style
			}];
			return this._commandService.executeCommand(CutContentCommand.id, {
				segmentId,
				textRanges: newTextRanges,
				rectRanges,
				selections: textRanges
			});
		} catch (_e) {
			this._logService.error("[DocClipboardController] cut content failed");
			return false;
		}
	}
	async _paste(docData, expectedUnitId) {
		var _body$customRanges, _body$paragraphs, _currentDocument$getS;
		const { body: _body } = docData;
		if (_body == null) return false;
		let body = (0, _univerjs_core.normalizeBody)(_body);
		const currentDocument = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!currentDocument || expectedUnitId && currentDocument.getUnitId() !== expectedUnitId) return false;
		this._clipboardHooks.forEach((hook) => {
			if (hook.onBeforePaste) body = hook.onBeforePaste(body, {
				documentData: docData,
				targetUnitId: currentDocument.getUnitId()
			});
		});
		const customRangeMappings = ((_body$customRanges = body.customRanges) === null || _body$customRanges === void 0 ? void 0 : _body$customRanges.map((sourceRange) => {
			return {
				sourceRange,
				targetRange: _univerjs_core.BuildTextUtils.customRange.copyCustomRange(sourceRange)
			};
		})) ?? [];
		body.customRanges = customRangeMappings.map(({ targetRange }) => targetRange);
		(_body$paragraphs = body.paragraphs) === null || _body$paragraphs === void 0 || _body$paragraphs.forEach((copy) => {
			var _copy$paragraphStyle;
			if ((_copy$paragraphStyle = copy.paragraphStyle) === null || _copy$paragraphStyle === void 0 ? void 0 : _copy$paragraphStyle.headingId) copy.paragraphStyle.headingId = (0, _univerjs_core.generateRandomId)(6);
		});
		const activeRange = this._docSelectionManagerService.getActiveTextRange();
		const docRanges = this._docSelectionManagerService.getDocRanges();
		const { segmentId, endOffset: activeEndOffset, style } = (activeRange ?? docRanges.find((range) => range.isActive) ?? docRanges[0]) || {};
		const ranges = this._docSelectionManagerService.getTextRanges() ?? [];
		if (segmentId == null) this._logService.error("[DocClipboardController] segmentId does not exist!");
		if (activeEndOffset == null) return false;
		const originBody = segmentId == null ? null : (_currentDocument$getS = currentDocument.getSelfOrHeaderFooterModel(segmentId)) === null || _currentDocument$getS === void 0 ? void 0 : _currentDocument$getS.getBody();
		if (originBody) ensureParagraphAtStructuralGap(body, originBody, ranges);
		try {
			let cursor = activeEndOffset;
			for (const range of ranges) {
				const { startOffset, endOffset } = range;
				if (startOffset == null || endOffset == null) continue;
				if (endOffset <= activeEndOffset) cursor += body.dataStream.length - (endOffset - startOffset);
			}
			const textRanges = [{
				startOffset: cursor,
				endOffset: cursor,
				style
			}];
			return this._commandService.executeCommand(InnerPasteCommand.id, {
				doc: {
					...docData,
					body
				},
				customRangeMappings,
				segmentId,
				textRanges
			});
		} catch {
			this._logService.error("[DocClipboardController]", "clipboard is empty.");
			return false;
		}
	}
	async _setClipboardData(documentList, needCache = true, plainTextList) {
		var _documentList$0$body;
		const copyId = (0, _univerjs_core.generateRandomId)(6);
		const text = (plainTextList ? plainTextList.join("\n") : documentList.length > 1 ? documentList.map((doc) => {
			var _doc$body;
			return ((_doc$body = doc.body) === null || _doc$body === void 0 ? void 0 : _doc$body.dataStream) || "";
		}).join("\n") : ((_documentList$0$body = documentList[0].body) === null || _documentList$0$body === void 0 ? void 0 : _documentList$0$body.dataStream) || "").replaceAll(_univerjs_core.DataStreamTreeTokenType.TABLE_START, "").replaceAll(_univerjs_core.DataStreamTreeTokenType.TABLE_END, "").replaceAll(_univerjs_core.DataStreamTreeTokenType.TABLE_ROW_START, "").replaceAll(_univerjs_core.DataStreamTreeTokenType.TABLE_ROW_END, "").replaceAll(_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_START, "").replaceAll(_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_END, "").replaceAll(_univerjs_core.DataStreamTreeTokenType.BLOCK_START, "").replaceAll(_univerjs_core.DataStreamTreeTokenType.BLOCK_END, "").replaceAll("\r\n", " ").replace(/[\x00-\x08\x0B\x0C\x0E-\x1F]/g, "");
		let html = this._umdToHtml.convert(documentList);
		let internalJson = "";
		let internalDocData = null;
		if (documentList.length === 1 && needCache) {
			html = html.replace(/(<[a-z]+)/, (_p0, p1) => `${p1} data-copy-id="${copyId}"`);
			const doc = documentList[0];
			const cache = createInternalClipboardDocData(doc);
			copyContentCache.set(copyId, cache);
			internalDocData = cache;
		} else internalDocData = createInternalClipboardDocDataList(documentList);
		if (internalDocData) {
			internalDocData = this._applyCopyDocDataHooks(internalDocData, documentList);
			internalJson = createInternalClipboardFragment(internalDocData);
			html = embedInternalClipboardFragment(html, internalJson);
		}
		html = wrapClipboardHtml(html);
		await this._clipboardInterfaceService.write(text, html, internalJson ? { [DOC_INTERNAL_FRAGMENT_MIME]: internalJson } : void 0);
		this._memoryClipboardData = _univerjs_core.Tools.deepClone(internalDocData);
	}
	addClipboardHook(hook) {
		this._clipboardHooks.push(hook);
		return (0, _univerjs_core.toDisposable)(() => {
			const index = this._clipboardHooks.indexOf(hook);
			if (index > -1) this._clipboardHooks.splice(index, 1);
		});
	}
	_applyCopyDocDataHooks(doc, sourceDocuments) {
		return this._clipboardHooks.reduce((currentDoc, hook) => {
			var _hook$onCopyDocData;
			return ((_hook$onCopyDocData = hook.onCopyDocData) === null || _hook$onCopyDocData === void 0 ? void 0 : _hook$onCopyDocData.call(hook, currentDoc, { sourceDocuments })) ?? currentDoc;
		}, doc);
	}
	_getDocumentBodyInRanges(sliceType, ranges) {
		var _this$_clipboardHooks, _docDataModel$getSelf;
		const docDataModel = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const allRanges = ranges ?? this._docSelectionManagerService.getDocRanges();
		const results = [];
		const plainTextResults = [];
		const copyContentHook = (_this$_clipboardHooks = this._clipboardHooks.find((hook) => hook.onCopyContent)) === null || _this$_clipboardHooks === void 0 ? void 0 : _this$_clipboardHooks.onCopyContent;
		let needCache = true;
		if (docDataModel == null || allRanges.length === 0) return;
		const segmentId = allRanges[0].segmentId;
		const body = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		const snapshot = docDataModel.getSnapshot();
		if (body == null) return;
		for (const range of allRanges) {
			var _docDataModel$getSelf2;
			const { startOffset, endOffset, collapsed, rangeType } = range;
			if (collapsed || startOffset == null || endOffset == null) continue;
			if (rangeType === _univerjs_core.DOC_RANGE_TYPE.RECT) {
				needCache = false;
				const bodySlice = getTableClipboardBodySlice(body, range);
				results.push(bodySlice);
				plainTextResults.push(bodySlice.dataStream);
				continue;
			}
			const deleteRange = {
				startOffset,
				endOffset,
				collapsed
			};
			const docBody = (_docDataModel$getSelf2 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf2 === void 0 ? void 0 : _docDataModel$getSelf2.sliceBody(deleteRange.startOffset, deleteRange.endOffset, sliceType);
			if (docBody == null) continue;
			results.push(docBody);
			plainTextResults.push((copyContentHook === null || copyContentHook === void 0 ? void 0 : copyContentHook(deleteRange.startOffset, deleteRange.endOffset, {
				body,
				segmentId,
				unitId: docDataModel.getUnitId()
			})) ?? docBody.dataStream);
		}
		return {
			newSnapshotList: results.map((e) => ({
				...snapshot,
				body: e
			})),
			needCache,
			plainTextList: plainTextResults,
			snapshot,
			ranges: allRanges
		};
	}
	async _genDocDataFromClipboardItems(items) {
		try {
			let html = "";
			let text = "";
			let internalJson = "";
			const files = [];
			for (const clipboardItem of items) for (const type of clipboardItem.types) switch (type) {
				case DOC_INTERNAL_FRAGMENT_MIME:
					internalJson = await clipboardItem.getType(type).then((blob) => blob && blob.text());
					break;
				case _univerjs_ui.PLAIN_TEXT_CLIPBOARD_MIME_TYPE:
					text = await clipboardItem.getType(type).then((blob) => blob && blob.text());
					break;
				case _univerjs_ui.HTML_CLIPBOARD_MIME_TYPE:
					html = await clipboardItem.getType(type).then((blob) => blob && blob.text());
					break;
				case _univerjs_ui.FILE__BMP_CLIPBOARD_MIME_TYPE:
				case _univerjs_ui.FILE__JPEG_CLIPBOARD_MIME_TYPE:
				case _univerjs_ui.FILE__WEBP_CLIPBOARD_MIME_TYPE:
				case _univerjs_ui.FILE_PNG_CLIPBOARD_MIME_TYPE: {
					const blob = await clipboardItem.getType(type);
					const file = new File([blob], `pasted_image.${type.split("/")[1]}`, { type });
					files.push(file);
					break;
				}
			}
			if (!html && !text && files.length) html = await this._createImagePasteHtml(files);
			html = await this._uploadBase64ImagesInHtml(html) ?? "";
			return this._genDocDataFromHtmlAndText(html, text, void 0, internalJson);
		} catch (e) {
			return Promise.reject(e);
		}
	}
	_genDocDataFromHtmlAndText(html, text, _unitId, internalJson) {
		const internalDocData = parseInternalClipboardFragment(internalJson) ?? extractInternalClipboardFragmentFromHtml(html);
		if (internalDocData === null || internalDocData === void 0 ? void 0 : internalDocData.body) return internalDocData;
		if (!html) if (text) return { body: _univerjs_core.BuildTextUtils.transform.fromPlainText(text) };
		else throw new Error("[DocClipboardService] html and text cannot be both empty!");
		const copyId = extractId(html);
		if (copyId) {
			const copyCache = copyContentCache.get(copyId);
			if (copyCache) return copyCache;
		}
		const currentDocInstance = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const unitId = _unitId || (currentDocInstance === null || currentDocInstance === void 0 ? void 0 : currentDocInstance.getUnitId()) || "";
		const doc = this._htmlToUDM.convert(html, { unitId });
		if (copyId) copyContentCache.set(copyId, doc);
		return doc;
	}
	async _uploadBase64ImagesInHtml(html) {
		var _this$_clipboardHooks2;
		if (!html || !html.includes("data:image/")) return html;
		const onBeforePasteImage = (_this$_clipboardHooks2 = this._clipboardHooks.find((e) => e.onBeforePasteImage)) === null || _this$_clipboardHooks2 === void 0 ? void 0 : _this$_clipboardHooks2.onBeforePasteImage;
		if (!onBeforePasteImage) return html;
		const doc = new DOMParser().parseFromString(html, "text/html");
		const images = Array.from(doc.querySelectorAll("img[src^=\"data:image/\"]"));
		if (images.length === 0) return html;
		await Promise.all(images.map(async (image, index) => {
			if (image.dataset.imageSourceType && image.dataset.imageSourceType !== _univerjs_core.ImageSourceType.BASE64) return;
			const file = dataUrlToFile(image.getAttribute("src") || image.src, `pasted_image_${index}`);
			const uploaded = await onBeforePasteImage(file);
			if (!uploaded) return;
			image.dataset.imageSourceType = uploaded.imageSourceType;
			if (uploaded.imageSourceType === _univerjs_core.ImageSourceType.UUID) {
				image.dataset.source = uploaded.source;
				image.setAttribute("src", uploaded.source);
			} else {
				image.removeAttribute("data-source");
				image.setAttribute("src", uploaded.source);
			}
		}));
		return doc.documentElement.outerHTML;
	}
	async _createImagePasteHtml(files) {
		var _this$_clipboardHooks3;
		const doc = {
			id: "",
			documentStyle: {},
			body: {
				dataStream: "",
				customBlocks: []
			},
			drawings: {}
		};
		const fileToBase64 = async (file) => {
			const reader = new FileReader();
			return new Promise((res) => {
				reader.onloadend = function() {
					res({
						source: reader.result,
						imageSourceType: _univerjs_core.ImageSourceType.BASE64
					});
				};
				reader.readAsDataURL(file);
			});
		};
		const getImageSize = (base64) => {
			const img = new Image();
			const maxWidth = 500;
			return new Promise((resolve) => {
				img.src = typeof base64 === "string" ? base64 : URL.createObjectURL(base64);
				img.onload = () => {
					const width = Math.min(maxWidth, img.naturalWidth);
					resolve({
						width,
						height: width * (img.naturalHeight / img.naturalWidth)
					});
				};
			});
		};
		const onBeforePasteImage = ((_this$_clipboardHooks3 = this._clipboardHooks.find((e) => e.onBeforePasteImage)) === null || _this$_clipboardHooks3 === void 0 ? void 0 : _this$_clipboardHooks3.onBeforePasteImage) ?? fileToBase64;
		await Promise.all(files.map(async (file, index) => {
			var _body$customBlocks;
			const image = await onBeforePasteImage(file);
			if (!image) return Promise.resolve();
			const { width = 100, height = 100 } = await getImageSize(file);
			const itemId = `paste_image_id_${index}`;
			const body = doc.body;
			const drawings = doc.drawings;
			body.dataStream += "\b";
			(_body$customBlocks = body.customBlocks) === null || _body$customBlocks === void 0 || _body$customBlocks.push({
				startIndex: index,
				blockId: itemId
			});
			drawings[itemId] = {
				drawingId: itemId,
				unitId: "",
				subUnitId: "",
				imageSourceType: image.imageSourceType,
				title: "",
				source: image.source,
				description: "",
				layoutType: _univerjs_core.PositionedObjectLayoutType.INLINE,
				drawingType: _univerjs_core.DrawingTypeEnum.DRAWING_IMAGE,
				transform: {
					width,
					height,
					angle: 0
				},
				docTransform: {
					angle: 0,
					size: {
						width,
						height
					},
					positionH: {
						relativeFrom: _univerjs_core.ObjectRelativeFromH.CHARACTER,
						posOffset: 0
					},
					positionV: {
						relativeFrom: _univerjs_core.ObjectRelativeFromV.LINE,
						posOffset: 0
					}
				}
			};
		}));
		return this._umdToHtml.convert([doc]);
	}
};
DocClipboardService = __decorate([
	__decorateParam(0, _univerjs_core.IUniverInstanceService),
	__decorateParam(1, _univerjs_core.ILogService),
	__decorateParam(2, _univerjs_core.ICommandService),
	__decorateParam(3, _univerjs_core.IPermissionService),
	__decorateParam(4, _univerjs_ui.IClipboardInterfaceService),
	__decorateParam(5, (0, _univerjs_core.Inject)(_univerjs_core.ErrorService)),
	__decorateParam(6, (0, _univerjs_core.Inject)(DocHtmlExportService)),
	__decorateParam(7, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService))
], DocClipboardService);
function endsAtDocumentBoundary(dataStream) {
	const lastToken = dataStream[dataStream.length - 1];
	return lastToken === _univerjs_core.DataStreamTreeTokenType.PARAGRAPH || lastToken === _univerjs_core.DataStreamTreeTokenType.SECTION_BREAK || lastToken === _univerjs_core.DataStreamTreeTokenType.BLOCK_END || lastToken === _univerjs_core.DataStreamTreeTokenType.TABLE_END || lastToken === _univerjs_core.DataStreamTreeTokenType.COLUMN_GROUP_END;
}
function ensureParagraphAtStructuralGap(body, originBody, ranges) {
	var _body$paragraphs2;
	if (endsAtDocumentBoundary(body.dataStream) || !ranges.every((range) => range.collapsed && isTopLevelStructuralGap(originBody.dataStream, range.startOffset))) return;
	const paragraphIndex = body.dataStream.length;
	const paragraphIds = new Set(((_body$paragraphs2 = body.paragraphs) === null || _body$paragraphs2 === void 0 ? void 0 : _body$paragraphs2.map((paragraph) => paragraph.paragraphId)) ?? []);
	body.dataStream += _univerjs_core.DataStreamTreeTokenType.PARAGRAPH;
	body.paragraphs = [...body.paragraphs ?? [], {
		startIndex: paragraphIndex,
		paragraphId: (0, _univerjs_core.createParagraphId)(paragraphIds)
	}];
}
function dataUrlToFile(dataUrl, fallbackName) {
	const match = /^data:([^;,]+)(;base64)?,(.*)$/i.exec(dataUrl);
	if (!match) throw new Error("[DocClipboardService] invalid image data url.");
	const [, mimeType, base64Marker, payload] = match;
	const bytes = base64Marker ? Uint8Array.from(atob(payload), (char) => char.charCodeAt(0)) : new TextEncoder().encode(decodeURIComponent(payload));
	const extension = mimeType.split("/")[1] || "png";
	return new File([bytes], `${fallbackName}.${extension}`, { type: mimeType });
}

//#endregion
//#region src/commands/commands/clipboard.command.ts
function whenDocOrEditor(contextService) {
	return contextService.getContextValue(_univerjs_core.FOCUSING_DOC) || contextService.getContextValue(_univerjs_core.EDITOR_ACTIVATED);
}
const DOC_CLIPBOARD_PRIORITY = 999;
const DocCopyCommand = {
	id: _univerjs_ui.CopyCommand.id,
	name: "doc.command.copy",
	type: _univerjs_core.CommandType.COMMAND,
	multi: true,
	priority: DOC_CLIPBOARD_PRIORITY,
	preconditions: whenDocOrEditor,
	handler: async (accessor) => {
		return accessor.get(IDocClipboardService).copy();
	}
};
const DocCopyCurrentParagraphCommand = {
	id: "doc.command.copy-current-paragraph",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		const docClipboardService = accessor.get(IDocClipboardService);
		const paragraph = getCurrentParagraph(accessor);
		if (!paragraph) return false;
		return docClipboardService.copy(_univerjs_core.SliceBodyType.copy, [{
			startOffset: paragraph.paragraphStart,
			endOffset: paragraph.paragraphEnd + 1,
			collapsed: false
		}]);
	}
};
const DocCutCommand = {
	id: _univerjs_ui.CutCommand.id,
	name: "doc.command.cut",
	type: _univerjs_core.CommandType.COMMAND,
	multi: true,
	priority: DOC_CLIPBOARD_PRIORITY,
	preconditions: whenDocOrEditor,
	handler: async (accessor) => {
		return accessor.get(IDocClipboardService).cut();
	}
};
const DocCutCurrentParagraphCommand = {
	id: "doc.command.cut-current-paragraph",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		const docClipboardService = accessor.get(IDocClipboardService);
		const paragraph = getCurrentParagraph(accessor);
		if (!paragraph) return false;
		return docClipboardService.cut([{
			startOffset: paragraph.paragraphStart,
			endOffset: paragraph.paragraphEnd + 1,
			collapsed: false,
			rangeType: _univerjs_core.DOC_RANGE_TYPE.TEXT
		}]);
	}
};
const DocPasteCommand = {
	id: _univerjs_ui.PasteCommand.id,
	name: "doc.command.paste",
	type: _univerjs_core.CommandType.COMMAND,
	multi: true,
	priority: DOC_CLIPBOARD_PRIORITY,
	preconditions: whenDocOrEditor,
	handler: async (accessor) => {
		const docClipboardService = accessor.get(IDocClipboardService);
		const clipboardInterfaceService = accessor.get(_univerjs_ui.IClipboardInterfaceService);
		const clipboardItems = clipboardInterfaceService.supportClipboard ? await clipboardInterfaceService.read() : [];
		return docClipboardService.paste(clipboardItems);
	}
};

//#endregion
//#region src/commands/commands/doc-block-move.command.ts
const DOC_BLOCK_MOVE_BODY_PATCH_KEYS = [
	"paragraphs",
	"sectionBreaks",
	"tables",
	"columnGroups",
	"customBlocks",
	"blockRanges",
	"customRanges",
	"customDecorations",
	"textRuns"
];
const MoveDocBlockCommand = {
	id: "doc.command.move-block",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		if (!params) return false;
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const { unitId, sourceRange, targetOffset } = params;
		const doc = unitId ? univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC) : univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!doc) return false;
		const previousDocumentData = doc.getSnapshot();
		const moveResult = buildMoveDocBlockActions({
			documentData: previousDocumentData,
			sourceRange,
			targetOffset
		});
		const { nextDocumentData, movedRange } = accessor.get(_univerjs_docs.DocBlockMoveValidatorService).transformMoveResult({
			unitId: doc.getUnitId(),
			sourceRange,
			targetOffset,
			previousDocumentData,
			result: moveResult
		});
		const actions = buildReplaceDocumentBodyActions(previousDocumentData, nextDocumentData, {
			sourceRange,
			targetOffset,
			movedRange
		});
		if (!actions) return false;
		const textRanges = [{
			startOffset: movedRange.startOffset,
			endOffset: movedRange.endOffset,
			collapsed: false
		}];
		return Boolean(commandService.syncExecuteCommand(_univerjs_docs.RichTextEditingMutation.id, {
			unitId: doc.getUnitId(),
			actions,
			textRanges
		}));
	}
};
function buildMoveDocBlockActions(params) {
	const nextDocumentData = _univerjs_core.Tools.deepClone(params.documentData);
	const body = nextDocumentData.body;
	if (!(body === null || body === void 0 ? void 0 : body.dataStream)) return {
		nextDocumentData,
		movedRange: params.sourceRange
	};
	const dataStreamLength = body.dataStream.length;
	const startOffset = clamp$1(params.sourceRange.startOffset, 0, dataStreamLength);
	const endOffset = clamp$1(params.sourceRange.endOffset, startOffset, dataStreamLength);
	const targetOffset = clamp$1(params.targetOffset, 0, dataStreamLength);
	if (startOffset === endOffset || targetOffset >= startOffset && targetOffset <= endOffset) return {
		nextDocumentData,
		movedRange: {
			startOffset,
			endOffset
		}
	};
	const movingText = body.dataStream.slice(startOffset, endOffset);
	const moveLength = movingText.length;
	const insertOffset = targetOffset > endOffset ? targetOffset - moveLength : targetOffset;
	const withoutMovingText = body.dataStream.slice(0, startOffset) + body.dataStream.slice(endOffset);
	body.dataStream = withoutMovingText.slice(0, insertOffset) + movingText + withoutMovingText.slice(insertOffset);
	remapBodyIndexesAfterMove(body, startOffset, endOffset, targetOffset, insertOffset, moveLength);
	return {
		nextDocumentData,
		movedRange: {
			startOffset: insertOffset,
			endOffset: insertOffset + moveLength
		}
	};
}
function buildReplaceDocumentBodyActions(previousDocumentData, nextDocumentData, move) {
	_univerjs_core.JSONX.getInstance();
	const previousBody = previousDocumentData.body;
	const nextBody = nextDocumentData.body;
	if (!previousBody || !nextBody) return null;
	const rawActions = [];
	let intermediateDocumentData = _univerjs_core.Tools.deepClone(previousDocumentData);
	const moveTextAction = move ? buildMoveBodyTextAction(previousBody, nextBody, move) : null;
	if (moveTextAction) {
		rawActions.push(moveTextAction);
		intermediateDocumentData = _univerjs_core.JSONX.apply(intermediateDocumentData, moveTextAction);
	}
	const residualTextAction = buildSingleBodyTextChangeAction(intermediateDocumentData.body, nextBody);
	if (residualTextAction) {
		rawActions.push(residualTextAction);
		intermediateDocumentData = _univerjs_core.JSONX.apply(intermediateDocumentData, residualTextAction);
	}
	if (!moveTextAction) collectBodyPatchActions(intermediateDocumentData.body, nextBody, rawActions);
	return rawActions.reduce((acc, cur) => _univerjs_core.JSONX.compose(acc, cur), null);
}
function buildMoveBodyTextAction(previousBody, nextBody, move) {
	const dataStreamLength = previousBody.dataStream.length;
	const startOffset = clamp$1(move.sourceRange.startOffset, 0, dataStreamLength);
	const endOffset = clamp$1(move.sourceRange.endOffset, startOffset, dataStreamLength);
	const targetOffset = clamp$1(move.targetOffset, 0, dataStreamLength);
	const moveLength = endOffset - startOffset;
	if (moveLength <= 0 || targetOffset >= startOffset && targetOffset <= endOffset) return null;
	const textX = new _univerjs_core.TextX();
	const insertOffset = move.movedRange.startOffset;
	const insertBody = (0, _univerjs_core.getBodySliceForTextXAction)(nextBody, insertOffset, insertOffset + moveLength, false);
	if (targetOffset < startOffset) {
		textX.retain(targetOffset);
		textX.insert(moveLength, insertBody);
		textX.retain(startOffset - targetOffset);
		textX.delete(moveLength);
	} else {
		textX.retain(startOffset);
		textX.delete(moveLength);
		textX.retain(targetOffset - endOffset);
		textX.insert(moveLength, insertBody);
	}
	return _univerjs_core.JSONX.getInstance().editOp(textX.serialize(), ["body"]);
}
function buildSingleBodyTextChangeAction(previousBody, nextBody) {
	const textChange = getSingleDataStreamChange(previousBody === null || previousBody === void 0 ? void 0 : previousBody.dataStream, nextBody.dataStream);
	if (!textChange) return null;
	const textX = new _univerjs_core.TextX();
	textX.retain(textChange.start);
	if (textChange.insertLength > 0) textX.insert(textChange.insertLength, (0, _univerjs_core.getBodySliceForTextXAction)(nextBody, textChange.start, textChange.start + textChange.insertLength, false));
	if (textChange.deleteLength > 0) textX.delete(textChange.deleteLength);
	return _univerjs_core.JSONX.getInstance().editOp(textX.serialize(), ["body"]);
}
function getSingleDataStreamChange(previousDataStream, nextDataStream) {
	if (previousDataStream == null || nextDataStream == null || previousDataStream === nextDataStream) return null;
	let start = 0;
	while (start < previousDataStream.length && start < nextDataStream.length && previousDataStream[start] === nextDataStream[start]) start++;
	let previousEnd = previousDataStream.length;
	let nextEnd = nextDataStream.length;
	while (previousEnd > start && nextEnd > start && previousDataStream[previousEnd - 1] === nextDataStream[nextEnd - 1]) {
		previousEnd--;
		nextEnd--;
	}
	return {
		start,
		deleteLength: previousEnd - start,
		insertLength: nextEnd - start
	};
}
function collectBodyPatchActions(previousBody, nextBody, actions) {
	for (const key of DOC_BLOCK_MOVE_BODY_PATCH_KEYS) collectPatchActions(_univerjs_core.JSONX.getInstance(), ["body", key], previousBody === null || previousBody === void 0 ? void 0 : previousBody[key], nextBody[key], actions);
}
function collectPatchActions(jsonX, path, oldValue, newValue, actions) {
	if (isEmptyArrayEquivalent$1(oldValue, newValue)) return;
	if (_univerjs_core.Tools.diffValue(oldValue, newValue)) return;
	if (oldValue == null) {
		actions.push(jsonX.insertOp(path, newValue));
		return;
	}
	if (newValue == null) {
		actions.push(jsonX.removeOp(path, oldValue));
		return;
	}
	if (Array.isArray(oldValue) && Array.isArray(newValue)) {
		collectArrayPatchActions(jsonX, path, oldValue, newValue, actions);
		return;
	}
	if (isPlainObject$1(oldValue) && isPlainObject$1(newValue)) {
		(/* @__PURE__ */ new Set([...Object.keys(oldValue), ...Object.keys(newValue)])).forEach((key) => {
			collectPatchActions(jsonX, [...path, key], oldValue[key], newValue[key], actions);
		});
		return;
	}
	actions.push(jsonX.replaceOp(path, oldValue, newValue));
}
function collectArrayPatchActions(jsonX, path, oldItems, newItems, actions) {
	if (oldItems.length === newItems.length) {
		oldItems.forEach((item, index) => collectPatchActions(jsonX, [...path, index], item, newItems[index], actions));
		return;
	}
	let prefix = 0;
	while (prefix < oldItems.length && prefix < newItems.length && _univerjs_core.Tools.diffValue(oldItems[prefix], newItems[prefix])) prefix++;
	let oldSuffix = oldItems.length - 1;
	let newSuffix = newItems.length - 1;
	while (oldSuffix >= prefix && newSuffix >= prefix && _univerjs_core.Tools.diffValue(oldItems[oldSuffix], newItems[newSuffix])) {
		oldSuffix--;
		newSuffix--;
	}
	for (let index = oldSuffix; index >= prefix; index--) actions.push(jsonX.removeOp([...path, index], oldItems[index]));
	for (let index = prefix; index <= newSuffix; index++) actions.push(jsonX.insertOp([...path, index], newItems[index]));
}
function isPlainObject$1(value) {
	return value != null && typeof value === "object" && !Array.isArray(value);
}
function isEmptyArrayEquivalent$1(oldValue, newValue) {
	return oldValue == null && Array.isArray(newValue) && newValue.length === 0 || newValue == null && Array.isArray(oldValue) && oldValue.length === 0;
}
function remapBodyIndexesAfterMove(body, startOffset, endOffset, targetOffset, insertOffset, moveLength) {
	var _body$paragraphs, _body$sectionBreaks, _body$customBlocks, _body$tables, _body$columnGroups, _body$blockRanges, _body$customRanges, _body$customDecoratio, _body$textRuns;
	body.paragraphs = (_body$paragraphs = body.paragraphs) === null || _body$paragraphs === void 0 ? void 0 : _body$paragraphs.map((paragraph) => ({
		...paragraph,
		startIndex: remapIndexAfterMove(paragraph.startIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength)
	})).sort((left, right) => left.startIndex - right.startIndex);
	body.sectionBreaks = (_body$sectionBreaks = body.sectionBreaks) === null || _body$sectionBreaks === void 0 ? void 0 : _body$sectionBreaks.map((sectionBreak) => ({
		...sectionBreak,
		startIndex: remapIndexAfterMove(sectionBreak.startIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength)
	})).sort((left, right) => left.startIndex - right.startIndex);
	body.customBlocks = (_body$customBlocks = body.customBlocks) === null || _body$customBlocks === void 0 ? void 0 : _body$customBlocks.map((customBlock) => ({
		...customBlock,
		startIndex: remapIndexAfterMove(customBlock.startIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength)
	})).sort((left, right) => left.startIndex - right.startIndex);
	body.tables = (_body$tables = body.tables) === null || _body$tables === void 0 ? void 0 : _body$tables.map((table) => remapExclusiveRange(table, startOffset, endOffset, targetOffset, insertOffset, moveLength)).sort((left, right) => left.startIndex - right.startIndex);
	body.columnGroups = (_body$columnGroups = body.columnGroups) === null || _body$columnGroups === void 0 ? void 0 : _body$columnGroups.map((columnGroup) => remapExclusiveRange(columnGroup, startOffset, endOffset, targetOffset, insertOffset, moveLength)).sort((left, right) => left.startIndex - right.startIndex);
	body.blockRanges = (_body$blockRanges = body.blockRanges) === null || _body$blockRanges === void 0 ? void 0 : _body$blockRanges.map((blockRange) => remapInclusiveRange(blockRange, startOffset, endOffset, targetOffset, insertOffset, moveLength)).sort((left, right) => left.startIndex - right.startIndex);
	body.customRanges = (_body$customRanges = body.customRanges) === null || _body$customRanges === void 0 ? void 0 : _body$customRanges.map((customRange) => remapInclusiveRange(customRange, startOffset, endOffset, targetOffset, insertOffset, moveLength)).sort((left, right) => left.startIndex - right.startIndex);
	body.customDecorations = (_body$customDecoratio = body.customDecorations) === null || _body$customDecoratio === void 0 ? void 0 : _body$customDecoratio.map((customDecoration) => remapInclusiveRange(customDecoration, startOffset, endOffset, targetOffset, insertOffset, moveLength)).sort((left, right) => left.startIndex - right.startIndex);
	body.textRuns = (_body$textRuns = body.textRuns) === null || _body$textRuns === void 0 ? void 0 : _body$textRuns.map((textRun) => {
		const remapped = remapExclusiveRange({
			startIndex: textRun.st,
			endIndex: textRun.ed
		}, startOffset, endOffset, targetOffset, insertOffset, moveLength);
		return {
			...textRun,
			st: remapped.startIndex,
			ed: remapped.endIndex
		};
	}).sort((left, right) => left.st - right.st);
}
function remapExclusiveRange(range, startOffset, endOffset, targetOffset, insertOffset, moveLength) {
	if (range.startIndex >= startOffset && range.endIndex <= endOffset) return {
		...range,
		startIndex: insertOffset + range.startIndex - startOffset,
		endIndex: insertOffset + range.endIndex - startOffset
	};
	return {
		...range,
		startIndex: remapIndexAfterMove(range.startIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength),
		endIndex: remapExclusiveEndIndexAfterMove(range.endIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength)
	};
}
function remapInclusiveRange(range, startOffset, endOffset, targetOffset, insertOffset, moveLength) {
	if (range.startIndex >= startOffset && range.endIndex < endOffset) return {
		...range,
		startIndex: insertOffset + range.startIndex - startOffset,
		endIndex: insertOffset + range.endIndex - startOffset
	};
	return {
		...range,
		startIndex: remapIndexAfterMove(range.startIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength),
		endIndex: remapIndexAfterMove(range.endIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength)
	};
}
function remapIndexAfterMove(index, startOffset, endOffset, targetOffset, insertOffset, moveLength) {
	if (index >= startOffset && index < endOffset) return insertOffset + index - startOffset;
	if (targetOffset < startOffset && index >= targetOffset && index < startOffset) return index + moveLength;
	if (targetOffset > endOffset && index >= endOffset && index < targetOffset) return index - moveLength;
	return index;
}
function remapExclusiveEndIndexAfterMove(index, startOffset, endOffset, targetOffset, insertOffset, moveLength) {
	if (index > startOffset && index <= endOffset) return insertOffset + index - startOffset;
	if (targetOffset > endOffset && index >= endOffset && index <= targetOffset) return index - moveLength;
	return remapIndexAfterMove(index, startOffset, endOffset, targetOffset, insertOffset, moveLength);
}
function clamp$1(value, min, max) {
	return Math.min(Math.max(value, min), max);
}

//#endregion
//#region src/commands/commands/doc-horizontal-line.command.ts
const HorizontalLineCommand = {
	id: "doc.command.horizontal-line",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(BreakLineCommand.id, {
			horizontalLine: {
				padding: 5,
				color: { rgb: "#CDD0D8" },
				width: 1,
				dashStyle: _univerjs_core.DashStyleType.SOLID
			},
			insertionMode: params === null || params === void 0 ? void 0 : params.insertionMode,
			textRange: params === null || params === void 0 ? void 0 : params.insertRange
		});
	}
};
const InsertHorizontalLineBellowCommand = {
	id: "doc.command.insert-horizontal-line-bellow",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const doc = accessor.get(_univerjs_core.IUniverInstanceService).getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		let contentInsertRange = null;
		try {
			contentInsertRange = accessor.get(_univerjs_docs.DocContentInsertService).consumeInsertRange(doc === null || doc === void 0 ? void 0 : doc.getUnitId());
		} catch {
			contentInsertRange = null;
		}
		if (contentInsertRange) return commandService.syncExecuteCommand(HorizontalLineCommand.id, {
			insertionMode: "insert-gap",
			insertRange: {
				startOffset: contentInsertRange.startOffset,
				endOffset: contentInsertRange.endOffset
			}
		});
		const paragraph = getCurrentParagraph(accessor);
		if (!paragraph) return false;
		return commandService.syncExecuteCommand(HorizontalLineCommand.id, { insertRange: {
			startOffset: paragraph.startIndex + 1,
			endOffset: paragraph.startIndex + 1
		} });
	}
};

//#endregion
//#region src/commands/commands/doc-select-all.command.ts
const DocSelectAllCommand = {
	id: "doc.command.select-all",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		var _docDataModel$getSelf;
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const docRanges = docSelectionManagerService.getDocRanges();
		const activeRange = docRanges.find((range) => range.isActive) ?? docRanges[0];
		if (docDataModel == null) return false;
		const segmentId = (params === null || params === void 0 ? void 0 : params.segmentId) ?? (activeRange === null || activeRange === void 0 ? void 0 : activeRange.segmentId);
		if (segmentId == null) return false;
		const unitId = docDataModel.getUnitId();
		const body = (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		if (body == null) return false;
		const { dataStream } = body;
		if (dataStream === "\r\n") return true;
		let textRanges;
		let wholeDocument;
		if (params === null || params === void 0 ? void 0 : params.wholeDocument) {
			textRanges = getWholeDocumentRanges(body);
			wholeDocument = true;
		} else {
			if (activeRange == null) return false;
			const scopes = getSelectAllScopes(body, activeRange);
			const currentScopeIndex = scopes.findIndex((scope) => isSameRanges(docRanges, scope));
			textRanges = scopes[Math.min(currentScopeIndex + 1, scopes.length - 1)];
			wholeDocument = textRanges === scopes[scopes.length - 1];
		}
		docSelectionManagerService.replaceDocRanges(textRanges, {
			unitId,
			subUnitId: unitId
		}, false, { wholeDocument });
		return true;
	}
};
function getWholeDocumentRanges(body) {
	return getRangesForOffsets(body, 0, body.dataStream.length - 2);
}
function getRangesForOffsets(body, startOffset, endOffset) {
	const columnGroups = [...body.columnGroups ?? []].sort((left, right) => left.startIndex - right.startIndex);
	const ranges = [];
	let offset = startOffset;
	for (const columnGroup of columnGroups) {
		if (columnGroup.endIndex < startOffset || columnGroup.startIndex > endOffset) continue;
		if (offset < columnGroup.startIndex) ranges.push(...getRangesWithTables(body, offset, Math.min(endOffset, columnGroup.startIndex - 1)));
		for (const column of getColumnRanges(body, columnGroup)) {
			const start = Math.max(startOffset, column.startOffset);
			const end = Math.min(endOffset, column.endOffset);
			if (start <= end) ranges.push(...getRangesWithTables(body, start, end));
		}
		offset = columnGroup.endIndex + 1;
	}
	if (offset <= endOffset) ranges.push(...getRangesWithTables(body, offset, endOffset));
	return ranges;
}
function getRangesWithTables(body, startOffset, endOffset) {
	const ranges = [];
	let offset = startOffset;
	for (const table of [...body.tables ?? []].sort((left, right) => left.startIndex - right.startIndex)) {
		const { startIndex, endIndex } = table;
		if (startIndex < startOffset || endIndex > endOffset) continue;
		if (offset !== startIndex) ranges.push(...getTextRangesByParagraphs(body, offset, startIndex - 1));
		ranges.push(getTableRectRange(table));
		offset = endIndex;
	}
	if (offset <= endOffset) ranges.push(...getTextRangesByParagraphs(body, offset, endOffset));
	return ranges;
}
function getTextRangesByParagraphs(body, startOffset, endOffset) {
	if (startOffset > endOffset) return [];
	const paragraphs = [...body.paragraphs ?? []].sort((left, right) => left.startIndex - right.startIndex);
	const ranges = [];
	let offset = startOffset;
	for (const paragraph of paragraphs) {
		if (paragraph.startIndex < offset) continue;
		if (paragraph.startIndex > endOffset) break;
		ranges.push({
			startOffset: offset,
			endOffset: paragraph.startIndex,
			rangeType: _univerjs_core.DOC_RANGE_TYPE.TEXT
		});
		offset = paragraph.startIndex + 1;
	}
	if (offset <= endOffset) ranges.push({
		startOffset: offset,
		endOffset,
		rangeType: _univerjs_core.DOC_RANGE_TYPE.TEXT
	});
	return ranges;
}
function getSelectAllScopes(body, activeRange) {
	const scopes = [];
	const addScope = (scope) => {
		if ((scope === null || scope === void 0 ? void 0 : scope.length) && !scopes.some((existing) => isSameRanges(existing, scope))) scopes.push(scope);
	};
	const startOffset = activeRange.startOffset;
	const endOffset = activeRange.endOffset;
	if (startOffset == null || endOffset == null) return [getWholeDocumentRanges(body)];
	const table = (body.tables ?? []).find((item) => isRangeInside(startOffset, endOffset, item.startIndex, item.endIndex));
	const customBlock = (body.customBlocks ?? []).find((item) => item.startIndex >= startOffset && item.startIndex <= endOffset);
	const blockRange = (body.blockRanges ?? []).find((item) => isRangeInside(startOffset, endOffset, item.startIndex, item.endIndex));
	if (table) addScope([getTableRectRange(table)]);
	else if (customBlock) addScope([{
		endOffset: customBlock.startIndex,
		rangeType: _univerjs_core.DOC_RANGE_TYPE.TEXT,
		startOffset: customBlock.startIndex
	}]);
	else if (blockRange) addScope([{
		endOffset: Math.max(blockRange.startIndex + 1, blockRange.endIndex - 1),
		rangeType: _univerjs_core.DOC_RANGE_TYPE.TEXT,
		startOffset: blockRange.startIndex + 1
	}]);
	else {
		const paragraphRange = clampParagraphRangeByTables(getParagraphRangeAtOffset(body, startOffset), body.tables ?? [], startOffset);
		addScope(paragraphRange ? [{
			...paragraphRange,
			rangeType: _univerjs_core.DOC_RANGE_TYPE.TEXT
		}] : null);
	}
	const columnGroup = (body.columnGroups ?? []).find((item) => isRangeInside(startOffset, endOffset, item.startIndex, item.endIndex));
	if (columnGroup) {
		const column = getColumnRanges(body, columnGroup).find((item) => isRangeInside(startOffset, endOffset, item.startOffset, item.endOffset));
		if (column) addScope(getRangesForOffsets(body, column.startOffset, column.endOffset));
		addScope(getRangesForOffsets(body, columnGroup.startIndex, columnGroup.endIndex));
	}
	addScope(getWholeDocumentRanges(body));
	return scopes;
}
function getColumnRanges(body, columnGroup) {
	const ranges = [];
	let startOffset;
	for (let index = columnGroup.startIndex; index <= columnGroup.endIndex; index++) {
		const token = body.dataStream[index];
		if (token === _univerjs_core.DataStreamTreeTokenType.COLUMN_START) startOffset = index + 1;
		else if (token === _univerjs_core.DataStreamTreeTokenType.COLUMN_END && startOffset != null) {
			ranges.push({
				startOffset,
				endOffset: index - 1,
				rangeType: _univerjs_core.DOC_RANGE_TYPE.TEXT
			});
			startOffset = void 0;
		}
	}
	return ranges;
}
function clampParagraphRangeByTables(paragraphRange, tables, activeOffset) {
	if (!paragraphRange) return null;
	const nextRange = { ...paragraphRange };
	for (const table of tables) if (activeOffset < table.startIndex && nextRange.startOffset < table.startIndex && table.startIndex <= nextRange.endOffset) nextRange.endOffset = table.startIndex - 1;
	else if (activeOffset >= table.endIndex && nextRange.startOffset <= table.endIndex && table.endIndex < nextRange.endOffset) nextRange.startOffset = table.endIndex;
	return nextRange.startOffset <= nextRange.endOffset ? nextRange : null;
}
function getTableRectRange(table) {
	return {
		startOffset: table.startIndex + 3,
		endOffset: table.endIndex - 5,
		rangeType: _univerjs_core.DOC_RANGE_TYPE.RECT
	};
}
function getParagraphRangeAtOffset(body, offset) {
	const sortedParagraphs = [...body.paragraphs ?? []].sort((left, right) => left.startIndex - right.startIndex);
	const paragraphStartOffsets = (0, _univerjs_core.getParagraphContentStartOffsets)(body);
	for (const paragraph of sortedParagraphs) {
		const startOffset = paragraphStartOffsets.get(paragraph.startIndex) ?? 0;
		if (startOffset <= offset && offset <= paragraph.startIndex) return {
			endOffset: paragraph.startIndex,
			startOffset
		};
	}
	return null;
}
function isRangeInside(startOffset, endOffset, scopeStart, scopeEnd) {
	return startOffset >= scopeStart && endOffset <= scopeEnd;
}
function isSameRanges(currentRanges, nextRanges) {
	if (currentRanges.length !== nextRanges.length) return isSameTextRangeCoverage(currentRanges, nextRanges);
	return currentRanges.every((currentRange, index) => {
		const nextRange = nextRanges[index];
		const sameType = getRangeType(currentRange) === getRangeType(nextRange);
		const sameTextEnd = sameType && getRangeType(nextRange) === _univerjs_core.DOC_RANGE_TYPE.TEXT && !currentRange.collapsed && currentRange.endOffset + 1 === nextRange.endOffset;
		return currentRange.startOffset === nextRange.startOffset && (currentRange.endOffset === nextRange.endOffset || sameTextEnd) && sameType;
	});
}
function isSameTextRangeCoverage(currentRanges, nextRanges) {
	if (nextRanges.length !== 1 || getRangeType(nextRanges[0]) !== _univerjs_core.DOC_RANGE_TYPE.TEXT) return false;
	const targetRange = nextRanges[0];
	if (targetRange.startOffset == null || targetRange.endOffset == null) return false;
	if (currentRanges.some((range) => getRangeType(range) !== _univerjs_core.DOC_RANGE_TYPE.TEXT || range.startOffset == null || range.endOffset == null)) return false;
	const startOffset = Math.min(...currentRanges.map((range) => range.startOffset));
	const endOffset = Math.max(...currentRanges.map((range) => range.endOffset));
	return startOffset === targetRange.startOffset && endOffset === targetRange.endOffset && currentRanges.every((range) => range.startOffset >= targetRange.startOffset && range.endOffset <= targetRange.endOffset);
}
function getRangeType(range) {
	return range.rangeType ?? _univerjs_core.DOC_RANGE_TYPE.TEXT;
}

//#endregion
//#region src/services/doc-ime-input-manager.service.ts
var DocIMEInputManagerService = class extends _univerjs_core.RxDisposable {
	constructor(_context) {
		super();
		this._context = _context;
		_defineProperty(this, "_previousActiveRange", null);
		_defineProperty(this, "_previousDocRanges", []);
		_defineProperty(this, "_compositionRange", null);
		_defineProperty(this, "_previousSelectionOptions", null);
		_defineProperty(this, "_undoMutationParamsCache", []);
		_defineProperty(this, "_redoMutationParamsCache", []);
	}
	clearUndoRedoMutationParamsCache() {
		this._undoMutationParamsCache = [];
		this._redoMutationParamsCache = [];
	}
	getUndoRedoMutationParamsCache() {
		return {
			undoCache: this._undoMutationParamsCache,
			redoCache: this._redoMutationParamsCache
		};
	}
	setUndoRedoMutationParamsCache({ undoCache = [], redoCache = [] }) {
		this._undoMutationParamsCache = undoCache;
		this._redoMutationParamsCache = redoCache;
	}
	getActiveRange() {
		return this._previousActiveRange;
	}
	setActiveRange(range) {
		this._previousActiveRange = range;
		this._compositionRange = range;
	}
	getCompositionRange() {
		return this._compositionRange;
	}
	setCompositionRange(range) {
		this._compositionRange = range;
	}
	getPreviousDocRanges() {
		return this._previousDocRanges;
	}
	setPreviousDocRanges(ranges) {
		this._previousDocRanges = ranges;
	}
	getPreviousSelectionOptions() {
		return this._previousSelectionOptions;
	}
	setPreviousSelectionOptions(options) {
		this._previousSelectionOptions = options;
	}
	pushUndoRedoMutationParams(undoParams, redoParams) {
		this._undoMutationParamsCache.push(undoParams);
		this._redoMutationParamsCache.push(redoParams);
	}
	fetchComposedUndoRedoMutationParams() {
		if (this._undoMutationParamsCache.length === 0 || this._previousActiveRange == null || this._redoMutationParamsCache.length === 0) return null;
		const { unitId } = this._undoMutationParamsCache[0];
		const firstUndoParams = this._undoMutationParamsCache[0];
		const lastRedoParams = this._redoMutationParamsCache.at(-1);
		const undoMutationParams = {
			unitId,
			actions: this._undoMutationParamsCache.reverse().reduce((acc, cur) => {
				return _univerjs_core.JSONX.compose(acc, cur.actions);
			}, null),
			textRanges: [],
			segmentId: firstUndoParams.segmentId
		};
		return {
			redoMutationParams: {
				unitId,
				actions: this._redoMutationParamsCache.reduce((acc, cur) => {
					return _univerjs_core.JSONX.compose(acc, cur.actions);
				}, null),
				textRanges: (lastRedoParams === null || lastRedoParams === void 0 ? void 0 : lastRedoParams.textRanges) ?? [],
				segmentId: lastRedoParams === null || lastRedoParams === void 0 ? void 0 : lastRedoParams.segmentId,
				options: lastRedoParams === null || lastRedoParams === void 0 ? void 0 : lastRedoParams.options,
				isEditing: lastRedoParams === null || lastRedoParams === void 0 ? void 0 : lastRedoParams.isEditing
			},
			undoMutationParams,
			previousActiveRange: this._previousActiveRange,
			previousDocRanges: this._previousDocRanges,
			previousSelectionOptions: this._previousSelectionOptions
		};
	}
	dispose() {
		this._undoMutationParamsCache = [];
		this._redoMutationParamsCache = [];
		this._previousActiveRange = null;
		this._previousDocRanges = [];
		this._compositionRange = null;
		this._previousSelectionOptions = null;
	}
};

//#endregion
//#region src/commands/commands/ime-input.command.ts
const IMEInputCommand = {
	id: "doc.command.ime-input",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		var _renderManagerService, _docDataModel$getSelf, _imeInputManagerServi;
		const { unitId, newText, oldTextLen, isCompositionEnd, isCompositionStart } = params;
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const renderManagerService = accessor.get(_univerjs_engine_render.IRenderManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const docMenuStyleService = accessor.get(DocMenuStyleService);
		const imeInputManagerService = (_renderManagerService = renderManagerService.getRenderUnitById(unitId)) === null || _renderManagerService === void 0 ? void 0 : _renderManagerService.with(DocIMEInputManagerService);
		const docDataModel = univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null || imeInputManagerService == null) return false;
		const previousActiveRange = imeInputManagerService.getCompositionRange();
		if (previousActiveRange == null) return false;
		const { style, segmentId } = previousActiveRange;
		const body = (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		if (body == null) return false;
		Object.assign(previousActiveRange, previousActiveRange);
		const { startOffset, endOffset } = previousActiveRange;
		const previousDocRanges = imeInputManagerService.getPreviousDocRanges();
		const previousRectRanges = previousDocRanges.filter(isRectRange);
		const previousTextRanges = previousDocRanges.filter((range) => !isRectRange(range));
		const wholeBodySelected = ((_imeInputManagerServi = imeInputManagerService.getPreviousSelectionOptions()) === null || _imeInputManagerServi === void 0 ? void 0 : _imeInputManagerServi.wholeDocument) === true;
		const replacesComplexSelection = isCompositionStart && (wholeBodySelected || previousRectRanges.length > 0 || previousTextRanges.length > 1);
		let replacementOffset = replacesComplexSelection ? wholeBodySelected ? 0 : getDocRangeInsertOffset(previousTextRanges, previousRectRanges) : startOffset;
		if (replacementOffset == null) return false;
		const len = newText.length;
		const textRanges = [{
			startOffset: replacementOffset + len,
			endOffset: replacementOffset + len,
			collapsed: true,
			style,
			segmentId
		}];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges,
				segmentId,
				trigger: IMEInputCommand.id
			}
		};
		const defaultTextStyle = docMenuStyleService.getDefaultStyle();
		const styleCache = docMenuStyleService.getStyleCache();
		const styleOffset = replacesComplexSelection ? replacementOffset : startOffset + oldTextLen;
		const curCustomRange = getCustomRangeAtPosition(body.customRanges ?? [], styleOffset, _univerjs_core.SHEET_EDITOR_UNITS.includes(unitId));
		const curTextRun = getTextRunAtPosition(body, replacesComplexSelection ? replacementOffset : isCompositionStart ? endOffset : startOffset + oldTextLen, defaultTextStyle, styleCache, _univerjs_core.SHEET_EDITOR_UNITS.includes(unitId));
		const customDecorations = getCustomDecorationAtPosition(body.customDecorations ?? [], styleOffset);
		const insertBody = {
			dataStream: newText,
			textRuns: curTextRun ? [{
				...curTextRun,
				st: 0,
				ed: newText.length
			}] : [],
			customRanges: curCustomRange ? [{
				...curCustomRange,
				startIndex: 0,
				endIndex: newText.length - 1
			}] : [],
			customDecorations: customDecorations.map((customDecoration) => ({
				...customDecoration,
				startIndex: 0,
				endIndex: newText.length - 1
			}))
		};
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		if (replacesComplexSelection) {
			var _renderManagerService2;
			const docSkeletonManagerService = (_renderManagerService2 = renderManagerService.getRenderUnitById(unitId)) === null || _renderManagerService2 === void 0 ? void 0 : _renderManagerService2.with(_univerjs_docs.DocSkeletonManagerService);
			if (!docSkeletonManagerService) return false;
			const replacement = getReplaceDocRangesActions(previousTextRanges, previousRectRanges, docDataModel, docSkeletonManagerService.getViewModel(), segmentId ?? "", insertBody, wholeBodySelected);
			if (!replacement) return false;
			replacementOffset = replacement.insertOffset;
			doMutation.params.actions = replacement.actions;
			doMutation.params.textRanges = [{
				startOffset: replacementOffset + len,
				endOffset: replacementOffset + len,
				collapsed: true,
				style,
				segmentId
			}];
		} else if (!previousActiveRange.collapsed && isCompositionStart) {
			const dos = _univerjs_core.BuildTextUtils.selection.delete([previousActiveRange], body, 0, null, false);
			textX.push(...dos);
			doMutation.params.textRanges = [{
				startOffset: replacementOffset + len,
				endOffset: replacementOffset + len,
				collapsed: true,
				segmentId
			}];
		} else textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: startOffset
		});
		if (!replacesComplexSelection && oldTextLen > 0) textX.push({
			t: _univerjs_core.TextXActionType.DELETE,
			len: oldTextLen
		});
		if (!replacesComplexSelection) textX.push({
			t: _univerjs_core.TextXActionType.INSERT,
			body: insertBody,
			len: newText.length
		});
		if (!replacesComplexSelection) {
			const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
			doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		}
		doMutation.params.noHistory = !isCompositionEnd;
		doMutation.params.isCompositionEnd = isCompositionEnd;
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		imeInputManagerService.pushUndoRedoMutationParams(result, doMutation.params);
		if (replacesComplexSelection) imeInputManagerService.setCompositionRange({
			...previousActiveRange,
			startOffset: replacementOffset,
			endOffset: replacementOffset,
			collapsed: true
		});
		return Boolean(result);
	}
};
function isRectRange(range) {
	return "tableId" in range;
}

//#endregion
//#region src/services/doc-render-background.ts
const DOC_TRADITIONAL_WORKSPACE_BACKGROUND_COLOR = "var(--univer-gray-100)";
const DOC_MODERN_WORKSPACE_BACKGROUND_COLOR = "var(--univer-gray-0)";
const DOC_UNSPECIFIED_WORKSPACE_BACKGROUND_COLOR = "var(--univer-gray-100)";
const DOC_EDITOR_INTERNAL_BACKGROUND_COLOR = "transparent";
const DOC_TRADITIONAL_WORKSPACE_BACKGROUND_TOKEN = "gray.100";
const DOC_MODERN_WORKSPACE_BACKGROUND_TOKEN = "gray.0";
function resolveDocRenderBackground(options) {
	const { documentFlavor, canvasColorService, editorBackgroundColor, isEditor } = options;
	const backgroundColor = editorBackgroundColor ?? getDefaultDocCanvasBackgroundColor(documentFlavor, isEditor);
	if (isEditor) return {
		canvasElementBackgroundColor: backgroundColor,
		docBackgroundFillColor: DOC_EDITOR_INTERNAL_BACKGROUND_COLOR
	};
	if (editorBackgroundColor != null) return {
		canvasElementBackgroundColor: (canvasColorService === null || canvasColorService === void 0 ? void 0 : canvasColorService.getRenderColor(editorBackgroundColor)) ?? editorBackgroundColor,
		docBackgroundFillColor: void 0
	};
	if (documentFlavor !== _univerjs_core.DocumentFlavor.TRADITIONAL && documentFlavor !== _univerjs_core.DocumentFlavor.MODERN) return {
		canvasElementBackgroundColor: backgroundColor,
		docBackgroundFillColor: void 0
	};
	const renderBackgroundColor = getDefaultDocCanvasBackgroundToken(documentFlavor);
	return {
		canvasElementBackgroundColor: (canvasColorService === null || canvasColorService === void 0 ? void 0 : canvasColorService.getRenderColor(renderBackgroundColor)) ?? backgroundColor,
		docBackgroundFillColor: void 0
	};
}
function getDefaultDocCanvasBackgroundColor(documentFlavor, isEditor) {
	if (isEditor) return DOC_EDITOR_INTERNAL_BACKGROUND_COLOR;
	switch (documentFlavor) {
		case _univerjs_core.DocumentFlavor.MODERN: return DOC_MODERN_WORKSPACE_BACKGROUND_COLOR;
		case _univerjs_core.DocumentFlavor.TRADITIONAL: return DOC_TRADITIONAL_WORKSPACE_BACKGROUND_COLOR;
		default: return DOC_UNSPECIFIED_WORKSPACE_BACKGROUND_COLOR;
	}
}
function getDefaultDocCanvasBackgroundToken(documentFlavor) {
	return documentFlavor === _univerjs_core.DocumentFlavor.MODERN ? DOC_MODERN_WORKSPACE_BACKGROUND_TOKEN : DOC_TRADITIONAL_WORKSPACE_BACKGROUND_TOKEN;
}

//#endregion
//#region src/commands/commands/replace-content.command.ts
const ReplaceSnapshotCommand = {
	id: "doc.command-replace-snapshot",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		var _docDataModel$getSelf;
		const { unitId, snapshot, textRanges, segmentId = "", options } = params;
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const docDataModel = univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
		const prevSnapshot = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getSnapshot();
		if (docDataModel == null || prevSnapshot == null) return false;
		const { body, tableSource, footers, headers, lists, drawings, drawingsOrder, documentStyle } = _univerjs_core.Tools.deepClone(snapshot);
		const { body: prevBody, tableSource: prevTableSource, footers: prevFooters, headers: prevHeaders, lists: prevLists, drawings: prevDrawings, drawingsOrder: prevDrawingsOrder, documentStyle: prevDocumentStyle } = prevSnapshot;
		if (body == null || prevBody == null) return false;
		if (_univerjs_core.Tools.diffValue(body, prevBody) && textRanges) {
			docSelectionManagerService.replaceDocRanges(textRanges, {
				unitId,
				subUnitId: unitId
			}, false);
			return true;
		}
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges
			}
		};
		if (options) doMutation.params.options = options;
		doMutation.params.actions = buildReplaceSnapshotActions({
			body: prevBody,
			documentStyle: prevDocumentStyle,
			tableSource: prevTableSource,
			footers: prevFooters,
			headers: prevHeaders,
			lists: prevLists,
			drawings: prevDrawings,
			drawingsOrder: prevDrawingsOrder
		}, {
			body,
			documentStyle,
			tableSource,
			footers,
			headers,
			lists,
			drawings,
			drawingsOrder
		});
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
function buildReplaceSnapshotActions(previousSnapshot, snapshot) {
	const jsonX = _univerjs_core.JSONX.getInstance();
	const rawActions = [];
	const bodyAction = buildReplaceSnapshotBodyAction(previousSnapshot.body, snapshot.body);
	if (bodyAction) rawActions.push(bodyAction);
	collectTopLevelReplaceAction(jsonX, rawActions, ["documentStyle"], previousSnapshot.documentStyle, snapshot.documentStyle);
	collectTopLevelReplaceAction(jsonX, rawActions, ["tableSource"], previousSnapshot.tableSource, snapshot.tableSource);
	collectTopLevelReplaceAction(jsonX, rawActions, ["footers"], previousSnapshot.footers, snapshot.footers);
	collectTopLevelReplaceAction(jsonX, rawActions, ["headers"], previousSnapshot.headers, snapshot.headers);
	collectTopLevelReplaceAction(jsonX, rawActions, ["lists"], previousSnapshot.lists, snapshot.lists);
	collectTopLevelReplaceAction(jsonX, rawActions, ["drawings"], previousSnapshot.drawings, snapshot.drawings);
	collectTopLevelReplaceAction(jsonX, rawActions, ["drawingsOrder"], previousSnapshot.drawingsOrder, snapshot.drawingsOrder);
	return rawActions.reduce((acc, cur) => _univerjs_core.JSONX.compose(acc, cur), null);
}
function buildReplaceSnapshotBodyAction(previousBody, body) {
	if (!previousBody || !body || _univerjs_core.Tools.diffValue(previousBody, body)) return null;
	const textX = new _univerjs_core.TextX();
	if (previousBody.dataStream.length > 0) textX.delete(previousBody.dataStream.length);
	if (body.dataStream.length > 0) textX.insert(body.dataStream.length, body);
	return _univerjs_core.JSONX.getInstance().editOp(textX.serialize(), ["body"]);
}
function collectTopLevelReplaceAction(jsonX, rawActions, path, previousValue, value) {
	if (_univerjs_core.Tools.diffValue(previousValue, value)) return;
	const action = jsonX.replaceOp(path, previousValue, value);
	if (action != null) rawActions.push(action);
}
const CoverContentCommand = {
	id: "doc.command-cover-content",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		const { unitId, body, segmentId = "", textRanges } = params;
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const undoRedoService = accessor.get(_univerjs_core.IUndoRedoService);
		const docDatModel = univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
		const prevBody = docDatModel === null || docDatModel === void 0 ? void 0 : docDatModel.getSnapshot().body;
		if (docDatModel == null || prevBody == null) return false;
		const doMutation = getMutationParams(unitId, segmentId, docDatModel, prevBody, body);
		doMutation.params.noNeedSetTextRange = true;
		doMutation.params.noHistory = true;
		doMutation.params.textRanges = textRanges;
		commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		undoRedoService.clearUndoRedo(unitId);
		return true;
	}
};
function getMutationActions(segmentId, docDatModel, prevBody, body) {
	const textX = new _univerjs_core.TextX();
	const jsonX = _univerjs_core.JSONX.getInstance();
	const deleteLen = (prevBody === null || prevBody === void 0 ? void 0 : prevBody.dataStream.length) - 2;
	if (deleteLen > 0) textX.push({
		t: _univerjs_core.TextXActionType.DELETE,
		len: deleteLen
	});
	if (body.dataStream.length > 0) textX.push({
		t: _univerjs_core.TextXActionType.INSERT,
		body,
		len: body.dataStream.length
	});
	const path = (0, _univerjs_core.getRichTextEditPath)(docDatModel, segmentId);
	return jsonX.editOp(textX.serialize(), path);
}
function getMutationParams(unitId, segmentId, docDatModel, prevBody, body) {
	const doMutation = {
		id: _univerjs_docs.RichTextEditingMutation.id,
		params: {
			unitId,
			actions: [],
			textRanges: []
		}
	};
	const actions = getMutationActions(segmentId, docDatModel, prevBody, body);
	doMutation.params.actions = actions;
	return doMutation;
}
const ReplaceSelectionCommand = {
	id: "doc.command.replace-selection",
	type: _univerjs_core.CommandType.COMMAND,
	handler(accessor, params) {
		var _docSelectionManagerS, _docDataModel$getSelf2, _body$blockRanges, _body$columnGroups, _body$customBlocks, _body$tables, _selectionInfo$option, _selectionInfo$option2, _docSelectionManagerS2;
		if (!params) return false;
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const { unitId, body: insertBody, historyAction, textRanges, segmentId } = params;
		const docDataModel = accessor.get(_univerjs_core.IUniverInstanceService).getUnit(unitId);
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		if (!docDataModel) return false;
		const selection = params.selection ?? docSelectionManagerService.getActiveTextRange();
		const targetSegmentId = segmentId ?? ((_docSelectionManagerS = docSelectionManagerService.getActiveTextRange()) === null || _docSelectionManagerS === void 0 ? void 0 : _docSelectionManagerS.segmentId) ?? "";
		const body = (_docDataModel$getSelf2 = docDataModel.getSelfOrHeaderFooterModel(targetSegmentId)) === null || _docDataModel$getSelf2 === void 0 ? void 0 : _docDataModel$getSelf2.getBody();
		if (!selection || !body) return false;
		const selectionInfo = docSelectionManagerService.getSelectionInfo();
		const selectedTextRanges = params.selection ? [params.selection] : docSelectionManagerService.getTextRanges() ?? [];
		const selectedRectRanges = params.selection ? [] : docSelectionManagerService.getRectRanges() ?? [];
		const docSkeletonManagerService = !selection.collapsed && (Boolean((_body$blockRanges = body.blockRanges) === null || _body$blockRanges === void 0 ? void 0 : _body$blockRanges.length) || Boolean((_body$columnGroups = body.columnGroups) === null || _body$columnGroups === void 0 ? void 0 : _body$columnGroups.length) || Boolean((_body$customBlocks = body.customBlocks) === null || _body$customBlocks === void 0 ? void 0 : _body$customBlocks.length) || Boolean((_body$tables = body.tables) === null || _body$tables === void 0 ? void 0 : _body$tables.length)) || selectedRectRanges.length > 0 || selectedTextRanges.length > 1 || (selectionInfo === null || selectionInfo === void 0 || (_selectionInfo$option = selectionInfo.options) === null || _selectionInfo$option === void 0 ? void 0 : _selectionInfo$option.wholeDocument) === true ? getCommandSkeleton(accessor, unitId) : null;
		const replacement = docSkeletonManagerService ? getReplaceDocRangesActions(selectedTextRanges, selectedRectRanges, docDataModel, docSkeletonManagerService.getViewModel(), targetSegmentId, insertBody, (selectionInfo === null || selectionInfo === void 0 || (_selectionInfo$option2 = selectionInfo.options) === null || _selectionInfo$option2 === void 0 ? void 0 : _selectionInfo$option2.wholeDocument) === true) : null;
		const insertOffset = (replacement === null || replacement === void 0 ? void 0 : replacement.insertOffset) ?? selection.startOffset;
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				historyAction,
				actions: [],
				textRanges: textRanges ?? [{
					startOffset: insertOffset + insertBody.dataStream.length,
					endOffset: insertOffset + insertBody.dataStream.length,
					collapsed: true,
					style: (_docSelectionManagerS2 = docSelectionManagerService.getActiveTextRange()) === null || _docSelectionManagerS2 === void 0 ? void 0 : _docSelectionManagerS2.style,
					segmentId: targetSegmentId
				}],
				segmentId: targetSegmentId,
				debounce: true,
				trigger: ReplaceSelectionCommand.id
			}
		};
		if (replacement) doMutation.params.actions = replacement.actions;
		else {
			const textX = new _univerjs_core.TextX();
			textX.push(..._univerjs_core.BuildTextUtils.selection.delete([selection], body, 0, insertBody));
			doMutation.params.actions = _univerjs_core.JSONX.getInstance().editOp(textX.serialize(), (0, _univerjs_core.getRichTextEditPath)(docDataModel, targetSegmentId));
		}
		return commandService.syncExecuteCommand(doMutation.id, doMutation.params);
	}
};
const ReplaceTextRunsCommand = {
	id: "doc.command.replace-text-runs",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		var _docDataModel$getSelf3;
		const { unitId, body, textRanges, segmentId = "", options } = params;
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const themeService = accessor.get(_univerjs_core.ThemeService);
		const docDataModel = univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
		const prevBody = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf3 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf3 === void 0 ? void 0 : _docDataModel$getSelf3.getBody();
		if (docDataModel == null || prevBody == null) return false;
		const textX = _univerjs_core.BuildTextUtils.selection.replaceTextRuns({
			doc: docDataModel,
			body,
			selection: {
				startOffset: 0,
				endOffset: prevBody.dataStream.length - 2,
				collapsed: false
			},
			themeService
		});
		if (!textX) return false;
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges,
				noHistory: true
			}
		};
		const jsonX = _univerjs_core.JSONX.getInstance();
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		doMutation.params.textRanges = textRanges;
		if (options) doMutation.params.options = options;
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};

//#endregion
//#region src/services/doc-embed-integration.service.ts
const DOC_EMBED_INTERACTION_BOUNDARY_OWNER_ATTRIBUTE = "data-embed-interaction-boundary-owner";
var EmbedInteractionBoundaryService = class {
	constructor() {
		_defineProperty(this, "_roots", /* @__PURE__ */ new Map());
	}
	registerOwnedElement(embedId, element) {
		element.setAttribute(DOC_EMBED_INTERACTION_BOUNDARY_OWNER_ATTRIBUTE, embedId);
		element.querySelectorAll("*").forEach((child) => child.setAttribute(DOC_EMBED_INTERACTION_BOUNDARY_OWNER_ATTRIBUTE, embedId));
		let roots = this._roots.get(embedId);
		if (!roots) {
			roots = /* @__PURE__ */ new Set();
			this._roots.set(embedId, roots);
		}
		roots.add(element);
		return (0, _univerjs_core.toDisposable)(() => {
			roots === null || roots === void 0 || roots.delete(element);
			element.removeAttribute(DOC_EMBED_INTERACTION_BOUNDARY_OWNER_ATTRIBUTE);
			element.querySelectorAll("*").forEach((child) => child.removeAttribute(DOC_EMBED_INTERACTION_BOUNDARY_OWNER_ATTRIBUTE));
		});
	}
	contains(embedId, target) {
		var _this$_roots$get;
		if (!(target instanceof HTMLElement)) return false;
		if (!embedId) return [...this._roots.values()].some((roots) => [...roots].some((root) => root.contains(target)));
		return ((_this$_roots$get = this._roots.get(embedId)) === null || _this$_roots$get === void 0 ? void 0 : _this$_roots$get.has(target)) === true || this._roots.get(embedId) != null && [...this._roots.get(embedId)].some((root) => root.contains(target));
	}
	hasRecentInteraction() {
		return false;
	}
	hasRecentInteractionFor() {
		return false;
	}
};
const IDocEmbedInteractionBoundaryService = EmbedInteractionBoundaryService;
var EmbedRuntimeFocusCoordinator = class {
	constructor() {
		_defineProperty(this, "_leases", /* @__PURE__ */ new Set());
		_defineProperty(this, "_elements", /* @__PURE__ */ new Map());
		_defineProperty(this, "runtimeSessionChanged$", new rxjs.Subject());
	}
	acquireLease(options) {
		this._leases.add(options);
		this.runtimeSessionChanged$.next();
		return (0, _univerjs_core.toDisposable)(() => {
			this._leases.delete(options);
			this.runtimeSessionChanged$.next();
		});
	}
	registerElement(options) {
		options.element.setAttribute(DOC_EMBED_INTERACTION_BOUNDARY_OWNER_ATTRIBUTE, options.embedId);
		let elements = this._elements.get(options.embedId);
		if (!elements) {
			elements = /* @__PURE__ */ new Set();
			this._elements.set(options.embedId, elements);
		}
		elements.add(options.element);
		return (0, _univerjs_core.toDisposable)(() => {
			elements === null || elements === void 0 || elements.delete(options.element);
			options.element.removeAttribute(DOC_EMBED_INTERACTION_BOUNDARY_OWNER_ATTRIBUTE);
		});
	}
	registerRuntimeScope(_options) {
		return (0, _univerjs_core.toDisposable)(() => {});
	}
	isChildUnitRuntimeEvent(_unitId, target) {
		return target instanceof HTMLElement && target.closest(`[${"data-embed-interaction-boundary-owner"}]`) != null;
	}
	isChildUnitInActiveSession(unitId) {
		return [...this._leases].some((lease) => lease.role !== "runtime" && lease.childUnitId === unitId);
	}
	shouldSuppressHostInteraction(unitId, target) {
		if (this.isChildUnitRuntimeEvent(unitId, target)) return false;
		return [...this._leases].some((lease) => {
			if (lease.role === "runtime" || lease.childUnitId === unitId) return false;
			return lease.hostUnitId === unitId || !lease.hostUnitId && !lease.childUnitId;
		});
	}
};
const IDocEmbedRuntimeFocusCoordinator = EmbedRuntimeFocusCoordinator;

//#endregion
//#region src/services/selection/convert-text-range.ts
let NodePositionType = /* @__PURE__ */ function(NodePositionType) {
	NodePositionType[NodePositionType["page"] = 0] = "page";
	NodePositionType[NodePositionType["section"] = 1] = "section";
	NodePositionType[NodePositionType["column"] = 2] = "column";
	NodePositionType[NodePositionType["line"] = 3] = "line";
	NodePositionType[NodePositionType["divide"] = 4] = "divide";
	NodePositionType[NodePositionType["glyph"] = 5] = "glyph";
	return NodePositionType;
}({});
const NodePositionMap = {
	page: 0,
	section: 1,
	column: 2,
	line: 3,
	divide: 4,
	glyph: 5
};
function compareNodePositionLogic(pos1, pos2) {
	const nestedPagePathOrder = (0, _univerjs_engine_render.compareDocumentSkeletonNestedPagePathOrder)(pos1, pos2);
	if (nestedPagePathOrder != null) return nestedPagePathOrder;
	if (pos1.page > pos2.page) return false;
	if (pos1.page < pos2.page) return true;
	if (pos1.section > pos2.section) return false;
	if (pos1.section < pos2.section) return true;
	if (pos1.column > pos2.column) return false;
	if (pos1.column < pos2.column) return true;
	if (pos1.line > pos2.line) return false;
	if (pos1.line < pos2.line) return true;
	if (pos1.divide > pos2.divide) return false;
	if (pos1.divide < pos2.divide) return true;
	if (pos1.glyph > pos2.glyph) return false;
	if (pos1.glyph < pos2.glyph) return true;
	return true;
}
function compareNodePosition(pos1, pos2) {
	if (compareNodePositionLogic(pos1, pos2)) return {
		start: pos1,
		end: pos2
	};
	return {
		start: pos2,
		end: pos1
	};
}
function getOneTextSelectionRange(rangeList) {
	const rangeCount = rangeList.length;
	if (rangeCount === 0) return;
	const firstCursor = rangeList[0];
	const lastCursor = rangeList[rangeCount - 1];
	const collapsed = rangeList.length === 1 && firstCursor.collapsed;
	return {
		startOffset: firstCursor.startOffset,
		endOffset: lastCursor.endOffset,
		collapsed
	};
}
function getOffsetInDivide(glyphGroup, startGlyphIndex, endGlyphIndex, st) {
	let startOffset = st;
	let endOffset = st;
	for (let i = 0; i < glyphGroup.length; i++) {
		const contentLength = glyphGroup[i].count;
		if (i < startGlyphIndex) startOffset += contentLength;
		if (i < endGlyphIndex) endOffset += contentLength;
	}
	return {
		startOffset,
		endOffset
	};
}
function pushToPoints(position) {
	const { startX, startY, endX, endY } = position;
	const points = [];
	points.push({
		x: startX,
		y: startY
	});
	points.push({
		x: endX,
		y: startY
	});
	points.push({
		x: endX,
		y: endY
	});
	points.push({
		x: startX,
		y: endY
	});
	points.push({
		x: startX,
		y: startY
	});
	return points;
}
var NodePositionConvertToCursor = class {
	constructor(_documentOffsetConfig, _docSkeleton) {
		this._documentOffsetConfig = _documentOffsetConfig;
		this._docSkeleton = _docSkeleton;
		_defineProperty(this, "_liquid", new _univerjs_engine_render.Liquid());
		_defineProperty(this, "_horizontalClip", null);
		_defineProperty(this, "_currentStartState", {
			page: 0,
			section: 0,
			column: 0,
			line: 0,
			divide: 0,
			glyph: 0
		});
		_defineProperty(this, "_currentEndState", {
			page: 0,
			section: 0,
			column: 0,
			line: 0,
			divide: 0,
			glyph: 0
		});
	}
	getRangePointData(startOrigin, endOrigin) {
		const borderBoxPointGroup = [];
		const contentBoxPointGroup = [];
		const cursorList = [];
		if (startOrigin == null || endOrigin == null) return {
			borderBoxPointGroup,
			contentBoxPointGroup,
			cursorList
		};
		if (!this._isValidPosition(startOrigin, endOrigin)) throw new Error(`
                Invalid positions in NodePositionConvertToCursor,
                they are not in the same segment page when in header or footer.`);
		const { start, end } = compareNodePosition(startOrigin, endOrigin);
		this._selectionIterator(start, end, (start_sp, end_sp, isFirst, isLast, divide, line) => {
			const { lineHeight, asc, paddingTop, paddingBottom, contentHeight, marginTop, marginBottom } = line;
			const { glyphGroup, st } = divide;
			if (glyphGroup.length === 0) return;
			const { x: startX, y: startY } = this._liquid;
			let borderBoxPosition;
			let contentBoxPosition;
			const firstGlyph = glyphGroup[start_sp];
			const lastGlyph = glyphGroup[end_sp];
			const preGlyph = glyphGroup[start_sp - 1];
			const firstGlyphLeft = (firstGlyph === null || firstGlyph === void 0 ? void 0 : firstGlyph.left) || 0;
			const firstGlyphWidth = (firstGlyph === null || firstGlyph === void 0 ? void 0 : firstGlyph.width) || 0;
			const lastGlyphLeft = (lastGlyph === null || lastGlyph === void 0 ? void 0 : lastGlyph.left) || 0;
			const lastGlyphWidth = (lastGlyph === null || lastGlyph === void 0 ? void 0 : lastGlyph.width) || 0;
			const isCurrentList = (firstGlyph === null || firstGlyph === void 0 ? void 0 : firstGlyph.glyphType) === _univerjs_engine_render.GlyphType.LIST;
			const { startOffset, endOffset } = getOffsetInDivide(glyphGroup, start_sp, end_sp, st);
			const isStartBack = start.glyph === start_sp && isFirst ? start.isBack : true;
			const isEndBack = end.glyph === end_sp && isLast ? end.isBack : false;
			const collapsed = start === end;
			const rawAnchorGlyph = isStartBack ? preGlyph ?? firstGlyph : firstGlyph;
			const anchorGlyph = this._getCaretGlyph(rawAnchorGlyph, glyphGroup, start_sp);
			const selectedGlyphs = glyphGroup.slice(start_sp, end_sp + 1);
			const isSelectionOnlyNonInlineDrawing = !collapsed && selectedGlyphs.length > 0 && selectedGlyphs.every((glyph) => this._isNonInlineDrawingGlyph(glyph));
			const borderBoxStartY = startY;
			const borderBoxEndY = contentHeight == null ? startY + lineHeight - marginTop - marginBottom : startY + paddingTop + contentHeight + paddingBottom;
			if (start_sp === 0 && end_sp === glyphGroup.length - 1) {
				borderBoxPosition = {
					startX: startX + firstGlyphLeft + (isCurrentList ? firstGlyphWidth : 0),
					startY: borderBoxStartY,
					endX: startX + lastGlyphLeft + (isEndBack ? 0 : lastGlyphWidth),
					endY: borderBoxEndY
				};
				contentBoxPosition = {
					startX: startX + firstGlyphLeft + (isCurrentList ? firstGlyphWidth : 0),
					startY: startY + paddingTop + asc - anchorGlyph.bBox.ba,
					endX: startX + lastGlyphLeft + (isEndBack ? 0 : lastGlyphWidth),
					endY: startY + paddingTop + asc + anchorGlyph.bBox.bd
				};
			} else {
				const isStartBackFin = isStartBack && !isCurrentList;
				borderBoxPosition = {
					startX: startX + firstGlyphLeft + (isStartBackFin ? 0 : firstGlyphWidth),
					startY: borderBoxStartY,
					endX: startX + lastGlyphLeft + (isEndBack ? 0 : lastGlyphWidth),
					endY: borderBoxEndY
				};
				contentBoxPosition = {
					startX: startX + firstGlyphLeft + (isStartBackFin ? 0 : firstGlyphWidth),
					startY: startY + paddingTop + asc - anchorGlyph.bBox.ba,
					endX: startX + lastGlyphLeft + (isEndBack ? 0 : lastGlyphWidth),
					endY: startY + paddingTop + asc + anchorGlyph.bBox.bd
				};
			}
			const clippedBorderBoxPosition = clipPositionToHorizontalRange(borderBoxPosition, this._horizontalClip);
			const clippedContentBoxPosition = clipPositionToHorizontalRange(contentBoxPosition, this._horizontalClip);
			if (clippedBorderBoxPosition && !isSelectionOnlyNonInlineDrawing) borderBoxPointGroup.push(pushToPoints(clippedBorderBoxPosition));
			if (clippedContentBoxPosition && !isSelectionOnlyNonInlineDrawing) contentBoxPointGroup.push(pushToPoints(clippedContentBoxPosition));
			cursorList.push({
				startOffset: isStartBack ? startOffset : startOffset + firstGlyph.count,
				endOffset: isEndBack ? endOffset : endOffset + lastGlyph.count,
				collapsed
			});
		});
		return {
			borderBoxPointGroup,
			contentBoxPointGroup,
			cursorList
		};
	}
	_getCaretGlyph(glyph, glyphGroup, glyphIndex) {
		if (!glyph || !this._isNonInlineDrawingGlyph(glyph)) return glyph;
		return this._findTextLikeGlyph(glyphGroup, glyphIndex - 1, -1) ?? this._findTextLikeGlyph(glyphGroup, glyphIndex + 1, 1) ?? {
			...glyph,
			bBox: getDefaultCaretBoundingBox(glyph)
		};
	}
	_findTextLikeGlyph(glyphGroup, startIndex, step) {
		for (let index = startIndex; index >= 0 && index < glyphGroup.length; index += step) {
			const glyph = glyphGroup[index];
			if (!this._isNonInlineDrawingGlyph(glyph)) return glyph;
		}
	}
	_isNonInlineDrawingGlyph(glyph) {
		var _this$_docSkeleton$ge, _this$_docSkeleton, _this$_docSkeleton$ge2, _this$_docSkeleton$ge3, _this$_docSkeleton$ge4, _this$_docSkeleton$ge5;
		if (!(glyph === null || glyph === void 0 ? void 0 : glyph.drawingId) || glyph.streamType !== _univerjs_core.DataStreamTreeTokenType.CUSTOM_BLOCK) return false;
		const drawing = (_this$_docSkeleton$ge = (_this$_docSkeleton = this._docSkeleton).getViewModel) === null || _this$_docSkeleton$ge === void 0 || (_this$_docSkeleton$ge3 = (_this$_docSkeleton$ge2 = _this$_docSkeleton$ge.call(_this$_docSkeleton)).getDataModel) === null || _this$_docSkeleton$ge3 === void 0 || (_this$_docSkeleton$ge5 = (_this$_docSkeleton$ge4 = _this$_docSkeleton$ge3.call(_this$_docSkeleton$ge2)).getSnapshot) === null || _this$_docSkeleton$ge5 === void 0 || (_this$_docSkeleton$ge5 = _this$_docSkeleton$ge5.call(_this$_docSkeleton$ge4).drawings) === null || _this$_docSkeleton$ge5 === void 0 ? void 0 : _this$_docSkeleton$ge5[glyph.drawingId];
		return (drawing === null || drawing === void 0 ? void 0 : drawing.layoutType) == null ? !(0, _univerjs_docs.shouldUseInlineTextSelectionForDocsCustomBlockDrawing)(drawing) : drawing.layoutType !== _univerjs_core.PositionedObjectLayoutType.INLINE;
	}
	_isValidPosition(startOrigin, endOrigin) {
		const { segmentPage: startPage, pageType: startPageType } = startOrigin;
		const { segmentPage: endPage, pageType: endPageType } = endOrigin;
		if (startPageType !== endPageType) return false;
		if (startPageType === _univerjs_engine_render.DocumentSkeletonPageType.HEADER || startPageType === _univerjs_engine_render.DocumentSkeletonPageType.FOOTER) return startPage === endPage;
		return true;
	}
	_resetCurrentNodePositionState() {
		this._currentStartState = {
			page: 0,
			section: 0,
			column: 0,
			line: 0,
			divide: 0,
			glyph: 0
		};
		this._currentEndState = {
			page: 0,
			section: 0,
			column: 0,
			line: 0,
			divide: 0,
			glyph: 0
		};
	}
	_setNodePositionState(type = 0, start, end, current) {
		if (current === start) this._currentStartState[type] = 1;
		else this._currentStartState[type] = 0;
		if (current === end) this._currentEndState[type] = 2;
		else this._currentEndState[type] = 0;
	}
	_checkPreviousNodePositionState(typeIndex, isStart = true) {
		let index = typeIndex;
		let resultState;
		while (index >= 0) {
			const type = NodePositionType[index];
			let state;
			if (isStart) state = this._currentStartState[type];
			else state = this._currentEndState[type];
			if (state === void 0) return;
			if (resultState === void 0) resultState = state;
			if (state !== resultState) return 0;
			index--;
		}
		return resultState;
	}
	_getSelectionRuler(typeIndex, startPosition, endPosition, nextLength, current) {
		let start_next = 0;
		let end_next = nextLength;
		const type = NodePositionType[typeIndex];
		const nextType = NodePositionType[typeIndex + 1];
		if (nextType === null || type === null) return {
			start_next,
			end_next
		};
		const start = startPosition[type];
		const end = endPosition[type];
		this._setNodePositionState(type, start, end, current);
		const preStartNestType = this._checkPreviousNodePositionState(typeIndex);
		const preEndNestType = this._checkPreviousNodePositionState(typeIndex, false);
		if (preStartNestType === 1) start_next = startPosition[nextType];
		if (preEndNestType === 2) end_next = endPosition[nextType];
		return {
			start_next,
			end_next
		};
	}
	_selectionIterator(startPosition, endPosition, func) {
		const skeleton = this._docSkeleton;
		if (!skeleton) return [];
		const { pageType, path } = startPosition;
		this._liquid.reset();
		const skeletonData = skeleton.getSkeletonData();
		if (skeletonData == null) return [];
		const { pages, skeHeaders, skeFooters } = skeletonData;
		const { page: pageIndex, segmentPage } = startPosition;
		const { page: endPageIndex, segmentPage: endSegmentPage } = endPosition;
		this._resetCurrentNodePositionState();
		if (this._documentOffsetConfig == null) return [];
		const { pageLayoutType, pageMarginLeft, pageMarginTop } = this._documentOffsetConfig;
		const startRootPageIndex = pageType === _univerjs_engine_render.DocumentSkeletonPageType.CELL && pages[pageIndex] == null ? segmentPage : pageIndex;
		const endRootPageIndex = pageType === _univerjs_engine_render.DocumentSkeletonPageType.CELL && pages[endPageIndex] == null ? endSegmentPage : endPageIndex;
		const skipPageIndex = pageType === _univerjs_engine_render.DocumentSkeletonPageType.BODY || pageType === _univerjs_engine_render.DocumentSkeletonPageType.CELL ? startRootPageIndex : segmentPage;
		for (let p = 0; p < skipPageIndex; p++) {
			const page = pages[p];
			this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
		}
		const endIndex = pageType === _univerjs_engine_render.DocumentSkeletonPageType.BODY || pageType === _univerjs_engine_render.DocumentSkeletonPageType.CELL ? endRootPageIndex : endSegmentPage;
		for (let p = skipPageIndex; p <= endIndex; p++) {
			const page = pages[p];
			if (page == null) continue;
			const { headerId, footerId, pageWidth } = page;
			let segmentPage = page;
			if (pageType === _univerjs_engine_render.DocumentSkeletonPageType.HEADER) {
				var _skeHeaders$get;
				segmentPage = (_skeHeaders$get = skeHeaders.get(headerId)) === null || _skeHeaders$get === void 0 ? void 0 : _skeHeaders$get.get(pageWidth);
			} else if (pageType === _univerjs_engine_render.DocumentSkeletonPageType.FOOTER) {
				var _skeFooters$get;
				segmentPage = (_skeFooters$get = skeFooters.get(footerId)) === null || _skeFooters$get === void 0 ? void 0 : _skeFooters$get.get(pageWidth);
			} else if (pageType === _univerjs_engine_render.DocumentSkeletonPageType.CELL) segmentPage = path[0] === "pages" ? (0, _univerjs_engine_render.getPageFromPath)(skeletonData, path) : getCellPageFromSegmentPath(skeletonData, page, path);
			if (segmentPage == null) {
				this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
				continue;
			}
			const sections = segmentPage.sections;
			const { start_next: start_s, end_next: end_s } = this._getSelectionRuler(NodePositionMap.page, startPosition, endPosition, sections.length - 1, pageType === _univerjs_engine_render.DocumentSkeletonPageType.BODY || pageType === _univerjs_engine_render.DocumentSkeletonPageType.CELL ? p : 0);
			this._liquid.translateSave();
			const previousHorizontalClip = this._horizontalClip;
			this._horizontalClip = null;
			switch (pageType) {
				case _univerjs_engine_render.DocumentSkeletonPageType.HEADER:
					this._liquid.translatePagePadding({
						...segmentPage,
						marginLeft: page.marginLeft
					});
					break;
				case _univerjs_engine_render.DocumentSkeletonPageType.FOOTER: {
					const footerTop = page.pageHeight - segmentPage.height - segmentPage.marginBottom;
					this._liquid.translate(page.marginLeft, footerTop);
					break;
				}
				case _univerjs_engine_render.DocumentSkeletonPageType.CELL: {
					const nestedPageOffset = (0, _univerjs_engine_render.getDocumentSkeletonNestedPageOffset)(segmentPage);
					if (nestedPageOffset) {
						this._liquid.translatePagePadding(page);
						this._liquid.translate(nestedPageOffset.left, nestedPageOffset.top);
						this._liquid.translatePagePadding(segmentPage);
						break;
					}
					const rowSke = segmentPage.parent;
					const tableSke = rowSke.parent;
					const tablePage = tableSke.parent;
					const tablePageNestedOffset = tablePage ? (0, _univerjs_engine_render.getDocumentSkeletonNestedPageOffset)(tablePage) : void 0;
					const { left: cellLeft } = segmentPage;
					const { top: tableTop, left: tableLeft } = tableSke;
					const { top: rowTop } = rowSke;
					const sourceTableId = (0, _univerjs_engine_render.getTableIdAndSliceIndex)(tableSke.tableId).tableId;
					const viewport = (0, _univerjs_engine_render.getDocsTableRenderViewport)(getDocumentUnitId$1(skeleton), sourceTableId);
					const hasHorizontalViewport = hasHorizontalTableViewport$2(viewport);
					const scrollLeft = hasHorizontalViewport ? viewport.scrollLeft : 0;
					if ((tablePage === null || tablePage === void 0 ? void 0 : tablePage.type) === _univerjs_engine_render.DocumentSkeletonPageType.HEADER) this._liquid.translatePagePadding({
						...tablePage,
						marginLeft: page.marginLeft
					});
					else if ((tablePage === null || tablePage === void 0 ? void 0 : tablePage.type) === _univerjs_engine_render.DocumentSkeletonPageType.FOOTER) {
						const footerTop = page.pageHeight - tablePage.height - tablePage.marginBottom;
						this._liquid.translate(page.marginLeft, footerTop);
					} else this._liquid.translatePagePadding(page);
					if (tablePageNestedOffset) {
						this._liquid.translate(tablePageNestedOffset.left, tablePageNestedOffset.top);
						this._liquid.translatePagePadding(tablePage);
					}
					if (hasHorizontalViewport) {
						const visibleLeft = this._liquid.x + tableLeft - (viewport.leadingInsetLeft ?? 0);
						this._horizontalClip = {
							left: visibleLeft,
							right: visibleLeft + viewport.viewportWidth
						};
					}
					this._liquid.translate(tableLeft + cellLeft - scrollLeft, tableTop + rowTop);
					this._liquid.translatePagePadding(segmentPage);
					break;
				}
				default:
					this._liquid.translatePagePadding(page);
					break;
			}
			for (let s = start_s; s <= end_s; s++) {
				const section = sections[s];
				const columns = section.columns;
				const { start_next: start_c, end_next: end_c } = this._getSelectionRuler(NodePositionMap.section, startPosition, endPosition, columns.length - 1, s);
				this._liquid.translateSave();
				this._liquid.translateSection(section);
				for (let c = start_c; c <= end_c; c++) {
					const column = columns[c];
					const lines = column.lines;
					const { start_next: start_l, end_next: end_l } = this._getSelectionRuler(NodePositionMap.column, startPosition, endPosition, lines.length - 1, c);
					this._liquid.translateSave();
					this._liquid.translateColumn(column);
					for (let l = start_l; l <= end_l; l++) {
						const line = lines[l];
						const { divides } = line;
						const { start_next: start_d, end_next: end_d } = this._getSelectionRuler(NodePositionMap.line, startPosition, endPosition, divides.length - 1, l);
						this._liquid.translateSave();
						this._liquid.translateLine(line, true, false);
						for (let d = start_d; d <= end_d; d++) {
							const divide = divides[d];
							this._liquid.translateSave();
							this._liquid.translateDivide(divide);
							const { glyphGroup } = divide;
							const { start_next: start_sp, end_next: end_sp } = this._getSelectionRuler(NodePositionMap.divide, startPosition, endPosition, glyphGroup.length - 1, d);
							let isFirst = false;
							let isLast = false;
							if (p === skipPageIndex && s === start_s && c === start_c && l === start_l && d === start_d) isFirst = true;
							if (p === endIndex && s === end_s && c === end_c && l === end_l && d === end_d) isLast = true;
							func && func(start_sp, end_sp, isFirst, isLast, divide, line, column, section, segmentPage);
							this._liquid.translateRestore();
						}
						this._liquid.translateRestore();
					}
					this._liquid.translateRestore();
				}
				this._liquid.translateRestore();
			}
			this._liquid.translateRestore();
			this._horizontalClip = previousHorizontalClip;
			this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
		}
	}
};
function hasHorizontalTableViewport$2(viewport) {
	return viewport != null && (viewport.leadingInsetLeft ?? 0) + viewport.contentWidth + (viewport.trailingInsetRight ?? 0) > viewport.viewportWidth;
}
function clipPositionToHorizontalRange(position, clip) {
	if (!clip) return position;
	const startX = Math.max(position.startX, clip.left);
	const endX = Math.min(position.endX, clip.right);
	if (position.startX === position.endX) return position.startX >= clip.left && position.startX <= clip.right ? position : null;
	if (endX <= startX) return null;
	return {
		...position,
		startX,
		endX
	};
}
function getCellPageFromSegmentPath(skeletonData, rootPage, path) {
	var _skeletonData$skeHead, _skeletonData$skeFoot;
	if (path[0] === "pages") return null;
	const segmentPages = [];
	const { headerId, footerId, pageWidth } = rootPage;
	const headerPage = headerId == null ? null : (_skeletonData$skeHead = skeletonData.skeHeaders.get(headerId)) === null || _skeletonData$skeHead === void 0 ? void 0 : _skeletonData$skeHead.get(pageWidth);
	const footerPage = footerId == null ? null : (_skeletonData$skeFoot = skeletonData.skeFooters.get(footerId)) === null || _skeletonData$skeFoot === void 0 ? void 0 : _skeletonData$skeFoot.get(pageWidth);
	if (headerPage != null) segmentPages.push(headerPage);
	if (footerPage != null) segmentPages.push(footerPage);
	for (const segmentPage of segmentPages) {
		const page = (0, _univerjs_engine_render.getPageFromPath)({
			...skeletonData,
			pages: [segmentPage]
		}, [
			"pages",
			0,
			...path
		]);
		if (page != null) return page;
	}
	return null;
}
function getDefaultCaretBoundingBox(glyph) {
	const fontSize = getGlyphFontSize(glyph);
	return {
		...glyph.bBox,
		ba: fontSize,
		bd: Math.max(2, Math.ceil(fontSize * .25))
	};
}
function getGlyphFontSize(glyph) {
	var _glyph$fontStyle, _glyph$fontStyle2, _glyph$ts;
	const fontSize = ((_glyph$fontStyle = glyph.fontStyle) === null || _glyph$fontStyle === void 0 ? void 0 : _glyph$fontStyle.originFontSize) ?? ((_glyph$fontStyle2 = glyph.fontStyle) === null || _glyph$fontStyle2 === void 0 ? void 0 : _glyph$fontStyle2.fontSize) ?? ((_glyph$ts = glyph.ts) === null || _glyph$ts === void 0 ? void 0 : _glyph$ts.fs);
	return typeof fontSize === "number" && Number.isFinite(fontSize) && fontSize > 0 ? fontSize : 14;
}
function getDocumentUnitId$1(docSkeleton) {
	var _viewModel$getDataMod, _viewModel$getDataMod2, _viewModel$getDataMod3;
	const viewModel = docSkeleton.getViewModel();
	return ((_viewModel$getDataMod = viewModel.getDataModel) === null || _viewModel$getDataMod === void 0 || (_viewModel$getDataMod3 = (_viewModel$getDataMod2 = _viewModel$getDataMod.call(viewModel)).getUnitId) === null || _viewModel$getDataMod3 === void 0 ? void 0 : _viewModel$getDataMod3.call(_viewModel$getDataMod2)) ?? "";
}

//#endregion
//#region src/services/selection/convert-rect-range.ts
function isValidRectRange(anchorNodePosition, focusNodePosition) {
	const { path: anchorPath } = anchorNodePosition;
	const { path: focusPath } = focusNodePosition;
	if (anchorPath.length !== focusPath.length) return false;
	if (anchorPath.indexOf("cells") === -1) return false;
	const tableIdIndex = anchorPath.indexOf("skeTables") + 1;
	const rowIndex = anchorPath.indexOf("rows") + 1;
	const cellIndex = anchorPath.indexOf("cells") + 1;
	const { tableId: anchorTableId, sliceIndex: anchorSliceIndex } = (0, _univerjs_engine_render.getTableIdAndSliceIndex)(anchorPath[tableIdIndex]);
	const { tableId: focusTableId, sliceIndex: focusSliceIndex } = (0, _univerjs_engine_render.getTableIdAndSliceIndex)(focusPath[tableIdIndex]);
	if (anchorTableId !== focusTableId) return false;
	const anchorRowIndex = anchorPath[rowIndex];
	const focusRowIndex = focusPath[rowIndex];
	const anchorCellIndex = anchorPath[cellIndex];
	const focusCellIndex = focusPath[cellIndex];
	if (anchorRowIndex === focusRowIndex && anchorCellIndex === focusCellIndex && anchorSliceIndex === focusSliceIndex) return false;
	return true;
}
function isInSameTableCell(anchorNodePosition, focusNodePosition) {
	const { path: anchorPath } = anchorNodePosition;
	const { path: focusPath } = focusNodePosition;
	if (anchorPath.indexOf("cells") === -1 || focusPath.indexOf("cells") === -1) return false;
	const anchorTableIdIndex = anchorPath.indexOf("skeTables") + 1;
	const anchorRowIndex = anchorPath.indexOf("rows") + 1;
	const anchorCellIndex = anchorPath.indexOf("cells") + 1;
	const focusTableIdIndex = focusPath.indexOf("skeTables") + 1;
	const focusRowIndex = focusPath.indexOf("rows") + 1;
	const focusCellIndex = focusPath.indexOf("cells") + 1;
	if (anchorTableIdIndex === 0 || anchorRowIndex === 0 || anchorCellIndex === 0 || focusTableIdIndex === 0 || focusRowIndex === 0 || focusCellIndex === 0) return false;
	return anchorPath[anchorTableIdIndex] === focusPath[focusTableIdIndex] && anchorPath[anchorRowIndex] === focusPath[focusRowIndex] && anchorPath[anchorCellIndex] === focusPath[focusCellIndex];
}
function isInSameTableCellData(skeleton, anchorNodePosition, focusNodePosition) {
	var _anchorGlyph$parent, _focusGlyph$parent;
	const { path: anchorPath } = anchorNodePosition;
	const { path: focusPath } = focusNodePosition;
	if (anchorPath.indexOf("cells") === -1 || focusPath.indexOf("cells") === -1) return false;
	const anchorGlyph = skeleton.findGlyphByPosition(anchorNodePosition);
	const focusGlyph = skeleton.findGlyphByPosition(focusNodePosition);
	const anchorCellPage = anchorGlyph === null || anchorGlyph === void 0 || (_anchorGlyph$parent = anchorGlyph.parent) === null || _anchorGlyph$parent === void 0 || (_anchorGlyph$parent = _anchorGlyph$parent.parent) === null || _anchorGlyph$parent === void 0 || (_anchorGlyph$parent = _anchorGlyph$parent.parent) === null || _anchorGlyph$parent === void 0 || (_anchorGlyph$parent = _anchorGlyph$parent.parent) === null || _anchorGlyph$parent === void 0 ? void 0 : _anchorGlyph$parent.parent;
	const focusCellPage = focusGlyph === null || focusGlyph === void 0 || (_focusGlyph$parent = focusGlyph.parent) === null || _focusGlyph$parent === void 0 || (_focusGlyph$parent = _focusGlyph$parent.parent) === null || _focusGlyph$parent === void 0 || (_focusGlyph$parent = _focusGlyph$parent.parent) === null || _focusGlyph$parent === void 0 || (_focusGlyph$parent = _focusGlyph$parent.parent) === null || _focusGlyph$parent === void 0 ? void 0 : _focusGlyph$parent.parent;
	if (anchorCellPage == null || focusCellPage == null) return false;
	const anchorRow = anchorCellPage.parent;
	const focusRow = focusCellPage.parent;
	return anchorRow.cells.indexOf(anchorCellPage) === focusRow.cells.indexOf(focusCellPage) && anchorRow.index === focusRow.index;
}
function compareNodePositionInTable(a, b) {
	if (isInSameTableCell(a, b)) return compareNodePositionLogic(a, b);
	const { path: aPath } = a;
	const { path: bPath } = b;
	const aTableId = aPath[aPath.length - 5];
	const bTableId = bPath[bPath.length - 5];
	if (aTableId !== bTableId && typeof aTableId === "string" && typeof bTableId === "string") {
		const aSlideId = aTableId.split("#-#")[1];
		const bSlideId = bTableId.split("#-#")[1];
		return +aSlideId < +bSlideId;
	}
	const aRowCount = aPath[aPath.length - 3];
	const bRowCount = bPath[bPath.length - 3];
	const aCellCount = aPath[aPath.length - 1];
	const bCellCount = bPath[bPath.length - 1];
	if (aRowCount < bRowCount) return true;
	if (aRowCount > bRowCount) return false;
	return aCellCount <= bCellCount;
}
function isEmptyCellPage(cell) {
	return cell.sections[0].columns[0].lines.length === 0;
}
function findNonEmptyCellPages(cells, startCol, endCol) {
	let s = startCol;
	let e = endCol;
	let startCell = cells[s];
	let endCell = cells[e];
	while (s < e && (isEmptyCellPage(startCell) || isEmptyCellPage(endCell))) if (isEmptyCellPage(startCell)) {
		s++;
		startCell = cells[s];
	} else if (isEmptyCellPage(endCell)) {
		e--;
		endCell = cells[e];
	}
	if (!isEmptyCellPage(startCell) && !isEmptyCellPage(endCell)) return [startCell, endCell];
}
function getColumnBoundary(table, column) {
	var _table$tableSource;
	const columns = (_table$tableSource = table.tableSource) === null || _table$tableSource === void 0 ? void 0 : _table$tableSource.tableColumns;
	if (columns && column >= 0 && column <= columns.length) return columns.slice(0, column).reduce((total, tableColumn) => {
		var _tableColumn$size;
		return total + (((_tableColumn$size = tableColumn.size) === null || _tableColumn$size === void 0 || (_tableColumn$size = _tableColumn$size.width) === null || _tableColumn$size === void 0 ? void 0 : _tableColumn$size.v) ?? 0);
	}, 0);
	const row = table.rows[0];
	const cell = row === null || row === void 0 ? void 0 : row.cells[column];
	if (cell) return cell.left;
	const previousCell = row === null || row === void 0 ? void 0 : row.cells[column - 1];
	if (previousCell) return previousCell.left + previousCell.pageWidth;
	return null;
}
function getDocumentUnitId(docSkeleton) {
	var _viewModel$getDataMod, _viewModel$getDataMod2, _viewModel$getDataMod3;
	const viewModel = docSkeleton.getViewModel();
	return ((_viewModel$getDataMod = viewModel.getDataModel) === null || _viewModel$getDataMod === void 0 || (_viewModel$getDataMod3 = (_viewModel$getDataMod2 = _viewModel$getDataMod.call(viewModel)).getUnitId) === null || _viewModel$getDataMod3 === void 0 ? void 0 : _viewModel$getDataMod3.call(_viewModel$getDataMod2)) ?? "";
}
function pushViewportClippedPoints(pointGroup, position, viewport, tableLeft) {
	const scrollLeft = hasHorizontalTableViewport$1(viewport) ? viewport.scrollLeft : 0;
	const viewportWidth = hasHorizontalTableViewport$1(viewport) ? viewport.viewportWidth : null;
	const visibleLeft = tableLeft - ((viewport === null || viewport === void 0 ? void 0 : viewport.leadingInsetLeft) ?? 0);
	const startX = position.startX - scrollLeft;
	const endX = position.endX - scrollLeft;
	const clippedStartX = viewportWidth == null ? startX : Math.max(startX, visibleLeft);
	const clippedEndX = viewportWidth == null ? endX : Math.min(endX, visibleLeft + viewportWidth);
	if (clippedEndX <= clippedStartX) return;
	pointGroup.push(pushToPoints({
		...position,
		startX: clippedStartX,
		endX: clippedEndX
	}));
}
function hasHorizontalTableViewport$1(viewport) {
	return viewport != null && (viewport.leadingInsetLeft ?? 0) + viewport.contentWidth + (viewport.trailingInsetRight ?? 0) > viewport.viewportWidth;
}
var NodePositionConvertToRectRange = class {
	constructor(_documentOffsetConfig, _docSkeleton) {
		this._documentOffsetConfig = _documentOffsetConfig;
		this._docSkeleton = _docSkeleton;
		_defineProperty(this, "_liquid", new _univerjs_engine_render.Liquid());
	}
	getRangePointData(startNodePosition, endNodePosition) {
		const pointGroup = [];
		const docSkeleton = this._docSkeleton;
		const skeletonData = docSkeleton.getSkeletonData();
		if (skeletonData == null) return;
		const { pages } = skeletonData;
		const { segmentPage: startSegmentPage, page: startPage, pageType } = startNodePosition;
		const rectInfo = this._getTableRectRangeInfo(startNodePosition, endNodePosition);
		if (rectInfo == null) return;
		const { tableId, startRowIndex: startRow, startColumnIndex: startColumn, endRowIndex: endRow, endColumnIndex: endColumn, intersectsMergedCell } = rectInfo;
		this._liquid.reset();
		const { pageLayoutType, pageMarginLeft, pageMarginTop } = this._documentOffsetConfig;
		const unitId = getDocumentUnitId(docSkeleton);
		const sourceTableId = (0, _univerjs_engine_render.getTableIdAndSliceIndex)(tableId).tableId;
		const nestedTableContexts = (0, _univerjs_engine_render.documentSkeletonTableIterator)(pages, {
			docsLeft: this._documentOffsetConfig.docsLeft ?? 0,
			docsTop: this._documentOffsetConfig.docsTop ?? 0,
			pageMarginTop,
			skeFooters: skeletonData === null || skeletonData === void 0 ? void 0 : skeletonData.skeFooters,
			skeHeaders: skeletonData === null || skeletonData === void 0 ? void 0 : skeletonData.skeHeaders,
			unitId
		});
		const startRootPage = pageType === _univerjs_engine_render.DocumentSkeletonPageType.CELL && pages[startPage] == null ? startSegmentPage : startPage;
		const skipPageIndex = pageType === _univerjs_engine_render.DocumentSkeletonPageType.BODY || pageType === _univerjs_engine_render.DocumentSkeletonPageType.CELL ? startRootPage : startSegmentPage;
		for (let p = 0; p < skipPageIndex; p++) {
			const page = pages[p];
			this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
		}
		for (let p = skipPageIndex; p < pages.length; p++) {
			const page = pages[p];
			this._liquid.translatePagePadding(page);
			const { skeTables } = page;
			let table = null;
			for (const [id, tableSke] of skeTables.entries()) if (id.startsWith(tableId)) table = tableSke;
			if (table == null) {
				const nestedTableContext = nestedTableContexts.find((context) => context.pageIndex === p && (context.source === "column" || context.source === "header" || context.source === "footer") && context.tableId.startsWith(tableId));
				if (nestedTableContext) {
					const nestedTable = nestedTableContext.table;
					const viewport = (0, _univerjs_engine_render.getDocsTableRenderViewport)(unitId, sourceTableId);
					const nestedX = nestedTableContext.tableRect.left - nestedTable.left - (this._documentOffsetConfig.docsLeft ?? 0);
					const nestedY = nestedTableContext.tableRect.top - (this._documentOffsetConfig.docsTop ?? 0);
					if (intersectsMergedCell) {
						const rows = nestedTable.rows.filter((row) => row.index >= startRow && row.index <= endRow);
						const firstRow = rows[0];
						const lastRow = rows[rows.length - 1];
						const startX = getColumnBoundary(nestedTable, startColumn);
						const endX = getColumnBoundary(nestedTable, endColumn + 1);
						if (firstRow && lastRow && startX != null && endX != null) pushViewportClippedPoints(pointGroup, {
							startX: nestedX + nestedTable.left + startX,
							startY: nestedY + firstRow.top,
							endX: nestedX + nestedTable.left + endX,
							endY: nestedY + lastRow.top + lastRow.height
						}, viewport, nestedX + nestedTable.left);
						this._liquid.restorePagePadding(page);
						this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
						continue;
					}
					for (const row of nestedTable.rows) if (row.index >= startRow && row.index <= endRow) {
						const cells = findNonEmptyCellPages(row.cells, startColumn, endColumn);
						if (cells == null) continue;
						const [rowStartCell, rowEndCell] = cells;
						pushViewportClippedPoints(pointGroup, {
							startX: nestedX + rowStartCell.left + nestedTable.left,
							startY: nestedY + row.top,
							endX: nestedX + rowEndCell.left + rowEndCell.pageWidth + nestedTable.left,
							endY: nestedY + row.top + row.height
						}, viewport, nestedX + nestedTable.left);
					}
					this._liquid.restorePagePadding(page);
					this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
					continue;
				}
				this._liquid.restorePagePadding(page);
				this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
				continue;
			}
			this._liquid.translateSave();
			this._liquid.translate(0, table.top);
			const { x, y } = this._liquid;
			const { left: tableLeft } = table;
			const viewport = (0, _univerjs_engine_render.getDocsTableRenderViewport)(unitId, sourceTableId);
			if (intersectsMergedCell) {
				const rows = table.rows.filter((row) => row.index >= startRow && row.index <= endRow);
				const firstRow = rows[0];
				const lastRow = rows[rows.length - 1];
				const startX = getColumnBoundary(table, startColumn);
				const endX = getColumnBoundary(table, endColumn + 1);
				if (firstRow && lastRow && startX != null && endX != null) pushViewportClippedPoints(pointGroup, {
					startX: x + tableLeft + startX,
					startY: y + firstRow.top,
					endX: x + tableLeft + endX,
					endY: y + lastRow.top + lastRow.height
				}, viewport, x + tableLeft);
				this._liquid.translateRestore();
				this._liquid.restorePagePadding(page);
				this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
				continue;
			}
			for (const row of table.rows) if (row.index >= startRow && row.index <= endRow) {
				const cells = findNonEmptyCellPages(row.cells, startColumn, endColumn);
				if (cells == null) continue;
				const [rowStartCell, rowEndCell] = cells;
				pushViewportClippedPoints(pointGroup, {
					startX: x + rowStartCell.left + tableLeft,
					startY: y + row.top,
					endX: x + rowEndCell.left + rowEndCell.pageWidth + tableLeft,
					endY: y + row.top + row.height
				}, viewport, x + tableLeft);
			}
			this._liquid.translateRestore();
			this._liquid.restorePagePadding(page);
			this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
		}
		return {
			pointGroup,
			startRow,
			startColumn,
			endRow,
			endColumn,
			tableId
		};
	}
	getNodePositionGroup(anchorNodePosition, focusNodePosition) {
		const nodePositionGroup = [];
		const anchorIndex = this._docSkeleton.findCharIndexByPosition(anchorNodePosition);
		const focusIndex = this._docSkeleton.findCharIndexByPosition(focusNodePosition);
		if (anchorIndex == null || focusIndex == null) return;
		const compare = anchorIndex < focusIndex;
		const rectInfo = this._getTableRectRangeInfo(anchorNodePosition, focusNodePosition);
		if (rectInfo == null) return;
		const { tableId, startRowIndex, startColumnIndex, endRowIndex, endColumnIndex, intersectsMergedCell } = rectInfo;
		if (intersectsMergedCell) return [{
			anchor: anchorNodePosition,
			focus: focusNodePosition
		}];
		const tableNode = this._docSkeleton.getViewModel().findTableNodeById(tableId);
		if (tableNode == null) return;
		const totalColumns = tableNode.children[0].children.length;
		if (startColumnIndex === 0 && endColumnIndex === totalColumns - 1) {
			const startCellNode = tableNode.children[startRowIndex].children[startColumnIndex];
			const startNodePosition = this._docSkeleton.findNodePositionByCharIndex(startCellNode.startIndex + 1);
			const endCellNode = tableNode.children[endRowIndex].children[endColumnIndex];
			const endNodePosition = this._docSkeleton.findNodePositionByCharIndex(endCellNode.endIndex - 2);
			if (startNodePosition == null || endNodePosition == null) return;
			nodePositionGroup.push({
				anchor: compare ? startNodePosition : endNodePosition,
				focus: compare ? endNodePosition : startNodePosition
			});
		} else for (let i = startRowIndex; i <= endRowIndex; i++) {
			const rowNode = tableNode.children[i];
			const startCellNode = rowNode.children[startColumnIndex];
			const endCellNode = rowNode.children[endColumnIndex];
			const startNodePosition = this._docSkeleton.findNodePositionByCharIndex(startCellNode.startIndex + 1);
			const endNodePosition = this._docSkeleton.findNodePositionByCharIndex(endCellNode.endIndex - 2);
			if (startNodePosition == null || endNodePosition == null) return;
			nodePositionGroup.push({
				anchor: compare ? startNodePosition : endNodePosition,
				focus: compare ? endNodePosition : startNodePosition
			});
		}
		return nodePositionGroup;
	}
	_getTableRectRangeInfo(anchorPosition, focusPosition) {
		var _docSkeleton$getViewM;
		const docSkeleton = this._docSkeleton;
		const skeletonData = docSkeleton.getSkeletonData();
		if (skeletonData == null) return;
		const { pages } = skeletonData;
		const { path: anchorPath } = anchorPosition;
		const { path: focusPath } = focusPosition;
		if (anchorPath.indexOf("cells") === -1 || focusPath.indexOf("cells") === -1) return;
		const anchorCell = getCellPageFromPositionPath(skeletonData, anchorPosition);
		const focusCell = getCellPageFromPositionPath(skeletonData, focusPosition);
		if (anchorCell == null || focusCell == null) return;
		const anchorRow = anchorCell.parent;
		const focusRow = focusCell.parent;
		if (!Array.isArray(anchorRow === null || anchorRow === void 0 ? void 0 : anchorRow.cells) || !Array.isArray(focusRow === null || focusRow === void 0 ? void 0 : focusRow.cells)) return;
		const tableId = anchorCell.segmentId;
		const anchorRowIndex = anchorRow.index;
		const anchorColumn = anchorRow.cells.indexOf(anchorCell);
		const focusRowIndex = focusRow.index;
		const focusColumn = focusRow.cells.indexOf(focusCell);
		const sourceTableId = (0, _univerjs_engine_render.getTableIdAndSliceIndex)(tableId).tableId;
		const tableSource = (_docSkeleton$getViewM = docSkeleton.getViewModel().getSnapshot().tableSource) === null || _docSkeleton$getViewM === void 0 ? void 0 : _docSkeleton$getViewM[sourceTableId];
		const rawRange = {
			startRowIndex: Math.min(anchorRowIndex, focusRowIndex),
			endRowIndex: Math.max(anchorRowIndex, focusRowIndex),
			startColumnIndex: Math.min(anchorColumn, focusColumn),
			endColumnIndex: Math.max(anchorColumn, focusColumn)
		};
		return {
			pages,
			tableId,
			intersectsMergedCell: rangeIntersectsMergedCell(tableSource, rawRange),
			...expandRangeByMergedCells(tableSource, rawRange)
		};
	}
};
function expandRangeByMergedCells(table, range) {
	if (!table) return range;
	let expanded = normalizeRange(range);
	let changed = true;
	while (changed) {
		changed = false;
		table.tableRows.forEach((row, rowIndex) => {
			row.tableCells.forEach((cell, columnIndex) => {
				const rowSpan = cell.rowSpan ?? 1;
				const columnSpan = cell.columnSpan ?? 1;
				if (rowSpan <= 0 || columnSpan <= 0 || rowSpan === 1 && columnSpan === 1) return;
				const mergedRange = {
					startRowIndex: rowIndex,
					endRowIndex: rowIndex + rowSpan - 1,
					startColumnIndex: columnIndex,
					endColumnIndex: columnIndex + columnSpan - 1
				};
				if (!rangesIntersect(expanded, mergedRange)) return;
				const next = {
					startRowIndex: Math.min(expanded.startRowIndex, mergedRange.startRowIndex),
					endRowIndex: Math.max(expanded.endRowIndex, mergedRange.endRowIndex),
					startColumnIndex: Math.min(expanded.startColumnIndex, mergedRange.startColumnIndex),
					endColumnIndex: Math.max(expanded.endColumnIndex, mergedRange.endColumnIndex)
				};
				if (!rangesEqual(expanded, next)) {
					expanded = next;
					changed = true;
				}
			});
		});
	}
	return expanded;
}
function rangeIntersectsMergedCell(table, range) {
	if (!table) return false;
	const normalized = normalizeRange(range);
	return table.tableRows.some((row, rowIndex) => row.tableCells.some((cell, columnIndex) => {
		const rowSpan = cell.rowSpan ?? 1;
		const columnSpan = cell.columnSpan ?? 1;
		if (rowSpan <= 0 || columnSpan <= 0 || rowSpan === 1 && columnSpan === 1) return false;
		return rangesIntersect(normalized, {
			startRowIndex: rowIndex,
			endRowIndex: rowIndex + rowSpan - 1,
			startColumnIndex: columnIndex,
			endColumnIndex: columnIndex + columnSpan - 1
		});
	}));
}
function getCellPageFromPositionPath(skeletonData, position) {
	var _skeletonData$skeHead, _skeletonData$skeFoot;
	const { path, segmentPage } = position;
	if (path[0] === "pages") return (0, _univerjs_engine_render.getPageFromPath)(skeletonData, path);
	const rootPage = skeletonData.pages[segmentPage];
	if (rootPage == null) return null;
	const { headerId, footerId, pageWidth } = rootPage;
	const segmentPages = [headerId == null ? null : (_skeletonData$skeHead = skeletonData.skeHeaders.get(headerId)) === null || _skeletonData$skeHead === void 0 ? void 0 : _skeletonData$skeHead.get(pageWidth), footerId == null ? null : (_skeletonData$skeFoot = skeletonData.skeFooters.get(footerId)) === null || _skeletonData$skeFoot === void 0 ? void 0 : _skeletonData$skeFoot.get(pageWidth)];
	for (const segmentPage of segmentPages) {
		if (segmentPage == null) continue;
		const page = (0, _univerjs_engine_render.getPageFromPath)({
			...skeletonData,
			pages: [segmentPage]
		}, [
			"pages",
			0,
			...path
		]);
		if (page != null) return page;
	}
	return null;
}
function normalizeRange(range) {
	return {
		startRowIndex: Math.min(range.startRowIndex, range.endRowIndex),
		endRowIndex: Math.max(range.startRowIndex, range.endRowIndex),
		startColumnIndex: Math.min(range.startColumnIndex, range.endColumnIndex),
		endColumnIndex: Math.max(range.startColumnIndex, range.endColumnIndex)
	};
}
function rangesIntersect(left, right) {
	return left.startRowIndex <= right.endRowIndex && left.endRowIndex >= right.startRowIndex && left.startColumnIndex <= right.endColumnIndex && left.endColumnIndex >= right.startColumnIndex;
}
function rangesEqual(left, right) {
	return left.startRowIndex === right.startRowIndex && left.endRowIndex === right.endRowIndex && left.startColumnIndex === right.startColumnIndex && left.endColumnIndex === right.endColumnIndex;
}

//#endregion
//#region src/services/selection/text-range.ts
const TEXT_RANGE_KEY_PREFIX = "__TestSelectionRange__";
const TEXT_ANCHOR_KEY_PREFIX = "__TestSelectionAnchor__";
const ID_LENGTH$1 = 6;
const BLINK_ON = 500;
const DEFAULT_CARET_COLOR = "gray.1000";
const TEXT_RANGE_LAYER_INDEX = 3;
function cursorConvertToTextRange(scene, range, docSkeleton, document) {
	const { startOffset, endOffset, style = _univerjs_engine_render.NORMAL_TEXT_SELECTION_PLUGIN_STYLE, segmentId = "", segmentPage } = range;
	const anchorNodePosition = docSkeleton.findNodePositionByCharIndex(startOffset, true, segmentId, segmentPage);
	const focusNodePosition = startOffset !== endOffset ? docSkeleton.findNodePositionByCharIndex(endOffset, true, segmentId, segmentPage) : null;
	return new TextRange(scene, document, docSkeleton, anchorNodePosition, focusNodePosition, style, segmentId, segmentPage);
}
function getAnchorBounding(pointsGroup) {
	const points = pointsGroup[0];
	const startPoint = points[0];
	const endPoint = points[2];
	const { x: startX, y: startY } = startPoint;
	const { x: endX, y: endY } = endPoint;
	return {
		left: startX,
		top: startY,
		width: endX - startX,
		height: endY - startY
	};
}
function getLineBounding(pointsGroup) {
	return pointsGroup.map((line) => {
		let xMin = Infinity;
		let xMax = -Infinity;
		let yMin = Infinity;
		let yMax = -Infinity;
		line.forEach((point) => {
			xMin = Math.min(point.x, xMin);
			xMax = Math.max(point.x, xMax);
			yMax = Math.max(point.y, yMax);
			yMin = Math.min(point.y, yMin);
		});
		return {
			left: xMin,
			right: xMax,
			top: yMin,
			bottom: yMax
		};
	});
}
var TextRange = class {
	constructor(_scene, _document, _docSkeleton, anchorNodePosition, focusNodePosition, style = _univerjs_engine_render.NORMAL_TEXT_SELECTION_PLUGIN_STYLE, _segmentId = "", _segmentPage = -1) {
		this._scene = _scene;
		this._document = _document;
		this._docSkeleton = _docSkeleton;
		this.anchorNodePosition = anchorNodePosition;
		this.focusNodePosition = focusNodePosition;
		this.style = style;
		this._segmentId = _segmentId;
		this._segmentPage = _segmentPage;
		_defineProperty(this, "rangeType", _univerjs_core.DOC_RANGE_TYPE.TEXT);
		_defineProperty(this, "_current", false);
		_defineProperty(this, "_rangeShape", void 0);
		_defineProperty(this, "_anchorShape", void 0);
		_defineProperty(this, "_cursorList", []);
		_defineProperty(this, "_anchorBlinkTimer", null);
		_defineProperty(this, "_anchorActiveStroke", DEFAULT_CARET_COLOR);
		this._anchorBlink();
		this.refresh();
	}
	_anchorBlink() {
		setTimeout(() => {
			if (this._anchorShape) {
				if (this._anchorShape.visible) this.deactivateStatic();
			}
		}, BLINK_ON);
		this._anchorBlinkTimer = setInterval(() => {
			if (this._anchorShape) {
				if (this._anchorShape.visible) {
					this.activeStatic();
					setTimeout(() => {
						this.deactivateStatic();
					}, BLINK_ON);
				}
			}
		}, 1e3);
	}
	get startOffset() {
		var _this$_docSkeleton$ge;
		const { startOffset } = getOneTextSelectionRange(this._cursorList) ?? {};
		const body = (_this$_docSkeleton$ge = this._docSkeleton.getViewModel().getDataModel().getSelfOrHeaderFooterModel(this._segmentId)) === null || _this$_docSkeleton$ge === void 0 ? void 0 : _this$_docSkeleton$ge.getBody();
		if (startOffset == null || body == null) return startOffset;
		const maxLength = body.dataStream.length - 2;
		return Math.min(maxLength, startOffset);
	}
	get endOffset() {
		var _this$_docSkeleton$ge2;
		const { endOffset } = getOneTextSelectionRange(this._cursorList) ?? {};
		const body = (_this$_docSkeleton$ge2 = this._docSkeleton.getViewModel().getDataModel().getSelfOrHeaderFooterModel(this._segmentId)) === null || _this$_docSkeleton$ge2 === void 0 ? void 0 : _this$_docSkeleton$ge2.getBody();
		if (endOffset == null || body == null) return endOffset;
		const maxLength = body.dataStream.length - 2;
		return Math.min(endOffset, maxLength);
	}
	get collapsed() {
		const { startOffset, endOffset } = this;
		return startOffset != null && startOffset === endOffset;
	}
	get startNodePosition() {
		if (this.anchorNodePosition == null) return null;
		if (this.focusNodePosition == null) return this.anchorNodePosition;
		const { start } = compareNodePosition(this.anchorNodePosition, this.focusNodePosition);
		return start;
	}
	get endNodePosition() {
		if (this.anchorNodePosition == null) return this.focusNodePosition;
		if (this.focusNodePosition == null) return null;
		const { end } = compareNodePosition(this.anchorNodePosition, this.focusNodePosition);
		return end;
	}
	get direction() {
		const { collapsed, anchorNodePosition, focusNodePosition } = this;
		if (collapsed || anchorNodePosition == null || focusNodePosition == null) return _univerjs_core.RANGE_DIRECTION.NONE;
		return compareNodePositionLogic(anchorNodePosition, focusNodePosition) ? _univerjs_core.RANGE_DIRECTION.FORWARD : _univerjs_core.RANGE_DIRECTION.BACKWARD;
	}
	get segmentId() {
		return this._segmentId;
	}
	get segmentPage() {
		return this._segmentPage;
	}
	getAbsolutePosition() {
		const anchor = this.anchorNodePosition;
		const focus = this.focusNodePosition;
		if (this._isEmpty()) return;
		const documentOffsetConfig = this._document.getOffsetConfig();
		const { docsLeft, docsTop } = documentOffsetConfig;
		const convertor = new NodePositionConvertToCursor(documentOffsetConfig, this._docSkeleton);
		if (this._isCollapsed()) {
			const { contentBoxPointGroup, cursorList } = convertor.getRangePointData(anchor, anchor);
			this._setCursorList(cursorList);
			if (contentBoxPointGroup.length === 0) return;
			const pos = getAnchorBounding(contentBoxPointGroup);
			return {
				...pos,
				left: pos.left + docsLeft,
				top: pos.top + docsTop
			};
		}
		const { borderBoxPointGroup, cursorList } = convertor.getRangePointData(anchor, focus);
		this._setCursorList(cursorList);
		if (borderBoxPointGroup.length === 0) return;
		const pos = getAnchorBounding(borderBoxPointGroup);
		return {
			...pos,
			left: pos.left + docsLeft,
			top: pos.top + docsTop
		};
	}
	getAnchor() {
		return this._anchorShape;
	}
	activeStatic() {
		var _this$_anchorShape;
		(_this$_anchorShape = this._anchorShape) === null || _this$_anchorShape === void 0 || _this$_anchorShape.setProps({ stroke: this._anchorActiveStroke });
	}
	deactivateStatic() {
		var _this$_anchorShape2, _this$style;
		(_this$_anchorShape2 = this._anchorShape) === null || _this$_anchorShape2 === void 0 || _this$_anchorShape2.setProps({ stroke: ((_this$style = this.style) === null || _this$style === void 0 ? void 0 : _this$style.stroke) || (0, _univerjs_engine_render.getColor)(_univerjs_core.COLORS.black, 0) });
	}
	isActive() {
		return this._current === true;
	}
	activate() {
		this._current = true;
		if (this._isCollapsed()) {
			var _this$_anchorShape3;
			(_this$_anchorShape3 = this._anchorShape) === null || _this$_anchorShape3 === void 0 || _this$_anchorShape3.show();
			this.activeStatic();
		}
	}
	deactivate() {
		this._current = false;
		if (this._isCollapsed()) {
			var _this$_anchorShape4;
			(_this$_anchorShape4 = this._anchorShape) === null || _this$_anchorShape4 === void 0 || _this$_anchorShape4.hide();
		}
	}
	dispose() {
		var _this$_rangeShape, _this$_anchorShape5;
		(_this$_rangeShape = this._rangeShape) === null || _this$_rangeShape === void 0 || _this$_rangeShape.dispose();
		this._rangeShape = null;
		(_this$_anchorShape5 = this._anchorShape) === null || _this$_anchorShape5 === void 0 || _this$_anchorShape5.dispose();
		this._anchorShape = null;
		if (this._anchorBlinkTimer) {
			clearInterval(this._anchorBlinkTimer);
			this._anchorBlinkTimer = null;
		}
	}
	isIntersection(compareRange) {
		const { startOffset: activeStart, endOffset: activeEnd } = this;
		const { startOffset: compareStart, endOffset: compareEnd } = compareRange;
		if (activeStart == null || activeEnd == null || compareStart == null || compareEnd == null) return false;
		return activeStart <= compareEnd && activeEnd >= compareStart;
	}
	refresh() {
		var _this$_anchorShape6, _this$_rangeShape2;
		const { _document, _docSkeleton } = this;
		const anchor = this.anchorNodePosition;
		const focus = this.focusNodePosition;
		(_this$_anchorShape6 = this._anchorShape) === null || _this$_anchorShape6 === void 0 || _this$_anchorShape6.hide();
		(_this$_rangeShape2 = this._rangeShape) === null || _this$_rangeShape2 === void 0 || _this$_rangeShape2.hide();
		if (this._isEmpty()) return;
		const documentOffsetConfig = _document.getOffsetConfig();
		const { docsLeft, docsTop } = documentOffsetConfig;
		const convertor = new NodePositionConvertToCursor(documentOffsetConfig, _docSkeleton);
		if (this._isCollapsed()) {
			const { contentBoxPointGroup, cursorList } = convertor.getRangePointData(anchor, anchor);
			this._setCursorList(cursorList);
			if (contentBoxPointGroup.length > 0) {
				const glyphAtCursor = _docSkeleton.findGlyphByPosition(anchor);
				this._createOrUpdateAnchor(contentBoxPointGroup, docsLeft, docsTop, glyphAtCursor);
			}
			return;
		}
		const { borderBoxPointGroup, cursorList } = convertor.getRangePointData(anchor, focus);
		this._setCursorList(cursorList);
		if (borderBoxPointGroup.length > 0) this._createOrUpdateRange(borderBoxPointGroup, docsLeft, docsTop);
	}
	_isEmpty() {
		return this.anchorNodePosition == null && this.focusNodePosition == null;
	}
	_isCollapsed() {
		const anchor = this.anchorNodePosition;
		const focus = this.focusNodePosition;
		if (anchor != null && focus == null) return true;
		if (anchor == null || focus == null) return false;
		const keys = Object.keys(NodePositionMap);
		for (const key of keys) if (anchor[key] !== focus[key]) return false;
		if (anchor.isBack !== focus.isBack) return false;
		return true;
	}
	_createOrUpdateRange(pointsGroup, left, top) {
		var _this$style2;
		if (this._rangeShape) {
			this._rangeShape.translate(left, top);
			this._rangeShape.updatePointGroup(pointsGroup);
			this._rangeShape.show();
			return;
		}
		const polygon = new _univerjs_engine_render.RegularPolygon(TEXT_RANGE_KEY_PREFIX + (0, _univerjs_core.generateRandomId)(ID_LENGTH$1), {
			pointsGroup,
			fill: ((_this$style2 = this.style) === null || _this$style2 === void 0 ? void 0 : _this$style2.fill) || (0, _univerjs_engine_render.getColor)(_univerjs_core.COLORS.black, .3),
			left,
			top,
			evented: false,
			debounceParentDirty: false
		});
		this._rangeShape = polygon;
		this._scene.addObject(polygon, 3);
	}
	_createOrUpdateAnchor(pointsGroup, docsLeft, docsTop, glyph) {
		var _glyph$ts, _this$style3;
		const bounding = getAnchorBounding(pointsGroup);
		const { left: boundingLeft } = bounding;
		let { top: boundingTop, height } = bounding;
		const MIN_CURSOR_HEIGHT = 14;
		if (height < MIN_CURSOR_HEIGHT) {
			boundingTop -= MIN_CURSOR_HEIGHT - height;
			height = MIN_CURSOR_HEIGHT;
		}
		const ITALIC_DEGREE = 12;
		let left = boundingLeft + docsLeft;
		const top = boundingTop + docsTop;
		const isItalic = (glyph === null || glyph === void 0 || (_glyph$ts = glyph.ts) === null || _glyph$ts === void 0 ? void 0 : _glyph$ts.it) === _univerjs_core.BooleanNumber.TRUE;
		this._anchorActiveStroke = this._getAnchorActiveStroke(glyph);
		if (isItalic) left += height * Math.tan(ITALIC_DEGREE * Math.PI / 180) / 2;
		if (this._anchorShape) {
			this._anchorShape.transformByState({
				left,
				top,
				height
			});
			this._anchorShape.show();
			if (isItalic) this._anchorShape.skew(-12, 0);
			else this._anchorShape.skew(0, 0);
			this.activeStatic();
			return;
		}
		const anchor = new _univerjs_engine_render.Rect(TEXT_ANCHOR_KEY_PREFIX + (0, _univerjs_core.generateRandomId)(ID_LENGTH$1), {
			left,
			top,
			height,
			strokeWidth: ((_this$style3 = this.style) === null || _this$style3 === void 0 ? void 0 : _this$style3.strokeWidth) || 1,
			stroke: this._anchorActiveStroke,
			evented: false
		});
		if (isItalic) anchor.skew(-12, 0);
		this._anchorShape = anchor;
		this._scene.addObject(anchor, 3);
		this.activeStatic();
	}
	_getAnchorActiveStroke(glyph) {
		var _this$style4, _this$_scene$getEngin, _this$_scene;
		if (((_this$style4 = this.style) === null || _this$style4 === void 0 ? void 0 : _this$style4.strokeActive) && this.style.strokeActive !== _univerjs_engine_render.NORMAL_TEXT_SELECTION_PLUGIN_STYLE.strokeActive) return this.style.strokeActive;
		const colorService = (_this$_scene$getEngin = (_this$_scene = this._scene).getEngine) === null || _this$_scene$getEngin === void 0 || (_this$_scene$getEngin = _this$_scene$getEngin.call(_this$_scene)) === null || _this$_scene$getEngin === void 0 ? void 0 : _this$_scene$getEngin.canvasColorService;
		if (!colorService) return DEFAULT_CARET_COLOR;
		try {
			const backgroundColorKit = new _univerjs_core.ColorKit(colorService.getRenderColor(this._getAnchorBackgroundColor(glyph)));
			if (!backgroundColorKit.isValid) return DEFAULT_CARET_COLOR;
			return _univerjs_core.ColorKit.getContrastRatio(colorService.getRenderColor("gray.0"), backgroundColorKit) > _univerjs_core.ColorKit.getContrastRatio(colorService.getRenderColor(DEFAULT_CARET_COLOR), backgroundColorKit) ? "gray.0" : DEFAULT_CARET_COLOR;
		} catch {
			return DEFAULT_CARET_COLOR;
		}
	}
	_getAnchorBackgroundColor(glyph) {
		var _glyph$parent, _line$parent, _glyph$ts2;
		const line = glyph === null || glyph === void 0 || (_glyph$parent = glyph.parent) === null || _glyph$parent === void 0 ? void 0 : _glyph$parent.parent;
		const page = line === null || line === void 0 || (_line$parent = line.parent) === null || _line$parent === void 0 || (_line$parent = _line$parent.parent) === null || _line$parent === void 0 ? void 0 : _line$parent.parent;
		let cellBackground;
		if ((page === null || page === void 0 ? void 0 : page.type) === _univerjs_engine_render.DocumentSkeletonPageType.CELL && page.parent && "rowSource" in page.parent) {
			var _row$rowSource$tableC;
			const row = page.parent;
			const cellIndex = row.cells.indexOf(page);
			cellBackground = (_row$rowSource$tableC = row.rowSource.tableCells[cellIndex]) === null || _row$rowSource$tableC === void 0 ? void 0 : _row$rowSource$tableC.backgroundColor;
		}
		const background = (glyph === null || glyph === void 0 || (_glyph$ts2 = glyph.ts) === null || _glyph$ts2 === void 0 ? void 0 : _glyph$ts2.bg) ?? (line === null || line === void 0 ? void 0 : line.backgroundColor) ?? cellBackground;
		return (background === null || background === void 0 ? void 0 : background.rgb) ?? (0, _univerjs_core.getColorStyle)(background) ?? "gray.0";
	}
	_setCursorList(cursorList) {
		if (cursorList.length === 0) return;
		this._cursorList = cursorList;
	}
};

//#endregion
//#region src/services/selection/rect-range.ts
const RECT_RANGE_KEY_PREFIX = "__DocTableRectRange__";
const ID_LENGTH = 6;
function convertPositionsToRectRanges(scene, document, docSkeleton, anchorNodePosition, focusNodePosition, style = _univerjs_engine_render.NORMAL_TEXT_SELECTION_PLUGIN_STYLE, segmentId = "", segmentPage = -1) {
	const nodePositionGroup = new NodePositionConvertToRectRange(document.getOffsetConfig(), docSkeleton).getNodePositionGroup(anchorNodePosition, focusNodePosition);
	const ranges = [];
	try {
		for (const position of nodePositionGroup ?? []) ranges.push(new RectRange(scene, document, docSkeleton, position.anchor, position.focus, style, segmentId, segmentPage));
		return ranges;
	} catch (error) {
		ranges.forEach((range) => range.dispose());
		throw error;
	}
}
var RectRange = class {
	constructor(_scene, _document, _docSkeleton, anchorNodePosition, focusNodePosition, style = _univerjs_engine_render.NORMAL_TEXT_SELECTION_PLUGIN_STYLE, _segmentId = "", _segmentPage = -1) {
		this._scene = _scene;
		this._document = _document;
		this._docSkeleton = _docSkeleton;
		this.anchorNodePosition = anchorNodePosition;
		this.focusNodePosition = focusNodePosition;
		this.style = style;
		this._segmentId = _segmentId;
		this._segmentPage = _segmentPage;
		_defineProperty(this, "rangeType", _univerjs_core.DOC_RANGE_TYPE.RECT);
		_defineProperty(this, "_rangeShape", void 0);
		_defineProperty(this, "_current", false);
		_defineProperty(this, "_startRow", void 0);
		_defineProperty(this, "_startCol", void 0);
		_defineProperty(this, "_endRow", void 0);
		_defineProperty(this, "_endCol", void 0);
		_defineProperty(this, "_tableId", void 0);
		this.refresh();
	}
	get startOffset() {
		const { startNodePosition } = this;
		return this._docSkeleton.findCharIndexByPosition(startNodePosition);
	}
	get endOffset() {
		const { endNodePosition } = this;
		return this._docSkeleton.findCharIndexByPosition(endNodePosition);
	}
	get collapsed() {
		return false;
	}
	get startRow() {
		return this._startRow;
	}
	get startColumn() {
		return this._startCol;
	}
	get endRow() {
		return this._endRow;
	}
	get endColumn() {
		return this._endCol;
	}
	get tableId() {
		return this._tableId;
	}
	get segmentId() {
		return this._segmentId;
	}
	get segmentPage() {
		return this._segmentPage;
	}
	get spanEntireRow() {
		var _viewModel$getSnapsho;
		const table = (_viewModel$getSnapsho = this._docSkeleton.getViewModel().getSnapshot().tableSource) === null || _viewModel$getSnapsho === void 0 ? void 0 : _viewModel$getSnapsho[this._tableId];
		const { _startCol, _endCol } = this;
		if (table == null) throw new Error("Table is not found.");
		const { tableColumns } = table;
		return _startCol === 0 && _endCol === tableColumns.length - 1;
	}
	get spanEntireColumn() {
		var _viewModel$getSnapsho2;
		const table = (_viewModel$getSnapsho2 = this._docSkeleton.getViewModel().getSnapshot().tableSource) === null || _viewModel$getSnapsho2 === void 0 ? void 0 : _viewModel$getSnapsho2[this._tableId];
		const { _startRow, _endRow } = this;
		if (table == null) throw new Error("Table is not found.");
		const { tableRows } = table;
		return _startRow === 0 && _endRow === tableRows.length - 1;
	}
	get spanEntireTable() {
		return this.spanEntireRow && this.spanEntireColumn;
	}
	get startNodePosition() {
		const { anchorNodePosition, focusNodePosition } = this;
		return compareNodePositionInTable(anchorNodePosition, focusNodePosition) ? anchorNodePosition : focusNodePosition;
	}
	get endNodePosition() {
		const { anchorNodePosition, focusNodePosition } = this;
		return compareNodePositionInTable(anchorNodePosition, focusNodePosition) ? focusNodePosition : anchorNodePosition;
	}
	get direction() {
		const { anchorNodePosition, focusNodePosition } = this;
		return compareNodePositionInTable(anchorNodePosition, focusNodePosition) ? _univerjs_core.RANGE_DIRECTION.FORWARD : _univerjs_core.RANGE_DIRECTION.BACKWARD;
	}
	isActive() {
		return this._current === true;
	}
	activate() {
		this._current = true;
	}
	deactivate() {
		this._current = false;
	}
	dispose() {
		var _this$_rangeShape;
		(_this$_rangeShape = this._rangeShape) === null || _this$_rangeShape === void 0 || _this$_rangeShape.dispose();
		this._rangeShape = null;
	}
	isIntersection(compareRange) {
		const { startRow, startColumn, endRow, endColumn } = this;
		const { startRow: cStartRow, startColumn: cStartColumn, endRow: cEndRow, endColumn: cEndColumn } = compareRange;
		const rect1 = {
			left: startColumn,
			top: startRow,
			right: endColumn,
			bottom: endRow
		};
		const rect2 = {
			left: cStartColumn,
			top: cStartRow,
			right: cEndColumn,
			bottom: cEndRow
		};
		return _univerjs_core.Rectangle.hasIntersectionBetweenTwoRect(rect1, rect2);
	}
	refresh() {
		var _this$_rangeShape2;
		(_this$_rangeShape2 = this._rangeShape) === null || _this$_rangeShape2 === void 0 || _this$_rangeShape2.hide();
		const { startNodePosition, endNodePosition, _document, _docSkeleton } = this;
		const documentOffsetConfig = _document.getOffsetConfig();
		const { docsLeft, docsTop } = documentOffsetConfig;
		const rectInfo = new NodePositionConvertToRectRange(documentOffsetConfig, _docSkeleton).getRangePointData(startNodePosition, endNodePosition);
		if (rectInfo == null) return;
		const { pointGroup = [], startRow, endRow, startColumn, endColumn, tableId } = rectInfo;
		if ((pointGroup === null || pointGroup === void 0 ? void 0 : pointGroup.length) > 0) this._createOrUpdateRange(pointGroup, docsLeft, docsTop);
		this._updateTableInfo(startRow, endRow, startColumn, endColumn, tableId);
	}
	_updateTableInfo(startRow, endRow, startCol, endCol, tableId) {
		this._startRow = startRow;
		this._endRow = endRow;
		this._startCol = startCol;
		this._endCol = endCol;
		this._tableId = tableId;
	}
	_createOrUpdateRange(pointsGroup, left, top) {
		var _this$style;
		if (this._rangeShape) {
			this._rangeShape.translate(left, top);
			this._rangeShape.updatePointGroup(pointsGroup);
			this._rangeShape.show();
			return;
		}
		const polygon = new _univerjs_engine_render.RegularPolygon(RECT_RANGE_KEY_PREFIX + (0, _univerjs_core.generateRandomId)(ID_LENGTH), {
			pointsGroup,
			fill: ((_this$style = this.style) === null || _this$style === void 0 ? void 0 : _this$style.fill) || (0, _univerjs_engine_render.getColor)(_univerjs_core.COLORS.black, .3),
			left,
			top,
			evented: false,
			debounceParentDirty: false
		});
		this._rangeShape = polygon;
		this._scene.addObject(polygon, 3);
	}
};

//#endregion
//#region src/services/selection/selection-utils.ts
function disposeRangeList({ textRanges, rectRanges }) {
	textRanges.forEach((range) => range.dispose());
	rectRanges.forEach((range) => range.dispose());
}
function isCompatibleTextRange(start, end) {
	if (start == null || end == null || start.pageType !== end.pageType) return false;
	if ((start.pageType === _univerjs_engine_render.DocumentSkeletonPageType.HEADER || start.pageType === _univerjs_engine_render.DocumentSkeletonPageType.FOOTER) && start.segmentPage !== end.segmentPage) return false;
	const startColumn = (0, _univerjs_engine_render.getDocumentSkeletonColumnPagePathInfo)(start);
	const endColumn = (0, _univerjs_engine_render.getDocumentSkeletonColumnPagePathInfo)(end);
	if (startColumn == null || endColumn == null) return startColumn == null && endColumn == null;
	return startColumn.pageIndex === endColumn.pageIndex && startColumn.columnGroupId === endColumn.columnGroupId && startColumn.columnIndex === endColumn.columnIndex;
}
function getDescendantTables(node) {
	if (node.nodeType === _univerjs_core.DataStreamTreeNodeType.TABLE) return [node];
	return node.children.flatMap(getDescendantTables);
}
function isInSameColumnPage(anchorPosition, focusPosition) {
	var _anchorPosition$path, _focusPosition$path;
	const anchorColumnInfo = (0, _univerjs_engine_render.getDocumentSkeletonColumnPagePathInfo)(anchorPosition);
	const focusColumnInfo = (0, _univerjs_engine_render.getDocumentSkeletonColumnPagePathInfo)(focusPosition);
	const anchorPageIndex = ((_anchorPosition$path = anchorPosition.path) === null || _anchorPosition$path === void 0 ? void 0 : _anchorPosition$path.indexOf("page")) ?? -1;
	const focusPageIndex = ((_focusPosition$path = focusPosition.path) === null || _focusPosition$path === void 0 ? void 0 : _focusPosition$path.indexOf("page")) ?? -1;
	return anchorPageIndex >= 0 && focusPageIndex >= 0 && anchorPageIndex === anchorPosition.path.length - 1 && focusPageIndex === focusPosition.path.length - 1 && !!anchorColumnInfo && !!focusColumnInfo && anchorColumnInfo.pageIndex === focusColumnInfo.pageIndex && anchorColumnInfo.columnGroupId === focusColumnInfo.columnGroupId && anchorColumnInfo.columnIndex === focusColumnInfo.columnIndex;
}
function getColumnInputBoundaryCollapsedPosition(anchorPosition, focusPosition, skeleton) {
	const anchorColumnInfo = (0, _univerjs_engine_render.getDocumentSkeletonColumnPagePathInfo)(anchorPosition);
	const focusColumnInfo = (0, _univerjs_engine_render.getDocumentSkeletonColumnPagePathInfo)(focusPosition);
	if (!anchorColumnInfo || !focusColumnInfo || anchorColumnInfo.pageIndex !== focusColumnInfo.pageIndex || anchorColumnInfo.columnGroupId !== focusColumnInfo.columnGroupId || anchorColumnInfo.columnIndex + 1 !== focusColumnInfo.columnIndex) return;
	const anchorOffset = skeleton.findCharIndexByPosition(anchorPosition);
	const focusOffset = skeleton.findCharIndexByPosition(focusPosition);
	if (anchorOffset == null || focusOffset == null || anchorOffset <= focusOffset) return;
	return anchorPosition;
}
function getTextRangeFromCharIndex(startOffset, endOffset, scene, document, skeleton, style, segmentId, segmentPage, startIsBack = true, endIsBack = true) {
	const startNodePosition = skeleton.findNodePositionByCharIndex(startOffset, startIsBack, segmentId, segmentPage);
	const endNodePosition = skeleton.findNodePositionByCharIndex(endOffset, endIsBack, segmentId, segmentPage);
	if (startNodePosition == null || endNodePosition == null) return;
	return new TextRange(scene, document, skeleton, startNodePosition, endNodePosition, style, segmentId, segmentPage);
}
function getRectRangeFromCharIndex(startOffset, endOffset, scene, document, skeleton, style, segmentId, segmentPage) {
	const startNodePosition = skeleton.findNodePositionByCharIndex(startOffset, true, segmentId, segmentPage);
	const endNodePosition = skeleton.findNodePositionByCharIndex(endOffset, true, segmentId, segmentPage);
	if (startNodePosition == null || endNodePosition == null) return;
	return new RectRange(scene, document, skeleton, startNodePosition, endNodePosition, style, segmentId, segmentPage);
}
function getRangeListFromCharIndex(startOffset, endOffset, scene, document, skeleton, style, segmentId, segmentPage) {
	const startNodePosition = skeleton.findNodePositionByCharIndex(startOffset, true, segmentId, segmentPage);
	const endNodePosition = skeleton.findNodePositionByCharIndex(endOffset, true, segmentId, segmentPage);
	if (startNodePosition == null || endNodePosition == null) return;
	return getRangeListFromSelection(startNodePosition, endNodePosition, scene, document, skeleton, style, segmentId, segmentPage);
}
function getRangeListFromSelection(anchorPosition, focusPosition, scene, document, skeleton, style, segmentId, segmentPage) {
	const textRanges = [];
	const rectRanges = [];
	const rangeParams = [
		scene,
		document,
		skeleton,
		anchorPosition,
		focusPosition,
		style,
		segmentId,
		segmentPage
	];
	const collapsedColumnPosition = getColumnInputBoundaryCollapsedPosition(anchorPosition, focusPosition, skeleton);
	if (collapsedColumnPosition) {
		textRanges.push(new TextRange(scene, document, skeleton, collapsedColumnPosition, collapsedColumnPosition, style, segmentId, segmentPage));
		return {
			textRanges,
			rectRanges
		};
	}
	if (isInSameTableCellData(skeleton, anchorPosition, focusPosition)) if (isInSameTableCell(anchorPosition, focusPosition)) {
		textRanges.push(new TextRange(...rangeParams));
		return {
			textRanges,
			rectRanges
		};
	} else {
		const ranges = convertPositionsToRectRanges(...rangeParams);
		rectRanges.push(...ranges);
		return {
			textRanges,
			rectRanges
		};
	}
	if (isValidRectRange(anchorPosition, focusPosition)) {
		const ranges = convertPositionsToRectRanges(...rangeParams);
		rectRanges.push(...ranges);
		return {
			textRanges,
			rectRanges
		};
	}
	if (isInSameColumnPage(anchorPosition, focusPosition)) {
		textRanges.push(new TextRange(...rangeParams));
		return {
			textRanges,
			rectRanges
		};
	}
	const viewModel = skeleton.getViewModel().getSelfOrHeaderFooterViewModel(segmentId);
	const anchorOffset = skeleton.findCharIndexByPosition(anchorPosition);
	const focusOffset = skeleton.findCharIndexByPosition(focusPosition);
	if (anchorOffset == null || focusOffset == null) return;
	const direction = anchorOffset <= focusOffset ? _univerjs_core.RANGE_DIRECTION.FORWARD : _univerjs_core.RANGE_DIRECTION.BACKWARD;
	const startOffset = Math.min(anchorOffset, focusOffset);
	const endOffset = Math.max(anchorOffset, focusOffset);
	const originRange = compareNodePosition(anchorPosition, focusPosition);
	const findStartNodePositionByCharIndex = (charIndex, isBack = true) => skeleton.findNodePositionByCharIndex(charIndex, isBack, segmentId, segmentPage) ?? (charIndex === startOffset ? originRange.start : void 0);
	const findEndNodePositionByCharIndex = (charIndex, isBack = true) => skeleton.findNodePositionByCharIndex(charIndex, isBack, segmentId, segmentPage) ?? (charIndex === endOffset ? originRange.end : void 0);
	const appendTextRange = (rangeStart, rangeEnd, endIsBack = true) => {
		if (rangeStart > rangeEnd) return true;
		const sp = findStartNodePositionByCharIndex(rangeStart, true);
		const ep = findEndNodePositionByCharIndex(rangeEnd, endIsBack);
		const ap = direction === _univerjs_core.RANGE_DIRECTION.FORWARD ? sp : ep;
		const fp = direction === _univerjs_core.RANGE_DIRECTION.FORWARD ? ep : sp;
		if (!isCompatibleTextRange(ap, fp)) return false;
		textRanges.push(new TextRange(scene, document, skeleton, ap, fp, style, segmentId, segmentPage));
		return true;
	};
	const appendTableRange = (table, rangeStart, rangeEnd) => {
		const { startIndex: tableStart, endIndex: tableEnd, children: rows } = table;
		const startRow = rows.find((row) => row.startIndex <= rangeStart && row.endIndex >= rangeStart);
		const endRow = rows.find((row) => row.startIndex <= rangeEnd && row.endIndex >= rangeEnd);
		const tableStartOffset = rangeStart > tableStart && rangeStart < tableEnd ? (startRow === null || startRow === void 0 ? void 0 : startRow.startIndex) == null ? void 0 : startRow.startIndex + 2 : tableStart + 3;
		const tableEndOffset = rangeEnd > tableStart && rangeEnd < tableEnd ? (endRow === null || endRow === void 0 ? void 0 : endRow.endIndex) == null ? void 0 : endRow.endIndex - 3 : tableEnd - 4;
		if (tableStartOffset == null || tableEndOffset == null) return false;
		const startPosition = skeleton.findNodePositionByCharIndex(tableStartOffset, true, segmentId, segmentPage);
		const endPosition = skeleton.findNodePositionByCharIndex(tableEndOffset, true, segmentId, segmentPage);
		if (startPosition == null || endPosition == null) return false;
		const ap = direction === _univerjs_core.RANGE_DIRECTION.FORWARD ? startPosition : endPosition;
		const fp = direction === _univerjs_core.RANGE_DIRECTION.FORWARD ? endPosition : startPosition;
		rectRanges.push(...convertPositionsToRectRanges(scene, document, skeleton, ap, fp, style, segmentId, segmentPage));
		return true;
	};
	const appendColumnRanges = (column, rangeStart, rangeEnd) => {
		let offset = rangeStart;
		const tables = getDescendantTables(column).sort((left, right) => left.startIndex - right.startIndex);
		for (const table of tables) {
			if (table.endIndex < rangeStart || table.startIndex > rangeEnd) continue;
			if (!appendTextRange(offset, Math.min(rangeEnd, table.startIndex - 1), false) || !appendTableRange(table, Math.max(rangeStart, table.startIndex), Math.min(rangeEnd, table.endIndex))) return false;
			offset = table.endIndex + 1;
		}
		return appendTextRange(offset, rangeEnd);
	};
	let start = startOffset;
	let end = endOffset;
	try {
		for (const section of viewModel.getChildren()) for (const paragraph of section.children) {
			const { startIndex, endIndex, children } = paragraph;
			if (paragraph.nodeType === _univerjs_core.DataStreamTreeNodeType.COLUMN_GROUP && start <= endIndex && end >= startIndex) {
				if (start < startIndex && !appendTextRange(start, Math.min(end, startIndex - 1), false)) {
					disposeRangeList({
						textRanges,
						rectRanges
					});
					return;
				}
				for (const column of children.filter((child) => child.nodeType === _univerjs_core.DataStreamTreeNodeType.COLUMN)) {
					const columnStart = Math.max(start, column.startIndex + 1);
					const columnEnd = Math.min(end, column.endIndex - 1);
					if (columnStart <= columnEnd && !appendColumnRanges(column, columnStart, columnEnd)) {
						disposeRangeList({
							textRanges,
							rectRanges
						});
						return;
					}
				}
				start = endIndex + 1;
				continue;
			}
			const paragraphIndex = section.children.indexOf(paragraph);
			const nextParagraph = section.children[paragraphIndex + 1];
			const table = children.find((child) => child.nodeType === _univerjs_core.DataStreamTreeNodeType.TABLE);
			const nextTable = nextParagraph === null || nextParagraph === void 0 ? void 0 : nextParagraph.children.find((child) => child.nodeType === _univerjs_core.DataStreamTreeNodeType.TABLE);
			let endInTable = false;
			if (table) {
				const { startIndex: tableStart, endIndex: tableEnd, children } = table;
				let tableStartPosition = null;
				let tableEndPosition = null;
				if (startOffset > tableStart && startOffset < tableEnd) {
					const startRow = children.find((row) => row.startIndex <= startOffset && row.endIndex >= startOffset);
					if (startRow == null) {
						disposeRangeList({
							textRanges,
							rectRanges
						});
						return;
					}
					tableStartPosition = skeleton.findNodePositionByCharIndex(startRow.startIndex + 2, true, segmentId, segmentPage);
					tableEndPosition = skeleton.findNodePositionByCharIndex(tableEnd - 4, true, segmentId, segmentPage);
					start = tableEnd + 1;
				} else if (endOffset > tableStart && endOffset < tableEnd) {
					const endRow = children.find((row) => row.startIndex <= endOffset && row.endIndex >= endOffset);
					if (endRow == null) {
						disposeRangeList({
							textRanges,
							rectRanges
						});
						return;
					}
					tableStartPosition = skeleton.findNodePositionByCharIndex(tableStart + 3, true, segmentId, segmentPage);
					tableEndPosition = skeleton.findNodePositionByCharIndex(endRow.endIndex - 3, true, segmentId, segmentPage);
					end = tableStart - 1;
					endInTable = true;
				} else if (tableStart > startOffset && tableEnd < endOffset) {
					tableStartPosition = skeleton.findNodePositionByCharIndex(tableStart + 3, true, segmentId, segmentPage);
					tableEndPosition = skeleton.findNodePositionByCharIndex(tableEnd - 4, true, segmentId, segmentPage);
					if (start <= tableStart - 1) {
						const sp = findStartNodePositionByCharIndex(start, true);
						const ep = findEndNodePositionByCharIndex(tableStart - 1, false);
						const ap = direction === _univerjs_core.RANGE_DIRECTION.FORWARD ? sp : ep;
						const fp = direction === _univerjs_core.RANGE_DIRECTION.FORWARD ? ep : sp;
						if (!isCompatibleTextRange(ap, fp)) {
							disposeRangeList({
								textRanges,
								rectRanges
							});
							return;
						}
						textRanges.push(new TextRange(scene, document, skeleton, ap, fp, style, segmentId, segmentPage));
					}
					start = tableEnd + 1;
				}
				if (tableStartPosition && tableEndPosition) {
					const ap = direction === _univerjs_core.RANGE_DIRECTION.FORWARD ? tableStartPosition : tableEndPosition;
					const fp = direction === _univerjs_core.RANGE_DIRECTION.FORWARD ? tableEndPosition : tableStartPosition;
					rectRanges.push(...convertPositionsToRectRanges(scene, document, skeleton, ap, fp, style, segmentId, segmentPage));
				}
			}
			if (end === endIndex + 1 && !endInTable && nextTable) {
				end = endIndex;
				endInTable = true;
			}
			if (end >= startIndex && end <= endIndex || endInTable) {
				const sp = findStartNodePositionByCharIndex(start, true);
				const ep = findEndNodePositionByCharIndex(end, !endInTable);
				const ap = direction === _univerjs_core.RANGE_DIRECTION.FORWARD ? sp : ep;
				const fp = direction === _univerjs_core.RANGE_DIRECTION.FORWARD ? ep : sp;
				if (rectRanges.length && _univerjs_core.Tools.diffValue(ap, fp)) continue;
				if (!isCompatibleTextRange(ap, fp)) {
					disposeRangeList({
						textRanges,
						rectRanges
					});
					return;
				}
				textRanges.push(new TextRange(scene, document, skeleton, ap, fp, style, segmentId, segmentPage));
			}
		}
		return {
			textRanges,
			rectRanges
		};
	} catch (error) {
		disposeRangeList({
			textRanges,
			rectRanges
		});
		throw error;
	}
}
function getCanvasOffsetByEngine(engine) {
	const canvas = engine === null || engine === void 0 ? void 0 : engine.getCanvasElement();
	if (!canvas) return {
		left: 0,
		top: 0
	};
	const { top, left } = (0, _univerjs_engine_render.getOffsetRectForDom)(canvas);
	return {
		left,
		top
	};
}
function getParagraphInfoByGlyph(node) {
	var _node$parent;
	const line = (_node$parent = node.parent) === null || _node$parent === void 0 ? void 0 : _node$parent.parent;
	const column = line === null || line === void 0 ? void 0 : line.parent;
	if (line == null || column == null) return;
	const { paragraphIndex } = line;
	const lines = column.lines.filter((line) => line.paragraphIndex === paragraphIndex);
	let nodeIndex = -1;
	let content = "";
	let hasFound = false;
	for (const line of lines) for (const divide of line.divides) for (const glyph of divide.glyphGroup) {
		if (!hasFound) nodeIndex += glyph.count;
		if (glyph === node) hasFound = true;
		content += glyph.count > 0 ? glyph.content : "";
	}
	return {
		st: lines[0].st,
		ed: paragraphIndex,
		content,
		nodeIndex
	};
}
function serializeTextRange(textRange) {
	const { startOffset, endOffset, collapsed, rangeType, startNodePosition, endNodePosition, direction, segmentId, segmentPage, style } = textRange;
	return {
		startOffset,
		endOffset,
		collapsed,
		rangeType,
		startNodePosition,
		endNodePosition,
		direction,
		segmentId,
		segmentPage,
		style,
		isActive: textRange.isActive()
	};
}
function serializeRectRange(rectRange) {
	const serializedTextRange = serializeTextRange(rectRange);
	const { startRow, startColumn, endRow, endColumn, tableId, spanEntireRow, spanEntireColumn, spanEntireTable } = rectRange;
	return {
		...serializedTextRange,
		startRow,
		startColumn,
		endRow,
		endColumn,
		tableId,
		spanEntireRow,
		spanEntireColumn,
		spanEntireTable
	};
}

//#endregion
//#region src/services/selection/word-boundary.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
function getWordRanges(content) {
	const segmenter = new Intl.Segmenter(void 0, { granularity: "word" });
	return Array.from(segmenter.segment(content)).filter((item) => item.isWordLike).map((item) => ({
		start: item.index,
		end: item.index + item.segment.length
	}));
}
function getWordBoundaryByIndex(content, nodeIndex, paragraphStartOffset) {
	if (nodeIndex < 0) return null;
	const range = getWordRanges(content).find((range) => range.start <= nodeIndex && nodeIndex < range.end);
	if (range == null) return null;
	return {
		startOffset: paragraphStartOffset + range.start,
		endOffset: paragraphStartOffset + range.end
	};
}
function getNextWordBoundaryOffset(content, nodeIndex, paragraphStartOffset, direction) {
	if (nodeIndex < 0) return null;
	const ranges = getWordRanges(content);
	if (direction === _univerjs_core.Direction.LEFT) {
		for (let i = ranges.length - 1; i >= 0; i--) {
			const range = ranges[i];
			if (range.start < nodeIndex) return paragraphStartOffset + range.start;
		}
		return paragraphStartOffset;
	}
	if (direction === _univerjs_core.Direction.RIGHT) {
		const target = ranges.find((range) => range.end > nodeIndex);
		return paragraphStartOffset + ((target === null || target === void 0 ? void 0 : target.end) ?? content.length);
	}
	return null;
}

//#endregion
//#region src/services/selection/doc-selection-render.service.ts
let DocSelectionRenderService = class DocSelectionRenderService extends _univerjs_core.RxDisposable {
	get hasPendingSelection() {
		return this._getPendingSelection() != null;
	}
	get isOnPointerEvent() {
		return this._onPointerEvent;
	}
	get isFocusing() {
		return this._input === this._getOwnerDocument().activeElement;
	}
	get canFocusing() {
		const ownerDocument = this._getOwnerDocument();
		const activeElement = ownerDocument.activeElement;
		return !this._shouldPreserveExternalFocus() && (this.isFocusing || activeElement === ownerDocument.body || activeElement === null || activeElement instanceof HTMLElement && this._containsCurrentEmbedRuntimeElement(activeElement));
	}
	cancelPointerSelection() {
		if (!this._onPointerEvent) return;
		this._scenePointerMoveSubs.forEach((subscription) => subscription.unsubscribe());
		this._scenePointerUpSubs.forEach((subscription) => subscription.unsubscribe());
		this._scenePointerMoveSubs = [];
		this._scenePointerUpSubs = [];
		this._onPointerEvent = false;
		this._anchorNodePosition = null;
		this._focusNodePosition = null;
		this._removeAllRanges();
		this._removeAllCacheRanges();
		this._disposeScrollTimers();
		this._context.scene.enableObjectsEvent();
	}
	constructor(_context, _layoutService, _logService, _univerInstanceService, _docSkeletonManagerService, _docSelectionManagerService, _embedInteractionBoundaryService, _embedRuntimeFocusCoordinator) {
		super();
		this._context = _context;
		this._layoutService = _layoutService;
		this._logService = _logService;
		this._univerInstanceService = _univerInstanceService;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		this._docSelectionManagerService = _docSelectionManagerService;
		this._embedInteractionBoundaryService = _embedInteractionBoundaryService;
		this._embedRuntimeFocusCoordinator = _embedRuntimeFocusCoordinator;
		_defineProperty(this, "_onInputBefore$", new rxjs.Subject());
		_defineProperty(this, "onInputBefore$", this._onInputBefore$.asObservable());
		_defineProperty(this, "_onKeydown$", new rxjs.Subject());
		_defineProperty(this, "onKeydown$", this._onKeydown$.asObservable());
		_defineProperty(this, "_onInput$", new rxjs.Subject());
		_defineProperty(this, "onInput$", this._onInput$.asObservable());
		_defineProperty(this, "_onCompositionstart$", new rxjs.BehaviorSubject(null));
		_defineProperty(this, "onCompositionstart$", this._onCompositionstart$.asObservable());
		_defineProperty(this, "_onCompositionupdate$", new rxjs.BehaviorSubject(null));
		_defineProperty(this, "onCompositionupdate$", this._onCompositionupdate$.asObservable());
		_defineProperty(this, "_onCompositionend$", new rxjs.BehaviorSubject(null));
		_defineProperty(this, "onCompositionend$", this._onCompositionend$.asObservable());
		_defineProperty(this, "_onSelectionStart$", new rxjs.BehaviorSubject(null));
		_defineProperty(this, "onSelectionStart$", this._onSelectionStart$.asObservable());
		_defineProperty(this, "onChangeByEvent$", (0, rxjs.merge)(this._onInput$, this._onKeydown$.pipe((0, rxjs.filter)((e) => e.event.keyCode === _univerjs_ui.KeyCode.BACKSPACE)), this._onCompositionend$));
		_defineProperty(this, "_onPaste$", new rxjs.Subject());
		_defineProperty(this, "onPaste$", this._onPaste$.asObservable());
		_defineProperty(this, "_textSelectionInner$", new rxjs.BehaviorSubject(null));
		_defineProperty(this, "textSelectionInner$", this._textSelectionInner$.asObservable());
		_defineProperty(this, "_onFocus$", new rxjs.Subject());
		_defineProperty(this, "onFocus$", this._onFocus$.asObservable());
		_defineProperty(this, "_onBlur$", new rxjs.Subject());
		_defineProperty(this, "onBlur$", this._onBlur$.asObservable());
		_defineProperty(this, "_onPointerDown$", new rxjs.Subject());
		_defineProperty(this, "onPointerDown$", this._onPointerDown$.asObservable());
		_defineProperty(this, "_container", void 0);
		_defineProperty(this, "_inputParent", void 0);
		_defineProperty(this, "_input", void 0);
		_defineProperty(this, "_scrollTimers", []);
		_defineProperty(this, "_rangeList", []);
		_defineProperty(this, "_rangeListCache", []);
		_defineProperty(this, "_rectRangeList", []);
		_defineProperty(this, "_rectRangeListCache", []);
		_defineProperty(this, "_anchorNodePosition", null);
		_defineProperty(this, "_focusNodePosition", null);
		_defineProperty(this, "_currentSegmentId", "");
		_defineProperty(this, "_currentSegmentPage", -1);
		_defineProperty(this, "_segmentContext$", new rxjs.BehaviorSubject({
			segmentId: "",
			segmentPage: -1
		}));
		_defineProperty(this, "segmentContext$", this._segmentContext$.asObservable());
		_defineProperty(this, "_selectionStyle", _univerjs_engine_render.NORMAL_TEXT_SELECTION_PLUGIN_STYLE);
		_defineProperty(this, "_onPointerEvent", false);
		_defineProperty(this, "_viewPortObserverMap", /* @__PURE__ */ new Map());
		_defineProperty(this, "_isIMEInputApply", false);
		_defineProperty(this, "_scenePointerMoveSubs", []);
		_defineProperty(this, "_scenePointerUpSubs", []);
		_defineProperty(this, "_reserveRanges", false);
		_defineProperty(this, "_pendingSelection", null);
		this._initDOM();
		this._registerContainer();
		this._setSystemHighlightColorToStyle();
		this._listenCurrentUnitChange();
	}
	_listenCurrentUnitChange() {
		this._univerInstanceService.getCurrentTypeOfUnit$(_univerjs_core.UniverInstanceType.UNIVER_DOC).pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe((documentModel) => {
			if (documentModel == null) return;
			if (documentModel.getUnitId() !== this._context.unitId && !this._reserveRanges) this.removeAllRanges();
		});
	}
	get activeViewPort() {
		return this._context.scene.getViewports()[0];
	}
	setSegment(id) {
		if (this._currentSegmentId === id) return;
		this._currentSegmentId = id;
		this._segmentContext$.next({
			segmentId: id,
			segmentPage: this._currentSegmentPage
		});
	}
	getSegment() {
		return this._currentSegmentId;
	}
	setSegmentPage(pageIndex) {
		if (this._currentSegmentPage === pageIndex) return;
		this._currentSegmentPage = pageIndex;
		this._segmentContext$.next({
			segmentId: this._currentSegmentId,
			segmentPage: pageIndex
		});
	}
	getSegmentPage() {
		return this._currentSegmentPage;
	}
	setReserveRangesStatus(status) {
		this._reserveRanges = status;
	}
	refreshRanges() {
		this._rangeList.forEach((range) => range.refresh());
		this._rectRangeList.forEach((range) => range.refresh());
	}
	_setRangeStyle(style = _univerjs_engine_render.NORMAL_TEXT_SELECTION_PLUGIN_STYLE) {
		this._selectionStyle = style;
	}
	replaceDocRanges(ranges, isEditing = true, options) {
		if (this._onPointerEvent) {
			this._pendingSelection = null;
			return false;
		}
		if (this._currentSegmentId === "" && ranges.length > 0) {
			const progress = this._docSkeletonManagerService.getSkeleton().getLayoutProgress();
			if ((progress === null || progress === void 0 ? void 0 : progress.reason) === "edit" && !progress.anchorReady && !progress.complete && !progress.cancelled) {
				const { unitId } = this._context;
				const selection = this._docSelectionManagerService.getSelectionInfo({
					unitId,
					subUnitId: unitId
				});
				if (selection != null && ranges.length === selection.textRanges.length && ranges.every((range, index) => range === selection.textRanges[index])) {
					this._pendingSelection = selection;
					return false;
				}
			}
		}
		this._pendingSelection = null;
		return this.addDocRanges(ranges, isEditing, options, true);
	}
	_getPendingSelection() {
		if (this._pendingSelection == null) return null;
		const { unitId } = this._context;
		if (this._docSelectionManagerService.getSelectionInfo({
			unitId,
			subUnitId: unitId
		}) !== this._pendingSelection) this._pendingSelection = null;
		return this._pendingSelection;
	}
	_canResolveCollapsedRanges(ranges, scene, document, docSkeleton) {
		for (const range of ranges) {
			if (range.rangeType === _univerjs_core.DOC_RANGE_TYPE.RECT || range.startOffset !== range.endOffset) continue;
			const textRange = cursorConvertToTextRange(scene, {
				...range,
				segmentId: this._currentSegmentId,
				segmentPage: this._currentSegmentPage,
				style: range.style ?? this._selectionStyle
			}, docSkeleton, document);
			if (textRange == null) return false;
			const resolved = textRange.startOffset != null && textRange.endOffset != null;
			textRange.dispose();
			if (!resolved) return false;
		}
		return true;
	}
	addDocRanges(ranges, isEditing = true, options, replace = false) {
		const { _currentSegmentId: segmentId, _currentSegmentPage: segmentPage, _selectionStyle: style } = this;
		const { scene, mainComponent } = this._context;
		const document = mainComponent;
		const docSkeleton = this._docSkeletonManagerService.getSkeleton();
		if (replace && !this._canResolveCollapsedRanges(ranges, scene, document, docSkeleton)) return false;
		if (replace) this.removeAllRanges();
		const generalAddRange = (startOffset, endOffset, rangeStyle) => {
			const rangeList = getRangeListFromCharIndex(startOffset, endOffset, scene, document, docSkeleton, rangeStyle, segmentId, segmentPage);
			if (rangeList == null) return;
			const { textRanges, rectRanges } = rangeList;
			for (const textRange of textRanges) this._addTextRange(textRange);
			this._addRectRanges(rectRanges);
		};
		for (const range of ranges) {
			const { startOffset, endOffset, rangeType, startNodePosition, endNodePosition } = range;
			const rangeStyle = range.style ?? style;
			if (rangeType !== _univerjs_core.DOC_RANGE_TYPE.RECT && startOffset === endOffset) {
				const textRange = cursorConvertToTextRange(scene, {
					...range,
					segmentId,
					segmentPage,
					style: rangeStyle
				}, docSkeleton, document);
				if (textRange) this._addTextRange(textRange);
			} else if (rangeType === _univerjs_core.DOC_RANGE_TYPE.RECT) {
				const rectRange = getRectRangeFromCharIndex(startOffset, endOffset, scene, document, docSkeleton, rangeStyle, segmentId, segmentPage);
				if (rectRange) this._addRectRanges([rectRange]);
			} else if (rangeType === _univerjs_core.DOC_RANGE_TYPE.TEXT) try {
				let textRange = null;
				if (startNodePosition && endNodePosition) textRange = getTextRangeFromCharIndex(startNodePosition.isBack ? startOffset : startOffset - 1, endNodePosition.isBack ? endOffset : endOffset - 1, scene, document, docSkeleton, rangeStyle, segmentId, segmentPage, startNodePosition.isBack, endNodePosition.isBack);
				else textRange = getTextRangeFromCharIndex(startOffset, endOffset, scene, document, docSkeleton, rangeStyle, segmentId, segmentPage);
				if (textRange) this._addTextRange(textRange);
			} catch (_e) {
				generalAddRange(startOffset, endOffset, rangeStyle);
			}
			else generalAddRange(startOffset, endOffset, rangeStyle);
		}
		this._hideCollapsedCaretsForVisibleSelection();
		this._textSelectionInner$.next({
			textRanges: this._getAllTextRanges(),
			rectRanges: this._getAllRectRanges(),
			segmentId,
			segmentPage,
			style,
			isEditing,
			options
		});
		if (!ranges.length || (options === null || options === void 0 ? void 0 : options.shouldFocus) === false) return true;
		this._updateInputPosition(options === null || options === void 0 ? void 0 : options.forceFocus);
		return true;
	}
	setCursorManually(evtOffsetX, evtOffsetY) {
		const startNode = this._findNodeByCoord(evtOffsetX, evtOffsetY, {
			strict: true,
			segmentId: this._currentSegmentId,
			segmentPage: this._currentSegmentPage
		});
		const position = this._getNodePosition(startNode);
		if (position == null) {
			this._clearUnresolvedSelection();
			return;
		}
		if ((startNode === null || startNode === void 0 ? void 0 : startNode.node.streamType) === _univerjs_core.DataStreamTreeTokenType.PARAGRAPH) position.isBack = true;
		this._createTextRangeByAnchorPosition(position);
		this._textSelectionInner$.next({
			textRanges: this._getAllTextRanges(),
			rectRanges: this._getAllRectRanges(),
			segmentId: this._currentSegmentId,
			segmentPage: this._currentSegmentPage,
			style: this._selectionStyle,
			isEditing: false
		});
	}
	sync() {
		this._updateInputPosition();
	}
	setInputPosition(x, y) {
		this._positionInput(x, y);
	}
	activate(x, y, force = false) {
		this._positionInput(x, y);
		if (force && !this._shouldPreserveExternalFocus() || !force && this.canFocusing) this.focus();
	}
	_positionInput(x, y) {
		this._ensureHostContainer();
		this._container.style.position = "fixed";
		let left = x;
		let top = y;
		const fixedContainer = this._container.offsetParent;
		if (fixedContainer) {
			const rect = fixedContainer.getBoundingClientRect();
			left -= rect.left;
			top -= rect.top;
		}
		this._container.style.left = `${left}px`;
		this._container.style.top = `${top}px`;
		this._container.style.zIndex = "1000";
	}
	hasFocus() {
		return this._getOwnerDocument().activeElement === this._input;
	}
	focus() {
		if (!this._input.hasAttribute("tabindex")) this._input.tabIndex = -1;
		this._input.focus({ preventScroll: true });
	}
	blur() {
		this._input.blur();
	}
	deactivate() {
		this._container.style.left = "0px";
		this._container.style.top = "0px";
	}
	__handleDblClick(evt) {
		const { offsetX: evtOffsetX, offsetY: evtOffsetY } = evt;
		const startNode = this._findNodeByCoord(evtOffsetX, evtOffsetY, {
			strict: false,
			segmentId: this._currentSegmentId,
			segmentPage: this._currentSegmentPage
		});
		if (startNode == null || startNode.node == null) {
			this._clearUnresolvedSelection();
			return;
		}
		const paragraphInfo = getParagraphInfoByGlyph(startNode.node);
		if (paragraphInfo == null) return;
		const { content, st, nodeIndex } = paragraphInfo;
		if (nodeIndex === -1) return;
		const wordBoundary = getWordBoundaryByIndex(content, nodeIndex, st);
		if (wordBoundary != null) {
			this.removeAllRanges();
			const textRanges = [{
				startOffset: wordBoundary.startOffset,
				endOffset: wordBoundary.endOffset
			}];
			this.addDocRanges(textRanges, false, { forceFocus: true });
		}
	}
	__handleTripleClick(evt) {
		const { offsetX: evtOffsetX, offsetY: evtOffsetY } = evt;
		const startNode = this._findNodeByCoord(evtOffsetX, evtOffsetY, {
			strict: false,
			segmentId: this._currentSegmentId,
			segmentPage: this._currentSegmentPage
		});
		if (startNode == null || startNode.node == null) {
			this._clearUnresolvedSelection();
			return;
		}
		const paragraphInfo = getParagraphInfoByGlyph(startNode.node);
		if (paragraphInfo == null) return;
		this.removeAllRanges();
		const { st, ed } = paragraphInfo;
		const textRanges = [{
			startOffset: st,
			endOffset: ed
		}];
		this.addDocRanges(textRanges, false, { forceFocus: true });
	}
	__onPointDown(evt) {
		var _this$_getActiveRange, _scene$getTransformer;
		const { scene, mainComponent } = this._context;
		const skeleton = this._docSkeletonManagerService.getSkeleton();
		const { offsetX: evtOffsetX, offsetY: evtOffsetY } = evt;
		const startNode = this._findNodeByCoord(evtOffsetX, evtOffsetY, {
			strict: false,
			segmentId: this._currentSegmentId,
			segmentPage: this._currentSegmentPage
		});
		const position = this._getNodePosition(startNode);
		if (position == null || startNode == null) {
			this._clearUnresolvedSelection();
			return;
		}
		if ((startNode === null || startNode === void 0 ? void 0 : startNode.node.streamType) === _univerjs_core.DataStreamTreeTokenType.PARAGRAPH) position.isBack = true;
		const docSelection = this._textSelectionInner$.value;
		if (startNode && evt.button === 2 && docSelection) {
			const nodeCharIndex = skeleton.findCharIndexByPosition(position);
			if (typeof nodeCharIndex === "number" && docSelection.textRanges.some((textRange) => textRange.startOffset <= nodeCharIndex && textRange.endOffset > nodeCharIndex)) return;
			if (typeof nodeCharIndex === "number" && docSelection.rectRanges.some((rectRange) => rectRange.startOffset <= nodeCharIndex && rectRange.endOffset >= nodeCharIndex)) return;
		}
		this._pendingSelection = null;
		const { segmentId, segmentPage } = startNode;
		if (segmentId && this._currentSegmentId && segmentId !== this._currentSegmentId) this.setSegment(segmentId);
		if (segmentId && segmentPage !== this._currentSegmentPage) this.setSegmentPage(segmentPage);
		this._anchorNodePosition = position;
		if (evt.shiftKey && this._getActiveRangeInstance()) this._updateActiveRangePosition(position);
		else if (evt.ctrlKey) this._removeAllCollapsedTextRanges();
		else if (!this._isEmpty()) this._removeAllRanges();
		scene.disableObjectsEvent();
		const scrollTimer = _univerjs_engine_render.ScrollTimer.create(scene);
		this._scrollTimers.push(scrollTimer);
		scrollTimer.startScroll(evtOffsetX, evtOffsetY);
		this._onSelectionStart$.next((_this$_getActiveRange = this._getActiveRangeInstance()) === null || _this$_getActiveRange === void 0 ? void 0 : _this$_getActiveRange.startNodePosition);
		(_scene$getTransformer = scene.getTransformer()) === null || _scene$getTransformer === void 0 || _scene$getTransformer.clearSelectedObjects();
		let preMoveOffsetX = evtOffsetX;
		let preMoveOffsetY = evtOffsetY;
		this._onPointerEvent = true;
		this._scenePointerMoveSubs.push(scene.onPointerMove$.subscribeEvent((moveEvt) => {
			const { offsetX: moveOffsetX, offsetY: moveOffsetY } = moveEvt;
			scene.setCursor(_univerjs_engine_render.CURSOR_TYPE.TEXT);
			if (Math.sqrt((moveOffsetX - preMoveOffsetX) ** 2 + (moveOffsetY - preMoveOffsetY) ** 2) < 3) return;
			this._tryMoving(moveOffsetX, moveOffsetY);
			scrollTimer.scrolling(moveOffsetX, moveOffsetY, () => {
				this._tryMoving(moveOffsetX, moveOffsetY);
			});
			preMoveOffsetX = moveOffsetX;
			preMoveOffsetY = moveOffsetY;
		}));
		this._scenePointerUpSubs.push(scene.onPointerUp$.subscribeEvent(() => {
			[...this._scenePointerMoveSubs, ...this._scenePointerUpSubs].forEach((e) => {
				e.unsubscribe();
			});
			this._onPointerEvent = false;
			scene.enableObjectsEvent();
			if (this._anchorNodePosition && !this._focusNodePosition) {
				if (evt.ctrlKey) {
					this._disposeScrollTimers();
					return;
				}
				const textRange = new TextRange(scene, mainComponent, skeleton, this._anchorNodePosition, void 0, this._selectionStyle, this._currentSegmentId, this._currentSegmentPage);
				this._addTextRange(textRange);
			} else if (this._anchorNodePosition && this._focusNodePosition) {
				for (const textRange of this._rangeListCache) if (evt.ctrlKey) if (textRange.collapsed) textRange.dispose();
				else this._addTextRange(textRange);
				else this._addTextRange(textRange);
				this._addRectRanges(this._rectRangeListCache);
				this._rangeListCache = [];
				this._rectRangeListCache = [];
			}
			this._anchorNodePosition = null;
			this._focusNodePosition = null;
			const selectionInfo = {
				textRanges: this._getAllTextRanges(),
				rectRanges: this._getAllRectRanges(),
				segmentId: this._currentSegmentId,
				segmentPage: this._currentSegmentPage,
				style: this._selectionStyle,
				isEditing: false
			};
			this._textSelectionInner$.next(selectionInfo);
			this._disposeScrollTimers();
			this._updateInputPosition(true);
		}));
	}
	_clearUnresolvedSelection() {
		this._anchorNodePosition = null;
		this._focusNodePosition = null;
		this._removeAllCacheRanges();
		this.addDocRanges([], false, { shouldFocus: false }, true);
		this.blur();
	}
	removeAllRanges() {
		this._pendingSelection = null;
		this._removeAllRanges();
		this.deactivate();
	}
	getActiveTextRange() {
		return this._getActiveRangeInstance();
	}
	_disposeScrollTimers() {
		this._scrollTimers.forEach((timer) => {
			timer === null || timer === void 0 || timer.dispose();
		});
		this._scrollTimers = [];
	}
	_setSystemHighlightColorToStyle() {
		const { r, g, b, a } = (0, _univerjs_engine_render.getSystemHighlightColor)();
		const style = {
			strokeWidth: 1,
			stroke: "rgba(0, 0, 0, 0)",
			strokeActive: "rgba(0, 0, 0, 1)",
			fill: `rgba(${r}, ${g}, ${b}, ${a ?? .3})`
		};
		this._setRangeStyle(style);
	}
	_getAllTextRanges() {
		return this._rangeList.map(serializeTextRange);
	}
	_getAllRectRanges() {
		return this._rectRangeList.map(serializeRectRange);
	}
	getAllTextRanges() {
		return this._getAllTextRanges();
	}
	getAllRectRanges() {
		return this._getAllRectRanges();
	}
	_getActiveRange() {
		const activeRange = this._rangeList.find((range) => range.isActive());
		if (activeRange == null) return null;
		const { startOffset, endOffset } = activeRange;
		if (startOffset == null || endOffset == null) return null;
		return serializeTextRange(activeRange);
	}
	_getActiveRangeInstance() {
		return this._rangeList.find((range) => range.isActive());
	}
	dispose() {
		this._pendingSelection = null;
		super.dispose();
		this._detachEvent();
		this._removeAllRanges();
		this._container.remove();
	}
	_initDOM() {
		const { unitId } = this._context;
		const container = document.createElement("div");
		container.style.position = "fixed";
		container.style.left = "0px";
		container.style.top = "0px";
		container.id = `univer-doc-selection-container-${unitId}`;
		const inputParent = document.createElement("div");
		const inputDom = document.createElement("div");
		inputParent.appendChild(inputDom);
		container.appendChild(inputParent);
		this._container = container;
		this._inputParent = inputParent;
		this._input = inputDom;
		this._initInput();
		this._initInputEvents();
		const host = this._layoutService.rootContainerElement;
		((host === null || host === void 0 ? void 0 : host.isConnected) ? host : document.body).appendChild(container);
	}
	_registerContainer() {
		this.disposeWithMe(this._layoutService.registerContainerElement(this._container));
	}
	_initInput() {
		this._inputParent.style.cssText = `
            position:absolute;
            height:1px;
            width:1px;
            overflow: hidden;
        `;
		this._input.contentEditable = "true";
		this._input.dataset.uComp = "editor";
		this._input.id = `__editor_${this._context.unitId}`;
		this._input.style.cssText = `
            position: absolute;
            overflow: hidden;
            opacity: 1;
            background: #000;
            color: transparent;
            outline: none;
            z-index: -2;
            caret-color: transparent;
            white-space: pre-wrap;
            user-select: text;
        `;
	}
	_ensureHostContainer() {
		const host = this._layoutService.rootContainerElement;
		if (host === null || host === void 0 ? void 0 : host.isConnected) {
			if (this._container.parentElement !== host) host.appendChild(this._container);
			return;
		}
		if (this._container.parentElement !== document.body) document.body.appendChild(this._container);
	}
	_getOwnerDocument() {
		var _this$_container;
		return ((_this$_container = this._container) === null || _this$_container === void 0 ? void 0 : _this$_container.ownerDocument) ?? document;
	}
	_getNodePosition(node) {
		if (node == null) return;
		const { node: glyph, ratioX, segmentPage } = node;
		const position = this._docSkeletonManagerService.getSkeleton().findPositionByGlyph(glyph, segmentPage);
		if (position == null) return;
		let isBack = ratioX < .5;
		if (glyph.glyphType === _univerjs_engine_render.GlyphType.LIST) isBack = true;
		return {
			...position,
			isBack
		};
	}
	_interactTextRanges(textRanges) {
		const newTextRanges = [];
		for (const range of this._rangeList) {
			if (textRanges.some((textRange) => textRange.isIntersection(range))) {
				range.dispose();
				continue;
			}
			newTextRanges.push(range);
		}
		this._rangeList = newTextRanges;
	}
	_interactRectRanges(rectRanges) {
		const newRanges = [];
		for (const range of this._rectRangeList) {
			if (rectRanges.some((rectRange) => rectRange.isIntersection(range))) {
				range.dispose();
				continue;
			}
			newRanges.push(range);
		}
		this._rectRangeList = newRanges;
	}
	_removeAllRanges() {
		this._removeAllTextRanges();
		this._removeAllRectRanges();
	}
	_removeAllCacheRanges() {
		this._rangeListCache.forEach((range) => {
			range.dispose();
		});
		this._rectRangeListCache.forEach((range) => {
			range.dispose();
		});
		this._rangeListCache = [];
		this._rectRangeListCache = [];
	}
	_removeAllTextRanges() {
		this._rangeList.forEach((range) => {
			range.dispose();
		});
		this._rangeList = [];
	}
	_removeAllRectRanges() {
		this._rectRangeList.forEach((range) => {
			range.dispose();
		});
		this._rectRangeList = [];
	}
	_removeAllCollapsedTextRanges() {
		const newRanges = [];
		for (const range of this._rangeList) {
			if (range.collapsed) {
				range.dispose();
				continue;
			}
			newRanges.push(range);
		}
		this._rangeList = newRanges;
	}
	_hideCollapsedCaretsForVisibleSelection() {
		var _expandedTextRanges;
		const expandedTextRanges = this._rangeList.filter((range) => !range.collapsed);
		if (expandedTextRanges.length === 0 && this._rectRangeList.length === 0) return;
		this._deactivateAllTextRanges();
		(_expandedTextRanges = expandedTextRanges[expandedTextRanges.length - 1]) === null || _expandedTextRanges === void 0 || _expandedTextRanges.activate();
	}
	_deactivateAllTextRanges() {
		this._rangeList.forEach((range) => {
			range.deactivate();
		});
	}
	_deactivateAllRectRanges() {
		this._rectRangeList.forEach((range) => {
			range.deactivate();
		});
	}
	_addTextRangesToCache(textRanges) {
		this._rangeListCache.push(...textRanges);
	}
	_addTextRange(textRange) {
		this._deactivateAllTextRanges();
		textRange.activate();
		this._rangeList.push(textRange);
	}
	_addRectRangesToCache(rectRanges) {
		this._rectRangeListCache.push(...rectRanges);
	}
	_addRectRanges(rectRanges) {
		if (rectRanges.length === 0) return;
		this._deactivateAllRectRanges();
		rectRanges[rectRanges.length - 1].activate();
		this._rectRangeList.push(...rectRanges);
	}
	_createTextRangeByAnchorPosition(position) {
		this._removeAllRanges();
		const { scene, mainComponent } = this._context;
		const lastRange = new TextRange(scene, mainComponent, this._docSkeletonManagerService.getSkeleton(), position, void 0, this._selectionStyle, this._currentSegmentId, this._currentSegmentPage);
		this._addTextRange(lastRange);
	}
	_updateActiveRangePosition(position) {
		const activeTextRange = this._getActiveRangeInstance();
		if (activeTextRange == null || activeTextRange.anchorNodePosition == null) {
			this._logService.error("[DocSelectionRenderService] _updateActiveRangeFocusPosition: active range has no anchor");
			return;
		}
		this._removeAllRanges();
		this._anchorNodePosition = activeTextRange.anchorNodePosition;
		this._focusNodePosition = position;
		const { scene, mainComponent } = this._context;
		const skeleton = this._docSkeletonManagerService.getSkeleton();
		const { _anchorNodePosition, _focusNodePosition, _selectionStyle, _currentSegmentId, _currentSegmentPage } = this;
		if (_anchorNodePosition == null || _focusNodePosition == null || mainComponent == null) return;
		const ranges = getRangeListFromSelection(_anchorNodePosition, _focusNodePosition, scene, mainComponent, skeleton, _selectionStyle, _currentSegmentId, _currentSegmentPage);
		if (ranges == null) return;
		const { textRanges, rectRanges } = ranges;
		this._addTextRangesToCache(textRanges);
		this._addRectRangesToCache(rectRanges);
		this.deactivate();
	}
	_isEmpty() {
		return this._rangeList.length === 0 && this._rectRangeList.length === 0;
	}
	_getCanvasOffset() {
		var _this$_context$scene;
		return getCanvasOffsetByEngine((_this$_context$scene = this._context.scene) === null || _this$_context$scene === void 0 ? void 0 : _this$_context$scene.getEngine());
	}
	_updateInputPosition(forceFocus = false) {
		const activeRangeInstance = this._getActiveRangeInstance();
		const anchor = activeRangeInstance === null || activeRangeInstance === void 0 ? void 0 : activeRangeInstance.getAnchor();
		if (!anchor || anchor && !anchor.visible || this.activeViewPort == null) {
			if (this._shouldPreserveExternalFocus() || !forceFocus && this._isAnotherEditorFocused()) return;
			this.focus();
			return;
		}
		const { left, top } = anchor;
		const { x, y } = this.activeViewPort.getAbsoluteVector(_univerjs_engine_render.Vector2.FromArray([left, top]));
		let { left: canvasLeft, top: canvasTop } = this._getCanvasOffset();
		canvasLeft += x;
		canvasTop += y;
		this.activate(canvasLeft, canvasTop, forceFocus);
	}
	_tryMoving(moveOffsetX, moveOffsetY) {
		try {
			this._moving(moveOffsetX, moveOffsetY);
		} catch (error) {
			this._logService.error("[DocSelectionRenderService] Failed to update moving selection", error);
		}
	}
	_moving(moveOffsetX, moveOffsetY) {
		var _this$_context$scene2;
		const { _currentSegmentId: segmentId, _currentSegmentPage: segmentPage } = this;
		const focusNode = this._findNodeByCoord(moveOffsetX, moveOffsetY, {
			strict: true,
			segmentId,
			segmentPage
		});
		const focusNodePosition = this._getNodePosition(focusNode);
		if (focusNodePosition == null || focusNode == null) return;
		const divide = focusNode === null || focusNode === void 0 ? void 0 : focusNode.node.parent;
		const nextGlyph = divide === null || divide === void 0 ? void 0 : divide.glyphGroup[divide.glyphGroup.indexOf(focusNode.node) + 1];
		if ((focusNode === null || focusNode === void 0 ? void 0 : focusNode.node.streamType) === _univerjs_core.DataStreamTreeTokenType.PARAGRAPH && (nextGlyph === null || nextGlyph === void 0 ? void 0 : nextGlyph.streamType) === _univerjs_core.DataStreamTreeTokenType.SECTION_BREAK) focusNodePosition.isBack = true;
		if (this._shouldSnapBackwardFocusToGlyphStart(focusNode, focusNodePosition)) focusNodePosition.isBack = true;
		const { _anchorNodePosition, _selectionStyle } = this;
		const { scene, mainComponent } = this._context;
		const skeleton = this._docSkeletonManagerService.getSkeleton();
		if (_anchorNodePosition == null || mainComponent == null) return;
		const ranges = getRangeListFromSelection(_anchorNodePosition, focusNodePosition, scene, mainComponent, skeleton, _selectionStyle, segmentId, segmentPage);
		if (ranges == null) return;
		const { textRanges, rectRanges } = ranges;
		if (!this._hasVisibleSelectionRanges(textRanges, rectRanges)) {
			textRanges.forEach((range) => range.dispose());
			rectRanges.forEach((range) => range.dispose());
			return;
		}
		this._focusNodePosition = focusNodePosition;
		this._removeAllCacheRanges();
		if (this._rangeList.length > 0 && textRanges.length > 0) this._interactTextRanges(textRanges);
		if (this._rectRangeList.length > 0 && rectRanges.length > 0) this._interactRectRanges(rectRanges);
		this._addTextRangesToCache(textRanges);
		this._addRectRangesToCache(rectRanges);
		this.deactivate();
		(_this$_context$scene2 = this._context.scene) === null || _this$_context$scene2 === void 0 || (_this$_context$scene2 = _this$_context$scene2.getEngine()) === null || _this$_context$scene2 === void 0 || _this$_context$scene2.setCapture();
	}
	_hasVisibleSelectionRanges(textRanges, rectRanges) {
		return rectRanges.length > 0 || textRanges.some((range) => !range.collapsed);
	}
	_shouldSnapBackwardFocusToGlyphStart(focusNode, focusNodePosition) {
		const anchorNodePosition = this._anchorNodePosition;
		if (!anchorNodePosition || !this._isBeforeNodePosition(focusNodePosition, anchorNodePosition)) return false;
		return this._isFirstSelectableGlyphInDivide(focusNode);
	}
	_isBeforeNodePosition(left, right) {
		if (!compareNodePositionLogic(left, right)) return false;
		return left.page !== right.page || left.section !== right.section || left.column !== right.column || left.line !== right.line || left.divide !== right.divide || left.glyph !== right.glyph;
	}
	_isFirstSelectableGlyphInDivide(nodeInfo) {
		var _nodeInfo$node$parent;
		const glyphGroup = (_nodeInfo$node$parent = nodeInfo.node.parent) === null || _nodeInfo$node$parent === void 0 ? void 0 : _nodeInfo$node$parent.glyphGroup;
		if (!glyphGroup) return false;
		return glyphGroup.find((glyph) => glyph.content && glyph.glyphType !== _univerjs_engine_render.GlyphType.LIST) === nodeInfo.node;
	}
	__attachScrollEvent() {
		const viewport = this.activeViewPort;
		if (!viewport) return;
		const { unitId } = this._context;
		if (this._viewPortObserverMap.has(unitId)) return;
		const scrollBefore = viewport.onScrollAfter$.subscribeEvent((param) => {
			if (!param.viewport) return;
			const activeRangeInstance = this._getActiveRangeInstance();
			activeRangeInstance === null || activeRangeInstance === void 0 || activeRangeInstance.activeStatic();
		});
		const scrollStop = viewport.onScrollEnd$.subscribeEvent((param) => {
			const viewport = param.viewport;
			if (!viewport) return;
			const bounds = viewport.calcViewportInfo();
			const activeRangeInstance = this._getActiveRangeInstance();
			const anchor = activeRangeInstance === null || activeRangeInstance === void 0 ? void 0 : activeRangeInstance.getAnchor();
			if (!anchor || anchor && !anchor.visible) return;
			if (bounds) {
				const { left, top, right, bottom } = bounds.viewBound;
				if (anchor.left < left || anchor.left > right || anchor.top < top || anchor.top > bottom) {
					activeRangeInstance === null || activeRangeInstance === void 0 || activeRangeInstance.deactivateStatic();
					return;
				}
			}
			this._updateInputPosition();
		});
		this._viewPortObserverMap.set(unitId, {
			scrollBefore,
			scrollStop
		});
	}
	_initInputEvents() {
		this.disposeWithMe((0, rxjs.fromEvent)(this._input, "keydown").subscribe((e) => {
			if (this._shouldSuppressHostHiddenEditorEvent(e)) return;
			if (this._isIMEInputApply) return;
			this._stopEmbedOwnedEditorShortcutPropagation(e);
			this._eventHandle(e, (config) => {
				this._onKeydown$.next(config);
			});
		}));
		this.disposeWithMe((0, rxjs.fromEvent)(this._input, "input").subscribe((e) => {
			if (this._shouldSuppressHostHiddenEditorEvent(e)) return;
			if (e.inputType === "historyUndo" || e.inputType === "historyRedo") return;
			if (this._rectRangeList.length > 0 && this._getActiveRange() == null) {
				e.stopPropagation();
				return e.preventDefault();
			}
			if (this._isIMEInputApply) return;
			this._eventHandle(e, (config) => {
				this._onInputBefore$.next(config);
				this._onInput$.next(config);
			});
		}));
		this.disposeWithMe((0, rxjs.fromEvent)(this._input, "compositionstart").subscribe((e) => {
			if (this._shouldSuppressHostHiddenEditorEvent(e)) return;
			if (this._rectRangeList.length > 0 && this._getActiveRange() == null) {
				e.stopPropagation();
				return e.preventDefault();
			}
			this._isIMEInputApply = true;
			this._eventHandle(e, (config) => {
				this._onCompositionstart$.next(config);
			});
		}));
		this.disposeWithMe((0, rxjs.fromEvent)(this._input, "compositionend").subscribe((e) => {
			if (this._shouldSuppressHostHiddenEditorEvent(e)) return;
			this._isIMEInputApply = false;
			this._eventHandle(e, (config) => {
				this._onCompositionend$.next(config);
			});
		}));
		this.disposeWithMe((0, rxjs.fromEvent)(this._input, "compositionupdate").subscribe((e) => {
			if (this._shouldSuppressHostHiddenEditorEvent(e)) return;
			this._eventHandle(e, (config) => {
				this._onInputBefore$.next(config);
				this._onCompositionupdate$.next(config);
			});
		}));
		this.disposeWithMe((0, rxjs.fromEvent)(this._input, "paste").subscribe((e) => {
			if (this._shouldSuppressHostHiddenEditorEvent(e)) return;
			this._eventHandle(e, (config) => {
				this._onPaste$.next(config);
			});
		}));
		this.disposeWithMe((0, rxjs.fromEvent)(this._input, "focus").subscribe((e) => {
			if (this._shouldSuppressHostHiddenEditorEvent(e)) return;
			this._eventHandle(e, (config) => {
				this._onFocus$.next(config);
			});
		}));
		this.disposeWithMe((0, rxjs.fromEvent)(this._input, "blur").subscribe((e) => {
			if (this._shouldSuppressHostHiddenEditorEvent(e)) return;
			this._eventHandle(e, (config) => {
				this._onBlur$.next(config);
			});
		}));
	}
	_eventHandle(e, func) {
		const content = this._input.textContent || "";
		this._input.innerHTML = "";
		const pendingSelection = this._getPendingSelection();
		const rangeList = (pendingSelection === null || pendingSelection === void 0 ? void 0 : pendingSelection.textRanges.map((range) => ({
			segmentId: pendingSelection.segmentId,
			segmentPage: pendingSelection.segmentPage,
			style: pendingSelection.style,
			...range
		}))) ?? this._getAllTextRanges();
		func({
			event: e,
			content,
			activeRange: pendingSelection == null ? this._getActiveRange() : rangeList.find((range) => range.isActive),
			rangeList
		});
	}
	_shouldSuppressHostHiddenEditorEvent(event) {
		var _this$_embedRuntimeFo, _this$_embedRuntimeFo2, _this$_embedRuntimeFo3;
		const unitId = this._context.unitId;
		if ((0, _univerjs_core.isInternalEditorID)(unitId)) return false;
		if ((_this$_embedRuntimeFo = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo === void 0 ? void 0 : _this$_embedRuntimeFo.isChildUnitRuntimeEvent(unitId, event.target, event)) return false;
		if ((_this$_embedRuntimeFo2 = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo2 === void 0 ? void 0 : _this$_embedRuntimeFo2.isChildUnitInActiveSession(unitId)) return false;
		if (event.target instanceof HTMLElement && this._containsCurrentEmbedRuntimeElement(event.target)) return false;
		if ((_this$_embedRuntimeFo3 = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo3 === void 0 ? void 0 : _this$_embedRuntimeFo3.shouldSuppressHostInteraction(unitId, event.target, event)) {
			event.stopPropagation();
			if (event.cancelable) event.preventDefault();
			if (event.type === "focus" && event.target instanceof HTMLElement) event.target.blur();
			return true;
		}
		return false;
	}
	_stopEmbedOwnedEditorShortcutPropagation(event) {
		if (!(event instanceof KeyboardEvent) || !this._getCurrentEmbedOwner()) return;
		if (!(event.key.toLowerCase() === "a" && (event.metaKey || event.ctrlKey))) return;
		event.stopPropagation();
	}
	_getTransformCoordForDocumentOffset(evtOffsetX, evtOffsetY) {
		const { documentTransform } = this._context.mainComponent.getOffsetConfig();
		if (this.activeViewPort == null || documentTransform == null) return;
		const originCoord = this.activeViewPort.transformVector2SceneCoord(_univerjs_engine_render.Vector2.FromArray([evtOffsetX, evtOffsetY]));
		if (!originCoord) return;
		return documentTransform.clone().invert().applyPoint(originCoord);
	}
	_findNodeByCoord(evtOffsetX, evtOffsetY, restrictions) {
		const coord = this._getTransformCoordForDocumentOffset(evtOffsetX, evtOffsetY);
		if (coord == null) return;
		const document = this._context.mainComponent;
		const skeleton = this._docSkeletonManagerService.getSkeleton();
		const { pageLayoutType = _univerjs_engine_render.PageLayoutType.VERTICAL, pageMarginLeft, pageMarginTop } = document.getOffsetConfig();
		return skeleton.findNodeByCoord(coord, pageLayoutType, pageMarginLeft, pageMarginTop, restrictions);
	}
	_shouldPreserveExternalFocus() {
		var _this$_embedRuntimeFo4, _this$_embedRuntimeFo5, _this$_embedRuntimeFo6, _this$_embedInteracti, _this$_embedInteracti2, _this$_embedInteracti3;
		const ownerDocument = this._getOwnerDocument();
		const activeElement = ownerDocument.activeElement;
		const currentEmbedOwner = this._getCurrentEmbedOwner();
		if ((_this$_embedRuntimeFo4 = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo4 === void 0 ? void 0 : _this$_embedRuntimeFo4.isChildUnitInActiveSession(this._context.unitId)) return false;
		if ((_this$_embedRuntimeFo5 = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo5 === void 0 ? void 0 : _this$_embedRuntimeFo5.isChildUnitRuntimeEvent(this._context.unitId, activeElement)) return false;
		if (activeElement instanceof HTMLElement && this._containsOwnEditorElement(activeElement)) return false;
		if (activeElement instanceof HTMLElement && this._containsCurrentEmbedRuntimeElement(activeElement)) return false;
		if ((_this$_embedRuntimeFo6 = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo6 === void 0 ? void 0 : _this$_embedRuntimeFo6.shouldSuppressHostInteraction(this._context.unitId, activeElement)) return true;
		if (currentEmbedOwner && ((_this$_embedInteracti = this._embedInteractionBoundaryService) === null || _this$_embedInteracti === void 0 ? void 0 : _this$_embedInteracti.contains(currentEmbedOwner, activeElement))) return true;
		if (activeElement instanceof HTMLElement && this._containsCurrentLayoutElement(activeElement)) return false;
		return ((_this$_embedInteracti2 = this._embedInteractionBoundaryService) === null || _this$_embedInteracti2 === void 0 || (_this$_embedInteracti3 = _this$_embedInteracti2.hasRecentInteractionFor) === null || _this$_embedInteracti3 === void 0 ? void 0 : _this$_embedInteracti3.call(_this$_embedInteracti2, currentEmbedOwner, ownerDocument)) === true;
	}
	_isAnotherEditorFocused() {
		const activeElement = this._getOwnerDocument().activeElement;
		return activeElement instanceof HTMLElement && activeElement !== this._input && activeElement.dataset.uComp === this._input.dataset.uComp;
	}
	_containsCurrentEmbedRuntimeElement(element) {
		const currentEmbedOwner = this._getCurrentEmbedOwner();
		if (!currentEmbedOwner) return false;
		return this._getElementEmbedOwner(element) === currentEmbedOwner && this._containsCurrentLayoutElement(element);
	}
	_getCurrentEmbedOwner() {
		var _this$_layoutService;
		const rootContainerElement = (_this$_layoutService = this._layoutService) === null || _this$_layoutService === void 0 ? void 0 : _this$_layoutService.rootContainerElement;
		return this._getElementEmbedOwner(this._input) ?? this._getElementEmbedOwner(this._container) ?? this._getElementEmbedOwner(rootContainerElement instanceof HTMLElement ? rootContainerElement : void 0);
	}
	_getElementEmbedOwner(element) {
		var _element$closest;
		if (!element || typeof element.closest !== "function") return;
		return ((_element$closest = element.closest(`[${"data-embed-interaction-boundary-owner"}]`)) === null || _element$closest === void 0 ? void 0 : _element$closest.getAttribute("data-embed-interaction-boundary-owner")) ?? void 0;
	}
	_containsOwnEditorElement(element) {
		var _this$_container2, _this$_inputParent;
		return this._container === element || this._input === element || typeof ((_this$_container2 = this._container) === null || _this$_container2 === void 0 ? void 0 : _this$_container2.contains) === "function" && this._container.contains(element) || typeof ((_this$_inputParent = this._inputParent) === null || _this$_inputParent === void 0 ? void 0 : _this$_inputParent.contains) === "function" && this._inputParent.contains(element);
	}
	_containsCurrentLayoutElement(element) {
		var _layoutService$checkE, _this$_container4;
		const layoutService = this._layoutService;
		if (!layoutService) {
			var _this$_container3;
			return this._container === element || typeof ((_this$_container3 = this._container) === null || _this$_container3 === void 0 ? void 0 : _this$_container3.contains) === "function" && this._container.contains(element);
		}
		if ((_layoutService$checkE = layoutService.checkElementInCurrentContainers) === null || _layoutService$checkE === void 0 ? void 0 : _layoutService$checkE.call(layoutService, element)) return true;
		const root = layoutService.rootContainerElement;
		return root === element || (root === null || root === void 0 ? void 0 : root.contains(element)) === true || this._container === element || typeof ((_this$_container4 = this._container) === null || _this$_container4 === void 0 ? void 0 : _this$_container4.contains) === "function" && this._container.contains(element);
	}
	_detachEvent() {
		this._onInputBefore$.complete();
		this._onKeydown$.complete();
		this._onInput$.complete();
		this._onCompositionstart$.complete();
		this._onCompositionupdate$.complete();
		this._onCompositionend$.complete();
		this._onSelectionStart$.complete();
		this._textSelectionInner$.complete();
		this._onPaste$.complete();
		this._onFocus$.complete();
		this._onBlur$.complete();
		this._onPointerDown$.complete();
		this._segmentContext$.complete();
	}
};
DocSelectionRenderService = __decorate([
	__decorateParam(1, _univerjs_ui.ILayoutService),
	__decorateParam(2, _univerjs_core.ILogService),
	__decorateParam(3, _univerjs_core.IUniverInstanceService),
	__decorateParam(4, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService)),
	__decorateParam(5, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(6, (0, _univerjs_core.Optional)(IDocEmbedInteractionBoundaryService)),
	__decorateParam(7, (0, _univerjs_core.Optional)(IDocEmbedRuntimeFocusCoordinator))
], DocSelectionRenderService);

//#endregion
//#region src/services/editor/editor.ts
var Editor = class extends _univerjs_core.Disposable {
	constructor(_param, _univerInstanceService, _docSelectionManagerService, _commandService, _undoRedoService, _injector) {
		super();
		this._param = _param;
		this._univerInstanceService = _univerInstanceService;
		this._docSelectionManagerService = _docSelectionManagerService;
		this._commandService = _commandService;
		this._undoRedoService = _undoRedoService;
		this._injector = _injector;
		_defineProperty(this, "_change$", new rxjs.Subject());
		_defineProperty(this, "change$", this._change$.asObservable());
		_defineProperty(this, "_input$", new rxjs.Subject());
		_defineProperty(this, "input$", this._input$.asObservable());
		_defineProperty(this, "_paste$", new rxjs.Subject());
		_defineProperty(this, "paste$", this._paste$.asObservable());
		_defineProperty(this, "_focus$", new rxjs.Subject());
		_defineProperty(this, "focus$", this._focus$.asObservable());
		_defineProperty(this, "_blur$", new rxjs.Subject());
		_defineProperty(this, "blur$", this._blur$.asObservable());
		_defineProperty(this, "_selectionChange$", new rxjs.Subject());
		_defineProperty(this, "selectionChange$", this._selectionChange$.asObservable());
		this._listenSelection();
	}
	get docSelectionRenderService() {
		return this._param.render.with(DocSelectionRenderService);
	}
	_listenSelection() {
		const docSelectionRenderService = this._param.render.with(DocSelectionRenderService);
		this.disposeWithMe(docSelectionRenderService.onBlur$.subscribe((e) => {
			this._blur$.next(e);
			const data = this.getDocumentData();
			this._change$.next({
				target: this,
				data
			});
		}));
		this.disposeWithMe(docSelectionRenderService.onFocus$.subscribe((e) => {
			this._focus$.next(e);
		}));
		this.disposeWithMe(docSelectionRenderService.onPaste$.subscribe((e) => {
			this._paste$.next(e);
		}));
		this.disposeWithMe((0, rxjs.merge)(docSelectionRenderService.onInput$, docSelectionRenderService.onKeydown$.pipe((0, rxjs_operators.filter)((e) => {
			const event = e.event;
			if (event.ctrlKey || event.metaKey) return [_univerjs_ui.KeyCode.X, _univerjs_ui.KeyCode.V].includes(event.keyCode);
			return [_univerjs_ui.KeyCode.BACKSPACE].includes(event.keyCode);
		})), docSelectionRenderService.onCompositionupdate$, docSelectionRenderService.onCompositionend$, docSelectionRenderService.onPaste$).subscribe((e) => {
			if (e == null) return;
			const { content = "" } = e;
			const data = this.getDocumentData();
			this._input$.next({
				target: this,
				content,
				data,
				isComposing: e.event.type === "compositionupdate"
			});
		}));
		this.disposeWithMe(this._docSelectionManagerService.textSelection$.subscribe((e) => {
			if (e == null) return;
			const { unitId, subUnitId, ...params } = e;
			if (unitId === this.getEditorId()) this._selectionChange$.next(params);
		}));
	}
	isFocus() {
		const docSelectionRenderService = this._param.render.with(DocSelectionRenderService);
		return docSelectionRenderService.isFocusing && Boolean(docSelectionRenderService.getActiveTextRange());
	}
	/**
	* Focus the input element directly. This is for internal usage.
	*/
	focus() {
		const curDoc = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const editorUnitId = this.getEditorId();
		if (curDoc == null || curDoc.getUnitId() !== editorUnitId) this._univerInstanceService.setCurrentUnitForType(editorUnitId);
		this._param.render.with(DocSelectionRenderService).focus();
	}
	/**
	* Blur the input element directly. This is for internal usage.
	*/
	blur() {
		this._param.render.with(DocSelectionRenderService).blur();
	}
	select() {
		const documentData = this.getDocumentData();
		return this.setSelectionRanges([{
			startOffset: 0,
			endOffset: documentData.body ? documentData.body.dataStream.length - 2 : 0
		}]);
	}
	setSelectionRanges(ranges, shouldFocus = true) {
		const editorUnitId = this.getEditorId();
		const params = {
			unitId: editorUnitId,
			subUnitId: editorUnitId
		};
		return this._docSelectionManagerService.replaceDocRanges(ranges, params, false, { shouldFocus });
	}
	getSelectionRanges() {
		const editorUnitId = this.getEditorId();
		const params = {
			unitId: editorUnitId,
			subUnitId: editorUnitId
		};
		return this._docSelectionManagerService.getDocRanges(params);
	}
	getCursorPosition() {
		var _selectionRanges$find;
		return ((_selectionRanges$find = this.getSelectionRanges().find((range) => range.collapsed)) === null || _selectionRanges$find === void 0 ? void 0 : _selectionRanges$find.startOffset) ?? -1;
	}
	getEditorId() {
		return this._getEditorId();
	}
	getDocumentData() {
		return this._getDocDataModel().getSnapshot();
	}
	getDocumentDataModel() {
		return this._getDocDataModel();
	}
	setDocumentData(data, textRanges) {
		this._commandService.syncExecuteCommand(ReplaceSnapshotCommand.id, {
			unitId: this.getEditorId(),
			snapshot: data,
			textRanges
		});
	}
	replaceText(text, resetCursor = true) {
		const data = this.getDocumentData();
		this.setDocumentData({
			...data,
			body: {
				dataStream: `${text}\r\n`,
				paragraphs: [{
					startIndex: text.length,
					paragraphId: (0, _univerjs_core.createParagraphId)(/* @__PURE__ */ new Set())
				}],
				customRanges: [],
				sectionBreaks: [{
					sectionId: (0, _univerjs_core.createSectionId)(/* @__PURE__ */ new Set()),
					startIndex: text.length + 1
				}],
				tables: [],
				textRuns: []
			}
		}, typeof resetCursor === "object" ? resetCursor : resetCursor ? [{
			startOffset: text.length,
			endOffset: text.length,
			collapsed: true
		}] : null);
	}
	clearUndoRedoHistory() {
		const editorUnitId = this.getEditorId();
		return this._undoRedoService.clearUndoRedo(editorUnitId);
	}
	dispose() {
		const docDataModel = this._getDocDataModel();
		docDataModel === null || docDataModel === void 0 || docDataModel.dispose();
	}
	get cancelDefaultResizeListener() {
		return this._param.cancelDefaultResizeListener;
	}
	getRenderConfig() {
		return {
			canvasStyle: this._param.canvasStyle ?? {},
			scrollBar: this._param.scrollBar,
			...this._param.backScrollOffset === void 0 ? {} : { backScrollOffset: this._param.backScrollOffset }
		};
	}
	get render() {
		return this._param.render;
	}
	isReadOnly() {
		return this._param.readonly === true;
	}
	getBoundingClientRect() {
		return this._param.editorDom.getBoundingClientRect();
	}
	get editorDOM() {
		return this._param.editorDom;
	}
	isVisible() {
		return this._param.visible;
	}
	getSkeleton() {
		var _this$_injector$get$g;
		return (_this$_injector$get$g = this._injector.get(_univerjs_engine_render.IRenderManagerService).getRenderUnitById(this._getEditorId())) === null || _this$_injector$get$g === void 0 ? void 0 : _this$_injector$get$g.with(_univerjs_docs.DocSkeletonManagerService).getSkeleton();
	}
	isSheetEditor() {
		return (0, _univerjs_core.isInternalEditorID)(this._getEditorId());
	}
	_getDocDataModel() {
		const editorUnitId = this._getEditorId();
		return this._univerInstanceService.getUnit(editorUnitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
	}
	_getEditorId() {
		var _this$_param$initialS;
		return ((_this$_param$initialS = this._param.initialSnapshot) === null || _this$_param$initialS === void 0 ? void 0 : _this$_param$initialS.id) || this._param.editorUnitId || "";
	}
};

//#endregion
//#region src/services/editor/editor-manager.service.ts
/**
* Not these elements will be considered as editor blur.
*/
const editorFocusInElements = ["editor", "render-canvas"];
let EditorService = class EditorService extends _univerjs_core.Disposable {
	constructor(_univerInstanceService, _renderManagerService, _docSelectionManagerService, _contextService, _commandService, _undoRedoService, _injector) {
		super();
		this._univerInstanceService = _univerInstanceService;
		this._renderManagerService = _renderManagerService;
		this._docSelectionManagerService = _docSelectionManagerService;
		this._contextService = _contextService;
		this._commandService = _commandService;
		this._undoRedoService = _undoRedoService;
		this._injector = _injector;
		_defineProperty(this, "_editors", /* @__PURE__ */ new Map());
		_defineProperty(this, "_editorRenderConfigs", /* @__PURE__ */ new Map());
		_defineProperty(this, "_focusEditorUnitId", void 0);
		_defineProperty(this, "_blur$", new rxjs.Subject());
		_defineProperty(this, "blur$", this._blur$.asObservable());
		_defineProperty(this, "_focus$", new rxjs.Subject());
		_defineProperty(this, "focus$", this._focus$.asObservable());
		this._initUniverFocusListener();
	}
	_initUniverFocusListener() {
		this.disposeWithMe((0, rxjs.fromEvent)(window, "focusin").subscribe((event) => {
			const target = event.target;
			this._blurSheetEditor(target);
		}));
	}
	_blurSheetEditor(target) {
		if (editorFocusInElements.some((item) => target.dataset.uComp === item)) return;
		const focusEditor = this.getFocusEditor();
		if (focusEditor && focusEditor.isSheetEditor() !== true) this.blur();
	}
	_setFocusId(id) {
		this._focusEditorUnitId = id;
	}
	getFocusId() {
		return this._focusEditorUnitId;
	}
	getFocusEditor() {
		if (this._focusEditorUnitId) return this.getEditor(this._focusEditorUnitId);
	}
	isEditor(editorUnitId) {
		return this._editorRenderConfigs.has(editorUnitId) || this._editors.has(editorUnitId);
	}
	isSheetEditor(editorUnitId) {
		const editor = this._editors.get(editorUnitId);
		return !!(editor && editor.isSheetEditor());
	}
	blur(force) {
		const focusingEditor = this.getFocusEditor();
		if (force) focusingEditor === null || focusingEditor === void 0 || focusingEditor.setSelectionRanges([]);
		focusingEditor === null || focusingEditor === void 0 || focusingEditor.blur();
		this._contextService.setContextValue(_univerjs_core.EDITOR_ACTIVATED, false);
		this._contextService.setContextValue(_univerjs_core.FOCUSING_EDITOR_STANDALONE, false);
		this._contextService.setContextValue(_univerjs_core.FOCUSING_COMMENT_EDITOR, false);
		this._setFocusId(null);
		this._blur$.next(null);
	}
	focus(editorUnitId) {
		var _editor$getDocumentDa;
		if (editorUnitId === this._focusEditorUnitId) return;
		if (this._focusEditorUnitId) this.blur();
		if (editorUnitId == null) return;
		const editor = this.getEditor(editorUnitId);
		if (editor == null) return;
		this._univerInstanceService.setCurrentUnitForType(editorUnitId);
		const valueCount = (((_editor$getDocumentDa = editor.getDocumentData().body) === null || _editor$getDocumentDa === void 0 ? void 0 : _editor$getDocumentDa.dataStream) ?? "").replace(/\r?\n/g, "").length;
		this._contextService.setContextValue(_univerjs_core.EDITOR_ACTIVATED, true);
		if (!(0, _univerjs_core.isInternalEditorID)(editorUnitId)) this._contextService.setContextValue(_univerjs_core.FOCUSING_EDITOR_STANDALONE, true);
		if ((0, _univerjs_core.isCommentEditorID)(editorUnitId)) this._contextService.setContextValue(_univerjs_core.FOCUSING_COMMENT_EDITOR, true);
		editor.focus();
		this._setFocusId(editorUnitId);
		this._focus$.next({
			startOffset: valueCount,
			endOffset: valueCount
		});
	}
	dispose() {
		this._editors.clear();
		this._editorRenderConfigs.clear();
		super.dispose();
	}
	getEditor(id = this._getCurrentEditorUnitId()) {
		return this._editors.get(id);
	}
	getAllEditor() {
		return this._editors;
	}
	getEditorRenderConfig(editorUnitId) {
		const editor = this._editors.get(editorUnitId);
		return this._editorRenderConfigs.get(editorUnitId) ?? (editor === null || editor === void 0 ? void 0 : editor.getRenderConfig()) ?? null;
	}
	register(config, container) {
		const { initialSnapshot, canvasStyle = {} } = config;
		const editorUnitId = initialSnapshot.id;
		this._editorRenderConfigs.set(editorUnitId, {
			canvasStyle,
			scrollBar: config.scrollBar,
			...config.backScrollOffset === void 0 ? {} : { backScrollOffset: config.backScrollOffset },
			...config.preserveHostFocus === void 0 ? {} : { preserveHostFocus: config.preserveHostFocus }
		});
		if (this._univerInstanceService.getUnit(editorUnitId, _univerjs_core.UniverInstanceType.UNIVER_DOC) == null) this._univerInstanceService.createUnit(_univerjs_core.UniverInstanceType.UNIVER_DOC, initialSnapshot || this._getBlank(editorUnitId), { makeCurrent: false });
		let render = this._renderManagerService.getRenderUnitById(editorUnitId);
		if (render == null) render = this._renderManagerService.createRender(editorUnitId);
		if (render) {
			render.engine.mount(container);
			const editor = new Editor({
				...config,
				render,
				editorDom: container,
				canvasStyle
			}, this._univerInstanceService, this._docSelectionManagerService, this._commandService, this._undoRedoService, this._injector);
			this._editors.set(editorUnitId, editor);
			const resolvedEditorBackground = resolveDocRenderBackground({
				canvasColorService: render.engine.canvasColorService,
				editorBackgroundColor: canvasStyle.backgroundColor,
				isEditor: true
			});
			render.engine.getCanvas().getCanvasEle().style.backgroundColor = resolvedEditorBackground.canvasElementBackgroundColor;
			const docBackground = render.components.get("__Document_Render_Background__");
			docBackground === null || docBackground === void 0 || docBackground.setFillColors(resolvedEditorBackground.docBackgroundFillColor, resolvedEditorBackground.docBackgroundFillColor, resolvedEditorBackground.docBackgroundFillColor, resolvedEditorBackground.docBackgroundFillColor);
			if (!config.scrollBar) {
				var _render$mainComponent, _viewport$getScrollBa;
				const viewport = (_render$mainComponent = render.mainComponent) === null || _render$mainComponent === void 0 || (_render$mainComponent = _render$mainComponent.getScene()) === null || _render$mainComponent === void 0 || (_render$mainComponent = _render$mainComponent.getViewports()) === null || _render$mainComponent === void 0 ? void 0 : _render$mainComponent[0];
				viewport === null || viewport === void 0 || (_viewport$getScrollBa = viewport.getScrollBar()) === null || _viewport$getScrollBa === void 0 || _viewport$getScrollBa.dispose();
				viewport === null || viewport === void 0 || viewport.updateScrollVal({
					scrollX: 0,
					scrollY: 0,
					viewportScrollX: 0,
					viewportScrollY: 0
				});
			}
		}
		return (0, _univerjs_core.toDisposable)(() => {
			this._unRegister(editorUnitId);
		});
	}
	_unRegister(editorUnitId) {
		var _this$_editorRenderCo;
		const editor = this._editors.get(editorUnitId);
		if (editor == null) {
			this._editorRenderConfigs.delete(editorUnitId);
			return;
		}
		const preserveHostFocus = ((_this$_editorRenderCo = this._editorRenderConfigs.get(editorUnitId)) === null || _this$_editorRenderCo === void 0 ? void 0 : _this$_editorRenderCo.preserveHostFocus) === true;
		const currentDoc = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const focusedHost = preserveHostFocus ? this._univerInstanceService.getFocusedUnit() : null;
		const focusedHostId = focusedHost === null || focusedHost === void 0 ? void 0 : focusedHost.getUnitId();
		const restoreHostUnitId = (currentDoc === null || currentDoc === void 0 ? void 0 : currentDoc.getUnitId()) === editorUnitId && focusedHostId != null && focusedHostId !== editorUnitId && this._univerInstanceService.getUnitType(focusedHostId) === _univerjs_core.UniverInstanceType.UNIVER_DOC ? focusedHostId : null;
		this._renderManagerService.removeRender(editorUnitId);
		editor.dispose();
		this._editors.delete(editorUnitId);
		this._editorRenderConfigs.delete(editorUnitId);
		this._univerInstanceService.disposeUnit(editorUnitId);
		if (restoreHostUnitId != null) this._univerInstanceService.setCurrentUnitForType(restoreHostUnitId);
	}
	_getCurrentEditorUnitId() {
		return this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC).getUnitId();
	}
	_getBlank(id) {
		return {
			id,
			body: {
				dataStream: `${_univerjs_core.DEFAULT_EMPTY_DOCUMENT_VALUE}`,
				textRuns: [],
				paragraphs: [{
					startIndex: 0,
					paragraphId: (0, _univerjs_core.createParagraphId)(/* @__PURE__ */ new Set())
				}]
			},
			documentStyle: {
				renderConfig: {
					verticalAlign: _univerjs_core.VerticalAlign.TOP,
					horizontalAlign: _univerjs_core.HorizontalAlign.LEFT
				},
				marginLeft: 3,
				marginTop: 0,
				marginRight: 3
			}
		};
	}
};
EditorService = __decorate([
	__decorateParam(0, _univerjs_core.IUniverInstanceService),
	__decorateParam(1, _univerjs_engine_render.IRenderManagerService),
	__decorateParam(2, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(3, _univerjs_core.IContextService),
	__decorateParam(4, _univerjs_core.ICommandService),
	__decorateParam(5, _univerjs_core.IUndoRedoService),
	__decorateParam(6, (0, _univerjs_core.Inject)(_univerjs_core.Injector))
], EditorService);
const IEditorService = (0, _univerjs_core.createIdentifier)("univer.editor.service");

//#endregion
//#region src/commands/commands/inline-format.command.ts
function handleInlineFormat(preCommandId, params, commandService) {
	return commandService.executeCommand(SetInlineFormatCommand.id, {
		preCommandId,
		...params ?? {}
	});
}
const SetInlineFormatBoldCommandId = "doc.command.set-inline-format-bold";
const SetInlineFormatBoldCommand = {
	id: SetInlineFormatBoldCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(SetInlineFormatBoldCommandId, params, commandService);
	}
};
const SetInlineFormatItalicCommandId = "doc.command.set-inline-format-italic";
const SetInlineFormatItalicCommand = {
	id: SetInlineFormatItalicCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(SetInlineFormatItalicCommandId, params, commandService);
	}
};
const SetInlineFormatUnderlineCommandId = "doc.command.set-inline-format-underline";
const SetInlineFormatUnderlineCommand = {
	id: SetInlineFormatUnderlineCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(SetInlineFormatUnderlineCommandId, params, commandService);
	}
};
const SetInlineFormatStrikethroughCommandId = "doc.command.set-inline-format-strikethrough";
const SetInlineFormatStrikethroughCommand = {
	id: SetInlineFormatStrikethroughCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(SetInlineFormatStrikethroughCommandId, params, commandService);
	}
};
const SetInlineFormatSubscriptCommandId = "doc.command.set-inline-format-subscript";
const SetInlineFormatSubscriptCommand = {
	id: SetInlineFormatSubscriptCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(SetInlineFormatSubscriptCommandId, params, commandService);
	}
};
const SetInlineFormatSuperscriptCommandId = "doc.command.set-inline-format-superscript";
const SetInlineFormatSuperscriptCommand = {
	id: SetInlineFormatSuperscriptCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(SetInlineFormatSuperscriptCommandId, params, commandService);
	}
};
const SetInlineFormatFontSizeCommandId = "doc.command.set-inline-format-fontsize";
const SetInlineFormatFontSizeCommand = {
	id: SetInlineFormatFontSizeCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(SetInlineFormatFontSizeCommandId, params, commandService);
	}
};
const SetInlineFormatFontFamilyCommandId = "doc.command.set-inline-format-font-family";
const SetInlineFormatFontFamilyCommand = {
	id: SetInlineFormatFontFamilyCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(SetInlineFormatFontFamilyCommandId, params, commandService);
	}
};
const SetInlineFormatTextColorCommandId = "doc.command.set-inline-format-text-color";
const SetInlineFormatTextColorCommand = {
	id: SetInlineFormatTextColorCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(SetInlineFormatTextColorCommandId, params, commandService);
	}
};
const ResetInlineFormatTextColorCommandId = "doc.command.reset-inline-format-text-color";
const ResetInlineFormatTextColorCommand = {
	id: ResetInlineFormatTextColorCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(ResetInlineFormatTextColorCommandId, params, commandService);
	}
};
const SetInlineFormatTextFillCommandId = "doc.command.set-inline-format-text-fill";
const SetInlineFormatTextFillCommand = {
	id: SetInlineFormatTextFillCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(SetInlineFormatTextFillCommandId, params, commandService);
	}
};
const SetInlineFormatTextBackgroundColorCommandId = "doc.command.set-inline-format-text-background-color";
const SetInlineFormatTextBackgroundColorCommand = {
	id: SetInlineFormatTextBackgroundColorCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(SetInlineFormatTextBackgroundColorCommandId, params, commandService);
	}
};
const ResetInlineFormatTextBackgroundColorCommandId = "doc.command.reset-inline-format-text-background-color";
const ResetInlineFormatTextBackgroundColorCommand = {
	id: ResetInlineFormatTextBackgroundColorCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		return handleInlineFormat(ResetInlineFormatTextBackgroundColorCommandId, params, commandService);
	}
};
const COMMAND_ID_TO_FORMAT_KEY_MAP = {
	[SetInlineFormatBoldCommand.id]: "bl",
	[SetInlineFormatItalicCommand.id]: "it",
	[SetInlineFormatUnderlineCommand.id]: "ul",
	[SetInlineFormatStrikethroughCommand.id]: "st",
	[SetInlineFormatFontSizeCommand.id]: "fs",
	[SetInlineFormatFontFamilyCommand.id]: "ff",
	[SetInlineFormatTextColorCommand.id]: "cl",
	[ResetInlineFormatTextColorCommand.id]: "cl",
	[SetInlineFormatTextBackgroundColorCommand.id]: "bg",
	[ResetInlineFormatTextBackgroundColorCommand.id]: "bg",
	[SetInlineFormatSubscriptCommand.id]: "va",
	[SetInlineFormatSuperscriptCommand.id]: "va"
};
const SetInlineFormatCommand = {
	id: "doc.command.set-inline-format",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		var _docDataModel$getSelf;
		const { value, preCommandId } = params;
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const editorService = accessor.has(IEditorService) ? accessor.get(IEditorService) : null;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const docMenuStyleService = accessor.get(DocMenuStyleService);
		const textRanges = docSelectionManagerService.getTextRanges() ?? [];
		const docRanges = textRanges.length > 0 ? textRanges.filter((range) => range.startOffset != null && range.endOffset != null) : docSelectionManagerService.getDocRanges();
		const activeRange = docRanges.find((r) => r.isActive) ?? docRanges[0];
		if (docRanges.length === 0) return false;
		const { segmentId } = docRanges[0];
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null) return false;
		const body = (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		if (body == null) return false;
		const unitId = docDataModel.getUnitId();
		let formatValue;
		let formatPatch;
		switch (preCommandId) {
			case SetInlineFormatBoldCommand.id:
			case SetInlineFormatItalicCommand.id:
			case SetInlineFormatUnderlineCommand.id:
			case SetInlineFormatStrikethroughCommand.id:
			case SetInlineFormatSubscriptCommand.id:
			case SetInlineFormatSuperscriptCommand.id:
				formatValue = getReverseFormatValueInSelection(getStyleInTextRange(body, activeRange, docMenuStyleService.getDefaultStyle()), preCommandId);
				break;
			case SetInlineFormatFontSizeCommand.id:
			case SetInlineFormatFontFamilyCommand.id:
				formatValue = value;
				break;
			case SetInlineFormatTextColorCommand.id:
			case SetInlineFormatTextBackgroundColorCommand.id:
				formatValue = { rgb: value };
				break;
			case SetInlineFormatTextFillCommand.id:
				formatPatch = value;
				break;
			case ResetInlineFormatTextColorCommand.id:
			case ResetInlineFormatTextBackgroundColorCommand.id:
				formatValue = { rgb: null };
				break;
			default: throw new Error(`Unknown command: ${preCommandId} in handleInlineFormat`);
		}
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges: docRanges
			}
		};
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const textStylePatch = formatPatch ?? { [COMMAND_ID_TO_FORMAT_KEY_MAP[preCommandId]]: formatValue };
		const shouldClearSolidTextFill = preCommandId === SetInlineFormatTextColorCommand.id && !(0, _univerjs_core.isInternalEditorID)(unitId) && !(editorService === null || editorService === void 0 ? void 0 : editorService.isEditor(unitId));
		const pushTextStyleUpdate = (len, ts, replace = false) => {
			if (len <= 0) return;
			textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				body: {
					dataStream: "",
					textRuns: [{
						st: 0,
						ed: len,
						ts
					}]
				},
				len,
				...replace ? { coverType: _univerjs_core.UpdateDocsAttributeType.REPLACE } : {}
			});
		};
		const memoryCursor = new _univerjs_core.MemoryCursor();
		memoryCursor.reset();
		for (const range of docRanges) {
			let { startOffset, endOffset, rangeType } = range;
			if (startOffset == null || endOffset == null) continue;
			if (rangeType === _univerjs_core.DOC_RANGE_TYPE.RECT) startOffset = startOffset - 1;
			if (startOffset === endOffset) {
				const cacheStyle = docMenuStyleService.getStyleCache();
				const key = COMMAND_ID_TO_FORMAT_KEY_MAP[preCommandId];
				if (formatPatch) docMenuStyleService.setStyleCache(formatPatch);
				else docMenuStyleService.setStyleCache({ [key]: (cacheStyle === null || cacheStyle === void 0 ? void 0 : cacheStyle[key]) !== void 0 && isNeedReverseValue(key) ? getReverseFormatValue(cacheStyle, key, preCommandId) : formatValue });
				continue;
			}
			const len = startOffset - memoryCursor.cursor;
			if (len !== 0) textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len
			});
			if (shouldClearSolidTextFill) {
				let currentOffset = startOffset;
				for (const textRun of body.textRuns ?? []) {
					var _textRun$ts;
					if (((_textRun$ts = textRun.ts) === null || _textRun$ts === void 0 || (_textRun$ts = _textRun$ts.textFill) === null || _textRun$ts === void 0 ? void 0 : _textRun$ts.type) !== "solid") continue;
					const solidStart = Math.max(currentOffset, textRun.st);
					const solidEnd = Math.min(endOffset, textRun.ed);
					if (solidStart >= solidEnd) continue;
					pushTextStyleUpdate(solidStart - currentOffset, textStylePatch);
					const replacementStyle = {
						...textRun.ts,
						...textStylePatch
					};
					delete replacementStyle.textFill;
					pushTextStyleUpdate(solidEnd - solidStart, replacementStyle, true);
					currentOffset = solidEnd;
				}
				pushTextStyleUpdate(endOffset - currentOffset, textStylePatch);
			} else pushTextStyleUpdate(endOffset - startOffset, textStylePatch);
			memoryCursor.reset();
			memoryCursor.moveCursor(endOffset);
		}
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
function isTextDecoration(value) {
	return value !== null && typeof value === "object";
}
function isNeedReverseValue(key) {
	return !/fs|ff|cl|bg/.test(key);
}
function getReverseFormatValue(ts, key, preCommandId) {
	if (/bl|it/.test(key)) return (ts === null || ts === void 0 ? void 0 : ts[key]) === _univerjs_core.BooleanNumber.TRUE ? _univerjs_core.BooleanNumber.FALSE : _univerjs_core.BooleanNumber.TRUE;
	if (/ul|st/.test(key)) return isTextDecoration(ts === null || ts === void 0 ? void 0 : ts[key]) && (ts === null || ts === void 0 ? void 0 : ts[key]).s === _univerjs_core.BooleanNumber.TRUE ? { s: _univerjs_core.BooleanNumber.FALSE } : { s: _univerjs_core.BooleanNumber.TRUE };
	if (/va/.test(key)) if (preCommandId === SetInlineFormatSubscriptCommand.id) return (ts === null || ts === void 0 ? void 0 : ts[key]) === _univerjs_core.BaselineOffset.SUBSCRIPT ? _univerjs_core.BaselineOffset.NORMAL : _univerjs_core.BaselineOffset.SUBSCRIPT;
	else return (ts === null || ts === void 0 ? void 0 : ts[key]) === _univerjs_core.BaselineOffset.SUPERSCRIPT ? _univerjs_core.BaselineOffset.NORMAL : _univerjs_core.BaselineOffset.SUPERSCRIPT;
}
function getStyleInTextRange(body, textRange, defaultStyle) {
	var _textRuns$find, _textRuns$0$ts, _textRuns$0$ts2, _textRuns$find2, _textRuns$find3, _textRuns$find4;
	const { startOffset, endOffset, collapsed } = textRange;
	if (collapsed) {
		const textRuns = body.textRuns ?? [];
		let textRun = null;
		for (let i = textRuns.length - 1; i >= 0; i--) {
			const curTextRun = textRuns[i];
			if (curTextRun.st < startOffset && startOffset <= curTextRun.ed) {
				textRun = curTextRun;
				break;
			}
		}
		return (textRun === null || textRun === void 0 ? void 0 : textRun.ts) ? {
			...defaultStyle,
			...textRun.ts
		} : defaultStyle;
	}
	const textRuns = (0, _univerjs_core.getTextRunSlice)(body, startOffset, endOffset) ?? [];
	const style = _univerjs_core.Tools.deepClone(defaultStyle);
	const fsArr = textRuns.map((t) => {
		var _t$ts;
		return t === null || t === void 0 || (_t$ts = t.ts) === null || _t$ts === void 0 ? void 0 : _t$ts.fs;
	}).filter(Boolean);
	style.fs = style.fs ? Math.max(style.fs, ...fsArr) : fsArr.length ? Math.max(...fsArr) : void 0;
	style.fs = !style.fs || Number.isNaN(style.fs) ? void 0 : style.fs;
	style.ff = ((_textRuns$find = textRuns.find((t) => {
		var _t$ts2;
		return ((_t$ts2 = t.ts) === null || _t$ts2 === void 0 ? void 0 : _t$ts2.ff) != null;
	})) === null || _textRuns$find === void 0 || (_textRuns$find = _textRuns$find.ts) === null || _textRuns$find === void 0 ? void 0 : _textRuns$find.ff) ?? style.ff;
	style.it = textRuns.length && textRuns.every((t) => {
		var _t$ts3;
		return ((_t$ts3 = t.ts) === null || _t$ts3 === void 0 ? void 0 : _t$ts3.it) === _univerjs_core.BooleanNumber.TRUE;
	}) ? _univerjs_core.BooleanNumber.TRUE : _univerjs_core.BooleanNumber.FALSE;
	style.bl = textRuns.length && textRuns.every((t) => {
		var _t$ts4;
		return ((_t$ts4 = t.ts) === null || _t$ts4 === void 0 ? void 0 : _t$ts4.bl) === _univerjs_core.BooleanNumber.TRUE;
	}) ? _univerjs_core.BooleanNumber.TRUE : _univerjs_core.BooleanNumber.FALSE;
	style.ul = textRuns.length && textRuns.every((t) => {
		var _t$ts5;
		return ((_t$ts5 = t.ts) === null || _t$ts5 === void 0 || (_t$ts5 = _t$ts5.ul) === null || _t$ts5 === void 0 ? void 0 : _t$ts5.s) === _univerjs_core.BooleanNumber.TRUE;
	}) ? (_textRuns$0$ts = textRuns[0].ts) === null || _textRuns$0$ts === void 0 ? void 0 : _textRuns$0$ts.ul : style.ul;
	style.st = textRuns.length && textRuns.every((t) => {
		var _t$ts6;
		return ((_t$ts6 = t.ts) === null || _t$ts6 === void 0 || (_t$ts6 = _t$ts6.st) === null || _t$ts6 === void 0 ? void 0 : _t$ts6.s) === _univerjs_core.BooleanNumber.TRUE;
	}) ? (_textRuns$0$ts2 = textRuns[0].ts) === null || _textRuns$0$ts2 === void 0 ? void 0 : _textRuns$0$ts2.st : style.st;
	style.bg = ((_textRuns$find2 = textRuns.find((t) => {
		var _t$ts7;
		return ((_t$ts7 = t.ts) === null || _t$ts7 === void 0 ? void 0 : _t$ts7.bg) != null;
	})) === null || _textRuns$find2 === void 0 || (_textRuns$find2 = _textRuns$find2.ts) === null || _textRuns$find2 === void 0 ? void 0 : _textRuns$find2.bg) ?? style.bg;
	style.cl = ((_textRuns$find3 = textRuns.find((t) => {
		var _t$ts8;
		return ((_t$ts8 = t.ts) === null || _t$ts8 === void 0 ? void 0 : _t$ts8.cl) != null;
	})) === null || _textRuns$find3 === void 0 || (_textRuns$find3 = _textRuns$find3.ts) === null || _textRuns$find3 === void 0 ? void 0 : _textRuns$find3.cl) ?? style.cl;
	style.textFill = ((_textRuns$find4 = textRuns.find((t) => {
		var _t$ts9;
		return ((_t$ts9 = t.ts) === null || _t$ts9 === void 0 ? void 0 : _t$ts9.textFill) != null;
	})) === null || _textRuns$find4 === void 0 || (_textRuns$find4 = _textRuns$find4.ts) === null || _textRuns$find4 === void 0 ? void 0 : _textRuns$find4.textFill) ?? style.textFill;
	const vas = textRuns.filter((t) => {
		var _t$ts10;
		return (t === null || t === void 0 || (_t$ts10 = t.ts) === null || _t$ts10 === void 0 ? void 0 : _t$ts10.va) != null;
	});
	if (vas.length > 0 && vas.length === textRuns.length) {
		var _vas$0$ts;
		const va = (_vas$0$ts = vas[0].ts) === null || _vas$0$ts === void 0 ? void 0 : _vas$0$ts.va;
		let isSame = true;
		for (let i = 1; i < vas.length; i++) {
			var _vas$i$ts;
			if (((_vas$i$ts = vas[i].ts) === null || _vas$i$ts === void 0 ? void 0 : _vas$i$ts.va) !== va) {
				isSame = false;
				break;
			}
		}
		if (isSame) style.va = va;
	}
	return style;
}
/**
* When clicking on a Bold menu item, you should un-bold if there is bold in the selections,
* or bold if there is no bold text. This method is used to get the reverse style value calculated
* from textRuns in the selection
*/
function getReverseFormatValueInSelection(textStyle, preCommandId) {
	const key = COMMAND_ID_TO_FORMAT_KEY_MAP[preCommandId];
	return getReverseFormatValue(textStyle, key, preCommandId);
}

//#endregion
//#region src/commands/commands/insert-custom-range.command.ts
const InsertCustomRangeCommand = {
	id: "doc.command.insert-custom-range",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		if (!params) return false;
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const { unitId, rangeId = (0, _univerjs_core.generateRandomId)(), textRanges, properties, text, wholeEntity } = params;
		const replaceSelectionParams = {
			unitId,
			historyAction: _univerjs_docs.DocHistoryAction.InsertCustomRange,
			textRanges,
			body: {
				dataStream: text,
				customRanges: [{
					startIndex: 0,
					endIndex: text.length - 1,
					rangeId,
					rangeType: _univerjs_core.CustomRangeType.CUSTOM,
					properties,
					wholeEntity
				}]
			}
		};
		return commandService.syncExecuteCommand(ReplaceSelectionCommand.id, replaceSelectionParams);
	}
};

//#endregion
//#region src/commands/commands/list.command.ts
const ListOperationCommand = {
	id: "doc.command.list-operation",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		var _segment$getBody, _segment$getBody2;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const listType = params.listType;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const docRanges = params.docRange ?? docSelectionManagerService.getDocRanges() ?? [];
		if (docDataModel == null || docRanges.length === 0) return false;
		const segmentId = docRanges[0].segmentId;
		const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
		const paragraphs = (segment === null || segment === void 0 || (_segment$getBody = segment.getBody()) === null || _segment$getBody === void 0 ? void 0 : _segment$getBody.paragraphs) ?? [];
		const dataStream = (segment === null || segment === void 0 || (_segment$getBody2 = segment.getBody()) === null || _segment$getBody2 === void 0 ? void 0 : _segment$getBody2.dataStream) ?? "";
		if (!paragraphs.length) return false;
		const currentParagraphs = _univerjs_core.BuildTextUtils.range.getParagraphsInRanges(docRanges, paragraphs, dataStream);
		const unitId = docDataModel.getUnitId();
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges: docRanges,
				isEditing: false
			}
		};
		new _univerjs_core.MemoryCursor().reset();
		const textX = _univerjs_core.BuildTextUtils.paragraph.bullet.switch({
			paragraphs: currentParagraphs,
			listType,
			document: docDataModel,
			segmentId
		});
		const jsonX = _univerjs_core.JSONX.getInstance();
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
const ChangeListTypeCommand = {
	id: "doc.command.change-list-type",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		var _segment$getBody3, _segment$getBody4;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const { listType } = params;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const activeRanges = params.docRange ?? docSelectionManagerService.getDocRanges();
		if (docDataModel == null || activeRanges == null || !activeRanges.length) return false;
		const { segmentId } = activeRanges[0];
		const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
		const paragraphs = (segment === null || segment === void 0 || (_segment$getBody3 = segment.getBody()) === null || _segment$getBody3 === void 0 ? void 0 : _segment$getBody3.paragraphs) ?? [];
		const dataStream = (segment === null || segment === void 0 || (_segment$getBody4 = segment.getBody()) === null || _segment$getBody4 === void 0 ? void 0 : _segment$getBody4.dataStream) ?? "";
		if (!paragraphs.length) return false;
		const currentParagraphs = _univerjs_core.BuildTextUtils.range.getParagraphsInRanges(activeRanges, paragraphs, dataStream);
		const unitId = docDataModel.getUnitId();
		const textX = _univerjs_core.BuildTextUtils.paragraph.bullet.set({
			paragraphs: currentParagraphs,
			listType,
			segmentId,
			document: docDataModel
		});
		if (!textX) return false;
		const jsonX = _univerjs_core.JSONX.getInstance();
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges: activeRanges,
				isEditing: false
			}
		};
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
let ChangeListNestingLevelType = /* @__PURE__ */ function(ChangeListNestingLevelType) {
	ChangeListNestingLevelType[ChangeListNestingLevelType["increase"] = 1] = "increase";
	ChangeListNestingLevelType[ChangeListNestingLevelType["decrease"] = -1] = "decrease";
	return ChangeListNestingLevelType;
}({});
const ChangeListNestingLevelCommand = {
	id: "doc.command.change-list-nesting-level",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		var _segment$getBody5, _segment$getBody6;
		if (!params) return false;
		const { type } = params;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const activeRange = docSelectionManagerService.getActiveTextRange();
		if (docDataModel == null || activeRange == null) return false;
		const { segmentId } = activeRange;
		const selections = docSelectionManagerService.getDocRanges() ?? [];
		const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
		const paragraphs = (segment === null || segment === void 0 || (_segment$getBody5 = segment.getBody()) === null || _segment$getBody5 === void 0 ? void 0 : _segment$getBody5.paragraphs) ?? [];
		const dataStream = (segment === null || segment === void 0 || (_segment$getBody6 = segment.getBody()) === null || _segment$getBody6 === void 0 ? void 0 : _segment$getBody6.dataStream) ?? "";
		if (!paragraphs.length) return false;
		const currentParagraphs = _univerjs_core.BuildTextUtils.range.getParagraphsInRange(activeRange, paragraphs, dataStream);
		const unitId = docDataModel.getUnitId();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const textX = _univerjs_core.BuildTextUtils.paragraph.bullet.changeNestLevel({
			paragraphs: currentParagraphs,
			type,
			segmentId,
			document: docDataModel
		});
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges: selections,
				isEditing: false
			}
		};
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
const BulletListCommand = {
	id: "doc.command.bullet-list",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		if (params === null || params === void 0 ? void 0 : params.value) return commandService.syncExecuteCommand(ChangeListTypeCommand.id, {
			listType: params.value,
			docRange: params.docRange
		});
		return commandService.syncExecuteCommand(ListOperationCommand.id, {
			listType: _univerjs_core.PresetListType.BULLET_LIST,
			docRange: params === null || params === void 0 ? void 0 : params.docRange
		});
	}
};
const CheckListCommand = {
	id: "doc.command.check-list",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		if (params === null || params === void 0 ? void 0 : params.value) return commandService.syncExecuteCommand(ChangeListTypeCommand.id, {
			listType: params.value,
			docRange: params.docRange
		});
		return commandService.syncExecuteCommand(ListOperationCommand.id, {
			listType: _univerjs_core.PresetListType.CHECK_LIST,
			docRange: params === null || params === void 0 ? void 0 : params.docRange
		});
	}
};
const ToggleCheckListCommand = {
	id: "doc.command.toggle-check-list",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		var _docDataModel$getSelf;
		if (!params) return false;
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const { index, segmentId, textRanges } = params;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null) return false;
		if (((_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 || (_docDataModel$getSelf = _docDataModel$getSelf.getBody()) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.paragraphs) == null) return false;
		const unitId = docDataModel.getUnitId();
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges: textRanges ?? [],
				segmentId,
				isEditing: false
			}
		};
		const textX = _univerjs_core.BuildTextUtils.paragraph.bullet.toggleChecklist({
			document: docDataModel,
			paragraphIndex: index,
			segmentId
		});
		if (!textX) return false;
		const jsonX = _univerjs_core.JSONX.getInstance();
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
const OrderListCommand = {
	id: "doc.command.order-list",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		if (params === null || params === void 0 ? void 0 : params.value) return commandService.syncExecuteCommand(ChangeListTypeCommand.id, {
			listType: params.value,
			docRange: params.docRange
		});
		return commandService.syncExecuteCommand(ListOperationCommand.id, {
			listType: _univerjs_core.PresetListType.ORDER_LIST,
			docRange: params === null || params === void 0 ? void 0 : params.docRange
		});
	}
};
const QuickListCommand = {
	id: "doc.command.quick-list",
	type: _univerjs_core.CommandType.COMMAND,
	handler(accessor, params) {
		var _docDataModel$getBody;
		if (!params) return false;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const activeRange = docSelectionManagerService.getActiveTextRange();
		if (docDataModel == null || activeRange == null) return false;
		const { segmentId, startOffset } = activeRange;
		const { listType, paragraph } = params;
		const { paragraphStart, paragraphEnd } = paragraph;
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const { paragraphStyle = {} } = paragraph;
		const bulletParagraphTextStyle = (_univerjs_core.PRESET_LIST_TYPE[listType].nestingLevel[0].paragraphProperties || {}).textStyle;
		let listId = (0, _univerjs_core.generateRandomId)(6);
		const paragraphs = ((_docDataModel$getBody = docDataModel.getBody()) === null || _docDataModel$getBody === void 0 ? void 0 : _docDataModel$getBody.paragraphs) ?? [];
		const curIndex = paragraphs.findIndex((i) => i.startIndex === paragraph.startIndex);
		const prevParagraph = paragraphs[curIndex - 1];
		const nextParagraph = paragraphs[curIndex + 1];
		if (prevParagraph && prevParagraph.bullet && prevParagraph.bullet.listType.indexOf(listType) === 0) listId = prevParagraph.bullet.listId;
		else if (nextParagraph && nextParagraph.bullet && nextParagraph.bullet.listType.indexOf(listType) === 0) listId = nextParagraph.bullet.listId;
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId: docDataModel.getUnitId(),
				actions: [],
				textRanges: [{
					startOffset: paragraphStart,
					endOffset: paragraphStart,
					collapsed: true
				}],
				isEditing: false
			}
		};
		textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: paragraphStart
		});
		textX.push({
			t: _univerjs_core.TextXActionType.DELETE,
			len: startOffset - paragraphStart
		});
		if (paragraphEnd > startOffset) textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: paragraphEnd - startOffset
		});
		textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: 1,
			body: {
				dataStream: "",
				paragraphs: [{
					startIndex: 0,
					paragraphId: paragraph.paragraphId,
					paragraphStyle: {
						...paragraphStyle,
						textStyle: {
							...paragraphStyle.textStyle,
							...bulletParagraphTextStyle
						},
						indentFirstLine: void 0
					},
					bullet: {
						...paragraph.bullet ?? { nestingLevel: 0 },
						listType,
						listId
					}
				}]
			}
		});
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
function insertList(accessor, listType, trigger) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	const docDataModel = accessor.get(_univerjs_core.IUniverInstanceService).getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
	if (!docDataModel) return false;
	let contentInsertRange = null;
	try {
		contentInsertRange = accessor.get(_univerjs_docs.DocContentInsertService).consumeInsertRange(docDataModel.getUnitId());
	} catch {
		contentInsertRange = null;
	}
	const paragraph = contentInsertRange ? null : getCurrentParagraph(accessor);
	if (!contentInsertRange && !paragraph) return false;
	const sourceParagraph = paragraph || void 0;
	const insertOffset = (contentInsertRange === null || contentInsertRange === void 0 ? void 0 : contentInsertRange.startOffset) ?? sourceParagraph.startIndex + 1;
	const sourceBullet = sourceParagraph === null || sourceParagraph === void 0 ? void 0 : sourceParagraph.bullet;
	const textX = _univerjs_core.BuildTextUtils.selection.replace({
		doc: docDataModel,
		selection: {
			startOffset: insertOffset,
			endOffset: (contentInsertRange === null || contentInsertRange === void 0 ? void 0 : contentInsertRange.endOffset) ?? insertOffset,
			collapsed: true
		},
		body: {
			dataStream: "\r",
			paragraphs: [{
				startIndex: 0,
				paragraphId: (0, _univerjs_core.createParagraphId)(/* @__PURE__ */ new Set()),
				paragraphStyle: { ...(sourceParagraph === null || sourceParagraph === void 0 ? void 0 : sourceParagraph.paragraphStyle) ?? {} },
				bullet: {
					listType,
					listId: (sourceBullet === null || sourceBullet === void 0 ? void 0 : sourceBullet.listType) === listType ? sourceBullet.listId : (0, _univerjs_core.generateRandomId)(6),
					nestingLevel: (sourceBullet === null || sourceBullet === void 0 ? void 0 : sourceBullet.listType) === listType ? sourceBullet.nestingLevel : 0
				}
			}]
		}
	});
	if (!textX) return false;
	const doMutation = {
		id: _univerjs_docs.RichTextEditingMutation.id,
		params: {
			unitId: docDataModel.getUnitId(),
			actions: [],
			textRanges: [{
				startOffset: insertOffset,
				endOffset: insertOffset,
				collapsed: true
			}],
			isEditing: false,
			trigger
		}
	};
	const jsonX = _univerjs_core.JSONX.getInstance();
	const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel);
	doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
	const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
	return Boolean(result);
}
const InsertBulletListBellowCommand = {
	id: "doc.command.insert-bullet-list-bellow",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return insertList(accessor, _univerjs_core.PresetListType.BULLET_LIST, InsertBulletListBellowCommand.id);
	}
};
const InsertOrderListBellowCommand = {
	id: "doc.command.insert-order-list-bellow",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return insertList(accessor, _univerjs_core.PresetListType.ORDER_LIST, InsertOrderListBellowCommand.id);
	}
};
const InsertCheckListBellowCommand = {
	id: "doc.command.insert-check-list-bellow",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return insertList(accessor, _univerjs_core.PresetListType.CHECK_LIST, InsertCheckListBellowCommand.id);
	}
};

//#endregion
//#region src/commands/commands/paragraph-align.command.ts
const AlignOperationCommand = {
	id: "doc.command.align-action",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		var _segment$getBody, _segment$getBody2;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const { alignType } = params;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null) return false;
		const allRanges = docSelectionManagerService.getDocRanges();
		if (allRanges.length === 0) return false;
		const segmentId = allRanges[0].segmentId;
		const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
		const paragraphs = (segment === null || segment === void 0 || (_segment$getBody = segment.getBody()) === null || _segment$getBody === void 0 ? void 0 : _segment$getBody.paragraphs) ?? [];
		const dataStream = (segment === null || segment === void 0 || (_segment$getBody2 = segment.getBody()) === null || _segment$getBody2 === void 0 ? void 0 : _segment$getBody2.dataStream) ?? "";
		if (paragraphs == null) return false;
		const currentParagraphs = _univerjs_core.BuildTextUtils.range.getParagraphsInRanges(allRanges, paragraphs, dataStream);
		const unitId = docDataModel.getUnitId();
		const isAlreadyAligned = currentParagraphs.every((paragraph) => {
			var _paragraph$paragraphS;
			return ((_paragraph$paragraphS = paragraph.paragraphStyle) === null || _paragraph$paragraphS === void 0 ? void 0 : _paragraph$paragraphS.horizontalAlign) === alignType;
		});
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges: allRanges
			}
		};
		const memoryCursor = new _univerjs_core.MemoryCursor();
		memoryCursor.reset();
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		for (const paragraph of currentParagraphs) {
			const { startIndex } = paragraph;
			textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len: startIndex - memoryCursor.cursor
			});
			const paragraphStyle = {
				...paragraph.paragraphStyle,
				horizontalAlign: isAlreadyAligned ? _univerjs_core.HorizontalAlign.UNSPECIFIED : alignType
			};
			textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len: 1,
				body: {
					dataStream: "",
					paragraphs: [{
						...paragraph,
						paragraphStyle,
						startIndex: 0
					}]
				},
				coverType: _univerjs_core.UpdateDocsAttributeType.REPLACE
			});
			memoryCursor.moveCursorTo(startIndex + 1);
		}
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
const AlignLeftCommand = {
	id: "doc.command.align-left",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(AlignOperationCommand.id, { alignType: _univerjs_core.HorizontalAlign.LEFT });
	}
};
const AlignCenterCommand = {
	id: "doc.command.align-center",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(AlignOperationCommand.id, { alignType: _univerjs_core.HorizontalAlign.CENTER });
	}
};
const AlignRightCommand = {
	id: "doc.command.align-right",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(AlignOperationCommand.id, { alignType: _univerjs_core.HorizontalAlign.RIGHT });
	}
};
const AlignJustifyCommand = {
	id: "doc.command.align-justify",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(AlignOperationCommand.id, { alignType: _univerjs_core.HorizontalAlign.JUSTIFIED });
	}
};

//#endregion
//#region src/commands/operations/set-doc-zoom-ratio.operation.ts
const SetDocZoomRatioUndoMutationFactory = (accessor, params) => {
	const documentModel = accessor.get(_univerjs_core.IUniverInstanceService).getUnit(params.unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
	documentModel === null || documentModel === void 0 || documentModel.setZoomRatio(params.zoomRatio);
	return { ...params };
};
const SetDocZoomRatioOperation = {
	id: "doc.operation.set-zoom-ratio",
	type: _univerjs_core.CommandType.OPERATION,
	handler: (accessor, params) => {
		const documentModel = accessor.get(_univerjs_core.IUniverInstanceService).getUnit(params.unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!documentModel) return false;
		documentModel.setZoomRatio(params.zoomRatio);
		return true;
	}
};

//#endregion
//#region src/commands/commands/set-doc-zoom-ratio.command.ts
const SetDocZoomRatioCommand = {
	type: _univerjs_core.CommandType.COMMAND,
	id: "doc.command.set-zoom-ratio",
	handler: async (accessor, params) => {
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const undoRedoService = accessor.get(_univerjs_core.IUndoRedoService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const { zoomRatio = 1, documentId } = params || {};
		const docDatModel = documentId ? univerInstanceService.getUnit(documentId, _univerjs_core.UniverInstanceType.UNIVER_DOC) : univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!docDatModel) return false;
		const unitId = docDatModel.getUnitId();
		const setZoomRatioMutationParams = {
			zoomRatio,
			unitId
		};
		const undoMutationParams = SetDocZoomRatioUndoMutationFactory(accessor, setZoomRatioMutationParams);
		if (commandService.syncExecuteCommand(SetDocZoomRatioOperation.id, setZoomRatioMutationParams)) {
			undoRedoService.pushUndoRedo({
				unitID: unitId,
				undoMutations: [{
					id: SetDocZoomRatioOperation.id,
					params: undoMutationParams
				}],
				redoMutations: [{
					id: SetDocZoomRatioOperation.id,
					params: setZoomRatioMutationParams
				}]
			});
			return true;
		}
		return false;
	}
};

//#endregion
//#region src/commands/commands/set-heading.command.ts
const SetParagraphNamedStyleCommand = {
	id: "doc.command.set-paragraph-named-style",
	type: _univerjs_core.CommandType.COMMAND,
	handler(accessor, params) {
		if (!params) return false;
		const doc = accessor.get(_univerjs_core.IUniverInstanceService).getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!doc) return false;
		const unitId = doc.getUnitId();
		const selectionService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const contentInsertRange = params.textRanges ? null : (0, _univerjs_docs.consumeContentInsertRange)(accessor, unitId);
		if (contentInsertRange) return insertNamedStyleParagraph(accessor, doc, params.value, contentInsertRange.startOffset, contentInsertRange.endOffset, contentInsertRange.segmentId);
		const selections = params.textRanges ?? selectionService.getTextRanges({
			unitId,
			subUnitId: unitId
		});
		if (!(selections === null || selections === void 0 ? void 0 : selections.length)) return false;
		const segmentId = selections[0].segmentId;
		const documentDataModel = doc.getSelfOrHeaderFooterModel(segmentId);
		if (!documentDataModel) return false;
		const textX = _univerjs_core.BuildTextUtils.paragraph.style.set({
			document: documentDataModel,
			textRanges: selections,
			style: {
				namedStyleType: params.value,
				headingId: !params.value || params.value === _univerjs_core.NamedStyleType.NORMAL_TEXT ? void 0 : (0, _univerjs_core.generateRandomId)(6),
				spaceAbove: void 0,
				spaceBelow: void 0,
				lineSpacing: void 0,
				spacingRule: void 0
			},
			paragraphTextRun: {}
		});
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				actions: [],
				textRanges: selections,
				unitId,
				segmentId
			}
		};
		const jsonX = _univerjs_core.JSONX.getInstance();
		const path = (0, _univerjs_core.getRichTextEditPath)(doc, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
function insertNamedStyleParagraph(accessor, doc, namedStyleType, startOffset, endOffset, segmentId = "") {
	const textX = _univerjs_core.BuildTextUtils.selection.replace({
		doc,
		selection: {
			startOffset,
			endOffset,
			collapsed: startOffset === endOffset,
			segmentId
		},
		body: {
			dataStream: "\r",
			paragraphs: [{
				startIndex: 0,
				paragraphId: (0, _univerjs_core.createParagraphId)(/* @__PURE__ */ new Set()),
				paragraphStyle: {
					namedStyleType,
					headingId: !namedStyleType || namedStyleType === _univerjs_core.NamedStyleType.NORMAL_TEXT ? void 0 : (0, _univerjs_core.generateRandomId)(6)
				}
			}]
		}
	});
	if (!textX) return false;
	const jsonX = _univerjs_core.JSONX.getInstance();
	const commandService = accessor.get(_univerjs_core.ICommandService);
	const success = Boolean(commandService.syncExecuteCommand(_univerjs_docs.RichTextEditingMutation.id, {
		actions: jsonX.editOp(textX.serialize(), (0, _univerjs_core.getRichTextEditPath)(doc, segmentId)),
		isEditing: false,
		textRanges: [{
			startOffset,
			endOffset: startOffset,
			collapsed: true
		}],
		segmentId,
		unitId: doc.getUnitId()
	}));
	if (success) accessor.get(_univerjs_docs.DocSelectionManagerService).replaceDocRanges([{
		startOffset,
		endOffset: startOffset
	}], void 0, false);
	return success;
}
const QuickHeadingCommand = {
	id: "doc.command.quick-heading",
	type: _univerjs_core.CommandType.COMMAND,
	handler(accessor, params) {
		var _segment$getBody, _segment$getBody2;
		if (!params) return false;
		const { value } = params;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const activeRange = docSelectionManagerService.getActiveTextRange();
		if (docDataModel == null || activeRange == null || !activeRange.collapsed) return false;
		const { segmentId, startOffset } = activeRange;
		const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
		const paragraphs = (segment === null || segment === void 0 || (_segment$getBody = segment.getBody()) === null || _segment$getBody === void 0 ? void 0 : _segment$getBody.paragraphs) ?? [];
		const dataStream = (segment === null || segment === void 0 || (_segment$getBody2 = segment.getBody()) === null || _segment$getBody2 === void 0 ? void 0 : _segment$getBody2.dataStream) ?? "";
		const paragraph = _univerjs_core.BuildTextUtils.paragraph.util.getParagraphsInRange(activeRange, paragraphs, dataStream)[0];
		if (!paragraph) return false;
		const { paragraphStart } = paragraph;
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const { paragraphStyle = {} } = paragraph;
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId: docDataModel.getUnitId(),
				actions: [],
				textRanges: [{
					startOffset: paragraphStart,
					endOffset: paragraphStart,
					collapsed: true
				}],
				isEditing: false
			}
		};
		textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: paragraphStart
		});
		_univerjs_core.BuildTextUtils.paragraph.style.set({
			textRanges: [activeRange],
			document: docDataModel,
			style: {
				...paragraphStyle,
				headingId: (0, _univerjs_core.generateRandomId)(6),
				namedStyleType: value,
				lineSpacing: void 0,
				spacingRule: void 0
			},
			cursor: startOffset,
			deleteLen: startOffset - paragraphStart,
			textX,
			paragraphTextRun: {}
		});
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
const QUICK_HEADING_MAP = {
	"#": _univerjs_core.NamedStyleType.HEADING_1,
	"##": _univerjs_core.NamedStyleType.HEADING_2,
	"###": _univerjs_core.NamedStyleType.HEADING_3,
	"####": _univerjs_core.NamedStyleType.HEADING_4,
	"#####": _univerjs_core.NamedStyleType.HEADING_5
};
const H1HeadingCommand = {
	id: "doc.command.h1-heading",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(SetParagraphNamedStyleCommand.id, { value: _univerjs_core.NamedStyleType.HEADING_1 });
	}
};
const H2HeadingCommand = {
	id: "doc.command.h2-heading",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(SetParagraphNamedStyleCommand.id, { value: _univerjs_core.NamedStyleType.HEADING_2 });
	}
};
const H3HeadingCommand = {
	id: "doc.command.h3-heading",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(SetParagraphNamedStyleCommand.id, { value: _univerjs_core.NamedStyleType.HEADING_3 });
	}
};
const H4HeadingCommand = {
	id: "doc.command.h4-heading",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(SetParagraphNamedStyleCommand.id, { value: _univerjs_core.NamedStyleType.HEADING_4 });
	}
};
const H5HeadingCommand = {
	id: "doc.command.h5-heading",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(SetParagraphNamedStyleCommand.id, { value: _univerjs_core.NamedStyleType.HEADING_5 });
	}
};
const NormalTextHeadingCommand = {
	id: "doc.command.normal-text-heading",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(SetParagraphNamedStyleCommand.id, { value: _univerjs_core.NamedStyleType.NORMAL_TEXT });
	}
};
const TitleHeadingCommand = {
	id: "doc.command.title",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(SetParagraphNamedStyleCommand.id, { value: _univerjs_core.NamedStyleType.TITLE });
	}
};
const SubtitleHeadingCommand = {
	id: "doc.command.subtitle-heading",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).syncExecuteCommand(SetParagraphNamedStyleCommand.id, { value: _univerjs_core.NamedStyleType.SUBTITLE });
	}
};

//#endregion
//#region src/commands/commands/switch-doc-mode.command.ts
const SwitchDocModeCommand = {
	id: "doc.command.switch-mode",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor) => {
		var _renderManagerService, _renderManagerService2;
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const renderManagerService = accessor.get(_univerjs_engine_render.IRenderManagerService);
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const docDataModel = accessor.get(_univerjs_core.IUniverInstanceService).getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null) return false;
		const unitId = docDataModel.getUnitId();
		const skeleton = (_renderManagerService = renderManagerService.getRenderUnitById(unitId)) === null || _renderManagerService === void 0 ? void 0 : _renderManagerService.with(_univerjs_docs.DocSkeletonManagerService).getSkeleton();
		const docSelectionRenderService = (_renderManagerService2 = renderManagerService.getRenderUnitById(unitId)) === null || _renderManagerService2 === void 0 ? void 0 : _renderManagerService2.with(DocSelectionRenderService);
		if (skeleton == null || docSelectionRenderService == null) return false;
		const segmentId = docSelectionRenderService === null || docSelectionRenderService === void 0 ? void 0 : docSelectionRenderService.getSegment();
		const segmentPage = docSelectionRenderService === null || docSelectionRenderService === void 0 ? void 0 : docSelectionRenderService.getSegmentPage();
		const oldDocumentFlavor = docDataModel.getSnapshot().documentStyle.documentFlavor;
		const docRanges = docSelectionManagerService.getDocRanges();
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges: oldDocumentFlavor === _univerjs_core.DocumentFlavor.TRADITIONAL && !!segmentId ? [] : docRanges
			}
		};
		const jsonX = _univerjs_core.JSONX.getInstance();
		const rawActions = [];
		let action;
		if (oldDocumentFlavor === void 0) action = jsonX.insertOp(["documentStyle", "documentFlavor"], _univerjs_core.DocumentFlavor.MODERN);
		else if (oldDocumentFlavor === _univerjs_core.DocumentFlavor.MODERN) action = jsonX.replaceOp(["documentStyle", "documentFlavor"], oldDocumentFlavor, _univerjs_core.DocumentFlavor.TRADITIONAL);
		else action = jsonX.replaceOp(["documentStyle", "documentFlavor"], oldDocumentFlavor, _univerjs_core.DocumentFlavor.MODERN);
		if (action) rawActions.push(action);
		else return false;
		if (oldDocumentFlavor !== _univerjs_core.DocumentFlavor.MODERN) {
			const { drawings = {}, body } = docDataModel.getSnapshot();
			const customBlocks = (body === null || body === void 0 ? void 0 : body.customBlocks) ?? [];
			for (const drawingId in drawings) {
				var _glyph$parent, _column$parent;
				const drawing = drawings[drawingId];
				const customBlock = customBlocks.find((block) => block.blockId === drawingId);
				if (customBlock == null) continue;
				const drawingPositionV = drawing.docTransform.positionV;
				const { relativeFrom: prevRelativeFrom, posOffset: prevPosOffset } = drawingPositionV;
				if (prevRelativeFrom === _univerjs_core.ObjectRelativeFromV.PARAGRAPH) continue;
				const { startIndex } = customBlock;
				const glyph = skeleton.findNodeByCharIndex(startIndex, segmentId, segmentPage);
				const line = glyph === null || glyph === void 0 || (_glyph$parent = glyph.parent) === null || _glyph$parent === void 0 ? void 0 : _glyph$parent.parent;
				const column = line === null || line === void 0 ? void 0 : line.parent;
				const paragraphStartLine = column === null || column === void 0 ? void 0 : column.lines.find((l) => l.paragraphIndex === (line === null || line === void 0 ? void 0 : line.paragraphIndex) && l.paragraphStart);
				const page = column === null || column === void 0 || (_column$parent = column.parent) === null || _column$parent === void 0 ? void 0 : _column$parent.parent;
				if (glyph == null || line == null || paragraphStartLine == null || column == null || page == null) continue;
				let delta = 0;
				if (prevRelativeFrom === _univerjs_core.ObjectRelativeFromV.LINE) delta -= line.top;
				else if (prevRelativeFrom === _univerjs_core.ObjectRelativeFromV.PAGE) delta += page.marginTop;
				delta += paragraphStartLine.top;
				const newPositionV = {
					...drawingPositionV,
					relativeFrom: _univerjs_core.ObjectRelativeFromV.PARAGRAPH,
					posOffset: (prevPosOffset ?? 0) - delta
				};
				const updateDrawingAction = jsonX.replaceOp([
					"drawings",
					drawingId,
					"docTransform",
					"positionV"
				], drawingPositionV, newPositionV);
				if (updateDrawingAction) rawActions.push(updateDrawingAction);
			}
		}
		doMutation.params.actions = rawActions.reduce((acc, cur) => {
			return _univerjs_core.JSONX.compose(acc, cur);
		}, null);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};

//#endregion
//#region src/commands/commands/table/doc-table-create.command.ts
const CreateDocTableCommandId = "doc.command.create-table";
function buildDocTableInsertBody(params) {
	const { tableDataStream, tableParagraphs, sectionBreaks, tableId, textRun } = params;
	const existingParagraphIds = params.existingParagraphIds ?? new Set(tableParagraphs.map((paragraph) => paragraph.paragraphId));
	const dataStream = `${tableDataStream}${_univerjs_core.DataStreamTreeTokenType.PARAGRAPH}`;
	const tableEnd = tableDataStream.length;
	return {
		dataStream,
		paragraphs: [...tableParagraphs, {
			startIndex: tableEnd,
			paragraphId: (0, _univerjs_core.createParagraphId)(existingParagraphIds)
		}],
		sectionBreaks,
		textRuns: [{
			...textRun,
			st: 0,
			ed: tableEnd
		}],
		tables: [{
			startIndex: 0,
			endIndex: tableEnd,
			tableId
		}]
	};
}
function shouldCreateParagraphBeforeTable(body, startOffset) {
	return startOffset <= 0 || body.dataStream[startOffset - 1] !== _univerjs_core.DataStreamTreeTokenType.PARAGRAPH;
}
function normalizeTableInsertOffset(body, startOffset) {
	return startOffset === 0 && body.dataStream[0] === _univerjs_core.DataStreamTreeTokenType.PARAGRAPH ? 1 : startOffset;
}
function canInsertTableAtOffset(body, offset) {
	return !(isOffsetInTableRange(body.tables, offset) || isOffsetInIndexRange(body.blockRanges, offset) || isOffsetOnPointRange(body.customBlocks, offset));
}
function isOffsetInTableRange(ranges, offset) {
	return Boolean(ranges === null || ranges === void 0 ? void 0 : ranges.some((range) => (0, _univerjs_core.containsInteriorInsertionOffset)((0, _univerjs_core.getTableRangeInterval)(range), offset)));
}
function isOffsetInIndexRange(ranges, offset) {
	return Boolean(ranges === null || ranges === void 0 ? void 0 : ranges.some((range) => (0, _univerjs_core.containsInteriorInsertionOffset)((0, _univerjs_core.getBlockRangeInterval)(range), offset)));
}
function isOffsetOnPointRange(ranges, offset) {
	return Boolean(ranges === null || ranges === void 0 ? void 0 : ranges.some((range) => range.startIndex === offset));
}
/**
* The command to create a table at cursor point.
*/
const CreateDocTableCommand = {
	id: CreateDocTableCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		var _docDataModel$getSelf, _curGlyph$parent, _body$paragraphs;
		const { rowCount, colCount } = params;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const docMenuStyleService = accessor.get(DocMenuStyleService);
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null) return false;
		let contentInsertRange = null;
		try {
			contentInsertRange = accessor.get(_univerjs_docs.DocContentInsertService).consumeInsertRange(docDataModel.getUnitId());
		} catch {
			contentInsertRange = null;
		}
		const activeRange = docSelectionManagerService.getActiveTextRange();
		if (activeRange == null && contentInsertRange == null) return false;
		const segmentId = (contentInsertRange === null || contentInsertRange === void 0 ? void 0 : contentInsertRange.segmentId) ?? (activeRange === null || activeRange === void 0 ? void 0 : activeRange.segmentId) ?? "";
		const segmentPage = activeRange === null || activeRange === void 0 ? void 0 : activeRange.segmentPage;
		const body = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		if (body == null) return false;
		const unitId = docDataModel.getUnitId();
		const docSkeletonManagerService = getCommandSkeleton(accessor, unitId);
		const skeleton = docSkeletonManagerService === null || docSkeletonManagerService === void 0 ? void 0 : docSkeletonManagerService.getSkeleton();
		if (skeleton == null) return false;
		const startOffset = normalizeTableInsertOffset(body, (contentInsertRange === null || contentInsertRange === void 0 ? void 0 : contentInsertRange.startOffset) ?? activeRange.startOffset);
		if (!canInsertTableAtOffset(body, startOffset)) return false;
		const prevParagraph = (body.paragraphs ?? []).find((paragraph) => {
			return (0, _univerjs_core.getParagraphContentStartOffset)(body, paragraph) < startOffset && startOffset <= paragraph.startIndex;
		});
		const curGlyph = skeleton.findNodeByCharIndex(startOffset, segmentId, segmentPage);
		if (curGlyph == null) return false;
		const needCreateParagraph = shouldCreateParagraphBeforeTable(body, startOffset);
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const rawActions = [];
		const cursor = startOffset + (needCreateParagraph ? 4 : 3);
		const textRanges = [{
			startOffset: cursor,
			endOffset: cursor,
			collapsed: true
		}];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges
			}
		};
		if (startOffset > 0) textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: startOffset
		});
		if (needCreateParagraph) textX.push({
			t: _univerjs_core.TextXActionType.INSERT,
			body: {
				dataStream: _univerjs_core.DataStreamTreeTokenType.PARAGRAPH,
				paragraphs: (0, _univerjs_docs.generateParagraphs)(_univerjs_core.DataStreamTreeTokenType.PARAGRAPH, prevParagraph)
			},
			len: 1
		});
		const curTextRun = getTextRunAtPosition(body, startOffset, docMenuStyleService.getDefaultStyle(), docMenuStyleService.getStyleCache());
		const { dataStream: tableDataStream, paragraphs: tableParagraphs, sectionBreaks } = genEmptyTable(rowCount, colCount);
		const page = (_curGlyph$parent = curGlyph.parent) === null || _curGlyph$parent === void 0 || (_curGlyph$parent = _curGlyph$parent.parent) === null || _curGlyph$parent === void 0 || (_curGlyph$parent = _curGlyph$parent.parent) === null || _curGlyph$parent === void 0 || (_curGlyph$parent = _curGlyph$parent.parent) === null || _curGlyph$parent === void 0 ? void 0 : _curGlyph$parent.parent;
		if (page == null) return false;
		const { pageWidth, marginLeft, marginRight } = page;
		const tableSource = genTableSource(rowCount, colCount, pageWidth - marginLeft - marginRight);
		const tableInsertBody = buildDocTableInsertBody({
			tableDataStream,
			tableParagraphs,
			sectionBreaks,
			tableId: tableSource.tableId,
			textRun: curTextRun,
			existingParagraphIds: new Set((_body$paragraphs = body.paragraphs) === null || _body$paragraphs === void 0 ? void 0 : _body$paragraphs.map((paragraph) => paragraph.paragraphId))
		});
		textX.push({
			t: _univerjs_core.TextXActionType.INSERT,
			body: tableInsertBody,
			len: tableInsertBody.dataStream.length
		});
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		rawActions.push(jsonX.editOp(textX.serialize(), path));
		const insertTableSource = jsonX.insertOp(["tableSource", tableSource.tableId], tableSource);
		rawActions.push(insertTableSource);
		doMutation.params.actions = rawActions.reduce((acc, cur) => {
			return _univerjs_core.JSONX.compose(acc, cur);
		}, null);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};

//#endregion
//#region src/commands/commands/table/doc-table-delete.command.ts
const DocTableDeleteRowsCommand = {
	id: "doc.table.delete-rows",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		var _docDataModel$getSelf;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const activeRectRanges = docSelectionManagerService.getRectRanges();
		const rangeInfo = getRangeInfoFromRanges(docSelectionManagerService.getActiveTextRange(), activeRectRanges);
		if (rangeInfo == null) return false;
		const { segmentId } = rangeInfo;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const body = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		if (docDataModel == null || body == null) return false;
		const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
		if (docSkeletonManagerService == null) return false;
		const viewModel = docSkeletonManagerService.getViewModel();
		const unitId = docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getUnitId();
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const actionParams = getDeleteRowsActionsParams(rangeInfo, viewModel);
		if (actionParams == null) return false;
		const { offset, rowIndexes, len, tableId, cursor, selectWholeTable } = actionParams;
		if (selectWholeTable) return commandService.executeCommand(DocTableDeleteTableCommand.id);
		const rawActions = [];
		const textRanges = [{
			startOffset: cursor,
			endOffset: cursor,
			collapsed: true
		}];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges
			}
		};
		if (offset > 0) textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: offset
		});
		textX.push({
			t: _univerjs_core.TextXActionType.DELETE,
			len
		});
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		rawActions.push(jsonX.editOp(textX.serialize(), path));
		for (const index of rowIndexes.reverse()) {
			const action = jsonX.removeOp([
				"tableSource",
				tableId,
				"tableRows",
				index
			]);
			rawActions.push(action);
		}
		doMutation.params.actions = rawActions.reduce((acc, cur) => {
			return _univerjs_core.JSONX.compose(acc, cur);
		}, null);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
const DocTableDeleteColumnsCommand = {
	id: "doc.table.delete-columns",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		var _docDataModel$getSelf2;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const activeRectRanges = docSelectionManagerService.getRectRanges();
		const rangeInfo = getRangeInfoFromRanges(docSelectionManagerService.getActiveTextRange(), activeRectRanges);
		if (rangeInfo == null) return false;
		const { segmentId } = rangeInfo;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const body = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf2 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf2 === void 0 ? void 0 : _docDataModel$getSelf2.getBody();
		if (docDataModel == null || body == null) return false;
		const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
		if (docSkeletonManagerService == null) return false;
		const viewModel = docSkeletonManagerService.getViewModel();
		const unitId = docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getUnitId();
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const actionParams = getDeleteColumnsActionParams(rangeInfo, viewModel);
		if (actionParams == null) return false;
		const { offsets, columnIndexes, tableId, cursor, rowCount, selectWholeTable } = actionParams;
		if (selectWholeTable) return commandService.executeCommand(DocTableDeleteTableCommand.id);
		const rawActions = [];
		const textRanges = [{
			startOffset: cursor,
			endOffset: cursor,
			collapsed: true
		}];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges
			}
		};
		for (const offset of offsets) {
			const { retain, delete: deleteLen } = offset;
			if (retain > 0) textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len: retain
			});
			textX.push({
				t: _univerjs_core.TextXActionType.DELETE,
				len: deleteLen
			});
		}
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		rawActions.push(jsonX.editOp(textX.serialize(), path));
		columnIndexes.reverse();
		for (let i = 0; i < rowCount; i++) for (const index of columnIndexes) {
			const action = jsonX.removeOp([
				"tableSource",
				tableId,
				"tableRows",
				i,
				"tableCells",
				index
			]);
			rawActions.push(action);
		}
		for (const index of columnIndexes) {
			const action = jsonX.removeOp([
				"tableSource",
				tableId,
				"tableColumns",
				index
			]);
			rawActions.push(action);
		}
		doMutation.params.actions = rawActions.reduce((acc, cur) => {
			return _univerjs_core.JSONX.compose(acc, cur);
		}, null);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
const DocTableDeleteTableCommand = {
	id: "doc.table.delete-table",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		var _docDataModel$getSelf3;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const activeRectRanges = docSelectionManagerService.getRectRanges();
		const activeTextRange = docSelectionManagerService.getActiveTextRange();
		const rangeInfo = getRangeInfoFromRanges((params === null || params === void 0 ? void 0 : params.targetRange) ?? activeTextRange, (params === null || params === void 0 ? void 0 : params.targetRange) ? [] : activeRectRanges);
		if (rangeInfo == null) return false;
		const { segmentId } = rangeInfo;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const body = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf3 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf3 === void 0 ? void 0 : _docDataModel$getSelf3.getBody();
		if (docDataModel == null || body == null) return false;
		const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
		if (docSkeletonManagerService == null) return false;
		const viewModel = docSkeletonManagerService.getViewModel();
		const unitId = docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getUnitId();
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const actionParams = getDeleteTableActionParams(rangeInfo, viewModel);
		if (actionParams == null) return false;
		const { offset, len, tableId, cursor } = actionParams;
		const rawActions = [];
		const textRanges = [{
			startOffset: cursor,
			endOffset: cursor,
			collapsed: true
		}];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges
			}
		};
		if (offset > 0) textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: offset
		});
		textX.push({
			t: _univerjs_core.TextXActionType.DELETE,
			len
		});
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		rawActions.push(jsonX.editOp(textX.serialize(), path));
		const action = jsonX.removeOp(["tableSource", tableId]);
		rawActions.push(action);
		doMutation.params.actions = rawActions.reduce((acc, cur) => {
			return _univerjs_core.JSONX.compose(acc, cur);
		}, null);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};

//#endregion
//#region src/commands/commands/table/doc-table-insert.command.ts
const DocTableInsertRowCommandId = "doc.command.table-insert-row";
const DocTableInsertColumnCommandId = "doc.command.table-insert-column";
const DocTableInsertRowAboveCommandId = "doc.command.table-insert-row-above";
const DocTableInsertRowBellowCommandId = "doc.command.table-insert-row-bellow";
const DocTableInsertColumnLeftCommandId = "doc.command.table-insert-column-left";
const DocTableInsertColumnRightCommandId = "doc.command.table-insert-column-right";
const DocTableInsertRowAboveCommand = {
	id: DocTableInsertRowAboveCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).executeCommand(DocTableInsertRowCommandId, { position: 0 });
	}
};
const DocTableInsertRowBellowCommand = {
	id: DocTableInsertRowBellowCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).executeCommand(DocTableInsertRowCommandId, { position: 1 });
	}
};
const DocTableInsertColumnLeftCommand = {
	id: DocTableInsertColumnLeftCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).executeCommand(DocTableInsertColumnCommandId, { position: 0 });
	}
};
const DocTableInsertColumnRightCommand = {
	id: DocTableInsertColumnRightCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		return accessor.get(_univerjs_core.ICommandService).executeCommand(DocTableInsertColumnCommandId, { position: 1 });
	}
};
/**
* The command to insert table row.
*/
const DocTableInsertRowCommand = {
	id: DocTableInsertRowCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		var _docDataModel$getSelf;
		const { position } = params;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const activeRectRanges = docSelectionManagerService.getRectRanges();
		const rangeInfo = getRangeInfoFromRanges(docSelectionManagerService.getActiveTextRange(), activeRectRanges);
		if (rangeInfo == null) return false;
		const { segmentId } = rangeInfo;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const body = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		if (docDataModel == null || body == null) return false;
		const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
		if (docSkeletonManagerService == null) return false;
		const viewModel = docSkeletonManagerService.getViewModel();
		const unitId = docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getUnitId();
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const actionParams = getInsertRowActionsParams(rangeInfo, position, viewModel);
		if (actionParams == null) return false;
		const { offset, colCount, tableId, insertRowIndex } = actionParams;
		const rawActions = [];
		const cursor = offset + 2;
		const textRanges = [{
			startOffset: cursor,
			endOffset: cursor,
			collapsed: true
		}];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges
			}
		};
		if (offset > 0) textX.push({
			t: _univerjs_core.TextXActionType.RETAIN,
			len: offset
		});
		const insertBody = getInsertRowBody(colCount);
		textX.push({
			t: _univerjs_core.TextXActionType.INSERT,
			body: insertBody,
			len: insertBody.dataStream.length
		});
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		rawActions.push(jsonX.editOp(textX.serialize(), path));
		const insertRow = getEmptyTableRow(colCount);
		const insertTableSource = jsonX.insertOp([
			"tableSource",
			tableId,
			"tableRows",
			insertRowIndex
		], insertRow);
		rawActions.push(insertTableSource);
		doMutation.params.actions = rawActions.reduce((acc, cur) => {
			return _univerjs_core.JSONX.compose(acc, cur);
		}, null);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};
/**
* The command to insert table column.
*/
const DocTableInsertColumnCommand = {
	id: DocTableInsertColumnCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		var _docDataModel$getSelf2, _snapshot$tableSource, _documentStyle$pageSi;
		const { position } = params;
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const activeRectRanges = docSelectionManagerService.getRectRanges();
		const rangeInfo = getRangeInfoFromRanges(docSelectionManagerService.getActiveTextRange(), activeRectRanges);
		if (rangeInfo == null) return false;
		const { segmentId } = rangeInfo;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const body = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf2 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf2 === void 0 ? void 0 : _docDataModel$getSelf2.getBody();
		if (docDataModel == null || body == null) return false;
		const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
		if (docSkeletonManagerService == null) return false;
		const viewModel = docSkeletonManagerService.getViewModel();
		const unitId = docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getUnitId();
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		const actionParams = getInsertColumnActionsParams(rangeInfo, position, viewModel);
		if (actionParams == null) return false;
		const { offsets, columnIndex, tableId, rowCount } = actionParams;
		const rawActions = [];
		const cursor = offsets[0] + 1;
		const textRanges = [{
			startOffset: cursor,
			endOffset: cursor,
			collapsed: true
		}];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				actions: [],
				textRanges
			}
		};
		for (const offset of offsets) {
			textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len: offset
			});
			const insertBody = getInsertColumnBody();
			textX.push({
				t: _univerjs_core.TextXActionType.INSERT,
				body: insertBody,
				len: insertBody.dataStream.length
			});
		}
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		rawActions.push(jsonX.editOp(textX.serialize(), path));
		for (let i = 0; i < rowCount; i++) {
			const insertCell = getEmptyTableCell();
			const insertTableSource = jsonX.insertOp([
				"tableSource",
				tableId,
				"tableRows",
				i,
				"tableCells",
				columnIndex
			], insertCell);
			rawActions.push(insertTableSource);
		}
		const snapshot = docDataModel.getSnapshot();
		const documentStyle = snapshot.documentStyle;
		const { marginLeft = 0, marginRight = 0 } = documentStyle;
		const tableColumns = snapshot === null || snapshot === void 0 || (_snapshot$tableSource = snapshot.tableSource) === null || _snapshot$tableSource === void 0 || (_snapshot$tableSource = _snapshot$tableSource[tableId]) === null || _snapshot$tableSource === void 0 ? void 0 : _snapshot$tableSource.tableColumns;
		if (!tableColumns) return false;
		const pageWidth = (((_documentStyle$pageSi = documentStyle.pageSize) === null || _documentStyle$pageSi === void 0 ? void 0 : _documentStyle$pageSi.width) ?? 800) - marginLeft - marginRight;
		const tableWidth = tableColumns.reduce((sum, column) => sum + column.size.width.v, 0);
		const { newColWidth, widths } = getColumnWidths(tableWidth > 0 ? tableWidth : pageWidth, tableColumns, columnIndex);
		for (let i = 0; i < widths.length; i++) {
			const action = jsonX.replaceOp([
				"tableSource",
				tableId,
				"tableColumns",
				i,
				"size",
				"width",
				"v"
			], tableColumns[i].size.width.v, widths[i]);
			rawActions.push(action);
		}
		const insertCol = getTableColumn(newColWidth);
		const insertTableColumn = jsonX.insertOp([
			"tableSource",
			tableId,
			"tableColumns",
			columnIndex
		], insertCol);
		rawActions.push(insertTableColumn);
		doMutation.params.actions = rawActions.reduce((acc, cur) => {
			return _univerjs_core.JSONX.compose(acc, cur);
		}, null);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};

//#endregion
//#region src/commands/commands/table/doc-table-tab.command.ts
const DocTableTabCommand = {
	id: "doc.table.tab-in-table",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		const { shift } = params;
		const textSelectionManager = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const docRanges = textSelectionManager.getDocRanges();
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const docDataModel = accessor.get(_univerjs_core.IUniverInstanceService).getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!docDataModel) return false;
		const activeRange = docRanges.find((range) => range.isActive) ?? docRanges[0];
		const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
		const skeleton = docSkeletonManagerService === null || docSkeletonManagerService === void 0 ? void 0 : docSkeletonManagerService.getSkeleton();
		const viewModel = skeleton === null || skeleton === void 0 ? void 0 : skeleton.getViewModel().getSelfOrHeaderFooterViewModel(activeRange === null || activeRange === void 0 ? void 0 : activeRange.segmentId);
		if (viewModel == null) return false;
		if (activeRange == null) return false;
		let offsets = null;
		if (shift) offsets = getCellOffsets(viewModel, activeRange, 1);
		else offsets = getCellOffsets(viewModel, activeRange, 0);
		if (offsets) {
			const { startOffset, endOffset } = offsets;
			const textRanges = [{
				startOffset,
				endOffset
			}];
			textSelectionManager.replaceDocRanges(textRanges);
			return true;
		}
		if (shift === false) return await commandService.executeCommand(DocTableInsertRowCommand.id, { position: 1 });
		return true;
	}
};

//#endregion
//#region src/views/table/create/component-name.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const COMPONENT_DOC_CREATE_TABLE_CONFIRM = "COMPONENT_DOC_CREATE_TABLE_CONFIRM";

//#endregion
//#region src/commands/operations/doc-create-table.operation.ts
const COMPONENT_DOC_CREATE_TABLE_CONFIRM_ID = "doc.component.create-table-confirm";
const DocCreateTableOperation = {
	id: "doc.operation.create-table",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor) => {
		const localeService = accessor.get(_univerjs_core.LocaleService);
		const confirmService = accessor.get(_univerjs_core.IConfirmService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const tableCreateParams = {
			rowCount: 3,
			colCount: 5
		};
		const handleRowColChange = (rowCount, colCount) => {
			tableCreateParams.rowCount = rowCount;
			tableCreateParams.colCount = colCount;
		};
		await confirmService.open({
			id: COMPONENT_DOC_CREATE_TABLE_CONFIRM_ID,
			children: { label: {
				name: COMPONENT_DOC_CREATE_TABLE_CONFIRM,
				props: {
					handleRowColChange,
					tableCreateParams
				}
			} },
			width: "auto",
			title: { title: localeService.t("docs-ui.toolbar.table.insert") },
			onConfirm: () => {
				commandService.executeCommand(CreateDocTableCommand.id, tableCreateParams);
				confirmService.close(COMPONENT_DOC_CREATE_TABLE_CONFIRM_ID);
			},
			onClose: () => {
				confirmService.close(COMPONENT_DOC_CREATE_TABLE_CONFIRM_ID);
			}
		});
		return true;
	}
};

//#endregion
//#region src/commands/operations/doc-cursor.operation.ts
/**
* The operation to move cursor in the current document.
*/
const MoveCursorOperation = {
	id: "doc.operation.move-cursor",
	type: _univerjs_core.CommandType.OPERATION,
	handler: (accessor, params) => {
		if (!params) return false;
		return true;
	}
};
/**
* The operation to move selection in the current document.
*/
const MoveSelectionOperation = {
	id: "doc.operation.move-selection",
	type: _univerjs_core.CommandType.OPERATION,
	handler: (accessor, params) => {
		if (!params) return false;
		return true;
	}
};

//#endregion
//#region src/config/config.ts
const DOCS_UI_PLUGIN_CONFIG_KEY = "docs-ui.config";
const configSymbol = Symbol(DOCS_UI_PLUGIN_CONFIG_KEY);
const DEFAULT_DOC_FIT_TO_WIDTH_OPTIONS = {
	mode: "none",
	target: "viewport",
	paddingX: 20,
	minScale: 1,
	maxScale: void 0,
	align: "center"
};
const defaultPluginConfig = {
	toc: false,
	footer: true,
	wordCount: true,
	placeholder: true,
	fitToWidth: DEFAULT_DOC_FIT_TO_WIDTH_OPTIONS
};

//#endregion
//#region src/controllers/render-controllers/back-scroll.render-controller.ts
const ANCHOR_WIDTH = 1.5;
const MAX_SELECTION_SCROLL_RETRY_FRAMES = 6e3;
let DocBackScrollRenderController = class DocBackScrollRenderController extends _univerjs_core.RxDisposable {
	constructor(_context, _textSelectionManagerService, _editorService, _docSkeletonManagerService) {
		super();
		this._context = _context;
		this._textSelectionManagerService = _textSelectionManagerService;
		this._editorService = _editorService;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		_defineProperty(this, "_pendingSelectionScrollFrame", null);
		_defineProperty(this, "_suppressedSelection", null);
		_defineProperty(this, "_pendingExplicitRange", null);
		_defineProperty(this, "_selectionBeforeExplicitRange", null);
		_defineProperty(this, "_scrollingToRange", false);
		this._init();
	}
	_init() {
		const viewport = this._context.scene.getViewport("viewMain");
		if (viewport != null) {
			let scrollX = viewport.viewportScrollX;
			let scrollY = viewport.viewportScrollY;
			this.disposeWithMe(viewport.onScrollAfter$.subscribeEvent((event) => {
				const changed = event.viewportScrollX !== scrollX || event.viewportScrollY !== scrollY;
				scrollX = event.viewportScrollX;
				scrollY = event.viewportScrollY;
				if (changed && !this._scrollingToRange) this._suspendSelectionScroll();
			}));
		}
		this.disposeWithMe(this._context.scene.onPointerDown$.subscribeEvent(() => this._suspendSelectionScroll()));
		this._textSelectionManagerService.textSelection$.pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe((params) => {
			if (params == null) return;
			const { isEditing, unitId } = params;
			const range = params.textRanges.find((item) => item.isActive);
			if (unitId !== this._context.unitId) return;
			if (!isEditing) {
				if (this._ownsPendingExplicitRange(range)) return;
				this._suspendSelectionScroll();
				return;
			}
			if (this._context.unitId === _univerjs_core.DOCS_NORMAL_EDITOR_UNIT_ID_KEY) return;
			if (this._ownsPendingExplicitRange(range)) return;
			this._clearExplicitRangeOwnership();
			const suppressed = this._suppressedSelection;
			if (range != null && suppressed != null && range.startOffset === suppressed.startOffset && range.endOffset === suppressed.endOffset && (range.segmentId ?? "") === (suppressed.segmentId ?? "") && (range.segmentPage ?? -1) === (suppressed.segmentPage ?? -1)) return;
			this._suppressedSelection = null;
			this._scheduleScrollToSelection();
		});
	}
	dispose() {
		this._cancelPendingSelectionScroll();
		this._suppressedSelection = null;
		this._clearExplicitRangeOwnership();
		super.dispose();
	}
	_suspendSelectionScroll() {
		this._cancelPendingSelectionScroll();
		this._clearExplicitRangeOwnership();
		const range = this._textSelectionManagerService.getActiveTextRange();
		this._suppressedSelection = range == null ? null : {
			startOffset: range.startOffset,
			endOffset: range.endOffset,
			collapsed: range.collapsed,
			segmentId: range.segmentId,
			segmentPage: range.segmentPage
		};
	}
	_cancelPendingSelectionScroll() {
		if (this._pendingSelectionScrollFrame != null && typeof cancelAnimationFrame !== "undefined") cancelAnimationFrame(this._pendingSelectionScrollFrame);
		this._pendingSelectionScrollFrame = null;
	}
	_scheduleScrollToSelection() {
		if (typeof requestAnimationFrame === "undefined") {
			this._scrollToSelection();
			return;
		}
		if (this._pendingSelectionScrollFrame != null) cancelAnimationFrame(this._pendingSelectionScrollFrame);
		this._pendingSelectionScrollFrame = requestAnimationFrame(() => {
			this._pendingSelectionScrollFrame = null;
			this._scrollToSelection();
		});
	}
	scrollToRange(range, onResolved) {
		this._cancelPendingSelectionScroll();
		if (this._pendingExplicitRange == null) this._selectionBeforeExplicitRange = this._textSelectionManagerService.getActiveTextRange();
		this._pendingExplicitRange = range;
		this._scrollingToRange = true;
		try {
			this._scrollToRange(range, onResolved);
		} finally {
			this._scrollingToRange = false;
			if (this._pendingSelectionScrollFrame == null) this._clearExplicitRangeOwnership();
		}
	}
	_isSameRange(left, right) {
		return left != null && right != null && left.startOffset === right.startOffset && left.endOffset === right.endOffset && (left.segmentId ?? "") === (right.segmentId ?? "") && (left.segmentPage ?? -1) === (right.segmentPage ?? -1);
	}
	_ownsPendingExplicitRange(range) {
		return this._pendingExplicitRange != null && (range == null || this._isSameRange(range, this._pendingExplicitRange) || this._isSameRange(range, this._selectionBeforeExplicitRange));
	}
	_clearExplicitRangeOwnership() {
		this._pendingExplicitRange = null;
		this._selectionBeforeExplicitRange = null;
	}
	_scrollToRange(range, onResolved) {
		var _skeleton$getSkeleton;
		const skeleton = this._docSkeletonManagerService.getSkeleton();
		if (!skeleton) return;
		if (!this.isViewportReady()) {
			this._scheduleScrollToReadyRange(range, 0, onResolved);
			return;
		}
		const { startOffset, segmentId, segmentPage } = range;
		const anchorNodePosition = skeleton.findNodePositionByCharIndex(startOffset, true, segmentId ?? "", segmentPage ?? -1);
		if (anchorNodePosition == null) {
			this._deferUnresolvedRange(skeleton, range, onResolved);
			return;
		}
		const pages = (_skeleton$getSkeleton = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton === void 0 ? void 0 : _skeleton$getSkeleton.pages;
		const rootPageIndex = anchorNodePosition.pageType === _univerjs_engine_render.DocumentSkeletonPageType.HEADER || anchorNodePosition.pageType === _univerjs_engine_render.DocumentSkeletonPageType.FOOTER || anchorNodePosition.pageType === _univerjs_engine_render.DocumentSkeletonPageType.CELL && (pages === null || pages === void 0 ? void 0 : pages[anchorNodePosition.page]) == null ? anchorNodePosition.segmentPage : anchorNodePosition.page;
		const rootPage = pages === null || pages === void 0 ? void 0 : pages[rootPageIndex];
		if (rootPage == null || rootPage.isLayoutPlaceholder) return;
		this.scrollToNode(anchorNodePosition);
		onResolved === null || onResolved === void 0 || onResolved();
	}
	_deferUnresolvedRange(skeleton, range, onResolved) {
		var _skeleton$getSkeleton2;
		const pageIndex = skeleton.findBodyPageIndexByCharIndex(range.startOffset);
		const page = (_skeleton$getSkeleton2 = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton2 === void 0 ? void 0 : _skeleton$getSkeleton2.pages[pageIndex];
		if (page === null || page === void 0 ? void 0 : page.isMaterializationPlaceholder) {
			this._scrollToPage(pageIndex);
			this._scheduleScrollToReadyRange(range, 0, onResolved);
		} else if (pageIndex < 0 && this._isValidBodyOffset(skeleton, range.startOffset)) this._scheduleScrollToReadyRange(range, 0, onResolved);
	}
	_scheduleScrollToReadyRange(range, attempt = 0, onResolved) {
		if (typeof requestAnimationFrame === "undefined" || attempt >= MAX_SELECTION_SCROLL_RETRY_FRAMES) return;
		if (this._pendingSelectionScrollFrame != null) cancelAnimationFrame(this._pendingSelectionScrollFrame);
		this._pendingSelectionScrollFrame = requestAnimationFrame(() => {
			this._pendingSelectionScrollFrame = null;
			const skeleton = this._docSkeletonManagerService.getSkeleton();
			const pageIndex = skeleton.findBodyPageIndexByCharIndex(range.startOffset);
			if (!this.isViewportReady() || pageIndex < 0 && this._isValidBodyOffset(skeleton, range.startOffset)) {
				this._scheduleScrollToReadyRange(range, attempt + 1, onResolved);
				return;
			}
			this.scrollToRange(range, onResolved);
		});
	}
	_isValidBodyOffset(skeleton, offset) {
		var _skeleton$getViewMode;
		const length = ((_skeleton$getViewMode = skeleton.getViewModel().getDataModel().getBody()) === null || _skeleton$getViewMode === void 0 ? void 0 : _skeleton$getViewMode.dataStream.length) ?? 0;
		return offset >= 0 && offset < length;
	}
	isViewportReady() {
		const { mainComponent, scene } = this._context;
		if (!(mainComponent instanceof _univerjs_engine_render.Documents)) return true;
		const { docsLeft, docsTop } = mainComponent.getOffsetConfig();
		if (docsLeft <= -1e4 || docsTop <= -1e4) return false;
		const viewport = scene.getViewport("viewMain");
		if (viewport == null) return true;
		const viewBound = viewport.calcViewportInfo().viewBound;
		return viewBound.right - viewBound.left > 1 && viewBound.bottom - viewBound.top > 1;
	}
	_scrollToPage(pageIndex) {
		var _skeleton$getSkeleton3;
		const { mainComponent, scene } = this._context;
		const pages = (_skeleton$getSkeleton3 = this._docSkeletonManagerService.getSkeleton().getSkeletonData()) === null || _skeleton$getSkeleton3 === void 0 ? void 0 : _skeleton$getSkeleton3.pages;
		if (!(mainComponent instanceof _univerjs_engine_render.Documents) || pages == null || pages[pageIndex] == null) return;
		const viewportMain = scene.getViewport("viewMain");
		if (viewportMain == null) return;
		const { docsLeft, docsTop, pageLayoutType = _univerjs_engine_render.PageLayoutType.VERTICAL, pageMarginLeft, pageMarginTop } = mainComponent.getOffsetConfig();
		let pageLeft = docsLeft;
		let pageTop = docsTop;
		for (let index = 0; index < pageIndex; index++) if (pageLayoutType === _univerjs_engine_render.PageLayoutType.HORIZONTAL) pageLeft += pages[index].pageWidth + pageMarginLeft;
		else pageTop += pages[index].pageHeight + pageMarginTop;
		const viewBound = viewportMain.calcViewportInfo().viewBound;
		const offsetX = pageLayoutType === _univerjs_engine_render.PageLayoutType.HORIZONTAL ? pageLeft - viewBound.left : 0;
		const offsetY = pageLayoutType === _univerjs_engine_render.PageLayoutType.HORIZONTAL ? 0 : pageTop - viewBound.top;
		viewportMain.scrollByBarDeltaValue(viewportMain.transViewportScroll2ScrollValue(offsetX, offsetY));
	}
	scrollToNode(startNodePosition) {
		const { unitId, scene, mainComponent } = this._context;
		const skeleton = this._docSkeletonManagerService.getSkeleton();
		if (!(mainComponent instanceof _univerjs_engine_render.Documents) || skeleton == null) return;
		const documentOffsetConfig = mainComponent.getOffsetConfig();
		const { docsLeft, docsTop } = documentOffsetConfig;
		const { contentBoxPointGroup } = new NodePositionConvertToCursor(documentOffsetConfig, skeleton).getRangePointData(startNodePosition, startNodePosition);
		if (contentBoxPointGroup.length === 0) return;
		const { left: aLeft, top: aTop, height } = getAnchorBounding(contentBoxPointGroup);
		const left = aLeft + docsLeft;
		const top = aTop + docsTop;
		const viewportMain = scene.getViewport("viewMain");
		if (viewportMain == null) return;
		const { left: boundLeft, top: boundTop, right: boundRight, bottom: boundBottom } = viewportMain.calcViewportInfo().viewBound;
		let offsetY = 0;
		let offsetX = 0;
		const editorRenderConfig = this._editorService.getEditorRenderConfig(unitId);
		const delta = editorRenderConfig ? editorRenderConfig.backScrollOffset ?? 0 : 100;
		if (top < boundTop) offsetY = top - boundTop - delta;
		else if (top > boundBottom - height) offsetY = top - boundBottom + height + delta;
		if (left < boundLeft) offsetX = left - boundLeft;
		else if (left > boundRight - ANCHOR_WIDTH) offsetX = left - boundRight + ANCHOR_WIDTH;
		const config = viewportMain.transViewportScroll2ScrollValue(offsetX, offsetY);
		viewportMain.scrollByBarDeltaValue(config);
	}
	_scrollToSelection() {
		const activeTextRange = this._textSelectionManagerService.getActiveTextRange();
		if (activeTextRange == null) return;
		const { collapsed } = activeTextRange;
		if (!collapsed) return;
		this.scrollToRange(activeTextRange);
	}
};
DocBackScrollRenderController = __decorate([
	__decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(2, IEditorService),
	__decorateParam(3, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService))
], DocBackScrollRenderController);

//#endregion
//#region src/controllers/render-controllers/doc-paragraph-placeholder.render-controller.ts
const PLACEHOLDER_COLOR = "gray.400";
const DEFAULT_PLACEHOLDER_FONT_SIZE = 12;
const MIN_BODY_TEXT_PLACEHOLDER_FONT_SIZE = 16;
const DEFAULT_PLACEHOLDER_FONT_FAMILY = "Arial";
const LIST_PLACEHOLDER_GAP = 4;
let DocParagraphPlaceholderRenderController = class DocParagraphPlaceholderRenderController extends _univerjs_core.Disposable {
	constructor(_context, _localeService, _docSelectionManagerService, _configService) {
		super();
		this._context = _context;
		this._localeService = _localeService;
		this._docSelectionManagerService = _docSelectionManagerService;
		this._configService = _configService;
		this._initParagraphPlaceholderRender();
	}
	_initParagraphPlaceholderRender() {
		const config = this._configService.getConfig(DOCS_UI_PLUGIN_CONFIG_KEY);
		if (!shouldRenderParagraphPlaceholder(this._context.unit, this._context.unitId, config)) return;
		const documents = this._context.mainComponent;
		if (!documents) return;
		this.disposeWithMe(documents.pageRender$.subscribe((pageRenderConfig) => this._drawPagePlaceholders(pageRenderConfig)));
		this.disposeWithMe(this._docSelectionManagerService.textSelection$.subscribe(({ unitId }) => {
			if (unitId === this._context.unitId) documents.makeDirty(true);
		}));
	}
	_drawPagePlaceholders({ page, pageLeft, pageTop, ctx }) {
		var _this$_context$unit$g, _this$_context$mainCo;
		const body = (_this$_context$unit$g = this._context.unit.getSelfOrHeaderFooterModel(page.segmentId)) === null || _this$_context$unit$g === void 0 ? void 0 : _this$_context$unit$g.getBody();
		if (!body) return;
		const activeRange = this._docSelectionManagerService.getActiveTextRange();
		if (!activeRange || (activeRange.segmentId ?? "") !== (page.segmentId ?? "")) return;
		if (activeRange.startOffset == null || activeRange.endOffset == null || activeRange.startOffset !== activeRange.endOffset) return;
		const placeholders = getParagraphPlaceholderLayouts(page, body, this._getLocale(), pageLeft, pageTop, activeRange.startOffset, {
			docsLeft: (_this$_context$mainCo = this._context.mainComponent) === null || _this$_context$mainCo === void 0 ? void 0 : _this$_context$mainCo.getOffsetConfig().docsLeft,
			unitId: this._context.unitId
		});
		if (!placeholders.length) return;
		drawParagraphPlaceholders(ctx, placeholders);
	}
	_getLocale() {
		return {
			heading1: this._localeService.t("docs-ui.placeholder.heading1"),
			heading2: this._localeService.t("docs-ui.placeholder.heading2"),
			heading3: this._localeService.t("docs-ui.placeholder.heading3"),
			heading4: this._localeService.t("docs-ui.placeholder.heading4"),
			heading5: this._localeService.t("docs-ui.placeholder.heading5"),
			listItem: this._localeService.t("docs-ui.placeholder.listItem"),
			normalText: this._localeService.t("docs-ui.placeholder.normalText")
		};
	}
};
DocParagraphPlaceholderRenderController = __decorate([
	__decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_core.LocaleService)),
	__decorateParam(2, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(3, _univerjs_core.IConfigService)
], DocParagraphPlaceholderRenderController);
function shouldRenderParagraphPlaceholder(documentModel, unitId, config) {
	var _documentModel$getSna;
	if ((config === null || config === void 0 ? void 0 : config.placeholder) === false) return false;
	if ((0, _univerjs_core.isInternalEditorID)(unitId)) return false;
	return ((_documentModel$getSna = documentModel.getSnapshot().documentStyle) === null || _documentModel$getSna === void 0 ? void 0 : _documentModel$getSna.documentFlavor) === _univerjs_core.DocumentFlavor.MODERN;
}
function getParagraphPlaceholderLayouts(page, body, locale, pageLeft = 0, pageTop = 0, activeOffset, options) {
	var _page$skeTables;
	const paragraphs = new Map((body.paragraphs ?? []).map((paragraph) => [paragraph.startIndex, paragraph]));
	const layouts = [];
	const visitSection = (section, originLeft, originTop, clip) => {
		for (const column of section.columns) visitColumn(column, body, paragraphs, locale, layouts, originLeft + column.left, originTop + section.top, activeOffset, clip);
	};
	for (const section of page.sections) visitSection(section, pageLeft + page.marginLeft, pageTop + page.marginTop);
	(_page$skeTables = page.skeTables) === null || _page$skeTables === void 0 || _page$skeTables.forEach((table, tableId) => {
		const sourceTableId = (0, _univerjs_engine_render.getTableIdAndSliceIndex)(table.tableId ?? tableId).tableId;
		const viewport = (0, _univerjs_engine_render.getDocsTableRenderViewport)((options === null || options === void 0 ? void 0 : options.unitId) ?? "", sourceTableId);
		const hasHorizontalViewport = viewport != null && (viewport.leadingInsetLeft ?? 0) + viewport.contentWidth + (viewport.trailingInsetRight ?? 0) > viewport.viewportWidth;
		const tableLeft = pageLeft + page.marginLeft + table.left;
		const tableViewportLeft = hasHorizontalViewport ? (0, _univerjs_engine_render.getDocsTableViewportLeft)(viewport, tableLeft - (viewport.leadingInsetLeft ?? 0), options === null || options === void 0 ? void 0 : options.docsLeft) : tableLeft;
		const tableViewportRight = tableViewportLeft + (hasHorizontalViewport ? viewport.viewportWidth : table.width);
		const tableScrollLeft = hasHorizontalViewport ? viewport.scrollLeft : 0;
		for (const row of table.rows) for (const cell of row.cells) {
			const cellLeft = tableLeft + cell.left - tableScrollLeft;
			const cellContentLeft = cellLeft + cell.marginLeft;
			const cellContentRight = cellLeft + cell.pageWidth - cell.marginRight;
			const clipLeft = Math.max(cellContentLeft, tableViewportLeft);
			const clipRight = Math.min(cellContentRight, tableViewportRight);
			if (clipRight <= clipLeft) continue;
			for (const section of cell.sections) visitSection(section, cellContentLeft, pageTop + page.marginTop + table.top + row.top + cell.marginTop, {
				left: clipLeft,
				right: clipRight
			});
		}
	});
	return layouts;
}
function visitColumn(column, body, paragraphs, locale, layouts, originLeft, originTop, activeOffset, clip) {
	for (const line of column.lines) {
		if (!line.paragraphStart) continue;
		const paragraphStart = line.st;
		const paragraphEnd = line.paragraphIndex;
		if (!isOffsetInParagraph(activeOffset, paragraphStart, paragraphEnd)) continue;
		const paragraph = paragraphs.get(paragraphEnd);
		if (!paragraph || !isEmptyParagraph(body.dataStream, line.st, line.paragraphIndex)) continue;
		const text = getPlaceholderText(paragraph, locale);
		if (!text) continue;
		layouts.push(getLinePlaceholderLayout(line, paragraph, text, originLeft, originTop, clip));
	}
}
function isOffsetInParagraph(offset, paragraphStart, paragraphEnd) {
	return paragraphStart <= offset && offset <= paragraphEnd;
}
function getPlaceholderText(paragraph, locale) {
	var _paragraph$paragraphS;
	if (paragraph.bullet) return locale.listItem;
	switch ((_paragraph$paragraphS = paragraph.paragraphStyle) === null || _paragraph$paragraphS === void 0 ? void 0 : _paragraph$paragraphS.namedStyleType) {
		case _univerjs_core.NamedStyleType.HEADING_1: return locale.heading1;
		case _univerjs_core.NamedStyleType.HEADING_2: return locale.heading2;
		case _univerjs_core.NamedStyleType.HEADING_3: return locale.heading3;
		case _univerjs_core.NamedStyleType.HEADING_4: return locale.heading4;
		case _univerjs_core.NamedStyleType.HEADING_5: return locale.heading5;
		default: return locale.normalText;
	}
}
function getLinePlaceholderLayout(line, paragraph, text, originLeft, originTop, clip) {
	var _paragraph$paragraphS2;
	const divide = line.divides[0];
	const firstGlyph = ((divide === null || divide === void 0 ? void 0 : divide.glyphGroup) ?? [])[0];
	const textStyle = _univerjs_core.NAMED_STYLE_MAP[((_paragraph$paragraphS2 = paragraph.paragraphStyle) === null || _paragraph$paragraphS2 === void 0 ? void 0 : _paragraph$paragraphS2.namedStyleType) ?? _univerjs_core.NamedStyleType.NORMAL_TEXT];
	const fontSize = getPlaceholderFontSize(line, paragraph);
	const fontFamily = getLineFontFamily(line) ?? DEFAULT_PLACEHOLDER_FONT_FAMILY;
	const fontWeight = (textStyle === null || textStyle === void 0 ? void 0 : textStyle.bl) ? "bold" : "normal";
	const lineStartX = originLeft + ((divide === null || divide === void 0 ? void 0 : divide.left) ?? 0) + ((divide === null || divide === void 0 ? void 0 : divide.paddingLeft) ?? 0);
	const listOffset = paragraph.bullet && firstGlyph ? firstGlyph.left + firstGlyph.width + LIST_PLACEHOLDER_GAP : 0;
	const x = lineStartX + listOffset;
	const naturalMaxWidth = Math.max(0, ((divide === null || divide === void 0 ? void 0 : divide.width) ?? line.width ?? 0) - listOffset);
	const clipLeft = Math.max(x, (clip === null || clip === void 0 ? void 0 : clip.left) ?? x);
	const clipRight = Math.min(x + naturalMaxWidth, (clip === null || clip === void 0 ? void 0 : clip.right) ?? x + naturalMaxWidth);
	const clipTop = originTop + line.top;
	return {
		clipBottom: clipTop + line.lineHeight,
		clipLeft,
		clipRight,
		clipTop,
		fontFamily,
		fontSize,
		fontWeight,
		maxWidth: Math.max(0, clipRight - clipLeft),
		text,
		x,
		y: originTop + line.top + line.marginTop + line.paddingTop + line.asc
	};
}
function isEmptyParagraph(dataStream, paragraphStart, paragraphEnd) {
	return stripNonTextTokens((dataStream === null || dataStream === void 0 ? void 0 : dataStream.slice(paragraphStart, paragraphEnd)) ?? "").trim() === "";
}
function stripNonTextTokens(text) {
	return text.replaceAll(_univerjs_core.DataStreamTreeTokenType.PARAGRAPH, "").replaceAll(_univerjs_core.DataStreamTreeTokenType.SECTION_BREAK, "").replaceAll(_univerjs_core.DataStreamTreeTokenType.DOCS_END, "");
}
function getLineFontSize(line) {
	for (const divide of line.divides) for (const glyph of divide.glyphGroup) {
		var _glyph$fontStyle, _glyph$fontStyle2, _glyph$ts;
		const fontSize = ((_glyph$fontStyle = glyph.fontStyle) === null || _glyph$fontStyle === void 0 ? void 0 : _glyph$fontStyle.originFontSize) ?? ((_glyph$fontStyle2 = glyph.fontStyle) === null || _glyph$fontStyle2 === void 0 ? void 0 : _glyph$fontStyle2.fontSize) ?? ((_glyph$ts = glyph.ts) === null || _glyph$ts === void 0 ? void 0 : _glyph$ts.fs);
		if (fontSize) return fontSize;
	}
	return null;
}
function getPlaceholderFontSize(line, paragraph) {
	var _paragraph$paragraphS3;
	const namedStyleType = ((_paragraph$paragraphS3 = paragraph.paragraphStyle) === null || _paragraph$paragraphS3 === void 0 ? void 0 : _paragraph$paragraphS3.namedStyleType) ?? _univerjs_core.NamedStyleType.NORMAL_TEXT;
	const textStyle = _univerjs_core.NAMED_STYLE_MAP[namedStyleType];
	const fontSize = getLineFontSize(line) ?? (textStyle === null || textStyle === void 0 ? void 0 : textStyle.fs) ?? DEFAULT_PLACEHOLDER_FONT_SIZE;
	if (namedStyleType !== _univerjs_core.NamedStyleType.NORMAL_TEXT) return fontSize;
	return Math.max(fontSize, MIN_BODY_TEXT_PLACEHOLDER_FONT_SIZE);
}
function getLineFontFamily(line) {
	for (const divide of line.divides) for (const glyph of divide.glyphGroup) {
		var _glyph$fontStyle3, _glyph$ts2;
		const fontFamily = ((_glyph$fontStyle3 = glyph.fontStyle) === null || _glyph$fontStyle3 === void 0 ? void 0 : _glyph$fontStyle3.fontFamily) ?? ((_glyph$ts2 = glyph.ts) === null || _glyph$ts2 === void 0 ? void 0 : _glyph$ts2.ff);
		if (fontFamily) return fontFamily;
	}
	return null;
}
function drawParagraphPlaceholders(ctx, placeholders) {
	ctx.save();
	ctx.fillStyle = PLACEHOLDER_COLOR;
	ctx.textBaseline = "alphabetic";
	for (const placeholder of placeholders) {
		const clipLeft = placeholder.clipLeft ?? placeholder.x;
		const clipRight = placeholder.clipRight ?? placeholder.x + placeholder.maxWidth;
		const clipTop = placeholder.clipTop ?? placeholder.y - placeholder.fontSize;
		const clipBottom = placeholder.clipBottom ?? placeholder.y + placeholder.fontSize;
		if (clipRight <= clipLeft || clipBottom <= clipTop) continue;
		ctx.save();
		ctx.beginPath();
		ctx.rectByPrecision(clipLeft, clipTop, clipRight - clipLeft, clipBottom - clipTop);
		ctx.closePath();
		ctx.clip();
		ctx.font = `${placeholder.fontWeight} ${placeholder.fontSize}px ${placeholder.fontFamily}`;
		ctx.fillText(placeholder.text, placeholder.x, placeholder.y);
		ctx.restore();
	}
	ctx.restore();
}

//#endregion
//#region src/services/doc-layout-coordinator.service.ts
const FOREGROUND_BUDGET_MS = 8;
const WORKER_FOREGROUND_BUDGET_MS = 32;
const WORKER_BACKGROUND_BUDGET_MS = 128;
const BACKGROUND_BUDGET_MS = 12;
const BACKGROUND_YIELD_MS = 4;
const BACKGROUND_RESUME_DELAY_MS = 4;
const EDIT_BACKGROUND_RESUME_DELAY_MS = 120;
const MIN_EDIT_FOREGROUND_WINDOW_SIZE = 3;
const MAX_EDIT_FOREGROUND_WINDOW_SIZE = 5;
const EDIT_FOREGROUND_WINDOW_SIZE = MAX_EDIT_FOREGROUND_WINDOW_SIZE;
const EDIT_FOREGROUND_BUDGET_MS = 28;
const MAX_SYNCHRONOUS_EDIT_STEPS = 4;
let docLayoutMountSequence = 0;
function hasInputPendingScheduling(value) {
	if (!("scheduling" in value)) return false;
	const scheduling = value.scheduling;
	return typeof scheduling === "object" && scheduling != null && "isInputPending" in scheduling && typeof scheduling.isInputPending === "function";
}
function hasPendingUserInput() {
	if (typeof navigator === "undefined") return false;
	return hasInputPendingScheduling(navigator) && navigator.scheduling.isInputPending({ includeContinuous: true });
}
function getBackgroundResumeDelay(reason) {
	return reason === "edit" ? EDIT_BACKGROUND_RESUME_DELAY_MS : BACKGROUND_RESUME_DELAY_MS;
}
function normalizeEditForegroundWindowSize(size) {
	return Math.min(MAX_EDIT_FOREGROUND_WINDOW_SIZE, Math.max(MIN_EDIT_FOREGROUND_WINDOW_SIZE, Math.floor(size ?? EDIT_FOREGROUND_WINDOW_SIZE)));
}
function scheduleAnimationFrame(callback) {
	if (typeof requestAnimationFrame === "function") {
		const frameId = requestAnimationFrame(callback);
		return () => cancelAnimationFrame(frameId);
	}
	const timerId = setTimeout(callback, 0);
	return () => clearTimeout(timerId);
}
/**
* Cooperative presentation scheduler for interactive document layout.
*
* The coordinator owns only timing and cancellation. The selected executor owns
* layout state; both executors publish the same generation/step contract.
*/
var DocLayoutCoordinatorService = class extends _univerjs_core.Disposable {
	constructor(_scheduleVisualFrame = scheduleAnimationFrame) {
		super();
		this._scheduleVisualFrame = _scheduleVisualFrame;
		_defineProperty(this, "_mountId", `docs-layout-mount-${++docLayoutMountSequence}`);
		_defineProperty(this, "_mountEpoch", 0);
		_defineProperty(this, "_viewportEpoch", 0);
		_defineProperty(this, "_workerMount", null);
		_defineProperty(this, "_scheduledLayout", null);
		_defineProperty(this, "_animationFrameId", null);
		_defineProperty(this, "_idleCallbackId", null);
		_defineProperty(this, "_fallbackTimerId", null);
		_defineProperty(this, "_pendingWorkerPresentation", null);
		_defineProperty(this, "_workerPresentationPaused", false);
	}
	schedule(skeleton, options, callbacks) {
		this.cancel();
		const generation = skeleton.startIncrementalLayout({
			...options,
			waitForHyphenationPatterns: true
		});
		const backgroundResumeDelayMs = getBackgroundResumeDelay(options.reason);
		this._scheduledLayout = {
			executor: "main-thread",
			skeleton,
			generation,
			callbacks,
			backgroundResumeDelayMs,
			nextBackgroundDelayMs: backgroundResumeDelayMs,
			anchorReady: false,
			foregroundStarted: false,
			foregroundBudgetMs: options.foregroundBudgetMs ?? EDIT_FOREGROUND_BUDGET_MS,
			foregroundElapsedMs: 0,
			foregroundWindowSize: normalizeEditForegroundWindowSize(options.foregroundWindowSize),
			foregroundTargetProgressCount: null,
			foregroundEndOffset: options.foregroundEndOffset ?? null,
			waitForAnimationFrame: options.reason !== "edit" || options.deferForeground === true
		};
		if (options.reason === "edit" && options.deferForeground !== true) {
			this._scheduledLayout.foregroundStarted = true;
			let synchronousStepCount = 0;
			while (this._scheduledLayout != null && !this._scheduledLayout.anchorReady && synchronousStepCount < MAX_SYNCHRONOUS_EDIT_STEPS) {
				this._runSlice(0, false, false);
				synchronousStepCount++;
			}
			const foregroundLayout = this._scheduledLayout;
			if ((foregroundLayout === null || foregroundLayout === void 0 ? void 0 : foregroundLayout.executor) === "main-thread" && foregroundLayout.anchorReady && foregroundLayout.foregroundEndOffset != null && foregroundLayout.foregroundElapsedMs < foregroundLayout.foregroundBudgetMs) this._runSlice(Math.min(FOREGROUND_BUDGET_MS, foregroundLayout.foregroundBudgetMs - foregroundLayout.foregroundElapsedMs), false, false);
			const scheduledLayout = this._scheduledLayout;
			if (scheduledLayout != null) {
				if (scheduledLayout.executor === "main-thread" && (!scheduledLayout.anchorReady || scheduledLayout.callbacks.onForegroundReady != null)) {
					scheduledLayout.foregroundStarted = false;
					scheduledLayout.waitForAnimationFrame = true;
				}
				if (!scheduledLayout.anchorReady || scheduledLayout.callbacks.onForegroundReady != null) this._scheduleForeground();
				else this._scheduleBackground();
			}
		} else this._scheduleForeground();
		return generation;
	}
	scheduleWorker(unitId, skeleton, executorService, options, protectedRange, callbacks, onError, preserveCompletedLayout = false) {
		this.cancel();
		const previousMount = this._workerMount;
		if (previousMount != null && (previousMount.unitId !== unitId || previousMount.executorService !== executorService)) previousMount.executorService.disposeLayoutMount({
			unitId: previousMount.unitId,
			mountId: this._mountId,
			mountEpoch: previousMount.mountEpoch,
			viewportEpoch: previousMount.viewportEpoch
		}).catch(onError);
		if (previousMount == null || previousMount.unitId !== unitId || previousMount.executorService !== executorService) this._mountEpoch++;
		const viewportEpoch = ++this._viewportEpoch;
		this._workerMount = {
			unitId,
			executorService,
			onError,
			mountEpoch: this._mountEpoch,
			viewportEpoch
		};
		if (!preserveCompletedLayout) skeleton.beginExternalLayout({
			reason: options.reason,
			protectedRange
		});
		const backgroundResumeDelayMs = getBackgroundResumeDelay(options.reason);
		this._scheduledLayout = {
			executor: "worker",
			unitId,
			mountId: this._mountId,
			mountEpoch: this._mountEpoch,
			viewportEpoch,
			skeleton,
			executorService,
			preserveCompletedLayout,
			options,
			generation: null,
			modelRevision: null,
			metricsRevision: null,
			callbacks,
			onError,
			backgroundResumeDelayMs,
			nextBackgroundDelayMs: backgroundResumeDelayMs,
			anchorReady: false
		};
		this._runSlice(WORKER_FOREGROUND_BUDGET_MS, false);
	}
	async getWorkerPage(pageIndex) {
		const workerMount = this._workerMount;
		if (workerMount == null) return null;
		const result = await workerMount.executorService.getLayoutPage({
			unitId: workerMount.unitId,
			mountId: this._mountId,
			mountEpoch: workerMount.mountEpoch,
			viewportEpoch: workerMount.viewportEpoch,
			pageIndex
		});
		if (this._workerMount !== workerMount || result.unitId !== workerMount.unitId || result.mountId !== this._mountId || result.mountEpoch !== workerMount.mountEpoch || result.viewportEpoch !== workerMount.viewportEpoch) return null;
		return result;
	}
	deferBackgroundWork() {
		const scheduledLayout = this._scheduledLayout;
		if (scheduledLayout == null || !scheduledLayout.anchorReady) return;
		scheduledLayout.nextBackgroundDelayMs = scheduledLayout.backgroundResumeDelayMs;
		if (scheduledLayout.executor === "worker") return;
		this._cancelCallbacks();
		this._scheduleBackground();
	}
	setWorkerPresentationPaused(paused) {
		if (this._workerPresentationPaused === paused) return;
		this._workerPresentationPaused = paused;
		if (!paused) {
			var _this$_pendingWorkerP;
			(_this$_pendingWorkerP = this._pendingWorkerPresentation) === null || _this$_pendingWorkerP === void 0 || _this$_pendingWorkerP.resume();
		}
	}
	hasScheduledLayout() {
		return this._scheduledLayout != null;
	}
	cancel() {
		const scheduledLayout = this._scheduledLayout;
		if (scheduledLayout != null) {
			if (scheduledLayout.executor === "main-thread") scheduledLayout.skeleton.cancelIncrementalLayout(scheduledLayout.generation);
			else if (scheduledLayout.generation != null) scheduledLayout.executorService.cancelLayout({
				unitId: scheduledLayout.unitId,
				mountId: scheduledLayout.mountId,
				mountEpoch: scheduledLayout.mountEpoch,
				viewportEpoch: scheduledLayout.viewportEpoch,
				generation: scheduledLayout.generation
			}).catch(scheduledLayout.onError);
			if (scheduledLayout.executor === "worker" && !scheduledLayout.preserveCompletedLayout) scheduledLayout.skeleton.cancelExternalLayout();
		}
		this._scheduledLayout = null;
		this._cancelCallbacks();
	}
	dispose() {
		this.cancel();
		const workerMount = this._workerMount;
		this._workerMount = null;
		if (workerMount != null) workerMount.executorService.disposeLayoutMount({
			unitId: workerMount.unitId,
			mountId: this._mountId,
			mountEpoch: workerMount.mountEpoch,
			viewportEpoch: workerMount.viewportEpoch
		}).catch(workerMount.onError);
		super.dispose();
	}
	_scheduleForeground() {
		if (this._scheduledLayout == null || this._animationFrameId != null || this._fallbackTimerId != null) return;
		if (this._scheduledLayout.executor === "worker") {
			this._fallbackTimerId = setTimeout(() => {
				this._fallbackTimerId = null;
				this._runSlice(WORKER_FOREGROUND_BUDGET_MS, false);
			}, 0);
			return;
		}
		const scheduledLayout = this._scheduledLayout;
		if (scheduledLayout.foregroundStarted || !scheduledLayout.waitForAnimationFrame) {
			this._fallbackTimerId = setTimeout(() => {
				this._fallbackTimerId = null;
				if (this._scheduledLayout !== scheduledLayout) return;
				scheduledLayout.foregroundStarted = true;
				this._runSlice(FOREGROUND_BUDGET_MS, false);
			}, 0);
			return;
		}
		if (typeof requestAnimationFrame === "function") {
			this._animationFrameId = requestAnimationFrame(() => {
				this._animationFrameId = null;
				if (this._scheduledLayout !== scheduledLayout) return;
				scheduledLayout.foregroundStarted = true;
				this._runSlice(FOREGROUND_BUDGET_MS, false);
			});
			return;
		}
		this._fallbackTimerId = setTimeout(() => {
			this._fallbackTimerId = null;
			if (this._scheduledLayout !== scheduledLayout) return;
			scheduledLayout.foregroundStarted = true;
			this._runSlice(FOREGROUND_BUDGET_MS, false);
		}, 0);
	}
	_scheduleBackground() {
		if (this._scheduledLayout == null || this._idleCallbackId != null || this._fallbackTimerId != null) return;
		const delay = this._scheduledLayout.nextBackgroundDelayMs;
		this._scheduledLayout.nextBackgroundDelayMs = BACKGROUND_YIELD_MS;
		this._fallbackTimerId = setTimeout(() => {
			this._fallbackTimerId = null;
			const scheduledLayout = this._scheduledLayout;
			if (scheduledLayout == null) return;
			if (scheduledLayout.executor === "worker") {
				if (hasPendingUserInput()) {
					this._scheduleBackground();
					return;
				}
				this._runSlice(WORKER_BACKGROUND_BUDGET_MS, true);
				return;
			}
			if (typeof requestIdleCallback === "function") {
				this._idleCallbackId = requestIdleCallback((deadline) => {
					this._idleCallbackId = null;
					if (hasPendingUserInput()) {
						this._scheduleBackground();
						return;
					}
					const availableTime = deadline.timeRemaining();
					const budget = availableTime > 0 ? Math.min(BACKGROUND_BUDGET_MS, Math.max(1, availableTime)) : FOREGROUND_BUDGET_MS;
					this._runSlice(budget, true);
				});
				return;
			}
			this._runSlice(FOREGROUND_BUDGET_MS, true);
		}, delay);
	}
	_runSlice(budgetMs, publishOneBacklogPage, scheduleContinuation = true) {
		const scheduledLayout = this._scheduledLayout;
		if (scheduledLayout == null) return;
		if (scheduledLayout.executor === "worker") {
			this._runWorkerSlice(scheduledLayout, budgetMs, publishOneBacklogPage).catch((error) => {
				if (this._scheduledLayout !== scheduledLayout) return;
				if (!scheduledLayout.preserveCompletedLayout) scheduledLayout.skeleton.cancelExternalLayout();
				this._scheduledLayout = null;
				this._cancelCallbacks();
				scheduledLayout.onError(error);
			});
			return;
		}
		try {
			this._runMainThreadSlice(scheduledLayout, budgetMs, publishOneBacklogPage, scheduleContinuation);
		} catch (error) {
			this.cancel();
			throw error;
		}
	}
	_runMainThreadSlice(scheduledLayout, budgetMs, publishOneBacklogPage, scheduleContinuation) {
		if (this._publishMainThreadBacklog(scheduledLayout, publishOneBacklogPage, scheduleContinuation)) return;
		const foregroundStepStartedAt = performance.now();
		const progress = scheduledLayout.skeleton.stepIncrementalLayout(scheduledLayout.generation, budgetMs);
		scheduledLayout.foregroundElapsedMs += performance.now() - foregroundStepStartedAt;
		if (this._scheduledLayout !== scheduledLayout) return;
		if (progress.cancelled) {
			this._scheduledLayout = null;
			this._cancelCallbacks();
			return;
		}
		scheduledLayout.anchorReady = progress.anchorReady;
		const publishedProgress = progress.complete && progress.mode !== "continuous" && scheduledLayout.callbacks.onForegroundReady != null ? {
			...progress,
			complete: false
		} : progress;
		scheduledLayout.callbacks.onProgress(publishedProgress);
		if (this._finishForegroundWindow(scheduledLayout, progress, scheduleContinuation)) return;
		if (progress.complete) {
			var _scheduledLayout$call, _scheduledLayout$call2;
			this._scheduledLayout = null;
			(_scheduledLayout$call = (_scheduledLayout$call2 = scheduledLayout.callbacks).onComplete) === null || _scheduledLayout$call === void 0 || _scheduledLayout$call.call(_scheduledLayout$call2, progress);
			return;
		}
		if (scheduleContinuation) if (scheduledLayout.anchorReady) this._scheduleBackground();
		else this._scheduleForeground();
	}
	_publishMainThreadBacklog(scheduledLayout, publishOneBacklogPage, scheduleContinuation) {
		if (!publishOneBacklogPage || !scheduledLayout.anchorReady || hasPendingUserInput() || typeof scheduledLayout.skeleton.publishIncrementalLayoutBacklog !== "function") return false;
		const backlogProgress = scheduledLayout.skeleton.publishIncrementalLayoutBacklog(scheduledLayout.generation);
		if (this._scheduledLayout !== scheduledLayout || backlogProgress.cancelled) return true;
		if (!backlogProgress.didPublish) return false;
		scheduledLayout.anchorReady = backlogProgress.anchorReady;
		scheduledLayout.callbacks.onProgress(backlogProgress);
		if (backlogProgress.complete) {
			var _scheduledLayout$call3, _scheduledLayout$call4;
			this._scheduledLayout = null;
			(_scheduledLayout$call3 = (_scheduledLayout$call4 = scheduledLayout.callbacks).onComplete) === null || _scheduledLayout$call3 === void 0 || _scheduledLayout$call3.call(_scheduledLayout$call4, backlogProgress);
			return true;
		}
		if (scheduleContinuation) this._scheduleBackground();
		return true;
	}
	_finishForegroundWindow(scheduledLayout, progress, scheduleContinuation) {
		const onForegroundReady = scheduledLayout.callbacks.onForegroundReady;
		if (onForegroundReady == null) return false;
		if (progress.complete) {
			this._completeForegroundWindow(scheduledLayout, progress, onForegroundReady);
			return true;
		}
		const progressCount = progress.mode === "continuous" ? progress.processedBlockCount : progress.publishedPageCount;
		if (progress.didPublishAnchor && progress.interactionWindowComplete) {
			this._completeForegroundWindow(scheduledLayout, progress, onForegroundReady);
			return true;
		}
		if (progress.didPublishAnchor && scheduledLayout.foregroundTargetProgressCount == null) scheduledLayout.foregroundTargetProgressCount = progressCount + Math.max(0, scheduledLayout.foregroundWindowSize - 1);
		const target = scheduledLayout.foregroundTargetProgressCount;
		if (target == null) return false;
		if (!(progress.mode === "continuous" && scheduledLayout.foregroundEndOffset != null ? progress.stableLaidOutThrough >= scheduledLayout.foregroundEndOffset : progressCount >= target || scheduledLayout.foregroundElapsedMs >= scheduledLayout.foregroundBudgetMs)) {
			if (scheduleContinuation) this._scheduleForeground();
			return true;
		}
		this._completeForegroundWindow(scheduledLayout, progress, onForegroundReady);
		return true;
	}
	_completeForegroundWindow(scheduledLayout, progress, onForegroundReady) {
		this._scheduledLayout = null;
		scheduledLayout.skeleton.cancelIncrementalLayout(scheduledLayout.generation);
		this._cancelCallbacks();
		onForegroundReady(progress);
	}
	async _runWorkerSlice(scheduledLayout, budgetMs, publishOneBacklogPage) {
		if (scheduledLayout.generation == null && await this._startWorkerLayout(scheduledLayout, budgetMs)) return;
		if (await this._publishWorkerBacklog(scheduledLayout, publishOneBacklogPage)) return;
		const result = await scheduledLayout.executorService.stepLayout({
			...this._getWorkerIdentity(scheduledLayout),
			generation: this._getWorkerGeneration(scheduledLayout),
			budgetMs
		});
		this._assertWorkerResultIdentity(scheduledLayout, result);
		this._assertWorkerResultRevisions(scheduledLayout, result, "step");
		if (this._scheduledLayout !== scheduledLayout || result.progress.cancelled) return;
		if (!await this._publishWorkerResult(scheduledLayout, result)) return;
		if (result.progress.complete) return;
		if (scheduledLayout.anchorReady) this._scheduleBackground();
		else this._scheduleForeground();
	}
	async _startWorkerLayout(scheduledLayout, budgetMs) {
		const startResult = await scheduledLayout.executorService.startLayout(this._getWorkerIdentity(scheduledLayout), scheduledLayout.options, budgetMs);
		if (this._scheduledLayout !== scheduledLayout) {
			if ((startResult === null || startResult === void 0 ? void 0 : startResult.status) === _univerjs_docs.DocLayoutSessionStatus.ACCEPTED) await scheduledLayout.executorService.cancelLayout({
				unitId: startResult.step.unitId,
				mountId: startResult.step.mountId,
				mountEpoch: startResult.step.mountEpoch,
				viewportEpoch: startResult.step.viewportEpoch,
				generation: startResult.step.progress.generation
			});
			return true;
		}
		if ((startResult === null || startResult === void 0 ? void 0 : startResult.status) !== _univerjs_docs.DocLayoutSessionStatus.ACCEPTED) throw new Error("The document layout Worker could not start a synchronized session.");
		this._assertWorkerResultIdentity(scheduledLayout, startResult.step);
		scheduledLayout.generation = startResult.step.progress.generation;
		scheduledLayout.modelRevision = startResult.step.modelRevision;
		scheduledLayout.metricsRevision = startResult.step.metricsRevision;
		if (startResult.step.progress.cancelled) return true;
		if (!await this._publishWorkerResult(scheduledLayout, startResult.step)) return true;
		if (startResult.step.progress.complete || scheduledLayout.anchorReady) {
			if (!startResult.step.progress.complete) this._scheduleBackground();
			return true;
		}
		return false;
	}
	async _publishWorkerBacklog(scheduledLayout, publishOneBacklogPage) {
		if (!publishOneBacklogPage || !scheduledLayout.anchorReady) return false;
		if (hasPendingUserInput()) {
			this._scheduleBackground();
			return true;
		}
		const backlogResult = await scheduledLayout.executorService.publishBacklog({
			...this._getWorkerIdentity(scheduledLayout),
			generation: this._getWorkerGeneration(scheduledLayout)
		});
		this._assertWorkerResultIdentity(scheduledLayout, backlogResult);
		this._assertWorkerResultRevisions(scheduledLayout, backlogResult, "backlog");
		if (this._scheduledLayout !== scheduledLayout || backlogResult.progress.cancelled) return true;
		if (!backlogResult.progress.didPublish) return false;
		if (!await this._publishWorkerResult(scheduledLayout, backlogResult)) return true;
		if (!backlogResult.progress.complete) this._scheduleBackground();
		return true;
	}
	_assertWorkerResultRevisions(scheduledLayout, result, phase) {
		if (result.modelRevision !== scheduledLayout.modelRevision) throw new Error(`The document layout Worker returned a stale ${phase} model revision.`);
		if (result.metricsRevision !== scheduledLayout.metricsRevision) throw new Error(`The document layout Worker returned stale ${phase} Custom Block metrics.`);
	}
	_publishWorkerResult(scheduledLayout, result) {
		return new Promise((resolve, reject) => {
			let settled = false;
			let cancelFrame = null;
			const settle = (committed) => {
				if (settled) return;
				settled = true;
				resolve(committed);
			};
			const cancel = () => {
				cancelFrame === null || cancelFrame === void 0 || cancelFrame();
				cancelFrame = null;
				settle(false);
			};
			const commit = () => {
				if (settled || cancelFrame != null) return;
				if (this._workerPresentationPaused) {
					this._pendingWorkerPresentation = {
						cancel,
						resume: commit
					};
					return;
				}
				cancelFrame = this._scheduleVisualFrame(() => {
					cancelFrame = null;
					if (this._workerPresentationPaused) {
						this._pendingWorkerPresentation = {
							cancel,
							resume: commit
						};
						return;
					}
					this._pendingWorkerPresentation = null;
					if (this._scheduledLayout !== scheduledLayout) {
						settle(false);
						return;
					}
					try {
						scheduledLayout.anchorReady = result.progress.anchorReady;
						if (!scheduledLayout.preserveCompletedLayout) scheduledLayout.callbacks.onProgress(result.progress, result.publication);
						if (result.progress.complete) {
							var _scheduledLayout$call5, _scheduledLayout$call6;
							this._scheduledLayout = null;
							(_scheduledLayout$call5 = (_scheduledLayout$call6 = scheduledLayout.callbacks).onComplete) === null || _scheduledLayout$call5 === void 0 || _scheduledLayout$call5.call(_scheduledLayout$call6, result.progress);
						}
						settle(true);
					} catch (error) {
						if (!settled) {
							settled = true;
							reject(error);
						}
					}
				});
				this._pendingWorkerPresentation = {
					cancel,
					resume: commit
				};
			};
			commit();
		});
	}
	_getWorkerIdentity(scheduledLayout) {
		return {
			unitId: scheduledLayout.unitId,
			mountId: scheduledLayout.mountId,
			mountEpoch: scheduledLayout.mountEpoch,
			viewportEpoch: scheduledLayout.viewportEpoch
		};
	}
	_getWorkerGeneration(scheduledLayout) {
		if (scheduledLayout.generation == null) throw new Error("The document layout Worker session has not started.");
		return scheduledLayout.generation;
	}
	_assertWorkerResultIdentity(scheduledLayout, result) {
		if (result.unitId !== scheduledLayout.unitId || result.mountId !== scheduledLayout.mountId || result.mountEpoch !== scheduledLayout.mountEpoch || result.viewportEpoch !== scheduledLayout.viewportEpoch || scheduledLayout.generation != null && result.progress.generation !== scheduledLayout.generation) throw new Error("The document layout Worker returned a stale publication identity.");
	}
	_cancelCallbacks() {
		var _this$_pendingWorkerP2;
		if (this._animationFrameId != null && typeof cancelAnimationFrame === "function") cancelAnimationFrame(this._animationFrameId);
		if (this._idleCallbackId != null && typeof cancelIdleCallback === "function") cancelIdleCallback(this._idleCallbackId);
		if (this._fallbackTimerId != null) clearTimeout(this._fallbackTimerId);
		(_this$_pendingWorkerP2 = this._pendingWorkerPresentation) === null || _this$_pendingWorkerP2 === void 0 || _this$_pendingWorkerP2.cancel();
		this._animationFrameId = null;
		this._idleCallbackId = null;
		this._fallbackTimerId = null;
		this._pendingWorkerPresentation = null;
	}
};

//#endregion
//#region src/services/doc-layout-interaction.service.ts
/**
* Coordinates render-scoped interactions that require the current document
* skeleton to stay stable until the interaction ends.
*/
var DocLayoutInteractionService = class extends _univerjs_core.Disposable {
	constructor(..._args) {
		super(..._args);
		_defineProperty(this, "_active$", new rxjs.BehaviorSubject(false));
		_defineProperty(this, "_interactionCount", 0);
		_defineProperty(this, "active$", this._active$.asObservable());
	}
	get isActive() {
		return this._interactionCount > 0;
	}
	beginInteraction() {
		this.ensureNotDisposed();
		this._interactionCount++;
		if (this._interactionCount === 1) this._active$.next(true);
		let disposed = false;
		return { dispose: () => {
			if (disposed) return;
			disposed = true;
			if (this._disposed) return;
			this._interactionCount--;
			if (this._interactionCount === 0) this._active$.next(false);
		} };
	}
	dispose() {
		if (this._disposed) return;
		if (this._interactionCount > 0) {
			this._interactionCount = 0;
			this._active$.next(false);
		}
		this._active$.complete();
		super.dispose();
	}
};

//#endregion
//#region src/services/doc-zoom.ts
const DEFAULT_DOC_ZOOM_RATIO = 1;
const DEFAULT_MODERN_DOC_ZOOM_RATIO = 1;
function getDocEffectiveZoomRatio(documentModel) {
	var _documentModel$getSet, _documentModel$getSna;
	const zoomRatio = (_documentModel$getSet = documentModel.getSettings()) === null || _documentModel$getSet === void 0 ? void 0 : _documentModel$getSet.zoomRatio;
	if (zoomRatio != null) return zoomRatio;
	return ((_documentModel$getSna = documentModel.getSnapshot().documentStyle) === null || _documentModel$getSna === void 0 ? void 0 : _documentModel$getSna.documentFlavor) === _univerjs_core.DocumentFlavor.MODERN ? 1 : DEFAULT_DOC_ZOOM_RATIO;
}

//#endregion
//#region src/services/doc-view-scale.ts
function normalizeDocFitToWidthOptions(options) {
	return {
		...DEFAULT_DOC_FIT_TO_WIDTH_OPTIONS,
		...options
	};
}
function resolveDocFitPaddingX(availableWidth, paddingX) {
	if (typeof paddingX === "number") return Number.isFinite(paddingX) ? paddingX : 0;
	if (typeof paddingX === "string") {
		const trimmedPadding = paddingX.trim();
		if (trimmedPadding.endsWith("%")) {
			const percent = Number(trimmedPadding.slice(0, -1));
			return Number.isFinite(percent) ? availableWidth * percent / 100 : 0;
		}
	}
	return 0;
}
function calcDocFitToWidthScale(params) {
	const options = normalizeDocFitToWidthOptions(params.options);
	if (options.mode !== "fit-width") return 1;
	if (!Number.isFinite(params.availableWidth) || !Number.isFinite(params.baseWidth) || params.baseWidth <= 0) return 1;
	const paddingX = resolveDocFitPaddingX(params.availableWidth, options.paddingX);
	const rawScale = Math.max(0, params.availableWidth - paddingX * 2) / params.baseWidth;
	const maxScale = options.maxScale ?? Number.POSITIVE_INFINITY;
	return Math.min(maxScale, Math.max(options.minScale, rawScale));
}
function resolveDocFitBaseWidth(params) {
	if (params.documentStylePageWidth != null && Number.isFinite(params.documentStylePageWidth) && params.documentStylePageWidth > 0) return params.documentStylePageWidth;
	if (params.documentFlavor === _univerjs_core.DocumentFlavor.MODERN) return _univerjs_core.MODERN_DOCUMENT_WIDTH[_univerjs_core.ModernDocumentWidthMode.MEDIUM];
	if (params.skeletonPageWidth != null && Number.isFinite(params.skeletonPageWidth) && params.skeletonPageWidth > 0) return params.skeletonPageWidth;
	return _univerjs_core.MODERN_DOCUMENT_WIDTH[_univerjs_core.ModernDocumentWidthMode.MEDIUM];
}
function resolveDocViewScale(userZoomRatio, fitToWidthScale) {
	return userZoomRatio * fitToWidthScale;
}
let DocViewScaleService = class DocViewScaleService extends _univerjs_core.Disposable {
	constructor(_context, _configService) {
		super();
		this._context = _context;
		this._configService = _configService;
	}
	getPluginConfig() {
		return this._configService.getConfig("docs-ui.config") ?? {};
	}
	getOptions() {
		return this.getPluginConfig().fitToWidth ?? DEFAULT_DOC_FIT_TO_WIDTH_OPTIONS;
	}
	getBaseWidth() {
		var _this$_context$unit, _this$_context$unit$g, _documentStyle$pageSi;
		const documentStyle = (_this$_context$unit = this._context.unit) === null || _this$_context$unit === void 0 || (_this$_context$unit$g = _this$_context$unit.getSnapshot) === null || _this$_context$unit$g === void 0 || (_this$_context$unit$g = _this$_context$unit$g.call(_this$_context$unit)) === null || _this$_context$unit$g === void 0 ? void 0 : _this$_context$unit$g.documentStyle;
		return resolveDocFitBaseWidth({
			documentFlavor: documentStyle === null || documentStyle === void 0 ? void 0 : documentStyle.documentFlavor,
			documentStylePageWidth: documentStyle === null || documentStyle === void 0 || (_documentStyle$pageSi = documentStyle.pageSize) === null || _documentStyle$pageSi === void 0 ? void 0 : _documentStyle$pageSi.width
		});
	}
	getAvailableWidth() {
		const config = this.getPluginConfig();
		if (normalizeDocFitToWidthOptions(config.fitToWidth).target === "container") {
			const container = this._resolveContainer(config.container);
			const containerWidth = container === null || container === void 0 ? void 0 : container.clientWidth;
			if (containerWidth != null && Number.isFinite(containerWidth) && containerWidth > 0) return containerWidth;
		}
		return this._context.engine.width;
	}
	getUserZoomRatio() {
		if (this._context.unit == null) return 1;
		return getDocEffectiveZoomRatio(this._context.unit);
	}
	getFitToWidthScale() {
		return calcDocFitToWidthScale({
			availableWidth: this.getAvailableWidth(),
			baseWidth: this.getBaseWidth(),
			options: this.getOptions()
		});
	}
	getViewScale(userZoomRatio = this.getUserZoomRatio()) {
		return resolveDocViewScale(userZoomRatio, this.getFitToWidthScale());
	}
	_resolveContainer(container) {
		if (container == null) return;
		if (typeof container === "string") return document.querySelector(container) ?? document.getElementById(container) ?? void 0;
		return container;
	}
};
DocViewScaleService = __decorate([__decorateParam(1, _univerjs_core.IConfigService)], DocViewScaleService);

//#endregion
//#region src/services/doc-page-layout.service.ts
let DocPageLayoutService = class DocPageLayoutService extends _univerjs_core.Disposable {
	constructor(_context, _docViewScaleService) {
		super();
		this._context = _context;
		this._docViewScaleService = _docViewScaleService;
	}
	calculatePagePosition() {
		if (this._disposed) return;
		const docObject = neoGetDocObject(this._context);
		const viewScale = this._docViewScaleService.getViewScale();
		const fitOptions = this._docViewScaleService.getOptions();
		const isStartAlignedFit = fitOptions.mode === "fit-width" && fitOptions.align === "start";
		const { document: docsComponent, scene, docBackground } = docObject;
		if (scene.scaleX !== viewScale || scene.scaleY !== viewScale) scene.scale(viewScale, viewScale);
		const parent = scene === null || scene === void 0 ? void 0 : scene.getParent();
		const { width: docsWidth, height: docsHeight, pageMarginLeft, pageMarginTop } = docsComponent;
		const horizontalMargin = isStartAlignedFit ? resolveDocFitPaddingX(this._docViewScaleService.getAvailableWidth(), fitOptions.paddingX) / viewScale : pageMarginLeft;
		if (parent == null || docsWidth === Number.POSITIVE_INFINITY || docsHeight === Number.POSITIVE_INFINITY) return;
		const { width: engineWidth, height: engineHeight } = parent;
		let docsLeft = 0;
		let docsTop = pageMarginTop;
		let sceneWidth = 0;
		let sceneHeight = 0;
		let scrollToX = Number.POSITIVE_INFINITY;
		if (engineWidth > (docsWidth + horizontalMargin * 2) * viewScale) {
			docsLeft = isStartAlignedFit ? horizontalMargin : (engineWidth / 2 - docsWidth * viewScale / 2) / viewScale;
			sceneWidth = (engineWidth - horizontalMargin * 2) / viewScale;
			scrollToX = 0;
		} else {
			docsLeft = horizontalMargin;
			sceneWidth = docsWidth + horizontalMargin * 2;
			scrollToX = 0;
		}
		if (engineHeight > docsHeight) sceneHeight = (engineHeight - pageMarginTop * 2) / viewScale;
		else sceneHeight = docsHeight + pageMarginTop * 2;
		scene.transformByState({
			width: sceneWidth,
			height: sceneHeight
		});
		if (engineWidth <= 1) {
			docsLeft = -1e4;
			docsTop = -1e4;
		}
		docsComponent.translate(docsLeft, docsTop);
		docBackground.translate(docsLeft, docsTop);
		const viewport = scene.getViewport("viewMain");
		if (scrollToX !== Number.POSITIVE_INFINITY && viewport != null) viewport.scrollToViewportPos({ viewportScrollX: scrollToX });
		return this;
	}
};
DocPageLayoutService = __decorate([__decorateParam(1, (0, _univerjs_core.Inject)(DocViewScaleService))], DocPageLayoutService);

//#endregion
//#region src/controllers/render-controllers/doc-mutation-layout.ts
const BODY_RANGE_COLLECTIONS = /* @__PURE__ */ new Set([
	"blockRanges",
	"columnGroups",
	"customBlocks",
	"customDecorations",
	"customRanges",
	"paragraphs",
	"renderedPageBreaks",
	"sectionBreaks",
	"tables",
	"textRuns"
]);
var DocumentMutationLayoutIndex = class {
	constructor(_body, _styles) {
		this._body = _body;
		this._styles = _styles;
		_defineProperty(this, "_tableRanges", null);
		_defineProperty(this, "_drawingRanges", null);
		_defineProperty(this, "_listRanges", null);
		_defineProperty(this, "_styleRanges", null);
		_defineProperty(this, "_styleDependents", null);
	}
	getTableRange(tableId) {
		if (this._tableRanges == null) {
			var _this$_body;
			this._tableRanges = /* @__PURE__ */ new Map();
			for (const table of ((_this$_body = this._body) === null || _this$_body === void 0 ? void 0 : _this$_body.tables) ?? []) if (!this._tableRanges.has(table.tableId)) this._tableRanges.set(table.tableId, {
				start: table.startIndex,
				end: table.endIndex
			});
		}
		return this._tableRanges.get(tableId);
	}
	getDrawingRange(drawingId) {
		if (this._drawingRanges == null) {
			var _this$_body2;
			this._drawingRanges = /* @__PURE__ */ new Map();
			for (const block of ((_this$_body2 = this._body) === null || _this$_body2 === void 0 ? void 0 : _this$_body2.customBlocks) ?? []) if (!this._drawingRanges.has(block.blockId)) this._drawingRanges.set(block.blockId, {
				start: block.startIndex,
				end: block.startIndex + 1
			});
		}
		return this._drawingRanges.get(drawingId);
	}
	getListRange(listIds) {
		this._ensureParagraphRanges();
		return mergeRangesById(this._listRanges, listIds);
	}
	getStyleRange(styleIds) {
		this._ensureParagraphRanges();
		if (this._styleDependents == null) {
			this._styleDependents = /* @__PURE__ */ new Map();
			for (const [styleId, style] of Object.entries(this._styles ?? {})) {
				if (style.basedOn == null) continue;
				const dependents = this._styleDependents.get(style.basedOn) ?? [];
				dependents.push(styleId);
				this._styleDependents.set(style.basedOn, dependents);
			}
		}
		const affectedStyleIds = new Set(styleIds);
		const pendingStyleIds = [...styleIds];
		while (pendingStyleIds.length > 0) {
			const styleId = pendingStyleIds.pop();
			for (const dependent of this._styleDependents.get(styleId) ?? []) if (!affectedStyleIds.has(dependent)) {
				affectedStyleIds.add(dependent);
				pendingStyleIds.push(dependent);
			}
		}
		return mergeRangesById(this._styleRanges, affectedStyleIds);
	}
	_ensureParagraphRanges() {
		var _this$_body3;
		if (this._listRanges != null && this._styleRanges != null) return;
		this._listRanges = /* @__PURE__ */ new Map();
		this._styleRanges = /* @__PURE__ */ new Map();
		for (const paragraph of ((_this$_body3 = this._body) === null || _this$_body3 === void 0 ? void 0 : _this$_body3.paragraphs) ?? []) {
			var _paragraph$bullet;
			const paragraphRange = {
				start: paragraph.startIndex,
				end: paragraph.startIndex + 1
			};
			const listId = (_paragraph$bullet = paragraph.bullet) === null || _paragraph$bullet === void 0 ? void 0 : _paragraph$bullet.listId;
			if (listId != null) this._listRanges.set(listId, mergeLayoutRange(this._listRanges.get(listId), paragraphRange));
			if (paragraph.styleId != null) this._styleRanges.set(paragraph.styleId, mergeLayoutRange(this._styleRanges.get(paragraph.styleId), paragraphRange));
		}
	}
};
function mergeRangesById(ranges, ids) {
	let range;
	for (const id of ids) range = mergeLayoutRange(range, ranges.get(id));
	return range;
}
function getBodyTextXActions(actions, segmentId) {
	if (segmentId || actions == null) return;
	let result;
	let duplicate = false;
	try {
		const cursor = _univerjs_core.JSON1.type.readCursor(actions);
		cursor.traverse(null, (component) => {
			const path = cursor.getPath();
			if (path.length !== 1 || path[0] !== "body" || component.et !== _univerjs_core.TextX.name || !Array.isArray(component.e)) return;
			if (result != null) duplicate = true;
			else result = component.e;
		});
	} catch {
		return;
	}
	return duplicate ? void 0 : result;
}
function getSingleBodyTextXActions(actions, segmentId) {
	return actions != null && actions.length === 2 && actions[0] === "body" ? getBodyTextXActions(actions, segmentId) : void 0;
}
function getDocumentMutationLayoutImpact(actions, snapshot) {
	if (actions == null) return { global: true };
	let global = false;
	let range;
	let sawComponent = false;
	let unresolvedLocal = false;
	const { body, styles } = snapshot;
	const index = new DocumentMutationLayoutIndex(body, styles);
	try {
		const cursor = _univerjs_core.JSON1.type.readCursor(actions);
		cursor.traverse(null, (component) => {
			sawComponent = true;
			const impact = getComponentLayoutImpact(cursor.getPath(), component, body, index);
			global ||= impact.global;
			unresolvedLocal ||= impact.unresolvedLocal === true;
			range = mergeLayoutRange(range, impact.range);
		});
	} catch {
		return { global: true };
	}
	return {
		global: global || !sawComponent,
		range,
		unresolvedLocal
	};
}
function getComponentLayoutImpact(path, component, body, index) {
	const root = path[0];
	if (root === "drawingsOrder") return { global: false };
	if (root === "body") return getBodyComponentLayoutImpact(path, component, body);
	if (root === "tableSource") {
		const tableId = path[1];
		const range = typeof tableId === "string" ? index.getTableRange(tableId) : void 0;
		return {
			global: false,
			range,
			unresolvedLocal: range == null
		};
	}
	if (root === "drawings") {
		const drawingId = path[1];
		const range = typeof drawingId === "string" ? index.getDrawingRange(drawingId) : void 0;
		return {
			global: false,
			range,
			unresolvedLocal: range == null
		};
	}
	if (root === "lists") {
		const range = body == null ? void 0 : index.getListRange(getChangedRecordKeys(path, component));
		return {
			global: false,
			range,
			unresolvedLocal: range == null
		};
	}
	if (root === "styles") {
		const range = body == null ? void 0 : index.getStyleRange(getChangedRecordKeys(path, component));
		return {
			global: false,
			range,
			unresolvedLocal: range == null
		};
	}
	return { global: true };
}
function getBodyComponentLayoutImpact(path, component, body) {
	if (path.length === 1 && component.et === _univerjs_core.TextX.name && Array.isArray(component.e)) return { global: false };
	const collection = path[1];
	if (typeof collection !== "string" || !BODY_RANGE_COLLECTIONS.has(collection)) return { global: true };
	const range = body == null ? void 0 : getBodyCollectionRange(body, path, component);
	return {
		global: false,
		range,
		unresolvedLocal: range == null
	};
}
function mergeMutationLayoutInvalidation(textInvalidation, structuralRange) {
	if (structuralRange == null) return textInvalidation;
	if (textInvalidation == null) return {
		oldStart: structuralRange.start,
		oldEnd: structuralRange.end,
		newEnd: structuralRange.end
	};
	return {
		oldStart: Math.min(textInvalidation.oldStart, mapCurrentOffsetToPrevious(structuralRange.start, textInvalidation)),
		oldEnd: Math.max(textInvalidation.oldEnd, mapCurrentOffsetToPrevious(structuralRange.end, textInvalidation)),
		newEnd: Math.max(textInvalidation.newEnd, structuralRange.end)
	};
}
function resolveMutationLayoutRequest(textInvalidation, impact, bodyRangeStarts) {
	if (impact.global) return {
		anchor: void 0,
		invalidation: void 0
	};
	if (impact.unresolvedLocal && textInvalidation == null) return {
		anchor: void 0,
		invalidation: void 0
	};
	const invalidation = mergeMutationLayoutInvalidation(textInvalidation, impact.range);
	return {
		anchor: (invalidation === null || invalidation === void 0 ? void 0 : invalidation.oldStart) ?? (bodyRangeStarts.length > 0 ? Math.min(...bodyRangeStarts) : void 0),
		invalidation
	};
}
function getBodyCollectionRange(body, path, component) {
	const collection = path[1];
	if (typeof collection !== "string" || !BODY_RANGE_COLLECTIONS.has(collection)) return;
	if ("p" in component || "d" in component) return;
	const index = path[2];
	if (typeof index === "number") {
		let range = path.length === 3 && ("r" in component || "i" in component) ? void 0 : getIndexedBodyRange(body, collection, index);
		range = mergeLayoutRange(range, getBodyItemRange(component.r, collection));
		return mergeLayoutRange(range, getBodyItemRange(component.i, collection));
	}
	let range;
	for (const value of [component.r, component.i]) {
		if (!Array.isArray(value)) continue;
		for (const item of value) range = mergeLayoutRange(range, getBodyItemRange(item, collection));
	}
	return range;
}
function getIndexedBodyRange(body, collection, index) {
	const items = body[collection];
	return Array.isArray(items) ? getBodyItemRange(items[index], collection) : void 0;
}
function getBodyItemRange(item, collection) {
	if (collection === "renderedPageBreaks" && typeof item === "number") return {
		start: item,
		end: item + 1
	};
	if (typeof item !== "object" || item == null) return;
	if ("st" in item && "ed" in item && typeof item.st === "number" && typeof item.ed === "number") return {
		start: item.st,
		end: item.ed
	};
	if (!("startIndex" in item) || typeof item.startIndex !== "number") return;
	const end = "endIndex" in item && typeof item.endIndex === "number" ? item.endIndex + (collection === "tables" ? 0 : 1) : item.startIndex + 1;
	return {
		start: item.startIndex,
		end
	};
}
function getChangedRecordKeys(path, component) {
	const keys = /* @__PURE__ */ new Set();
	if (typeof path[1] === "string") {
		keys.add(path[1]);
		return keys;
	}
	for (const value of [component.r, component.i]) if (typeof value === "object" && value != null && !Array.isArray(value)) Object.keys(value).forEach((key) => keys.add(key));
	return keys;
}
function mergeLayoutRange(current, next) {
	if (next == null) return current;
	return current == null ? next : {
		start: Math.min(current.start, next.start),
		end: Math.max(current.end, next.end)
	};
}
function mapCurrentOffsetToPrevious(offset, invalidation) {
	if (offset < invalidation.oldStart) return offset;
	if (offset < invalidation.newEnd) return invalidation.oldStart;
	return offset - (invalidation.newEnd - invalidation.oldEnd);
}

//#endregion
//#region src/controllers/render-controllers/doc.render-controller.ts
function getTextXActionLength(action) {
	if (typeof action !== "object" || action == null || !("t" in action) || !("len" in action)) return;
	return typeof action.len === "number" && Number.isFinite(action.len) && action.len >= 0 ? action.len : void 0;
}
function isPlainTextXRetain(action) {
	return action.t === _univerjs_core.TextXActionType.RETAIN && !("body" in action) && !("oldBody" in action) && !("coverType" in action);
}
function isHyperlinkCustomRange(value) {
	return typeof value === "object" && value != null && "rangeType" in value && value.rangeType === _univerjs_core.CustomRangeType.HYPERLINK;
}
function isLayoutOnlyCustomDecoration(value) {
	return typeof value === "object" && value != null && "type" in value && (value.type === _univerjs_core.CustomDecorationType.COMMENT || value.type === _univerjs_core.CustomDecorationType.DELETED);
}
function getLayoutMetadataCount(value) {
	const ranges = value.customRanges;
	const decorations = value.customDecorations;
	if (ranges != null && (!Array.isArray(ranges) || !ranges.every(isHyperlinkCustomRange)) || decorations != null && (!Array.isArray(decorations) || !decorations.every(isLayoutOnlyCustomDecoration))) return null;
	return (Array.isArray(ranges) ? ranges.length : 0) + (Array.isArray(decorations) ? decorations.length : 0);
}
function omitLayoutMetadata(value) {
	return Object.fromEntries(Object.entries(value).filter(([key]) => key !== "customRanges" && key !== "customDecorations"));
}
function isLayoutMetadataOnlyMutation(actions, segmentId) {
	const textActions = getSingleBodyTextXActions(actions, segmentId);
	return textActions != null && textActions.length > 0 && textActions.every((action) => {
		if (typeof action !== "object" || action == null || !("t" in action) || action.t !== _univerjs_core.TextXActionType.RETAIN) return false;
		if (isPlainTextXRetain(action)) return true;
		if (!("body" in action) || typeof action.body !== "object" || action.body == null) return false;
		const body = action.body;
		const bodyMetadataCount = getLayoutMetadataCount(body);
		if (body.dataStream === "" && bodyMetadataCount != null && bodyMetadataCount > 0 && Object.keys(body).every((key) => key === "dataStream" || key === "customRanges" || key === "customDecorations")) return true;
		if (!("oldBody" in action) || typeof action.oldBody !== "object" || action.oldBody == null) return false;
		const oldBody = action.oldBody;
		const oldBodyMetadataCount = getLayoutMetadataCount(oldBody);
		return body.dataStream === "" && oldBody.dataStream === "" && bodyMetadataCount != null && oldBodyMetadataCount != null && bodyMetadataCount + oldBodyMetadataCount > 0 && _univerjs_core.Tools.diffValue(omitLayoutMetadata(body), omitLayoutMetadata(oldBody));
	});
}
function getBodyMutationInvalidation(actions, segmentId) {
	const textActions = getBodyTextXActions(actions, segmentId);
	if (textActions == null) return;
	let oldOffset = 0;
	let newOffset = 0;
	let invalidation;
	let sawTrailingRetain = false;
	for (const action of textActions) {
		const length = getTextXActionLength(action);
		if (length == null || typeof action !== "object" || action == null || !("t" in action)) return;
		if (isPlainTextXRetain(action)) {
			oldOffset += length;
			newOffset += length;
			sawTrailingRetain ||= invalidation != null;
			continue;
		}
		if (sawTrailingRetain) return;
		invalidation ??= {
			oldStart: oldOffset,
			oldEnd: oldOffset,
			newEnd: newOffset
		};
		switch (action.t) {
			case _univerjs_core.TextXActionType.RETAIN:
				oldOffset += length;
				newOffset += length;
				break;
			case _univerjs_core.TextXActionType.INSERT:
				newOffset += length;
				break;
			case _univerjs_core.TextXActionType.DELETE:
				oldOffset += length;
				break;
			default: return;
		}
		invalidation.oldEnd = oldOffset;
		invalidation.newEnd = newOffset;
	}
	return invalidation;
}
function isPositionedObjectLayoutType(value) {
	switch (value) {
		case _univerjs_core.PositionedObjectLayoutType.INLINE:
		case _univerjs_core.PositionedObjectLayoutType.WRAP_NONE:
		case _univerjs_core.PositionedObjectLayoutType.WRAP_POLYGON:
		case _univerjs_core.PositionedObjectLayoutType.WRAP_SQUARE:
		case _univerjs_core.PositionedObjectLayoutType.WRAP_THROUGH:
		case _univerjs_core.PositionedObjectLayoutType.WRAP_TIGHT:
		case _univerjs_core.PositionedObjectLayoutType.WRAP_TOP_AND_BOTTOM: return true;
		default: return false;
	}
}
function getDrawingLayoutType(value) {
	if (typeof value !== "object" || value == null || !("layoutType" in value)) return;
	const layoutType = value.layoutType;
	return isPositionedObjectLayoutType(layoutType) ? layoutType : void 0;
}
function isRichTextEditingMutationParams(value) {
	return typeof value === "object" && value != null && "unitId" in value && typeof value.unitId === "string" && "actions" in value && (value.actions == null || Array.isArray(value.actions));
}
function doesMutationScheduleLocalSelectionUpdate(params, isRemoteMutation) {
	return !isRemoteMutation && params.isSync !== true && params.noNeedSetTextRange !== true && params.trigger != null && params.textRanges != null;
}
const EDIT_WORKER_RESUME_DELAY_MS = 150;
const MATERIALIZED_PAGE_WINDOW_SIZE = 5;
function resolveMaterializedPageAxis(docsComponent, viewport) {
	if (!(docsComponent instanceof _univerjs_engine_render.Documents) || viewport == null) return null;
	const viewBound = viewport.calcViewportInfo().viewBound;
	const { docsLeft, docsTop, pageLayoutType = _univerjs_engine_render.PageLayoutType.VERTICAL, pageMarginLeft, pageMarginTop } = docsComponent.getOffsetConfig();
	const vertical = pageLayoutType !== _univerjs_engine_render.PageLayoutType.HORIZONTAL;
	return {
		pageGap: vertical ? pageMarginTop : pageMarginLeft,
		vertical,
		viewportCenter: vertical ? (viewBound.top + viewBound.bottom) / 2 - docsTop : (viewBound.left + viewBound.right) / 2 - docsLeft
	};
}
function mapPreviousOffsetToCurrent(offset, invalidation) {
	if (invalidation == null || offset < invalidation.oldStart) return offset;
	if (offset >= invalidation.oldEnd) return offset + invalidation.newEnd - invalidation.oldEnd;
	return invalidation.newEnd;
}
function mergeDocumentLayoutInvalidations(previous, current) {
	if (previous == null || current == null) return;
	const previousDelta = previous.newEnd - previous.oldEnd;
	const mapCurrentToPrevious = (offset) => {
		if (offset < previous.oldStart) return offset;
		if (offset < previous.newEnd) return previous.oldStart;
		return offset - previousDelta;
	};
	const oldStart = Math.min(previous.oldStart, mapCurrentToPrevious(current.oldStart));
	const oldEnd = Math.max(previous.oldEnd, mapCurrentToPrevious(current.oldEnd));
	const currentDelta = current.newEnd - current.oldEnd;
	return {
		oldStart,
		oldEnd,
		newEnd: oldEnd + previousDelta + currentDelta
	};
}
function findContinuousLayoutEndOffset(page, documentTop, viewportTop, viewportBottom) {
	const protectedBottom = viewportBottom + Math.max(0, viewportBottom - viewportTop);
	let endOffset;
	for (const section of page.sections) for (const column of section.columns) {
		const localBottom = protectedBottom - documentTop - page.marginTop - section.top;
		let low = 0;
		let high = column.lines.length;
		while (low < high) {
			const middle = low + Math.floor((high - low) / 2);
			if (column.lines[middle].top <= localBottom) low = middle + 1;
			else high = middle;
		}
		const lastVisibleLine = column.lines[low - 1];
		if (lastVisibleLine != null) endOffset = Math.max(endOffset ?? lastVisibleLine.ed, lastVisibleLine.ed);
	}
	return endOffset;
}
const DRAWING_METADATA_FIELDS = /* @__PURE__ */ new Set(["title", "description"]);
/**
* Returns false only when a mutation is proven not to affect text layout.
*
* WRAP_NONE drawings are positioned independently from text, so moving, resizing,
* rotating, or switching them between front/behind text must refresh drawing
* geometry without starting a document layout generation. Accessibility metadata
* never participates in layout, regardless of wrapping mode. Every uncertain case
* remains conservative and reflows.
*/
function doesDocMutationRequireLayout(actions, drawings) {
	if (actions == null || !Array.isArray(actions) || actions.length === 0) return true;
	const transitions = /* @__PURE__ */ new Map();
	let sawComponent = false;
	let drawingOnly = true;
	try {
		const cursor = _univerjs_core.JSON1.type.readCursor(actions);
		cursor.traverse(null, (component) => {
			sawComponent = true;
			const path = cursor.getPath();
			if (path[0] === "drawingsOrder") return;
			const drawingId = path[1];
			if (path[0] !== "drawings" || typeof drawingId !== "string") {
				drawingOnly = false;
				return;
			}
			if (path.length >= 3 && DRAWING_METADATA_FIELDS.has(String(path[2]))) return;
			const transition = transitions.get(drawingId) ?? {};
			if (path.length === 2) {
				transition.before = getDrawingLayoutType(component.r);
				transition.after = getDrawingLayoutType(component.i);
			} else if (path.length === 3 && path[2] === "layoutType") {
				transition.before = isPositionedObjectLayoutType(component.r) ? component.r : transition.before;
				transition.after = isPositionedObjectLayoutType(component.i) ? component.i : transition.after;
			}
			transitions.set(drawingId, transition);
		});
	} catch {
		return true;
	}
	if (!sawComponent || !drawingOnly) return true;
	if (transitions.size === 0) return false;
	for (const [drawingId, transition] of transitions) {
		var _drawings$drawingId;
		const currentLayoutType = drawings === null || drawings === void 0 || (_drawings$drawingId = drawings[drawingId]) === null || _drawings$drawingId === void 0 ? void 0 : _drawings$drawingId.layoutType;
		const before = transition.before ?? currentLayoutType;
		const after = transition.after ?? currentLayoutType;
		if (before == null || after == null || before !== _univerjs_core.PositionedObjectLayoutType.WRAP_NONE || after !== _univerjs_core.PositionedObjectLayoutType.WRAP_NONE) return true;
	}
	return false;
}
let DocRenderController = class DocRenderController extends _univerjs_core.RxDisposable {
	constructor(_context, _commandService, _docSelectionRenderService, _docSkeletonManagerService, _editorService, _renderManagerService, _univerInstanceService, _docPageLayoutService, _textSelectionManagerService, _docViewScaleService, _themeService, _docLayoutExecutorService, _docLayoutInteractionService, _logService) {
		super();
		this._context = _context;
		this._commandService = _commandService;
		this._docSelectionRenderService = _docSelectionRenderService;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		this._editorService = _editorService;
		this._renderManagerService = _renderManagerService;
		this._univerInstanceService = _univerInstanceService;
		this._docPageLayoutService = _docPageLayoutService;
		this._textSelectionManagerService = _textSelectionManagerService;
		this._docViewScaleService = _docViewScaleService;
		this._themeService = _themeService;
		this._docLayoutExecutorService = _docLayoutExecutorService;
		this._docLayoutInteractionService = _docLayoutInteractionService;
		this._logService = _logService;
		_defineProperty(this, "_changesetRenderScheduled", false);
		_defineProperty(this, "_pendingChangesetLayoutRequest", null);
		_defineProperty(this, "_layoutCoordinator", void 0);
		_defineProperty(this, "_layoutRequestId", 0);
		_defineProperty(this, "_workerHandoffTimer", null);
		_defineProperty(this, "_workerPresentationResumeTimer", null);
		_defineProperty(this, "_pendingWorkerHandoff", null);
		_defineProperty(this, "_pendingWorkerEditBatch", null);
		_defineProperty(this, "_latestLayoutRestart", null);
		_defineProperty(this, "_pendingImeLayoutRestart", null);
		_defineProperty(this, "_isImeComposing", false);
		_defineProperty(this, "_recoveryViewportAnchor", null);
		_defineProperty(this, "_pendingMaterializedPageRange", null);
		_defineProperty(this, "_isMaterializingPages", false);
		_defineProperty(this, "_reservedLayoutWidth", 0);
		_defineProperty(this, "_reservedLayoutHeight", 0);
		this._layoutCoordinator = new DocLayoutCoordinatorService((callback) => this._scheduleVisualFrame(callback));
		this.disposeWithMe(this._layoutCoordinator);
		this._addNewRender();
		this._initRenderRefresh();
		this._initCommandListener();
		this._initInteractionLayoutProtection();
		this._initThemeListener();
	}
	_scheduleVisualFrame(callback) {
		const beginFrame$ = this._context.engine.beginFrame$;
		if (beginFrame$ == null) {
			const frameId = requestAnimationFrame(callback);
			return () => cancelAnimationFrame(frameId);
		}
		let disposed = false;
		let unsubscribe = null;
		const schedule = () => {
			const subscription = beginFrame$.pipe((0, rxjs.take)(1)).subscribe(() => {
				if (disposed) return;
				const parent = this._context.scene.getParent();
				if (parent == null || parent.width <= 1 || parent.height <= 1) {
					schedule();
					return;
				}
				callback();
			});
			unsubscribe = () => subscription.unsubscribe();
		};
		schedule();
		return () => {
			disposed = true;
			unsubscribe === null || unsubscribe === void 0 || unsubscribe();
		};
	}
	reRender(unitId, anchor, invalidation, priorityAnchor, refreshMainSelection = true, preserveInactiveViewportAnchor = false, deferForeground = false) {
		var _this$_renderManagerS;
		const docSkeletonManagerService = (_this$_renderManagerS = this._renderManagerService.getRenderUnitById(unitId)) === null || _this$_renderManagerS === void 0 ? void 0 : _this$_renderManagerS.with(_univerjs_docs.DocSkeletonManagerService);
		if (!docSkeletonManagerService) return;
		const skeleton = docSkeletonManagerService.getSkeleton();
		if (!skeleton) return;
		if (!!skeleton.getViewModel().getDataModel().getSnapshot().disabled) return;
		if (!docSkeletonManagerService.supportsIncrementalLayout()) {
			docSkeletonManagerService.recalculate();
			return;
		}
		const hasExplicitEditAnchor = anchor != null || priorityAnchor != null || invalidation != null;
		const layoutOptions = !skeleton.hasCompleteLayout() && !hasExplicitEditAnchor ? { reason: "initial" } : {
			reason: "edit",
			anchor: anchor ?? 0,
			priorityAnchor,
			invalidation,
			...deferForeground ? { deferForeground: true } : {}
		};
		this._scheduleLayout(unitId, skeleton, layoutOptions, refreshMainSelection, preserveInactiveViewportAnchor);
	}
	dispose() {
		this._layoutRequestId++;
		if (this._workerHandoffTimer != null) {
			clearTimeout(this._workerHandoffTimer);
			this._workerHandoffTimer = null;
		}
		if (this._workerPresentationResumeTimer != null) {
			clearTimeout(this._workerPresentationResumeTimer);
			this._workerPresentationResumeTimer = null;
		}
		this._pendingWorkerHandoff = null;
		this._pendingWorkerEditBatch = null;
		this._pendingChangesetLayoutRequest = null;
		this._latestLayoutRestart = null;
		this._pendingImeLayoutRestart = null;
		this._pendingMaterializedPageRange = null;
		this._recoveryViewportAnchor = null;
		super.dispose();
	}
	refreshCustomBlockPresentation(unitId) {
		var _this$_renderManagerS2;
		const skeleton = (_this$_renderManagerS2 = this._renderManagerService.getRenderUnitById(unitId)) === null || _this$_renderManagerS2 === void 0 || (_this$_renderManagerS2 = _this$_renderManagerS2.with(_univerjs_docs.DocSkeletonManagerService)) === null || _this$_renderManagerS2 === void 0 ? void 0 : _this$_renderManagerS2.getSkeleton();
		if (skeleton == null) return false;
		const result = skeleton.refreshCustomBlockPresentationViewports();
		if (result.didRefresh) {
			var _this$_context$mainCo, _this$_context$compon;
			(_this$_context$mainCo = this._context.mainComponent) === null || _this$_context$mainCo === void 0 || _this$_context$mainCo.makeDirty(true);
			(_this$_context$compon = this._context.components.get("__Document_Render_Background__")) === null || _this$_context$compon === void 0 || _this$_context$compon.makeDirty(true);
		}
		return !result.requiresLayout;
	}
	_scheduleLayout(unitId, skeleton, options, refreshMainSelection = true, preserveInactiveViewportAnchor = false) {
		this._latestLayoutRestart = () => this._scheduleLayout(unitId, skeleton, options, refreshMainSelection, preserveInactiveViewportAnchor);
		if (this._isImeComposing) this._pendingImeLayoutRestart = null;
		const layoutRequestId = ++this._layoutRequestId;
		this._cancelWorkerHandoff();
		const isInitialLayout = options.reason === "initial";
		const docsComponent = this._context.mainComponent;
		if (!(docsComponent instanceof _univerjs_engine_render.Documents)) return;
		this._prepareReservedLayoutExtent(skeleton, docsComponent);
		const mainThreadCallbacks = this._createLayoutCallbacks(unitId, skeleton, options, refreshMainSelection, true, preserveInactiveViewportAnchor, void 0, false);
		if (options.reuseMainBaseline) {
			this._layoutCoordinator.schedule(skeleton, options, mainThreadCallbacks);
			return;
		}
		if (this._docLayoutExecutorService.getExecutor() == null) {
			this._pendingWorkerEditBatch = null;
			this._layoutCoordinator.schedule(skeleton, options, mainThreadCallbacks);
			return;
		}
		const workerOptions = isInitialLayout ? options : this._accumulateWorkerEditBatch(options);
		if (isInitialLayout) {
			this._pendingWorkerEditBatch = null;
			this._scheduleInitialInteractionWindow(layoutRequestId, unitId, skeleton, options, workerOptions, mainThreadCallbacks, preserveInactiveViewportAnchor);
			return;
		}
		this._scheduleMainInteractionWindow(layoutRequestId, unitId, skeleton, options, workerOptions, mainThreadCallbacks, refreshMainSelection, preserveInactiveViewportAnchor);
	}
	_scheduleInitialInteractionWindow(layoutRequestId, unitId, skeleton, options, workerOptions, mainThreadCallbacks, preserveInactiveViewportAnchor) {
		this._layoutCoordinator.schedule(skeleton, {
			...options,
			priorityAnchor: options.priorityAnchor ?? 0
		}, {
			onProgress: mainThreadCallbacks.onProgress,
			onForegroundReady: (progress) => {
				if (progress.complete) return;
				const protectedRange = this._resolveProtectedRange(skeleton, 0, progress);
				this._queueWorkerHandoff(layoutRequestId, () => {
					this._scheduleWorkerLayout(layoutRequestId, unitId, skeleton, options, workerOptions, protectedRange, mainThreadCallbacks, preserveInactiveViewportAnchor);
				});
			}
		});
	}
	_prepareReservedLayoutExtent(skeleton, docsComponent) {
		if (skeleton.hasCompleteLayout()) {
			this._reservedLayoutWidth = Math.max(this._reservedLayoutWidth, docsComponent.width || 0);
			this._reservedLayoutHeight = Math.max(this._reservedLayoutHeight, docsComponent.height || 0);
		} else {
			this._reservedLayoutWidth = 0;
			this._reservedLayoutHeight = 0;
		}
	}
	_scheduleMainInteractionWindow(layoutRequestId, unitId, skeleton, options, workerOptions, mainThreadCallbacks, refreshMainSelection, preserveInactiveViewportAnchor) {
		const priorityAnchorBelongsToMutation = options.priorityAnchor != null && options.invalidation != null && options.priorityAnchor >= options.invalidation.oldStart && options.priorityAnchor <= options.invalidation.newEnd;
		const interactionAnchor = priorityAnchorBelongsToMutation ? options.anchor : options.priorityAnchor ?? options.anchor;
		let interactionViewportAnchor = priorityAnchorBelongsToMutation ? null : this._captureViewportAnchor(skeleton, options.priorityAnchor, options.invalidation);
		const foregroundEndOffset = this._resolveContinuousForegroundEndOffset(skeleton, options.invalidation);
		this._layoutCoordinator.schedule(skeleton, {
			...options,
			anchor: interactionAnchor,
			...foregroundEndOffset == null ? {} : { foregroundEndOffset },
			preserveInteractionWindow: interactionAnchor != null && interactionAnchor !== options.anchor,
			reuseUnaffectedTail: false
		}, {
			onProgress: (progress, publication) => {
				mainThreadCallbacks.onProgress(progress, publication);
				if (interactionViewportAnchor != null && (progress.didPublishAnchor || progress.complete)) {
					this._restoreViewportAnchor(skeleton, interactionViewportAnchor);
					interactionViewportAnchor = null;
				}
			},
			onForegroundReady: (progress) => {
				this._refreshSelectionAfterForegroundLayout(unitId, refreshMainSelection);
				const protectedRange = this._resolveProtectedRange(skeleton, options.priorityAnchor ?? options.anchor, progress);
				this._queueWorkerHandoff(layoutRequestId, () => {
					this._scheduleWorkerLayout(layoutRequestId, unitId, skeleton, options, workerOptions, protectedRange, mainThreadCallbacks, preserveInactiveViewportAnchor, true, progress.mode === "continuous" && progress.complete);
				});
			}
		});
	}
	_refreshSelectionAfterForegroundLayout(unitId, refreshMainSelection) {
		if (refreshMainSelection) return;
		this._refreshPagePositionAndSelection(this._getActiveEditingRange(unitId) != null);
	}
	_resolveContinuousForegroundEndOffset(skeleton, invalidation) {
		var _skeleton$getSkeleton;
		if (this._context.unit.getSnapshot().documentStyle.documentFlavor !== _univerjs_core.DocumentFlavor.MODERN) return;
		const page = (_skeleton$getSkeleton = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton === void 0 ? void 0 : _skeleton$getSkeleton.pages[0];
		const docsComponent = this._context.mainComponent;
		const viewport = this._context.scene.getViewport("viewMain");
		if (page == null || !(docsComponent instanceof _univerjs_engine_render.Documents) || viewport == null) return;
		const viewBound = viewport.calcViewportInfo().viewBound;
		if (!Number.isFinite(viewBound.top) || !Number.isFinite(viewBound.bottom)) return;
		const previousEndOffset = findContinuousLayoutEndOffset(page, docsComponent.getOffsetConfig().docsTop, viewBound.top, viewBound.bottom);
		return previousEndOffset == null ? void 0 : mapPreviousOffsetToCurrent(previousEndOffset, invalidation);
	}
	_createLayoutCallbacks(unitId, skeleton, options, refreshIncompleteAnchorSelection, refreshCompleteSelection, preserveInactiveViewportAnchor, layoutRequestId, deferCompleteDuringInteraction) {
		const isInitialLayout = options.reason === "initial";
		return { onProgress: (progress, publication) => this._handleLayoutProgress(unitId, skeleton, options, progress, publication, isInitialLayout, refreshIncompleteAnchorSelection, refreshCompleteSelection, preserveInactiveViewportAnchor, layoutRequestId, deferCompleteDuringInteraction) };
	}
	_handleLayoutProgress(unitId, skeleton, options, progress, publication, isInitialLayout, refreshIncompleteAnchorSelection, refreshCompleteSelection, preserveInactiveViewportAnchor, layoutRequestId, deferCompleteDuringInteraction) {
		if (!progress.didPublish) return;
		if (deferCompleteDuringInteraction && progress.complete && publication != null && layoutRequestId != null && this._docLayoutInteractionService.isActive) {
			this._queueWorkerHandoff(layoutRequestId, () => this._handleLayoutProgress(unitId, skeleton, options, progress, publication, isInitialLayout, refreshIncompleteAnchorSelection, refreshCompleteSelection, preserveInactiveViewportAnchor, layoutRequestId, false), true);
			return;
		}
		const completionViewportAnchor = this._captureCompletionViewportAnchor(unitId, skeleton, publication != null && progress.complete && !isInitialLayout, preserveInactiveViewportAnchor);
		const applyResult = this._applyLayoutPublication(unitId, skeleton, publication, progress);
		const editorRenderConfig = this._editorService.getEditorRenderConfig(unitId);
		if (editorRenderConfig && !editorRenderConfig.scrollBar) {
			this._markDocumentRenderDirty();
			return;
		}
		this._recalculateSizeBySkeleton(skeleton, progress);
		if (progress.complete) this._completeLayoutPresentation(unitId, skeleton, completionViewportAnchor, refreshCompleteSelection, refreshIncompleteAnchorSelection, (applyResult === null || applyResult === void 0 ? void 0 : applyResult.didReplaceProtectedPages) === true);
		else if (isInitialLayout) this._refreshPagePosition();
		else if (progress.didPublishAnchor && (refreshIncompleteAnchorSelection || this._docSelectionRenderService.hasPendingSelection)) this._textSelectionManagerService.refreshSelection({
			unitId,
			subUnitId: unitId
		}, this._getActiveEditingRange(unitId) != null);
		this._markDocumentRenderDirty();
	}
	_applyLayoutPublication(unitId, skeleton, publication, progress) {
		if (publication == null) return null;
		const hydrationStartedAt = _univerjs_core.Tools.now();
		try {
			const materializedPageRange = progress.mode === "paginated" ? this._resolveMaterializedPageRange(skeleton, progress.publishedPageCount) : void 0;
			return skeleton.applyLayoutPublication(publication, progress, materializedPageRange);
		} finally {
			this._docLayoutExecutorService.recordHydrationDuration(unitId, _univerjs_core.Tools.now() - hydrationStartedAt);
		}
	}
	_completeLayoutPresentation(unitId, skeleton, completionViewportAnchor, refreshCompleteSelection, preserveEditingSelection, didReplaceProtectedPages) {
		this._docLayoutExecutorService.completeRecovery(unitId);
		if (refreshCompleteSelection || didReplaceProtectedPages || this._docSelectionRenderService.hasPendingSelection) {
			const isEditing = preserveEditingSelection || didReplaceProtectedPages || this._getActiveEditingRange(unitId) != null;
			this._refreshPagePositionAndSelection(isEditing);
		} else this._refreshPagePosition();
		this._restoreViewportAnchor(skeleton, completionViewportAnchor);
		this._restoreRecoveryViewportAnchor(skeleton);
	}
	_scheduleWorkerLayout(layoutRequestId, unitId, skeleton, options, workerOptions, protectedRange, mainThreadCallbacks, preserveInactiveViewportAnchor, allowRecovery = true, preserveCompletedLayout = false) {
		if (layoutRequestId !== this._layoutRequestId) return;
		this._layoutCoordinator.scheduleWorker(unitId, skeleton, this._docLayoutExecutorService, workerOptions, protectedRange, {
			...this._createLayoutCallbacks(unitId, skeleton, options, false, false, preserveInactiveViewportAnchor, layoutRequestId, true),
			onComplete: () => {
				if (layoutRequestId === this._layoutRequestId) this._pendingWorkerEditBatch = null;
			}
		}, (error) => this._handleWorkerLayoutFailure(layoutRequestId, unitId, skeleton, options, workerOptions, protectedRange, mainThreadCallbacks, preserveInactiveViewportAnchor, allowRecovery, error, preserveCompletedLayout), preserveCompletedLayout);
	}
	_handleWorkerLayoutFailure(layoutRequestId, unitId, skeleton, options, workerOptions, protectedRange, mainThreadCallbacks, preserveInactiveViewportAnchor, allowRecovery, error, preserveCompletedLayout) {
		if (!allowRecovery) {
			this._logService.error("[DocRenderController]: Worker layout failed; using main-thread layout.", error);
			this._layoutCoordinator.schedule(skeleton, options, mainThreadCallbacks);
			return;
		}
		this._recoveryViewportAnchor = this._captureViewportAnchor(skeleton, options.priorityAnchor ?? options.anchor, options.invalidation);
		const diagnostic = error instanceof Error ? error.message : String(error);
		this._logService.warn("[DocRenderController]: restarting the document layout Worker after a fatal layout failure.", error);
		this._docLayoutExecutorService.recoverExecutor(unitId, diagnostic).then(() => {
			this._scheduleWorkerLayout(layoutRequestId, unitId, skeleton, options, workerOptions, protectedRange, mainThreadCallbacks, preserveInactiveViewportAnchor, false, preserveCompletedLayout);
		}).catch((recoveryError) => {
			this._logService.error("[DocRenderController]: document layout Worker recovery failed; using main-thread layout.", recoveryError);
			this._layoutCoordinator.schedule(skeleton, options, mainThreadCallbacks);
		});
	}
	_accumulateWorkerEditBatch(options) {
		const pending = this._pendingWorkerEditBatch;
		this._pendingWorkerEditBatch = pending == null ? {
			anchor: options.anchor,
			invalidation: options.invalidation
		} : {
			anchor: pending.anchor == null || options.anchor == null ? void 0 : Math.min(pending.anchor, options.anchor),
			invalidation: mergeDocumentLayoutInvalidations(pending.invalidation, options.invalidation)
		};
		return {
			...options,
			anchor: this._pendingWorkerEditBatch.anchor,
			priorityAnchor: this._pendingWorkerEditBatch.anchor,
			invalidation: this._pendingWorkerEditBatch.invalidation
		};
	}
	_initInteractionLayoutProtection() {
		this._docLayoutInteractionService.active$.pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe((active) => {
			if (active) {
				var _this$_pendingWorkerH;
				if (this._workerPresentationResumeTimer != null) {
					clearTimeout(this._workerPresentationResumeTimer);
					this._workerPresentationResumeTimer = null;
				}
				this._layoutCoordinator.setWorkerPresentationPaused(true);
				if (((_this$_pendingWorkerH = this._pendingWorkerHandoff) === null || _this$_pendingWorkerH === void 0 ? void 0 : _this$_pendingWorkerH.waitForInteractionEnd) && this._workerHandoffTimer != null) {
					clearTimeout(this._workerHandoffTimer);
					this._workerHandoffTimer = null;
				}
				return;
			}
			this._workerPresentationResumeTimer = setTimeout(() => {
				this._workerPresentationResumeTimer = null;
				if (!this._docLayoutInteractionService.isActive) this._layoutCoordinator.setWorkerPresentationPaused(false);
			}, EDIT_WORKER_RESUME_DELAY_MS);
			this._startWorkerHandoffTimer();
		});
		this._docSelectionRenderService.onCompositionstart$.pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe((config) => {
			if (config == null) return;
			this._isImeComposing = true;
			if (this._workerHandoffTimer != null) {
				clearTimeout(this._workerHandoffTimer);
				this._workerHandoffTimer = null;
			}
			if (this._pendingWorkerHandoff == null && this._layoutCoordinator.hasScheduledLayout()) this._pendingImeLayoutRestart = this._latestLayoutRestart;
			this._layoutCoordinator.cancel();
		});
		this._docSelectionRenderService.onCompositionend$.pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe((config) => {
			if (config == null) return;
			this._isImeComposing = false;
			const restartLayout = this._pendingImeLayoutRestart;
			this._pendingImeLayoutRestart = null;
			if (restartLayout != null) {
				restartLayout();
				return;
			}
			this._startWorkerHandoffTimer();
		});
	}
	_queueWorkerHandoff(layoutRequestId, run, waitForInteractionEnd = false) {
		this._cancelWorkerHandoff();
		this._pendingWorkerHandoff = {
			layoutRequestId,
			run,
			waitForInteractionEnd
		};
		this._startWorkerHandoffTimer();
	}
	_startWorkerHandoffTimer() {
		if (this._pendingWorkerHandoff == null || this._workerHandoffTimer != null || this._isImeComposing || this._pendingWorkerHandoff.waitForInteractionEnd && this._docLayoutInteractionService.isActive) return;
		this._workerHandoffTimer = setTimeout(() => {
			this._workerHandoffTimer = null;
			const handoff = this._pendingWorkerHandoff;
			if (handoff == null) return;
			if (handoff.layoutRequestId !== this._layoutRequestId) {
				this._pendingWorkerHandoff = null;
				return;
			}
			if (this._isImeComposing || handoff.waitForInteractionEnd && this._docLayoutInteractionService.isActive || this._docSelectionRenderService.isOnPointerEvent) {
				this._startWorkerHandoffTimer();
				return;
			}
			this._pendingWorkerHandoff = null;
			handoff.run();
		}, EDIT_WORKER_RESUME_DELAY_MS);
	}
	_deferWorkerHandoff() {
		if (this._pendingWorkerHandoff == null) return;
		if (this._workerHandoffTimer != null) {
			clearTimeout(this._workerHandoffTimer);
			this._workerHandoffTimer = null;
		}
		this._startWorkerHandoffTimer();
	}
	_cancelWorkerHandoff() {
		if (this._workerHandoffTimer != null) {
			clearTimeout(this._workerHandoffTimer);
			this._workerHandoffTimer = null;
		}
		this._pendingWorkerHandoff = null;
	}
	_resolveProtectedRange(skeleton, anchor, progress) {
		var _skeleton$getSkeleton2, _skeleton$findNodePos, _pages$startPageIndex, _pages$startPageIndex2;
		if (progress.mode === "continuous") {
			if (progress.stableLaidOutThrough < 0) return;
			return {
				mode: "continuous",
				startOffset: Math.max(0, Math.min(anchor ?? progress.stableLaidOutThrough, progress.stableLaidOutThrough)),
				endOffset: progress.stableLaidOutThrough
			};
		}
		const pages = (_skeleton$getSkeleton2 = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton2 === void 0 ? void 0 : _skeleton$getSkeleton2.pages;
		if (pages == null || pages.length === 0 || progress.publishedPageCount <= 0) return;
		const publishedEndPageIndex = Math.min(progress.publishedPageCount, pages.length) - 1;
		let startPageIndex = anchor == null ? Math.max(0, publishedEndPageIndex - 4) : ((_skeleton$findNodePos = skeleton.findNodePositionByCharIndex(anchor)) === null || _skeleton$findNodePos === void 0 ? void 0 : _skeleton$findNodePos.page) ?? -1;
		if (startPageIndex < 0 || startPageIndex > publishedEndPageIndex) startPageIndex = Math.max(0, publishedEndPageIndex - 4);
		while (startPageIndex <= publishedEndPageIndex && (((_pages$startPageIndex = pages[startPageIndex]) === null || _pages$startPageIndex === void 0 ? void 0 : _pages$startPageIndex.isLayoutPlaceholder) || ((_pages$startPageIndex2 = pages[startPageIndex]) === null || _pages$startPageIndex2 === void 0 ? void 0 : _pages$startPageIndex2.isMaterializationPlaceholder))) startPageIndex++;
		if (startPageIndex > publishedEndPageIndex) return;
		return {
			mode: "paginated",
			startPageIndex,
			endPageIndex: publishedEndPageIndex
		};
	}
	_resolveMaterializedPageRange(skeleton, publishedPageCount) {
		var _skeleton$getSkeleton3;
		if (this._context.unit.getSnapshot().documentStyle.documentFlavor !== _univerjs_core.DocumentFlavor.TRADITIONAL) return;
		const pages = (_skeleton$getSkeleton3 = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton3 === void 0 ? void 0 : _skeleton$getSkeleton3.pages;
		const docsComponent = this._context.mainComponent;
		const pageAxis = resolveMaterializedPageAxis(docsComponent, this._context.scene.getViewport("viewMain"));
		if (pages == null || pages.length === 0 || pageAxis == null) return;
		const { pageGap, vertical, viewportCenter } = pageAxis;
		let pageStart = 0;
		let pageIndex = 0;
		for (; pageIndex < pages.length; pageIndex++) {
			const page = pages[pageIndex];
			const pageExtent = vertical ? page.pageHeight : page.pageWidth;
			if (viewportCenter <= pageStart + pageExtent) break;
			pageStart += pageExtent + pageGap;
		}
		const availablePageCount = Math.max(pages.length, publishedPageCount ?? 0);
		if (pageIndex >= pages.length && availablePageCount > pages.length) {
			const lastPage = pages.at(-1);
			const estimatedExtent = vertical ? lastPage === null || lastPage === void 0 ? void 0 : lastPage.pageHeight : lastPage === null || lastPage === void 0 ? void 0 : lastPage.pageWidth;
			if (estimatedExtent != null && estimatedExtent + pageGap > 0) pageIndex += Math.floor(Math.max(0, viewportCenter - pageStart) / (estimatedExtent + pageGap));
		}
		pageIndex = Math.max(0, Math.min(pageIndex, availablePageCount - 1));
		const preferredStart = Math.max(0, pageIndex - 1);
		const endPageIndex = Math.min(availablePageCount - 1, preferredStart + MATERIALIZED_PAGE_WINDOW_SIZE - 1);
		return {
			startPageIndex: Math.max(0, endPageIndex - MATERIALIZED_PAGE_WINDOW_SIZE + 1),
			endPageIndex
		};
	}
	_queueMaterializedPageRange(range) {
		this._pendingMaterializedPageRange = range;
		if (this._isMaterializingPages) return;
		this._isMaterializingPages = true;
		this._materializePendingPageRanges().catch((error) => {
			this._logService.warn("[DocRenderController]: failed to materialize a document page.", error);
		}).finally(() => {
			this._isMaterializingPages = false;
			if (this._pendingMaterializedPageRange != null) this._queueMaterializedPageRange(this._pendingMaterializedPageRange);
		});
	}
	async _materializePendingPageRanges() {
		while (this._pendingMaterializedPageRange != null) {
			const range = this._pendingMaterializedPageRange;
			this._pendingMaterializedPageRange = null;
			const requestId = this._layoutRequestId;
			const skeleton = this._docSkeletonManagerService.getSkeleton();
			if (skeleton == null) return;
			for (let pageIndex = range.startPageIndex; pageIndex <= range.endPageIndex; pageIndex++) {
				var _skeleton$getSkeleton4;
				if (!((_skeleton$getSkeleton4 = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton4 === void 0 || (_skeleton$getSkeleton4 = _skeleton$getSkeleton4.pages[pageIndex]) === null || _skeleton$getSkeleton4 === void 0 ? void 0 : _skeleton$getSkeleton4.isMaterializationPlaceholder)) continue;
				const result = await this._layoutCoordinator.getWorkerPage(pageIndex);
				if (requestId !== this._layoutRequestId || result == null) return;
				if (result.page == null) continue;
				const hydrationStartedAt = _univerjs_core.Tools.now();
				try {
					skeleton.applyLayoutPagePublication(result.page, range);
				} finally {
					this._docLayoutExecutorService.recordHydrationDuration(this._context.unitId, _univerjs_core.Tools.now() - hydrationStartedAt);
				}
				this._markDocumentRenderDirty();
			}
		}
	}
	_captureViewportAnchor(skeleton, offset, invalidation) {
		if (offset == null) return null;
		const previousOffset = invalidation == null || offset < invalidation.oldStart ? offset : offset < invalidation.newEnd ? invalidation.oldStart : offset - (invalidation.newEnd - invalidation.oldEnd);
		const position = skeleton.findNodePositionByCharIndex(previousOffset);
		const docsComponent = this._context.mainComponent;
		const viewport = this._context.scene.getViewport("viewMain");
		if (position == null || !(docsComponent instanceof _univerjs_engine_render.Documents) || viewport == null) return null;
		const offsetConfig = docsComponent.getOffsetConfig();
		const { contentBoxPointGroup } = new NodePositionConvertToCursor(offsetConfig, skeleton).getRangePointData(position, position);
		const anchor = getAnchorBounding(contentBoxPointGroup);
		const viewBound = viewport.calcViewportInfo().viewBound;
		return {
			offset,
			relativeLeft: anchor.left + offsetConfig.docsLeft - viewBound.left,
			relativeTop: anchor.top + offsetConfig.docsTop - viewBound.top
		};
	}
	_restoreRecoveryViewportAnchor(skeleton) {
		const captured = this._recoveryViewportAnchor;
		if (captured == null) return;
		this._recoveryViewportAnchor = null;
		this._restoreViewportAnchor(skeleton, captured);
	}
	_restoreViewportAnchor(skeleton, captured) {
		if (captured == null) return;
		const position = skeleton.findNodePositionByCharIndex(captured.offset);
		const docsComponent = this._context.mainComponent;
		const viewport = this._context.scene.getViewport("viewMain");
		if (position == null || !(docsComponent instanceof _univerjs_engine_render.Documents) || viewport == null) return;
		const offsetConfig = docsComponent.getOffsetConfig();
		const { contentBoxPointGroup } = new NodePositionConvertToCursor(offsetConfig, skeleton).getRangePointData(position, position);
		const anchor = getAnchorBounding(contentBoxPointGroup);
		const viewBound = viewport.calcViewportInfo().viewBound;
		viewport.scrollByViewportDeltaVal({
			viewportScrollX: anchor.left + offsetConfig.docsLeft - viewBound.left - captured.relativeLeft,
			viewportScrollY: anchor.top + offsetConfig.docsTop - viewBound.top - captured.relativeTop
		});
	}
	_addNewRender() {
		const { scene, engine } = this._context;
		const viewMain = new _univerjs_engine_render.Viewport("viewMain", scene, {
			left: 0,
			top: 0,
			bottom: 0,
			right: 0,
			isWheelPreventDefaultX: true
		});
		scene.attachControl();
		this._initViewportInteraction(viewMain);
		new _univerjs_engine_render.ScrollBar(viewMain, { enableHorizontal: this._shouldEnableHorizontalScrollBar() });
		scene.addLayer(new _univerjs_engine_render.Layer(scene, [], 2), new _univerjs_engine_render.Layer(scene, [], 4));
		this._addComponent();
		const frameFn = () => scene.render();
		this.disposeWithMe(this._context.activated$.subscribe((activated) => {
			if (activated) engine.runRenderLoop(frameFn);
			else engine.stopRenderLoop(frameFn);
		}));
		this._docSelectionRenderService.__attachScrollEvent();
	}
	_initViewportInteraction(viewMain) {
		var _scene$onPointerDown$;
		const { scene } = this._context;
		const pointerDownSubscription = (_scene$onPointerDown$ = scene.onPointerDown$) === null || _scene$onPointerDown$ === void 0 ? void 0 : _scene$onPointerDown$.subscribeEvent(() => {
			this._deferWorkerHandoff();
			this._layoutCoordinator.deferBackgroundWork();
		});
		if (pointerDownSubscription != null) this.disposeWithMe(pointerDownSubscription);
		this.disposeWithMe(viewMain.onScrollAfter$.subscribeEvent(() => {
			const skeleton = this._docSkeletonManagerService.getSkeleton();
			const range = skeleton == null ? void 0 : this._resolveMaterializedPageRange(skeleton);
			if (range != null) this._queueMaterializedPageRange(range);
		}));
		if (typeof document !== "undefined") this.disposeWithMe((0, rxjs.merge)((0, rxjs.fromEvent)(document, "keydown", { capture: true }), (0, rxjs.fromEvent)(document, "beforeinput", { capture: true })).pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe(() => {
			const currentDocUnit = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
			if ((currentDocUnit === null || currentDocUnit === void 0 ? void 0 : currentDocUnit.getUnitId()) === this._context.unitId) {
				this._deferWorkerHandoff();
				this._layoutCoordinator.deferBackgroundWork();
			}
		}));
		scene.onMouseWheel$.subscribeEvent((event, state) => {
			const currentDocUnit = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
			if ((currentDocUnit === null || currentDocUnit === void 0 ? void 0 : currentDocUnit.getUnitId()) !== this._context.unitId) return;
			this._deferWorkerHandoff();
			this._layoutCoordinator.deferBackgroundWork();
			if (event.ctrlKey) {
				const deltaFactor = Math.abs(event.deltaX);
				let scrollNum = deltaFactor < 40 ? .2 : deltaFactor < 80 ? .4 : .2;
				scrollNum *= event.deltaY > 0 ? -1 : 1;
				if (scene.scaleX < 1) scrollNum /= 2;
				if (scene.scaleX + scrollNum > 4) scene.scale(4, 4);
				else if (scene.scaleX + scrollNum < .1) scene.scale(.1, .1);
				else event.preventDefault();
			} else viewMain.onMouseWheel(event, state);
		});
	}
	_shouldEnableHorizontalScrollBar() {
		const options = this._docViewScaleService.getOptions();
		return !(options.mode === "fit-width" && options.target === "container" && options.align === "start");
	}
	_addComponent() {
		const { scene, unit: documentModel, components } = this._context;
		const config = {
			pageMarginLeft: 20,
			pageMarginTop: 20,
			...this._getEditorBackgroundConfig()
		};
		const documents = new _univerjs_engine_render.Documents("__Document_Render_Main__", void 0, config);
		documents.zIndex = 10;
		const docBackground = new _univerjs_engine_render.DocBackground("__Document_Render_Background__", void 0, config);
		docBackground.zIndex = 10;
		this._context.mainComponent = documents;
		components.set("__Document_Render_Main__", documents);
		components.set("__Document_Render_Background__", docBackground);
		scene.addObjects([documents], 2);
		scene.addObjects([docBackground], 0);
		if (!this._isEditorRenderUnit(documentModel.getUnitId())) scene.enableLayerCache(2);
	}
	_initRenderRefresh() {
		this._docSkeletonManagerService.currentSkeletonBefore$.pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe((param) => {
			this._create(param);
		});
	}
	_create(skeleton) {
		if (!skeleton) return;
		const { mainComponent, components } = this._context;
		const docsComponent = mainComponent;
		const docBackground = components.get("__Document_Render_Background__");
		docsComponent.changeSkeleton(skeleton);
		docBackground.changeSkeleton(skeleton);
		this._syncCanvasBackground();
		const { unitId } = this._context;
		if (skeleton.getSkeletonData() == null) {
			this._scheduleLayout(unitId, skeleton, { reason: "initial" });
			return;
		}
		const editorRenderConfig = this._editorService.getEditorRenderConfig(unitId);
		if (editorRenderConfig && !editorRenderConfig.scrollBar) {
			var _this$_context$mainCo2;
			(_this$_context$mainCo2 = this._context.mainComponent) === null || _this$_context$mainCo2 === void 0 || _this$_context$mainCo2.makeDirty();
			return;
		}
		this._recalculateSizeBySkeleton(skeleton);
		this._refreshPagePositionAndSelection();
	}
	_initCommandListener() {
		const updateCommandList = [_univerjs_docs.RichTextEditingMutation.id];
		this.disposeWithMe(this._commandService.onCommandExecuted((command, executionOptions) => {
			if (!updateCommandList.includes(command.id)) return;
			const params = command.params;
			if (!isRichTextEditingMutationParams(params)) return;
			const { unitId, textRanges } = params;
			if (unitId !== this._context.unitId) return;
			const snapshot = this._context.unit.getSnapshot();
			if (!doesDocMutationRequireLayout(params.actions, snapshot.drawings)) return;
			const bodyRanges = (textRanges === null || textRanges === void 0 ? void 0 : textRanges.filter((range) => !range.segmentId)) ?? [];
			const { anchor, invalidation } = resolveMutationLayoutRequest(getBodyMutationInvalidation(params.actions, params.segmentId), getDocumentMutationLayoutImpact(params.actions, snapshot), bodyRanges.map((range) => range.startOffset));
			if (executionOptions === null || executionOptions === void 0 ? void 0 : executionOptions.fromChangeset) {
				this._scheduleChangesetRender(anchor, invalidation);
				return;
			}
			const activeRange = this._getActiveEditingRange(unitId);
			const isRemoteMutation = params.isSync === true || (executionOptions === null || executionOptions === void 0 ? void 0 : executionOptions.fromCollab) === true;
			const mutationActiveRange = isRemoteMutation ? void 0 : bodyRanges.find((range) => range.isActive) ?? (bodyRanges.length === 1 ? bodyRanges[0] : void 0);
			const priorityAnchor = (mutationActiveRange === null || mutationActiveRange === void 0 ? void 0 : mutationActiveRange.endOffset) ?? (activeRange === null || activeRange === void 0 ? void 0 : activeRange.endOffset);
			const hasPendingLocalSelectionUpdate = doesMutationScheduleLocalSelectionUpdate(params, isRemoteMutation);
			if (invalidation != null && isLayoutMetadataOnlyMutation(params.actions, params.segmentId)) {
				var _this$_renderManagerS3, _skeleton$getSkeleton5;
				const manager = (_this$_renderManagerS3 = this._renderManagerService.getRenderUnitById(unitId)) === null || _this$_renderManagerS3 === void 0 ? void 0 : _this$_renderManagerS3.with(_univerjs_docs.DocSkeletonManagerService);
				const skeleton = manager === null || manager === void 0 ? void 0 : manager.getSkeleton();
				const pages = skeleton === null || skeleton === void 0 || (_skeleton$getSkeleton5 = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton5 === void 0 ? void 0 : _skeleton$getSkeleton5.pages;
				const affectedOffsets = [invalidation.oldStart, Math.max(invalidation.oldStart, invalidation.oldEnd - 1)];
				const affectedPagesAreMaterialized = skeleton != null && pages != null && affectedOffsets.every((offset) => {
					var _skeleton$findNodePos2;
					const pageIndex = ((_skeleton$findNodePos2 = skeleton.findNodePositionByCharIndex(offset)) === null || _skeleton$findNodePos2 === void 0 ? void 0 : _skeleton$findNodePos2.page) ?? -1;
					const page = pages[pageIndex];
					return page != null && !page.isLayoutPlaceholder && !page.isMaterializationPlaceholder;
				});
				if ((manager === null || manager === void 0 ? void 0 : manager.supportsIncrementalLayout()) && this._context.unit.documentStyle.documentFlavor === _univerjs_core.DocumentFlavor.TRADITIONAL && !this._context.unit.getSnapshot().disabled && (skeleton === null || skeleton === void 0 ? void 0 : skeleton.hasCompleteLayout()) && skeleton.getLayoutProgress() == null && pages != null && pages.length > 0 && pages.every((page) => !page.isLayoutPlaceholder) && affectedPagesAreMaterialized) {
					this._scheduleLayout(unitId, skeleton, {
						reason: "edit",
						anchor,
						priorityAnchor,
						invalidation,
						reuseMainBaseline: true,
						allowMetadataOnlyStructuralTailReuse: true
					}, !hasPendingLocalSelectionUpdate, isRemoteMutation);
					return;
				}
			}
			this.reRender(unitId, anchor, invalidation, priorityAnchor, !hasPendingLocalSelectionUpdate, isRemoteMutation, Array.isArray(params.actions) && params.actions.length > 0 && bodyRanges.length === 0 && invalidation == null);
		}));
	}
	_scheduleChangesetRender(anchor, invalidation) {
		const pending = this._pendingChangesetLayoutRequest;
		this._pendingChangesetLayoutRequest = pending == null ? {
			anchor,
			invalidation
		} : {
			anchor: pending.anchor == null || anchor == null ? void 0 : Math.min(pending.anchor, anchor),
			invalidation: mergeDocumentLayoutInvalidations(pending.invalidation, invalidation)
		};
		if (this._changesetRenderScheduled) return;
		this._changesetRenderScheduled = true;
		queueMicrotask(() => {
			this._changesetRenderScheduled = false;
			if (this._disposed) return;
			const request = this._pendingChangesetLayoutRequest;
			this._pendingChangesetLayoutRequest = null;
			this.reRender(this._context.unitId, request === null || request === void 0 ? void 0 : request.anchor, request === null || request === void 0 ? void 0 : request.invalidation);
		});
	}
	_initThemeListener() {
		this.disposeWithMe((0, rxjs.combineLatest)([this._themeService.currentTheme$, this._themeService.darkMode$]).pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe(() => {
			this._syncCanvasBackground();
			this._markDocumentRenderDirty();
		}));
	}
	_markDocumentRenderDirty() {
		var _this$_context$mainCo3, _this$_context$compon2;
		(_this$_context$mainCo3 = this._context.mainComponent) === null || _this$_context$mainCo3 === void 0 || _this$_context$mainCo3.makeDirty(true);
		(_this$_context$compon2 = this._context.components.get("__Document_Render_Background__")) === null || _this$_context$compon2 === void 0 || _this$_context$compon2.makeDirty(true);
	}
	_refreshPagePositionAndSelection(isEditing = false) {
		this._refreshPagePosition();
		if (this._isEditorRenderUnit()) return;
		const unitId = this._context.unitId;
		const activeRange = this._getActiveEditingRange(unitId);
		this._textSelectionManagerService.refreshSelection({
			unitId,
			subUnitId: unitId
		}, isEditing && activeRange != null);
	}
	_getFocusedSelectionInfo(unitId) {
		var _this$_univerInstance;
		if (((_this$_univerInstance = this._univerInstanceService.getFocusedUnit()) === null || _this$_univerInstance === void 0 ? void 0 : _this$_univerInstance.getUnitId()) !== unitId) return;
		return this._textSelectionManagerService.getSelectionInfo({
			unitId,
			subUnitId: unitId
		});
	}
	_getActiveEditingRange(unitId) {
		const selectionInfo = this._getFocusedSelectionInfo(unitId);
		if ((selectionInfo === null || selectionInfo === void 0 ? void 0 : selectionInfo.isEditing) !== true) return;
		return selectionInfo.textRanges.find((range) => range.isActive);
	}
	_getActiveRange(unitId) {
		const selectionInfo = this._getFocusedSelectionInfo(unitId);
		return selectionInfo === null || selectionInfo === void 0 ? void 0 : selectionInfo.textRanges.find((range) => range.isActive);
	}
	_captureCompletionViewportAnchor(unitId, skeleton, shouldCapture, preserveInactiveViewportAnchor) {
		if (!shouldCapture || this._recoveryViewportAnchor != null) return null;
		const selectionInfo = this._getFocusedSelectionInfo(unitId);
		if ((selectionInfo === null || selectionInfo === void 0 ? void 0 : selectionInfo.isEditing) !== true && !preserveInactiveViewportAnchor) return null;
		const activeRange = selectionInfo === null || selectionInfo === void 0 ? void 0 : selectionInfo.textRanges.find((range) => range.isActive);
		if (activeRange == null || activeRange.segmentId) return null;
		return this._captureViewportAnchor(skeleton, activeRange.endOffset, void 0);
	}
	_refreshPagePosition() {
		if (this._isEditorRenderUnit()) return;
		this._docPageLayoutService.calculatePagePosition();
	}
	_recalculateSizeBySkeleton(skeleton, progress) {
		var _skeleton$getSkeleton6, _skeleton$getLayoutPr;
		const { mainComponent, scene, unitId, components } = this._context;
		const docsComponent = mainComponent;
		const docBackground = components.get("__Document_Render_Background__");
		const pages = (_skeleton$getSkeleton6 = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton6 === void 0 ? void 0 : _skeleton$getSkeleton6.pages;
		if (pages == null) return;
		const documentFlavor = this._context.unit.getSnapshot().documentStyle.documentFlavor;
		this._syncCanvasBackground(documentFlavor);
		const measured = this._measureDocumentExtent(pages, docsComponent, documentFlavor);
		const layoutProgress = progress ?? ((_skeleton$getLayoutPr = skeleton.getLayoutProgress) === null || _skeleton$getLayoutPr === void 0 ? void 0 : _skeleton$getLayoutPr.call(skeleton)) ?? null;
		const { width, height } = this._reserveDocumentExtent(measured, pages, docsComponent, documentFlavor, layoutProgress);
		docsComponent.resize(width, height);
		docBackground.resize(width, height);
		const editorRenderConfig = this._editorService.getEditorRenderConfig(unitId);
		if ((!editorRenderConfig || editorRenderConfig.scrollBar) && (scene.width !== width || scene.height !== height)) scene.transformByState({
			width,
			height
		});
	}
	_measureDocumentExtent(pages, docsComponent, documentFlavor) {
		let width = 0;
		let height = 0;
		for (let i = 0, len = pages.length; i < len; i++) {
			const page = pages[i];
			let { pageWidth, pageHeight } = page;
			if (documentFlavor === _univerjs_core.DocumentFlavor.MODERN) {
				const modernPageSize = getPageSizeInModernMode(page);
				pageWidth = modernPageSize.pageWidth;
				pageHeight = modernPageSize.pageHeight;
			}
			if (docsComponent.pageLayoutType === _univerjs_engine_render.PageLayoutType.VERTICAL) {
				height += pageHeight;
				height += docsComponent.pageMarginTop;
				if (i === len - 1) height += docsComponent.pageMarginTop;
				width = Math.max(width, pageWidth);
			} else if (docsComponent.pageLayoutType === _univerjs_engine_render.PageLayoutType.HORIZONTAL) {
				width += pageWidth;
				if (i !== len - 1) width += docsComponent.pageMarginLeft;
				height = Math.max(height, pageHeight);
			}
		}
		return {
			width,
			height
		};
	}
	_reserveDocumentExtent(measured, pages, docsComponent, documentFlavor, layoutProgress) {
		let { width, height } = measured;
		if (layoutProgress != null && !layoutProgress.complete && pages.length > 0) {
			const firstPage = pages[0];
			if (documentFlavor === _univerjs_core.DocumentFlavor.MODERN) this._reservedLayoutHeight = Math.max(this._reservedLayoutHeight, layoutProgress.estimatedHeight + docsComponent.pageMarginTop * 2);
			else if (docsComponent.pageLayoutType === _univerjs_engine_render.PageLayoutType.VERTICAL) {
				const estimatedHeight = layoutProgress.estimatedPageCount * (firstPage.pageHeight + docsComponent.pageMarginTop) + docsComponent.pageMarginTop;
				this._reservedLayoutHeight = Math.max(this._reservedLayoutHeight, estimatedHeight);
			} else {
				const estimatedWidth = layoutProgress.estimatedPageCount * (firstPage.pageWidth + docsComponent.pageMarginLeft);
				this._reservedLayoutWidth = Math.max(this._reservedLayoutWidth, estimatedWidth);
			}
			if (documentFlavor === _univerjs_core.DocumentFlavor.MODERN || docsComponent.pageLayoutType === _univerjs_engine_render.PageLayoutType.VERTICAL) height = Math.max(height, this._reservedLayoutHeight);
			else width = Math.max(width, this._reservedLayoutWidth);
		} else {
			this._reservedLayoutWidth = width;
			this._reservedLayoutHeight = height;
		}
		return {
			width,
			height
		};
	}
	_syncCanvasBackground(documentFlavor = this._context.unit.getSnapshot().documentStyle.documentFlavor) {
		var _docBackground$setFil;
		const editorRenderConfig = this._editorService.getEditorRenderConfig(this._context.unitId);
		const editorBackgroundColor = editorRenderConfig === null || editorRenderConfig === void 0 ? void 0 : editorRenderConfig.canvasStyle.backgroundColor;
		const resolvedBackground = resolveDocRenderBackground({
			documentFlavor,
			canvasColorService: this._context.engine.canvasColorService,
			editorBackgroundColor,
			isEditor: this._isEditorRenderUnit()
		});
		this._context.engine.getCanvas().getCanvasEle().style.backgroundColor = resolvedBackground.canvasElementBackgroundColor;
		const docBackground = this._context.components.get("__Document_Render_Background__");
		docBackground === null || docBackground === void 0 || (_docBackground$setFil = docBackground.setFillColors) === null || _docBackground$setFil === void 0 || _docBackground$setFil.call(docBackground, resolvedBackground.docBackgroundFillColor, resolvedBackground.docBackgroundFillColor, resolvedBackground.docBackgroundFillColor, resolvedBackground.docBackgroundFillColor);
	}
	_getEditorBackgroundConfig() {
		if (!this._isEditorRenderUnit()) return {};
		const editorBackgroundColor = "transparent";
		return {
			backgroundFillColor: editorBackgroundColor,
			pageFillColor: editorBackgroundColor,
			pageStrokeColor: editorBackgroundColor,
			marginStrokeColor: editorBackgroundColor
		};
	}
	_isEditorRenderUnit(unitId = this._context.unitId) {
		return this._editorService.isEditor(unitId) || (0, _univerjs_core.isInternalEditorID)(unitId);
	}
};
DocRenderController = __decorate([
	__decorateParam(1, _univerjs_core.ICommandService),
	__decorateParam(2, (0, _univerjs_core.Inject)(DocSelectionRenderService)),
	__decorateParam(3, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService)),
	__decorateParam(4, IEditorService),
	__decorateParam(5, _univerjs_engine_render.IRenderManagerService),
	__decorateParam(6, _univerjs_core.IUniverInstanceService),
	__decorateParam(7, (0, _univerjs_core.Inject)(DocPageLayoutService)),
	__decorateParam(8, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(9, (0, _univerjs_core.Inject)(DocViewScaleService)),
	__decorateParam(10, (0, _univerjs_core.Inject)(_univerjs_core.ThemeService)),
	__decorateParam(11, (0, _univerjs_core.Inject)(_univerjs_docs.DocLayoutExecutorService)),
	__decorateParam(12, (0, _univerjs_core.Inject)(DocLayoutInteractionService)),
	__decorateParam(13, _univerjs_core.ILogService)
], DocRenderController);
function getPageSizeInModernMode(page) {
	let { pageWidth, pageHeight } = page;
	const { marginLeft, marginRight, marginTop, marginBottom, skeDrawings, skeTables } = page;
	if (pageWidth === Number.POSITIVE_INFINITY) pageWidth = page.width + marginLeft + marginRight;
	if (pageHeight === Number.POSITIVE_INFINITY) pageHeight = page.height + marginTop + marginBottom;
	for (const drawing of skeDrawings.values()) pageHeight = Math.max(pageHeight, drawing.aTop + drawing.height + marginTop + marginBottom);
	for (const table of skeTables.values()) pageHeight = Math.max(pageHeight, table.top + table.height + marginTop + marginBottom);
	return {
		pageWidth,
		pageHeight
	};
}

//#endregion
//#region src/basics/selection.ts
function findFirstCursorOffset(snapshot) {
	const { dataStream } = snapshot.body ?? {};
	const EXCLUDE_TOKENS = [
		_univerjs_core.DataStreamTreeTokenType.TABLE_START,
		_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_END,
		_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_START,
		_univerjs_core.DataStreamTreeTokenType.TABLE_END,
		_univerjs_core.DataStreamTreeTokenType.TABLE_ROW_END,
		_univerjs_core.DataStreamTreeTokenType.TABLE_ROW_START,
		_univerjs_core.DataStreamTreeTokenType.COLUMN_BREAK,
		_univerjs_core.DataStreamTreeTokenType.PAGE_BREAK,
		_univerjs_core.DataStreamTreeTokenType.TAB,
		_univerjs_core.DataStreamTreeTokenType.DOCS_END,
		_univerjs_core.DataStreamTreeTokenType.CUSTOM_BLOCK
	];
	if (typeof dataStream === "string") for (let i = 0; i < dataStream.length; i++) {
		const char = dataStream[i];
		if (!EXCLUDE_TOKENS.includes(char)) return i;
	}
	return 0;
}

//#endregion
//#region src/utils/section-header-footer.ts
function getDocPageSectionContext(snapshot, page) {
	const sections = snapshot.body ? (0, _univerjs_docs.getTopLevelSectionBreaks)(snapshot.body) : [];
	const sectionIndex = (page === null || page === void 0 ? void 0 : page.sectionId) == null ? -1 : sections.findIndex((item) => item.sectionId === page.sectionId);
	const section = sectionIndex < 0 ? void 0 : sections[sectionIndex];
	const references = section == null ? {} : (0, _univerjs_core.resolveSectionHeaderFooterReferences)(snapshot.documentStyle, sections, sectionIndex);
	return {
		sectionId: section === null || section === void 0 ? void 0 : section.sectionId,
		sectionIndex,
		sections,
		section,
		config: {
			...snapshot.documentStyle,
			...section,
			...references
		}
	};
}
function getHeaderFooterTarget(viewModel, editArea, segmentPage, page) {
	const { sectionId, config } = getDocPageSectionContext(viewModel.getDataModel().getSnapshot(), page);
	const { defaultHeaderId, defaultFooterId, evenPageHeaderId, evenPageFooterId, firstPageHeaderId, firstPageFooterId, evenAndOddHeaders, useFirstPageHeaderFooter } = config;
	const isFirstPage = page ? page.pageNumber === page.pageNumberStart : segmentPage === 0;
	const isEvenPage = page ? page.pageNumber % 2 === 0 : segmentPage % 2 === 1;
	if (editArea === _univerjs_engine_render.DocumentEditArea.BODY) return {
		createType: null,
		headerFooterId: null,
		sectionId
	};
	const isHeader = editArea === _univerjs_engine_render.DocumentEditArea.HEADER;
	if (!isHeader && editArea !== _univerjs_engine_render.DocumentEditArea.FOOTER) throw new Error(`Invalid editArea: ${editArea}`);
	const variants = isHeader ? {
		first: [firstPageHeaderId, _univerjs_docs.HeaderFooterType.FIRST_PAGE_HEADER],
		even: [evenPageHeaderId, _univerjs_docs.HeaderFooterType.EVEN_PAGE_HEADER],
		default: [defaultHeaderId, _univerjs_docs.HeaderFooterType.DEFAULT_HEADER]
	} : {
		first: [firstPageFooterId, _univerjs_docs.HeaderFooterType.FIRST_PAGE_FOOTER],
		even: [evenPageFooterId, _univerjs_docs.HeaderFooterType.EVEN_PAGE_FOOTER],
		default: [defaultFooterId, _univerjs_docs.HeaderFooterType.DEFAULT_FOOTER]
	};
	const [headerFooterId, createType] = useFirstPageHeaderFooter === _univerjs_core.BooleanNumber.TRUE && isFirstPage ? variants.first : evenAndOddHeaders === _univerjs_core.BooleanNumber.TRUE && isEvenPage ? variants.even : variants.default;
	return {
		createType: headerFooterId ? null : createType,
		headerFooterId: headerFooterId ?? null,
		sectionId
	};
}

//#endregion
//#region src/views/header-footer/panel/component-name.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const COMPONENT_DOC_HEADER_FOOTER_PANEL = "COMPONENT_DOC_HEADER_FOOTER_PANEL";

//#endregion
//#region src/commands/operations/doc-header-footer-panel.operation.ts
const SidebarDocHeaderFooterPanelOperation = {
	id: "sidebar.operation.doc-header-footer-panel",
	type: _univerjs_core.CommandType.OPERATION,
	handler: async (accessor, params) => {
		const sidebarService = accessor.get(_univerjs_ui.ISidebarService);
		const localeService = accessor.get(_univerjs_core.LocaleService);
		switch (params.value) {
			case "open":
				sidebarService.open({
					header: { title: localeService.t("docs-ui.headerFooter.panel") },
					children: { label: COMPONENT_DOC_HEADER_FOOTER_PANEL },
					onClose: () => {},
					width: 400
				});
				break;
			default:
				sidebarService.close();
				break;
		}
		return true;
	}
};

//#endregion
//#region src/commands/commands/doc-header-footer.command.ts
const CoreHeaderFooterCommandId = "doc.command.core-header-footer";
/**
* The command to update header and footer or create them.
*/
const CoreHeaderFooterCommand = {
	id: CoreHeaderFooterCommandId,
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		var _renderManagerService;
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const renderManagerService = accessor.get(_univerjs_engine_render.IRenderManagerService);
		const { unitId, segmentId, createType, headerFooterProps, sectionId: requestedSectionId } = params;
		const docSkeletonManagerService = (_renderManagerService = renderManagerService.getRenderUnitById(unitId)) === null || _renderManagerService === void 0 ? void 0 : _renderManagerService.with(_univerjs_docs.DocSkeletonManagerService);
		const docDataModel = univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
		const docViewModel = docSkeletonManagerService === null || docSkeletonManagerService === void 0 ? void 0 : docSkeletonManagerService.getViewModel();
		if (docDataModel == null || docViewModel == null) return false;
		const editArea = docViewModel.getEditArea();
		const snapshot = docDataModel.getSnapshot();
		const sectionId = requestedSectionId;
		const sections = snapshot.body ? (0, _univerjs_docs.getTopLevelSectionBreaks)(snapshot.body) : [];
		const sectionIndex = sectionId == null ? -1 : sections.findIndex((section) => section.sectionId === sectionId);
		const section = sectionIndex < 0 ? void 0 : sections[sectionIndex];
		const headerFooterConfig = sectionId == null ? snapshot.documentStyle : {
			...snapshot.documentStyle,
			...section,
			...(0, _univerjs_core.resolveSectionHeaderFooterReferences)(snapshot.documentStyle, sections, sectionIndex)
		};
		let resolvedCreateType = createType;
		if (headerFooterProps != null) Object.keys(headerFooterProps).forEach((key) => {
			const value = headerFooterProps[key];
			if (resolvedCreateType == null && key === "useFirstPageHeaderFooter" && value === _univerjs_core.BooleanNumber.TRUE && !headerFooterConfig.firstPageHeaderId) resolvedCreateType = editArea === _univerjs_engine_render.DocumentEditArea.HEADER ? _univerjs_docs.HeaderFooterType.FIRST_PAGE_HEADER : _univerjs_docs.HeaderFooterType.FIRST_PAGE_FOOTER;
			else if (resolvedCreateType == null && key === "evenAndOddHeaders" && value === _univerjs_core.BooleanNumber.TRUE && !headerFooterConfig.evenPageHeaderId) resolvedCreateType = editArea === _univerjs_engine_render.DocumentEditArea.HEADER ? _univerjs_docs.HeaderFooterType.EVEN_PAGE_HEADER : _univerjs_docs.HeaderFooterType.EVEN_PAGE_FOOTER;
		});
		return commandService.syncExecuteCommand(_univerjs_docs.CreateHeaderFooterCommand.id, {
			unitId,
			segmentId,
			createType: resolvedCreateType,
			headerFooterProps,
			createMode: sectionId == null ? "pair" : "single",
			sectionId
		});
	}
};
const OpenHeaderFooterPanelCommand = {
	id: "doc.command.open-header-footer-panel",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, _params) => {
		var _skeleton$getSkeleton;
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const instanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const renderManagerService = accessor.get(_univerjs_engine_render.IRenderManagerService);
		const docDataModel = instanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null) return false;
		const unitId = docDataModel.getUnitId();
		const renderObject = renderManagerService.getRenderUnitById(unitId);
		const skeletonManager = renderObject === null || renderObject === void 0 ? void 0 : renderObject.with(_univerjs_docs.DocSkeletonManagerService);
		const viewModel = skeletonManager === null || skeletonManager === void 0 ? void 0 : skeletonManager.getViewModel();
		const skeleton = skeletonManager === null || skeletonManager === void 0 ? void 0 : skeletonManager.getSkeleton();
		const selectionRenderService = renderObject === null || renderObject === void 0 ? void 0 : renderObject.with(DocSelectionRenderService);
		const pages = skeleton === null || skeleton === void 0 || (_skeleton$getSkeleton = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton === void 0 ? void 0 : _skeleton$getSkeleton.pages;
		if (viewModel == null || selectionRenderService == null || !(pages === null || pages === void 0 ? void 0 : pages.length)) return false;
		const currentPage = selectionRenderService.getSegmentPage();
		const pageNumber = currentPage >= 0 && currentPage < pages.length ? currentPage : 0;
		const page = pages[pageNumber];
		viewModel.setEditArea(_univerjs_engine_render.DocumentEditArea.HEADER);
		const { createType, headerFooterId, sectionId } = getHeaderFooterTarget(viewModel, _univerjs_engine_render.DocumentEditArea.HEADER, pageNumber, page);
		selectionRenderService.setSegmentPage(pageNumber);
		if (createType != null) {
			const segmentId = (0, _univerjs_core.generateRandomId)(6);
			selectionRenderService.setSegment(segmentId);
			if (!await commandService.executeCommand(CoreHeaderFooterCommand.id, {
				unitId,
				createType,
				segmentId,
				sectionId
			})) {
				selectionRenderService.setSegment("");
				selectionRenderService.setSegmentPage(-1);
				viewModel.setEditArea(_univerjs_engine_render.DocumentEditArea.BODY);
				return false;
			}
		} else if (headerFooterId != null) selectionRenderService.setSegment(headerFooterId);
		return commandService.executeCommand(SidebarDocHeaderFooterPanelOperation.id, { value: "open" });
	}
};
const CloseHeaderFooterCommand = {
	id: "doc.command.close-header-footer",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, params) => {
		var _instanceService$getC, _skeleton$getSkeleton2, _renderObject$mainCom;
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const renderManagerService = accessor.get(_univerjs_engine_render.IRenderManagerService);
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const instanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const unitId = (params === null || params === void 0 ? void 0 : params.unitId) ?? ((_instanceService$getC = instanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC)) === null || _instanceService$getC === void 0 ? void 0 : _instanceService$getC.getUnitId());
		if (unitId == null) return false;
		const renderObject = renderManagerService.getRenderUnitById(unitId);
		if (renderObject == null) return false;
		const docSkeletonManagerService = renderObject.with(_univerjs_docs.DocSkeletonManagerService);
		const viewModel = docSkeletonManagerService === null || docSkeletonManagerService === void 0 ? void 0 : docSkeletonManagerService.getViewModel();
		if (viewModel == null) return false;
		if (viewModel.getEditArea() === _univerjs_engine_render.DocumentEditArea.BODY) return false;
		const skeleton = docSkeletonManagerService === null || docSkeletonManagerService === void 0 ? void 0 : docSkeletonManagerService.getSkeleton();
		if (skeleton == null) return false;
		const { scene } = renderObject;
		const transformer = scene.getTransformerByCreate();
		const docSelectionRenderService = renderObject.with(DocSelectionRenderService);
		docSelectionManagerService.replaceDocRanges([]);
		transformer.clearSelectedObjects();
		docSelectionRenderService.setSegment("");
		docSelectionRenderService.setSegmentPage(-1);
		viewModel.setEditArea(_univerjs_engine_render.DocumentEditArea.BODY);
		if ((typeof skeleton.getSkeletonData === "function" ? ((_skeleton$getSkeleton2 = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton2 === void 0 ? void 0 : _skeleton$getSkeleton2.pages.length) ?? 0 : 0) === 0) skeleton.calculate();
		(_renderObject$mainCom = renderObject.mainComponent) === null || _renderObject$mainCom === void 0 || _renderObject$mainCom.makeDirty(true);
		queueMicrotask(() => {
			const docDataModel = instanceService.getUnit(unitId);
			const snapshot = docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getSnapshot();
			if (snapshot == null) return;
			const offset = findFirstCursorOffset(snapshot);
			docSelectionManagerService.replaceDocRanges([{
				startOffset: offset,
				endOffset: offset
			}]);
		});
		commandService.executeCommand(SidebarDocHeaderFooterPanelOperation.id, { value: "close" });
		return true;
	}
};

//#endregion
//#region src/types/const/padding.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const DOC_VERTICAL_PADDING = 4;

//#endregion
//#region src/services/doc-popup-manager.service.ts
function transformBound2OffsetBound(originBound, scene) {
	const topLeft = transformPosition2Offset(originBound.left, originBound.top, scene);
	const bottomRight = transformPosition2Offset(originBound.right, originBound.bottom, scene);
	return {
		left: topLeft.x,
		top: topLeft.y,
		right: bottomRight.x,
		bottom: bottomRight.y
	};
}
function transformPosition2Offset(x, y, scene) {
	const { scaleX, scaleY } = scene.getAncestorScale();
	const viewMain = scene.getViewport("viewMain");
	if (!viewMain) return {
		x,
		y
	};
	const { viewportScrollX: actualScrollX, viewportScrollY: actualScrollY } = viewMain;
	return {
		x: (x - actualScrollX) * scaleX,
		y: (y - actualScrollY) * scaleY
	};
}
function transformOffset2Bound(offsetX, offsetY, scene) {
	const { scaleX, scaleY } = scene.getAncestorScale();
	const viewMain = scene.getViewport("viewMain");
	if (!viewMain) return {
		x: offsetX,
		y: offsetY
	};
	const { viewportScrollX: actualScrollX, viewportScrollY: actualScrollY } = viewMain;
	return {
		x: offsetX / scaleX + actualScrollX,
		y: offsetY / scaleY + actualScrollY
	};
}
const calcDocRangePositions = (range, currentRender) => {
	const { scene, mainComponent, engine } = currentRender;
	const skeleton = currentRender.with(_univerjs_docs.DocSkeletonManagerService).getSkeleton();
	const startPosition = skeleton.findNodePositionByCharIndex(range.startOffset, true, range.segmentId, range.segmentPage);
	const endIndex = range.collapsed ? range.startOffset : range.endOffset - 1;
	const endPosition = skeleton.findNodePositionByCharIndex(endIndex, range.collapsed === true, range.segmentId, range.segmentPage);
	const document = mainComponent;
	if (!endPosition || !startPosition) return;
	const documentOffsetConfig = document.getOffsetConfig();
	const { docsLeft, docsTop } = documentOffsetConfig;
	const canvasElement = engine.getCanvasElement();
	if (canvasElement == null) return;
	const canvasClientRect = canvasElement.getBoundingClientRect();
	const widthOfCanvas = (0, _univerjs_engine_render.pxToNum)(canvasElement.style.width);
	const { top, left, width } = canvasClientRect;
	const scaleAdjust = width / widthOfCanvas;
	const { scaleX, scaleY } = scene.getAncestorScale();
	const { borderBoxPointGroup } = new NodePositionConvertToCursor(documentOffsetConfig, skeleton).getRangePointData(startPosition, endPosition);
	return getLineBounding(borderBoxPointGroup).map((bound) => transformBound2OffsetBound(bound, scene)).map((i) => ({
		left: (i.left + docsLeft * scaleX) * scaleAdjust + left,
		right: (i.right + docsLeft * scaleX) * scaleAdjust + left,
		top: (i.top + docsTop * scaleY) * scaleAdjust + top,
		bottom: (i.bottom + docsTop * scaleY) * scaleAdjust + top
	}));
};
let DocCanvasPopManagerService = class DocCanvasPopManagerService extends _univerjs_core.Disposable {
	constructor(_globalPopupManagerService, _renderManagerService, _univerInstanceService, _commandService) {
		super();
		this._globalPopupManagerService = _globalPopupManagerService;
		this._renderManagerService = _renderManagerService;
		this._univerInstanceService = _univerInstanceService;
		this._commandService = _commandService;
		_defineProperty(this, "_popupCountByUnit", /* @__PURE__ */ new Map());
		_defineProperty(this, "_popupUnits$", new rxjs.BehaviorSubject(/* @__PURE__ */ new Set()));
		_defineProperty(this, "popupUnits$", this._popupUnits$.asObservable());
	}
	dispose() {
		this._popupCountByUnit.clear();
		this._popupUnits$.next(/* @__PURE__ */ new Set());
		this._popupUnits$.complete();
		super.dispose();
	}
	_beginPopupActivity(unitId) {
		this._popupCountByUnit.set(unitId, (this._popupCountByUnit.get(unitId) ?? 0) + 1);
		this._publishPopupUnits();
		let disposed = false;
		return { dispose: () => {
			if (disposed) return;
			disposed = true;
			const count = this._popupCountByUnit.get(unitId) ?? 0;
			if (count <= 1) this._popupCountByUnit.delete(unitId);
			else this._popupCountByUnit.set(unitId, count - 1);
			this._publishPopupUnits();
		} };
	}
	_publishPopupUnits() {
		this._popupUnits$.next(new Set(this._popupCountByUnit.keys()));
	}
	_shouldUpdateForCommand(commandInfo, unitId) {
		if (commandInfo.id !== SetDocZoomRatioOperation.id && commandInfo.id !== _univerjs_docs.RichTextEditingMutation.id) return false;
		const params = commandInfo.params;
		return (params === null || params === void 0 ? void 0 : params.unitId) == null || params.unitId === unitId;
	}
	_createRectPositionObserver(rect, currentRender) {
		const calc = () => {
			const { scene, engine } = currentRender;
			const bound = typeof rect === "function" ? rect() : rect;
			const canvasElement = engine.getCanvasElement();
			if (canvasElement == null) return;
			const canvasClientRect = canvasElement.getBoundingClientRect();
			const widthOfCanvas = (0, _univerjs_engine_render.pxToNum)(canvasElement.style.width);
			const offsetBound = transformBound2OffsetBound(bound, scene);
			const { top: topOffset, left: leftOffset, width: domWidth } = canvasClientRect;
			const scaleAdjust = domWidth / widthOfCanvas;
			return {
				left: offsetBound.left * scaleAdjust + leftOffset,
				right: offsetBound.right * scaleAdjust + leftOffset,
				top: offsetBound.top * scaleAdjust + topOffset,
				bottom: offsetBound.bottom * scaleAdjust + topOffset
			};
		};
		const position = calc();
		if (position == null) throw new Error(`Current render canvas not found, unitId: ${currentRender.unitId}`);
		const position$ = new rxjs.BehaviorSubject(position);
		const disposable = new _univerjs_core.DisposableCollection();
		const updatePosition = () => {
			try {
				const newPosition = calc();
				if (newPosition) position$.next(newPosition);
			} catch {}
		};
		disposable.add(this._commandService.onCommandExecuted((commandInfo) => {
			if (this._shouldUpdateForCommand(commandInfo, currentRender.unitId)) updatePosition();
		}));
		const viewMain = currentRender.scene.getViewport("viewMain");
		if (viewMain) disposable.add(viewMain.onScrollAfter$.subscribeEvent(() => {
			updatePosition();
		}));
		disposable.add(currentRender.scene.onTransformChange$.subscribeEvent(() => {
			updatePosition();
		}));
		return {
			position,
			position$,
			disposable
		};
	}
	_createObjectPositionObserver(targetObject, currentRender) {
		const getBound = () => {
			const { left, top, width, height } = targetObject;
			return {
				left,
				right: left + width,
				top,
				bottom: top + height
			};
		};
		return this._createRectPositionObserver(getBound, currentRender);
	}
	_createRangePositionObserver(range, currentRender) {
		const positions = calcDocRangePositions(range, currentRender) ?? [];
		const positions$ = new rxjs.BehaviorSubject(positions);
		const disposable = new _univerjs_core.DisposableCollection();
		const updatePositions = () => {
			try {
				const position = calcDocRangePositions(range, currentRender);
				if (position) positions$.next(position);
			} catch {}
		};
		disposable.add(this._commandService.onCommandExecuted((commandInfo) => {
			if (this._shouldUpdateForCommand(commandInfo, currentRender.unitId)) updatePositions();
		}));
		const viewMain = currentRender.scene.getViewport("viewMain");
		if (viewMain) disposable.add(viewMain.onScrollAfter$.subscribeEvent(() => {
			updatePositions();
		}));
		disposable.add(currentRender.scene.onTransformChange$.subscribeEvent(() => {
			updatePositions();
		}));
		return {
			positions,
			positions$,
			disposable
		};
	}
	attachPopupToRect(rect, popup, unitId) {
		const currentRender = this._renderManagerService.getRenderUnitById(unitId);
		if (!currentRender) throw new Error(`Current render not found, unitId: ${unitId}`);
		const popupInjector = this._resolveEmbeddedPopupInjector(unitId, currentRender);
		const popupManagerService = this._resolvePopupManagerService(popupInjector);
		const { position, position$, disposable } = this._createRectPositionObserver(rect, currentRender);
		const id = popupManagerService.addPopup({
			...popup,
			unitId,
			subUnitId: "default",
			connectorInjector: popupInjector,
			anchorRect: position,
			anchorRect$: position$,
			canvasElement: currentRender.engine.getCanvasElement()
		});
		const popupActivity = popup.requiresStableLayout === false ? null : this._beginPopupActivity(unitId);
		return {
			dispose: () => {
				popupManagerService.removePopup(id);
				position$.complete();
				disposable.dispose();
				popupActivity === null || popupActivity === void 0 || popupActivity.dispose();
			},
			canDispose: () => popupManagerService.activePopupId !== id
		};
	}
	/**
	* attach a popup to canvas object
	* @param targetObject target canvas object
	* @param popup popup item
	* @returns disposable
	*/
	attachPopupToObject(targetObject, popup, unitId) {
		const currentRender = this._renderManagerService.getRenderUnitById(unitId);
		if (!currentRender) throw new Error(`Current render not found, unitId: ${unitId}`);
		const popupInjector = this._resolveEmbeddedPopupInjector(unitId, currentRender);
		const popupManagerService = this._resolvePopupManagerService(popupInjector);
		const { position, position$, disposable } = this._createObjectPositionObserver(targetObject, currentRender);
		const id = popupManagerService.addPopup({
			...popup,
			unitId,
			subUnitId: "default",
			connectorInjector: popupInjector,
			anchorRect: position,
			anchorRect$: position$,
			canvasElement: currentRender.engine.getCanvasElement()
		});
		const popupActivity = popup.requiresStableLayout === false ? null : this._beginPopupActivity(unitId);
		return {
			dispose: () => {
				popupManagerService.removePopup(id);
				position$.complete();
				disposable.dispose();
				popupActivity === null || popupActivity === void 0 || popupActivity.dispose();
			},
			canDispose: () => popupManagerService.activePopupId !== id
		};
	}
	/**
	* attach a popup to doc range
	* @param range doc range
	* @param popup popup item
	* @param unitId unit id
	* @returns disposable
	*/
	attachPopupToRange(range, popup, unitId) {
		if (!this._univerInstanceService.getUnit(unitId)) throw new Error(`Document not found, unitId: ${unitId}`);
		const { direction = "top", multipleDirection } = popup;
		const currentRender = this._renderManagerService.getRenderUnitById(unitId);
		if (!currentRender) throw new Error(`Current render not found, unitId: ${unitId}`);
		const popupInjector = this._resolveEmbeddedPopupInjector(unitId, currentRender);
		const popupManagerService = this._resolvePopupManagerService(popupInjector);
		const { positions: bounds, positions$: bounds$, disposable } = this._createRangePositionObserver(range, currentRender);
		const getAnchor = (bounds) => {
			if (popup.rangeAnchor === "selection-end") {
				const backward = range.direction === "backward";
				const bound = backward ? bounds[0] : bounds[bounds.length - 1];
				const x = backward ? bound.left : bound.right;
				return {
					...bound,
					left: x,
					right: x
				};
			}
			return direction.includes("top") ? bounds[0] : bounds[bounds.length - 1];
		};
		const position$ = bounds$.pipe((0, rxjs.map)(getAnchor));
		const id = popupManagerService.addPopup({
			...popup,
			unitId,
			subUnitId: "default",
			connectorInjector: popupInjector,
			anchorRect: getAnchor(bounds),
			anchorRect$: position$,
			excludeRects: bounds,
			excludeRects$: bounds$,
			direction: [
				"top",
				"bottom",
				"horizontal"
			].some((i) => direction.includes(i)) ? bounds.length > 1 ? multipleDirection ?? direction : direction : direction,
			canvasElement: currentRender.engine.getCanvasElement()
		});
		const popupActivity = popup.requiresStableLayout === false ? null : this._beginPopupActivity(unitId);
		return {
			dispose: () => {
				popupManagerService.removePopup(id);
				bounds$.complete();
				disposable.dispose();
				popupActivity === null || popupActivity === void 0 || popupActivity.dispose();
			},
			canDispose: () => popupManagerService.activePopupId !== id
		};
	}
	_resolveEmbeddedPopupInjector(unitId, currentRender) {
		var _this$_univerInstance, _currentRender$getInj;
		return currentRender.isMainScene === false || ((_this$_univerInstance = this._univerInstanceService.getUnitCreateOptions(unitId)) === null || _this$_univerInstance === void 0 ? void 0 : _this$_univerInstance.embeddedRender) === true ? (_currentRender$getInj = currentRender.getInjector) === null || _currentRender$getInj === void 0 ? void 0 : _currentRender$getInj.call(currentRender) : void 0;
	}
	_resolvePopupManagerService(popupInjector) {
		return (popupInjector === null || popupInjector === void 0 ? void 0 : popupInjector.has(_univerjs_ui.ICanvasPopupService)) ? popupInjector.get(_univerjs_ui.ICanvasPopupService) : this._globalPopupManagerService;
	}
};
DocCanvasPopManagerService = __decorate([
	__decorateParam(0, (0, _univerjs_core.Inject)(_univerjs_ui.ICanvasPopupService)),
	__decorateParam(1, _univerjs_engine_render.IRenderManagerService),
	__decorateParam(2, _univerjs_core.IUniverInstanceService),
	__decorateParam(3, _univerjs_core.ICommandService)
], DocCanvasPopManagerService);

//#endregion
//#region src/services/doc-event-manager.service.ts
const calcDocRangePositions$1 = (range, documents, skeleton, pageIndex) => {
	const startPosition = skeleton.findNodePositionByCharIndex(range.startOffset, true, range.segmentId, pageIndex);
	const skeletonData = skeleton.getSkeletonData();
	let end = range.collapsed ? range.startOffset : range.endOffset - 1;
	if (range.segmentId) {
		var _skeletonData$skeFoot, _skeletonData$skeHead;
		const root = Array.from((skeletonData === null || skeletonData === void 0 || (_skeletonData$skeFoot = skeletonData.skeFooters.get(range.segmentId)) === null || _skeletonData$skeFoot === void 0 ? void 0 : _skeletonData$skeFoot.values()) ?? [])[0] ?? Array.from((skeletonData === null || skeletonData === void 0 || (_skeletonData$skeHead = skeletonData.skeHeaders.get(range.segmentId)) === null || _skeletonData$skeHead === void 0 ? void 0 : _skeletonData$skeHead.values()) ?? [])[0];
		if (root) end = Math.min(root.ed, end);
	}
	const endPosition = skeleton.findNodePositionByCharIndex(end, range.collapsed === true, range.segmentId, pageIndex);
	if (!endPosition || !startPosition) return;
	const documentOffsetConfig = documents.getOffsetConfig();
	const { borderBoxPointGroup } = new NodePositionConvertToCursor(documentOffsetConfig, skeleton).getRangePointData(startPosition, endPosition);
	return getLineBounding(borderBoxPointGroup).map((rect) => ({
		top: rect.top + documentOffsetConfig.docsTop - 4,
		bottom: rect.bottom + documentOffsetConfig.docsTop + 4,
		left: rect.left + documentOffsetConfig.docsLeft,
		right: rect.right + documentOffsetConfig.docsLeft
	}));
};
const calcDocParagraphPositions = (sections, top, left, pageWidth) => {
	const paragraphBounds = [];
	for (const section of sections) {
		const sectionTop = section.top;
		for (const column of section.columns) {
			const columnLeft = column.left;
			const width = section.colCount === 1 ? pageWidth : column.width;
			let currentParagraph = null;
			for (const line of column.lines) {
				const startIndex = line.paragraphIndex;
				if (line.paragraphStart) {
					if (currentParagraph) {
						paragraphBounds.push(currentParagraph);
						currentParagraph = null;
					}
					const lineRect = {
						top: top + sectionTop + line.top,
						left: left + columnLeft,
						right: left + columnLeft + width,
						bottom: top + sectionTop + line.top + line.lineHeight
					};
					currentParagraph = {
						paragraphStart: line.st,
						paragraphEnd: startIndex,
						startIndex,
						rect: lineRect,
						fisrtLine: {
							top: top + sectionTop + line.top + line.marginTop + line.paddingTop,
							left: left + columnLeft,
							right: left + columnLeft + width,
							bottom: top + sectionTop + line.top + line.marginTop + line.paddingTop + line.contentHeight
						}
					};
				} else if (currentParagraph && currentParagraph.startIndex === startIndex) currentParagraph.rect.bottom = top + sectionTop + line.top + line.lineHeight;
			}
			if (currentParagraph) paragraphBounds.push(currentParagraph);
		}
	}
	return paragraphBounds;
};
const calcParagraphPositionsFromLineContexts = (contexts, docsLeft, docsTop) => {
	const paragraphBounds = [];
	const currentParagraphs = /* @__PURE__ */ new Map();
	for (const context of contexts) {
		const { column, line, lineWidth, pageLeft, sectionTop } = context;
		const startIndex = line.paragraphIndex;
		const columnLeft = pageLeft + column.left + docsLeft;
		const lineTop = sectionTop + line.top + docsTop;
		const lineBottom = getParagraphLineBottom(context, lineTop);
		const key = `${context.source}:${context.pageIndex}:${pageLeft}:${column.left}:${startIndex}`;
		if (line.paragraphStart) {
			const currentParagraph = currentParagraphs.get(key);
			if (currentParagraph) paragraphBounds.push(currentParagraph);
			currentParagraphs.set(key, {
				paragraphStart: line.st,
				paragraphEnd: startIndex,
				startIndex,
				rect: {
					top: lineTop,
					left: columnLeft,
					right: columnLeft + lineWidth,
					bottom: lineBottom
				},
				fisrtLine: {
					top: lineTop + line.marginTop + line.paddingTop,
					left: columnLeft,
					right: columnLeft + lineWidth,
					bottom: lineTop + line.marginTop + line.paddingTop + line.contentHeight
				}
			});
		} else {
			const currentParagraph = currentParagraphs.get(key);
			if (currentParagraph) currentParagraph.rect.bottom = lineBottom;
		}
	}
	paragraphBounds.push(...currentParagraphs.values());
	return paragraphBounds;
};
function getParagraphLineBottom(context, lineTop) {
	const lineBottom = lineTop + context.line.lineHeight;
	if (context.source !== "column" || context.line !== context.column.lines[context.column.lines.length - 1] || !Number.isFinite(context.page.pageHeight)) return lineBottom;
	const pageTop = lineTop - context.line.top - context.section.top;
	return Math.max(lineBottom, pageTop + context.page.pageHeight);
}
const calcDocGlyphPosition = (glyph, documents, skeleton, pageIndex = -1) => {
	const start = skeleton.findPositionByGlyph(glyph, pageIndex);
	if (!start) return;
	const documentOffsetConfig = documents.getOffsetConfig();
	const startPosition = {
		...start,
		isBack: true
	};
	const { borderBoxPointGroup } = new NodePositionConvertToCursor(documentOffsetConfig, skeleton).getRangePointData(startPosition, startPosition);
	const rect = getLineBounding(borderBoxPointGroup)[0];
	if (!rect) return;
	return {
		top: rect.top + documentOffsetConfig.docsTop,
		bottom: rect.bottom + documentOffsetConfig.docsTop,
		left: rect.left + documentOffsetConfig.docsLeft,
		right: rect.left + documentOffsetConfig.docsLeft + glyph.width
	};
};
const LIST_MARKER_FALLBACK_HIT_WIDTH = 36;
function getListMarkerFallbackHit(paragraphBound, paragraph, point) {
	const markerBound = getListMarkerFallbackBound(paragraphBound, paragraph);
	if (!markerBound || !isPointInRect(point.x, point.y, markerBound.rect)) return null;
	return markerBound;
}
function getListParagraphContextMenuHit(paragraphBound, paragraph, point) {
	return getListMarkerFallbackHit(paragraphBound, paragraph, point);
}
function isChecklistListType(listType) {
	return listType === _univerjs_core.PresetListType.CHECK_LIST || listType === _univerjs_core.PresetListType.CHECK_LIST_CHECKED;
}
function getListMarkerFallbackBound(paragraphBound, paragraph) {
	if (!(paragraph === null || paragraph === void 0 ? void 0 : paragraph.bullet)) return null;
	return {
		paragraph,
		rect: {
			bottom: paragraphBound.firstLine.bottom,
			left: paragraphBound.rect.left,
			right: Math.min(paragraphBound.rect.right, paragraphBound.rect.left + LIST_MARKER_FALLBACK_HIT_WIDTH),
			top: paragraphBound.firstLine.top
		},
		segmentId: paragraphBound.segmentId,
		segmentPageIndex: paragraphBound.pageIndex
	};
}
const TABLE_BLOCK_MENU_HOVER_GUTTER_LEFT = 72;
const TABLE_BLOCK_MENU_HOVER_GUTTER_TOP = 42;
function isPointInRect(x, y, rect) {
	const { left, right, top, bottom } = rect;
	if (x >= left && x <= right && y >= top && y <= bottom) return true;
	return false;
}
function getRectArea(rect) {
	return Math.max(0, rect.right - rect.left) * Math.max(0, rect.bottom - rect.top);
}
function isRectContainingRect(outer, inner) {
	return outer.left <= inner.left && outer.right >= inner.right && outer.top <= inner.top && outer.bottom >= inner.bottom;
}
function getParagraphTextRangeLength(bound) {
	return Math.max(0, bound.paragraphEnd - bound.paragraphStart);
}
function getMostSpecificParagraphBound(bounds) {
	return bounds.sort((left, right) => getRectArea(left.rect) - getRectArea(right.rect) || getParagraphTextRangeLength(left) - getParagraphTextRangeLength(right))[0];
}
function removeContainerParagraphBounds(bounds) {
	return bounds.filter((bound) => !bounds.some((other) => other !== bound && getRectArea(other.rect) < getRectArea(bound.rect) && isRectContainingRect(bound.rect, other.rect)));
}
function getTableBlockMenuHoverRect(tableRect) {
	return {
		bottom: tableRect.bottom,
		left: tableRect.left - TABLE_BLOCK_MENU_HOVER_GUTTER_LEFT,
		right: tableRect.right,
		top: tableRect.top - TABLE_BLOCK_MENU_HOVER_GUTTER_TOP
	};
}
function getTableHorizontalViewportGeometry(tableLeft, tableWidth, viewport) {
	const hasHorizontalViewport = hasHorizontalTableViewport(viewport);
	const visibleLeft = hasHorizontalViewport ? tableLeft - (viewport.leadingInsetLeft ?? 0) : tableLeft;
	return {
		scrollLeft: hasHorizontalViewport ? viewport.scrollLeft : 0,
		visibleLeft,
		visibleRight: visibleLeft + (hasHorizontalViewport ? viewport.viewportWidth : tableWidth)
	};
}
function hasHorizontalTableViewport(viewport) {
	return viewport != null && (viewport.leadingInsetLeft ?? 0) + viewport.contentWidth + (viewport.trailingInsetRight ?? 0) > viewport.viewportWidth;
}
let DocEventManagerService = class DocEventManagerService extends _univerjs_core.Disposable {
	get hoverParagraph() {
		return this._hoverParagraph$.value;
	}
	get hoverParagraphLeft() {
		return this._hoverParagraphLeft$.value;
	}
	get _skeleton() {
		return this._docSkeletonManagerService.getSkeleton();
	}
	get _documents() {
		return this._context.mainComponent;
	}
	constructor(_context, _docSkeletonManagerService) {
		super();
		this._context = _context;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		_defineProperty(this, "_hoverCustomRanges$", new rxjs.BehaviorSubject([]));
		_defineProperty(this, "hoverCustomRanges$", this._hoverCustomRanges$.pipe((0, rxjs.distinctUntilChanged)((pre, aft) => pre.length === aft.length && pre.every((item, i) => aft[i].range.rangeId === item.range.rangeId && aft[i].segmentId === item.segmentId && aft[i].segmentPageIndex === item.segmentPageIndex && aft[i].range.startIndex === item.range.startIndex))));
		_defineProperty(this, "_clickCustomRanges$", new rxjs.Subject());
		_defineProperty(this, "clickCustomRanges$", this._clickCustomRanges$.asObservable());
		_defineProperty(this, "_pointerDownCustomRanges$", new rxjs.Subject());
		_defineProperty(this, "pointerDownCustomRanges$", this._pointerDownCustomRanges$.asObservable());
		_defineProperty(this, "_hoverBullet$", new rxjs.Subject());
		_defineProperty(this, "hoverBullet$", this._hoverBullet$.pipe((0, rxjs.distinctUntilChanged)((pre, aft) => (pre === null || pre === void 0 ? void 0 : pre.paragraph.startIndex) === (aft === null || aft === void 0 ? void 0 : aft.paragraph.startIndex) && (pre === null || pre === void 0 ? void 0 : pre.segmentId) === (aft === null || aft === void 0 ? void 0 : aft.segmentId) && (pre === null || pre === void 0 ? void 0 : pre.segmentPageIndex) === (aft === null || aft === void 0 ? void 0 : aft.segmentPageIndex))));
		_defineProperty(this, "_clickBullet$", new rxjs.Subject());
		_defineProperty(this, "clickBullets$", this._clickBullet$.asObservable());
		_defineProperty(this, "_contextMenuBullet$", new rxjs.Subject());
		_defineProperty(this, "contextMenuBullets$", this._contextMenuBullet$.asObservable());
		_defineProperty(this, "_hoverParagraph$", new rxjs.BehaviorSubject(null));
		_defineProperty(this, "hoverParagraph$", this._hoverParagraph$.pipe((0, rxjs.distinctUntilChanged)((pre, aft) => (pre === null || pre === void 0 ? void 0 : pre.startIndex) === (aft === null || aft === void 0 ? void 0 : aft.startIndex) && (pre === null || pre === void 0 ? void 0 : pre.segmentId) === (aft === null || aft === void 0 ? void 0 : aft.segmentId) && (pre === null || pre === void 0 ? void 0 : pre.pageIndex) === (aft === null || aft === void 0 ? void 0 : aft.pageIndex))));
		_defineProperty(this, "hoverParagraphRealTime$", this._hoverParagraph$.asObservable());
		_defineProperty(this, "_hoverParagraphLeft$", new rxjs.BehaviorSubject(null));
		_defineProperty(this, "hoverParagraphLeft$", this._hoverParagraphLeft$.pipe((0, rxjs.distinctUntilChanged)((pre, aft) => (pre === null || pre === void 0 ? void 0 : pre.startIndex) === (aft === null || aft === void 0 ? void 0 : aft.startIndex) && (pre === null || pre === void 0 ? void 0 : pre.segmentId) === (aft === null || aft === void 0 ? void 0 : aft.segmentId) && (pre === null || pre === void 0 ? void 0 : pre.pageIndex) === (aft === null || aft === void 0 ? void 0 : aft.pageIndex))));
		_defineProperty(this, "hoverParagraphLeftRealTime$", this._hoverParagraphLeft$.asObservable());
		_defineProperty(this, "_hoverTableCell$", new rxjs.Subject());
		_defineProperty(this, "hoverTableCell$", this._hoverTableCell$.pipe((0, rxjs.distinctUntilChanged)((pre, aft) => (pre === null || pre === void 0 ? void 0 : pre.rowIndex) === (aft === null || aft === void 0 ? void 0 : aft.rowIndex) && (pre === null || pre === void 0 ? void 0 : pre.colIndex) === (aft === null || aft === void 0 ? void 0 : aft.colIndex) && (pre === null || pre === void 0 ? void 0 : pre.tableId) === (aft === null || aft === void 0 ? void 0 : aft.tableId) && (pre === null || pre === void 0 ? void 0 : pre.pageIndex) === (aft === null || aft === void 0 ? void 0 : aft.pageIndex))));
		_defineProperty(this, "hoverTableCellRealTime$", this._hoverTableCell$.asObservable());
		_defineProperty(this, "_hoverTable$", new rxjs.Subject());
		_defineProperty(this, "hoverTable$", this._hoverTable$.pipe((0, rxjs.distinctUntilChanged)((pre, aft) => (pre === null || pre === void 0 ? void 0 : pre.tableId) === (aft === null || aft === void 0 ? void 0 : aft.tableId) && (pre === null || pre === void 0 ? void 0 : pre.pageIndex) === (aft === null || aft === void 0 ? void 0 : aft.pageIndex))));
		_defineProperty(this, "hoverTableRealTime$", this._hoverTable$.asObservable());
		_defineProperty(this, "_customRangeDirty", true);
		_defineProperty(this, "_bulletDirty", true);
		_defineProperty(this, "_paragraphDirty", true);
		_defineProperty(this, "_tableBoundsDirty", true);
		_defineProperty(this, "_customRangeBounds", []);
		_defineProperty(this, "_bulletBounds", []);
		_defineProperty(this, "_paragraphBounds", /* @__PURE__ */ new Map());
		_defineProperty(this, "_paragraphLeftBounds", []);
		_defineProperty(this, "_tableParagraphBounds", /* @__PURE__ */ new Map());
		_defineProperty(this, "_tableParagraphBoundsByIndex", /* @__PURE__ */ new Map());
		_defineProperty(this, "_segmentParagraphBounds", /* @__PURE__ */ new Map());
		_defineProperty(this, "_tableViewportSignature", "");
		_defineProperty(this, "_tableCellBounds", /* @__PURE__ */ new Map());
		_defineProperty(this, "_tableCellGeometries", /* @__PURE__ */ new Map());
		_defineProperty(this, "_tableCellPages", /* @__PURE__ */ new Set());
		_defineProperty(this, "_tableBounds", /* @__PURE__ */ new Map());
		_defineProperty(this, "contextMenuParagraph", null);
		this._initResetDirty();
		this._initEvents();
		this._initPointer();
	}
	dispose() {
		this._hoverCustomRanges$.complete();
		this._clickCustomRanges$.complete();
		this._pointerDownCustomRanges$.complete();
		super.dispose();
	}
	_initPointer() {
		let preCursor = _univerjs_engine_render.CURSOR_TYPE.TEXT;
		this.disposeWithMe(this.hoverCustomRanges$.subscribe((ranges) => {
			if (ranges.length) {
				preCursor = this._context.scene.getCursor();
				this._context.scene.setCursor(_univerjs_engine_render.CURSOR_TYPE.POINTER);
			} else this._context.scene.setCursor(preCursor);
		}));
	}
	_initResetDirty() {
		this.disposeWithMe(this._skeleton.dirty$.subscribe(() => {
			this._customRangeDirty = true;
			this._bulletDirty = true;
			this._paragraphDirty = true;
			this._tableBoundsDirty = true;
		}));
		this.disposeWithMe((0, _univerjs_core.fromEventSubject)(this._context.engine.onTransformChange$).pipe((0, rxjs.filter)((evt) => evt.type === _univerjs_engine_render.TRANSFORM_CHANGE_OBSERVABLE_TYPE.resize)).subscribe(() => {
			this._customRangeDirty = true;
			this._bulletDirty = true;
			this._paragraphDirty = true;
			this._tableBoundsDirty = true;
		}));
		this.disposeWithMe((0, _univerjs_core.fromEventSubject)(this._context.scene.onTransformChange$).pipe((0, rxjs.filter)((evt) => evt.type === _univerjs_engine_render.TRANSFORM_CHANGE_OBSERVABLE_TYPE.scale)).subscribe(() => {
			this._customRangeDirty = true;
			this._bulletDirty = true;
			this._paragraphDirty = true;
			this._tableBoundsDirty = true;
		}));
	}
	_initEvents() {
		this.disposeWithMe((0, _univerjs_core.fromEventSubject)(this._context.scene.onPointerMove$).pipe((0, rxjs.throttleTime)(30, void 0, {
			leading: true,
			trailing: true
		})).subscribe((evt) => {
			if (evt.buttons > 0) {
				this._hoverBullet$.next(null);
				this._hoverCustomRanges$.next([]);
				this._hoverParagraph$.next(null);
				this._hoverParagraphLeft$.next(null);
				this._hoverTableCell$.next(null);
				this._hoverTable$.next(null);
				return;
			}
			const { x, y } = transformOffset2Bound(evt.offsetX, evt.offsetY, this._context.scene);
			this._hoverCustomRanges$.next(this._calcActiveRanges({
				x,
				y
			}));
			this._hoverParagraph$.next(this._calcActiveParagraph({
				x,
				y
			}));
			this._hoverParagraphLeft$.next(this._calcActiveParagraphLeft({
				x,
				y
			}));
			this._hoverBullet$.next(this._calcActiveBullet({
				x,
				y
			}));
		}));
		this.disposeWithMe(this._context.scene.onPointerEnter$.subscribeEvent(() => {
			this._hoverBullet$.next(null);
			this._hoverCustomRanges$.next([]);
		}));
		const onPointerDown$ = (0, _univerjs_core.fromEventSubject)(this._context.mainComponent.onPointerDown$);
		const onPointerUp$ = (0, _univerjs_core.fromEventSubject)(this._context.scene.onPointerUp$);
		this.disposeWithMe(onPointerDown$.pipe((0, rxjs.tap)((down) => {
			if (down.button !== 0) return;
			const point = transformOffset2Bound(down.offsetX, down.offsetY, this._context.scene);
			this._pointerDownCustomRanges$.next(this._calcActiveRanges(point));
		}), (0, rxjs.switchMap)((down) => onPointerUp$.pipe((0, rxjs.take)(1), (0, rxjs.map)((up) => ({
			down,
			up
		})))), (0, rxjs.filter)(({ down, up }) => down.target === up.target && (down.button === 2 || up.timeStamp - down.timeStamp < 300))).subscribe(({ down }) => {
			const point = transformOffset2Bound(down.offsetX, down.offsetY, this._context.scene);
			if (down.button === 2) {
				const bullet = this._calcActiveBullet(point) ?? this._calcActiveListParagraph(point);
				if (bullet) this._contextMenuBullet$.next({
					...bullet,
					x: down.offsetX,
					y: down.offsetY
				});
				return;
			}
			const ranges = this._calcActiveRanges(point);
			if (ranges.length) this._clickCustomRanges$.next({
				...ranges.pop(),
				ctrlKey: !!down.ctrlKey,
				metaKey: !!down.metaKey
			});
			const bullet = this._calcActiveBullet(point);
			if (bullet) this._clickBullet$.next(bullet);
		}));
	}
	isPointerOnBullet(offsetX, offsetY) {
		return Boolean(this._calcActiveBullet(transformOffset2Bound(offsetX, offsetY, this._context.scene)));
	}
	isPointerOnNonChecklistBullet(offsetX, offsetY) {
		var _bullet$paragraph$bul;
		const bullet = this._calcActiveBullet(transformOffset2Bound(offsetX, offsetY, this._context.scene));
		return Boolean(bullet && !isChecklistListType((_bullet$paragraph$bul = bullet.paragraph.bullet) === null || _bullet$paragraph$bul === void 0 ? void 0 : _bullet$paragraph$bul.listType));
	}
	getBulletBounds() {
		this._buildBulletBounds();
		return [...this._bulletBounds];
	}
	getListContextMenuBulletByOffset(offsetX, offsetY) {
		const point = transformOffset2Bound(offsetX, offsetY, this._context.scene);
		return this._calcActiveBullet(point) ?? this._calcActiveListParagraph(point);
	}
	_buildCustomRangeBoundsBySegment(segmentId, segmentPage = -1) {
		var _this$_context$unit$g;
		const customRanges = ((_this$_context$unit$g = this._context.unit.getSelfOrHeaderFooterModel(segmentId)) === null || _this$_context$unit$g === void 0 || (_this$_context$unit$g = _this$_context$unit$g.getBody()) === null || _this$_context$unit$g === void 0 ? void 0 : _this$_context$unit$g.customRanges) ?? [];
		const layouts = [];
		customRanges.forEach((range) => {
			const textRange = {
				startOffset: range.startIndex,
				endOffset: range.endIndex + 1,
				collapsed: false,
				segmentId
			};
			const rects = calcDocRangePositions$1(textRange, this._documents, this._skeleton, segmentPage);
			if (!rects) return null;
			layouts.push({
				customRange: range,
				rects,
				segmentId,
				segmentPageIndex: segmentPage
			});
		});
		return layouts;
	}
	_buildCustomRangeBounds() {
		var _this$_skeleton$getSk;
		if (!this._customRangeDirty) return;
		this._customRangeDirty = false;
		const customRangeBounds = [];
		customRangeBounds.push(...this._buildCustomRangeBoundsBySegment());
		(_this$_skeleton$getSk = this._skeleton.getSkeletonData()) === null || _this$_skeleton$getSk === void 0 || _this$_skeleton$getSk.pages.forEach((page, pageIndex) => {
			if (page.headerId) customRangeBounds.push(...this._buildCustomRangeBoundsBySegment(page.headerId, pageIndex));
			if (page.footerId) customRangeBounds.push(...this._buildCustomRangeBoundsBySegment(page.footerId, pageIndex));
		});
		this._customRangeBounds = customRangeBounds;
	}
	_calcActiveRanges(evt) {
		this._buildCustomRangeBounds();
		const { x, y } = evt;
		return this._customRangeBounds.filter((layout) => {
			return layout.rects.some((rect) => isPointInRect(x, y, rect));
		}).map((range) => ({
			segmentId: range.segmentId,
			range: range.customRange,
			segmentPageIndex: range.segmentPageIndex,
			rects: range.rects
		}));
	}
	_buildBulletBoundsBySegment(segmentId, segmentPage = -1) {
		var _this$_context$unit$g2;
		const body = (_this$_context$unit$g2 = this._context.unit.getSelfOrHeaderFooterModel(segmentId)) === null || _this$_context$unit$g2 === void 0 ? void 0 : _this$_context$unit$g2.getBody();
		const paragraphs = ((body === null || body === void 0 ? void 0 : body.paragraphs) ?? []).filter((p) => p.bullet);
		const bounds = [];
		const skeletonData = this._skeleton.getSkeletonData();
		if (!skeletonData) return bounds;
		const calc = (pages) => {
			for (const page of pages) {
				const sections = [...page.sections];
				if (page.skeTables) {
					const tables = Array.from(page.skeTables.values());
					sections.push(...tables.map((i) => i.rows.map((i) => i.cells.map((i) => i.sections))).flat(4));
				}
				for (const selection of sections) for (const column of selection.columns) for (const line of column.lines) if (line.paragraphStart) {
					const paragraph = paragraphs.find((p) => p.startIndex === line.paragraphIndex);
					if (paragraph) {
						var _targetLine$divides;
						const targetLine = line;
						const bulletNode = targetLine === null || targetLine === void 0 || (_targetLine$divides = targetLine.divides) === null || _targetLine$divides === void 0 || (_targetLine$divides = _targetLine$divides[0]) === null || _targetLine$divides === void 0 || (_targetLine$divides = _targetLine$divides.glyphGroup) === null || _targetLine$divides === void 0 ? void 0 : _targetLine$divides[0];
						if (!bulletNode) continue;
						const rect = calcDocGlyphPosition(bulletNode, this._documents, this._skeleton, segmentPage);
						if (!rect) continue;
						bounds.push({
							rect,
							segmentId,
							segmentPageIndex: segmentPage,
							paragraph
						});
					}
				}
			}
			return bounds;
		};
		if (segmentId) {
			var _skeletonData$skeFoot2, _skeletonData$skeHead2;
			const page = ((_skeletonData$skeFoot2 = skeletonData.skeFooters.get(segmentId)) === null || _skeletonData$skeFoot2 === void 0 ? void 0 : _skeletonData$skeFoot2.values()) ?? ((_skeletonData$skeHead2 = skeletonData.skeHeaders.get(segmentId)) === null || _skeletonData$skeHead2 === void 0 ? void 0 : _skeletonData$skeHead2.values());
			if (!page) return bounds;
			return calc(Array.from(page));
		}
		return calc(skeletonData.pages);
	}
	_buildBulletBounds() {
		var _this$_skeleton$getSk2;
		if (!this._bulletDirty) return;
		this._bulletDirty = false;
		this._bulletBounds = [];
		this._bulletBounds.push(...this._buildBulletBoundsBySegment());
		(_this$_skeleton$getSk2 = this._skeleton.getSkeletonData()) === null || _this$_skeleton$getSk2 === void 0 || _this$_skeleton$getSk2.pages.forEach((page, pageIndex) => {
			if (page.headerId) this._bulletBounds.push(...this._buildBulletBoundsBySegment(page.headerId, pageIndex));
			if (page.footerId) this._bulletBounds.push(...this._buildBulletBoundsBySegment(page.footerId, pageIndex));
		});
	}
	_calcActiveBullet(evt) {
		this._buildBulletBounds();
		const { x, y } = evt;
		const bullet = this._bulletBounds.find((layout) => isPointInRect(x, y, layout.rect));
		if (bullet) return bullet;
		const paragraphBound = this._calcActiveParagraph(evt);
		if (!paragraphBound) return null;
		return getListMarkerFallbackHit(paragraphBound, this._getParagraphByStartIndex(paragraphBound.startIndex, paragraphBound.segmentId), evt);
	}
	_calcActiveListParagraph(evt) {
		const paragraphBound = this._calcActiveParagraph(evt);
		if (!paragraphBound) return null;
		return getListParagraphContextMenuHit(paragraphBound, this._getParagraphByStartIndex(paragraphBound.startIndex, paragraphBound.segmentId), evt);
	}
	_getParagraphByStartIndex(startIndex, segmentId) {
		var _this$_context$unit$g3;
		return (_this$_context$unit$g3 = this._context.unit.getSelfOrHeaderFooterModel(segmentId)) === null || _this$_context$unit$g3 === void 0 || (_this$_context$unit$g3 = _this$_context$unit$g3.getBody()) === null || _this$_context$unit$g3 === void 0 || (_this$_context$unit$g3 = _this$_context$unit$g3.paragraphs) === null || _this$_context$unit$g3 === void 0 ? void 0 : _this$_context$unit$g3.find((paragraph) => paragraph.startIndex === startIndex);
	}
	_buildParagraphBoundsBySegment(segmentId) {
		const skeletonData = this._skeleton.getSkeletonData();
		const documentOffsetConfig = this._documents.getOffsetConfig();
		if (!skeletonData) return null;
		const calc = (pages) => {
			const paragraphMap = /* @__PURE__ */ new Map();
			const addParagraphBoundsToMap = (map, bounds, pageIndex, segmentId) => {
				bounds.forEach((bound) => {
					if (!map.has(bound.startIndex)) map.set(bound.startIndex, {
						rect: bound.rect,
						paragraphStart: bound.paragraphStart,
						paragraphEnd: bound.paragraphEnd,
						startIndex: bound.startIndex,
						rects: [bound.rect],
						pageIndex,
						segmentId,
						firstLine: bound.fisrtLine
					});
					else {
						const current = map.get(bound.startIndex);
						if (current) {
							current.rect.bottom = bound.rect.bottom;
							current.rects.push(bound.rect);
						}
					}
				});
			};
			const lineContextsBySource = /* @__PURE__ */ new Map();
			(0, _univerjs_engine_render.documentSkeletonLineIterator)(pages, {
				docsLeft: documentOffsetConfig.docsLeft,
				pageMarginTop: documentOffsetConfig.pageMarginTop,
				unitId: this._context.unitId
			}, (context) => {
				if (context.source === "table-cell") return;
				const lineContexts = lineContextsBySource.get(context.pageIndex) ?? [];
				lineContexts.push(context);
				lineContextsBySource.set(context.pageIndex, lineContexts);
			});
			lineContextsBySource.forEach((contexts, pageIndex) => {
				addParagraphBoundsToMap(paragraphMap, calcParagraphPositionsFromLineContexts(contexts, documentOffsetConfig.docsLeft, documentOffsetConfig.docsTop), pageIndex, segmentId);
			});
			return paragraphMap;
		};
		if (segmentId) {
			var _skeletonData$skeFoot3, _skeletonData$skeHead3;
			const page = ((_skeletonData$skeFoot3 = skeletonData.skeFooters.get(segmentId)) === null || _skeletonData$skeFoot3 === void 0 ? void 0 : _skeletonData$skeFoot3.values()) ?? ((_skeletonData$skeHead3 = skeletonData.skeHeaders.get(segmentId)) === null || _skeletonData$skeHead3 === void 0 ? void 0 : _skeletonData$skeHead3.values());
			if (!page) return null;
			return calc(Array.from(page));
		}
		return calc(skeletonData.pages);
	}
	_buildParagraphBounds() {
		var _this$_skeleton$getSk3;
		if (!this._paragraphDirty) return;
		this._paragraphDirty = false;
		this._tableParagraphBounds = /* @__PURE__ */ new Map();
		this._tableParagraphBoundsByIndex = /* @__PURE__ */ new Map();
		this._paragraphBounds = this._buildParagraphBoundsBySegment() ?? /* @__PURE__ */ new Map();
		this._paragraphLeftBounds = Array.from(this._paragraphBounds.values()).map((bound) => ({
			...bound,
			rect: {
				left: bound.rect.left - 60,
				right: bound.rect.left,
				top: bound.rect.top,
				bottom: bound.rect.bottom
			}
		}));
		const handleSegment = (segmentId) => {
			this._segmentParagraphBounds.set(segmentId, this._buildParagraphBoundsBySegment(segmentId) ?? /* @__PURE__ */ new Map());
		};
		(_this$_skeleton$getSk3 = this._skeleton.getSkeletonData()) === null || _this$_skeleton$getSk3 === void 0 || _this$_skeleton$getSk3.pages.forEach((page) => {
			if (page.headerId) handleSegment(page.headerId);
			if (page.footerId) handleSegment(page.footerId);
		});
	}
	_buildTableBounds() {
		const tableViewportSignature = this._getTableViewportSignature();
		if (!this._tableBoundsDirty && this._tableViewportSignature === tableViewportSignature) return;
		this._tableBoundsDirty = false;
		this._tableViewportSignature = tableViewportSignature;
		this._tableBounds = /* @__PURE__ */ new Map();
		this._tableCellBounds = /* @__PURE__ */ new Map();
		this._tableCellGeometries = /* @__PURE__ */ new Map();
		this._tableCellPages = /* @__PURE__ */ new Set();
		this._tableParagraphBounds = /* @__PURE__ */ new Map();
		this._tableParagraphBoundsByIndex = /* @__PURE__ */ new Map();
		const skeletonData = this._skeleton.getSkeletonData();
		const pages = skeletonData === null || skeletonData === void 0 ? void 0 : skeletonData.pages;
		if (!pages) return;
		const { docsLeft, docsTop, pageMarginTop } = this._documents.getOffsetConfig();
		for (const tableContext of (0, _univerjs_engine_render.documentSkeletonTableIterator)(pages, {
			docsLeft,
			docsTop,
			includeCells: false,
			pageMarginTop,
			skeFooters: skeletonData.skeFooters,
			skeHeaders: skeletonData.skeHeaders,
			unitId: this._context.unitId
		})) {
			const tableId = tableContext.tableId;
			const sourceTableId = (0, _univerjs_engine_render.getTableIdAndSliceIndex)(tableId).tableId;
			const tableViewport = getTableHorizontalViewportGeometry(tableContext.tableRect.left, tableContext.table.width, (0, _univerjs_engine_render.getDocsTableRenderViewport)(this._context.unitId, sourceTableId));
			this._tableBounds.set(tableId, {
				rect: {
					...tableContext.tableRect,
					right: tableViewport.visibleRight
				},
				pageIndex: tableContext.pageIndex,
				tableId
			});
		}
	}
	_buildTableCellsForPage(pageIndex) {
		if (this._tableCellPages.has(pageIndex)) return;
		const skeletonData = this._skeleton.getSkeletonData();
		const page = skeletonData === null || skeletonData === void 0 ? void 0 : skeletonData.pages[pageIndex];
		if (!skeletonData || !page) return;
		this._tableCellPages.add(pageIndex);
		const { docsLeft, docsTop, pageMarginTop } = this._documents.getOffsetConfig();
		const pageTop = skeletonData.pages.slice(0, pageIndex).reduce((top, previousPage) => top + (previousPage.pageHeight === Infinity ? 0 : previousPage.pageHeight) + pageMarginTop, 0);
		const contexts = (0, _univerjs_engine_render.documentSkeletonTableIterator)([page], {
			docsLeft,
			docsTop: docsTop + pageTop,
			includeCells: true,
			pageMarginTop,
			skeFooters: skeletonData.skeFooters,
			skeHeaders: skeletonData.skeHeaders,
			unitId: this._context.unitId
		});
		for (const tableContext of contexts) {
			const tableId = tableContext.tableId;
			const segmentId = tableContext.source === "header" ? tableContext.rootPage.headerId : tableContext.source === "footer" ? tableContext.rootPage.footerId : void 0;
			const cellGeometries = this._tableCellGeometries.get(tableId) ?? [];
			const cellBounds = this._tableCellBounds.get(tableId) ?? [];
			for (const cellGeometry of tableContext.cells) {
				cellGeometries.push({
					cellGeometry,
					pageIndex,
					segmentId
				});
				cellBounds.push({
					rect: cellGeometry.cellRect,
					pageIndex,
					rowIndex: cellGeometry.rowIndex,
					colIndex: cellGeometry.columnIndex,
					tableId
				});
			}
			this._tableCellGeometries.set(tableId, cellGeometries);
			this._tableCellBounds.set(tableId, cellBounds);
		}
	}
	_buildTableCellsForDocumentRange(startIndex, endIndex) {
		var _this$_context$unit$g4;
		const body = (_this$_context$unit$g4 = this._context.unit.getSelfOrHeaderFooterModel()) === null || _this$_context$unit$g4 === void 0 ? void 0 : _this$_context$unit$g4.getBody();
		const tableIds = new Set(((body === null || body === void 0 ? void 0 : body.tables) ?? []).filter((table) => endIndex >= table.startIndex && startIndex < table.endIndex).map((table) => table.tableId));
		if (tableIds.size === 0) {
			this._tableBounds.forEach((bound) => this._buildTableCellsForPage(bound.pageIndex));
			return;
		}
		this._tableBounds.forEach((bound, tableId) => {
			if (tableIds.has((0, _univerjs_engine_render.getTableIdAndSliceIndex)(tableId).tableId)) this._buildTableCellsForPage(bound.pageIndex);
		});
	}
	_getTableParagraphBoundsForCell(tableId, rowIndex, colIndex, pageIndex) {
		var _this$_tableParagraph, _this$_tableCellGeome;
		const cached = (_this$_tableParagraph = this._tableParagraphBounds.get(tableId)) === null || _this$_tableParagraph === void 0 ? void 0 : _this$_tableParagraph.filter((bound) => bound.pageIndex === pageIndex && bound.rowIndex === rowIndex && bound.colIndex === colIndex);
		if (cached === null || cached === void 0 ? void 0 : cached.length) return cached;
		const entry = (_this$_tableCellGeome = this._tableCellGeometries.get(tableId)) === null || _this$_tableCellGeome === void 0 ? void 0 : _this$_tableCellGeome.find((item) => item.pageIndex === pageIndex && item.cellGeometry.rowIndex === rowIndex && item.cellGeometry.columnIndex === colIndex);
		if (!entry) return [];
		const { cellGeometry, segmentId } = entry;
		const cell = cellGeometry.cell;
		const cellContentWidth = (cell.pageWidth ?? 0) - (cell.marginLeft ?? 0) - (cell.marginRight ?? 0);
		const paragraphBounds = calcDocParagraphPositions(cell.sections, cellGeometry.pageTop, cellGeometry.pageLeft, cellContentWidth).map((bound) => clipParagraphBoundHorizontally(bound, cellGeometry.clipLeft, cellGeometry.clipRight)).filter((bound) => bound != null).map((bound) => ({
			rect: bound.rect,
			paragraphStart: bound.paragraphStart,
			paragraphEnd: bound.paragraphEnd,
			startIndex: bound.startIndex,
			pageIndex,
			segmentId,
			rowIndex,
			colIndex,
			firstLine: bound.fisrtLine,
			tableId
		}));
		const retained = (this._tableParagraphBounds.get(tableId) ?? []).filter((bound) => bound.pageIndex !== pageIndex || bound.rowIndex !== rowIndex || bound.colIndex !== colIndex);
		retained.push(...paragraphBounds);
		this._tableParagraphBounds.set(tableId, retained);
		paragraphBounds.forEach((bound) => this._tableParagraphBoundsByIndex.set(bound.startIndex, bound));
		return paragraphBounds;
	}
	getParagraphByOffset(offsetX, offsetY) {
		return this._calcActiveParagraph(transformOffset2Bound(offsetX, offsetY, this._context.scene));
	}
	_calcActiveParagraph(evt) {
		const { x, y } = evt;
		this._buildTableBounds();
		const table = Array.from(this._tableBounds.values()).find((bound) => isPointInRect(x, y, getTableBlockMenuHoverRect(bound.rect)));
		this._hoverTable$.next(table);
		if (table) {
			var _this$_tableCellBound;
			this._buildTableCellsForPage(table.pageIndex);
			const tableCell = (_this$_tableCellBound = this._tableCellBounds.get(table.tableId)) === null || _this$_tableCellBound === void 0 ? void 0 : _this$_tableCellBound.find((bound) => isPointInRect(x, y, bound.rect));
			this._hoverTableCell$.next(tableCell);
			if (!tableCell) return null;
			const paragraphs = this._getTableParagraphBoundsForCell(tableCell.tableId, tableCell.rowIndex, tableCell.colIndex, tableCell.pageIndex);
			const paragraph = paragraphs === null || paragraphs === void 0 ? void 0 : paragraphs.find((bound) => isPointInRect(x, y, bound.rect));
			return paragraph && {
				...paragraph,
				rects: [paragraph.rect]
			};
		}
		this._buildParagraphBounds();
		return getMostSpecificParagraphBound([...this._paragraphBounds.values()].filter((bound) => bound.rects.some((rect) => isPointInRect(x, y, rect))));
	}
	_calcActiveParagraphLeft(evt) {
		this._buildParagraphBounds();
		const { x, y } = evt;
		return getMostSpecificParagraphBound(this._paragraphLeftBounds.filter((bound) => isPointInRect(x, y, bound.rect)));
	}
	get paragraphBounds() {
		this._buildParagraphBounds();
		return this._paragraphBounds;
	}
	get tableBounds() {
		this._buildTableBounds();
		return this._tableBounds;
	}
	findTableCellBound(tableId, rowIndex, colIndex, pageIndex) {
		var _this$_tableCellBound2;
		this._buildTableBounds();
		if (pageIndex != null) this._buildTableCellsForPage(pageIndex);
		else this._tableBounds.forEach((bound, id) => {
			if ((0, _univerjs_engine_render.getTableIdAndSliceIndex)(id).tableId === (0, _univerjs_engine_render.getTableIdAndSliceIndex)(tableId).tableId) this._buildTableCellsForPage(bound.pageIndex);
		});
		return (_this$_tableCellBound2 = this._tableCellBounds.get(tableId)) === null || _this$_tableCellBound2 === void 0 ? void 0 : _this$_tableCellBound2.find((bound) => bound.rowIndex === rowIndex && bound.colIndex === colIndex && (pageIndex == null || bound.pageIndex === pageIndex));
	}
	findParagraphBoundByIndex(index) {
		this._buildTableBounds();
		this._buildTableCellsForDocumentRange(index, index);
		let tableParagraph = this._tableParagraphBoundsByIndex.get(index);
		if (!tableParagraph) for (const [tableId, entries] of this._tableCellGeometries) for (const entry of entries) {
			const { st, ed } = entry.cellGeometry.cell;
			if (st != null && ed != null && (index < st || index > ed)) continue;
			this._getTableParagraphBoundsForCell(tableId, entry.cellGeometry.rowIndex, entry.cellGeometry.columnIndex, entry.pageIndex);
			tableParagraph = this._tableParagraphBoundsByIndex.get(index);
			if (tableParagraph) return tableParagraph;
		}
		if (tableParagraph) return tableParagraph;
		this._buildParagraphBounds();
		const paragraph = this._paragraphBounds.get(index);
		if (paragraph) return paragraph;
	}
	findParagraphBoundsInRange(startIndex, endIndex) {
		this._buildTableBounds();
		this._buildTableCellsForDocumentRange(startIndex, endIndex);
		this._buildParagraphBounds();
		for (const [tableId, entries] of this._tableCellGeometries) entries.filter((item) => {
			const { st, ed } = item.cellGeometry.cell;
			return st == null || ed == null || endIndex >= st && startIndex <= ed;
		}).forEach((entry) => this._getTableParagraphBoundsForCell(tableId, entry.cellGeometry.rowIndex, entry.cellGeometry.columnIndex, entry.pageIndex));
		return getPreferredParagraphBoundsInRange([...this._paragraphBounds.values()], Array.from(this._tableParagraphBounds.values()).flat().map((bound) => ({
			rect: bound.rect,
			paragraphStart: bound.paragraphStart,
			paragraphEnd: bound.paragraphEnd,
			startIndex: bound.startIndex,
			rects: [bound.rect],
			pageIndex: bound.pageIndex,
			segmentId: bound.segmentId,
			firstLine: bound.firstLine
		})), startIndex, endIndex);
	}
	_getTableViewportSignature() {
		var _this$_skeleton$getSk4;
		if (this._tableBounds.size > 0) {
			const tableIds = /* @__PURE__ */ new Set();
			this._tableBounds.forEach((_bound, tableId) => tableIds.add((0, _univerjs_engine_render.getTableIdAndSliceIndex)(tableId).tableId));
			return Array.from(tableIds).map((tableId) => {
				const viewport = (0, _univerjs_engine_render.getDocsTableRenderViewport)(this._context.unitId, tableId);
				return viewport ? `${tableId}:${viewport.contentWidth}:${viewport.viewportWidth}:${viewport.scrollLeft}` : `${tableId}:none`;
			}).join("|");
		}
		const pages = ((_this$_skeleton$getSk4 = this._skeleton.getSkeletonData()) === null || _this$_skeleton$getSk4 === void 0 ? void 0 : _this$_skeleton$getSk4.pages) ?? [];
		const signatures = [];
		for (const tableContext of (0, _univerjs_engine_render.documentSkeletonTableIterator)(pages, {
			includeCells: false,
			pageMarginTop: this._documents.getOffsetConfig().pageMarginTop,
			resolveViewport: false,
			unitId: this._context.unitId
		})) {
			const tableId = (0, _univerjs_engine_render.getTableIdAndSliceIndex)(tableContext.tableId).tableId;
			const viewport = (0, _univerjs_engine_render.getDocsTableRenderViewport)(this._context.unitId, tableId);
			if (!viewport) {
				signatures.push(`${tableId}:none`);
				continue;
			}
			signatures.push(`${tableId}:${viewport.contentWidth}:${viewport.viewportWidth}:${viewport.scrollLeft}`);
		}
		return signatures.join("|");
	}
};
DocEventManagerService = __decorate([__decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService))], DocEventManagerService);
function getPreferredParagraphBoundsInRange(bodyBounds, tableBounds, startIndex, endIndex) {
	const matchedTableBounds = tableBounds.filter((bound) => isRangeOverlappingParagraph(bound, startIndex, endIndex));
	if (matchedTableBounds.length) return matchedTableBounds;
	return removeContainerParagraphBounds(bodyBounds.filter((bound) => isRangeOverlappingParagraph(bound, startIndex, endIndex)));
}
function isRangeOverlappingParagraph(bound, startIndex, endIndex) {
	return Math.max(bound.paragraphStart, startIndex) <= Math.min(bound.paragraphEnd, endIndex);
}
function clipParagraphBoundHorizontally(bound, visibleLeft, visibleRight) {
	const rect = clipRectHorizontally(bound.rect, visibleLeft, visibleRight);
	if (!rect) return null;
	return {
		...bound,
		rect,
		fisrtLine: clipRectHorizontally(bound.fisrtLine, visibleLeft, visibleRight) ?? rect
	};
}
function clipRectHorizontally(rect, visibleLeft, visibleRight) {
	const left = Math.max(rect.left, visibleLeft);
	const right = Math.min(rect.right, visibleRight);
	if (right <= left) return null;
	return {
		...rect,
		left,
		right
	};
}

//#endregion
//#region src/commands/operations/paragraph-permission.operation.ts
const OpenDocParagraphPermissionOperation = {
	id: "doc.operation.open-paragraph-permission",
	type: _univerjs_core.CommandType.OPERATION,
	handler(accessor) {
		var _accessor$get$getRend;
		const model = accessor.get(_univerjs_core.IUniverInstanceService).getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const target = model && ((_accessor$get$getRend = accessor.get(_univerjs_engine_render.IRenderManagerService).getRenderUnitById(model.getUnitId())) === null || _accessor$get$getRend === void 0 ? void 0 : _accessor$get$getRend.with(DocEventManagerService).contextMenuParagraph);
		if (!target) return false;
		const { unitId, segmentId, paragraphId } = target;
		const exists = () => {
			var _accessor$get$getUnit;
			return ((_accessor$get$getUnit = accessor.get(_univerjs_core.IUniverInstanceService).getUnit(unitId)) === null || _accessor$get$getUnit === void 0 || (_accessor$get$getUnit = _accessor$get$getUnit.getSelfOrHeaderFooterModel(segmentId)) === null || _accessor$get$getUnit === void 0 || (_accessor$get$getUnit = _accessor$get$getUnit.getBody()) === null || _accessor$get$getUnit === void 0 || (_accessor$get$getUnit = _accessor$get$getUnit.paragraphs) === null || _accessor$get$getUnit === void 0 ? void 0 : _accessor$get$getUnit.some((item) => item.paragraphId === paragraphId)) === true;
		};
		if (!exists()) return false;
		return (0, _univerjs_ui.openObjectPermissionDialog)(accessor, {
			target: {
				unitId,
				objectId: (0, _univerjs_docs.getDocumentParagraphPermissionObjectId)(segmentId, paragraphId),
				objectType: _univerjs_protocol.UnitObject.DocumentParagraph
			},
			name: accessor.get(_univerjs_core.LocaleService).t("docs-ui.objectPermission.paragraph"),
			commandId: _univerjs_docs.SetDocumentPermissionCommand.id,
			exists
		});
	}
};

//#endregion
//#region src/commands/operations/permission-panel.operation.ts
const OpenDocPermissionPanelOperation = {
	id: "doc.operation.open-permission-panel",
	type: _univerjs_core.CommandType.OPERATION,
	handler(accessor) {
		const currentModel = accessor.get(_univerjs_core.IUniverInstanceService).getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!currentModel || (0, _univerjs_core.isInternalEditorID)(currentModel.getUnitId())) return false;
		const model = currentModel;
		const unitId = model.getUnitId();
		function* getTargets() {
			const snapshot = model.getSnapshot();
			const commandId = _univerjs_docs.SetDocumentPermissionCommand.id;
			const segments = /* @__PURE__ */ new Set([
				"",
				...Object.keys(snapshot.headers ?? {}),
				...Object.keys(snapshot.footers ?? {})
			]);
			for (const segmentId of segments) {
				var _snapshot$headers, _snapshot$footers;
				const body = segmentId ? ((_snapshot$headers = snapshot.headers) === null || _snapshot$headers === void 0 || (_snapshot$headers = _snapshot$headers[segmentId]) === null || _snapshot$headers === void 0 ? void 0 : _snapshot$headers.body) ?? ((_snapshot$footers = snapshot.footers) === null || _snapshot$footers === void 0 || (_snapshot$footers = _snapshot$footers[segmentId]) === null || _snapshot$footers === void 0 ? void 0 : _snapshot$footers.body) : snapshot.body;
				for (const section of (body === null || body === void 0 ? void 0 : body.sectionBreaks) ?? []) {
					if (!section.sectionId) continue;
					const objectId = (0, _univerjs_docs.getDocumentSectionPermissionObjectId)(segmentId, section.sectionId);
					yield {
						target: {
							unitId,
							objectId,
							objectType: _univerjs_protocol.UnitObject.DocumentSection
						},
						name: "",
						commandId,
						exists: () => {
							var _model$getSelfOrHeade;
							return ((_model$getSelfOrHeade = model.getSelfOrHeaderFooterModel(segmentId)) === null || _model$getSelfOrHeade === void 0 || (_model$getSelfOrHeade = _model$getSelfOrHeade.getBody()) === null || _model$getSelfOrHeade === void 0 || (_model$getSelfOrHeade = _model$getSelfOrHeade.sectionBreaks) === null || _model$getSelfOrHeade === void 0 ? void 0 : _model$getSelfOrHeade.some((item) => item.sectionId === section.sectionId)) === true;
						}
					};
				}
			}
		}
		return (0, _univerjs_ui.openObjectPermissionPanel)(accessor, {
			unitId,
			target: {
				unitId,
				objectId: unitId,
				objectType: _univerjs_protocol.UnitObject.DocumentSection
			},
			getTargets
		});
	}
};

//#endregion
//#region src/controllers/doc-paragraph-setting.controller.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const paragraphSettingIndexKey$1 = "doc_ui_paragraph-setting-panel";
let DocParagraphSettingController = class DocParagraphSettingController extends _univerjs_core.Disposable {
	constructor(_commandService, _sidebarService) {
		super();
		this._commandService = _commandService;
		this._sidebarService = _sidebarService;
		_defineProperty(this, "_id", void 0);
	}
	openPanel() {
		const props = {
			header: { title: "docs-ui.doc.slider.paragraphSetting" },
			id: this._id,
			children: { label: paragraphSettingIndexKey$1 },
			width: 300
		};
		this._sidebarService.open(props);
	}
	closePanel() {
		this._sidebarService.close(this._id);
	}
};
DocParagraphSettingController = __decorate([__decorateParam(0, _univerjs_core.ICommandService), __decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_ui.ISidebarService))], DocParagraphSettingController);

//#endregion
//#region src/commands/operations/doc-paragraph-setting-panel.operation.ts
const DocParagraphSettingPanelOperation = {
	id: "sidebar.operation.doc-paragraph-setting-panel",
	type: _univerjs_core.CommandType.OPERATION,
	handler: (accessor) => {
		accessor.get(DocParagraphSettingController).openPanel();
		return true;
	}
};

//#endregion
//#region src/views/section-setting/component-name.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const DOC_SECTION_SETTING_COMPONENT = "doc_ui_section-setting-panel";

//#endregion
//#region src/controllers/doc-section-setting.controller.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
let DocSectionSettingController = class DocSectionSettingController extends _univerjs_core.Disposable {
	constructor(_sidebarService, _selectionManager) {
		super();
		this._sidebarService = _sidebarService;
		this._selectionManager = _selectionManager;
		_defineProperty(this, "_panelId", "DocSectionSetting");
	}
	openPanel() {
		this._sidebarService.open({
			header: { title: "docs-ui.doc.slider.sectionSetting" },
			id: this._panelId,
			children: { label: DOC_SECTION_SETTING_COMPONENT },
			width: 300
		});
	}
	closePanel() {
		this._sidebarService.close(this._panelId);
	}
	navigateToSectionEnd(unitId, sectionStart, sectionEnd) {
		const params = {
			unitId,
			subUnitId: unitId
		};
		const cursorOffset = Math.max(sectionStart, sectionEnd - 1);
		this._selectionManager.replaceDocRanges([{
			startOffset: cursorOffset,
			endOffset: cursorOffset
		}], params, true, { shouldFocus: false });
	}
};
DocSectionSettingController = __decorate([__decorateParam(0, (0, _univerjs_core.Inject)(_univerjs_ui.ISidebarService)), __decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService))], DocSectionSettingController);

//#endregion
//#region src/commands/operations/doc-section-setting-panel.operation.ts
const DocSectionSettingPanelOperation = {
	id: "sidebar.operation.doc-section-setting-panel",
	type: _univerjs_core.CommandType.OPERATION,
	handler: (accessor) => {
		accessor.get(DocSectionSettingController).openPanel();
		return true;
	}
};

//#endregion
//#region src/commands/operations/insert-break.operation.ts
function getCollapsedBodyOffset(selectionManager) {
	const range = selectionManager.getActiveTextRange();
	if (!(range === null || range === void 0 ? void 0 : range.collapsed) || range.segmentId || !Number.isInteger(range.startOffset)) return null;
	return range.startOffset;
}
const InsertDocumentColumnBreakOperation = {
	id: "docs.operation.insert-column-break",
	type: _univerjs_core.CommandType.OPERATION,
	handler: (accessor) => {
		const instanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const selectionManager = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const document = instanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const offset = getCollapsedBodyOffset(selectionManager);
		if (!document || document.getDocumentStyle().documentFlavor !== _univerjs_core.DocumentFlavor.TRADITIONAL || offset == null) return false;
		return commandService.syncExecuteCommand(_univerjs_docs.InsertDocumentColumnBreakCommand.id, {
			unitId: document.getUnitId(),
			offset
		});
	}
};
const InsertDocumentSectionBreakOperation = {
	id: "docs.operation.insert-section-break",
	type: _univerjs_core.CommandType.OPERATION,
	handler: (accessor, params) => {
		var _body$sectionBreaks;
		if (!params) return false;
		const instanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const selectionManager = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const document = instanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const offset = getCollapsedBodyOffset(selectionManager);
		const body = document === null || document === void 0 ? void 0 : document.getBody();
		if (!document || !body || document.getDocumentStyle().documentFlavor !== _univerjs_core.DocumentFlavor.TRADITIONAL || offset == null) return false;
		const sectionId = (0, _univerjs_core.createSectionId)(new Set((_body$sectionBreaks = body.sectionBreaks) === null || _body$sectionBreaks === void 0 ? void 0 : _body$sectionBreaks.map((section) => section.sectionId)));
		return commandService.syncExecuteCommand(_univerjs_docs.InsertDocumentSectionBreakCommand.id, {
			unitId: document.getUnitId(),
			offset,
			sectionId,
			nextSectionType: params.sectionType
		});
	}
};

//#endregion
//#region src/views/PageSettings.tsx
const MODERN_WIDTH_OPTIONS = [
	_univerjs_core.ModernDocumentWidthMode.NARROW,
	_univerjs_core.ModernDocumentWidthMode.MEDIUM,
	_univerjs_core.ModernDocumentWidthMode.WIDE
];
const getPaperSize = (size) => {
	return Object.keys(_univerjs_core.PAGE_SIZE).find((key) => {
		const { width, height } = _univerjs_core.PAGE_SIZE[key];
		if (size.width === width && size.height === height) return true;
		return false;
	}) ?? "A4";
};
const getModernWidthMode = (size) => {
	const width = (size === null || size === void 0 ? void 0 : size.width) ?? _univerjs_core.MODERN_DOCUMENT_WIDTH[_univerjs_core.ModernDocumentWidthMode.MEDIUM];
	return MODERN_WIDTH_OPTIONS.reduce((nearest, option) => {
		const nearestDistance = Math.abs(_univerjs_core.MODERN_DOCUMENT_WIDTH[nearest] - width);
		return Math.abs(_univerjs_core.MODERN_DOCUMENT_WIDTH[option] - width) < nearestDistance ? option : nearest;
	}, _univerjs_core.ModernDocumentWidthMode.MEDIUM);
};
function SettingsLabel(props) {
	const { children, muted } = props;
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("label", {
		className: (0, _univerjs_design.clsx)("univer-text-sm univer-font-medium", muted ? "univer-text-gray-500" : "univer-text-gray-900 dark:!univer-text-gray-0"),
		children
	});
}
function PageSettings(props) {
	const { hooks } = props;
	const documentStyle = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService).getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC).getDocumentStyle();
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const [settings, setSettings] = (0, react.useState)(() => {
		var _documentStyle$pageSi, _documentStyle$pageSi2;
		return {
			mode: documentStyle.documentFlavor === _univerjs_core.DocumentFlavor.TRADITIONAL ? _univerjs_core.DocumentFlavor.TRADITIONAL : _univerjs_core.DocumentFlavor.MODERN,
			modernWidth: getModernWidthMode(documentStyle.pageSize),
			paperSize: getPaperSize(documentStyle.pageSize ?? _univerjs_core.PAGE_SIZE.A4),
			pageSize: {
				width: ((_documentStyle$pageSi = documentStyle.pageSize) === null || _documentStyle$pageSi === void 0 ? void 0 : _documentStyle$pageSi.width) ?? 0,
				height: ((_documentStyle$pageSi2 = documentStyle.pageSize) === null || _documentStyle$pageSi2 === void 0 ? void 0 : _documentStyle$pageSi2.height) ?? 0
			},
			orientation: documentStyle.pageOrient ?? _univerjs_core.PageOrientType.PORTRAIT,
			margins: {
				top: documentStyle.marginTop ?? 0,
				bottom: documentStyle.marginBottom ?? 0,
				left: documentStyle.marginLeft ?? 0,
				right: documentStyle.marginRight ?? 0
			}
		};
	});
	(0, react.useEffect)(() => {
		hooks.beforeClose = () => {
			return settings;
		};
		hooks.beforeConfirm = () => {
			return settings;
		};
	}, [settings]);
	const handlePaperSizeChange = (value) => {
		setSettings((prev) => ({
			...prev,
			paperSize: value,
			pageSize: _univerjs_core.PAGE_SIZE[value]
		}));
	};
	const handleMarginChange = (position, value) => {
		setSettings((prev) => ({
			...prev,
			margins: {
				...prev.margins,
				[position]: value || 0
			}
		}));
	};
	const handleModernWidthChange = (modernWidth) => {
		setSettings((prev) => ({
			...prev,
			modernWidth
		}));
	};
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
		className: "univer-flex univer-flex-col univer-gap-4",
		children: settings.mode === _univerjs_core.DocumentFlavor.MODERN ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			className: "univer-flex univer-flex-col univer-gap-2.5",
			children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingsLabel, { children: localeService.t("docs-ui.page-settings.modern-width") }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				className: "univer-grid univer-grid-cols-3 univer-gap-2",
				children: MODERN_WIDTH_OPTIONS.map((option) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("button", {
					type: "button",
					className: (0, _univerjs_design.clsx)(_univerjs_design.resetButtonClassName, _univerjs_design.borderClassName, "univer-h-16 univer-flex-col univer-gap-1 univer-rounded-lg univer-bg-gray-0 univer-text-sm univer-font-medium univer-text-gray-700 univer-transition-colors hover:univer-border-primary-500 hover:univer-text-primary-600 dark:!univer-bg-gray-900 dark:!univer-text-gray-100", { "!univer-border-primary-500 !univer-bg-primary-50 !univer-text-primary-600 dark:!univer-bg-primary-900": settings.modernWidth === option }),
					onClick: () => handleModernWidthChange(option),
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { children: localeService.t(`docs-ui.page-settings.modern-width-${option}`) }), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
						className: "univer-text-xs univer-font-normal univer-text-gray-400",
						children: [_univerjs_core.MODERN_DOCUMENT_WIDTH[option], "px"]
					})]
				}, option))
			})]
		}) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			className: "univer-flex univer-flex-col univer-gap-2",
			children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingsLabel, { children: localeService.t("docs-ui.page-settings.paper-size") }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Select, {
				value: settings.paperSize,
				onChange: handlePaperSizeChange,
				options: _univerjs_core.PAPER_TYPES.map((p) => ({
					label: localeService.t(`docs-ui.page-settings.page-size.${p.toLocaleLowerCase()}`),
					value: p
				}))
			})]
		}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			className: "univer-flex univer-flex-col univer-gap-2",
			children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingsLabel, { children: localeService.t("docs-ui.page-settings.custom-paper-size") }), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: "univer-flex univer-flex-col univer-gap-2.5",
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: "univer-flex univer-gap-2.5",
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: "univer-flex univer-flex-1 univer-flex-col univer-gap-2",
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingsLabel, {
							muted: true,
							children: localeService.t("docs-ui.page-settings.top")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							precision: 2,
							min: 0,
							max: settings.pageSize.height / 2,
							value: settings.margins.top,
							onChange: (e) => handleMarginChange("top", e)
						})]
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: "univer-flex univer-flex-1 univer-flex-col univer-gap-2",
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingsLabel, {
							muted: true,
							children: localeService.t("docs-ui.page-settings.bottom")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							precision: 2,
							min: 0,
							max: settings.pageSize.height / 2,
							value: settings.margins.bottom,
							onChange: (e) => handleMarginChange("bottom", e)
						})]
					})]
				}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: "univer-flex univer-gap-2.5",
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: "univer-flex univer-flex-1 univer-flex-col univer-gap-2",
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingsLabel, {
							muted: true,
							children: localeService.t("docs-ui.page-settings.left")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							precision: 2,
							min: 0,
							max: settings.pageSize.width / 2,
							value: settings.margins.left,
							onChange: (e) => handleMarginChange("left", e)
						})]
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
						className: "univer-flex univer-flex-1 univer-flex-col univer-gap-2",
						children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingsLabel, {
							muted: true,
							children: localeService.t("docs-ui.page-settings.right")
						}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							precision: 2,
							min: 0,
							max: settings.pageSize.width / 2,
							value: settings.margins.right,
							onChange: (e) => handleMarginChange("right", e)
						})]
					})]
				})]
			})]
		})] })
	});
}
const PAGE_SETTING_COMPONENT_ID = "docs.component.page-setting";

//#endregion
//#region src/commands/commands/doc-page-setup.command.ts
const PAGE_FILL_TABLE_TOLERANCE = 2;
function getPageContentWidth(config) {
	var _config$pageSize;
	const pageWidth = ((_config$pageSize = config.pageSize) === null || _config$pageSize === void 0 ? void 0 : _config$pageSize.width) ?? 0;
	if (!pageWidth) return 0;
	if (config.documentFlavor === _univerjs_core.DocumentFlavor.MODERN) return Math.max(0, pageWidth - _univerjs_core.MODERN_DOCUMENT_DEFAULT_MARGIN * 2);
	return Math.max(0, pageWidth - (config.marginLeft ?? 0) - (config.marginRight ?? 0));
}
function getTableColumnTotalWidth(table) {
	return table.tableColumns.reduce((total, column) => total + (column.size.width.v ?? 0), 0);
}
function isPageFillTable(table, oldContentWidth) {
	if (oldContentWidth <= 0 || table.tableColumns.length === 0) return false;
	const tableWidth = getTableColumnTotalWidth(table) || table.size.width.v;
	return Math.abs(tableWidth - oldContentWidth) <= PAGE_FILL_TABLE_TOLERANCE;
}
function resizePageFillTables(jsonX, rawActions, tableSource, oldContentWidth, newContentWidth) {
	if (!tableSource || oldContentWidth <= 0 || newContentWidth <= 0 || Math.abs(oldContentWidth - newContentWidth) <= PAGE_FILL_TABLE_TOLERANCE) return;
	Object.entries(tableSource).forEach(([tableId, table]) => {
		if (!isPageFillTable(table, oldContentWidth)) return;
		const totalColumnWidth = getTableColumnTotalWidth(table);
		if (totalColumnWidth <= 0) return;
		const tableWidthAction = jsonX.replaceOp([
			"tableSource",
			tableId,
			"size",
			"width",
			"v"
		], table.size.width.v, newContentWidth);
		tableWidthAction && rawActions.push(tableWidthAction);
		table.tableColumns.forEach((column, index) => {
			const nextWidth = column.size.width.v / totalColumnWidth * newContentWidth;
			const action = jsonX.replaceOp([
				"tableSource",
				tableId,
				"tableColumns",
				index,
				"size",
				"width",
				"v"
			], column.size.width.v, nextWidth);
			action && rawActions.push(action);
		});
	});
}
function updateTopLevelSectionPageSetup(jsonX, rawActions, body, config) {
	var _body$sectionBreaks;
	if (!(body === null || body === void 0 || (_body$sectionBreaks = body.sectionBreaks) === null || _body$sectionBreaks === void 0 ? void 0 : _body$sectionBreaks.length)) return;
	const sectionIds = new Set((0, _univerjs_docs.getTopLevelSectionBreaks)(body).map(({ sectionId }) => sectionId));
	body.sectionBreaks.forEach((section, index) => {
		if (!sectionIds.has(section.sectionId)) return;
		const nextSection = {
			...section,
			...config
		};
		const action = jsonX.replaceOp([
			"body",
			"sectionBreaks",
			index
		], section, nextSection);
		action && rawActions.push(action);
	});
}
const DocPageSetupCommand = {
	id: "docs.command.page-setup",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		if (!params) return false;
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!docDataModel) return false;
		const { documentFlavor, marginLeft, marginRight, marginBottom, marginTop, pageOrient, pageSize } = params;
		const jsonX = _univerjs_core.JSONX.getInstance();
		const documentStyle = docDataModel.getDocumentStyle();
		const snapshot = docDataModel.getSnapshot();
		const newDocumentStyle = {
			...documentStyle,
			documentFlavor: documentFlavor ?? documentStyle.documentFlavor,
			marginBottom,
			marginLeft,
			marginRight,
			marginTop,
			pageOrient,
			pageSize
		};
		const { marginBottom: oldMarginBottom, marginLeft: oldMarginLeft, marginRight: oldMarginRight, marginTop: oldMarginTop, pageOrient: oldPageOrient, pageSize: oldPageSize, documentFlavor: oldDocumentFlavor } = documentStyle;
		const rawActions = [];
		resizePageFillTables(jsonX, rawActions, snapshot.tableSource, getPageContentWidth(documentStyle), getPageContentWidth(newDocumentStyle));
		if (newDocumentStyle.documentFlavor !== _univerjs_core.DocumentFlavor.MODERN) updateTopLevelSectionPageSetup(jsonX, rawActions, snapshot.body, {
			marginBottom,
			marginLeft,
			marginRight,
			marginTop,
			pageOrient,
			pageSize
		});
		if (documentFlavor !== void 0) if (oldDocumentFlavor === void 0) {
			const action = jsonX.insertOp(["documentStyle", "documentFlavor"], documentFlavor);
			action && rawActions.push(action);
		} else {
			const action = jsonX.replaceOp(["documentStyle", "documentFlavor"], oldDocumentFlavor, documentFlavor);
			action && rawActions.push(action);
		}
		if (oldMarginBottom === void 0) {
			const action = jsonX.insertOp(["documentStyle", "marginBottom"], marginBottom);
			action && rawActions.push(action);
		} else {
			const action = jsonX.replaceOp(["documentStyle", "marginBottom"], oldMarginBottom, marginBottom);
			action && rawActions.push(action);
		}
		if (oldMarginLeft === void 0) {
			const action = jsonX.insertOp(["documentStyle", "marginLeft"], marginLeft);
			action && rawActions.push(action);
		} else {
			const action = jsonX.replaceOp(["documentStyle", "marginLeft"], oldMarginLeft, marginLeft);
			action && rawActions.push(action);
		}
		if (oldMarginRight === void 0) {
			const action = jsonX.insertOp(["documentStyle", "marginRight"], marginRight);
			action && rawActions.push(action);
		} else {
			const action = jsonX.replaceOp(["documentStyle", "marginRight"], oldMarginRight, marginRight);
			action && rawActions.push(action);
		}
		if (oldMarginTop === void 0) {
			const action = jsonX.insertOp(["documentStyle", "marginTop"], marginTop);
			action && rawActions.push(action);
		} else {
			const action = jsonX.replaceOp(["documentStyle", "marginTop"], oldMarginTop, marginTop);
			action && rawActions.push(action);
		}
		if (oldPageSize === void 0) {
			const action = jsonX.insertOp(["documentStyle", "pageSize"], pageSize);
			action && rawActions.push(action);
		} else {
			const action = jsonX.replaceOp(["documentStyle", "pageSize"], oldPageSize, pageSize);
			action && rawActions.push(action);
		}
		if (oldPageOrient === void 0) {
			const action = jsonX.insertOp(["documentStyle", "pageOrient"], pageOrient);
			action && rawActions.push(action);
		} else {
			const action = jsonX.replaceOp(["documentStyle", "pageOrient"], oldPageOrient, pageOrient);
			action && rawActions.push(action);
		}
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId: docDataModel.getUnitId(),
				historyAction: _univerjs_docs.DocHistoryAction.UpdatePageLayout,
				actions: [],
				textRanges: void 0
			}
		};
		doMutation.params.actions = rawActions.reduce((acc, cur) => {
			return _univerjs_core.JSONX.compose(acc, cur);
		}, null);
		const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
		return Boolean(result);
	}
};

//#endregion
//#region src/commands/operations/open-page-setting.operation.ts
const DocOpenPageSettingCommand = {
	id: "docs.operation.open-page-setting",
	type: _univerjs_core.CommandType.OPERATION,
	handler: (accessor) => {
		const confirmService = accessor.get(_univerjs_core.IConfirmService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const localeService = accessor.get(_univerjs_core.LocaleService);
		const disposable = confirmService.open({
			id: PAGE_SETTING_COMPONENT_ID,
			title: { label: localeService.t("docs-ui.page-settings.document-setting") },
			children: { label: PAGE_SETTING_COMPONENT_ID },
			width: 528,
			onClose: () => {
				disposable.dispose();
			},
			onConfirm: async (result) => {
				if (!result) return;
				const paperSize = result.mode === _univerjs_core.DocumentFlavor.MODERN ? {
					width: _univerjs_core.MODERN_DOCUMENT_WIDTH[result.modernWidth],
					height: _univerjs_core.PAGE_SIZE.A4.height
				} : _univerjs_core.PAGE_SIZE[result.paperSize];
				if (await commandService.executeCommand(DocPageSetupCommand.id, {
					documentFlavor: result.mode,
					pageOrient: result.orientation,
					marginTop: result.margins.top,
					marginBottom: result.margins.bottom,
					marginLeft: result.margins.left,
					marginRight: result.margins.right,
					pageSize: paperSize
				})) disposable.dispose();
			},
			confirmText: localeService.t("docs-ui.page-settings.confirm"),
			cancelText: localeService.t("docs-ui.page-settings.cancel")
		});
		return true;
	}
};

//#endregion
//#region src/commands/commands/insert-special-character.command.ts
const InsertSpecialCharacterCommand = {
	id: "doc.command.insert-special-character",
	type: _univerjs_core.CommandType.COMMAND,
	handler: (accessor, params) => {
		var _docDataModel$getSelf;
		if (!(params === null || params === void 0 ? void 0 : params.value)) return false;
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const docMenuStyleService = accessor.get(DocMenuStyleService);
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const activeRange = docSelectionManagerService.getActiveTextRange();
		if (!docDataModel || !activeRange) return false;
		const { segmentId } = activeRange;
		const originBody = (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		if (!originBody) return false;
		const { value } = params;
		const curTextRun = getTextRunAtPosition(originBody, activeRange.endOffset, docMenuStyleService.getDefaultStyle(), docMenuStyleService.getStyleCache());
		const curCustomRange = getCustomRangeAtPosition(originBody.customRanges ?? [], activeRange.endOffset);
		const curCustomDecorations = getCustomDecorationAtPosition(originBody.customDecorations ?? [], activeRange.endOffset);
		return commandService.executeCommand(_univerjs_docs.InsertTextCommand.id, {
			unitId: docDataModel.getUnitId(),
			body: {
				dataStream: value,
				textRuns: [{
					...curTextRun,
					st: 0,
					ed: value.length
				}],
				customRanges: curCustomRange ? [{
					...curCustomRange,
					startIndex: 0,
					endIndex: value.length - 1
				}] : [],
				customDecorations: curCustomDecorations.map((customDecoration) => ({
					...customDecoration,
					startIndex: 0,
					endIndex: value.length - 1
				}))
			},
			range: activeRange,
			segmentId
		});
	}
};

//#endregion
//#region src/views/const.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const COMPONENT_PREFIX = "UI_PLUGIN_DOCS";

//#endregion
//#region src/views/list-type-picker/Picker.tsx
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const ListTypePicker = (props) => {
	const { value, onChange, options } = props;
	const previewRows = [
		{
			id: "level-0-top",
			levelIndex: 0
		},
		{
			id: "level-1",
			levelIndex: 1
		},
		{
			id: "level-2",
			levelIndex: 2
		},
		{
			id: "level-0-bottom",
			levelIndex: 0
		}
	];
	const previewIndentClassNames = [
		"",
		"univer-pl-2",
		"univer-pl-4"
	];
	const getOrderedGlyph = (glyphType) => {
		switch (glyphType) {
			case _univerjs_core.ListGlyphType.DECIMAL_ZERO: return "01";
			case _univerjs_core.ListGlyphType.UPPER_LETTER: return "A";
			case _univerjs_core.ListGlyphType.LOWER_LETTER: return "a";
			case _univerjs_core.ListGlyphType.UPPER_ROMAN: return "I";
			case _univerjs_core.ListGlyphType.LOWER_ROMAN: return "i";
			default: return "1";
		}
	};
	const getMarker = (listType, levelIndex) => {
		var _level$glyphFormat;
		const levels = _univerjs_core.PRESET_LIST_TYPE[listType].nestingLevel;
		const level = levels[levelIndex];
		if (level.glyphSymbol) return level.glyphSymbol;
		return ((_level$glyphFormat = level.glyphFormat) === null || _level$glyphFormat === void 0 ? void 0 : _level$glyphFormat.replace(/%(\d+)/g, (_match, index) => {
			var _levels;
			return getOrderedGlyph((_levels = levels[Number(index) - 1]) === null || _levels === void 0 ? void 0 : _levels.glyphType);
		})) ?? "1.";
	};
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
		className: "univer-grid univer-grid-cols-3 univer-gap-2 univer-p-1.5",
		"data-u-editor-interaction-boundary": "true",
		children: options.map((item) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("a", {
			className: (0, _univerjs_design.clsx)("univer-block univer-h-20 univer-w-[72px] univer-cursor-pointer univer-overflow-hidden univer-rounded univer-transition-all hover:univer-border-primary-500", _univerjs_design.borderClassName, { "univer-border-primary-500": value === item }),
			onClick: () => onChange(item),
			children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				className: "univer-box-border univer-grid univer-size-full univer-grid-rows-4 univer-gap-1 univer-bg-gray-50 univer-p-2 univer-text-gray-900",
				"data-u-comp": "list-type-preview",
				children: previewRows.map(({ id, levelIndex }) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: (0, _univerjs_design.clsx)("univer-flex univer-items-center univer-gap-1", previewIndentClassNames[levelIndex]),
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
						className: "univer-w-3 univer-shrink-0 univer-overflow-hidden univer-text-center univer-text-[10px] univer-leading-none",
						children: getMarker(item, levelIndex)
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { className: "univer-h-1 univer-flex-1 univer-rounded univer-bg-gray-200" })]
				}, id))
			})
		}, item))
	});
};
const orderListOptions = [
	_univerjs_core.PresetListType.ORDER_LIST,
	_univerjs_core.PresetListType.ORDER_LIST_1,
	_univerjs_core.PresetListType.ORDER_LIST_2,
	_univerjs_core.PresetListType.ORDER_LIST_3,
	_univerjs_core.PresetListType.ORDER_LIST_4,
	_univerjs_core.PresetListType.ORDER_LIST_5
];
const OrderListTypePicker = (props) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ListTypePicker, {
	...props,
	options: orderListOptions
});
const bulletOptions = [
	_univerjs_core.PresetListType.BULLET_LIST,
	_univerjs_core.PresetListType.BULLET_LIST_1,
	_univerjs_core.PresetListType.BULLET_LIST_2,
	_univerjs_core.PresetListType.BULLET_LIST_3,
	_univerjs_core.PresetListType.BULLET_LIST_4,
	_univerjs_core.PresetListType.BULLET_LIST_5
];
const BulletListTypePicker = (props) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ListTypePicker, {
	...props,
	options: bulletOptions
});

//#endregion
//#region src/views/list-type-picker/index.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const ORDER_LIST_TYPE_COMPONENT = `${COMPONENT_PREFIX}_ORDER_LIST_TYPE_COMPONENT`;
const BULLET_LIST_TYPE_COMPONENT = `${COMPONENT_PREFIX}_BULLET_LIST_TYPE_COMPONENT`;

//#endregion
//#region src/menu/menu.ts
function shouldSuppressDocMenuStateRefresh(accessor) {
	let univerInstanceService;
	let focusCoordinator;
	try {
		univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	} catch (error) {
		if (isInjectorDisposedError(error)) return true;
		throw error;
	}
	try {
		focusCoordinator = accessor.get(IDocEmbedRuntimeFocusCoordinator);
	} catch {
		return false;
	}
	const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
	const unitId = docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getUnitId();
	return focusCoordinator.shouldSuppressHostInteraction(unitId);
}
function getInsertTableHiddenObservable(accessor) {
	const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	const renderManagerService = accessor.get(_univerjs_engine_render.IRenderManagerService);
	return new rxjs.Observable((subscriber) => {
		let editAreaSubscription = null;
		const subscription = univerInstanceService.focused$.subscribe((unitId) => {
			editAreaSubscription === null || editAreaSubscription === void 0 || editAreaSubscription.unsubscribe();
			editAreaSubscription = null;
			if (unitId == null) return subscriber.next(true);
			if (univerInstanceService.getUnitType(unitId) !== _univerjs_core.UniverInstanceType.UNIVER_DOC) return subscriber.next(true);
			const currentRender = renderManagerService.getRenderUnitById(unitId);
			if (currentRender == null) return subscriber.next(true);
			editAreaSubscription = currentRender.with(_univerjs_docs.DocSkeletonManagerService).getViewModel().editAreaChange$.subscribe((editArea) => {
				subscriber.next(editArea === _univerjs_engine_render.DocumentEditArea.HEADER || editArea === _univerjs_engine_render.DocumentEditArea.FOOTER);
			});
		});
		return () => {
			editAreaSubscription === null || editAreaSubscription === void 0 || editAreaSubscription.unsubscribe();
			subscription.unsubscribe();
		};
	});
}
function disableMenuWhenHeaderFooterEditing(accessor) {
	const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	const renderManagerService = accessor.get(_univerjs_engine_render.IRenderManagerService);
	return new rxjs.Observable((subscriber) => {
		let editAreaSubscription = null;
		let lastDisabled;
		const emit = (disabled) => {
			if (disabled !== lastDisabled) {
				lastDisabled = disabled;
				subscriber.next(disabled);
			}
		};
		const emitByUnit = (unitId) => {
			editAreaSubscription === null || editAreaSubscription === void 0 || editAreaSubscription.unsubscribe();
			editAreaSubscription = null;
			if (!unitId || univerInstanceService.getUnitType(unitId) !== _univerjs_core.UniverInstanceType.UNIVER_DOC) {
				emit(true);
				return;
			}
			const currentRender = renderManagerService.getRenderUnitById(unitId);
			const skeletonManager = currentRender === null || currentRender === void 0 ? void 0 : currentRender.with(_univerjs_docs.DocSkeletonManagerService);
			const viewModel = skeletonManager === null || skeletonManager === void 0 ? void 0 : skeletonManager.getViewModel();
			if (!viewModel) {
				emit(true);
				return;
			}
			const emitDisabled = (editArea) => {
				const currentEditArea = editArea ?? viewModel.getEditArea();
				emit(currentEditArea === _univerjs_engine_render.DocumentEditArea.HEADER || currentEditArea === _univerjs_engine_render.DocumentEditArea.FOOTER);
			};
			emitDisabled();
			editAreaSubscription = viewModel.editAreaChange$.subscribe(emitDisabled);
		};
		const focusedSubscription = univerInstanceService.focused$.subscribe(emitByUnit);
		return () => {
			editAreaSubscription === null || editAreaSubscription === void 0 || editAreaSubscription.unsubscribe();
			focusedSubscription.unsubscribe();
		};
	});
}
function getTraditionalDocMenuHiddenObservable(accessor) {
	const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return new rxjs.Observable((subscriber) => {
		const subscription0 = commandService.onCommandExecuted((command) => {
			if (command.id === _univerjs_docs.RichTextEditingMutation.id) {
				const { unitId } = command.params;
				const docDataModel = univerInstanceService.getUnit(unitId);
				if (docDataModel == null) {
					subscriber.next(true);
					return;
				}
				const { documentStyle } = docDataModel.getSnapshot();
				subscriber.next((documentStyle === null || documentStyle === void 0 ? void 0 : documentStyle.documentFlavor) !== _univerjs_core.DocumentFlavor.TRADITIONAL);
			}
		});
		const subscription = univerInstanceService.focused$.subscribe((unitId) => {
			if (unitId == null) return subscriber.next(true);
			const docDataModel = univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
			const documentFlavor = docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getSnapshot().documentStyle.documentFlavor;
			subscriber.next(documentFlavor !== _univerjs_core.DocumentFlavor.TRADITIONAL);
		});
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const documentFlavor = docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getSnapshot().documentStyle.documentFlavor;
		subscriber.next(docDataModel == null || documentFlavor !== _univerjs_core.DocumentFlavor.TRADITIONAL);
		return () => {
			subscription0.dispose();
			subscription.unsubscribe();
		};
	});
}
function getTableDisabledObservable(accessor) {
	const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
	const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	return new rxjs.Observable((subscriber) => {
		const subscription = docSelectionManagerService.textSelection$.subscribe((selection) => {
			if (selection == null) {
				subscriber.next(true);
				return;
			}
			const { textRanges } = selection;
			if (textRanges.length !== 1) {
				subscriber.next(true);
				return;
			}
			const { collapsed, startNodePosition, startOffset } = textRanges[0];
			if (!collapsed || startOffset == null) {
				subscriber.next(true);
				return;
			}
			const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
			if (docDataModel == null) {
				subscriber.next(true);
				return;
			}
			const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
			if (docSkeletonManagerService == null) {
				subscriber.next(true);
				return;
			}
			if (docSkeletonManagerService.getViewModel().getCustomRangeRaw(startOffset)) {
				subscriber.next(true);
				return;
			}
			if (startNodePosition != null) {
				const { path } = startNodePosition;
				if (path.indexOf("cells") !== -1) {
					subscriber.next(true);
					return;
				}
			}
			subscriber.next(false);
		});
		return () => subscription.unsubscribe();
	});
}
function disableMenuWhenNoDocRange(accessor) {
	const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
	const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	return (0, rxjs.combineLatest)([new rxjs.Observable((subscriber) => {
		const subscription = docSelectionManagerService.textSelection$.subscribe((selection) => {
			var _document$getBody;
			if (selection == null) {
				subscriber.next(true);
				return;
			}
			const { textRanges, rectRanges } = selection;
			if (textRanges.length === 0 && rectRanges.length === 0) {
				subscriber.next(true);
				return;
			}
			const document = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
			if (((document === null || document === void 0 || (_document$getBody = document.getBody()) === null || _document$getBody === void 0 || (_document$getBody = _document$getBody.blockRanges) === null || _document$getBody === void 0 ? void 0 : _document$getBody.filter((range) => range.blockType === _univerjs_core.DocumentBlockRangeType.CODE)) ?? []).some((blockRange) => textRanges.some((range) => Math.max(range.startOffset, blockRange.startIndex) <= Math.min(range.endOffset, blockRange.endIndex)))) {
				subscriber.next(true);
				return;
			}
			subscriber.next(false);
		});
		return () => subscription.unsubscribe();
	}), disableMenuWithoutDocumentEditPermission(accessor)]).pipe((0, rxjs.map)(([selectionDisabled, permissionDisabled]) => selectionDisabled || permissionDisabled));
}
function disableMenuWithoutDocumentUnitPermission(accessor, action) {
	const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	const permissionService = accessor.get(_univerjs_core.IPermissionService);
	return (0, rxjs.combineLatest)([univerInstanceService.getCurrentTypeOfUnit$(_univerjs_core.UniverInstanceType.UNIVER_DOC), permissionService.permissionPointUpdate$.pipe((0, rxjs.startWith)(void 0))]).pipe((0, rxjs.map)(([document]) => !document || !(0, _univerjs_docs.getDocumentPermissionValue)(permissionService, document.getUnitId(), document.getUnitId(), action)));
}
function disableMenuWithoutDocumentEditPermission(accessor) {
	return disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit);
}
const DOC_INSERT_EMOJI_MENU_ID = "doc.menu.insert-emoji";
const DOC_INSERT_SYMBOL_MENU_ID = "doc.menu.insert-symbol";
function EmojiPickerMenuItemFactory(accessor) {
	return {
		id: DOC_INSERT_EMOJI_MENU_ID,
		type: _univerjs_ui.MenuItemType.SELECTOR,
		icon: "SmileIcon",
		tooltip: "ui.emojiPicker.emojis",
		selectionsCommandId: InsertSpecialCharacterCommand.id,
		selections: [{ label: {
			name: _univerjs_ui.EMOJI_PICKER_COMPONENT,
			hoverable: false,
			selectable: false,
			props: { embedded: true }
		} }],
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function SymbolPickerMenuItemFactory(accessor) {
	return {
		id: DOC_INSERT_SYMBOL_MENU_ID,
		type: _univerjs_ui.MenuItemType.SELECTOR,
		icon: "SymbolsIcon",
		tooltip: "ui.emojiPicker.symbols",
		selectionsCommandId: InsertSpecialCharacterCommand.id,
		selections: [{ label: {
			name: _univerjs_ui.SYMBOL_PICKER_COMPONENT,
			hoverable: false,
			selectable: false,
			props: { embedded: true }
		} }],
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function isTextRangeInAnyBlockRange(document, range) {
	var _document$getBody2;
	const blockRanges = (document === null || document === void 0 || (_document$getBody2 = document.getBody()) === null || _document$getBody2 === void 0 ? void 0 : _document$getBody2.blockRanges) ?? [];
	const startOffset = range.startOffset;
	const endOffset = range.collapsed ? range.startOffset : range.endOffset - 1;
	return blockRanges.some((blockRange) => Math.max(startOffset, blockRange.startIndex) <= Math.min(endOffset, blockRange.endIndex));
}
function hideMenuWhenSelectionInBlockRange(accessor) {
	const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
	const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	return new rxjs.Observable((subscriber) => {
		const calc = (selection) => {
			var _getCurrentSelection;
			const currentSelection = (_getCurrentSelection = docSelectionManagerService.__getCurrentSelection) === null || _getCurrentSelection === void 0 ? void 0 : _getCurrentSelection.call(docSelectionManagerService);
			const textRanges = (selection === null || selection === void 0 ? void 0 : selection.textRanges) ?? [...docSelectionManagerService.getTextRanges() ?? []];
			const unitId = (selection === null || selection === void 0 ? void 0 : selection.unitId) ?? (currentSelection === null || currentSelection === void 0 ? void 0 : currentSelection.unitId);
			const document = unitId ? univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC) : univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
			subscriber.next(textRanges.some((range) => isTextRangeInAnyBlockRange(document, range)));
		};
		calc();
		const subscription = docSelectionManagerService.textSelection$.subscribe((selection) => calc(selection));
		return () => subscription.unsubscribe();
	});
}
function BoldMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: SetInlineFormatBoldCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "BoldIcon",
		title: "docs-ui.toolbar.bold",
		tooltip: "docs-ui.toolbar.bold",
		activated$: new rxjs.Observable((subscriber) => {
			const calc = () => {
				var _textRun$ts;
				if (shouldSuppressDocMenuStateRefresh(accessor)) return;
				const textRun = getFontStyleAtCursor(accessor);
				if (textRun == null) {
					subscriber.next(false);
					return;
				}
				const bl = (_textRun$ts = textRun.ts) === null || _textRun$ts === void 0 ? void 0 : _textRun$ts.bl;
				subscriber.next(bl === _univerjs_core.BooleanNumber.TRUE);
			};
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) calc();
			});
			calc();
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ItalicMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: SetInlineFormatItalicCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "ItalicIcon",
		title: "docs-ui.toolbar.italic",
		tooltip: "docs-ui.toolbar.italic",
		activated$: new rxjs.Observable((subscriber) => {
			const calc = () => {
				var _textRun$ts2;
				if (shouldSuppressDocMenuStateRefresh(accessor)) return;
				const textRun = getFontStyleAtCursor(accessor);
				if (textRun == null) {
					subscriber.next(false);
					return;
				}
				const it = (_textRun$ts2 = textRun.ts) === null || _textRun$ts2 === void 0 ? void 0 : _textRun$ts2.it;
				subscriber.next(it === _univerjs_core.BooleanNumber.TRUE);
			};
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) calc();
			});
			calc();
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function UnderlineMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: SetInlineFormatUnderlineCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "UnderlineIcon",
		title: "docs-ui.toolbar.underline",
		tooltip: "docs-ui.toolbar.underline",
		activated$: new rxjs.Observable((subscriber) => {
			const calc = () => {
				var _textRun$ts3;
				if (shouldSuppressDocMenuStateRefresh(accessor)) return;
				const textRun = getFontStyleAtCursor(accessor);
				if (textRun == null) {
					subscriber.next(false);
					return;
				}
				const ul = (_textRun$ts3 = textRun.ts) === null || _textRun$ts3 === void 0 ? void 0 : _textRun$ts3.ul;
				subscriber.next((ul === null || ul === void 0 ? void 0 : ul.s) === _univerjs_core.BooleanNumber.TRUE);
			};
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) calc();
			});
			calc();
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function StrikeThroughMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: SetInlineFormatStrikethroughCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "StrikethroughIcon",
		title: "docs-ui.toolbar.strikethrough",
		tooltip: "docs-ui.toolbar.strikethrough",
		activated$: new rxjs.Observable((subscriber) => {
			const calc = () => {
				var _textRun$ts4;
				if (shouldSuppressDocMenuStateRefresh(accessor)) return;
				const textRun = getFontStyleAtCursor(accessor);
				if (textRun == null) {
					subscriber.next(false);
					return;
				}
				const st = (_textRun$ts4 = textRun.ts) === null || _textRun$ts4 === void 0 ? void 0 : _textRun$ts4.st;
				subscriber.next((st === null || st === void 0 ? void 0 : st.s) === _univerjs_core.BooleanNumber.TRUE);
			};
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) calc();
			});
			calc();
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function SubscriptMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: SetInlineFormatSubscriptCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "SubscriptIcon",
		tooltip: "docs-ui.toolbar.subscript",
		activated$: new rxjs.Observable((subscriber) => {
			const calc = () => {
				var _textRun$ts5;
				if (shouldSuppressDocMenuStateRefresh(accessor)) return;
				const textRun = getFontStyleAtCursor(accessor);
				if (textRun == null) {
					subscriber.next(false);
					return;
				}
				const va = (_textRun$ts5 = textRun.ts) === null || _textRun$ts5 === void 0 ? void 0 : _textRun$ts5.va;
				subscriber.next(va === _univerjs_core.BaselineOffset.SUBSCRIPT);
			};
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) calc();
			});
			calc();
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function SuperscriptMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: SetInlineFormatSuperscriptCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "SuperscriptIcon",
		tooltip: "docs-ui.toolbar.superscript",
		activated$: new rxjs.Observable((subscriber) => {
			const calc = () => {
				var _textRun$ts6;
				if (shouldSuppressDocMenuStateRefresh(accessor)) return;
				const textRun = getFontStyleAtCursor(accessor);
				if (textRun == null) {
					subscriber.next(false);
					return;
				}
				const va = (_textRun$ts6 = textRun.ts) === null || _textRun$ts6 === void 0 ? void 0 : _textRun$ts6.va;
				subscriber.next(va === _univerjs_core.BaselineOffset.SUPERSCRIPT);
			};
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) calc();
			});
			calc();
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function FontFamilySelectorMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: SetInlineFormatFontFamilyCommand.id,
		tooltip: "docs-ui.toolbar.font",
		type: _univerjs_ui.MenuItemType.SELECTOR,
		label: {
			name: _univerjs_ui.FONT_FAMILY_COMPONENT,
			props: { id: SetInlineFormatFontFamilyCommand.id }
		},
		selections: [{ label: {
			name: _univerjs_ui.FONT_FAMILY_ITEM_COMPONENT,
			hoverable: false,
			selectable: false,
			props: { id: SetInlineFormatFontFamilyCommand.id }
		} }],
		value$: new rxjs.Observable((subscriber) => {
			const defaultValue = _univerjs_core.DEFAULT_STYLES.ff;
			const calc = () => {
				var _textRun$ts7;
				if (shouldSuppressDocMenuStateRefresh(accessor)) return;
				const textRun = getFontStyleAtCursor(accessor);
				if (textRun == null) {
					subscriber.next(defaultValue);
					return;
				}
				const ff = (_textRun$ts7 = textRun.ts) === null || _textRun$ts7 === void 0 ? void 0 : _textRun$ts7.ff;
				subscriber.next(ff ?? defaultValue);
			};
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === SetInlineFormatFontFamilyCommand.id) calc();
			});
			calc();
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function FontSizeSelectorMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: SetInlineFormatFontSizeCommand.id,
		type: _univerjs_ui.MenuItemType.SELECTOR,
		tooltip: "docs-ui.toolbar.fontSize",
		label: {
			name: _univerjs_ui.FONT_SIZE_COMPONENT,
			props: {
				min: 1,
				max: 400
			}
		},
		selections: _univerjs_ui.FONT_SIZE_LIST,
		value$: new rxjs.Observable((subscriber) => {
			const DEFAULT_SIZE = _univerjs_core.DEFAULT_STYLES.fs;
			const calc = () => {
				var _textRun$ts8;
				if (shouldSuppressDocMenuStateRefresh(accessor)) return;
				const textRun = getFontStyleAtCursor(accessor);
				if (textRun == null) {
					subscriber.next(DEFAULT_SIZE);
					return;
				}
				const fs = (_textRun$ts8 = textRun.ts) === null || _textRun$ts8 === void 0 ? void 0 : _textRun$ts8.fs;
				subscriber.next(fs ?? DEFAULT_SIZE);
			};
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === SetInlineFormatFontSizeCommand.id) calc();
			});
			calc();
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function HeadingSelectorMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: SetParagraphNamedStyleCommand.id,
		type: _univerjs_ui.MenuItemType.SELECTOR,
		tooltip: "docs-ui.toolbar.heading.tooltip",
		label: {
			name: _univerjs_ui.COMMON_LABEL_COMPONENT,
			props: { selections: _univerjs_ui.HEADING_LIST }
		},
		selections: _univerjs_ui.HEADING_LIST.map((item) => ({
			label: {
				name: _univerjs_ui.HEADING_ITEM_COMPONENT,
				props: {
					value: item.value,
					text: item.label
				}
			},
			value: item.value
		})),
		value$: new rxjs.Observable((subscriber) => {
			const DEFAULT_TYPE = _univerjs_core.NamedStyleType.NORMAL_TEXT;
			const calc = () => {
				var _paragraph$paragraphS;
				if (shouldSuppressDocMenuStateRefresh(accessor)) return;
				const paragraph = getParagraphStyleAtCursor(accessor);
				if (paragraph == null) {
					subscriber.next(DEFAULT_TYPE);
					return;
				}
				const namedStyleType = ((_paragraph$paragraphS = paragraph.paragraphStyle) === null || _paragraph$paragraphS === void 0 ? void 0 : _paragraph$paragraphS.namedStyleType) ?? DEFAULT_TYPE;
				subscriber.next(namedStyleType);
			};
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === SetInlineFormatFontSizeCommand.id) calc();
			});
			calc();
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
const FLOAT_TEXT_STYLE_MENU_ID = "doc.menu.float-text-style";
const FLOAT_TOOLBAR_MENU_POSITION = "doc.menu.float-toolbar";
const FLOAT_TEXT_STYLE_OPTIONS = [
	{
		icon: "TextTypeIcon",
		label: "docs-ui.toolbar.heading.normal",
		value: _univerjs_core.NamedStyleType.NORMAL_TEXT
	},
	{
		icon: "H1Icon",
		label: "docs-ui.toolbar.heading.leading1",
		value: _univerjs_core.NamedStyleType.HEADING_1
	},
	{
		icon: "H2Icon",
		label: "docs-ui.toolbar.heading.leading2",
		value: _univerjs_core.NamedStyleType.HEADING_2
	},
	{
		icon: "H3Icon",
		label: "docs-ui.toolbar.heading.leading3",
		value: _univerjs_core.NamedStyleType.HEADING_3
	},
	{
		icon: "H4Icon",
		label: "docs-ui.toolbar.heading.leading4",
		value: _univerjs_core.NamedStyleType.HEADING_4
	},
	{
		icon: "H5Icon",
		label: "docs-ui.toolbar.heading.leading5",
		value: _univerjs_core.NamedStyleType.HEADING_5
	},
	{
		id: OrderListCommand.id,
		icon: "OrderIcon",
		label: "docs-ui.toolbar.order",
		value: _univerjs_core.PresetListType.ORDER_LIST
	},
	{
		id: BulletListCommand.id,
		icon: "UnorderIcon",
		label: "docs-ui.toolbar.unorder",
		value: _univerjs_core.PresetListType.BULLET_LIST
	},
	{
		id: CheckListCommand.id,
		icon: "TodoListDoubleIcon",
		label: "docs-ui.toolbar.checklist",
		value: _univerjs_core.PresetListType.CHECK_LIST
	}
];
function normalizeFloatingTextStyleValue(paragraph) {
	var _paragraph$bullet, _paragraph$paragraphS2;
	const listType = paragraph === null || paragraph === void 0 || (_paragraph$bullet = paragraph.bullet) === null || _paragraph$bullet === void 0 ? void 0 : _paragraph$bullet.listType;
	if (listType === null || listType === void 0 ? void 0 : listType.startsWith(_univerjs_core.PresetListType.ORDER_LIST)) return _univerjs_core.PresetListType.ORDER_LIST;
	if (listType === null || listType === void 0 ? void 0 : listType.startsWith(_univerjs_core.PresetListType.BULLET_LIST)) return _univerjs_core.PresetListType.BULLET_LIST;
	if (listType === _univerjs_core.PresetListType.CHECK_LIST || listType === _univerjs_core.PresetListType.CHECK_LIST_CHECKED) return _univerjs_core.PresetListType.CHECK_LIST;
	return (paragraph === null || paragraph === void 0 || (_paragraph$paragraphS2 = paragraph.paragraphStyle) === null || _paragraph$paragraphS2 === void 0 ? void 0 : _paragraph$paragraphS2.namedStyleType) ?? _univerjs_core.NamedStyleType.NORMAL_TEXT;
}
function FloatTextStyleMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: FLOAT_TEXT_STYLE_MENU_ID,
		commandId: SetParagraphNamedStyleCommand.id,
		type: _univerjs_ui.MenuItemType.SELECTOR,
		icon: (0, rxjs.of)(""),
		label: {
			name: _univerjs_ui.COMMON_LABEL_COMPONENT,
			props: { selections: FLOAT_TEXT_STYLE_OPTIONS }
		},
		tooltip: "docs-ui.toolbar.heading.tooltip",
		selections: FLOAT_TEXT_STYLE_OPTIONS,
		value$: new rxjs.Observable((subscriber) => {
			const calc = () => {
				if (shouldSuppressDocMenuStateRefresh(accessor)) return;
				subscriber.next(normalizeFloatingTextStyleValue(getParagraphStyleAtCursor(accessor)));
			};
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === SetParagraphNamedStyleCommand.id || id === OrderListCommand.id || id === BulletListCommand.id || id === CheckListCommand.id) calc();
			});
			calc();
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function TextColorSelectorMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	const themeService = accessor.get(_univerjs_core.ThemeService);
	return {
		id: SetInlineFormatTextColorCommand.id,
		icon: "FontColorDoubleIcon",
		tooltip: "docs-ui.toolbar.textColor.main",
		type: _univerjs_ui.MenuItemType.BUTTON_SELECTOR,
		selections: [{
			label: {
				name: _univerjs_ui.COLOR_PICKER_COMPONENT,
				hoverable: false,
				selectable: false
			},
			value$: new rxjs.Observable((subscriber) => {
				const defaultValue = themeService.getColorFromTheme("gray.900");
				const calc = () => {
					var _textRun$ts9;
					if (shouldSuppressDocMenuStateRefresh(accessor)) return;
					const textRun = getFontStyleAtCursor(accessor);
					if (!textRun) {
						subscriber.next(defaultValue);
						return;
					}
					const color = (_textRun$ts9 = textRun.ts) === null || _textRun$ts9 === void 0 || (_textRun$ts9 = _textRun$ts9.cl) === null || _textRun$ts9 === void 0 ? void 0 : _textRun$ts9.rgb;
					subscriber.next(color ?? defaultValue);
				};
				const disposable = commandService.onCommandExecuted((c) => {
					if (c.id === SetInlineFormatTextColorCommand.id) calc();
				});
				calc();
				return disposable.dispose;
			})
		}],
		value$: new rxjs.Observable((subscriber) => {
			const defaultColor = themeService.getColorFromTheme("gray.900");
			const disposable = commandService.onCommandExecuted((c) => {
				if (c.id === SetInlineFormatTextColorCommand.id) {
					if (shouldSuppressDocMenuStateRefresh(accessor)) return;
					const color = c.params.value;
					subscriber.next(color ?? defaultColor);
				}
			});
			subscriber.next(defaultColor);
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function HeaderFooterMenuItemFactory(accessor) {
	return {
		id: OpenHeaderFooterPanelCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "HeaderFooterIcon",
		tooltip: "docs-ui.toolbar.headerFooter",
		hidden$: (0, rxjs.combineLatest)((0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC), getTraditionalDocMenuHiddenObservable(accessor), (one, two) => {
			return one || two;
		})
	};
}
const DOC_BREAKS_MENU_ID = "doc.menu.breaks";
const DOC_SECTION_BREAK_NEXT_PAGE_MENU_ID = "doc.menu.section-break.next-page";
const DOC_SECTION_BREAK_CONTINUOUS_MENU_ID = "doc.menu.section-break.continuous";
const DOC_SECTION_BREAK_NEXT_COLUMN_MENU_ID = "doc.menu.section-break.next-column";
const DOC_SECTION_BREAK_EVEN_PAGE_MENU_ID = "doc.menu.section-break.even-page";
const DOC_SECTION_BREAK_ODD_PAGE_MENU_ID = "doc.menu.section-break.odd-page";
function BreaksMenuFactory(accessor) {
	return {
		id: DOC_BREAKS_MENU_ID,
		type: _univerjs_ui.MenuItemType.SUBITEMS,
		icon: "ColumnIcon",
		tooltip: "docs-ui.toolbar.breaks",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, rxjs.combineLatest)((0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC), getTraditionalDocMenuHiddenObservable(accessor), (hidden, unsupported) => hidden || unsupported)
	};
}
function InsertColumnBreakMenuFactory(accessor) {
	return {
		id: InsertDocumentColumnBreakOperation.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.toolbar.columnBreak",
		disabled$: disableMenuWhenNoDocRange(accessor)
	};
}
function createSectionBreakMenuItem(accessor, id, title, sectionType) {
	return {
		id,
		commandId: InsertDocumentSectionBreakOperation.id,
		params: { sectionType },
		type: _univerjs_ui.MenuItemType.BUTTON,
		title,
		disabled$: disableMenuWhenNoDocRange(accessor)
	};
}
function InsertNextPageSectionBreakMenuFactory(accessor) {
	return createSectionBreakMenuItem(accessor, DOC_SECTION_BREAK_NEXT_PAGE_MENU_ID, "docs-ui.toolbar.sectionBreakNextPage", _univerjs_core.SectionType.NEXT_PAGE);
}
function InsertContinuousSectionBreakMenuFactory(accessor) {
	return createSectionBreakMenuItem(accessor, DOC_SECTION_BREAK_CONTINUOUS_MENU_ID, "docs-ui.toolbar.sectionBreakContinuous", _univerjs_core.SectionType.CONTINUOUS);
}
function InsertNextColumnSectionBreakMenuFactory(accessor) {
	return createSectionBreakMenuItem(accessor, DOC_SECTION_BREAK_NEXT_COLUMN_MENU_ID, "docs-ui.toolbar.sectionBreakNextColumn", _univerjs_core.SectionType.NEXT_COLUMN);
}
function InsertEvenPageSectionBreakMenuFactory(accessor) {
	return createSectionBreakMenuItem(accessor, DOC_SECTION_BREAK_EVEN_PAGE_MENU_ID, "docs-ui.toolbar.sectionBreakEvenPage", _univerjs_core.SectionType.EVEN_PAGE);
}
function InsertOddPageSectionBreakMenuFactory(accessor) {
	return createSectionBreakMenuItem(accessor, DOC_SECTION_BREAK_ODD_PAGE_MENU_ID, "docs-ui.toolbar.sectionBreakOddPage", _univerjs_core.SectionType.ODD_PAGE);
}
const TableIcon = "GridIcon";
const TABLE_MENU_ID = "doc.menu.table";
function TableMenuFactory(accessor) {
	return {
		id: TABLE_MENU_ID,
		type: _univerjs_ui.MenuItemType.SUBITEMS,
		icon: TableIcon,
		tooltip: "docs-ui.toolbar.table.main",
		disabled$: getTableDisabledObservable(accessor),
		hidden$: (0, rxjs.combineLatest)((0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC), getInsertTableHiddenObservable(accessor), (one, two) => {
			return one || two;
		})
	};
}
function InsertTableMenuFactory(_accessor) {
	return {
		id: DocCreateTableOperation.id,
		title: "docs-ui.toolbar.table.insert",
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: TableIcon,
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(_accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function InsertDefaultTableMenuFactory(_accessor) {
	return {
		id: DocCreateTableOperation.id,
		commandId: CreateDocTableCommand.id,
		params: {
			rowCount: 3,
			colCount: 5
		},
		title: "docs-ui.toolbar.table.insert",
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: TableIcon,
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(_accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function AlignLeftMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: AlignLeftCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "LeftJustifyingIcon",
		tooltip: "docs-ui.toolbar.alignLeft",
		disabled$: disableMenuWhenNoDocRange(accessor),
		activated$: new rxjs.Observable((subscriber) => {
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === AlignOperationCommand.id) {
					var _paragraph$paragraphS3;
					if (shouldSuppressDocMenuStateRefresh(accessor)) return;
					const paragraph = getParagraphStyleAtCursor(accessor);
					if (paragraph == null) return;
					const alignType = (_paragraph$paragraphS3 = paragraph.paragraphStyle) === null || _paragraph$paragraphS3 === void 0 ? void 0 : _paragraph$paragraphS3.horizontalAlign;
					subscriber.next(alignType === _univerjs_core.HorizontalAlign.LEFT);
				}
			});
			subscriber.next(false);
			return disposable.dispose;
		}),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function AlignCenterMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: AlignCenterCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "HorizontallyIcon",
		tooltip: "docs-ui.toolbar.alignCenter",
		activated$: new rxjs.Observable((subscriber) => {
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === AlignOperationCommand.id) {
					var _paragraph$paragraphS4;
					if (shouldSuppressDocMenuStateRefresh(accessor)) return;
					const paragraph = getParagraphStyleAtCursor(accessor);
					if (paragraph == null) return;
					const alignType = (_paragraph$paragraphS4 = paragraph.paragraphStyle) === null || _paragraph$paragraphS4 === void 0 ? void 0 : _paragraph$paragraphS4.horizontalAlign;
					subscriber.next(alignType === _univerjs_core.HorizontalAlign.CENTER);
				}
			});
			subscriber.next(false);
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function AlignRightMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: AlignRightCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "RightJustifyingIcon",
		tooltip: "docs-ui.toolbar.alignRight",
		activated$: new rxjs.Observable((subscriber) => {
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === AlignOperationCommand.id) {
					var _paragraph$paragraphS5;
					if (shouldSuppressDocMenuStateRefresh(accessor)) return;
					const paragraph = getParagraphStyleAtCursor(accessor);
					if (paragraph == null) return;
					const alignType = (_paragraph$paragraphS5 = paragraph.paragraphStyle) === null || _paragraph$paragraphS5 === void 0 ? void 0 : _paragraph$paragraphS5.horizontalAlign;
					subscriber.next(alignType === _univerjs_core.HorizontalAlign.RIGHT);
				}
			});
			subscriber.next(false);
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function AlignJustifyMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return {
		id: AlignJustifyCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "AlignTextBothIcon",
		tooltip: "docs-ui.toolbar.alignJustify",
		activated$: new rxjs.Observable((subscriber) => {
			const disposable = commandService.onCommandExecuted((c) => {
				const id = c.id;
				if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === AlignOperationCommand.id) {
					var _paragraph$paragraphS6;
					if (shouldSuppressDocMenuStateRefresh(accessor)) return;
					const paragraph = getParagraphStyleAtCursor(accessor);
					if (paragraph == null) return;
					const alignType = (_paragraph$paragraphS6 = paragraph.paragraphStyle) === null || _paragraph$paragraphS6 === void 0 ? void 0 : _paragraph$paragraphS6.horizontalAlign;
					subscriber.next(alignType === _univerjs_core.HorizontalAlign.JUSTIFIED);
				}
			});
			subscriber.next(false);
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
const HORIZONTAL_ALIGN_OPTIONS = [
	{
		id: AlignLeftCommand.id,
		value: _univerjs_core.HorizontalAlign.LEFT,
		label: "docs-ui.toolbar.alignLeft",
		icon: "LeftJustifyingIcon"
	},
	{
		id: AlignCenterCommand.id,
		value: _univerjs_core.HorizontalAlign.CENTER,
		label: "docs-ui.toolbar.alignCenter",
		icon: "HorizontallyIcon"
	},
	{
		id: AlignRightCommand.id,
		value: _univerjs_core.HorizontalAlign.RIGHT,
		label: "docs-ui.toolbar.alignRight",
		icon: "RightJustifyingIcon"
	},
	{
		id: AlignJustifyCommand.id,
		value: _univerjs_core.HorizontalAlign.JUSTIFIED,
		label: "docs-ui.toolbar.alignJustify",
		icon: "AlignTextBothIcon"
	}
];
function AlignMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	const value$ = new rxjs.Observable((subscriber) => {
		const calc = () => {
			var _paragraph$paragraphS7;
			if (shouldSuppressDocMenuStateRefresh(accessor)) {
				subscriber.next(_univerjs_core.HorizontalAlign.LEFT);
				return;
			}
			const paragraph = getParagraphStyleAtCursor(accessor);
			subscriber.next((paragraph === null || paragraph === void 0 || (_paragraph$paragraphS7 = paragraph.paragraphStyle) === null || _paragraph$paragraphS7 === void 0 ? void 0 : _paragraph$paragraphS7.horizontalAlign) ?? _univerjs_core.HorizontalAlign.LEFT);
		};
		const disposable = commandService.onCommandExecuted((c) => {
			if (c.id === _univerjs_docs.SetTextSelectionsOperation.id || c.id === AlignOperationCommand.id) calc();
		});
		calc();
		return disposable.dispose;
	}).pipe((0, rxjs.distinctUntilChanged)(), (0, rxjs.shareReplay)({
		bufferSize: 1,
		refCount: true
	}));
	return {
		id: AlignOperationCommand.id,
		type: _univerjs_ui.MenuItemType.SELECTOR,
		icon: value$.pipe((0, rxjs.map)((alignType) => {
			var _HORIZONTAL_ALIGN_OPT;
			return ((_HORIZONTAL_ALIGN_OPT = HORIZONTAL_ALIGN_OPTIONS.find((option) => option.value === alignType)) === null || _HORIZONTAL_ALIGN_OPT === void 0 ? void 0 : _HORIZONTAL_ALIGN_OPT.icon) ?? "LeftJustifyingIcon";
		})),
		tooltip: "docs-ui.toolbar.alignLeft",
		selections: HORIZONTAL_ALIGN_OPTIONS,
		value$,
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function HorizontalLineFactory(accessor) {
	return {
		id: HorizontalLineCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "ReduceIcon",
		tooltip: "docs-ui.toolbar.horizontalLine",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
const listValueFactory$ = (accessor) => {
	return new rxjs.Observable((subscriber) => {
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const subscription = univerInstanceService.focused$.subscribe((unitId) => {
			if (shouldSuppressDocMenuStateRefresh(accessor)) return;
			if (unitId == null) return;
			const docDataModel = univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
			if (docDataModel == null) return;
			const docRanges = docSelectionManagerService.getDocRanges();
			const range = docRanges.find((r) => r.isActive) ?? docRanges[0];
			if (range) {
				var _doc$getBody, _doc$getBody2;
				const doc = docDataModel.getSelfOrHeaderFooterModel(range === null || range === void 0 ? void 0 : range.segmentId);
				const paragraphs = _univerjs_core.BuildTextUtils.range.getParagraphsInRange(range, (doc === null || doc === void 0 || (_doc$getBody = doc.getBody()) === null || _doc$getBody === void 0 ? void 0 : _doc$getBody.paragraphs) ?? [], (doc === null || doc === void 0 || (_doc$getBody2 = doc.getBody()) === null || _doc$getBody2 === void 0 ? void 0 : _doc$getBody2.dataStream) ?? "");
				let listType;
				if (paragraphs.every((p) => {
					if (!listType) {
						var _p$bullet;
						listType = (_p$bullet = p.bullet) === null || _p$bullet === void 0 ? void 0 : _p$bullet.listType;
					}
					return p.bullet && p.bullet.listType === listType;
				})) {
					subscriber.next(listType);
					return;
				}
			}
			subscriber.next(void 0);
		});
		return () => {
			subscription.unsubscribe();
		};
	});
};
function OrderListMenuItemFactory(accessor) {
	return {
		id: OrderListCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON_SELECTOR,
		slot: true,
		selections: [{
			label: {
				name: ORDER_LIST_TYPE_COMPONENT,
				hoverable: false,
				selectable: false
			},
			value$: listValueFactory$(accessor)
		}],
		icon: "OrderIcon",
		tooltip: "docs-ui.toolbar.order",
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		disabled$: disableMenuWhenNoDocRange(accessor),
		activated$: listValueFactory$(accessor).pipe((0, rxjs.map)((v) => Boolean(v && v.indexOf("ORDER_LIST") === 0)))
	};
}
function BulletListMenuItemFactory(accessor) {
	return {
		id: BulletListCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON_SELECTOR,
		slot: true,
		selections: [{
			label: {
				name: BULLET_LIST_TYPE_COMPONENT,
				hoverable: false,
				selectable: false
			},
			value$: listValueFactory$(accessor)
		}],
		icon: "UnorderIcon",
		tooltip: "docs-ui.toolbar.unorder",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		activated$: listValueFactory$(accessor).pipe((0, rxjs.map)((v) => Boolean(v && v.indexOf("BULLET_LIST") === 0)))
	};
}
function CheckListMenuItemFactory(accessor) {
	return {
		id: CheckListCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "TodoListDoubleIcon",
		tooltip: "docs-ui.toolbar.checklist",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		activated$: listValueFactory$(accessor).pipe((0, rxjs.map)((v) => Boolean(v && v.indexOf("CHECK_LIST") === 0)))
	};
}
function ResetTextColorMenuItemFactory(accessor) {
	const themeService = accessor.get(_univerjs_core.ThemeService);
	return {
		id: ResetInlineFormatTextColorCommand.id,
		commandId: SetInlineFormatTextColorCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.toolbar.resetColor",
		icon: "NoColorDoubleIcon",
		params: () => ({ value: themeService.getColorFromTheme("gray.900") })
	};
}
function ResetBackgroundColorMenuItemFactory(accessor) {
	const themeService = accessor.get(_univerjs_core.ThemeService);
	return {
		id: ResetInlineFormatTextBackgroundColorCommand.id,
		commandId: SetInlineFormatTextBackgroundColorCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.toolbar.resetColor",
		icon: "NoColorDoubleIcon",
		params: () => ({ value: themeService.getColorFromTheme("primary.600") })
	};
}
function BackgroundColorSelectorMenuItemFactory(accessor) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	const themeService = accessor.get(_univerjs_core.ThemeService);
	return {
		id: SetInlineFormatTextBackgroundColorCommand.id,
		tooltip: "docs-ui.toolbar.fillColor.main",
		type: _univerjs_ui.MenuItemType.BUTTON_SELECTOR,
		icon: "PaintBucketDoubleIcon",
		selections: [{
			label: {
				name: _univerjs_ui.COLOR_PICKER_COMPONENT,
				hoverable: false,
				selectable: false
			},
			value$: new rxjs.Observable((subscriber) => {
				const defaultValue = themeService.getColorFromTheme("primary.600");
				const calc = () => {
					var _textRun$ts10;
					if (shouldSuppressDocMenuStateRefresh(accessor)) return;
					const textRun = getFontStyleAtCursor(accessor);
					if (!textRun) {
						subscriber.next(defaultValue);
						return;
					}
					const color = (_textRun$ts10 = textRun.ts) === null || _textRun$ts10 === void 0 || (_textRun$ts10 = _textRun$ts10.bg) === null || _textRun$ts10 === void 0 ? void 0 : _textRun$ts10.rgb;
					subscriber.next(color ?? defaultValue);
				};
				const disposable = commandService.onCommandExecuted((c) => {
					if (c.id === SetInlineFormatTextBackgroundColorCommand.id) calc();
				});
				calc();
				return disposable.dispose;
			})
		}],
		value$: new rxjs.Observable((subscriber) => {
			const defaultColor = themeService.getColorFromTheme("primary.600");
			const disposable = commandService.onCommandExecuted((c) => {
				if (c.id === SetInlineFormatTextBackgroundColorCommand.id) {
					if (shouldSuppressDocMenuStateRefresh(accessor)) return;
					const color = c.params.value;
					subscriber.next(color ?? defaultColor);
				}
			});
			subscriber.next(defaultColor);
			return disposable.dispose;
		}),
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function getFontStyleAtCursor(accessor) {
	var _paragraph$paragraphS8, _paragraph$paragraphS9, _docDataModel$getSelf;
	if (shouldSuppressDocMenuStateRefresh(accessor)) return;
	let univerInstanceService;
	let textSelectionService;
	let docMenuStyleService;
	try {
		univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		textSelectionService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		docMenuStyleService = accessor.get(DocMenuStyleService);
	} catch (error) {
		if (isInjectorDisposedError(error)) return;
		throw error;
	}
	const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
	const docRanges = textSelectionService.getDocRanges();
	const activeRange = docRanges.find((r) => r.isActive) ?? docRanges[0];
	const defaultTextStyle = docMenuStyleService.getDefaultStyle();
	const cacheStyle = docMenuStyleService.getStyleCache() ?? {};
	const paragraph = getParagraphStyleAtCursor(accessor);
	const namedStyle = (paragraph === null || paragraph === void 0 || (_paragraph$paragraphS8 = paragraph.paragraphStyle) === null || _paragraph$paragraphS8 === void 0 ? void 0 : _paragraph$paragraphS8.namedStyleType) ? _univerjs_core.NAMED_STYLE_MAP[paragraph === null || paragraph === void 0 || (_paragraph$paragraphS9 = paragraph.paragraphStyle) === null || _paragraph$paragraphS9 === void 0 ? void 0 : _paragraph$paragraphS9.namedStyleType] : null;
	if (docDataModel == null || activeRange == null) return { ts: {
		...defaultTextStyle,
		...namedStyle,
		...cacheStyle
	} };
	const { segmentId } = activeRange;
	const body = (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
	if (body == null) return { ts: {
		...defaultTextStyle,
		...namedStyle,
		...cacheStyle
	} };
	const curTextStyle = getStyleInTextRange(body, activeRange, {});
	_univerjs_core.Tools.deleteNull(curTextStyle);
	return { ts: {
		...defaultTextStyle,
		...namedStyle,
		...curTextStyle,
		...cacheStyle
	} };
}
function getParagraphStyleAtCursor(accessor) {
	var _docDataModel$getSelf2;
	if (shouldSuppressDocMenuStateRefresh(accessor)) return;
	let univerInstanceService;
	let textSelectionService;
	try {
		univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		textSelectionService = accessor.get(_univerjs_docs.DocSelectionManagerService);
	} catch (error) {
		if (isInjectorDisposedError(error)) return;
		throw error;
	}
	const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
	const docRanges = textSelectionService.getDocRanges();
	const activeRange = docRanges.find((r) => r.isActive) ?? docRanges[0];
	if (docDataModel == null || activeRange == null) return;
	const { startOffset, segmentId } = activeRange;
	const paragraphs = (_docDataModel$getSelf2 = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf2 === void 0 || (_docDataModel$getSelf2 = _docDataModel$getSelf2.getBody()) === null || _docDataModel$getSelf2 === void 0 ? void 0 : _docDataModel$getSelf2.paragraphs;
	if (paragraphs == null) return;
	let prevIndex = -1;
	for (const paragraph of paragraphs) {
		const { startIndex } = paragraph;
		if (startOffset > prevIndex && startOffset <= startIndex) return paragraph;
		prevIndex = startIndex;
	}
	return null;
}
function isInjectorDisposedError(error) {
	if (!(error instanceof Error)) return false;
	return error.name === "InjectorAlreadyDisposedError" || error.message.includes("Injector cannot be accessed after it was disposed");
}
function PageSettingMenuItemFactory(accessor) {
	return {
		id: DocOpenPageSettingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "DocSettingIcon",
		tooltip: "docs-ui.toolbar.pageSetup",
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		disabled$: disableMenuWithoutDocumentEditPermission(accessor)
	};
}

//#endregion
//#region src/menu/context-menu.ts
const getDisableOnCollapsedObservable = (accessor) => {
	const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
	return new rxjs.Observable((subscriber) => {
		const observable = docSelectionManagerService.textSelection$.subscribe(() => {
			if (docSelectionManagerService.getDocRanges().some((range) => range.collapsed === false || range.rangeType === _univerjs_core.DOC_RANGE_TYPE.RECT)) subscriber.next(false);
			else subscriber.next(true);
		});
		return () => observable.unsubscribe();
	});
};
function combineMenuDisabled(...states) {
	return (0, rxjs.combineLatest)(states).pipe((0, rxjs.map)((values) => values.some(Boolean)));
}
function inSameTable(rectRanges) {
	if (rectRanges.length < 2) return true;
	const tableIds = rectRanges.map((rectRange) => rectRange.tableId);
	return tableIds.every((tableId) => tableId === tableIds[0]);
}
function notInTableSubscriber(subscriber, docSelectionManagerService, univerInstanceService) {
	const rectRanges = docSelectionManagerService.getRectRanges();
	const activeRange = docSelectionManagerService.getActiveTextRange();
	if (rectRanges && rectRanges.length && inSameTable(rectRanges) && activeRange == null) {
		subscriber.next(false);
		return;
	}
	if (activeRange && (rectRanges == null || rectRanges.length === 0)) {
		var _docDataModel$getSelf;
		const { segmentId, startOffset, endOffset } = activeRange;
		if (!(activeRange.collapsed === true || startOffset === endOffset)) {
			subscriber.next(true);
			return;
		}
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const tables = docDataModel === null || docDataModel === void 0 || (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 || (_docDataModel$getSelf = _docDataModel$getSelf.getBody()) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.tables;
		if (tables && tables.length) {
			if (tables.some((table) => {
				const { startIndex, endIndex } = table;
				return startOffset > startIndex && startOffset < endIndex || endOffset > startIndex && endOffset < endIndex;
			})) {
				subscriber.next(false);
				return;
			}
		}
	}
	subscriber.next(true);
}
const getDisableWhenSelectionNotInTableObservable = (accessor) => {
	const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
	const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	return new rxjs.Observable((subscriber) => {
		const observable = docSelectionManagerService.textSelection$.subscribe(() => {
			notInTableSubscriber(subscriber, docSelectionManagerService, univerInstanceService);
		});
		notInTableSubscriber(subscriber, docSelectionManagerService, univerInstanceService);
		return () => observable.unsubscribe();
	});
};
function CopyMenuFactory(accessor) {
	return {
		id: DocCopyCommand.name,
		commandId: DocCopyCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "CopyDoubleIcon",
		title: "docs-ui.rightClick.copy",
		disabled$: combineMenuDisabled(getDisableOnCollapsedObservable(accessor), disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Copy)),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphSettingMenuFactory(accessor) {
	return {
		id: DocParagraphSettingPanelOperation.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.doc.menu.paragraphSetting",
		disabled$: disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function CutMenuFactory(accessor) {
	return {
		id: DocCutCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "CopyDoubleIcon",
		title: "docs-ui.rightClick.cut",
		disabled$: combineMenuDisabled(getDisableOnCollapsedObservable(accessor), disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Copy), disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit)),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function PasteMenuFactory(accessor) {
	return {
		id: DocPasteCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "PasteSpecialDoubleIcon",
		title: "docs-ui.rightClick.paste",
		disabled$: disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function DeleteMenuFactory(accessor) {
	return {
		id: DeleteLeftCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "PasteSpecialDoubleIcon",
		title: "docs-ui.rightClick.delete",
		disabled$: combineMenuDisabled(getDisableOnCollapsedObservable(accessor), disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit)),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
const TABLE_INSERT_MENU_ID = "doc.menu.table-insert";
function TableInsertMenuItemFactory(accessor) {
	return {
		id: TABLE_INSERT_MENU_ID,
		type: _univerjs_ui.MenuItemType.SUBITEMS,
		title: "docs-ui.table.insert",
		icon: "InsertDoubleIcon",
		disabled$: disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit),
		hidden$: (0, rxjs.combineLatest)((0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC), getDisableWhenSelectionNotInTableObservable(accessor), (one, two) => {
			return one || two;
		})
	};
}
function getSectionSettingUnavailableObservable(accessor) {
	const selectionManager = accessor.get(_univerjs_docs.DocSelectionManagerService);
	const instanceService = accessor.get(_univerjs_core.IUniverInstanceService);
	return new rxjs.Observable((subscriber) => {
		const emit = () => {
			var _body$tables;
			const documentDataModel = instanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
			const activeRange = selectionManager.getActiveTextRange();
			const body = documentDataModel === null || documentDataModel === void 0 ? void 0 : documentDataModel.getBody();
			const unavailable = !documentDataModel || documentDataModel.getDocumentStyle().documentFlavor !== _univerjs_core.DocumentFlavor.TRADITIONAL || !activeRange || Boolean(activeRange.segmentId) || Boolean(body === null || body === void 0 || (_body$tables = body.tables) === null || _body$tables === void 0 ? void 0 : _body$tables.some((table) => activeRange.startOffset > table.startIndex && activeRange.startOffset < table.endIndex));
			subscriber.next(unavailable);
		};
		emit();
		const subscription = selectionManager.textSelection$.subscribe(emit);
		return () => subscription.unsubscribe();
	});
}
function SectionSettingMenuFactory(accessor) {
	return {
		id: DocSectionSettingPanelOperation.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.doc.menu.sectionSetting",
		disabled$: disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit),
		hidden$: (0, rxjs.combineLatest)((0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC), getSectionSettingUnavailableObservable(accessor), (hidden, unavailable) => hidden || unavailable)
	};
}
function InsertRowBeforeMenuItemFactory(accessor) {
	return {
		id: DocTableInsertRowAboveCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.table.insertRowAbove",
		icon: "InsertRowAboveDoubleIcon",
		disabled$: combineMenuDisabled(getDisableWhenSelectionNotInTableObservable(accessor), disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit)),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function InsertRowAfterMenuItemFactory(accessor) {
	return {
		id: DocTableInsertRowBellowCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.table.insertRowBelow",
		icon: "InsertRowBelowDoubleIcon",
		disabled$: combineMenuDisabled(getDisableWhenSelectionNotInTableObservable(accessor), disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit)),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function InsertColumnLeftMenuItemFactory(accessor) {
	return {
		id: DocTableInsertColumnLeftCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.table.insertColumnLeft",
		icon: "LeftInsertColumnDoubleIcon",
		disabled$: combineMenuDisabled(getDisableWhenSelectionNotInTableObservable(accessor), disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit)),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function InsertColumnRightMenuItemFactory(accessor) {
	return {
		id: DocTableInsertColumnRightCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.table.insertColumnRight",
		icon: "RightInsertColumnDoubleIcon",
		disabled$: combineMenuDisabled(getDisableWhenSelectionNotInTableObservable(accessor), disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit)),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
const TABLE_DELETE_MENU_ID = "doc.menu.table-delete";
function TableDeleteMenuItemFactory(accessor) {
	return {
		id: TABLE_DELETE_MENU_ID,
		type: _univerjs_ui.MenuItemType.SUBITEMS,
		title: "docs-ui.table.delete",
		icon: "ReduceDoubleIcon",
		disabled$: disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit),
		hidden$: (0, rxjs.combineLatest)((0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC), getDisableWhenSelectionNotInTableObservable(accessor), (one, two) => {
			return one || two;
		})
	};
}
function DeleteRowsMenuItemFactory(accessor) {
	return {
		id: DocTableDeleteRowsCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.table.deleteRows",
		icon: "DeleteRowDoubleIcon",
		disabled$: combineMenuDisabled(getDisableWhenSelectionNotInTableObservable(accessor), disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit)),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function DeleteColumnsMenuItemFactory(accessor) {
	return {
		id: DocTableDeleteColumnsCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.table.deleteColumns",
		icon: "DeleteColumnDoubleIcon",
		disabled$: combineMenuDisabled(getDisableWhenSelectionNotInTableObservable(accessor), disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit)),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function DeleteTableMenuItemFactory(accessor) {
	return {
		id: DocTableDeleteTableCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		title: "docs-ui.table.deleteTable",
		icon: "DeleteTableDoubleIcon",
		disabled$: combineMenuDisabled(getDisableWhenSelectionNotInTableObservable(accessor), disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Edit)),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}

//#endregion
//#region src/views/paragraph-menu/theme-color.ts
function getHighlightBackgroundColor(themeService, index) {
	const tokenPath = `highlight.background.${index + 1}`;
	const token = themeService.getColorFromTheme(tokenPath);
	if (token == null || typeof token.color !== "string" || typeof token.alpha !== "number") throw new Error(`Theme token ${tokenPath} is required.`);
	return new _univerjs_core.ColorKit(themeService.getColorFromTheme(token.color) ?? token.color).setAlpha(token.alpha).toRgbString();
}

//#endregion
//#region src/menu/paragraph-menu.ts
const TEXT_COLORS = [
	"#FE4B4B",
	"#FF8C51",
	"#A4DC16",
	"#2DAEFF",
	"#3A60F7",
	"#9E6DE3",
	"#F248A6"
];
const TEXT_COLOR_SWATCH_ICONS = [
	"DocParagraphTextColorSwatchIcon.0",
	"DocParagraphTextColorSwatchIcon.1",
	"DocParagraphTextColorSwatchIcon.2",
	"DocParagraphTextColorSwatchIcon.3",
	"DocParagraphTextColorSwatchIcon.4",
	"DocParagraphTextColorSwatchIcon.5",
	"DocParagraphTextColorSwatchIcon.6"
];
const BACKGROUND_COLOR_SWATCH_ICONS = [
	"DocParagraphBackgroundColorSwatchIcon.0",
	"DocParagraphBackgroundColorSwatchIcon.1",
	"DocParagraphBackgroundColorSwatchIcon.2",
	"DocParagraphBackgroundColorSwatchIcon.3",
	"DocParagraphBackgroundColorSwatchIcon.4",
	"DocParagraphBackgroundColorSwatchIcon.5",
	"DocParagraphBackgroundColorSwatchIcon.6",
	"DocParagraphBackgroundColorSwatchIcon.7",
	"DocParagraphBackgroundColorSwatchIcon.8",
	"DocParagraphBackgroundColorSwatchIcon.9",
	"DocParagraphBackgroundColorSwatchIcon.10",
	"DocParagraphBackgroundColorSwatchIcon.11",
	"DocParagraphBackgroundColorSwatchIcon.12",
	"DocParagraphBackgroundColorSwatchIcon.13",
	"DocParagraphBackgroundColorSwatchIcon.14"
];
function getHeadingActivatedObservable(accessor, headingType) {
	const commandService = accessor.get(_univerjs_core.ICommandService);
	return new rxjs.Observable((subscriber) => {
		const DEFAULT_TYPE = _univerjs_core.NamedStyleType.NORMAL_TEXT;
		const calc = () => {
			var _paragraph$paragraphS;
			if (shouldSuppressDocMenuStateRefresh(accessor)) return;
			const paragraph = getParagraphStyleAtCursor(accessor);
			if (paragraph == null) {
				subscriber.next(DEFAULT_TYPE === headingType);
				return;
			}
			const namedStyleType = ((_paragraph$paragraphS = paragraph.paragraphStyle) === null || _paragraph$paragraphS === void 0 ? void 0 : _paragraph$paragraphS.namedStyleType) ?? DEFAULT_TYPE;
			subscriber.next(namedStyleType === headingType);
		};
		const disposable = commandService.onCommandExecuted((c) => {
			const id = c.id;
			if (id === _univerjs_docs.SetTextSelectionsOperation.id || id === SetInlineFormatFontSizeCommand.id) calc();
		});
		calc();
		return disposable.dispose;
	});
}
function H1HeadingMenuItemFactory(accessor) {
	return {
		id: H1HeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H1Icon",
		title: "docs-ui.toolbar.heading.leading1",
		tooltip: "docs-ui.toolbar.heading.leading1",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		activated$: getHeadingActivatedObservable(accessor, _univerjs_core.NamedStyleType.HEADING_1)
	};
}
function H2HeadingMenuItemFactory(accessor) {
	return {
		id: H2HeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H2Icon",
		title: "docs-ui.toolbar.heading.leading2",
		tooltip: "docs-ui.toolbar.heading.leading2",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		activated$: getHeadingActivatedObservable(accessor, _univerjs_core.NamedStyleType.HEADING_2)
	};
}
function H3HeadingMenuItemFactory(accessor) {
	return {
		id: H3HeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H3Icon",
		title: "docs-ui.toolbar.heading.leading3",
		tooltip: "docs-ui.toolbar.heading.leading3",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		activated$: getHeadingActivatedObservable(accessor, _univerjs_core.NamedStyleType.HEADING_3)
	};
}
function H4HeadingMenuItemFactory(accessor) {
	return {
		id: H4HeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H4Icon",
		title: "docs-ui.toolbar.heading.leading4",
		tooltip: "docs-ui.toolbar.heading.leading4",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		activated$: getHeadingActivatedObservable(accessor, _univerjs_core.NamedStyleType.HEADING_4)
	};
}
function H5HeadingMenuItemFactory(accessor) {
	return {
		id: H5HeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H5Icon",
		title: "docs-ui.toolbar.heading.leading5",
		tooltip: "docs-ui.toolbar.heading.leading5",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		activated$: getHeadingActivatedObservable(accessor, _univerjs_core.NamedStyleType.HEADING_5)
	};
}
function NormalTextHeadingMenuItemFactory(accessor) {
	return {
		id: NormalTextHeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "TextTypeIcon",
		title: "docs-ui.toolbar.heading.normal",
		tooltip: "docs-ui.toolbar.heading.normal",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		activated$: getHeadingActivatedObservable(accessor, _univerjs_core.NamedStyleType.NORMAL_TEXT)
	};
}
function TitleHeadingMenuItemFactory(accessor) {
	return {
		id: TitleHeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "TitleTypeIcon",
		title: "docs-ui.toolbar.heading.title",
		tooltip: "docs-ui.toolbar.heading.title",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		activated$: getHeadingActivatedObservable(accessor, _univerjs_core.NamedStyleType.TITLE)
	};
}
function SubtitleHeadingMenuItemFactory(accessor) {
	return {
		id: SubtitleHeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "SubtitleTypeIcon",
		title: "docs-ui.toolbar.heading.subTitle",
		tooltip: "docs-ui.toolbar.heading.subTitle",
		disabled$: disableMenuWhenNoDocRange(accessor),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC),
		activated$: getHeadingActivatedObservable(accessor, _univerjs_core.NamedStyleType.SUBTITLE)
	};
}
const EMPTY_PARAGRAPH_MENU_ID = "doc.menu.empty-paragraph";
function EmptyParagraphH1MenuItemFactory() {
	return {
		id: H1HeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H1Icon",
		title: "docs-ui.toolbar.heading.leading1",
		tooltip: "docs-ui.toolbar.heading.leading1"
	};
}
function EmptyParagraphH2MenuItemFactory() {
	return {
		id: H2HeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H2Icon",
		title: "docs-ui.toolbar.heading.leading2",
		tooltip: "docs-ui.toolbar.heading.leading2"
	};
}
function EmptyParagraphH3MenuItemFactory() {
	return {
		id: H3HeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H3Icon",
		title: "docs-ui.toolbar.heading.leading3",
		tooltip: "docs-ui.toolbar.heading.leading3"
	};
}
function EmptyParagraphH4MenuItemFactory() {
	return {
		id: H4HeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H4Icon",
		title: "docs-ui.toolbar.heading.leading4",
		tooltip: "docs-ui.toolbar.heading.leading4"
	};
}
function EmptyParagraphH5MenuItemFactory() {
	return {
		id: H5HeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H5Icon",
		title: "docs-ui.toolbar.heading.leading5",
		tooltip: "docs-ui.toolbar.heading.leading5"
	};
}
function EmptyParagraphNormalTextMenuItemFactory() {
	return {
		id: NormalTextHeadingCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "TextTypeIcon",
		title: "docs-ui.toolbar.heading.normal",
		tooltip: "docs-ui.toolbar.heading.normal"
	};
}
function EmptyParagraphOrderListMenuItemFactory() {
	return {
		id: OrderListCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "OrderIcon",
		title: "docs-ui.rightClick.orderList",
		tooltip: "docs-ui.rightClick.orderList"
	};
}
function EmptyParagraphBulletListMenuItemFactory() {
	return {
		id: BulletListCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "UnorderIcon",
		title: "docs-ui.rightClick.bulletList",
		tooltip: "docs-ui.rightClick.bulletList"
	};
}
function EmptyParagraphCheckListMenuItemFactory() {
	return {
		id: CheckListCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "TodoListDoubleIcon",
		title: "docs-ui.rightClick.checkList",
		tooltip: "docs-ui.rightClick.checkList"
	};
}
function EmptyParagraphHorizontalLineMenuItemFactory() {
	return {
		id: HorizontalLineCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "ReduceIcon",
		title: "docs-ui.toolbar.horizontalLine",
		tooltip: "docs-ui.toolbar.horizontalLine"
	};
}
function CopyCurrentParagraphMenuItemFactory(accessor) {
	return {
		id: DocCopyCurrentParagraphCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "CopyDoubleIcon",
		title: "docs-ui.rightClick.copy",
		disabled$: disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Copy)
	};
}
function CutCurrentParagraphMenuItemFactory(accessor) {
	return {
		id: DocCutCurrentParagraphCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "CutIcon",
		title: "docs-ui.rightClick.cut",
		disabled$: disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Copy)
	};
}
function DeleteCurrentParagraphMenuItemFactory() {
	return {
		id: DeleteCurrentParagraphCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "DeleteIcon",
		title: "docs-ui.rightClick.delete"
	};
}
function InsertBulletListBellowMenuItemFactory() {
	return {
		id: InsertBulletListBellowCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "UnorderIcon",
		title: "docs-ui.rightClick.bulletList",
		tooltip: "docs-ui.rightClick.bulletList"
	};
}
function InsertOrderListBellowMenuItemFactory() {
	return {
		id: InsertOrderListBellowCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "OrderIcon",
		title: "docs-ui.rightClick.orderList",
		tooltip: "docs-ui.rightClick.orderList"
	};
}
function InsertCheckListBellowMenuItemFactory() {
	return {
		id: InsertCheckListBellowCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "TodoListDoubleIcon",
		title: "docs-ui.rightClick.checkList",
		tooltip: "docs-ui.rightClick.checkList"
	};
}
function InsertHorizontalLineBellowMenuItemFactory() {
	return {
		id: InsertHorizontalLineBellowCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "ReduceIcon",
		title: "docs-ui.toolbar.horizontalLine",
		tooltip: "docs-ui.toolbar.horizontalLine"
	};
}
const INSERT_BELLOW_MENU_ID = "doc.menu.insert-bellow";
const DOC_CONTENT_INSERT_MENU_ID = "doc.menu.content-insert";
const DOC_TABLE_BLOCK_MENU_ID = "doc.menu.table-block";
const DOC_PARAGRAPH_T_INSERT_MENU_ID = "doc.menu.paragraph-t.insert";
const DOC_PARAGRAPH_T_EDIT_MENU_ID = "doc.menu.paragraph-t.edit";
const DOC_PARAGRAPH_T_DIVIDER_MENU_ID = "doc.menu.paragraph-t.divider";
const DOC_PARAGRAPH_T_ALIGN_MENU_ID = "doc.menu.paragraph-t.align";
const DOC_PARAGRAPH_T_COLORS_MENU_ID = "doc.menu.paragraph-t.colors";
const DOC_PARAGRAPH_T_INSERT_BELOW_MENU_ID = "doc.menu.paragraph-t.insert-below";
const DOC_PARAGRAPH_T_RESET_COLORS_ID = "doc.menu.paragraph-t.reset-colors";
const DOC_PARAGRAPH_T_INDENT_INCREASE_ID = "doc.menu.paragraph-t.indent.increase";
const DOC_PARAGRAPH_T_INDENT_DECREASE_ID = "doc.menu.paragraph-t.indent.decrease";
const DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID = "doc.menu.paragraph-t.insert-below.command";
function getDocBlockRangeMenuId(blockType) {
	return `doc.block-range.${blockType}.menu`;
}
function TableBlockCopyMenuItemFactory(accessor) {
	return {
		id: DocCopyCommand.name,
		commandId: DocCopyCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "CopyDoubleIcon",
		title: "docs-ui.rightClick.copy",
		disabled$: disableMenuWithoutDocumentUnitPermission(accessor, _univerjs_protocol.UnitAction.Copy)
	};
}
function TableBlockPasteMenuItemFactory() {
	return {
		id: DocPasteCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "PasteSpecialDoubleIcon",
		title: "docs-ui.rightClick.paste"
	};
}
function TableBlockDeleteMenuItemFactory() {
	return {
		id: DocTableDeleteTableCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "DeleteIcon",
		title: "docs-ui.rightClick.delete"
	};
}
function DocInsertBellowMenuItemFactory() {
	return {
		id: INSERT_BELLOW_MENU_ID,
		type: _univerjs_ui.MenuItemType.SUBITEMS,
		title: "docs-ui.rightClick.insertBellow"
	};
}
function ParagraphMenuAlignSubmenuItemFactory(accessor) {
	return {
		id: DOC_PARAGRAPH_T_ALIGN_MENU_ID,
		type: _univerjs_ui.MenuItemType.SUBITEMS,
		icon: "LeftJustifyingIcon",
		title: "docs-ui.paragraphMenu.alignAndIndent",
		tooltip: "docs-ui.paragraphMenu.alignAndIndent",
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuColorsSubmenuItemFactory(accessor) {
	return {
		id: DOC_PARAGRAPH_T_COLORS_MENU_ID,
		type: _univerjs_ui.MenuItemType.SUBITEMS,
		icon: "PaintBucketDoubleIcon",
		title: "docs-ui.paragraphMenu.color",
		tooltip: "docs-ui.paragraphMenu.color",
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuTextColorHeaderActionMenuItemFactory(accessor) {
	return {
		...TextColorSelectorMenuItemFactory(accessor),
		icon: "HeaderTextColorIcon",
		tooltip: void 0,
		title: void 0
	};
}
function ParagraphMenuBackgroundColorHeaderActionMenuItemFactory(accessor) {
	return {
		...BackgroundColorSelectorMenuItemFactory(accessor),
		icon: "PaintBucketDoubleIcon",
		tooltip: void 0,
		title: void 0
	};
}
function ParagraphMenuInsertBelowSubmenuItemFactory(accessor) {
	return {
		id: DOC_PARAGRAPH_T_INSERT_BELOW_MENU_ID,
		type: _univerjs_ui.MenuItemType.SUBITEMS,
		icon: "TextTypeIcon",
		title: "docs-ui.rightClick.insertBellow",
		tooltip: "docs-ui.rightClick.insertBellow",
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuInsertBelowHeadingH1MenuItemFactory(accessor) {
	return {
		id: `${DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID}.h1`,
		commandId: DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H1Icon",
		title: "docs-ui.toolbar.heading.leading1",
		tooltip: "docs-ui.toolbar.heading.leading1",
		params: {
			commandId: H1HeadingCommand.id,
			paragraphMenuInsertMode: "breakline"
		},
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuInsertBelowHeadingH2MenuItemFactory(accessor) {
	return {
		id: `${DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID}.h2`,
		commandId: DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H2Icon",
		title: "docs-ui.toolbar.heading.leading2",
		tooltip: "docs-ui.toolbar.heading.leading2",
		params: {
			commandId: H2HeadingCommand.id,
			paragraphMenuInsertMode: "breakline"
		},
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuInsertBelowHeadingH3MenuItemFactory(accessor) {
	return {
		id: `${DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID}.h3`,
		commandId: DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H3Icon",
		title: "docs-ui.toolbar.heading.leading3",
		tooltip: "docs-ui.toolbar.heading.leading3",
		params: {
			commandId: H3HeadingCommand.id,
			paragraphMenuInsertMode: "breakline"
		},
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuInsertBelowHeadingH4MenuItemFactory(accessor) {
	return {
		id: `${DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID}.h4`,
		commandId: DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H4Icon",
		title: "docs-ui.toolbar.heading.leading4",
		tooltip: "docs-ui.toolbar.heading.leading4",
		params: {
			commandId: H4HeadingCommand.id,
			paragraphMenuInsertMode: "breakline"
		},
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuInsertBelowHeadingH5MenuItemFactory(accessor) {
	return {
		id: `${DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID}.h5`,
		commandId: DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "H5Icon",
		title: "docs-ui.toolbar.heading.leading5",
		tooltip: "docs-ui.toolbar.heading.leading5",
		params: {
			commandId: H5HeadingCommand.id,
			paragraphMenuInsertMode: "breakline"
		},
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuInsertBelowTableMenuItemFactory(accessor) {
	return {
		id: `${DocCreateTableOperation.id}.below`,
		commandId: DocCreateTableOperation.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "GridIcon",
		title: "docs-ui.toolbar.table.insert",
		tooltip: "docs-ui.toolbar.table.insert",
		params: {
			rowCount: 3,
			colCount: 5
		},
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuIndentIncreaseMenuItemFactory(accessor) {
	return {
		id: DOC_PARAGRAPH_T_INDENT_INCREASE_ID,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "LineIndentIncreaseIcon",
		title: "docs-ui.paragraphMenu.increase",
		tooltip: "docs-ui.paragraphMenu.increaseIndent",
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuIndentDecreaseMenuItemFactory(accessor) {
	return {
		id: DOC_PARAGRAPH_T_INDENT_DECREASE_ID,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "LineIndentDecreaseIcon",
		title: "docs-ui.paragraphMenu.decrease",
		tooltip: "docs-ui.paragraphMenu.decreaseIndent",
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuNoBackgroundMenuItemFactory(accessor) {
	return {
		id: ResetInlineFormatTextBackgroundColorCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "NoColorDoubleIcon",
		title: "docs-ui.paragraphMenu.noBackground",
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
function ParagraphMenuResetTextColorMenuItemFactory(accessor) {
	const themeService = accessor.get(_univerjs_core.ThemeService);
	return {
		id: ResetInlineFormatTextColorCommand.id,
		commandId: SetInlineFormatTextColorCommand.id,
		type: _univerjs_ui.MenuItemType.BUTTON,
		icon: "NoColorDoubleIcon",
		title: "docs-ui.toolbar.resetColor",
		tooltip: "docs-ui.toolbar.resetColor",
		params: () => ({ value: themeService.getColorFromTheme("gray.900") }),
		hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
	};
}
const ParagraphMenuTextColorSwatchMenuItemFactories = TEXT_COLORS.reduce((items, color, index) => {
	const id = `doc.menu.paragraph-t.text-color.${index}`;
	items[id] = {
		order: index,
		menuItemFactory: (accessor) => ({
			id,
			commandId: SetInlineFormatTextColorCommand.id,
			type: _univerjs_ui.MenuItemType.BUTTON,
			icon: TEXT_COLOR_SWATCH_ICONS[index],
			params: { value: color },
			hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
		})
	};
	return items;
}, {});
const ParagraphMenuBackgroundColorSwatchMenuItemFactories = BACKGROUND_COLOR_SWATCH_ICONS.reduce((items, icon, index) => {
	const id = `doc.menu.paragraph-t.background-color.${index}`;
	items[id] = {
		order: index,
		menuItemFactory: (accessor) => {
			const color = getHighlightBackgroundColor(accessor.get(_univerjs_core.ThemeService), index);
			return {
				id,
				commandId: SetInlineFormatTextBackgroundColorCommand.id,
				type: _univerjs_ui.MenuItemType.BUTTON,
				icon,
				params: { value: color },
				hidden$: (0, _univerjs_ui.getMenuHiddenObservable)(accessor, _univerjs_core.UniverInstanceType.UNIVER_DOC)
			};
		}
	};
	return items;
}, {});

//#endregion
//#region src/menu/permission.menu.ts
function DocPermissionMenuItemFactory(accessor) {
	return (0, _univerjs_ui.objectPermissionMenuItemFactory)(accessor, OpenDocPermissionPanelOperation.id, _univerjs_core.UniverInstanceType.UNIVER_DOC, _univerjs_protocol.UnitObject.DocumentSection);
}
function DocParagraphPermissionMenuItemFactory(accessor) {
	return {
		...(0, _univerjs_ui.objectPermissionMenuItemFactory)(accessor, OpenDocParagraphPermissionOperation.id, _univerjs_core.UniverInstanceType.UNIVER_DOC, _univerjs_protocol.UnitObject.DocumentParagraph),
		title: "docs-ui.objectPermission.paragraphPermission"
	};
}

//#endregion
//#region src/menu/schema.ts
const floatToolbarMenuSchema = { [FLOAT_TOOLBAR_MENU_POSITION]: { [FLOAT_TEXT_STYLE_MENU_ID]: {
	order: 0,
	menuItemFactory: FloatTextStyleMenuItemFactory
} } };
const menuSchema = {
	...floatToolbarMenuSchema,
	[_univerjs_ui.RibbonStartGroup.OTHERS]: { [OpenDocPermissionPanelOperation.id]: {
		order: 0,
		gridLayout: {
			row: 1,
			column: 1,
			rowSpan: 2,
			showLabel: true
		},
		menuItemFactory: DocPermissionMenuItemFactory
	} },
	[_univerjs_ui.RibbonStartGroup.FORMAT]: {
		[SetInlineFormatBoldCommand.id]: {
			order: 0,
			gridLayout: {
				row: 2,
				column: 1
			},
			menuItemFactory: BoldMenuItemFactory
		},
		[SetInlineFormatItalicCommand.id]: {
			order: 1,
			gridLayout: {
				row: 2,
				column: 2
			},
			menuItemFactory: ItalicMenuItemFactory
		},
		[SetInlineFormatUnderlineCommand.id]: {
			order: 2,
			gridLayout: {
				row: 2,
				column: 3
			},
			menuItemFactory: UnderlineMenuItemFactory
		},
		[SetInlineFormatStrikethroughCommand.id]: {
			order: 3,
			gridLayout: {
				row: 2,
				column: 4
			},
			menuItemFactory: StrikeThroughMenuItemFactory
		},
		[SetInlineFormatSubscriptCommand.id]: {
			order: 4,
			gridLayout: {
				row: 2,
				column: 5
			},
			menuItemFactory: SubscriptMenuItemFactory
		},
		[SetInlineFormatSuperscriptCommand.id]: {
			order: 5,
			gridLayout: {
				row: 2,
				column: 6
			},
			menuItemFactory: SuperscriptMenuItemFactory
		},
		[SetParagraphNamedStyleCommand.id]: {
			order: 5.5,
			gridLayout: {
				row: 1,
				column: 1,
				columnSpan: 2,
				width: 100
			},
			menuItemFactory: HeadingSelectorMenuItemFactory
		},
		[SetInlineFormatFontSizeCommand.id]: {
			order: 6,
			gridLayout: {
				row: 1,
				column: 7,
				width: 64
			},
			menuItemFactory: FontSizeSelectorMenuItemFactory
		},
		[SetInlineFormatFontFamilyCommand.id]: {
			order: 7,
			gridLayout: {
				row: 1,
				column: 3,
				columnSpan: 4,
				width: 160
			},
			menuItemFactory: FontFamilySelectorMenuItemFactory
		},
		[SetInlineFormatTextColorCommand.id]: {
			order: 8,
			gridLayout: {
				row: 2,
				column: 7
			},
			menuItemFactory: TextColorSelectorMenuItemFactory,
			[ResetInlineFormatTextColorCommand.id]: {
				order: 0,
				menuItemFactory: ResetTextColorMenuItemFactory
			}
		},
		[SetInlineFormatTextBackgroundColorCommand.id]: {
			order: 9,
			gridLayout: {
				row: 2,
				column: 8
			},
			menuItemFactory: BackgroundColorSelectorMenuItemFactory,
			[ResetInlineFormatTextBackgroundColorCommand.id]: {
				order: 0,
				menuItemFactory: ResetBackgroundColorMenuItemFactory
			}
		}
	},
	[_univerjs_ui.RibbonStartGroup.LAYOUT]: {
		[AlignOperationCommand.id]: {
			order: 2,
			gridLayout: {
				row: 1,
				column: 1
			},
			menuItemFactory: AlignMenuItemFactory
		},
		[OrderListCommand.id]: {
			order: 7,
			gridLayout: {
				row: 2,
				column: 1
			},
			menuItemFactory: OrderListMenuItemFactory
		},
		[BulletListCommand.id]: {
			order: 8,
			gridLayout: {
				row: 1,
				column: 2
			},
			menuItemFactory: BulletListMenuItemFactory
		},
		[CheckListCommand.id]: {
			order: 9,
			gridLayout: {
				row: 2,
				column: 2
			},
			menuItemFactory: CheckListMenuItemFactory
		},
		[OpenHeaderFooterPanelCommand.id]: {
			order: 10,
			gridLayout: {
				row: 1,
				column: 3,
				rowSpan: 2,
				showLabel: true
			},
			menuItemFactory: HeaderFooterMenuItemFactory
		},
		[DocOpenPageSettingCommand.id]: {
			order: 12,
			gridLayout: {
				row: 1,
				column: 4,
				rowSpan: 2,
				showLabel: true
			},
			menuItemFactory: PageSettingMenuItemFactory
		}
	},
	[_univerjs_ui.RibbonInsertGroup.MEDIA]: {
		[TABLE_MENU_ID]: {
			order: 2,
			gridLayout: {
				row: 1,
				column: 1,
				rowSpan: 2,
				showLabel: true
			},
			menuItemFactory: TableMenuFactory,
			[DocCreateTableOperation.id]: {
				order: 0,
				menuItemFactory: InsertTableMenuFactory
			}
		},
		[HorizontalLineCommand.id]: {
			order: 3,
			gridLayout: {
				row: 1,
				column: 2,
				rowSpan: 2,
				showLabel: true
			},
			menuItemFactory: HorizontalLineFactory
		},
		[DOC_BREAKS_MENU_ID]: {
			order: 4,
			gridLayout: {
				row: 1,
				column: 3,
				rowSpan: 2,
				showLabel: true
			},
			menuItemFactory: BreaksMenuFactory,
			[InsertDocumentColumnBreakOperation.id]: {
				order: 0,
				menuItemFactory: InsertColumnBreakMenuFactory
			},
			[DOC_SECTION_BREAK_NEXT_PAGE_MENU_ID]: {
				order: 1,
				menuItemFactory: InsertNextPageSectionBreakMenuFactory
			},
			[DOC_SECTION_BREAK_CONTINUOUS_MENU_ID]: {
				order: 2,
				menuItemFactory: InsertContinuousSectionBreakMenuFactory
			},
			[DOC_SECTION_BREAK_NEXT_COLUMN_MENU_ID]: {
				order: 3,
				menuItemFactory: InsertNextColumnSectionBreakMenuFactory
			},
			[DOC_SECTION_BREAK_EVEN_PAGE_MENU_ID]: {
				order: 4,
				menuItemFactory: InsertEvenPageSectionBreakMenuFactory
			},
			[DOC_SECTION_BREAK_ODD_PAGE_MENU_ID]: {
				order: 5,
				menuItemFactory: InsertOddPageSectionBreakMenuFactory
			}
		},
		[DOC_INSERT_EMOJI_MENU_ID]: {
			order: 5,
			gridLayout: {
				row: 1,
				column: 4
			},
			menuItemFactory: EmojiPickerMenuItemFactory
		},
		[DOC_INSERT_SYMBOL_MENU_ID]: {
			order: 6,
			gridLayout: {
				row: 2,
				column: 4
			},
			menuItemFactory: SymbolPickerMenuItemFactory
		}
	},
	[_univerjs_ui.ContextMenuPosition.MAIN_AREA]: {
		[_univerjs_ui.ContextMenuGroup.OTHERS]: { [OpenDocParagraphPermissionOperation.id]: {
			order: 10,
			menuItemFactory: DocParagraphPermissionMenuItemFactory
		} },
		[_univerjs_ui.ContextMenuGroup.QUICK]: {
			quickLayout: "tile",
			[DocCopyCommand.name]: {
				order: 0,
				menuItemFactory: CopyMenuFactory
			},
			[DocCutCommand.id]: {
				order: 1,
				menuItemFactory: CutMenuFactory
			},
			[DocPasteCommand.id]: {
				order: 2,
				menuItemFactory: PasteMenuFactory
			}
		},
		[_univerjs_ui.ContextMenuGroup.FORMAT]: { [DeleteLeftCommand.id]: {
			order: 0,
			menuItemFactory: DeleteMenuFactory
		} },
		[_univerjs_ui.ContextMenuGroup.LAYOUT]: {
			[DocParagraphSettingPanelOperation.id]: {
				order: 0,
				menuItemFactory: ParagraphSettingMenuFactory
			},
			[DocSectionSettingPanelOperation.id]: {
				order: 1,
				menuItemFactory: SectionSettingMenuFactory
			},
			[TABLE_INSERT_MENU_ID]: {
				order: 1,
				menuItemFactory: TableInsertMenuItemFactory,
				[DocTableInsertRowAboveCommand.id]: {
					order: 1,
					menuItemFactory: InsertRowBeforeMenuItemFactory
				},
				[DocTableInsertRowBellowCommand.id]: {
					order: 2,
					menuItemFactory: InsertRowAfterMenuItemFactory
				},
				[DocTableInsertColumnLeftCommand.id]: {
					order: 3,
					menuItemFactory: InsertColumnLeftMenuItemFactory
				},
				[DocTableInsertColumnRightCommand.id]: {
					order: 4,
					menuItemFactory: InsertColumnRightMenuItemFactory
				}
			},
			[TABLE_DELETE_MENU_ID]: {
				order: 2,
				menuItemFactory: TableDeleteMenuItemFactory,
				[DocTableDeleteRowsCommand.id]: {
					order: 1,
					menuItemFactory: DeleteRowsMenuItemFactory
				},
				[DocTableDeleteColumnsCommand.id]: {
					order: 2,
					menuItemFactory: DeleteColumnsMenuItemFactory
				},
				[DocTableDeleteTableCommand.id]: {
					order: 3,
					menuItemFactory: DeleteTableMenuItemFactory
				}
			}
		}
	},
	[_univerjs_ui.ContextMenuPosition.PARAGRAPH]: {
		[_univerjs_ui.ContextMenuGroup.QUICK]: {
			[H1HeadingCommand.id]: {
				order: 0,
				menuItemFactory: H1HeadingMenuItemFactory
			},
			[H2HeadingCommand.id]: {
				order: 1,
				menuItemFactory: H2HeadingMenuItemFactory
			},
			[H3HeadingCommand.id]: {
				order: 2,
				menuItemFactory: H3HeadingMenuItemFactory
			},
			[H4HeadingCommand.id]: {
				order: 3,
				menuItemFactory: H4HeadingMenuItemFactory
			},
			[H5HeadingCommand.id]: {
				order: 4,
				menuItemFactory: H5HeadingMenuItemFactory
			},
			[TitleHeadingCommand.id]: {
				order: -1,
				menuItemFactory: TitleHeadingMenuItemFactory
			},
			[SubtitleHeadingCommand.id]: {
				order: -1,
				menuItemFactory: SubtitleHeadingMenuItemFactory
			},
			[NormalTextHeadingCommand.id]: {
				order: 5,
				menuItemFactory: NormalTextHeadingMenuItemFactory
			}
		},
		[_univerjs_ui.ContextMenuGroup.FORMAT]: {
			[DocCopyCurrentParagraphCommand.id]: {
				order: 0,
				menuItemFactory: CopyCurrentParagraphMenuItemFactory
			},
			[DocCutCurrentParagraphCommand.id]: {
				order: 1,
				menuItemFactory: CutCurrentParagraphMenuItemFactory
			},
			[DeleteCurrentParagraphCommand.id]: {
				order: 2,
				menuItemFactory: DeleteCurrentParagraphMenuItemFactory
			}
		},
		[_univerjs_ui.ContextMenuGroup.LAYOUT]: {
			[DocParagraphSettingPanelOperation.id]: {
				order: 0,
				menuItemFactory: ParagraphSettingMenuFactory
			},
			[DocSectionSettingPanelOperation.id]: {
				order: 1,
				menuItemFactory: SectionSettingMenuFactory
			},
			[INSERT_BELLOW_MENU_ID]: {
				order: 1,
				menuItemFactory: DocInsertBellowMenuItemFactory,
				[InsertBulletListBellowCommand.id]: {
					order: 0,
					menuItemFactory: InsertBulletListBellowMenuItemFactory
				},
				[InsertOrderListBellowCommand.id]: {
					order: 1,
					menuItemFactory: InsertOrderListBellowMenuItemFactory
				},
				[InsertCheckListBellowCommand.id]: {
					order: 2,
					menuItemFactory: InsertCheckListBellowMenuItemFactory
				},
				[InsertHorizontalLineBellowCommand.id]: {
					order: 3,
					menuItemFactory: InsertHorizontalLineBellowMenuItemFactory
				},
				[DocCreateTableOperation.id]: {
					order: 4,
					menuItemFactory: InsertDefaultTableMenuFactory
				}
			}
		},
		[DOC_CONTENT_INSERT_MENU_ID]: {
			[_univerjs_ui.ContextMenuGroup.QUICK]: {
				order: -2,
				quickLayout: "icon",
				[H1HeadingCommand.id]: {
					order: 0,
					menuItemFactory: H1HeadingMenuItemFactory
				},
				[H2HeadingCommand.id]: {
					order: 1,
					menuItemFactory: H2HeadingMenuItemFactory
				},
				[H3HeadingCommand.id]: {
					order: 2,
					menuItemFactory: H3HeadingMenuItemFactory
				},
				[H4HeadingCommand.id]: {
					order: 3,
					menuItemFactory: H4HeadingMenuItemFactory
				},
				[H5HeadingCommand.id]: {
					order: 4,
					menuItemFactory: H5HeadingMenuItemFactory
				},
				[NormalTextHeadingCommand.id]: {
					order: 5,
					menuItemFactory: NormalTextHeadingMenuItemFactory
				}
			},
			[_univerjs_ui.ContextMenuGroup.LAYOUT]: {
				order: 1,
				[InsertBulletListBellowCommand.id]: {
					order: 0,
					menuItemFactory: InsertBulletListBellowMenuItemFactory
				},
				[InsertOrderListBellowCommand.id]: {
					order: 1,
					menuItemFactory: InsertOrderListBellowMenuItemFactory
				},
				[InsertCheckListBellowCommand.id]: {
					order: 2,
					menuItemFactory: InsertCheckListBellowMenuItemFactory
				},
				[InsertHorizontalLineBellowCommand.id]: {
					order: 3,
					menuItemFactory: InsertHorizontalLineBellowMenuItemFactory
				},
				[DocCreateTableOperation.id]: {
					order: 4,
					menuItemFactory: InsertDefaultTableMenuFactory
				}
			}
		},
		[EMPTY_PARAGRAPH_MENU_ID]: {
			[_univerjs_ui.ContextMenuGroup.QUICK]: {
				order: -1,
				quickLayout: "icon",
				[H1HeadingCommand.id]: {
					order: 0,
					menuItemFactory: EmptyParagraphH1MenuItemFactory
				},
				[H2HeadingCommand.id]: {
					order: 1,
					menuItemFactory: EmptyParagraphH2MenuItemFactory
				},
				[H3HeadingCommand.id]: {
					order: 2,
					menuItemFactory: EmptyParagraphH3MenuItemFactory
				},
				[H4HeadingCommand.id]: {
					order: 3,
					menuItemFactory: EmptyParagraphH4MenuItemFactory
				},
				[H5HeadingCommand.id]: {
					order: 4,
					menuItemFactory: EmptyParagraphH5MenuItemFactory
				},
				[NormalTextHeadingCommand.id]: {
					order: 5,
					menuItemFactory: EmptyParagraphNormalTextMenuItemFactory
				}
			},
			[_univerjs_ui.ContextMenuGroup.LAYOUT]: {
				order: 1,
				[DocParagraphSettingPanelOperation.id]: {
					order: 0,
					menuItemFactory: ParagraphSettingMenuFactory
				},
				[DocSectionSettingPanelOperation.id]: {
					order: 1,
					menuItemFactory: SectionSettingMenuFactory
				},
				[BulletListCommand.id]: {
					order: 1,
					menuItemFactory: EmptyParagraphBulletListMenuItemFactory
				},
				[OrderListCommand.id]: {
					order: 2,
					menuItemFactory: EmptyParagraphOrderListMenuItemFactory
				},
				[CheckListCommand.id]: {
					order: 3,
					menuItemFactory: EmptyParagraphCheckListMenuItemFactory
				},
				[HorizontalLineCommand.id]: {
					order: 4,
					menuItemFactory: EmptyParagraphHorizontalLineMenuItemFactory
				},
				[DocCreateTableOperation.id]: {
					order: 5,
					menuItemFactory: InsertDefaultTableMenuFactory
				}
			}
		},
		[DOC_TABLE_BLOCK_MENU_ID]: {
			[_univerjs_ui.ContextMenuGroup.FORMAT]: {
				[DocCopyCommand.name]: {
					order: 0,
					menuItemFactory: TableBlockCopyMenuItemFactory
				},
				[DocPasteCommand.id]: {
					order: 1,
					menuItemFactory: TableBlockPasteMenuItemFactory
				},
				[DocTableDeleteTableCommand.id]: {
					order: 2,
					menuItemFactory: TableBlockDeleteMenuItemFactory
				}
			},
			[_univerjs_ui.ContextMenuGroup.LAYOUT]: { [INSERT_BELLOW_MENU_ID]: {
				order: 0,
				menuItemFactory: DocInsertBellowMenuItemFactory,
				[InsertBulletListBellowCommand.id]: {
					order: 0,
					menuItemFactory: InsertBulletListBellowMenuItemFactory
				},
				[InsertOrderListBellowCommand.id]: {
					order: 1,
					menuItemFactory: InsertOrderListBellowMenuItemFactory
				},
				[InsertCheckListBellowCommand.id]: {
					order: 2,
					menuItemFactory: InsertCheckListBellowMenuItemFactory
				},
				[InsertHorizontalLineBellowCommand.id]: {
					order: 3,
					menuItemFactory: InsertHorizontalLineBellowMenuItemFactory
				},
				[DocCreateTableOperation.id]: {
					order: 4,
					menuItemFactory: InsertDefaultTableMenuFactory
				}
			} }
		},
		[DOC_PARAGRAPH_T_INSERT_MENU_ID]: {
			quickTop: {
				order: 0,
				quickLayout: "icon",
				[H1HeadingCommand.id]: {
					order: 0,
					menuItemFactory: EmptyParagraphH1MenuItemFactory
				},
				[H2HeadingCommand.id]: {
					order: 1,
					menuItemFactory: EmptyParagraphH2MenuItemFactory
				},
				[H3HeadingCommand.id]: {
					order: 2,
					menuItemFactory: EmptyParagraphH3MenuItemFactory
				},
				[H4HeadingCommand.id]: {
					order: 3,
					menuItemFactory: EmptyParagraphH4MenuItemFactory
				},
				[H5HeadingCommand.id]: {
					order: 4,
					menuItemFactory: EmptyParagraphH5MenuItemFactory
				},
				[OrderListCommand.id]: {
					order: 5,
					menuItemFactory: EmptyParagraphOrderListMenuItemFactory
				}
			},
			quickBottom: {
				order: 1,
				quickLayout: "icon",
				[BulletListCommand.id]: {
					order: 0,
					menuItemFactory: EmptyParagraphBulletListMenuItemFactory
				},
				[CheckListCommand.id]: {
					order: 1,
					menuItemFactory: EmptyParagraphCheckListMenuItemFactory
				},
				[HorizontalLineCommand.id]: {
					order: 5,
					menuItemFactory: EmptyParagraphHorizontalLineMenuItemFactory
				}
			},
			insert: {
				order: 2,
				[DocCreateTableOperation.id]: {
					order: 0,
					menuItemFactory: InsertDefaultTableMenuFactory
				}
			},
			format: {
				order: 3,
				[DocPasteCommand.id]: {
					order: 0,
					menuItemFactory: PasteMenuFactory
				}
			}
		},
		[DOC_PARAGRAPH_T_INSERT_BELOW_MENU_ID]: {
			quickTop: {
				order: 0,
				quickLayout: "icon",
				[`${DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID}.h1`]: {
					order: 0,
					menuItemFactory: ParagraphMenuInsertBelowHeadingH1MenuItemFactory
				},
				[`${DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID}.h2`]: {
					order: 1,
					menuItemFactory: ParagraphMenuInsertBelowHeadingH2MenuItemFactory
				},
				[`${DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID}.h3`]: {
					order: 2,
					menuItemFactory: ParagraphMenuInsertBelowHeadingH3MenuItemFactory
				},
				[`${DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID}.h4`]: {
					order: 3,
					menuItemFactory: ParagraphMenuInsertBelowHeadingH4MenuItemFactory
				},
				[`${DOC_PARAGRAPH_T_INSERT_BELOW_COMMAND_ID}.h5`]: {
					order: 4,
					menuItemFactory: ParagraphMenuInsertBelowHeadingH5MenuItemFactory
				},
				[InsertOrderListBellowCommand.id]: {
					order: 5,
					menuItemFactory: InsertOrderListBellowMenuItemFactory
				}
			},
			quickBottom: {
				order: 1,
				quickLayout: "icon",
				[InsertBulletListBellowCommand.id]: {
					order: 0,
					menuItemFactory: InsertBulletListBellowMenuItemFactory
				},
				[InsertCheckListBellowCommand.id]: {
					order: 1,
					menuItemFactory: InsertCheckListBellowMenuItemFactory
				},
				[InsertHorizontalLineBellowCommand.id]: {
					order: 5,
					menuItemFactory: InsertHorizontalLineBellowMenuItemFactory
				}
			},
			insert: {
				order: 2,
				[`${DocCreateTableOperation.id}.below`]: {
					order: 0,
					menuItemFactory: ParagraphMenuInsertBelowTableMenuItemFactory
				}
			}
		},
		[DOC_PARAGRAPH_T_EDIT_MENU_ID]: {
			quickTop: {
				order: 0,
				quickLayout: "icon",
				[NormalTextHeadingCommand.id]: {
					order: 0,
					menuItemFactory: NormalTextHeadingMenuItemFactory
				},
				[H1HeadingCommand.id]: {
					order: 1,
					menuItemFactory: H1HeadingMenuItemFactory
				},
				[H2HeadingCommand.id]: {
					order: 2,
					menuItemFactory: H2HeadingMenuItemFactory
				},
				[H3HeadingCommand.id]: {
					order: 3,
					menuItemFactory: H3HeadingMenuItemFactory
				},
				[H4HeadingCommand.id]: {
					order: 4,
					menuItemFactory: H4HeadingMenuItemFactory
				},
				[H5HeadingCommand.id]: {
					order: 5,
					menuItemFactory: H5HeadingMenuItemFactory
				},
				[TitleHeadingCommand.id]: {
					order: 6,
					menuItemFactory: TitleHeadingMenuItemFactory
				},
				[SubtitleHeadingCommand.id]: {
					order: 7,
					menuItemFactory: SubtitleHeadingMenuItemFactory
				}
			},
			quickBottom: {
				order: 1,
				quickLayout: "icon",
				[OrderListCommand.id]: {
					order: 0,
					menuItemFactory: OrderListMenuItemFactory
				},
				[BulletListCommand.id]: {
					order: 1,
					menuItemFactory: BulletListMenuItemFactory
				},
				[CheckListCommand.id]: {
					order: 2,
					menuItemFactory: CheckListMenuItemFactory
				}
			},
			layout: {
				order: 2,
				[DOC_PARAGRAPH_T_ALIGN_MENU_ID]: {
					order: 0,
					menuItemFactory: ParagraphMenuAlignSubmenuItemFactory
				},
				[DOC_PARAGRAPH_T_COLORS_MENU_ID]: {
					order: 1,
					menuItemFactory: ParagraphMenuColorsSubmenuItemFactory
				}
			},
			format: {
				order: 3,
				[DocCutCurrentParagraphCommand.id]: {
					order: 0,
					menuItemFactory: CutCurrentParagraphMenuItemFactory
				},
				[DocCopyCurrentParagraphCommand.id]: {
					order: 1,
					menuItemFactory: CopyCurrentParagraphMenuItemFactory
				},
				[DocPasteCommand.id]: {
					order: 2,
					menuItemFactory: PasteMenuFactory
				},
				[DeleteCurrentParagraphCommand.id]: {
					order: 3,
					menuItemFactory: DeleteCurrentParagraphMenuItemFactory
				}
			},
			others: {
				order: 4,
				[DOC_PARAGRAPH_T_INSERT_BELOW_MENU_ID]: {
					order: 0,
					menuItemFactory: ParagraphMenuInsertBelowSubmenuItemFactory
				}
			}
		},
		[DOC_PARAGRAPH_T_DIVIDER_MENU_ID]: {
			layout: {
				order: 1,
				[DOC_PARAGRAPH_T_COLORS_MENU_ID]: {
					order: 0,
					menuItemFactory: ParagraphMenuColorsSubmenuItemFactory
				}
			},
			format: {
				order: 2,
				[DocCutCurrentParagraphCommand.id]: {
					order: 0,
					menuItemFactory: CutCurrentParagraphMenuItemFactory
				},
				[DocCopyCurrentParagraphCommand.id]: {
					order: 1,
					menuItemFactory: CopyCurrentParagraphMenuItemFactory
				},
				[DeleteCurrentParagraphCommand.id]: {
					order: 2,
					menuItemFactory: DeleteCurrentParagraphMenuItemFactory
				}
			},
			others: {
				order: 3,
				[DOC_PARAGRAPH_T_INSERT_BELOW_MENU_ID]: {
					order: 0,
					menuItemFactory: ParagraphMenuInsertBelowSubmenuItemFactory
				}
			}
		},
		[DOC_PARAGRAPH_T_ALIGN_MENU_ID]: {
			align: {
				order: 0,
				title: "docs-ui.paragraphMenu.align",
				quickLayout: "icon",
				[AlignOperationCommand.id]: {
					order: 0,
					menuItemFactory: AlignLeftMenuItemFactory
				},
				[`${AlignOperationCommand.id}.center`]: {
					order: 1,
					menuItemFactory: AlignCenterMenuItemFactory
				},
				[`${AlignOperationCommand.id}.right`]: {
					order: 2,
					menuItemFactory: AlignRightMenuItemFactory
				},
				[`${AlignOperationCommand.id}.justify`]: {
					order: 3,
					menuItemFactory: AlignJustifyMenuItemFactory
				}
			},
			indent: {
				order: 1,
				title: "docs-ui.paragraphMenu.indent",
				[DOC_PARAGRAPH_T_INDENT_INCREASE_ID]: {
					order: 0,
					menuItemFactory: ParagraphMenuIndentIncreaseMenuItemFactory
				},
				[DOC_PARAGRAPH_T_INDENT_DECREASE_ID]: {
					order: 1,
					menuItemFactory: ParagraphMenuIndentDecreaseMenuItemFactory
				}
			}
		},
		[DOC_PARAGRAPH_T_COLORS_MENU_ID]: {
			text: {
				order: 0,
				title: "docs-ui.toolbar.textColor.main",
				headerActionMenuItemFactory: ParagraphMenuTextColorHeaderActionMenuItemFactory,
				quickLayout: "icon",
				quickColumns: 8,
				quickLayoutVariant: "compact",
				[ResetInlineFormatTextColorCommand.id]: {
					order: 0,
					menuItemFactory: ParagraphMenuResetTextColorMenuItemFactory
				},
				...ParagraphMenuTextColorSwatchMenuItemFactories
			},
			backgroundTop: {
				order: 1,
				title: "docs-ui.toolbar.fillColor.main",
				headerActionMenuItemFactory: ParagraphMenuBackgroundColorHeaderActionMenuItemFactory,
				quickLayout: "icon",
				quickColumns: 8,
				quickLayoutVariant: "compact",
				[ResetInlineFormatTextBackgroundColorCommand.id]: {
					order: 0,
					menuItemFactory: ParagraphMenuNoBackgroundMenuItemFactory
				},
				...Object.fromEntries(Object.entries(ParagraphMenuBackgroundColorSwatchMenuItemFactories).filter(([, config]) => config.order < 7))
			},
			backgroundBottom: {
				order: 2,
				quickLayout: "icon",
				quickColumns: 8,
				quickLayoutVariant: "compact",
				...Object.fromEntries(Object.entries(ParagraphMenuBackgroundColorSwatchMenuItemFactories).filter(([, config]) => config.order >= 7))
			}
		}
	}
};

//#endregion
//#region src/shortcuts/utils.ts
function whenDocAndEditorFocused(contextService) {
	return contextService.getContextValue(_univerjs_core.FOCUSING_DOC) && contextService.getContextValue(_univerjs_core.FOCUSING_UNIVER_EDITOR) && !contextService.getContextValue(_univerjs_core.FOCUSING_COMMON_DRAWINGS);
}
function whenDocAndEditorFocusedWithBreakLine(contextService) {
	return contextService.getContextValue(_univerjs_core.FOCUSING_DOC) && contextService.getContextValue(_univerjs_core.FOCUSING_UNIVER_EDITOR) && !contextService.getContextValue(_univerjs_core.FOCUSING_COMMON_DRAWINGS);
}

//#endregion
//#region src/shortcuts/format.shortcut.ts
const TabShortCut = {
	id: TabCommand.id,
	binding: _univerjs_ui.KeyCode.TAB,
	preconditions: whenDocAndEditorFocused
};
const ShiftTabShortCut = {
	id: TabCommand.id,
	binding: _univerjs_ui.KeyCode.TAB | _univerjs_ui.MetaKeys.SHIFT,
	preconditions: whenDocAndEditorFocused,
	staticParameters: { shift: true }
};

//#endregion
//#region src/shortcuts/toolbar.shortcut.ts
const BoldShortCut = {
	id: SetInlineFormatBoldCommand.id,
	binding: _univerjs_ui.KeyCode.B | _univerjs_ui.MetaKeys.CTRL_COMMAND,
	preconditions: whenDocAndEditorFocused
};
const ItalicShortCut = {
	id: SetInlineFormatItalicCommand.id,
	binding: _univerjs_ui.KeyCode.I | _univerjs_ui.MetaKeys.CTRL_COMMAND,
	preconditions: whenDocAndEditorFocused
};
const UnderlineShortCut = {
	id: SetInlineFormatUnderlineCommand.id,
	binding: _univerjs_ui.KeyCode.U | _univerjs_ui.MetaKeys.CTRL_COMMAND,
	preconditions: whenDocAndEditorFocused
};
const StrikeThroughShortCut = {
	id: SetInlineFormatStrikethroughCommand.id,
	binding: _univerjs_ui.KeyCode.X | _univerjs_ui.MetaKeys.SHIFT | _univerjs_ui.MetaKeys.CTRL_COMMAND,
	preconditions: whenDocAndEditorFocused
};
const SubscriptShortCut = {
	id: SetInlineFormatSubscriptCommand.id,
	binding: _univerjs_ui.KeyCode.COMMA | _univerjs_ui.MetaKeys.CTRL_COMMAND,
	preconditions: whenDocAndEditorFocused
};
const SuperscriptShortCut = {
	id: SetInlineFormatSuperscriptCommand.id,
	binding: _univerjs_ui.KeyCode.PERIOD | _univerjs_ui.MetaKeys.CTRL_COMMAND,
	preconditions: whenDocAndEditorFocused
};
const AlignLeftShortCut = {
	id: AlignLeftCommand.id,
	binding: _univerjs_ui.KeyCode.L | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT,
	preconditions: whenDocAndEditorFocused
};
const AlignRightShortCut = {
	id: AlignRightCommand.id,
	binding: _univerjs_ui.KeyCode.R | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT,
	preconditions: whenDocAndEditorFocused
};
const AlignCenterShortCut = {
	id: AlignCenterCommand.id,
	binding: _univerjs_ui.KeyCode.E | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT,
	preconditions: whenDocAndEditorFocused
};
const AlignJustifyShortCut = {
	id: AlignJustifyCommand.id,
	binding: _univerjs_ui.KeyCode.J | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT,
	preconditions: whenDocAndEditorFocused
};
const OrderListShortCut = {
	id: OrderListCommand.id,
	binding: _univerjs_ui.KeyCode.Digit7 | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT,
	preconditions: whenDocAndEditorFocused
};
const BulletListShortCut = {
	id: BulletListCommand.id,
	binding: _univerjs_ui.KeyCode.Digit8 | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT,
	preconditions: whenDocAndEditorFocused
};

//#endregion
//#region src/views/count-bar/ZoomSlider.tsx
const ZOOM_MAP = [
	50,
	80,
	100,
	130,
	150,
	170,
	200,
	400
];
const DOC_ZOOM_RANGE = [10, 400];
function ZoomSlider() {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const univerInstanceService = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService);
	const documentDataModel = (0, _univerjs_ui.useObservable)(() => univerInstanceService.getCurrentTypeOfUnit$(_univerjs_core.UniverInstanceType.UNIVER_DOC).pipe((0, rxjs.filter)((documentDataModel) => documentDataModel != null)), null, false, [univerInstanceService]);
	const getCurrentZoom = (0, react.useCallback)((docModel = documentDataModel) => {
		if (!docModel) return 100;
		return Math.round(getDocEffectiveZoomRatio(docModel) * 100);
	}, [documentDataModel]);
	const [zoom, setZoom] = (0, react.useState)(100);
	(0, react.useEffect)(() => setZoom(getCurrentZoom(documentDataModel)), [documentDataModel, getCurrentZoom]);
	(0, react.useEffect)(() => {
		return commandService.onCommandExecuted((commandInfo) => {
			if (commandInfo.id === SetDocZoomRatioOperation.id) setZoom(getCurrentZoom());
		}).dispose;
	}, [commandService, getCurrentZoom]);
	function handleChange(value) {
		setZoom(value);
		if (documentDataModel == null) return;
		const zoomRatio = value / 100;
		commandService.executeCommand(SetDocZoomRatioOperation.id, {
			unitId: documentDataModel.getUnitId(),
			zoomRatio
		});
	}
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_ui.Slider, {
		min: DOC_ZOOM_RANGE[0],
		value: zoom,
		shortcuts: ZOOM_MAP,
		onChange: handleChange
	});
}

//#endregion
//#region src/views/count-bar/CountBar.tsx
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
function CountBar(_props) {
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("section", {
		className: "univer-flex univer-flex-shrink-0 univer-justify-end",
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ZoomSlider, {})
	});
}

//#endregion
//#region src/views/doc-statistics/use-doc-statistics.ts
const STATISTICS_DEBOUNCE_TIME = 200;
const EMPTY_STATISTICS = {
	words: 0,
	charactersWithoutSpaces: 0,
	charactersWithSpaces: 0,
	paragraphs: 0,
	nonAsianWords: 0,
	asianCharactersAndKoreanWords: 0
};
const EMPTY_LAYOUT_STATISTICS = {
	pages: 0,
	lines: 0
};
function getBodySelectionRanges(selectionManager) {
	return (selectionManager.getTextRanges() ?? []).filter((range) => !range.collapsed && !range.segmentId).map(({ collapsed, direction, endOffset, isActive, rangeType, segmentId, segmentPage, startOffset }) => ({
		collapsed,
		direction,
		endOffset,
		isActive,
		rangeType,
		segmentId,
		segmentPage,
		startOffset
	}));
}
function rangeIntersectsLine(range, startOffset, endOffset) {
	const rangeStart = Math.min(range.startOffset, range.endOffset);
	const rangeEnd = Math.max(range.startOffset, range.endOffset);
	return rangeStart < endOffset && rangeEnd > startOffset;
}
function getDocLayoutStatistics(skeleton, ranges = [], calculatePages = true) {
	var _skeleton$getSkeleton;
	const pages = (skeleton === null || skeleton === void 0 || (_skeleton$getSkeleton = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton === void 0 ? void 0 : _skeleton$getSkeleton.pages) ?? [];
	if (ranges.length === 0) {
		let lines = 0;
		(0, _univerjs_engine_render.documentSkeletonLineIterator)(pages, {}, () => lines++);
		return {
			lines,
			pages: calculatePages ? pages.length : 0
		};
	}
	let lines = 0;
	const selectedPages = calculatePages ? /* @__PURE__ */ new Set() : null;
	(0, _univerjs_engine_render.documentSkeletonLineIterator)(pages, {}, ({ line, pageIndex }) => {
		if (ranges.some((range) => rangeIntersectsLine(range, line.st, line.ed))) {
			lines += 1;
			selectedPages === null || selectedPages === void 0 || selectedPages.add(pageIndex);
		}
	});
	return {
		lines,
		pages: (selectedPages === null || selectedPages === void 0 ? void 0 : selectedPages.size) ?? 0
	};
}
async function calculateStatistics({ calculateLayout, calculatePages, documentDataModel, cachedStatistics, locale, selectionRanges, signal, skeleton }) {
	const documentLayout = calculateLayout ? getDocLayoutStatistics(skeleton, [], calculatePages) : EMPTY_LAYOUT_STATISTICS;
	const selectionLayout = calculateLayout && selectionRanges.length > 0 ? getDocLayoutStatistics(skeleton, selectionRanges, calculatePages) : EMPTY_LAYOUT_STATISTICS;
	const [documentStatistics, selectionStatistics] = await Promise.all([cachedStatistics ?? documentDataModel.getStatistics({
		locale,
		signal
	}), selectionRanges.length > 0 ? documentDataModel.getStatistics({
		locale,
		ranges: selectionRanges,
		signal
	}) : null]);
	return {
		document: {
			...documentStatistics,
			...documentLayout
		},
		selection: selectionStatistics ? {
			...selectionStatistics,
			...selectionLayout
		} : null
	};
}
function cacheDocumentStatistics(cacheRef, documentDataModel, locale, revision, statistics) {
	cacheRef.current = {
		documentDataModel,
		locale,
		revision,
		statistics: {
			words: statistics.words,
			charactersWithoutSpaces: statistics.charactersWithoutSpaces,
			charactersWithSpaces: statistics.charactersWithSpaces,
			paragraphs: statistics.paragraphs,
			nonAsianWords: statistics.nonAsianWords,
			asianCharactersAndKoreanWords: statistics.asianCharactersAndKoreanWords
		}
	};
}
function useStatisticsSelection(documentDataModel, selectionManager) {
	return (0, _univerjs_ui.useObservable)(documentDataModel ? () => (0, rxjs.combineLatest)([selectionManager.textSelection$.pipe((0, rxjs.startWith)(void 0)), documentDataModel.change$]).pipe((0, rxjs.map)(([, revision]) => ({
		ranges: getBodySelectionRanges(selectionManager),
		revision
	}))) : null, {
		ranges: [],
		revision: 0
	}, false, [documentDataModel, selectionManager]);
}
function useDocumentSkeleton(documentDataModel, renderManagerService) {
	var _renderManagerService;
	const skeletonManager = documentDataModel ? (_renderManagerService = renderManagerService.getRenderUnitById(documentDataModel.getUnitId())) === null || _renderManagerService === void 0 ? void 0 : _renderManagerService.with(_univerjs_docs.DocSkeletonManagerService) : null;
	return (0, _univerjs_ui.useObservable)(skeletonManager ? skeletonManager.currentSkeleton$.pipe((0, rxjs.map)((value) => ({ value }))) : null, { value: (skeletonManager === null || skeletonManager === void 0 ? void 0 : skeletonManager.getSkeleton()) ?? null }, false, [skeletonManager]).value;
}
function useDocStatistics(calculateLayout = false) {
	const instanceService = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService);
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const renderManagerService = (0, _univerjs_ui.useDependency)(_univerjs_engine_render.IRenderManagerService);
	const selectionManager = (0, _univerjs_ui.useDependency)(_univerjs_docs.DocSelectionManagerService);
	const documentStatisticsCacheRef = (0, react.useRef)(null);
	const documentDataModel = (0, _univerjs_ui.useObservable)((0, react.useMemo)(() => instanceService.getCurrentTypeOfUnit$(_univerjs_core.UniverInstanceType.UNIVER_DOC), [instanceService]));
	const selectionSnapshot = useStatisticsSelection(documentDataModel, selectionManager);
	const locale = (0, _univerjs_ui.useObservable)(localeService.currentLocale$, localeService.getCurrentLocale());
	const showPages = (documentDataModel === null || documentDataModel === void 0 ? void 0 : documentDataModel.getDocumentStyle().documentFlavor) !== _univerjs_core.DocumentFlavor.MODERN;
	const skeleton = useDocumentSkeleton(calculateLayout ? documentDataModel : null, renderManagerService);
	const [state, setState] = (0, react.useState)({
		document: {
			...EMPTY_STATISTICS,
			lines: 0,
			pages: 0
		},
		selection: null,
		loading: true
	});
	const selectionRanges = selectionSnapshot.ranges;
	(0, react.useEffect)(() => {
		if (!(documentDataModel === null || documentDataModel === void 0 ? void 0 : documentDataModel.getBody())) return;
		const currentDocumentDataModel = documentDataModel;
		const cachedDocumentStatistics = documentStatisticsCacheRef.current;
		const hasCachedDocumentStatistics = (cachedDocumentStatistics === null || cachedDocumentStatistics === void 0 ? void 0 : cachedDocumentStatistics.documentDataModel) === currentDocumentDataModel && cachedDocumentStatistics.locale === locale && cachedDocumentStatistics.revision === selectionSnapshot.revision;
		const abortController = new AbortController();
		const timeout = setTimeout(async () => {
			setState((current) => ({
				...current,
				loading: true
			}));
			try {
				const result = await calculateStatistics({
					calculateLayout,
					calculatePages: showPages,
					documentDataModel: currentDocumentDataModel,
					cachedStatistics: hasCachedDocumentStatistics ? cachedDocumentStatistics.statistics : void 0,
					locale,
					selectionRanges,
					signal: abortController.signal,
					skeleton: skeleton ?? null
				});
				if (!hasCachedDocumentStatistics) cacheDocumentStatistics(documentStatisticsCacheRef, currentDocumentDataModel, locale, selectionSnapshot.revision, result.document);
				setState({
					...result,
					loading: false
				});
			} catch {
				if (!abortController.signal.aborted) setState((current) => ({
					...current,
					loading: false
				}));
			}
		}, STATISTICS_DEBOUNCE_TIME);
		return () => {
			clearTimeout(timeout);
			abortController.abort();
		};
	}, [
		calculateLayout,
		documentDataModel,
		locale,
		selectionRanges,
		selectionSnapshot,
		showPages,
		skeleton
	]);
	return {
		...state,
		showPages
	};
}

//#endregion
//#region src/views/doc-statistics/DocStatistics.tsx
const STATISTIC_ROW_GROUPS = [
	[{
		key: "pages",
		label: "docs-ui.statistics.pages"
	}, {
		key: "words",
		label: "docs-ui.statistics.words"
	}],
	[{
		key: "charactersWithoutSpaces",
		label: "docs-ui.statistics.charactersWithoutSpaces"
	}, {
		key: "charactersWithSpaces",
		label: "docs-ui.statistics.charactersWithSpaces"
	}],
	[{
		key: "paragraphs",
		label: "docs-ui.statistics.paragraphs"
	}, {
		key: "lines",
		label: "docs-ui.statistics.lines"
	}],
	[{
		key: "nonAsianWords",
		label: "docs-ui.statistics.nonAsianWords"
	}, {
		key: "asianCharactersAndKoreanWords",
		label: "docs-ui.statistics.asianCharactersAndKoreanWords"
	}]
];
function StatisticsContent({ statistics, selection, showPages, loading }) {
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const regionService = (0, _univerjs_ui.useDependency)(_univerjs_core.RegionService);
	const direction = (0, _univerjs_ui.useObservable)(localeService.direction$, localeService.getDirection());
	const regionTag = _univerjs_core.LOCALE_META[(0, _univerjs_ui.useObservable)(regionService.currentRegion$, regionService.getCurrentRegion())].tag;
	const statisticGroups = STATISTIC_ROW_GROUPS.map((rows) => rows.filter(({ key }) => showPages || key !== "pages")).filter((rows) => rows.length > 0);
	const showLoadingPlaceholder = loading && statisticGroups.flat().every(({ key }) => statistics[key] === 0);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		className: "univer-w-full",
		dir: direction,
		children: [
			/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("header", {
				className: "univer-flex univer-items-center univer-gap-3 univer-px-4 univer-py-3",
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: "univer-min-w-0 univer-flex-1 rtl:univer-text-right",
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						className: "univer-truncate univer-text-sm univer-font-semibold univer-leading-5",
						children: localeService.t("docs-ui.statistics.title")
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						className: "univer-truncate univer-text-xs univer-leading-4 univer-text-gray-500 dark:!univer-text-gray-400",
						children: localeService.t(selection ? "docs-ui.statistics.selection" : "docs-ui.statistics.document")
					})]
				}), loading && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.LoadingMultiIcon, {
					"aria-hidden": "true",
					className: "univer-size-4 univer-shrink-0 univer-animate-spin univer-text-primary-600"
				})]
			}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Separator, {}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsx)("dl", {
				className: "univer-m-0 univer-p-2",
				children: statisticGroups.map((rows, groupIndex) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", { children: [groupIndex > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Separator, { className: "univer-my-1" }), rows.map(({ key, label }) => /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: "univer-flex univer-items-center univer-justify-between univer-gap-4 univer-rounded-md univer-px-2 univer-py-1",
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("dt", {
						className: "univer-min-w-0 univer-flex-1 univer-text-sm univer-leading-5 univer-text-gray-600 rtl:univer-text-right dark:!univer-text-gray-300",
						children: localeService.t(label)
					}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("dd", {
						className: "univer-m-0 univer-min-w-12 univer-shrink-0 univer-text-right univer-text-sm univer-font-semibold univer-tabular-nums univer-leading-5 univer-text-gray-900 rtl:univer-text-left dark:!univer-text-gray-0",
						children: showLoadingPlaceholder ? "—" : statistics[key].toLocaleString(regionTag)
					})]
				}, key))] }, rows[0].key))
			})
		]
	});
}
function DocStatistics() {
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const regionService = (0, _univerjs_ui.useDependency)(_univerjs_core.RegionService);
	const [open, setOpen] = (0, react.useState)(false);
	const { document, selection, loading, showPages } = useDocStatistics(open);
	const regionTag = _univerjs_core.LOCALE_META[(0, _univerjs_ui.useObservable)(regionService.currentRegion$, regionService.getCurrentRegion())].tag;
	const displayedStatistics = selection ?? document;
	const openLabel = localeService.t("docs-ui.statistics.open");
	const title = localeService.t("docs-ui.statistics.title");
	const label = selection ? localeService.t("docs-ui.statistics.selectedWords", selection.words.toLocaleString(regionTag), document.words.toLocaleString(regionTag)) : localeService.t("docs-ui.statistics.wordCount", document.words.toLocaleString(regionTag));
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Dropdown, {
		align: "start",
		className: "univer-w-80 univer-shadow-xl",
		side: "top",
		open,
		"aria-busy": loading,
		"aria-label": title,
		onOpenChange: setOpen,
		overlay: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(StatisticsContent, {
			statistics: displayedStatistics,
			selection: selection != null,
			showPages,
			loading
		}),
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
			title: open ? void 0 : openLabel,
			children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_ui.ToolbarButton, {
				noIcon: true,
				active: open,
				className: "!univer-h-7 univer-px-2 univer-text-xs !univer-text-gray-600 focus-visible:univer-ring-2 focus-visible:univer-ring-primary-500 dark:!univer-text-gray-300",
				"aria-expanded": open,
				"aria-haspopup": "dialog",
				"aria-label": openLabel,
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
					className: "univer-flex univer-items-center univer-gap-1.5",
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.StatisticalFunctionIcon, { className: "univer-size-4" }),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { children: label }),
						loading && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.LoadingMultiIcon, { className: "univer-size-3.5 univer-animate-spin" })
					]
				})
			})
		})
	});
}

//#endregion
//#region src/services/doc-layout-progress.service.ts
let DocLayoutProgressService = class DocLayoutProgressService extends _univerjs_core.RxDisposable {
	constructor(_univerInstanceService) {
		super();
		this._univerInstanceService = _univerInstanceService;
		_defineProperty(this, "_progressByUnit$", new rxjs.BehaviorSubject(/* @__PURE__ */ new Map()));
		_defineProperty(this, "currentProgress$", void 0);
		this.currentProgress$ = (0, rxjs.combineLatest)([this._univerInstanceService.getCurrentTypeOfUnit$(_univerjs_core.UniverInstanceType.UNIVER_DOC), this._progressByUnit$]).pipe((0, rxjs.map)(([document, progressByUnit]) => {
			if (!document) return null;
			const unitId = document.getUnitId();
			const progress = progressByUnit.get(unitId);
			return progress == null ? null : {
				progress,
				unitId
			};
		}), (0, rxjs.distinctUntilChanged)((previous, current) => (previous === null || previous === void 0 ? void 0 : previous.unitId) === (current === null || current === void 0 ? void 0 : current.unitId) && (previous === null || previous === void 0 ? void 0 : previous.progress) === (current === null || current === void 0 ? void 0 : current.progress)), (0, rxjs.takeUntil)(this.dispose$));
	}
	setProgress(unitId, progress) {
		const current = this._progressByUnit$.value;
		if (current.get(unitId) === progress) return;
		this._progressByUnit$.next(new Map(current).set(unitId, progress));
	}
	clearProgress(unitId) {
		const current = this._progressByUnit$.value;
		if (!current.has(unitId)) return;
		const next = new Map(current);
		next.delete(unitId);
		this._progressByUnit$.next(next);
	}
	dispose() {
		this._progressByUnit$.complete();
		super.dispose();
	}
};
DocLayoutProgressService = __decorate([__decorateParam(0, _univerjs_core.IUniverInstanceService)], DocLayoutProgressService);

//#endregion
//#region src/views/DocLayoutProgress.tsx
function DocLayoutProgress() {
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const progressState = (0, _univerjs_ui.useObservable)((0, _univerjs_ui.useDependency)(DocLayoutProgressService).currentProgress$, null);
	if (progressState == null) return null;
	const { progress } = progressState;
	const label = localeService.t("docs-ui.layout.progress");
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		className: "univer-pointer-events-none univer-absolute univer-left-1/2 univer-top-1/2 univer-flex -univer-translate-x-1/2 -univer-translate-y-1/2 univer-items-center univer-gap-2 univer-text-xs univer-text-gray-600 dark:univer-text-gray-300",
		children: [
			/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
				className: "univer-whitespace-nowrap",
				children: label
			}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				"aria-label": label,
				"aria-valuemax": 100,
				"aria-valuemin": 0,
				"aria-valuenow": progress,
				className: "univer-h-1 univer-w-32 univer-overflow-hidden univer-rounded-full univer-bg-gray-200 dark:univer-bg-gray-700",
				role: "progressbar",
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: "univer-h-full univer-rounded-full univer-bg-primary-600 univer-transition-[width]",
					style: { width: `${progress}%` }
				})
			}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("span", {
				className: "univer-w-8 univer-tabular-nums",
				children: [progress, "%"]
			})
		]
	});
}

//#endregion
//#region src/views/doc-footer/DocFooter.tsx
function DocFooterContent() {
	const config = (0, _univerjs_ui.useConfigValue)(DOCS_UI_PLUGIN_CONFIG_KEY);
	return ((config === null || config === void 0 ? void 0 : config.footer) ?? true) && /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		className: "univer-relative univer-box-border univer-flex univer-items-center univer-justify-between univer-px-5 univer-py-1.5",
		children: [
			(config === null || config === void 0 ? void 0 : config.wordCount) !== false && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(DocStatistics, {}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsx)(DocLayoutProgress, {}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsx)(CountBar, {})
		]
	});
}
function DocFooter() {
	if ((0, _univerjs_ui.useObservable)((0, _univerjs_ui.useDependency)(_univerjs_ui.IWorkbenchService).rootUnitType$, null, true) !== _univerjs_core.UniverInstanceType.UNIVER_DOC) return null;
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(DocFooterContent, {});
}

//#endregion
//#region src/views/SideMenu.tsx
const SideMenu = (0, react.forwardRef)((props, ref) => {
	const { menus, onClick, className, style, mode, maxHeight, activeId, open, onOpenChange, maxWidth, wrapperClass, wrapperStyle, iconClass, iconStyle } = props;
	const isSideBar = mode === "side-bar";
	const containerRef = (0, react.useRef)(null);
	const menusRef = (0, react.useRef)(menus);
	menusRef.current = menus;
	const instance = (0, react.useMemo)(() => ({ scrollTo: (id) => {
		var _containerRef$current;
		if (!menusRef.current) return;
		if (menusRef.current.findIndex((menu) => menu.id === id) === -1 || !containerRef.current) return;
		const targetElement = document.getElementById(`univer-side-menu-${id}`);
		if (!targetElement) return;
		const targetTop = targetElement.offsetTop;
		const containerHeight = containerRef.current.clientHeight;
		const maxScrollTop = containerRef.current.scrollHeight - containerHeight;
		const scrollPosition = Math.max(0, Math.min(targetTop - containerHeight / 2 + targetElement.clientHeight / 2, maxScrollTop));
		(_containerRef$current = containerRef.current) === null || _containerRef$current === void 0 || _containerRef$current.scrollTo({
			behavior: "smooth",
			top: scrollPosition
		});
	} }), []);
	(0, react.useImperativeHandle)(ref, () => instance);
	(0, react.useEffect)(() => {
		if (activeId) instance.scrollTo(activeId);
	}, [activeId, instance]);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		className: (0, _univerjs_design.clsx)("univer-relative", wrapperClass),
		style: wrapperStyle,
		children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
			onClick: () => onOpenChange === null || onOpenChange === void 0 ? void 0 : onOpenChange(!open),
			className: (0, _univerjs_design.clsx)("univer-absolute univer-left-5 univer-top-4 univer-z-[100] univer-flex univer-size-8 univer-cursor-pointer univer-items-center univer-justify-center univer-rounded-full univer-bg-gray-0 univer-text-gray-800 univer-shadow-sm dark:!univer-bg-gray-600 dark:!univer-text-gray-200", iconClass),
			style: iconStyle,
			children: open ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.MoreLeftIcon, {}) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.CatalogueIcon, {})
		}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
			className: (0, _univerjs_design.clsx)(className, "univer-absolute univer-left-0 univer-top-0 univer-box-border univer-flex univer-min-w-[180px] univer-flex-col univer-px-4 univer-pb-4 univer-pt-14 univer-transition-all univer-duration-300", { "univer-rounded-r-2xl univer-bg-gray-0 univer-shadow univer-backdrop-blur-[10px] dark:!univer-bg-gray-900": isSideBar }),
			style: {
				...style,
				transform: open ? "translateX(0)" : "translateX(-100%)",
				maxHeight,
				opacity: open ? 1 : 0,
				maxWidth: maxWidth ?? (mode === "side-bar" ? 320 : 180),
				paddingRight: mode === "float" ? void 0 : 0
			},
			children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				ref: containerRef,
				className: (0, _univerjs_design.clsx)("univer-flex-1 univer-overflow-y-auto univer-overflow-x-hidden", _univerjs_design.scrollbarClassName),
				children: menus === null || menus === void 0 ? void 0 : menus.map((menu) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					id: `univer-side-menu-${menu.id}`,
					className: (0, _univerjs_design.clsx)("univer-mb-2 univer-h-[24px] univer-cursor-pointer univer-truncate univer-pr-1 univer-font-[500] univer-leading-[24px]", {
						"univer-text-base univer-font-semibold": menu.isTitle,
						"univer-text-sm univer-font-semibold": menu.level === 1,
						"univer-text-sm": menu.level > 1,
						"univer-text-gray-500 dark:!univer-text-gray-400": menu.id !== activeId,
						"univer-text-gray-800 dark:!univer-text-gray-200": menu.id === activeId
					}),
					style: { paddingLeft: (menu.level - 1) * 12 },
					onClick: () => {
						onClick === null || onClick === void 0 || onClick(menu);
					},
					children: menu.text
				}, menu.id))
			})
		})]
	});
});

//#endregion
//#region src/views/DocSideMenu.tsx
const transformNamedStyleTypeToLevel = (type) => {
	switch (type) {
		case _univerjs_core.NamedStyleType.HEADING_1: return 1;
		case _univerjs_core.NamedStyleType.HEADING_2: return 2;
		case _univerjs_core.NamedStyleType.HEADING_3: return 3;
		case _univerjs_core.NamedStyleType.HEADING_4: return 4;
		case _univerjs_core.NamedStyleType.HEADING_5: return 5;
		case _univerjs_core.NamedStyleType.TITLE: return 1;
		default: return 1;
	}
};
function findActiveHeading(boundMap, paragraphMap, scrollTop, bottom) {
	if (!boundMap) return;
	const paragraphBounds = Array.from(boundMap.values());
	const paragraphIndex = paragraphBounds.findIndex((p) => p.paragraphStart !== p.paragraphEnd && p.rect.top < bottom && p.rect.bottom > scrollTop);
	if (paragraphIndex === -1) return void 0;
	const lastParagraphIndex = paragraphBounds === null || paragraphBounds === void 0 ? void 0 : paragraphBounds.findLastIndex((p) => p.paragraphStart !== p.paragraphEnd && p.rect.top < bottom && p.rect.bottom > scrollTop);
	for (let i = paragraphIndex; i <= lastParagraphIndex; i++) {
		var _paragraph$paragraphS;
		const bound = paragraphBounds[i];
		const paragraph = paragraphMap.get(bound.startIndex);
		if (paragraph === null || paragraph === void 0 || (_paragraph$paragraphS = paragraph.paragraphStyle) === null || _paragraph$paragraphS === void 0 ? void 0 : _paragraph$paragraphS.headingId) return paragraph.paragraphStyle.headingId;
	}
	for (let i = paragraphIndex; i >= 0; i--) {
		var _paragraph$paragraphS2;
		const bound = paragraphBounds[i];
		const paragraph = paragraphMap.get(bound.startIndex);
		if (paragraph === null || paragraph === void 0 || (_paragraph$paragraphS2 = paragraph.paragraphStyle) === null || _paragraph$paragraphS2 === void 0 ? void 0 : _paragraph$paragraphS2.headingId) return paragraph.paragraphStyle.headingId;
	}
}
const TITLE_ID = "__title";
function DocSideMenu() {
	const config = (0, _univerjs_ui.useConfigValue)(DOCS_UI_PLUGIN_CONFIG_KEY);
	if ((0, _univerjs_ui.useObservable)((0, _univerjs_ui.useDependency)(_univerjs_ui.IWorkbenchService).rootUnitType$, null, true) === _univerjs_core.UniverInstanceType.UNIVER_DOC && (config === null || config === void 0 ? void 0 : config.toc)) return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(DocSideMenuContent, {});
	return null;
}
function DocSideMenuContent() {
	var _documentData$body, _documentData$body3;
	const instanceService = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService);
	const currentDoc = (0, _univerjs_ui.useObservable)(() => instanceService.getCurrentTypeOfUnit$(_univerjs_core.UniverInstanceType.UNIVER_DOC), void 0, false, [instanceService]);
	const renderManagerService = (0, _univerjs_ui.useDependency)(_univerjs_engine_render.IRenderManagerService);
	const documentData = (0, _univerjs_ui.useObservable)(currentDoc ? () => currentDoc.change$.pipe((0, rxjs.debounceTime)(100), (0, rxjs.map)(() => currentDoc.getSnapshot())) : null, currentDoc === null || currentDoc === void 0 ? void 0 : currentDoc.getSnapshot(), false, [currentDoc]);
	const fullDataStream = (documentData === null || documentData === void 0 || (_documentData$body = documentData.body) === null || _documentData$body === void 0 ? void 0 : _documentData$body.dataStream) ?? "";
	const [activeId, setActiveId] = (0, react.useState)(void 0);
	const unitId = (currentDoc === null || currentDoc === void 0 ? void 0 : currentDoc.getUnitId()) ?? "";
	const renderer = renderManagerService.getRenderUnitById(unitId);
	const title = documentData === null || documentData === void 0 ? void 0 : documentData.title;
	const docEventManagerService = renderer === null || renderer === void 0 ? void 0 : renderer.with(DocEventManagerService);
	const paragraphBounds = docEventManagerService === null || docEventManagerService === void 0 ? void 0 : docEventManagerService.paragraphBounds;
	const readLayout = () => {
		var _renderer$mainCompone;
		return {
			left: (renderer === null || renderer === void 0 || (_renderer$mainCompone = renderer.mainComponent) === null || _renderer$mainCompone === void 0 ? void 0 : _renderer$mainCompone.left) ?? 0,
			canvasHeight: (renderer === null || renderer === void 0 ? void 0 : renderer.engine.height) ?? 0,
			scaleY: (renderer === null || renderer === void 0 ? void 0 : renderer.scene.scaleY) ?? 1
		};
	};
	const { left, canvasHeight, scaleY } = (0, _univerjs_ui.useObservable)((renderer === null || renderer === void 0 ? void 0 : renderer.engine.onTransformChange$) ? () => (0, _univerjs_core.fromEventSubject)(renderer.engine.onTransformChange$).pipe((0, rxjs.throttleTime)(33), (0, rxjs.map)(readLayout), (0, rxjs.startWith)(readLayout())) : null, readLayout(), false, [renderer]);
	const paragraphs = (0, react.useMemo)(() => {
		var _documentData$body2;
		return (documentData === null || documentData === void 0 || (_documentData$body2 = documentData.body) === null || _documentData$body2 === void 0 ? void 0 : _documentData$body2.paragraphs) ?? [];
	}, [documentData === null || documentData === void 0 || (_documentData$body3 = documentData.body) === null || _documentData$body3 === void 0 ? void 0 : _documentData$body3.paragraphs]);
	const paragraphMap = (0, react.useMemo)(() => {
		const map = /* @__PURE__ */ new Map();
		paragraphs.forEach((p) => {
			map.set(p.startIndex, p);
		});
		return map;
	}, [paragraphs]);
	const mode = left < 180 ? "float" : "side-bar";
	const paragraphMenus = paragraphs === null || paragraphs === void 0 ? void 0 : paragraphs.filter((p) => {
		var _p$paragraphStyle;
		return ((_p$paragraphStyle = p.paragraphStyle) === null || _p$paragraphStyle === void 0 ? void 0 : _p$paragraphStyle.namedStyleType) !== void 0 && p.paragraphStyle.namedStyleType !== _univerjs_core.NamedStyleType.SUBTITLE && p.paragraphStyle.namedStyleType !== _univerjs_core.NamedStyleType.NORMAL_TEXT;
	}).map((p) => {
		var _p$paragraphStyle2;
		const level = transformNamedStyleTypeToLevel(p.paragraphStyle.namedStyleType);
		const bound = paragraphBounds === null || paragraphBounds === void 0 ? void 0 : paragraphBounds.get(p.startIndex);
		if (!bound) return null;
		const { paragraphStart, paragraphEnd } = bound;
		return {
			id: p.paragraphStyle.headingId,
			text: (0, _univerjs_core.getPlainText)(fullDataStream.slice(paragraphStart, paragraphEnd)),
			level,
			isTitle: ((_p$paragraphStyle2 = p.paragraphStyle) === null || _p$paragraphStyle2 === void 0 ? void 0 : _p$paragraphStyle2.namedStyleType) === _univerjs_core.NamedStyleType.TITLE
		};
	}).filter((item) => item === null || item === void 0 ? void 0 : item.text);
	const handleScroll = (0, _univerjs_ui.useEvent)((params) => {
		const scrollTop = params.viewportScrollY;
		const bottom = scrollTop + canvasHeight / scaleY;
		const activeId = findActiveHeading(paragraphBounds, paragraphMap, scrollTop, bottom);
		if (activeId) setActiveId(activeId);
	});
	const menus = (paragraphMenus === null || paragraphMenus === void 0 ? void 0 : paragraphMenus.find((p) => p.isTitle)) ? paragraphMenus : [...title ? [{
		id: TITLE_ID,
		text: title,
		level: 1,
		isTitle: true
	}] : [], ...paragraphMenus ?? []].filter(Boolean);
	const [open, setOpen] = (0, react.useState)(true);
	(0, react.useEffect)(() => {
		const viewport = renderer === null || renderer === void 0 ? void 0 : renderer.scene.getViewport("viewMain");
		if (!viewport) return;
		const sub = (0, _univerjs_core.fromEventSubject)(viewport.onScrollAfter$).pipe((0, rxjs.throttleTime)(33)).subscribe(handleScroll);
		return () => {
			sub.unsubscribe();
		};
	}, [handleScroll, renderer]);
	const handleClick = (0, _univerjs_ui.useEvent)((menu) => {
		const viewport = renderer === null || renderer === void 0 ? void 0 : renderer.scene.getViewport("viewMain");
		if (!viewport) return;
		if (menu.id === TITLE_ID) {
			viewport.scrollToViewportPos({ viewportScrollY: 0 });
			return;
		}
		const paragraph = paragraphs.find((p) => {
			var _p$paragraphStyle3;
			return ((_p$paragraphStyle3 = p.paragraphStyle) === null || _p$paragraphStyle3 === void 0 ? void 0 : _p$paragraphStyle3.headingId) === menu.id;
		});
		if (!paragraph) return;
		const bound = paragraphBounds === null || paragraphBounds === void 0 ? void 0 : paragraphBounds.get(paragraph.startIndex);
		if (!bound) return;
		viewport.scrollToViewportPos({ viewportScrollY: bound.rect.top });
		setActiveId(menu.id);
	});
	if (!currentDoc || (0, _univerjs_core.isInternalEditorID)(unitId) || !(menus === null || menus === void 0 ? void 0 : menus.length)) return null;
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
		className: "univer-absolute univer-bottom-0 univer-left-0 univer-top-0 univer-z-[100] univer-w-[0px]",
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SideMenu, {
			menus,
			open,
			onOpenChange: setOpen,
			mode,
			maxWidth: mode === "float" ? void 0 : Math.floor(left) - 10,
			wrapperClass: "univer-mt-12",
			activeId,
			onClick: handleClick,
			maxHeight: canvasHeight - 48
		})
	});
}

//#endregion
//#region src/controllers/ui.controller.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
let DocUIController = class DocUIController extends _univerjs_core.Disposable {
	constructor(_injector, _commandService, _layoutService, _menuManagerService, _uiPartsService, _univerInstanceService, _shortcutService, _configService, _embedInteractionBoundaryService, _embedRuntimeFocusCoordinator) {
		super();
		this._injector = _injector;
		this._commandService = _commandService;
		this._layoutService = _layoutService;
		this._menuManagerService = _menuManagerService;
		this._uiPartsService = _uiPartsService;
		this._univerInstanceService = _univerInstanceService;
		this._shortcutService = _shortcutService;
		this._configService = _configService;
		this._embedRuntimeFocusCoordinator = _embedRuntimeFocusCoordinator;
		this._init();
	}
	_initUiParts() {
		this.disposeWithMe(this._uiPartsService.registerComponent(_univerjs_ui.BuiltInUIPart.FOOTER, () => (0, _univerjs_ui.connectInjector)(DocFooter, this._injector)));
		this.disposeWithMe(this._uiPartsService.registerComponent(_univerjs_ui.BuiltInUIPart.CONTENT, () => (0, _univerjs_ui.connectInjector)(DocSideMenu, this._injector)));
	}
	_initMenus() {
		this._menuManagerService.appendRootMenu(floatToolbarMenuSchema);
		this._menuManagerService.mergeMenu(menuSchema);
	}
	_initShortCut() {
		[
			BoldShortCut,
			ItalicShortCut,
			UnderlineShortCut,
			StrikeThroughShortCut,
			SubscriptShortCut,
			SuperscriptShortCut,
			AlignCenterShortCut,
			AlignJustifyShortCut,
			AlignRightShortCut,
			AlignLeftShortCut,
			OrderListShortCut,
			BulletListShortCut,
			TabShortCut
		].forEach((shortcut) => {
			this.disposeWithMe(this._shortcutService.registerShortcut(shortcut));
		});
	}
	_init() {
		this._initMenus();
		this._initFocusHandler();
		this._initCommands();
		this._initUiParts();
		this._initShortCut();
	}
	_initCommands() {
		[
			OpenDocPermissionPanelOperation,
			OpenDocParagraphPermissionOperation,
			CoreHeaderFooterCommand,
			OpenHeaderFooterPanelCommand,
			SidebarDocHeaderFooterPanelOperation
		].forEach((command) => this.disposeWithMe(this._commandService.registerCommand(command)));
	}
	_initFocusHandler() {
		this.disposeWithMe(this._layoutService.registerFocusHandler(_univerjs_core.UniverInstanceType.UNIVER_DOC, (unitId) => {
			if (this._shouldPreserveEmbedFocus(unitId)) return;
			const renderUnit = this._injector.get(_univerjs_engine_render.IRenderManagerService).getRenderUnitById(unitId);
			if (!renderUnit) return;
			renderUnit.with(DocSelectionRenderService).focus();
		}));
	}
	_shouldPreserveEmbedFocus(unitId) {
		var _this$_embedRuntimeFo;
		if ((_this$_embedRuntimeFo = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo === void 0 ? void 0 : _this$_embedRuntimeFo.shouldSuppressHostInteraction(unitId)) return true;
		return false;
	}
};
DocUIController = __decorate([
	__decorateParam(0, (0, _univerjs_core.Inject)(_univerjs_core.Injector)),
	__decorateParam(1, _univerjs_core.ICommandService),
	__decorateParam(2, _univerjs_ui.ILayoutService),
	__decorateParam(3, _univerjs_ui.IMenuManagerService),
	__decorateParam(4, _univerjs_ui.IUIPartsService),
	__decorateParam(5, _univerjs_core.IUniverInstanceService),
	__decorateParam(6, _univerjs_ui.IShortcutService),
	__decorateParam(7, _univerjs_core.IConfigService),
	__decorateParam(8, (0, _univerjs_core.Optional)(IDocEmbedInteractionBoundaryService)),
	__decorateParam(9, (0, _univerjs_core.Optional)(IDocEmbedRuntimeFocusCoordinator))
], DocUIController);

//#endregion
//#region src/embed-docs-custom-block-bleed.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const DOCS_CUSTOM_BLOCK_VIEWPORT_INSET = 10;
function createDefaultDocsTableLikeCustomBlockBleedViewport() {
	if (typeof window === "undefined") return {
		bleedLeft: 0,
		bleedRight: 0,
		bleedWidth: 1,
		contentWidth: 1,
		virtualWidth: 1
	};
	const bleedWidth = Math.max(1, window.innerWidth - DOCS_CUSTOM_BLOCK_VIEWPORT_INSET * 2);
	return {
		bleedLeft: 0,
		bleedRight: 0,
		bleedWidth,
		contentWidth: 1,
		virtualWidth: bleedWidth
	};
}
function resolveDocsTableLikeCustomBlockBleedViewport(root, contentWidth, hint) {
	const rootRect = root.getBoundingClientRect();
	const hintedViewScale = hint === null || hint === void 0 ? void 0 : hint.viewScale;
	const viewScale = typeof hintedViewScale === "number" && Number.isFinite(hintedViewScale) && hintedViewScale > 0 ? hintedViewScale : 1;
	const rootWidth = Math.max(1, rootRect.width / viewScale);
	const normalizedContentWidth = Math.max(1, contentWidth);
	const hintedBleedLeft = hint === null || hint === void 0 ? void 0 : hint.bleedLeft;
	const hintedBleedWidth = hint === null || hint === void 0 ? void 0 : hint.bleedWidth;
	const hasBleedHint = Number.isFinite(hintedBleedWidth) && (hintedBleedWidth ?? 0) > 0;
	if (normalizedContentWidth <= rootWidth && ((hint === null || hint === void 0 ? void 0 : hint.authoritativeContentWidth) || !hasBleedHint)) return {
		bleedLeft: 0,
		bleedRight: 0,
		bleedWidth: rootWidth,
		contentWidth: normalizedContentWidth,
		virtualWidth: rootWidth
	};
	if (hasBleedHint) {
		const bleedLeft = Math.max(0, hintedBleedLeft ?? 0);
		const bleedWidth = Math.max(1, hintedBleedWidth);
		return {
			bleedLeft,
			bleedRight: Math.max(0, bleedWidth - bleedLeft - rootWidth),
			bleedWidth,
			contentWidth: normalizedContentWidth,
			virtualWidth: Math.max(bleedWidth, bleedLeft + normalizedContentWidth)
		};
	}
	const bounds = resolveDocsTableLikeCustomBlockBleedBounds(root);
	const viewportLeft = bounds.left + DOCS_CUSTOM_BLOCK_VIEWPORT_INSET;
	const viewportWidth = Math.max(1, bounds.width - DOCS_CUSTOM_BLOCK_VIEWPORT_INSET * 2);
	const bleedLeft = Math.max(0, (rootRect.left - viewportLeft) / viewScale);
	const bleedRight = Math.max(0, (viewportLeft + viewportWidth - rootRect.right) / viewScale);
	const logicalViewportWidth = viewportWidth / viewScale;
	return {
		bleedLeft,
		bleedRight,
		bleedWidth: logicalViewportWidth,
		contentWidth: normalizedContentWidth,
		virtualWidth: Math.max(logicalViewportWidth, bleedLeft + normalizedContentWidth)
	};
}
function resolveDocsTableLikeCustomBlockContentWidth(authoritativeContentWidth, fallbackContentWidth) {
	return Number.isFinite(authoritativeContentWidth) && (authoritativeContentWidth ?? 0) > 0 ? authoritativeContentWidth : Math.max(1, fallbackContentWidth);
}
function resolveDocsTableLikeCustomBlockContentHeight(authoritativeContentHeight, fallbackContentHeight) {
	return Number.isFinite(authoritativeContentHeight) && (authoritativeContentHeight ?? 0) > 0 ? authoritativeContentHeight : Math.max(1, fallbackContentHeight);
}
function resolveDocsTableLikeCustomBlockBleedBounds(root) {
	const clippingAncestor = findClippingAncestor(root);
	if (clippingAncestor) {
		const rect = clippingAncestor.getBoundingClientRect();
		if (Number.isFinite(rect.width) && rect.width > 0) return {
			left: rect.left,
			width: rect.width
		};
	}
	return {
		left: 0,
		width: typeof window === "undefined" ? 1 : Math.max(1, window.innerWidth)
	};
}
function findClippingAncestor(root) {
	let current = root.parentElement;
	while (current && current !== document.body && current !== document.documentElement) {
		if (clipsOverflow(current)) return current;
		current = current.parentElement;
	}
	return null;
}
function clipsOverflow(element) {
	const style = window.getComputedStyle(element);
	return hasClippingOverflow(style.overflow) || hasClippingOverflow(style.overflowX) || hasClippingOverflow(style.overflowY);
}
function hasClippingOverflow(value) {
	return value === "hidden" || value === "auto" || value === "scroll" || value === "clip";
}

//#endregion
//#region src/embed-docs-custom-block-refresh.ts
function collectDocsTableLikeEmbedChildUnitIds(drawings, resolveChildUnitId) {
	const childUnitIds = /* @__PURE__ */ new Set();
	Object.values(drawings ?? {}).forEach((drawing) => {
		const data = getDrawingData(drawing);
		const childUnitId = data ? (resolveChildUnitId === null || resolveChildUnitId === void 0 ? void 0 : resolveChildUnitId(data)) ?? (typeof data.childUnitId === "string" ? data.childUnitId : void 0) : void 0;
		const childType = typeof (data === null || data === void 0 ? void 0 : data.childType) === "number" ? data.childType : void 0;
		if (!childUnitId || !(0, _univerjs_docs.isSheetLikeDocsCustomBlockChildType)(childType)) return;
		childUnitIds.add(childUnitId);
	});
	return childUnitIds;
}
function getDrawingData(drawing) {
	if (!drawing || typeof drawing !== "object") return;
	const data = drawing.data;
	return data && typeof data === "object" ? data : void 0;
}
function getCommandUnitId(commandParams) {
	if (!commandParams || typeof commandParams !== "object") return;
	const params = commandParams;
	if (typeof params.unitId === "string") return params.unitId;
	return typeof params.unitID === "string" ? params.unitID : void 0;
}
function shouldRefreshDocsCustomBlockSizeForCommand(params) {
	if (params.commandId && isDocsCustomBlockLayoutNeutralCommand(params.commandId)) return false;
	const commandUnitId = getCommandUnitId(params.commandParams);
	return Boolean(commandUnitId && commandUnitId !== params.hostUnitId && params.childUnitIds.has(commandUnitId));
}
const DOCS_CUSTOM_BLOCK_LAYOUT_NEUTRAL_COMMAND_IDS = /* @__PURE__ */ new Set([
	"sheet.command.expand-selection",
	"sheet.command.move-selection",
	"sheet.operation.scroll-to-cell",
	"sheet.operation.scroll-to-range",
	"sheet.operation.set-activate-cell-edit",
	"sheet.operation.set-cell-edit-visible",
	"sheet.operation.set-cell-edit-visible-arrow",
	"sheet.operation.set-cell-edit-visible-f2",
	"sheet.operation.set-format-painter",
	"sheet.operation.set-scroll",
	"sheet.operation.set-selections",
	"sheet.operation.set-zoom-ratio"
]);
function isDocsCustomBlockLayoutNeutralCommand(commandId) {
	return DOCS_CUSTOM_BLOCK_LAYOUT_NEUTRAL_COMMAND_IDS.has(commandId);
}
function createDocsCustomBlockSizeRefreshScheduler(refresh) {
	let pendingFrame;
	return {
		dispose: () => {
			if (pendingFrame == null) return;
			window.cancelAnimationFrame(pendingFrame);
			pendingFrame = void 0;
		},
		schedule: () => {
			if (pendingFrame != null) return;
			pendingFrame = window.requestAnimationFrame(() => {
				pendingFrame = void 0;
				refresh();
			});
		}
	};
}

//#endregion
//#region src/embed-docs-custom-block-scroll.ts
function scrollDocsTableLikeCustomBlockLive(event, live, options = {}) {
	if (event.defaultPrevented || event.ctrlKey || event.metaKey) return false;
	const deltaX = event.deltaX || (event.shiftKey ? event.deltaY : 0);
	const deltaY = event.shiftKey ? 0 : event.deltaY;
	const previousLeft = live.scrollLeft;
	const previousTop = live.scrollTop;
	const maxScrollLeft = resolveMaxScrollLeft(live, options.maxScrollLeft);
	if (deltaX && canScrollX(live, deltaX, maxScrollLeft)) live.scrollLeft = clampScroll(previousLeft + deltaX, 0, maxScrollLeft);
	if (deltaY && canScrollY(live, deltaY)) live.scrollTop = clampScroll(previousTop + deltaY, 0, live.scrollHeight - live.clientHeight);
	return live.scrollLeft !== previousLeft || live.scrollTop !== previousTop;
}
function canScrollX(element, deltaX, maxScrollLeft) {
	return element.scrollWidth > element.clientWidth && (deltaX < 0 ? element.scrollLeft > 0 : element.scrollLeft < maxScrollLeft);
}
function canScrollY(element, deltaY) {
	return element.scrollHeight > element.clientHeight && (deltaY < 0 ? element.scrollTop > 0 : element.scrollTop + element.clientHeight < element.scrollHeight);
}
function clampScroll(value, min, max) {
	return Math.max(min, Math.min(max, value));
}
function resolveMaxScrollLeft(element, requestedMaxScrollLeft) {
	const nativeMaxScrollLeft = Math.max(0, element.scrollWidth - element.clientWidth);
	if (!Number.isFinite(requestedMaxScrollLeft)) return nativeMaxScrollLeft;
	return clampScroll(requestedMaxScrollLeft ?? nativeMaxScrollLeft, 0, nativeMaxScrollLeft);
}

//#endregion
//#region src/embed-host-anchor.ts
const MODERN_DOCS_CUSTOM_BLOCK_VIEWPORT_INSET = 10;
function resolveDocsCustomBlockRenderViewport(params) {
	const defaultSize = (0, _univerjs_docs.resolveDocsCustomBlockSize)(params.childType);
	const fallbackWidth = params.fallbackWidth ?? defaultSize.width;
	const fallbackHeight = params.fallbackHeight ?? defaultSize.height;
	const sheetLike = (0, _univerjs_docs.isSheetLikeDocsCustomBlockChildType)(params.childType);
	const contentHeight = Number.isFinite(params.contentHeight) && (params.contentHeight ?? 0) > 0 ? params.contentHeight : fallbackHeight;
	const visibleCanvasHeight = Number.isFinite(params.visibleCanvasHeight) && (params.visibleCanvasHeight ?? 0) > 0 ? params.visibleCanvasHeight : void 0;
	const height = sheetLike ? contentHeight : fallbackHeight;
	const viewportHeight = sheetLike && visibleCanvasHeight != null ? Math.min(contentHeight, visibleCanvasHeight) : height;
	const viewScale = params.scale && params.scale > 0 ? params.scale : 1;
	if (!sheetLike) return {
		height,
		width: fallbackWidth
	};
	const pageWidth = params.pageWidth;
	const pageMarginLeft = params.pageMarginLeft ?? 0;
	const pageMarginRight = params.pageMarginRight ?? 0;
	const pageContentWidth = Number.isFinite(pageWidth) ? Math.max(0, pageWidth - pageMarginLeft - pageMarginRight) : fallbackWidth;
	const contentWidth = Number.isFinite(params.contentWidth) && (params.contentWidth ?? 0) > 0 ? params.contentWidth : fallbackWidth;
	if (params.documentFlavor !== _univerjs_core.DocumentFlavor.MODERN || !Number.isFinite(pageWidth)) {
		const layoutWidth = Math.min(contentWidth, pageContentWidth || contentWidth);
		return {
			contentHeight,
			contentWidth,
			height,
			layoutWidth,
			offsetLeft: 0,
			pageContentWidth,
			viewScale,
			visibleCanvasHeight,
			viewportHeight,
			width: layoutWidth
		};
	}
	const inset = MODERN_DOCS_CUSTOM_BLOCK_VIEWPORT_INSET / viewScale;
	const docsLeft = params.docsLeft ?? 0;
	const fallbackViewportLeft = docsLeft + inset;
	const fallbackViewportWidth = Math.max(0, pageWidth - inset * 2);
	const hasVisibleCanvas = Number.isFinite(params.visibleCanvasLeft) && Number.isFinite(params.visibleCanvasWidth) && (params.visibleCanvasWidth ?? 0) > 0;
	const viewportLeft = hasVisibleCanvas ? params.visibleCanvasLeft + inset : fallbackViewportLeft;
	const viewportWidth = hasVisibleCanvas ? Math.max(0, params.visibleCanvasWidth - inset * 2) : fallbackViewportWidth;
	const paragraphTextStart = docsLeft + pageMarginLeft;
	const leadingInsetLeft = Math.max(0, paragraphTextStart - viewportLeft);
	const layoutWidth = Math.min(contentWidth, pageContentWidth || contentWidth);
	return {
		bleedLeft: leadingInsetLeft,
		bleedWidth: viewportWidth,
		contentHeight,
		contentWidth,
		height,
		layoutWidth,
		offsetLeft: 0,
		pageContentWidth,
		viewScale,
		visibleCanvasHeight,
		viewportHeight,
		width: layoutWidth
	};
}

//#endregion
//#region package.json
var name = "@univerjs/docs-ui";
var version = "1.0.0-insiders.20260907-70fc579";

//#endregion
//#region src/commands/commands/doc-paragraph-setting.command.ts
const DocParagraphSettingCommand = {
	id: "doc-paragraph-setting.command",
	type: _univerjs_core.CommandType.COMMAND,
	handler: async (accessor, config) => {
		const docSelectionManagerService = accessor.get(_univerjs_docs.DocSelectionManagerService);
		const univerInstanceService = accessor.get(_univerjs_core.IUniverInstanceService);
		const commandService = accessor.get(_univerjs_core.ICommandService);
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const docRanges = docSelectionManagerService.getDocRanges();
		if (!docDataModel || docRanges.length === 0 || !config) return false;
		const segmentId = docRanges[0].segmentId;
		const unitId = docDataModel.getUnitId();
		const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
		const segmentBody = segment === null || segment === void 0 ? void 0 : segment.getBody();
		const allParagraphs = (segmentBody === null || segmentBody === void 0 ? void 0 : segmentBody.paragraphs) ?? [];
		const dataStream = (segmentBody === null || segmentBody === void 0 ? void 0 : segmentBody.dataStream) ?? "";
		const paragraphs = _univerjs_core.BuildTextUtils.range.getParagraphsInRanges(docRanges, allParagraphs, dataStream) ?? [];
		const doMutation = {
			id: _univerjs_docs.RichTextEditingMutation.id,
			params: {
				unitId,
				historyAction: _univerjs_docs.DocHistoryAction.FormatParagraph,
				actions: [],
				textRanges: docRanges
			}
		};
		if (!config.paragraph || Object.keys(config.paragraph).length === 0) return false;
		const memoryCursor = new _univerjs_core.MemoryCursor();
		memoryCursor.reset();
		const textX = new _univerjs_core.TextX();
		const jsonX = _univerjs_core.JSONX.getInstance();
		for (const paragraph of paragraphs) {
			const { startIndex } = paragraph;
			textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len: startIndex - memoryCursor.cursor
			});
			textX.push({
				t: _univerjs_core.TextXActionType.RETAIN,
				len: 1,
				body: {
					dataStream: "",
					paragraphs: [{
						...paragraph,
						paragraphStyle: {
							...paragraph.paragraphStyle,
							...config.paragraph
						},
						startIndex: 0
					}]
				},
				coverType: _univerjs_core.UpdateDocsAttributeType.REPLACE
			});
			memoryCursor.moveCursorTo(startIndex + 1);
		}
		const path = (0, _univerjs_core.getRichTextEditPath)(docDataModel, segmentId);
		doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
		return !!commandService.syncExecuteCommand(doMutation.id, doMutation.params);
	}
};

//#endregion
//#region src/views/DocLayoutRecovery.tsx
const DOC_LAYOUT_RECOVERY_COMPONENT = "DOC_LAYOUT_RECOVERY_COMPONENT";
function DocLayoutRecovery() {
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		"aria-live": "polite",
		className: "univer-flex univer-items-center univer-gap-2 univer-rounded-lg univer-bg-white univer-px-4 univer-py-3 univer-text-sm univer-text-gray-700 univer-shadow-lg dark:univer-bg-gray-800 dark:univer-text-gray-200",
		role: "status",
		children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.LoadingMultiIcon, {
			"aria-hidden": true,
			className: "univer-size-5 univer-animate-spin univer-text-violet-500"
		}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { children: localeService.t("docs-ui.layout.recovering") })]
	});
}

//#endregion
//#region src/views/float-toolbar/FloatToolbar.tsx
const FLOAT_MENU_COMPONENT_KEY = "univer.doc.float-menu";
const DEFAULT_AVALIABLE_MENUS = [
	FLOAT_TEXT_STYLE_MENU_ID,
	SetInlineFormatFontSizeCommand.id,
	SetInlineFormatBoldCommand.id,
	SetInlineFormatItalicCommand.id,
	SetInlineFormatUnderlineCommand.id,
	SetInlineFormatStrikethroughCommand.id,
	SetInlineFormatSubscriptCommand.id,
	SetInlineFormatSuperscriptCommand.id,
	SetInlineFormatTextColorCommand.id,
	{
		id: SetInlineFormatTextBackgroundColorCommand.id,
		iconColor: "var(--univer-primary-600)"
	}
];
function resolveFloatToolbarMenus(menuManagerService, avaliableMenus) {
	const floatToolbarMenus = menuManagerService.getMenuByPositionKey(FLOAT_TOOLBAR_MENU_POSITION);
	const flatMenus = [...menuManagerService.getFlatMenuByPositionKey(FLOAT_TOOLBAR_MENU_POSITION), ...menuManagerService.getFlatMenuByPositionKey(_univerjs_ui.MenuManagerPosition.RIBBON)];
	const menus = [];
	const menuIds = avaliableMenus.map((config) => typeof config === "string" ? config : config.id);
	for (const config of avaliableMenus) {
		const menuId = typeof config === "string" ? config : config.id;
		const item = flatMenus.find((item) => item.key === menuId);
		if (item) menus.push(typeof config === "string" ? item : {
			...item,
			iconColor: config.iconColor
		});
	}
	return {
		menus,
		extraMenus: floatToolbarMenus.filter((item) => item.item && !menuIds.includes(item.key))
	};
}
function FloatToolbar(props) {
	var _props$popup;
	const { avaliableMenus = DEFAULT_AVALIABLE_MENUS } = props;
	const onDismiss = (_props$popup = props.popup) === null || _props$popup === void 0 || (_props$popup = _props$popup.extraProps) === null || _props$popup === void 0 ? void 0 : _props$popup.onDismiss;
	const menuManagerService = (0, _univerjs_ui.useDependency)(_univerjs_ui.IMenuManagerService);
	const toolbarRef = (0, react.useRef)(null);
	const hoveredRef = (0, react.useRef)(false);
	const dismissedRef = (0, react.useRef)(false);
	const [menus, setMenus] = (0, react.useState)([]);
	const [extraMenus, setExtraMenus] = (0, react.useState)([]);
	(0, react.useEffect)(() => {
		function getRibbon() {
			const { menus, extraMenus } = resolveFloatToolbarMenus(menuManagerService, avaliableMenus);
			setMenus(menus);
			setExtraMenus(extraMenus);
		}
		getRibbon();
		const subscription = menuManagerService.menuChanged$.subscribe(getRibbon);
		return () => {
			subscription.unsubscribe();
		};
	}, [avaliableMenus, menuManagerService]);
	(0, react.useEffect)(() => {
		const toolbar = toolbarRef.current;
		if (!toolbar) return;
		return (0, _univerjs_ui.preventBrowserZoomInContainers)([toolbar]);
	}, []);
	(0, react.useEffect)(() => {
		const toolbar = toolbarRef.current;
		if (!toolbar || !onDismiss) return;
		const onPointerMove = (event) => {
			if (hoveredRef.current || dismissedRef.current || event.pointerType === "touch") return;
			const rect = toolbar.getBoundingClientRect();
			const distance = Math.max(rect.left - event.clientX, event.clientX - rect.right, rect.top - event.clientY, event.clientY - rect.bottom, 0);
			const opacity = Math.max(0, 1 - Math.max(0, distance - 10) / 80);
			toolbar.style.opacity = String(opacity);
			if (opacity === 0) {
				dismissedRef.current = true;
				onDismiss();
			}
		};
		toolbar.ownerDocument.addEventListener("pointermove", onPointerMove);
		return () => toolbar.ownerDocument.removeEventListener("pointermove", onPointerMove);
	}, [onDismiss]);
	const retainToolbar = () => {
		hoveredRef.current = true;
		if (toolbarRef.current) toolbarRef.current.style.opacity = "1";
	};
	const selectors = menus.filter((menu) => menu.key === "doc.menu.float-text-style" || menu.key === SetInlineFormatFontSizeCommand.id);
	const formatting = menus.filter((menu) => !selectors.includes(menu));
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		ref: toolbarRef,
		"data-doc-float-toolbar": true,
		onPointerEnter: retainToolbar,
		onFocusCapture: retainToolbar,
		className: (0, _univerjs_design.clsx)("univer-box-border univer-flex univer-flex-col univer-gap-1 univer-rounded univer-bg-gray-0 univer-p-1 univer-shadow-sm dark:!univer-border-gray-700 dark:!univer-bg-gray-900", _univerjs_design.borderClassName),
		children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			className: "univer-flex univer-items-center univer-gap-1 [&_.univer-toolbar-selector-root]:univer-text-xs",
			children: [
				selectors.map((menu) => menu.item && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: menu.key === "doc.menu.float-text-style" ? `
                              univer-min-w-0 univer-flex-1
                              [&_.univer-toolbar-selector-root]:univer-box-border
                              [&_.univer-toolbar-selector-root]:univer-w-full
                              [&_.univer-toolbar-selector-root]:univer-justify-between
                            ` : "univer-shrink-0",
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_ui.ToolbarItem, {
						...menu.item,
						iconSize: 14,
						iconColor: menu.iconColor
					})
				}, menu.key)),
				selectors.length > 0 && extraMenus.length > 0 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", { className: "univer-mx-0.5 univer-h-4 univer-w-px univer-bg-gray-200 dark:univer-bg-gray-700" }),
				extraMenus.map((menu) => menu.item && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_ui.ToolbarItem, {
					...menu.item,
					iconSize: 14
				}, menu.key))
			]
		}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
			className: "univer-flex univer-items-center univer-gap-0.5",
			children: formatting.map((menu) => menu.item && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_ui.ToolbarItem, {
				...menu.item,
				iconSize: 14,
				iconColor: menu.iconColor
			}, menu.key))
		})]
	});
}

//#endregion
//#region src/views/header-footer/panel/DocHeaderFooterOptions.tsx
function getSegmentId(documentStyle, editArea, pageIndex, page) {
	const { useFirstPageHeaderFooter, evenAndOddHeaders, defaultHeaderId, defaultFooterId, firstPageHeaderId, firstPageFooterId, evenPageHeaderId, evenPageFooterId } = documentStyle;
	const isFirstPage = page ? page.pageNumber === page.pageNumberStart : pageIndex === 0;
	const isEvenPage = page ? page.pageNumber % 2 === 0 : pageIndex % 2 === 1;
	if (editArea === _univerjs_engine_render.DocumentEditArea.HEADER) if (useFirstPageHeaderFooter === _univerjs_core.BooleanNumber.TRUE) if (isFirstPage) return firstPageHeaderId;
	else return evenAndOddHeaders === _univerjs_core.BooleanNumber.TRUE && isEvenPage ? evenPageHeaderId : defaultHeaderId;
	else return evenAndOddHeaders === _univerjs_core.BooleanNumber.TRUE && isEvenPage ? evenPageHeaderId : defaultHeaderId;
	else if (useFirstPageHeaderFooter === _univerjs_core.BooleanNumber.TRUE) if (isFirstPage) return firstPageFooterId;
	else return evenAndOddHeaders === _univerjs_core.BooleanNumber.TRUE && isEvenPage ? evenPageFooterId : defaultFooterId;
	else return evenAndOddHeaders === _univerjs_core.BooleanNumber.TRUE && isEvenPage ? evenPageFooterId : defaultFooterId;
}
const DocHeaderFooterOptions = (props) => {
	const docSelectionRenderService = (0, _univerjs_ui.useDependency)(_univerjs_engine_render.IRenderManagerService).getRenderUnitById(props.unitId).with(DocSelectionRenderService);
	const segmentContext = (0, _univerjs_ui.useObservable)(docSelectionRenderService.segmentContext$, {
		segmentId: docSelectionRenderService.getSegment(),
		segmentPage: docSelectionRenderService.getSegmentPage()
	});
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(DocHeaderFooterOptionsContent, { ...props }, `${(segmentContext === null || segmentContext === void 0 ? void 0 : segmentContext.segmentId) ?? ""}:${(segmentContext === null || segmentContext === void 0 ? void 0 : segmentContext.segmentPage) ?? -1}`);
};
function DocHeaderFooterOptionsContent(props) {
	var _renderManagerService2, _sectionContext$secti;
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const univerInstanceService = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService);
	const renderManagerService = (0, _univerjs_ui.useDependency)(_univerjs_engine_render.IRenderManagerService);
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const layoutService = (0, _univerjs_ui.useDependency)(_univerjs_ui.ILayoutService);
	const { unitId } = props;
	const docSelectionRenderService = renderManagerService.getRenderUnitById(unitId).with(DocSelectionRenderService);
	const docDataModel = univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
	const getCurrentSectionContext = () => {
		var _renderManagerService, _docSkeletonManagerSe;
		const snapshot = docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getSnapshot();
		const docSkeletonManagerService = (_renderManagerService = renderManagerService.getRenderUnitById(unitId)) === null || _renderManagerService === void 0 ? void 0 : _renderManagerService.with(_univerjs_docs.DocSkeletonManagerService);
		const page = docSkeletonManagerService === null || docSkeletonManagerService === void 0 || (_docSkeletonManagerSe = docSkeletonManagerService.getSkeleton) === null || _docSkeletonManagerSe === void 0 || (_docSkeletonManagerSe = _docSkeletonManagerSe.call(docSkeletonManagerService)) === null || _docSkeletonManagerSe === void 0 || (_docSkeletonManagerSe = _docSkeletonManagerSe.getSkeletonData()) === null || _docSkeletonManagerSe === void 0 ? void 0 : _docSkeletonManagerSe.pages[docSelectionRenderService.getSegmentPage()];
		return snapshot == null ? void 0 : {
			...getDocPageSectionContext(snapshot, page),
			page
		};
	};
	const [options, setOptions] = (0, react.useState)(() => {
		var _getCurrentSectionCon;
		const config = (_getCurrentSectionCon = getCurrentSectionContext()) === null || _getCurrentSectionCon === void 0 ? void 0 : _getCurrentSectionCon.config;
		return {
			marginHeader: (config === null || config === void 0 ? void 0 : config.marginHeader) ?? 0,
			marginFooter: (config === null || config === void 0 ? void 0 : config.marginFooter) ?? 0,
			useFirstPageHeaderFooter: (config === null || config === void 0 ? void 0 : config.useFirstPageHeaderFooter) ?? _univerjs_core.BooleanNumber.FALSE,
			evenAndOddHeaders: (config === null || config === void 0 ? void 0 : config.evenAndOddHeaders) ?? _univerjs_core.BooleanNumber.FALSE
		};
	});
	const sectionContext = getCurrentSectionContext();
	const referenceKey = (0, _univerjs_core.getSectionHeaderFooterReferenceKey)(((_renderManagerService2 = renderManagerService.getRenderUnitById(unitId)) === null || _renderManagerService2 === void 0 || (_renderManagerService2 = _renderManagerService2.with(_univerjs_docs.DocSkeletonManagerService)) === null || _renderManagerService2 === void 0 || (_renderManagerService2 = _renderManagerService2.getViewModel()) === null || _renderManagerService2 === void 0 ? void 0 : _renderManagerService2.getEditArea()) === _univerjs_engine_render.DocumentEditArea.FOOTER ? "footer" : "header", getHeaderFooterVariant((sectionContext === null || sectionContext === void 0 ? void 0 : sectionContext.config) ?? {}, docSelectionRenderService.getSegmentPage(), sectionContext === null || sectionContext === void 0 ? void 0 : sectionContext.page));
	const canLinkToPrevious = ((sectionContext === null || sectionContext === void 0 ? void 0 : sectionContext.sectionIndex) ?? -1) > 0;
	const [linkedToPrevious, setLinkedToPrevious] = (0, react.useState)(canLinkToPrevious && !(sectionContext === null || sectionContext === void 0 || (_sectionContext$secti = sectionContext.section) === null || _sectionContext$secti === void 0 ? void 0 : _sectionContext$secti[referenceKey]));
	const handleCheckboxChange = (val, type) => {
		var _renderManagerService3;
		setOptions((prev) => ({
			...prev,
			[type]: val ? _univerjs_core.BooleanNumber.TRUE : _univerjs_core.BooleanNumber.FALSE
		}));
		const sectionContext = getCurrentSectionContext();
		const documentStyle = sectionContext === null || sectionContext === void 0 ? void 0 : sectionContext.config;
		const docSkeletonManagerService = (_renderManagerService3 = renderManagerService.getRenderUnitById(unitId)) === null || _renderManagerService3 === void 0 ? void 0 : _renderManagerService3.with(_univerjs_docs.DocSkeletonManagerService);
		const viewModel = docSkeletonManagerService === null || docSkeletonManagerService === void 0 ? void 0 : docSkeletonManagerService.getViewModel();
		if (documentStyle == null || viewModel == null) return;
		const editArea = viewModel.getEditArea();
		let needCreateHeaderFooter = false;
		const segmentPage = docSelectionRenderService.getSegmentPage();
		const isFirstSectionPage = (sectionContext === null || sectionContext === void 0 ? void 0 : sectionContext.page) ? sectionContext.page.pageNumber === sectionContext.page.pageNumberStart : segmentPage === 0;
		const isEvenPage = (sectionContext === null || sectionContext === void 0 ? void 0 : sectionContext.page) ? sectionContext.page.pageNumber % 2 === 0 : segmentPage % 2 === 1;
		let needChangeSegmentId = false;
		if (type === "useFirstPageHeaderFooter" && val === true) {
			if (editArea === _univerjs_engine_render.DocumentEditArea.HEADER && !documentStyle.firstPageHeaderId) needCreateHeaderFooter = true;
			else if (editArea === _univerjs_engine_render.DocumentEditArea.FOOTER && !documentStyle.firstPageFooterId) needCreateHeaderFooter = true;
			if (needCreateHeaderFooter && isFirstSectionPage) needChangeSegmentId = true;
		}
		if (type === "evenAndOddHeaders" && val === true) {
			if (editArea === _univerjs_engine_render.DocumentEditArea.HEADER && !documentStyle.evenPageHeaderId) needCreateHeaderFooter = true;
			else if (editArea === _univerjs_engine_render.DocumentEditArea.FOOTER && !documentStyle.evenPageFooterId) needCreateHeaderFooter = true;
			if (needCreateHeaderFooter && isEvenPage) needChangeSegmentId = true;
		}
		if (needCreateHeaderFooter) {
			const segmentId = (0, _univerjs_core.generateRandomId)(6);
			if (needChangeSegmentId) docSelectionRenderService.setSegment(segmentId);
			commandService.executeCommand(CoreHeaderFooterCommandId, {
				unitId,
				segmentId,
				headerFooterProps: { [type]: val ? _univerjs_core.BooleanNumber.TRUE : _univerjs_core.BooleanNumber.FALSE },
				sectionId: sectionContext === null || sectionContext === void 0 ? void 0 : sectionContext.sectionId
			});
		} else {
			const segmentPageIndex = docSelectionRenderService.getSegmentPage();
			const prevSegmentId = docSelectionRenderService.getSegment();
			const needFocusSegmentId = getSegmentId({
				...documentStyle,
				[type]: val ? _univerjs_core.BooleanNumber.TRUE : _univerjs_core.BooleanNumber.FALSE
			}, editArea, segmentPageIndex, sectionContext === null || sectionContext === void 0 ? void 0 : sectionContext.page);
			if (needFocusSegmentId && needFocusSegmentId !== prevSegmentId) docSelectionRenderService.setSegment(needFocusSegmentId);
			commandService.executeCommand(CoreHeaderFooterCommandId, {
				unitId,
				headerFooterProps: { [type]: val ? _univerjs_core.BooleanNumber.TRUE : _univerjs_core.BooleanNumber.FALSE },
				sectionId: sectionContext === null || sectionContext === void 0 ? void 0 : sectionContext.sectionId
			});
		}
		layoutService.focus();
	};
	const handleMarginChange = async (val, type) => {
		var _getCurrentSectionCon2;
		setOptions((prev) => ({
			...prev,
			[type]: val
		}));
		await commandService.executeCommand(CoreHeaderFooterCommandId, {
			unitId,
			headerFooterProps: { [type]: val },
			sectionId: (_getCurrentSectionCon2 = getCurrentSectionContext()) === null || _getCurrentSectionCon2 === void 0 ? void 0 : _getCurrentSectionCon2.sectionId
		});
		docSelectionRenderService.removeAllRanges();
		docSelectionRenderService.blur();
	};
	const closeHeaderFooter = () => {
		commandService.executeCommand(CloseHeaderFooterCommand.id, { unitId });
	};
	const handleLinkToPreviousChange = async (linked) => {
		var _renderManagerService4;
		const context = getCurrentSectionContext();
		if (!(context === null || context === void 0 ? void 0 : context.sectionId) || context.sectionIndex <= 0 || !docDataModel) return;
		const kind = ((_renderManagerService4 = renderManagerService.getRenderUnitById(unitId)) === null || _renderManagerService4 === void 0 || (_renderManagerService4 = _renderManagerService4.with(_univerjs_docs.DocSkeletonManagerService)) === null || _renderManagerService4 === void 0 || (_renderManagerService4 = _renderManagerService4.getViewModel()) === null || _renderManagerService4 === void 0 ? void 0 : _renderManagerService4.getEditArea()) === _univerjs_engine_render.DocumentEditArea.FOOTER ? "footer" : "header";
		const currentVariant = getHeaderFooterVariant(context.config, docSelectionRenderService.getSegmentPage(), context.page);
		const key = (0, _univerjs_core.getSectionHeaderFooterReferenceKey)(kind, currentVariant);
		const segmentId = (0, _univerjs_core.generateRandomId)(6);
		const previousSegmentId = (0, _univerjs_core.resolveSectionHeaderFooterReference)(docDataModel.getDocumentStyle(), context.sections, context.sectionIndex - 1, key).segmentId;
		if (!await commandService.executeCommand(_univerjs_docs.SetSectionHeaderFooterLinkCommand.id, {
			unitId,
			sectionId: context.sectionId,
			kind,
			variant: currentVariant,
			linkedToPrevious: linked,
			...linked ? {} : { segmentId }
		})) return;
		setLinkedToPrevious(linked);
		docSelectionRenderService.setSegment(linked ? previousSegmentId ?? "" : segmentId);
		layoutService.focus();
	};
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		className: "univer-grid univer-gap-4",
		children: [
			canLinkToPrevious && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Checkbox, {
				checked: linkedToPrevious,
				onChange: (val) => {
					handleLinkToPreviousChange(val);
				},
				children: localeService.t("docs-ui.headerFooter.linkToPrevious")
			}) }),
			/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: "univer-grid univer-gap-2",
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Checkbox, {
					checked: options.useFirstPageHeaderFooter === _univerjs_core.BooleanNumber.TRUE,
					onChange: (val) => {
						handleCheckboxChange(val, "useFirstPageHeaderFooter");
					},
					children: localeService.t("docs-ui.headerFooter.firstPageCheckBox")
				}) }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Checkbox, {
					checked: options.evenAndOddHeaders === _univerjs_core.BooleanNumber.TRUE,
					onChange: (val) => {
						handleCheckboxChange(val, "evenAndOddHeaders");
					},
					children: localeService.t("docs-ui.headerFooter.oddEvenCheckBox")
				}) })]
			}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: "univer-mb-1 univer-flex",
				children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { children: localeService.t("docs-ui.headerFooter.headerTopMargin") }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
					className: "univer-mt-1.5 univer-w-4/5",
					min: 0,
					max: 200,
					precision: 1,
					value: options.marginHeader,
					onChange: (val) => {
						handleMarginChange(val, "marginHeader");
					}
				})] }), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", { children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { children: localeService.t("docs-ui.headerFooter.footerBottomMargin") }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
					className: "univer-mt-1.5 univer-w-4/5",
					min: 0,
					max: 200,
					precision: 1,
					value: options.marginFooter,
					onChange: (val) => {
						handleMarginChange(val, "marginFooter");
					}
				})] })]
			}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				className: "univer-flex univer-justify-end",
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Button, {
					onClick: closeHeaderFooter,
					children: localeService.t("docs-ui.headerFooter.closeHeaderFooter")
				})
			})
		]
	});
}
function getHeaderFooterVariant(documentStyle, pageIndex, page) {
	const isFirstPage = page ? page.pageNumber === page.pageNumberStart : pageIndex === 0;
	const isEvenPage = page ? page.pageNumber % 2 === 0 : pageIndex % 2 === 1;
	if (documentStyle.useFirstPageHeaderFooter === _univerjs_core.BooleanNumber.TRUE && isFirstPage) return "first";
	if (documentStyle.evenAndOddHeaders === _univerjs_core.BooleanNumber.TRUE && isEvenPage) return "even";
	return "default";
}

//#endregion
//#region src/views/header-footer/panel/DocHeaderFooterPanel.tsx
const DocHeaderFooterPanel = () => {
	var _renderManagerService;
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const renderManagerService = (0, _univerjs_ui.useDependency)(_univerjs_engine_render.IRenderManagerService);
	const unitId = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService).getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC).getUnitId();
	const viewModel = ((_renderManagerService = renderManagerService.getRenderUnitById(unitId)) === null || _renderManagerService === void 0 ? void 0 : _renderManagerService.with(_univerjs_docs.DocSkeletonManagerService)).getViewModel();
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
		className: "univer-text-sm",
		children: (0, _univerjs_ui.useObservable)(() => viewModel.editAreaChange$.pipe((0, rxjs.filter)((editArea) => editArea != null), (0, rxjs.map)((editArea) => editArea !== _univerjs_engine_render.DocumentEditArea.BODY)), viewModel.getEditArea() !== _univerjs_engine_render.DocumentEditArea.BODY, false, [viewModel]) ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(DocHeaderFooterOptions, { unitId }) : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
			className: "univer-text-gray-400",
			children: localeService.t("docs-ui.headerFooter.disableText")
		})
	});
};

//#endregion
//#region src/views/Icon.tsx
function TitleTypeIcon({ className, style, onClick }) {
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("svg", {
		className,
		style,
		viewBox: "0 0 24 24",
		fill: "currentColor",
		"aria-hidden": "true",
		onClick,
		children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("text", {
			x: 2,
			y: 19,
			fontFamily: "Arial, sans-serif",
			fontSize: 19,
			fontWeight: 500,
			children: "T"
		}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("text", {
			x: 15,
			y: 19,
			fontFamily: "Arial, sans-serif",
			fontSize: 13,
			fontWeight: 500,
			children: "t"
		})]
	});
}
function SubtitleTypeIcon({ className, style, onClick }) {
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("svg", {
		className,
		style,
		viewBox: "0 0 24 24",
		fill: "currentColor",
		"aria-hidden": "true",
		onClick,
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("text", {
			x: 5,
			y: 19,
			fontFamily: "Arial, sans-serif",
			fontSize: 19,
			fontWeight: 500,
			children: "S"
		})
	});
}
function TextColorSwatchIcon({ className, style, onClick, color }) {
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("svg", {
		className,
		style,
		viewBox: "0 0 24 24",
		width: "1em",
		height: "1em",
		fill: "none",
		"aria-hidden": "true",
		onClick,
		children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("rect", {
			x: 1,
			y: 1,
			width: 22,
			height: 22,
			rx: 2.5,
			fill: "none",
			stroke: "currentColor",
			strokeWidth: 1,
			strokeOpacity: .12
		}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("text", {
			x: 12,
			y: 17.25,
			direction: "ltr",
			fontFamily: "Arial, sans-serif",
			fontSize: 15,
			fontWeight: 400,
			fill: color,
			textAnchor: "middle",
			children: "A"
		})]
	});
}
function BackgroundColorSwatchIcon({ className, style, onClick, color }) {
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("svg", {
		className,
		style,
		viewBox: "0 0 24 24",
		width: "1em",
		height: "1em",
		fill: "none",
		"aria-hidden": "true",
		onClick,
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("rect", {
			x: 3,
			y: 3,
			width: 18,
			height: 18,
			rx: 3,
			fill: color,
			stroke: "currentColor",
			strokeOpacity: .12
		})
	});
}
function HeaderTextColorIcon({ className, style, onClick, extend }) {
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(TextColorSwatchIcon, {
		className,
		style,
		onClick,
		color: (extend === null || extend === void 0 ? void 0 : extend.colorChannel1) ?? "currentColor"
	});
}
function DefaultTextColorIcon({ className, style, onClick }) {
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(TextColorSwatchIcon, {
		className,
		style,
		onClick,
		color: "#000000"
	});
}
function createTextColorSwatchIcon(color) {
	return function DocParagraphTextColorSwatchIcon(props) {
		return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(TextColorSwatchIcon, {
			...props,
			color
		});
	};
}
function createBackgroundColorSwatchIcon(index) {
	return function DocParagraphBackgroundColorSwatchIcon(props) {
		const themeService = (0, _univerjs_ui.useDependency)(_univerjs_core.ThemeService);
		const color = (0, _univerjs_ui.useObservable)(() => themeService.currentTheme$.pipe((0, rxjs.map)(() => getHighlightBackgroundColor(themeService, index))), getHighlightBackgroundColor(themeService, index), false, [index, themeService]);
		return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(BackgroundColorSwatchIcon, {
			...props,
			color
		});
	};
}
const DocParagraphTextColorSwatchIcon0 = createTextColorSwatchIcon("#FE4B4B");
const DocParagraphTextColorSwatchIcon1 = createTextColorSwatchIcon("#FF8C51");
const DocParagraphTextColorSwatchIcon2 = createTextColorSwatchIcon("#A4DC16");
const DocParagraphTextColorSwatchIcon3 = createTextColorSwatchIcon("#2DAEFF");
const DocParagraphTextColorSwatchIcon4 = createTextColorSwatchIcon("#3A60F7");
const DocParagraphTextColorSwatchIcon5 = createTextColorSwatchIcon("#9E6DE3");
const DocParagraphTextColorSwatchIcon6 = createTextColorSwatchIcon("#F248A6");
const DocParagraphBackgroundColorSwatchIcon0 = createBackgroundColorSwatchIcon(0);
const DocParagraphBackgroundColorSwatchIcon1 = createBackgroundColorSwatchIcon(1);
const DocParagraphBackgroundColorSwatchIcon2 = createBackgroundColorSwatchIcon(2);
const DocParagraphBackgroundColorSwatchIcon3 = createBackgroundColorSwatchIcon(3);
const DocParagraphBackgroundColorSwatchIcon4 = createBackgroundColorSwatchIcon(4);
const DocParagraphBackgroundColorSwatchIcon5 = createBackgroundColorSwatchIcon(5);
const DocParagraphBackgroundColorSwatchIcon6 = createBackgroundColorSwatchIcon(6);
const DocParagraphBackgroundColorSwatchIcon7 = createBackgroundColorSwatchIcon(7);
const DocParagraphBackgroundColorSwatchIcon8 = createBackgroundColorSwatchIcon(8);
const DocParagraphBackgroundColorSwatchIcon9 = createBackgroundColorSwatchIcon(9);
const DocParagraphBackgroundColorSwatchIcon10 = createBackgroundColorSwatchIcon(10);
const DocParagraphBackgroundColorSwatchIcon11 = createBackgroundColorSwatchIcon(11);
const DocParagraphBackgroundColorSwatchIcon12 = createBackgroundColorSwatchIcon(12);
const DocParagraphBackgroundColorSwatchIcon13 = createBackgroundColorSwatchIcon(13);
const DocParagraphBackgroundColorSwatchIcon14 = createBackgroundColorSwatchIcon(14);
const DocParagraphBackgroundColorSwatchIcon15 = createBackgroundColorSwatchIcon(15);

//#endregion
//#region src/views/paragraph-menu/component-keys.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const DOC_PARAGRAPH_MENU_COMPONENT_KEY = "doc.paragraph.menu";
const DOC_TABLE_BLOCK_MENU_COMPONENT_KEY = "doc.table-block.menu";

//#endregion
//#region src/views/paragraph-setting/line-spacing.ts
const AUTO_LINE_SPACING_CONFIG = {
	min: 1,
	max: 5,
	step: .1
};
const FIXED_LINE_SPACING_CONFIG = {
	min: 1,
	max: 100
};
function round(value, digits) {
	const factor = 10 ** digits;
	return Math.round(value * factor) / factor;
}
function clamp(value, min, max) {
	return Math.min(max, Math.max(min, value));
}
const LINE_SPACING_RULE_OPTIONS = [
	{
		label: "docs-ui.doc.paragraphSetting.multiSpace",
		value: `${_univerjs_core.SpacingRule.AUTO}`
	},
	{
		label: "docs-ui.doc.paragraphSetting.atLeast",
		value: `${_univerjs_core.SpacingRule.AT_LEAST}`
	},
	{
		label: "docs-ui.doc.paragraphSetting.exactly",
		value: `${_univerjs_core.SpacingRule.EXACT}`
	}
];
function getLineSpacingInputConfig(spacingRule) {
	return spacingRule === _univerjs_core.SpacingRule.AUTO ? AUTO_LINE_SPACING_CONFIG : FIXED_LINE_SPACING_CONFIG;
}
function convertStoredLineSpacingToDisplayValue(lineSpacing, spacingRule) {
	return spacingRule === _univerjs_core.SpacingRule.AUTO ? lineSpacing : round(lineSpacing, 2);
}
function convertDisplayLineSpacingToStoredValue(lineSpacing, spacingRule) {
	return spacingRule === _univerjs_core.SpacingRule.AUTO ? lineSpacing : round(lineSpacing, 4);
}
function getLineSpacingMetrics(lineNode) {
	if (!lineNode) return null;
	const glyphLineHeight = Math.max(0, ...(lineNode.divides ?? []).flatMap((divide) => (divide.glyphGroup ?? []).map((glyph) => {
		var _glyph$bBox, _glyph$bBox2;
		return (((_glyph$bBox = glyph.bBox) === null || _glyph$bBox === void 0 ? void 0 : _glyph$bBox.ba) ?? 0) + (((_glyph$bBox2 = glyph.bBox) === null || _glyph$bBox2 === void 0 ? void 0 : _glyph$bBox2.bd) ?? 0);
	}))) || lineNode.contentHeight || 0;
	if (!glyphLineHeight) return null;
	return {
		glyphLineHeight,
		renderedLineHeight: (lineNode.paddingTop ?? 0) + (lineNode.contentHeight ?? glyphLineHeight) + (lineNode.paddingBottom ?? 0) || glyphLineHeight
	};
}
function convertLineSpacingForRuleChange(currentStoredValue, currentRule, nextRule, metrics) {
	if (currentRule === nextRule) return currentStoredValue;
	if (nextRule === _univerjs_core.SpacingRule.AUTO) {
		if ((metrics === null || metrics === void 0 ? void 0 : metrics.glyphLineHeight) && metrics.renderedLineHeight) return round(clamp(metrics.renderedLineHeight / metrics.glyphLineHeight, AUTO_LINE_SPACING_CONFIG.min, AUTO_LINE_SPACING_CONFIG.max), 2);
		return _univerjs_core.DEFAULT_DOCUMENT_PARAGRAPH_LINE_SPACING;
	}
	if (metrics === null || metrics === void 0 ? void 0 : metrics.renderedLineHeight) return round(metrics.renderedLineHeight, 4);
	return round(_univerjs_core.DEFAULT_STYLES.fs * currentStoredValue, 4);
}

//#endregion
//#region src/views/paragraph-setting/hook/utils.ts
const PARAGRAPH_LAYOUT_BLOCK_TYPES = /* @__PURE__ */ new Set([
	_univerjs_core.DocumentBlockRangeType.CALLOUT,
	_univerjs_core.DocumentBlockRangeType.CODE,
	_univerjs_core.DocumentBlockRangeType.QUOTE
]);
function resolveParagraphsForSettingPanel(paragraphs, body, documentStyle) {
	return paragraphs.map((paragraph) => {
		var _body$blockRanges;
		const hasLayoutBlockRange = ((_body$blockRanges = body.blockRanges) === null || _body$blockRanges === void 0 ? void 0 : _body$blockRanges.some((range) => PARAGRAPH_LAYOUT_BLOCK_TYPES.has(range.blockType) && paragraph.startIndex > range.startIndex && paragraph.startIndex < range.endIndex)) ?? false;
		return {
			...paragraph,
			paragraphStyle: (0, _univerjs_core.resolveDocumentParagraphStyle)(documentStyle, paragraph.paragraphStyle, { excludeDocumentOuterSpacing: hasLayoutBlockRange })
		};
	});
}
const useDocRanges = () => {
	const docSelectionManagerService = (0, _univerjs_ui.useDependency)(_univerjs_docs.DocSelectionManagerService);
	const docParagraphSettingController = (0, _univerjs_ui.useDependency)(DocParagraphSettingController);
	const docRanges = (0, react.useMemo)(() => docSelectionManagerService.getDocRanges(), []);
	(0, react.useEffect)(() => {
		if (!docRanges.length) docParagraphSettingController.closePanel();
	}, [docRanges]);
	return docRanges;
};
const useCurrentParagraph = () => {
	const docDataModel = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService).getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
	const docRanges = useDocRanges();
	if (!docDataModel || docRanges.length === 0) return [];
	const segmentId = docRanges[0].segmentId;
	const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
	const body = segment === null || segment === void 0 ? void 0 : segment.getBody();
	const paragraphs = (body === null || body === void 0 ? void 0 : body.paragraphs) ?? [];
	const dataStream = (body === null || body === void 0 ? void 0 : body.dataStream) ?? "";
	const currentParagraphs = _univerjs_core.BuildTextUtils.range.getParagraphsInRanges(docRanges, paragraphs, dataStream) ?? [];
	return body == null ? currentParagraphs : resolveParagraphsForSettingPanel(currentParagraphs, body, docDataModel.getDocumentStyle());
};
const useFirstParagraphHorizontalAlign = (paragraph, defaultValue) => {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const [horizontalAlign, setHorizontalAlignSetInternal] = (0, react.useState)(() => {
		var _firstParagraph$parag;
		const firstParagraph = paragraph[0];
		if (!firstParagraph) return defaultValue;
		return String(((_firstParagraph$parag = firstParagraph.paragraphStyle) === null || _firstParagraph$parag === void 0 ? void 0 : _firstParagraph$parag.horizontalAlign) ?? defaultValue);
	});
	const sethorizontalAlign = (v) => {
		setHorizontalAlignSetInternal(v);
		return commandService.executeCommand(DocParagraphSettingCommand.id, { paragraph: { horizontalAlign: Number(v) } });
	};
	return [horizontalAlign, sethorizontalAlign];
};
const useFirstParagraphIndentStart = (paragraph) => {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const [indentStart, setIndentStartInternal] = (0, react.useState)(() => {
		var _firstParagraph$parag2;
		const firstParagraph = paragraph[0];
		if (!firstParagraph) return 0;
		return (0, _univerjs_engine_render.getNumberUnitValue)((_firstParagraph$parag2 = firstParagraph.paragraphStyle) === null || _firstParagraph$parag2 === void 0 ? void 0 : _firstParagraph$parag2.indentStart, 0);
	});
	const setIndentStart = (v) => {
		setIndentStartInternal(v);
		return commandService.executeCommand(DocParagraphSettingCommand.id, { paragraph: { indentStart: { v } } });
	};
	return [indentStart, setIndentStart];
};
const useFirstParagraphIndentEnd = (paragraph) => {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const [indentEnd, setIndentEndInternal] = (0, react.useState)(() => {
		var _firstParagraph$parag3;
		const firstParagraph = paragraph[0];
		if (!firstParagraph) return 0;
		return (0, _univerjs_engine_render.getNumberUnitValue)((_firstParagraph$parag3 = firstParagraph.paragraphStyle) === null || _firstParagraph$parag3 === void 0 ? void 0 : _firstParagraph$parag3.indentEnd, 0);
	});
	const setIndentEnd = (v) => {
		setIndentEndInternal(v);
		return commandService.executeCommand(DocParagraphSettingCommand.id, { paragraph: { indentEnd: { v } } });
	};
	return [indentEnd, setIndentEnd];
};
const useFirstParagraphIndentFirstLine = (paragraph) => {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const [indentFirstLine, setIndentFirstLineInternal] = (0, react.useState)(() => {
		var _firstParagraph$parag4;
		const firstParagraph = paragraph[0];
		if (!firstParagraph) return 0;
		return (0, _univerjs_engine_render.getNumberUnitValue)((_firstParagraph$parag4 = firstParagraph.paragraphStyle) === null || _firstParagraph$parag4 === void 0 ? void 0 : _firstParagraph$parag4.indentFirstLine, 0);
	});
	const setIndentFirstLine = (v) => {
		setIndentFirstLineInternal(v);
		return commandService.executeCommand(DocParagraphSettingCommand.id, { paragraph: { indentFirstLine: { v } } });
	};
	return [indentFirstLine, setIndentFirstLine];
};
const useFirstParagraphIndentHanging = (paragraph) => {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const [hanging, setHangingInternal] = (0, react.useState)(() => {
		var _firstParagraph$parag5;
		const firstParagraph = paragraph[0];
		if (!firstParagraph) return 0;
		return (0, _univerjs_engine_render.getNumberUnitValue)((_firstParagraph$parag5 = firstParagraph.paragraphStyle) === null || _firstParagraph$parag5 === void 0 ? void 0 : _firstParagraph$parag5.hanging, 0);
	});
	const setHanging = (v) => {
		setHangingInternal(v);
		return commandService.executeCommand(DocParagraphSettingCommand.id, { paragraph: { hanging: { v } } });
	};
	return [hanging, setHanging];
};
const useFirstParagraphIndentSpaceAbove = (paragraph) => {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const [spaceAbove, setSpaceAboveInternal] = (0, react.useState)(() => {
		var _firstParagraph$parag6;
		const firstParagraph = paragraph[0];
		if (!firstParagraph) return 0;
		return (0, _univerjs_engine_render.getNumberUnitValue)((_firstParagraph$parag6 = firstParagraph.paragraphStyle) === null || _firstParagraph$parag6 === void 0 ? void 0 : _firstParagraph$parag6.spaceAbove, _univerjs_core.DEFAULT_DOCUMENT_PARAGRAPH_SPACE_ABOVE);
	});
	const setSpaceAbove = (v) => {
		setSpaceAboveInternal(v);
		return commandService.executeCommand(DocParagraphSettingCommand.id, { paragraph: { spaceAbove: { v } } });
	};
	return [spaceAbove, setSpaceAbove];
};
const useFirstParagraphSpaceBelow = (paragraph) => {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const [spaceBelow, setSpaceBelowInternal] = (0, react.useState)(() => {
		var _firstParagraph$parag7;
		const firstParagraph = paragraph[0];
		if (!firstParagraph) return 0;
		return (0, _univerjs_engine_render.getNumberUnitValue)((_firstParagraph$parag7 = firstParagraph.paragraphStyle) === null || _firstParagraph$parag7 === void 0 ? void 0 : _firstParagraph$parag7.spaceBelow, _univerjs_core.DEFAULT_DOCUMENT_PARAGRAPH_SPACE_BELOW);
	});
	const setSpaceBelow = (v) => {
		setSpaceBelowInternal(v);
		return commandService.executeCommand(DocParagraphSettingCommand.id, { paragraph: { spaceBelow: { v } } });
	};
	return [spaceBelow, setSpaceBelow];
};
const useFirstParagraphLineSpacing = (paragraph) => {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const renderManagerService = (0, _univerjs_ui.useDependency)(_univerjs_engine_render.IRenderManagerService);
	const univerInstanceService = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService);
	const skeleton = (0, react.useMemo)(() => {
		var _renderManagerService;
		const docDataModel = univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!docDataModel) return;
		return (_renderManagerService = renderManagerService.getRenderUnitById(docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getUnitId())) === null || _renderManagerService === void 0 ? void 0 : _renderManagerService.with(_univerjs_docs.DocSkeletonManagerService).getSkeleton();
	}, []);
	const stateChange$ = (0, react.useMemo)(() => new rxjs.BehaviorSubject({}), []);
	const [spacingRule, setSpacingRuleInternal] = (0, react.useState)(() => {
		var _firstParagraph$parag8;
		const firstParagraph = paragraph[0];
		if (!firstParagraph) return _univerjs_core.SpacingRule.AUTO;
		return ((_firstParagraph$parag8 = firstParagraph.paragraphStyle) === null || _firstParagraph$parag8 === void 0 ? void 0 : _firstParagraph$parag8.spacingRule) ?? _univerjs_core.SpacingRule.AUTO;
	});
	const [lineSpacing, setLineSpacingInternal] = (0, react.useState)(() => {
		var _firstParagraph$parag9, _firstParagraph$parag10;
		const firstParagraph = paragraph[0];
		if (!firstParagraph) return _univerjs_core.DEFAULT_DOCUMENT_PARAGRAPH_LINE_SPACING;
		const currentSpacingRule = ((_firstParagraph$parag9 = firstParagraph.paragraphStyle) === null || _firstParagraph$parag9 === void 0 ? void 0 : _firstParagraph$parag9.spacingRule) ?? _univerjs_core.SpacingRule.AUTO;
		return convertStoredLineSpacingToDisplayValue(((_firstParagraph$parag10 = firstParagraph.paragraphStyle) === null || _firstParagraph$parag10 === void 0 ? void 0 : _firstParagraph$parag10.lineSpacing) ?? _univerjs_core.DEFAULT_DOCUMENT_PARAGRAPH_LINE_SPACING, currentSpacingRule);
	});
	const setLineSpacing = async (v) => {
		setLineSpacingInternal(v);
		stateChange$.next({
			lineSpacing: convertDisplayLineSpacingToStoredValue(v, spacingRule),
			spacingRule
		});
	};
	const setSpacingRule = async (v) => {
		if (v !== spacingRule) {
			const glyphNode = skeleton === null || skeleton === void 0 ? void 0 : skeleton.findNodeByCharIndex(paragraph[0].startIndex);
			const divideNode = glyphNode === null || glyphNode === void 0 ? void 0 : glyphNode.parent;
			const metrics = getLineSpacingMetrics(divideNode === null || divideNode === void 0 ? void 0 : divideNode.parent);
			const nextStoredLineSpacing = convertLineSpacingForRuleChange(convertDisplayLineSpacingToStoredValue(lineSpacing, spacingRule), spacingRule, v, metrics);
			const nextDisplayLineSpacing = convertStoredLineSpacingToDisplayValue(nextStoredLineSpacing, v);
			setLineSpacingInternal(nextDisplayLineSpacing);
			setSpacingRuleInternal(v);
			stateChange$.next({
				spacingRule: v,
				lineSpacing: nextStoredLineSpacing
			});
		}
	};
	(0, react.useEffect)(() => {
		const dispose = stateChange$.pipe((0, rxjs_operators.filter)((obj) => !!Object.keys(obj).length), (0, rxjs_operators.bufferTime)(16), (0, rxjs_operators.filter)((list) => !!list.length), (0, rxjs_operators.map)((list) => {
			return list.reduce((a, b) => {
				Object.keys(b).forEach((key) => {
					a[key] = b[key];
				});
				return a;
			}, {});
		})).subscribe((v) => {
			return commandService.executeCommand(DocParagraphSettingCommand.id, { paragraph: { ...v } });
		});
		return () => dispose.unsubscribe();
	}, []);
	return {
		lineSpacing: [lineSpacing, setLineSpacing],
		spacingRule: [spacingRule, setSpacingRule]
	};
};

//#endregion
//#region src/views/paragraph-setting/Setting.tsx
const ALIGNMENT_OPTIONS = [
	{
		label: "docs-ui.toolbar.alignLeft",
		value: String(_univerjs_core.HorizontalAlign.LEFT),
		icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.LeftJustifyingIcon, {})
	},
	{
		label: "docs-ui.toolbar.alignCenter",
		value: String(_univerjs_core.HorizontalAlign.CENTER),
		icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.HorizontallyIcon, {})
	},
	{
		label: "docs-ui.toolbar.alignRight",
		value: String(_univerjs_core.HorizontalAlign.RIGHT),
		icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.RightJustifyingIcon, {})
	},
	{
		label: "docs-ui.toolbar.alignJustify",
		value: String(_univerjs_core.HorizontalAlign.JUSTIFIED),
		icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.AlignTextBothIcon, {})
	}
];
const ParagraphSettingSection = (props) => {
	const { title, children, first = false } = props;
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("section", {
		className: (0, _univerjs_design.clsx)(!first && "univer-mt-5"),
		children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
			className: "univer-text-sm univer-font-medium univer-leading-5",
			children: title
		}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
			className: "univer-mt-3",
			children
		})]
	});
};
const ParagraphSettingRow = (props) => {
	const { label, unit, children } = props;
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		className: "univer-grid univer-min-h-8 univer-items-center univer-gap-3",
		style: { gridTemplateColumns: "minmax(0, 1fr) minmax(160px, 180px)" },
		children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			className: "univer-min-w-0 univer-text-xs univer-leading-5 univer-text-gray-900 dark:!univer-text-gray-100",
			children: [label, unit]
		}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
			className: "univer-w-full univer-min-w-0",
			children
		})]
	});
};
const AutoFocusInputNumber = (props) => {
	const { value, onChange, className = "", min = 0, max = 100, step = .1, precision = 1 } = props;
	const ref = (0, react.useRef)(null);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
		step,
		precision,
		ref,
		min,
		max,
		value,
		onChange: (v) => {
			onChange(v ?? 0).finally(() => {
				setTimeout(() => {
					var _ref$current;
					(_ref$current = ref.current) === null || _ref$current === void 0 || _ref$current.focus();
				}, 30);
			});
		},
		className: (0, _univerjs_design.clsx)("univer-w-full", className)
	});
};
function ParagraphSetting() {
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const currentParagraph = useCurrentParagraph();
	const [horizontalAlignValue, setHorizontalAlign] = useFirstParagraphHorizontalAlign(currentParagraph, ALIGNMENT_OPTIONS[0].value);
	const [indentStart, setIndentStart] = useFirstParagraphIndentStart(currentParagraph);
	const [indentEnd, setIndentEnd] = useFirstParagraphIndentEnd(currentParagraph);
	const [indentFirstLine, setIndentFirstLine] = useFirstParagraphIndentFirstLine(currentParagraph);
	const [hanging, setHanging] = useFirstParagraphIndentHanging(currentParagraph);
	const [spaceAbove, setSpaceAbove] = useFirstParagraphIndentSpaceAbove(currentParagraph);
	const [spaceBelow, setSpaceBelow] = useFirstParagraphSpaceBelow(currentParagraph);
	const { lineSpacing: [lineSpacing, setLineSpacing], spacingRule: [spacingRule, setSpacingRule] } = useFirstParagraphLineSpacing(currentParagraph);
	const lineSpaceConfig = (0, react.useMemo)(() => getLineSpacingInputConfig(spacingRule), [spacingRule]);
	const lineSpacingOptions = LINE_SPACING_RULE_OPTIONS.map((option) => ({
		...option,
		label: localeService.t(option.label)
	}));
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		className: "univer-box-border univer-w-full",
		children: [
			/* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphSettingSection, {
				title: localeService.t("docs-ui.doc.paragraphSetting.alignment"),
				first: true,
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: (0, _univerjs_design.clsx)("univer-flex univer-grid-cols-4 univer-items-center univer-justify-around univer-gap-2 univer-rounded-lg univer-bg-gray-0 univer-p-1 dark:!univer-bg-gray-900", _univerjs_design.borderClassName),
					children: ALIGNMENT_OPTIONS.map((item) => {
						return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Tooltip, {
							title: localeService.t(item.label),
							placement: "bottom",
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
								className: "univer-flex univer-w-full univer-items-center univer-justify-center",
								children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Button, {
									type: "button",
									variant: "text",
									className: (0, _univerjs_design.clsx)({ "!univer-bg-gray-200 dark:!univer-bg-gray-700": horizontalAlignValue === item.value }),
									onClick: () => setHorizontalAlign(item.value),
									children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
										className: "univer-flex univer-size-5 univer-items-center univer-justify-center univer-text-lg",
										children: item.icon
									})
								})
							})
						}, item.value);
					})
				})
			}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphSettingSection, {
				title: localeService.t("docs-ui.doc.paragraphSetting.indentation"),
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: "univer-grid univer-gap-3",
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphSettingRow, {
							label: localeService.t("docs-ui.doc.paragraphSetting.left"),
							unit: "(px)",
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutoFocusInputNumber, {
								value: indentStart,
								onChange: (v) => setIndentStart(v ?? 0)
							})
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphSettingRow, {
							label: localeService.t("docs-ui.doc.paragraphSetting.right"),
							unit: "(px)",
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutoFocusInputNumber, {
								value: indentEnd,
								onChange: (v) => setIndentEnd(v ?? 0)
							})
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphSettingRow, {
							label: localeService.t("docs-ui.doc.paragraphSetting.firstLine"),
							unit: "(px)",
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutoFocusInputNumber, {
								value: indentFirstLine,
								onChange: (v) => setIndentFirstLine(v ?? 0)
							})
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphSettingRow, {
							label: localeService.t("docs-ui.doc.paragraphSetting.hanging"),
							unit: "(px)",
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutoFocusInputNumber, {
								value: hanging,
								onChange: (v) => setHanging(v ?? 0)
							})
						})
					]
				})
			}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphSettingSection, {
				title: localeService.t("docs-ui.doc.paragraphSetting.spacing"),
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
					className: "univer-grid univer-gap-3",
					children: [
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphSettingRow, {
							label: localeService.t("docs-ui.doc.paragraphSetting.before"),
							unit: "(px)",
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutoFocusInputNumber, {
								value: spaceAbove,
								onChange: (v) => setSpaceAbove(v ?? 0)
							})
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphSettingRow, {
							label: localeService.t("docs-ui.doc.paragraphSetting.after"),
							unit: "(px)",
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutoFocusInputNumber, {
								value: spaceBelow,
								onChange: (v) => setSpaceBelow(v ?? 0)
							})
						}),
						/* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphSettingRow, {
							label: localeService.t("docs-ui.doc.paragraphSetting.lineSpace"),
							children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
								className: "univer-flex univer-w-full univer-flex-col univer-gap-2",
								children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Select, {
									className: "univer-w-full",
									value: `${spacingRule}`,
									options: lineSpacingOptions,
									onChange: (v) => setSpacingRule(Number(v))
								}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(AutoFocusInputNumber, {
									...lineSpaceConfig,
									value: lineSpacing,
									onChange: (v) => setLineSpacing(v ?? 0)
								})]
							})
						})
					]
				})
			})
		]
	});
}

//#endregion
//#region src/views/paragraph-setting/index.tsx
const isRangesEqual = (oldRanges, ranges) => {
	return ranges.length === oldRanges.length && oldRanges.some((oldRange) => ranges.some((range) => range.startOffset === oldRange.startOffset && range.endOffset === oldRange.endOffset));
};
function ParagraphSettingIndex() {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const currentLocale = (0, _univerjs_ui.useObservable)((0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService).currentLocale$);
	const [key, setKey] = (0, react.useState)("");
	const debounceReset = (0, react.useMemo)(() => {
		return (0, _univerjs_core.debounce)(() => setKey((0, _univerjs_core.generateRandomId)(4)), 300);
	}, []);
	const rangeRef = (0, react.useRef)([]);
	(0, react.useEffect)(() => {
		const dispose = commandService.onCommandExecuted((info) => {
			if (_univerjs_docs.SetTextSelectionsOperation.id === info.id) {
				const ranges = info.params.ranges;
				if (!isRangesEqual(ranges, rangeRef.current)) setKey((0, _univerjs_core.generateRandomId)(4));
				rangeRef.current = ranges;
			}
			if (_univerjs_docs.RichTextEditingMutation.id === info.id) {
				if (info.params.trigger !== DocParagraphSettingCommand.id) debounceReset();
			}
		});
		return () => dispose.dispose();
	}, [debounceReset]);
	(0, react.useEffect)(() => {
		setKey((0, _univerjs_core.generateRandomId)(4));
	}, [currentLocale]);
	(0, react.useEffect)(() => () => debounceReset.cancel(), [debounceReset]);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphSetting, {}, key);
}

//#endregion
//#region src/services/float-menu.service.ts
const SKIP_SYMBOLS = [_univerjs_core.DataStreamTreeTokenType.CUSTOM_BLOCK, _univerjs_core.DataStreamTreeTokenType.PARAGRAPH];
let DocFloatMenuService = class DocFloatMenuService extends _univerjs_core.Disposable {
	constructor(_context, _docSelectionManagerService, _docCanvasPopManagerService, _univerInstanceService, _docSelectionRenderService, _contextService, _permissionService, _docLayoutInteractionService, _embedRuntimeFocusCoordinator) {
		super();
		this._context = _context;
		this._docSelectionManagerService = _docSelectionManagerService;
		this._docCanvasPopManagerService = _docCanvasPopManagerService;
		this._univerInstanceService = _univerInstanceService;
		this._docSelectionRenderService = _docSelectionRenderService;
		this._contextService = _contextService;
		this._permissionService = _permissionService;
		this._docLayoutInteractionService = _docLayoutInteractionService;
		this._embedRuntimeFocusCoordinator = _embedRuntimeFocusCoordinator;
		_defineProperty(this, "_floatMenu", null);
		_defineProperty(this, "_suppressed", false);
		_defineProperty(this, "_embedSuppressed", false);
		_defineProperty(this, "_invalidatedSelection", null);
		if ((0, _univerjs_core.isInternalEditorID)(this._context.unitId)) return;
		this._initPermissionLifecycle();
		this._initSelectionChange();
		this._initEmbedRuntimeLifecycle();
		this.disposeWithMe(() => {
			this._hideFloatMenu();
		});
	}
	get floatMenu() {
		return this._floatMenu;
	}
	hideFloatMenu() {
		this._hideFloatMenu();
	}
	setSuppressed(suppressed) {
		if (this._suppressed === suppressed) return;
		this._suppressed = suppressed;
		if (suppressed) this._hideFloatMenu();
	}
	_initSelectionChange() {
		this.disposeWithMe(this._docSelectionRenderService.onSelectionStart$.subscribe(() => {
			this._invalidatedSelection = null;
			this._hideFloatMenu();
		}));
		this.disposeWithMe(this._docSelectionManagerService.textSelection$.subscribe((selection) => {
			const { unitId, textRanges } = selection;
			if (unitId !== this._context.unitId) return;
			if (this._suppressed || this._embedSuppressed || this._contextService.getContextValue(_univerjs_core.FOCUSING_COMMON_DRAWINGS)) {
				this._hideFloatMenu();
				return;
			}
			const range = textRanges.length > 0 && textRanges.find((range) => !range.collapsed);
			if (range) {
				var _this$_floatMenu, _this$_floatMenu2, _this$_floatMenu3;
				if (this._getSelectionKey(range) === this._invalidatedSelection) {
					this._hideFloatMenu();
					return;
				}
				this._invalidatedSelection = null;
				if (range.startOffset === ((_this$_floatMenu = this._floatMenu) === null || _this$_floatMenu === void 0 ? void 0 : _this$_floatMenu.start) && range.endOffset === ((_this$_floatMenu2 = this._floatMenu) === null || _this$_floatMenu2 === void 0 ? void 0 : _this$_floatMenu2.end) && (range.segmentId ?? "") === ((_this$_floatMenu3 = this._floatMenu) === null || _this$_floatMenu3 === void 0 ? void 0 : _this$_floatMenu3.segmentId)) return;
				this._hideFloatMenu();
				this._showFloatMenu(unitId, range);
				return;
			}
			this._invalidatedSelection = null;
			this._hideFloatMenu();
		}));
	}
	_initPermissionLifecycle() {
		this.disposeWithMe(this._permissionService.permissionPointUpdate$.subscribe(() => {
			if (this._floatMenu && !this._canEditDocument({
				startOffset: this._floatMenu.start,
				endOffset: this._floatMenu.end
			}, this._floatMenu.segmentId)) this._hideFloatMenu();
		}));
	}
	_canEditDocument(range, segmentId = "") {
		const documentDataModel = this._univerInstanceService.getUnit(this._context.unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!documentDataModel) return false;
		return (0, _univerjs_docs.canEditDocumentTargets)(this._permissionService, this._context.unitId, range ? (0, _univerjs_docs.getDocumentEditTargetObjectIds)(documentDataModel, segmentId, range) : []);
	}
	_initEmbedRuntimeLifecycle() {
		if (!this._embedRuntimeFocusCoordinator) return;
		const syncSuppressedState = () => {
			var _this$_embedRuntimeFo, _this$_docSelectionMa;
			const suppressed = ((_this$_embedRuntimeFo = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo === void 0 ? void 0 : _this$_embedRuntimeFo.shouldSuppressHostInteraction(this._context.unitId)) === true;
			if (this._embedSuppressed === suppressed) return;
			this._embedSuppressed = suppressed;
			if (!suppressed) return;
			const expandedRange = (_this$_docSelectionMa = this._docSelectionManagerService.getTextRanges({
				unitId: this._context.unitId,
				subUnitId: this._context.unitId
			})) === null || _this$_docSelectionMa === void 0 ? void 0 : _this$_docSelectionMa.find((range) => !range.collapsed);
			this._invalidatedSelection = expandedRange ? this._getSelectionKey(expandedRange) : this._floatMenu && this._getSelectionKey({
				startOffset: this._floatMenu.start,
				endOffset: this._floatMenu.end,
				segmentId: this._floatMenu.segmentId
			});
			this._hideFloatMenu();
		};
		this.disposeWithMe(this._embedRuntimeFocusCoordinator.runtimeSessionChanged$.subscribe(syncSuppressedState));
		syncSuppressedState();
	}
	_getSelectionKey(range) {
		return `${range.segmentId ?? ""}:${range.startOffset}:${range.endOffset}`;
	}
	_hideFloatMenu() {
		var _this$_floatMenu4;
		(_this$_floatMenu4 = this._floatMenu) === null || _this$_floatMenu4 === void 0 || _this$_floatMenu4.disposable.dispose();
		this._floatMenu = null;
	}
	_showFloatMenu(unitId, range) {
		var _documentDataModel$ge, _documentDataModel$ge2;
		const documentDataModel = this._univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
		const segmentId = range.segmentId ?? "";
		if (!documentDataModel || documentDataModel.getDisabled() || !this._canEditDocument(range, segmentId)) return;
		if (isRangeInCodeBlock(documentDataModel, range)) return;
		const token = (_documentDataModel$ge = documentDataModel.getBody()) === null || _documentDataModel$ge === void 0 ? void 0 : _documentDataModel$ge.dataStream[range.startOffset];
		if (range.endOffset - range.startOffset === 1 && token && SKIP_SYMBOLS.includes(token)) return;
		const wholeCustomRanges = (_documentDataModel$ge2 = documentDataModel.getBody()) === null || _documentDataModel$ge2 === void 0 || (_documentDataModel$ge2 = _documentDataModel$ge2.customRanges) === null || _documentDataModel$ge2 === void 0 ? void 0 : _documentDataModel$ge2.filter((range) => range.wholeEntity);
		if (wholeCustomRanges === null || wholeCustomRanges === void 0 ? void 0 : wholeCustomRanges.some((customRange) => customRange.startIndex === range.startOffset && customRange.endIndex === range.endOffset - 1)) return;
		const popup = this._docCanvasPopManagerService.attachPopupToRange(range, {
			componentKey: FLOAT_MENU_COMPONENT_KEY,
			direction: "top-left",
			rangeAnchor: "selection-end",
			offset: [0, 8],
			extraProps: { onDismiss: () => {
				this._invalidatedSelection = this._getSelectionKey(range);
				this._hideFloatMenu();
			} }
		}, unitId);
		const layoutInteraction = this._docLayoutInteractionService.beginInteraction();
		this._floatMenu = {
			disposable: (0, _univerjs_core.toDisposable)(() => {
				popup.dispose();
				layoutInteraction.dispose();
			}),
			start: range.startOffset,
			end: range.endOffset,
			segmentId
		};
		return (0, _univerjs_core.toDisposable)(() => this._hideFloatMenu());
	}
};
DocFloatMenuService = __decorate([
	__decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(2, (0, _univerjs_core.Inject)(DocCanvasPopManagerService)),
	__decorateParam(3, (0, _univerjs_core.Inject)(_univerjs_core.IUniverInstanceService)),
	__decorateParam(4, (0, _univerjs_core.Inject)(DocSelectionRenderService)),
	__decorateParam(5, _univerjs_core.IContextService),
	__decorateParam(6, _univerjs_core.IPermissionService),
	__decorateParam(7, (0, _univerjs_core.Inject)(DocLayoutInteractionService)),
	__decorateParam(8, (0, _univerjs_core.Optional)(IDocEmbedRuntimeFocusCoordinator))
], DocFloatMenuService);
function isRangeInCodeBlock(documentDataModel, range) {
	var _documentDataModel$ge3;
	return (((_documentDataModel$ge3 = documentDataModel.getBody()) === null || _documentDataModel$ge3 === void 0 ? void 0 : _documentDataModel$ge3.blockRanges) ?? []).some((blockRange) => blockRange.blockType === _univerjs_core.DocumentBlockRangeType.CODE && Math.max(range.startOffset, blockRange.startIndex) <= Math.min(range.endOffset, blockRange.endIndex));
}

//#endregion
//#region src/services/doc-paragraph-menu.service.ts
const BLOCK_RANGE_ICON_MAP = {
	code: "CodeBlockIcon",
	quote: "QuoteIcon",
	callout: "CalloutIcon"
};
const LIST_ICON_MAP = {
	[_univerjs_core.PresetListType.ORDER_LIST]: "OrderIcon",
	[_univerjs_core.PresetListType.ORDER_LIST_1]: "OrderIcon",
	[_univerjs_core.PresetListType.ORDER_LIST_2]: "OrderIcon",
	[_univerjs_core.PresetListType.ORDER_LIST_3]: "OrderIcon",
	[_univerjs_core.PresetListType.ORDER_LIST_4]: "OrderIcon",
	[_univerjs_core.PresetListType.ORDER_LIST_5]: "OrderIcon",
	[_univerjs_core.PresetListType.ORDER_LIST_QUICK_2]: "OrderIcon",
	[_univerjs_core.PresetListType.ORDER_LIST_QUICK_3]: "OrderIcon",
	[_univerjs_core.PresetListType.ORDER_LIST_QUICK_4]: "OrderIcon",
	[_univerjs_core.PresetListType.ORDER_LIST_QUICK_5]: "OrderIcon",
	[_univerjs_core.PresetListType.ORDER_LIST_QUICK_6]: "OrderIcon",
	[_univerjs_core.PresetListType.BULLET_LIST]: "UnorderIcon",
	[_univerjs_core.PresetListType.BULLET_LIST_1]: "UnorderIcon",
	[_univerjs_core.PresetListType.BULLET_LIST_2]: "UnorderIcon",
	[_univerjs_core.PresetListType.BULLET_LIST_3]: "UnorderIcon",
	[_univerjs_core.PresetListType.BULLET_LIST_4]: "UnorderIcon",
	[_univerjs_core.PresetListType.BULLET_LIST_5]: "UnorderIcon",
	[_univerjs_core.PresetListType.CHECK_LIST]: "TodoListDoubleIcon",
	[_univerjs_core.PresetListType.CHECK_LIST_CHECKED]: "TodoListDoubleIcon"
};
let DocParagraphMenuService = class DocParagraphMenuService extends _univerjs_core.Disposable {
	get activeParagraph() {
		var _this$_paragraphMenu;
		return (_this$_paragraphMenu = this._paragraphMenu) === null || _this$_paragraphMenu === void 0 ? void 0 : _this$_paragraphMenu.paragraph;
	}
	get activeTarget() {
		var _this$_paragraphMenu2;
		return ((_this$_paragraphMenu2 = this._paragraphMenu) === null || _this$_paragraphMenu2 === void 0 ? void 0 : _this$_paragraphMenu2.target) ?? null;
	}
	setBlockMenuDragging(isDragging) {
		this._isBlockMenuDragging = isDragging;
		if (isDragging) this._dragInteraction ??= this._docLayoutInteractionService.beginInteraction();
		else {
			var _this$_dragInteractio;
			(_this$_dragInteractio = this._dragInteraction) === null || _this$_dragInteractio === void 0 || _this$_dragInteractio.dispose();
			this._dragInteraction = null;
		}
	}
	get isBlockMenuDragging() {
		return this._isBlockMenuDragging;
	}
	constructor(_context, _docSelectionManagerService, _docEventManagerService, _docPopupManagerService, _docSkeletonManagerService, _floatMenuService, _docSelectionRenderService, _permissionService, _docLayoutInteractionService) {
		super();
		this._context = _context;
		this._docSelectionManagerService = _docSelectionManagerService;
		this._docEventManagerService = _docEventManagerService;
		this._docPopupManagerService = _docPopupManagerService;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		this._floatMenuService = _floatMenuService;
		this._docSelectionRenderService = _docSelectionRenderService;
		this._permissionService = _permissionService;
		this._docLayoutInteractionService = _docLayoutInteractionService;
		_defineProperty(this, "_paragraphMenu", null);
		_defineProperty(this, "_activeTarget$", new rxjs.BehaviorSubject(null));
		_defineProperty(this, "activeTarget$", this._activeTarget$.asObservable());
		_defineProperty(this, "_slashMenuRequest$", new rxjs.BehaviorSubject(null));
		_defineProperty(this, "slashMenuRequest$", this._slashMenuRequest$.asObservable());
		_defineProperty(this, "_isBlockMenuDragging", false);
		_defineProperty(this, "_isSlashMenuActive", false);
		_defineProperty(this, "_slashMenuRequestNonce", 0);
		_defineProperty(this, "_menuInteraction", null);
		_defineProperty(this, "_dragInteraction", null);
		if ((0, _univerjs_core.isInternalEditorID)(this._context.unitId)) return;
		this._init();
		this.disposeWithMe(this._permissionService.permissionPointUpdate$.subscribe(() => {
			const menu = this._paragraphMenu;
			if (!this._canEditDocument(menu === null || menu === void 0 ? void 0 : menu.target.menuRange, menu === null || menu === void 0 ? void 0 : menu.paragraph.segmentId)) {
				this._isBlockMenuDragging = false;
				this.hideParagraphMenu(true);
			}
		}));
	}
	_isCursorInActiveParagraph() {
		if (!this._paragraphMenu) return false;
		const activeTextRange = this._docSelectionManagerService.getActiveTextRange();
		if (!(activeTextRange === null || activeTextRange === void 0 ? void 0 : activeTextRange.collapsed)) return false;
		if (!(activeTextRange === null || activeTextRange === void 0 ? void 0 : activeTextRange.collapsed) || activeTextRange.startOffset < this._paragraphMenu.paragraph.paragraphStart || activeTextRange.startOffset > this._paragraphMenu.paragraph.paragraphEnd) return false;
		return true;
	}
	_getExpandedSelectionState(ranges) {
		return ranges.reduce((state, range) => {
			if (range.rangeType === _univerjs_core.DOC_RANGE_TYPE.RECT) {
				state.hasRectRange = true;
				return state;
			}
			if (range.collapsed === false) {
				state.hasExpandedTextRange = true;
				return state;
			}
			if (typeof range.startOffset === "number" && typeof range.endOffset === "number" && range.endOffset > range.startOffset && range.collapsed !== true) state.hasExpandedTextRange = true;
			return state;
		}, {
			hasExpandedTextRange: false,
			hasRectRange: false
		});
	}
	_hasExpandedSelectionRanges(ranges) {
		const state = this._getExpandedSelectionState(ranges);
		return state.hasExpandedTextRange || state.hasRectRange;
	}
	_hasExpandedTextSelection() {
		return this._getExpandedSelectionState(this._docSelectionManagerService.getDocRanges()).hasExpandedTextRange;
	}
	_hasExpandedSelection() {
		return this._hasExpandedSelectionRanges(this._docSelectionManagerService.getDocRanges());
	}
	setParagraphMenuActive(active) {
		if (this._paragraphMenu) this._paragraphMenu.active = active;
		if (active && this._paragraphMenu) this._menuInteraction ??= this._docLayoutInteractionService.beginInteraction();
		else {
			var _this$_menuInteractio;
			(_this$_menuInteractio = this._menuInteraction) === null || _this$_menuInteractio === void 0 || _this$_menuInteractio.dispose();
			this._menuInteraction = null;
		}
	}
	_init() {
		const handleHoverTarget = (paragraph, tableBound) => {
			if (this._isBlockMenuDragging) return;
			if (this._docSelectionRenderService.isOnPointerEvent) {
				this.hideParagraphMenu(true);
				return;
			}
			const selectionState = this._getExpandedSelectionState(this._docSelectionManagerService.getDocRanges());
			if (selectionState.hasExpandedTextRange) {
				this.hideParagraphMenu(true);
				return;
			}
			if (selectionState.hasRectRange) {
				var _this$_paragraphMenu3;
				if (tableBound) {
					this.showTableMenu(tableBound);
					return;
				}
				if (((_this$_paragraphMenu3 = this._paragraphMenu) === null || _this$_paragraphMenu3 === void 0 ? void 0 : _this$_paragraphMenu3.target.kind) === _univerjs_core.DocumentBlockType.TABLE) return;
				this.hideParagraphMenu(true);
				return;
			}
			if (this._docSkeletonManagerService.getViewModel().getEditArea() === _univerjs_engine_render.DocumentEditArea.BODY && !this._floatMenuService.floatMenu && !this._context.unit.getDisabled() && this._canEditDocument()) {
				var _this$_paragraphMenu4;
				if ((_this$_paragraphMenu4 = this._paragraphMenu) === null || _this$_paragraphMenu4 === void 0 ? void 0 : _this$_paragraphMenu4.active) return;
				if (paragraph) {
					this.showParagraphMenu(paragraph);
					return;
				}
				if (tableBound) {
					this.showTableMenu(tableBound);
					return;
				}
			}
			this.hideParagraphMenu(true);
		};
		this.disposeWithMe((0, rxjs.combineLatest)([
			this._docEventManagerService.hoverParagraphRealTime$,
			this._docEventManagerService.hoverParagraphLeft$,
			this._docEventManagerService.hoverTableRealTime$ ?? new rxjs.BehaviorSubject(null)
		]).pipe((0, rxjs.throttleTime)(16, void 0, {
			leading: true,
			trailing: true
		})).subscribe(([p, left, table]) => {
			handleHoverTarget(p ?? left, table);
		}));
		this.disposeWithMe(this._docSelectionRenderService.onSelectionStart$.subscribe(() => {
			this.hideParagraphMenu(true);
		}));
		this.disposeWithMe(this._docSelectionManagerService.textSelection$.subscribe(({ textRanges, rectRanges }) => {
			var _this$_paragraphMenu5;
			const selectionState = this._getExpandedSelectionState([...textRanges, ...rectRanges]);
			if (selectionState.hasExpandedTextRange || selectionState.hasRectRange && ((_this$_paragraphMenu5 = this._paragraphMenu) === null || _this$_paragraphMenu5 === void 0 ? void 0 : _this$_paragraphMenu5.target.kind) !== _univerjs_core.DocumentBlockType.TABLE) this.hideParagraphMenu(true);
		}));
		let lastScrollY = 0;
		this.disposeWithMe(this._context.scene.getViewport("viewMain").onScrollAfter$.subscribeEvent((e) => {
			var _this$_paragraphMenu6;
			if (lastScrollY === e.scrollY) return;
			lastScrollY = e.scrollY;
			if ((_this$_paragraphMenu6 = this._paragraphMenu) === null || _this$_paragraphMenu6 === void 0 ? void 0 : _this$_paragraphMenu6.blockRange) return;
			if (this._isBlockMenuDragging) return;
			this.hideParagraphMenu(true);
		}));
		this.disposeWithMe(this._docEventManagerService.clickCustomRanges$.subscribe(() => {
			if (this._isBlockMenuDragging) return;
			this.hideParagraphMenu(true);
		}));
		this.disposeWithMe(this._docSelectionRenderService.onKeydown$.subscribe((config) => {
			if (this._isBlockMenuDragging) return;
			if (this._handleSlashMenuKeydown(config)) return;
			this.hideParagraphMenu(true);
		}));
		this.disposeWithMe(this._docSelectionRenderService.onInputBefore$.subscribe((config) => {
			if (!config || this._isBlockMenuDragging) return;
			this._handleSlashMenuInputBefore(config);
		}));
	}
	_handleSlashMenuKeydown(config) {
		if (!this._shouldOpenSlashMenu(config)) return false;
		return this._openSlashMenu(config);
	}
	_handleSlashMenuInputBefore(config) {
		if (!this._shouldOpenSlashMenuFromInput(config)) return false;
		return this._openSlashMenu(config);
	}
	_openSlashMenu(config) {
		config.event.preventDefault();
		config.event.stopPropagation();
		const paragraph = this._getSlashMenuParagraph(config);
		if (!paragraph) return true;
		this.showParagraphMenu(paragraph);
		const target = this.activeTarget;
		if (!target) return true;
		this._slashMenuRequest$.next({
			anchorRect: this._getSlashMenuAnchorRect(config, paragraph),
			nonce: ++this._slashMenuRequestNonce,
			target
		});
		this._isSlashMenuActive = true;
		return true;
	}
	_shouldOpenSlashMenu(config) {
		const event = config.event;
		const range = this._getCollapsedTextRange(config);
		if (!range || !this._canEditDocument(range, range.segmentId) || event.key !== "/" || event.altKey || event.ctrlKey || event.metaKey) return false;
		return true;
	}
	_shouldOpenSlashMenuFromInput(config) {
		const event = config.event;
		const content = config.content ?? event.data ?? "";
		const range = this._getCollapsedTextRange(config);
		if (!range || !this._canEditDocument(range, range.segmentId) || content !== "/") return false;
		return true;
	}
	_getCollapsedTextRange(config) {
		const activeRange = config.activeRange ?? this._docSelectionManagerService.getActiveTextRange();
		if (!activeRange) return null;
		if (activeRange.collapsed || activeRange.startOffset === activeRange.endOffset) return {
			...activeRange,
			collapsed: true
		};
		return null;
	}
	_getSlashMenuParagraph(config) {
		var _this$_paragraphMenu7;
		const activeRange = this._getCollapsedTextRange(config);
		const offset = activeRange === null || activeRange === void 0 ? void 0 : activeRange.startOffset;
		const activeParagraph = ((_this$_paragraphMenu7 = this._paragraphMenu) === null || _this$_paragraphMenu7 === void 0 ? void 0 : _this$_paragraphMenu7.paragraph) ?? null;
		if (activeParagraph && offset != null && offset >= activeParagraph.paragraphStart && offset <= activeParagraph.paragraphEnd) return activeParagraph;
		if (activeParagraph && offset == null) return activeParagraph;
		if (offset == null) return null;
		return [...this._docEventManagerService.paragraphBounds.values()].find((paragraph) => offset >= paragraph.paragraphStart && offset <= paragraph.paragraphEnd) ?? null;
	}
	_getSlashMenuAnchorRect(config, paragraph) {
		const activeRange = this._getCollapsedTextRange(config);
		if (!activeRange) return paragraph.firstLine;
		try {
			var _calcDocRangePosition;
			return ((_calcDocRangePosition = calcDocRangePositions({
				...activeRange,
				collapsed: true,
				endOffset: activeRange.startOffset
			}, this._context)) === null || _calcDocRangePosition === void 0 ? void 0 : _calcDocRangePosition[0]) ?? paragraph.firstLine;
		} catch {
			return paragraph.firstLine;
		}
	}
	showParagraphMenu(paragraph) {
		var _this$_paragraphMenu8;
		if (this._hasExpandedSelection()) {
			this.hideParagraphMenu(true);
			return;
		}
		const target = this._buildParagraphMenuTarget(paragraph);
		if (!target || !this._canEditDocument(target.menuRange, paragraph.segmentId)) {
			this.hideParagraphMenu(true);
			return;
		}
		if (((_this$_paragraphMenu8 = this._paragraphMenu) === null || _this$_paragraphMenu8 === void 0 ? void 0 : _this$_paragraphMenu8.target.key) === target.key) return;
		this.hideParagraphMenu(true);
		const targetParagraph = target.paragraph ?? paragraph;
		const blockRange = target.blockRange;
		const getFirstLine = () => {
			const latestParagraphBound = this._docEventManagerService.findParagraphBoundByIndex(targetParagraph.startIndex) ?? targetParagraph;
			const baseAnchor = blockRange ? getBlockRangeAnchorRect(this._docEventManagerService, blockRange, paragraph) : latestParagraphBound.firstLine ?? paragraph.firstLine;
			if (!target.cellRange) return baseAnchor;
			return getTableCellParagraphAnchorRect(this._docEventManagerService, latestParagraphBound, baseAnchor);
		};
		const disposable = this._docPopupManagerService.attachPopupToRect(getFirstLine, {
			componentKey: DOC_PARAGRAPH_MENU_COMPONENT_KEY,
			requiresStableLayout: false,
			direction: "left-center",
			onClickOutside: () => {
				if (this._isSlashMenuActive) {
					this.hideParagraphMenu(true);
					return;
				}
				this._docSelectionManagerService.textSelection$.pipe((0, rxjs.first)()).subscribe(() => {
					if (!this._isCursorInActiveParagraph()) this.hideParagraphMenu(true);
				});
			},
			zIndex: 101
		}, this._context.unitId);
		this._paragraphMenu = {
			paragraph: targetParagraph,
			target,
			blockRange,
			disposable,
			active: false
		};
		this._activeTarget$.next(target);
	}
	showTableMenu(tableBound) {
		var _body$tables, _this$_paragraphMenu9;
		if (this._hasExpandedTextSelection()) {
			this.hideParagraphMenu(true);
			return;
		}
		const body = this._context.unit.getBody();
		const table = body === null || body === void 0 || (_body$tables = body.tables) === null || _body$tables === void 0 ? void 0 : _body$tables.find((item) => item.tableId === tableBound.tableId);
		if (!table) return;
		const target = {
			kind: _univerjs_core.DocumentBlockType.TABLE,
			key: `${_univerjs_core.DocumentBlockType.TABLE}:${table.tableId}`,
			table,
			icon: "GridIcon",
			menuRange: {
				startOffset: table.startIndex,
				endOffset: table.endIndex,
				collapsed: false
			},
			moveRange: {
				startOffset: table.startIndex,
				endOffset: table.endIndex
			},
			emptyMode: false,
			draggable: true
		};
		if (!this._canEditDocument(target.menuRange)) {
			this.hideParagraphMenu(true);
			return;
		}
		if (this._shouldKeepCurrentCellMenuForTable(table)) return;
		if (((_this$_paragraphMenu9 = this._paragraphMenu) === null || _this$_paragraphMenu9 === void 0 ? void 0 : _this$_paragraphMenu9.target.key) === target.key) return;
		const anchorRect = getTableAnchorRect(tableBound);
		const paragraph = {
			rect: tableBound.rect,
			rects: [tableBound.rect],
			paragraphStart: table.startIndex,
			paragraphEnd: table.endIndex,
			startIndex: table.startIndex,
			pageIndex: tableBound.pageIndex,
			firstLine: anchorRect
		};
		this.hideParagraphMenu(true);
		const getFirstLine = () => getTableAnchorRect(this._docEventManagerService.tableBounds.get(table.tableId) ?? tableBound);
		const disposable = this._docPopupManagerService.attachPopupToRect(getFirstLine, {
			componentKey: DOC_TABLE_BLOCK_MENU_COMPONENT_KEY,
			requiresStableLayout: false,
			direction: "top-right",
			onClickOutside: () => {
				this.hideParagraphMenu(true);
			},
			zIndex: 101
		}, this._context.unitId);
		this._paragraphMenu = {
			paragraph,
			target,
			disposable,
			active: false
		};
		this._activeTarget$.next(target);
	}
	_shouldKeepCurrentCellMenuForTable(table) {
		var _this$_paragraphMenu10;
		const target = (_this$_paragraphMenu10 = this._paragraphMenu) === null || _this$_paragraphMenu10 === void 0 ? void 0 : _this$_paragraphMenu10.target;
		if (!target || target.kind === _univerjs_core.DocumentBlockType.TABLE || !target.cellRange) return false;
		return target.cellRange.startOffset >= table.startIndex && target.cellRange.startOffset <= table.endIndex;
	}
	getDropTargetFromClientPoint(clientX, clientY, sourceRange) {
		const canvasRect = this._context.engine.getCanvasElement().getBoundingClientRect();
		const point = transformOffset2Bound(clientX - canvasRect.left, clientY - canvasRect.top, this._context.scene);
		const body = this._context.unit.getBody();
		const sourceCellRange = (body === null || body === void 0 ? void 0 : body.dataStream) ? getContainingTableCellRange(body.dataStream, sourceRange.startOffset) : null;
		const blocks = (sourceCellRange ? this._getCellBlockTargets(sourceCellRange) : this._getBodyBlockTargets()).filter((block) => block.moveRange.startOffset !== sourceRange.startOffset || block.moveRange.endOffset !== sourceRange.endOffset).filter((block) => block.moveRange.endOffset <= sourceRange.startOffset || block.moveRange.startOffset >= sourceRange.endOffset).sort((left, right) => left.rect.top - right.rect.top || left.rect.left - right.rect.left);
		if (!blocks.length) return null;
		let nearest = blocks[0];
		let nearestDistance = Number.POSITIVE_INFINITY;
		for (const block of blocks) {
			const middle = (block.rect.top + block.rect.bottom) / 2;
			const distance = Math.abs(point.y - middle);
			if (distance < nearestDistance) {
				nearestDistance = distance;
				nearest = block;
			}
		}
		const before = point.y < (nearest.rect.top + nearest.rect.bottom) / 2;
		const targetOffset = before ? nearest.moveRange.startOffset : nearest.moveRange.endOffset;
		const screenRect = transformBound2OffsetBound({
			left: nearest.rect.left,
			right: nearest.rect.right,
			top: before ? nearest.rect.top : nearest.rect.bottom,
			bottom: before ? nearest.rect.top : nearest.rect.bottom
		}, this._context.scene);
		return {
			targetOffset,
			rect: {
				left: canvasRect.left + screenRect.left,
				right: canvasRect.left + screenRect.right,
				top: canvasRect.top + screenRect.top,
				bottom: canvasRect.top + screenRect.bottom
			}
		};
	}
	hideParagraphMenu(force = false) {
		if (this._isBlockMenuDragging) return;
		if (this._paragraphMenu && (this._paragraphMenu.disposable.canDispose() || force)) {
			var _this$_menuInteractio2;
			this._paragraphMenu.disposable.dispose();
			this._paragraphMenu = null;
			(_this$_menuInteractio2 = this._menuInteraction) === null || _this$_menuInteractio2 === void 0 || _this$_menuInteractio2.dispose();
			this._menuInteraction = null;
			this._isSlashMenuActive = false;
			this._slashMenuRequest$.next(null);
			this._activeTarget$.next(null);
		}
	}
	_canEditDocument(range, segmentId = "") {
		return (0, _univerjs_docs.canEditDocumentTargets)(this._permissionService, this._context.unitId, range ? (0, _univerjs_docs.getDocumentEditTargetObjectIds)(this._context.unit, segmentId, range) : []);
	}
	dispose() {
		var _this$_menuInteractio3, _this$_dragInteractio2;
		(_this$_menuInteractio3 = this._menuInteraction) === null || _this$_menuInteractio3 === void 0 || _this$_menuInteractio3.dispose();
		this._menuInteraction = null;
		(_this$_dragInteractio2 = this._dragInteraction) === null || _this$_dragInteractio2 === void 0 || _this$_dragInteractio2.dispose();
		this._dragInteraction = null;
		super.dispose();
	}
	_buildParagraphMenuTarget(paragraph) {
		var _body$paragraphs, _paragraphModel$bulle, _body$customBlocks;
		const body = this._context.unit.getBody();
		const dataStream = (body === null || body === void 0 ? void 0 : body.dataStream) ?? "";
		const cellRange = getContainingTableCellRange(dataStream, paragraph.paragraphStart);
		const inTable = cellRange != null || isParagraphInTable(this._context.unit, paragraph.paragraphStart);
		const blockRange = getParagraphBlockRange(this._context.unit, paragraph);
		if (blockRange) {
			const targetParagraph = {
				...paragraph,
				paragraphStart: blockRange.startIndex,
				paragraphEnd: blockRange.endIndex + 1,
				startIndex: blockRange.startIndex,
				blockRange
			};
			return {
				kind: _univerjs_core.DocumentBlockType.BLOCK_RANGE,
				key: `${_univerjs_core.DocumentBlockType.BLOCK_RANGE}:${blockRange.blockId}`,
				paragraph: targetParagraph,
				blockRange,
				icon: BLOCK_RANGE_ICON_MAP[blockRange.blockType],
				cellRange: cellRange ?? void 0,
				menuRange: {
					startOffset: blockRange.startIndex,
					endOffset: blockRange.endIndex + 1,
					collapsed: false
				},
				moveRange: {
					startOffset: blockRange.startIndex,
					endOffset: blockRange.endIndex + 1
				},
				emptyMode: false,
				draggable: cellRange != null || !inTable
			};
		}
		const paragraphDataStream = dataStream.slice(paragraph.paragraphStart, paragraph.paragraphEnd);
		const paragraphModel = body === null || body === void 0 || (_body$paragraphs = body.paragraphs) === null || _body$paragraphs === void 0 ? void 0 : _body$paragraphs.find((item) => item.startIndex === paragraph.startIndex);
		const listIcon = getListIcon(paragraphModel === null || paragraphModel === void 0 || (_paragraphModel$bulle = paragraphModel.bullet) === null || _paragraphModel$bulle === void 0 ? void 0 : _paragraphModel$bulle.listType);
		const isHorizontalRuleParagraph = isHorizontalLineParagraph(paragraphDataStream, paragraphModel);
		const customBlock = body === null || body === void 0 || (_body$customBlocks = body.customBlocks) === null || _body$customBlocks === void 0 ? void 0 : _body$customBlocks.find((item) => item.startIndex >= paragraph.paragraphStart && item.startIndex <= paragraph.paragraphEnd);
		const isCustomBlockOnly = (customBlock === null || customBlock === void 0 ? void 0 : customBlock.blockType) === _univerjs_core.BlockType.CUSTOM && paragraphDataStream.replace(/[\b\r\n]/g, "") === "";
		if (customBlock && customBlock.blockType === _univerjs_core.BlockType.CUSTOM && isCustomBlockOnly) return {
			kind: _univerjs_core.DocumentBlockType.CUSTOM_BLOCK,
			key: `${_univerjs_core.DocumentBlockType.CUSTOM_BLOCK}:${customBlock.blockId}`,
			paragraph,
			customBlock,
			icon: "TextTypeIcon",
			cellRange: cellRange ?? void 0,
			menuRange: {
				startOffset: customBlock.startIndex,
				endOffset: customBlock.startIndex,
				collapsed: true
			},
			moveRange: getParagraphMoveRange(this._context.unit, paragraph, cellRange),
			emptyMode: false,
			draggable: cellRange != null || !inTable
		};
		if (paragraphDataStream === "\b") return null;
		return {
			kind: _univerjs_core.DocumentBlockType.PARAGRAPH,
			key: `${_univerjs_core.DocumentBlockType.PARAGRAPH}:${paragraph.startIndex}`,
			paragraph,
			icon: isHorizontalRuleParagraph ? "ReduceIcon" : listIcon,
			cellRange: cellRange ?? void 0,
			menuRange: {
				startOffset: paragraph.paragraphStart,
				endOffset: paragraph.paragraphStart,
				collapsed: true
			},
			moveRange: getParagraphMoveRange(this._context.unit, paragraph, cellRange),
			emptyMode: isHorizontalRuleParagraph ? false : paragraphDataStream.replace(/[\r\n]/g, "") === "",
			draggable: cellRange != null || !inTable
		};
	}
	_getBodyBlockTargets() {
		const body = this._context.unit.getBody();
		if (!body) return [];
		const tableRanges = body.tables ?? [];
		const blockRanges = body.blockRanges ?? [];
		const paragraphBounds = [...this._docEventManagerService.paragraphBounds.values()];
		const targets = [];
		for (const table of tableRanges) {
			const tableBound = this._docEventManagerService.tableBounds.get(table.tableId);
			if (!tableBound) continue;
			targets.push({
				kind: _univerjs_core.DocumentBlockType.TABLE,
				key: `${_univerjs_core.DocumentBlockType.TABLE}:${table.tableId}`,
				table,
				icon: "GridIcon",
				menuRange: {
					startOffset: table.startIndex,
					endOffset: table.endIndex,
					collapsed: false
				},
				moveRange: {
					startOffset: table.startIndex,
					endOffset: table.endIndex
				},
				emptyMode: false,
				draggable: true,
				rect: tableBound.rect
			});
		}
		for (const blockRange of blockRanges) {
			const rect = unionRects(paragraphBounds.filter((bound) => Math.max(bound.paragraphStart, blockRange.startIndex) <= Math.min(bound.paragraphEnd, blockRange.endIndex)).map((bound) => bound.rect));
			if (!rect) continue;
			targets.push({
				kind: _univerjs_core.DocumentBlockType.BLOCK_RANGE,
				key: `${_univerjs_core.DocumentBlockType.BLOCK_RANGE}:${blockRange.blockId}`,
				blockRange,
				icon: BLOCK_RANGE_ICON_MAP[blockRange.blockType],
				menuRange: {
					startOffset: blockRange.startIndex,
					endOffset: blockRange.endIndex + 1,
					collapsed: false
				},
				moveRange: {
					startOffset: blockRange.startIndex,
					endOffset: blockRange.endIndex + 1
				},
				emptyMode: false,
				draggable: true,
				rect
			});
		}
		for (const paragraph of paragraphBounds) {
			if (tableRanges.some((table) => paragraph.paragraphStart > table.startIndex && paragraph.paragraphStart < table.endIndex)) continue;
			if (blockRanges.some((range) => Math.max(paragraph.paragraphStart, range.startIndex) <= Math.min(paragraph.paragraphEnd, range.endIndex))) continue;
			const target = this._buildParagraphMenuTarget(paragraph);
			if (!target) continue;
			targets.push({
				...target,
				rect: paragraph.rect
			});
		}
		return targets;
	}
	_getCellBlockTargets(cellRange) {
		const body = this._context.unit.getBody();
		if (!body) return [];
		const blockRanges = body.blockRanges ?? [];
		const paragraphBounds = [...this._docEventManagerService.paragraphBounds.values()].filter((paragraph) => paragraph.paragraphStart > cellRange.startOffset && paragraph.paragraphStart < cellRange.endOffset);
		const targets = [];
		for (const blockRange of blockRanges) {
			if (blockRange.startIndex <= cellRange.startOffset || blockRange.endIndex >= cellRange.endOffset) continue;
			const rect = unionRects(paragraphBounds.filter((bound) => Math.max(bound.paragraphStart, blockRange.startIndex) <= Math.min(bound.paragraphEnd, blockRange.endIndex)).map((bound) => bound.rect));
			if (!rect) continue;
			targets.push({
				kind: _univerjs_core.DocumentBlockType.BLOCK_RANGE,
				key: `${_univerjs_core.DocumentBlockType.BLOCK_RANGE}:${blockRange.blockId}`,
				blockRange,
				icon: BLOCK_RANGE_ICON_MAP[blockRange.blockType],
				cellRange,
				menuRange: {
					startOffset: blockRange.startIndex,
					endOffset: blockRange.endIndex + 1,
					collapsed: false
				},
				moveRange: {
					startOffset: blockRange.startIndex,
					endOffset: blockRange.endIndex + 1
				},
				emptyMode: false,
				draggable: true,
				rect
			});
		}
		for (const paragraph of paragraphBounds) {
			if (blockRanges.some((range) => Math.max(paragraph.paragraphStart, range.startIndex) <= Math.min(paragraph.paragraphEnd, range.endIndex))) continue;
			const target = this._buildParagraphMenuTarget(paragraph);
			if (!target) continue;
			targets.push({
				...target,
				rect: paragraph.rect
			});
		}
		return targets;
	}
};
DocParagraphMenuService = __decorate([
	__decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(2, (0, _univerjs_core.Inject)(DocEventManagerService)),
	__decorateParam(3, (0, _univerjs_core.Inject)(DocCanvasPopManagerService)),
	__decorateParam(4, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService)),
	__decorateParam(5, (0, _univerjs_core.Inject)(DocFloatMenuService)),
	__decorateParam(6, (0, _univerjs_core.Inject)(DocSelectionRenderService)),
	__decorateParam(7, _univerjs_core.IPermissionService),
	__decorateParam(8, (0, _univerjs_core.Inject)(DocLayoutInteractionService))
], DocParagraphMenuService);
function isParagraphInTable(documentDataModel, paragraphStart) {
	var _documentDataModel$ge;
	return (((_documentDataModel$ge = documentDataModel.getBody()) === null || _documentDataModel$ge === void 0 ? void 0 : _documentDataModel$ge.tables) ?? []).some((table) => paragraphStart > table.startIndex && paragraphStart < table.endIndex);
}
function getParagraphMoveRange(documentDataModel, paragraph, cellRange) {
	const body = documentDataModel.getBody();
	const paragraphStartOffset = body ? (0, _univerjs_core.getParagraphContentStartOffset)(body, paragraph) : 0;
	return {
		startOffset: cellRange ? Math.max(paragraphStartOffset, cellRange.startOffset + 1) : paragraphStartOffset,
		endOffset: paragraph.startIndex + 1
	};
}
function getListIcon(listType) {
	if (!listType) return;
	return LIST_ICON_MAP[listType];
}
function getContainingTableCellRange(dataStream, offset) {
	const startOffset = dataStream.lastIndexOf(_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_START, Math.max(0, offset));
	if (startOffset < 0) return null;
	if (dataStream.lastIndexOf(_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_END, Math.max(0, offset)) > startOffset) return null;
	const endOffset = dataStream.indexOf(_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_END, startOffset + 1);
	if (endOffset < 0 || offset > endOffset) return null;
	return {
		startOffset,
		endOffset
	};
}
function getTableAnchorRect(tableBound) {
	const gap = 4;
	const left = tableBound.rect.left - gap;
	const top = tableBound.rect.top - gap;
	return {
		left,
		right: left,
		top,
		bottom: top
	};
}
const TABLE_CELL_PARAGRAPH_MENU_RESIZE_SAFE_GAP = 10;
function getTableCellParagraphAnchorRect(docEventManagerService, paragraph, baseAnchor) {
	const tableParagraph = paragraph;
	if (!tableParagraph.tableId || tableParagraph.rowIndex == null || tableParagraph.colIndex == null) return baseAnchor;
	const cellBound = docEventManagerService.findTableCellBound(tableParagraph.tableId, tableParagraph.rowIndex, tableParagraph.colIndex, tableParagraph.pageIndex);
	if (!cellBound) return baseAnchor;
	const gap = tableParagraph.colIndex === 0 ? 20 : TABLE_CELL_PARAGRAPH_MENU_RESIZE_SAFE_GAP;
	const left = cellBound.rect.left - gap;
	return {
		...baseAnchor,
		left,
		right: left
	};
}
function unionRects(rects) {
	if (!rects.length) return null;
	return rects.reduce((acc, rect) => ({
		left: Math.min(acc.left, rect.left),
		right: Math.max(acc.right, rect.right),
		top: Math.min(acc.top, rect.top),
		bottom: Math.max(acc.bottom, rect.bottom)
	}));
}
function getParagraphBlockRange(documentDataModel, paragraph) {
	var _documentDataModel$ge2;
	return (((_documentDataModel$ge2 = documentDataModel.getBody()) === null || _documentDataModel$ge2 === void 0 ? void 0 : _documentDataModel$ge2.blockRanges) ?? []).find((range) => Math.max(paragraph.paragraphStart, range.startIndex) <= Math.min(paragraph.paragraphEnd, range.endIndex));
}
function getBlockRangeAnchorRect(docEventManagerService, blockRange, fallbackParagraph) {
	var _bounds$;
	const bounds = docEventManagerService.findParagraphBoundsInRange(blockRange.startIndex, blockRange.endIndex).sort((left, right) => left.firstLine.top - right.firstLine.top || left.firstLine.left - right.firstLine.left);
	const anchor = ((_bounds$ = bounds[0]) === null || _bounds$ === void 0 ? void 0 : _bounds$.firstLine) ?? fallbackParagraph.firstLine;
	const left = Math.min(...(bounds.length ? bounds : [fallbackParagraph]).map((bound) => bound.firstLine.left));
	const top = anchor.top;
	return {
		...anchor,
		left,
		right: left,
		top,
		bottom: top
	};
}

//#endregion
//#region src/views/ParagraphMenu.tsx
function getParagraphMenuPopupDirection(anchorLeft, menuWidth = 212, viewportPadding = 8, options = {}) {
	const { anchorRight = anchorLeft, direction = "ltr", viewportWidth = Number.POSITIVE_INFINITY } = options;
	const hasLeftSpace = anchorLeft - menuWidth >= viewportPadding;
	const hasRightSpace = anchorRight + menuWidth + viewportPadding <= viewportWidth;
	if (direction === "rtl") return hasRightSpace || !hasLeftSpace ? "right" : "left";
	return hasLeftSpace || !hasRightSpace ? "left" : "right";
}
const PARAGRAPH_MENU_HOVER_OPEN_DELAY = 800;
const PARAGRAPH_MENU_HOVER_HIDE_DELAY = 240;
const PARAGRAPH_MENU_HOVER_BRIDGE_EDGE_OVERLAP = 12;
const PARAGRAPH_MENU_HOVER_BRIDGE_VERTICAL_PADDING = 8;
function getParagraphMenuIconSizeClass(iconKey) {
	return iconKey === "TextTypeIcon" ? "univer-size-3" : "univer-size-4";
}
function shouldExecuteParagraphMenuMove(validatorService, context) {
	return validatorService.canMoveBlock(context);
}
function getParagraphMenuTriggerIconKey(namedStyleType) {
	switch (namedStyleType) {
		case _univerjs_core.NamedStyleType.HEADING_1: return "H1Icon";
		case _univerjs_core.NamedStyleType.HEADING_2: return "H2Icon";
		case _univerjs_core.NamedStyleType.HEADING_3: return "H3Icon";
		case _univerjs_core.NamedStyleType.HEADING_4: return "H4Icon";
		case _univerjs_core.NamedStyleType.HEADING_5: return "H5Icon";
		case _univerjs_core.NamedStyleType.TITLE: return "TitleTypeIcon";
		case _univerjs_core.NamedStyleType.SUBTITLE: return "SubtitleTypeIcon";
		case _univerjs_core.NamedStyleType.NORMAL_TEXT:
		case _univerjs_core.NamedStyleType.NAMED_STYLE_TYPE_UNSPECIFIED:
		default: return "TextTypeIcon";
	}
}
function createParagraphMenuHoverOpenScheduler(openMenu, delay = 800) {
	let openTimer = null;
	const cancel = () => {
		if (openTimer != null) {
			window.clearTimeout(openTimer);
			openTimer = null;
		}
	};
	return {
		schedule() {
			cancel();
			openTimer = window.setTimeout(() => {
				openTimer = null;
				openMenu();
			}, delay);
		},
		cancel,
		openNow() {
			cancel();
			openMenu();
		}
	};
}
function setParagraphMenuInteractionActive(docParagraphMenuService, active) {
	docParagraphMenuService === null || docParagraphMenuService === void 0 || docParagraphMenuService.setParagraphMenuActive(active);
}
function isEmptyParagraphMenuTarget(dataStream, paragraph) {
	if (!paragraph) return false;
	return dataStream.slice(paragraph.paragraphStart, paragraph.paragraphEnd).replace(/[\r\n]/g, "") === "";
}
function getParagraphMenuTargetRange(paragraph) {
	if (!paragraph) return null;
	const blockRange = paragraph.blockRange;
	if (blockRange) return {
		startOffset: blockRange.startIndex,
		endOffset: blockRange.endIndex + 1,
		collapsed: false,
		segmentId: paragraph.segmentId
	};
	return {
		startOffset: paragraph.paragraphStart,
		endOffset: paragraph.paragraphStart,
		collapsed: true,
		segmentId: paragraph.segmentId
	};
}
function getParagraphMenuHoverBridgeStyle(anchorRect, direction, edgeOverlap = PARAGRAPH_MENU_HOVER_BRIDGE_EDGE_OVERLAP, verticalPadding = PARAGRAPH_MENU_HOVER_BRIDGE_VERTICAL_PADDING) {
	if (!anchorRect) return;
	return {
		left: direction === "left" ? anchorRect.left - edgeOverlap : anchorRect.right - edgeOverlap,
		top: anchorRect.top - verticalPadding,
		width: edgeOverlap * 2,
		height: anchorRect.bottom - anchorRect.top + verticalPadding * 2
	};
}
const HEADING_COMMAND_VALUES = {
	[H1HeadingCommand.id]: _univerjs_core.NamedStyleType.HEADING_1,
	[H2HeadingCommand.id]: _univerjs_core.NamedStyleType.HEADING_2,
	[H3HeadingCommand.id]: _univerjs_core.NamedStyleType.HEADING_3,
	[H4HeadingCommand.id]: _univerjs_core.NamedStyleType.HEADING_4,
	[H5HeadingCommand.id]: _univerjs_core.NamedStyleType.HEADING_5,
	[NormalTextHeadingCommand.id]: _univerjs_core.NamedStyleType.NORMAL_TEXT,
	[TitleHeadingCommand.id]: _univerjs_core.NamedStyleType.TITLE,
	[SubtitleHeadingCommand.id]: _univerjs_core.NamedStyleType.SUBTITLE
};
const NAMED_STYLE_HEADING_COMMAND_IDS = {
	[_univerjs_core.NamedStyleType.HEADING_1]: H1HeadingCommand.id,
	[_univerjs_core.NamedStyleType.HEADING_2]: H2HeadingCommand.id,
	[_univerjs_core.NamedStyleType.HEADING_3]: H3HeadingCommand.id,
	[_univerjs_core.NamedStyleType.HEADING_4]: H4HeadingCommand.id,
	[_univerjs_core.NamedStyleType.HEADING_5]: H5HeadingCommand.id,
	[_univerjs_core.NamedStyleType.NORMAL_TEXT]: NormalTextHeadingCommand.id,
	[_univerjs_core.NamedStyleType.TITLE]: TitleHeadingCommand.id,
	[_univerjs_core.NamedStyleType.SUBTITLE]: SubtitleHeadingCommand.id
};
function getParagraphMenuActiveHeadingCommandId(namedStyleType) {
	return NAMED_STYLE_HEADING_COMMAND_IDS[namedStyleType ?? _univerjs_core.NamedStyleType.NORMAL_TEXT] ?? NormalTextHeadingCommand.id;
}
function getParagraphMenuHiddenHeadingCommandIds(namedStyleType) {
	if (namedStyleType === _univerjs_core.NamedStyleType.TITLE) return [
		H4HeadingCommand.id,
		H5HeadingCommand.id,
		SubtitleHeadingCommand.id
	];
	if (namedStyleType === _univerjs_core.NamedStyleType.SUBTITLE) return [
		H4HeadingCommand.id,
		H5HeadingCommand.id,
		TitleHeadingCommand.id
	];
	if (namedStyleType === _univerjs_core.NamedStyleType.HEADING_5) return [
		H4HeadingCommand.id,
		TitleHeadingCommand.id,
		SubtitleHeadingCommand.id
	];
	return [
		H5HeadingCommand.id,
		TitleHeadingCommand.id,
		SubtitleHeadingCommand.id
	];
}
const LIST_ICON_TO_COMMAND_ID = {
	OrderIcon: OrderListCommand.id,
	UnorderIcon: BulletListCommand.id,
	TodoListDoubleIcon: CheckListCommand.id
};
const PARAGRAPH_MENU_BLOCK_RANGE_TYPES = [
	_univerjs_core.DocumentBlockRangeType.CODE,
	_univerjs_core.DocumentBlockRangeType.QUOTE,
	_univerjs_core.DocumentBlockRangeType.CALLOUT
];
function getParagraphMenuBlockRangeCommandId(blockType) {
	return blockType ? `docs-${blockType}.command.insert` : void 0;
}
const PARAGRAPH_MENU_BLOCK_RANGE_COMMAND_IDS = new Set(PARAGRAPH_MENU_BLOCK_RANGE_TYPES.map((blockType) => getParagraphMenuBlockRangeCommandId(blockType)));
const PARAGRAPH_MENU_SELECTION_COMMAND_IDS = /* @__PURE__ */ new Set([
	DocPasteCommand.id,
	BulletListCommand.id,
	OrderListCommand.id,
	CheckListCommand.id,
	HorizontalLineCommand.id,
	SetInlineFormatTextColorCommand.id,
	ResetInlineFormatTextColorCommand.id,
	SetInlineFormatTextBackgroundColorCommand.id,
	ResetInlineFormatTextBackgroundColorCommand.id,
	...PARAGRAPH_MENU_BLOCK_RANGE_COMMAND_IDS,
	AlignLeftCommand.id,
	AlignCenterCommand.id,
	AlignRightCommand.id,
	AlignJustifyCommand.id
]);
const PARAGRAPH_MENU_SKIP_REPLACE_SELECTION_COMMAND_IDS = /* @__PURE__ */ new Set([
	DocCopyCurrentParagraphCommand.id,
	DocCutCurrentParagraphCommand.id,
	DeleteCurrentParagraphCommand.id
]);
const PARAGRAPH_MENU_RESTORE_SELECTION_COMMAND_IDS = /* @__PURE__ */ new Set([
	SetInlineFormatTextColorCommand.id,
	ResetInlineFormatTextColorCommand.id,
	SetInlineFormatTextBackgroundColorCommand.id,
	ResetInlineFormatTextBackgroundColorCommand.id
]);
function shouldRestoreParagraphMenuSelectionAfterCommand(commandId) {
	return !!commandId && PARAGRAPH_MENU_RESTORE_SELECTION_COMMAND_IDS.has(commandId);
}
function getParagraphMenuCommand(params, targetRange) {
	const commandId = params.commandId ?? params.id ?? (typeof params.label === "string" ? params.label : void 0);
	if (commandId && targetRange && commandId in HEADING_COMMAND_VALUES) return {
		commandId: SetParagraphNamedStyleCommand.id,
		params: {
			value: HEADING_COMMAND_VALUES[commandId],
			textRanges: [targetRange]
		}
	};
	if (commandId && targetRange && (commandId === BulletListCommand.id || commandId === OrderListCommand.id || commandId === CheckListCommand.id)) return {
		commandId,
		params: { docRange: [targetRange] }
	};
	if (commandId === HorizontalLineCommand.id && targetRange) return {
		commandId,
		params: { insertRange: targetRange }
	};
	const fallbackParams = typeof params.params === "function" ? params.params() : params.params;
	const commandParams = typeof params.value === "undefined" ? fallbackParams : { value: params.value };
	return {
		commandId,
		params: commandParams && typeof commandParams === "object" ? commandParams : void 0
	};
}
function getParagraphMenuResolvedCommand(option, targetRange) {
	const commandId = option.commandId ?? option.id ?? (typeof option.label === "string" ? option.label : void 0);
	if (!commandId) return {};
	return getParagraphMenuCommand({
		...option,
		commandId,
		id: option.id ?? commandId
	}, targetRange);
}
function getParagraphMenuCommandParams(commandId, commandParams, target, unitId) {
	if (commandId !== DeleteCurrentParagraphCommand.id || (target === null || target === void 0 ? void 0 : target.kind) !== "blockRange" || !target.blockRange) return commandParams;
	return {
		...commandParams ?? {},
		unitId,
		blockRange: target.blockRange
	};
}
function getParagraphMenuType(target, emptyMode) {
	if ((target === null || target === void 0 ? void 0 : target.kind) === "table") return DOC_TABLE_BLOCK_MENU_ID;
	if (emptyMode) return DOC_PARAGRAPH_T_INSERT_MENU_ID;
	if ((target === null || target === void 0 ? void 0 : target.icon) === "ReduceIcon") return DOC_PARAGRAPH_T_DIVIDER_MENU_ID;
	return DOC_PARAGRAPH_T_EDIT_MENU_ID;
}
function getParagraphMenuActiveItemIds(target, namedStyleType) {
	if ((target === null || target === void 0 ? void 0 : target.kind) === "blockRange") {
		var _target$blockRange;
		const commandId = getParagraphMenuBlockRangeCommandId((_target$blockRange = target.blockRange) === null || _target$blockRange === void 0 ? void 0 : _target$blockRange.blockType);
		return commandId && PARAGRAPH_MENU_BLOCK_RANGE_COMMAND_IDS.has(commandId) ? [commandId] : [];
	}
	const activeIds = [getParagraphMenuActiveHeadingCommandId(namedStyleType)];
	const listCommandId = (target === null || target === void 0 ? void 0 : target.icon) ? LIST_ICON_TO_COMMAND_ID[target.icon] : void 0;
	if (listCommandId) activeIds.push(listCommandId);
	return activeIds;
}
function getParagraphMenuHiddenItemIds(menuType, target, namedStyleType) {
	var _target$blockRange2;
	if (menuType === "doc.menu.paragraph-t.insert") return [];
	const hiddenIds = [...getParagraphMenuHiddenHeadingCommandIds(namedStyleType)];
	const blockType = (target === null || target === void 0 ? void 0 : target.kind) === "blockRange" ? (_target$blockRange2 = target.blockRange) === null || _target$blockRange2 === void 0 ? void 0 : _target$blockRange2.blockType : void 0;
	if ((target === null || target === void 0 ? void 0 : target.kind) !== "blockRange") hiddenIds.push(DocPasteCommand.id);
	if (blockType === _univerjs_core.DocumentBlockRangeType.CALLOUT) hiddenIds.push(getParagraphMenuBlockRangeCommandId(_univerjs_core.DocumentBlockRangeType.CODE), getParagraphMenuBlockRangeCommandId(_univerjs_core.DocumentBlockRangeType.QUOTE));
	else if (blockType === _univerjs_core.DocumentBlockRangeType.QUOTE || blockType === _univerjs_core.DocumentBlockRangeType.CODE) hiddenIds.push(getParagraphMenuBlockRangeCommandId(_univerjs_core.DocumentBlockRangeType.CALLOUT));
	return hiddenIds;
}
function getEndTokenOffset(body, blockRange) {
	return body.dataStream[blockRange.endIndex] === _univerjs_core.DataStreamTreeTokenType.BLOCK_END ? blockRange.endIndex : body.dataStream[blockRange.endIndex + 1] === _univerjs_core.DataStreamTreeTokenType.BLOCK_END ? blockRange.endIndex + 1 : blockRange.endIndex;
}
function shiftPointRangesAfterDelete(ranges, offset, length) {
	const endOffset = offset + length;
	return ranges === null || ranges === void 0 ? void 0 : ranges.map((range) => {
		if (range.startIndex >= offset && range.startIndex < endOffset) return null;
		return range.startIndex >= endOffset ? {
			...range,
			startIndex: range.startIndex - length
		} : range;
	}).filter((range) => range != null);
}
function shiftTextRunsAfterDelete(runs, offset, length) {
	const endOffset = offset + length;
	return runs === null || runs === void 0 ? void 0 : runs.map((run) => {
		if (run.st >= offset && run.ed < endOffset) return null;
		if (run.st >= endOffset) return {
			...run,
			st: run.st - length,
			ed: run.ed - length
		};
		if (run.ed >= offset) return {
			...run,
			st: run.st < offset ? run.st : offset,
			ed: Math.max(offset, run.ed - length)
		};
		return run;
	}).filter((run) => run != null);
}
function shiftIndexRangesAfterDelete(ranges, offset, length) {
	const endOffset = offset + length;
	return ranges === null || ranges === void 0 ? void 0 : ranges.map((range) => {
		if (range.startIndex >= offset && range.endIndex < endOffset) return null;
		if (range.startIndex >= endOffset) return {
			...range,
			startIndex: range.startIndex - length,
			endIndex: range.endIndex - length
		};
		if (range.endIndex >= offset) return {
			...range,
			startIndex: range.startIndex < offset ? range.startIndex : offset,
			endIndex: Math.max(offset, range.endIndex - length)
		};
		return range;
	}).filter((range) => range != null);
}
function deleteBodyText(body, startOffset, endOffset) {
	const length = endOffset - startOffset;
	body.dataStream = `${body.dataStream.slice(0, startOffset)}${body.dataStream.slice(endOffset)}`;
	body.paragraphs = shiftPointRangesAfterDelete(body.paragraphs, startOffset, length);
	body.sectionBreaks = shiftPointRangesAfterDelete(body.sectionBreaks, startOffset, length);
	body.customBlocks = shiftPointRangesAfterDelete(body.customBlocks, startOffset, length);
	body.textRuns = shiftTextRunsAfterDelete(body.textRuns, startOffset, length);
	body.tables = shiftIndexRangesAfterDelete(body.tables, startOffset, length);
	body.customRanges = shiftIndexRangesAfterDelete(body.customRanges, startOffset, length);
	body.customDecorations = shiftIndexRangesAfterDelete(body.customDecorations, startOffset, length);
	body.blockRanges = shiftIndexRangesAfterDelete(body.blockRanges, startOffset, length);
}
function stripBlockParagraphStyle(style, blockType) {
	const nextStyle = { ...style };
	if (blockType === _univerjs_core.DocumentBlockRangeType.QUOTE) {
		delete nextStyle.indentStart;
		delete nextStyle.spaceAbove;
		delete nextStyle.spaceBelow;
		return nextStyle;
	}
	if (blockType === _univerjs_core.DocumentBlockRangeType.CODE) {
		var _nextStyle$textStyle, _nextStyle$textStyle2;
		delete nextStyle.indentStart;
		delete nextStyle.indentEnd;
		delete nextStyle.spaceAbove;
		delete nextStyle.spaceBelow;
		if (((_nextStyle$textStyle = nextStyle.textStyle) === null || _nextStyle$textStyle === void 0 ? void 0 : _nextStyle$textStyle.ff) === "monospace" && ((_nextStyle$textStyle2 = nextStyle.textStyle) === null || _nextStyle$textStyle2 === void 0 ? void 0 : _nextStyle$textStyle2.fs) === 12) delete nextStyle.textStyle;
		return nextStyle;
	}
	if (blockType === _univerjs_core.DocumentBlockRangeType.CALLOUT) {
		delete nextStyle.indentStart;
		delete nextStyle.indentEnd;
		delete nextStyle.spaceAbove;
		delete nextStyle.spaceBelow;
		return nextStyle;
	}
	return nextStyle;
}
function unwrapBlockRangeBody(documentBody, blockRange) {
	var _body$blockRanges;
	const body = _univerjs_core.Tools.deepClone(documentBody);
	const endTokenOffset = getEndTokenOffset(body, blockRange);
	(body.paragraphs ?? []).filter((paragraph) => paragraph.startIndex > blockRange.startIndex && paragraph.startIndex < endTokenOffset).forEach((paragraph) => {
		paragraph.paragraphStyle = stripBlockParagraphStyle(paragraph.paragraphStyle, blockRange.blockType);
	});
	deleteBodyText(body, endTokenOffset, endTokenOffset + 1);
	deleteBodyText(body, blockRange.startIndex, blockRange.startIndex + 1);
	body.blockRanges = (_body$blockRanges = body.blockRanges) === null || _body$blockRanges === void 0 ? void 0 : _body$blockRanges.filter((range) => range.blockId !== blockRange.blockId);
	return {
		body,
		range: {
			startOffset: blockRange.startIndex,
			endOffset: Math.max(blockRange.startIndex, endTokenOffset - 1),
			collapsed: false
		}
	};
}
const UNWRAP_BLOCK_BODY_PATCH_KEYS = [
	"paragraphs",
	"sectionBreaks",
	"textRuns",
	"customRanges",
	"customDecorations",
	"customBlocks",
	"tables",
	"columnGroups",
	"blockRanges"
];
function buildUnwrapBlockRangeActions(previousBody, nextBody, blockRange) {
	const endTokenOffset = getEndTokenOffset(previousBody, blockRange);
	const textX = new _univerjs_core.TextX();
	textX.retain(blockRange.startIndex);
	textX.delete(1);
	textX.retain(Math.max(0, endTokenOffset - blockRange.startIndex - 1));
	textX.delete(1);
	const textAction = _univerjs_core.JSONX.getInstance().editOp(textX.serialize(), ["body"]);
	const intermediateBody = _univerjs_core.Tools.deepClone(previousBody);
	_univerjs_core.TextX.apply(intermediateBody, textX.serialize());
	const rawActions = [textAction];
	collectUnwrapBlockBodyPatchActions(intermediateBody, nextBody, rawActions);
	return rawActions.reduce((acc, action) => _univerjs_core.JSONX.compose(acc, action), null);
}
function collectUnwrapBlockBodyPatchActions(previousBody, nextBody, actions) {
	for (const key of UNWRAP_BLOCK_BODY_PATCH_KEYS) collectUnwrapPatchActions(_univerjs_core.JSONX.getInstance(), ["body", key], previousBody[key], nextBody[key], actions);
}
function collectUnwrapPatchActions(jsonX, path, oldValue, newValue, actions) {
	if (isEmptyArrayEquivalent(oldValue, newValue) || _univerjs_core.Tools.diffValue(oldValue, newValue)) return;
	if (oldValue == null) {
		actions.push(jsonX.insertOp(path, newValue));
		return;
	}
	if (newValue == null) {
		actions.push(jsonX.removeOp(path, oldValue));
		return;
	}
	if (Array.isArray(oldValue) && Array.isArray(newValue)) {
		collectUnwrapArrayPatchActions(jsonX, path, oldValue, newValue, actions);
		return;
	}
	if (isPlainObject(oldValue) && isPlainObject(newValue)) {
		(/* @__PURE__ */ new Set([...Object.keys(oldValue), ...Object.keys(newValue)])).forEach((key) => collectUnwrapPatchActions(jsonX, [...path, key], oldValue[key], newValue[key], actions));
		return;
	}
	actions.push(jsonX.replaceOp(path, oldValue, newValue));
}
function collectUnwrapArrayPatchActions(jsonX, path, oldItems, newItems, actions) {
	if (oldItems.length === newItems.length) {
		oldItems.forEach((item, index) => collectUnwrapPatchActions(jsonX, [...path, index], item, newItems[index], actions));
		return;
	}
	let prefix = 0;
	while (prefix < oldItems.length && prefix < newItems.length && _univerjs_core.Tools.diffValue(oldItems[prefix], newItems[prefix])) prefix++;
	let oldSuffix = oldItems.length - 1;
	let newSuffix = newItems.length - 1;
	while (oldSuffix >= prefix && newSuffix >= prefix && _univerjs_core.Tools.diffValue(oldItems[oldSuffix], newItems[newSuffix])) {
		oldSuffix--;
		newSuffix--;
	}
	for (let index = oldSuffix; index >= prefix; index--) actions.push(jsonX.removeOp([...path, index], oldItems[index]));
	for (let index = prefix; index <= newSuffix; index++) actions.push(jsonX.insertOp([...path, index], newItems[index]));
}
function isPlainObject(value) {
	return value != null && typeof value === "object" && !Array.isArray(value);
}
function isEmptyArrayEquivalent(oldValue, newValue) {
	return oldValue == null && Array.isArray(newValue) && newValue.length === 0 || newValue == null && Array.isArray(oldValue) && oldValue.length === 0;
}
function getTargetSelectionRange(target, paragraph) {
	if (!target) return getParagraphMenuTargetRange(paragraph);
	return {
		...target.menuRange,
		segmentId: paragraph === null || paragraph === void 0 ? void 0 : paragraph.segmentId
	};
}
function getParagraphFormattingRange(target, paragraph) {
	if ((target === null || target === void 0 ? void 0 : target.kind) === "blockRange") return getBlockSelectionRange(target, paragraph);
	if (paragraph) return {
		startOffset: paragraph.paragraphStart,
		endOffset: paragraph.paragraphEnd,
		collapsed: false,
		segmentId: paragraph.segmentId
	};
	return getTargetSelectionRange(target, paragraph);
}
function getParagraphMenuCommandTargetRange(commandId, targetRange, formattingRange) {
	if (commandId === DocPasteCommand.id) return targetRange;
	if (commandId && PARAGRAPH_MENU_SELECTION_COMMAND_IDS.has(commandId)) return formattingRange ?? targetRange;
	return targetRange ?? formattingRange;
}
function finishParagraphMenuCommand(docParagraphMenuService, layoutService, hideMenu) {
	docParagraphMenuService === null || docParagraphMenuService === void 0 || docParagraphMenuService.hideParagraphMenu(true);
	hideMenu();
	layoutService.focus();
}
function getBlockSelectionRange(target, paragraph) {
	if ((target === null || target === void 0 ? void 0 : target.kind) !== "blockRange") return getTargetSelectionRange(target, paragraph);
	const blockRange = target.blockRange;
	if (!blockRange) return getTargetSelectionRange(target, paragraph);
	return {
		startOffset: blockRange.startIndex,
		endOffset: blockRange.endIndex + 1,
		collapsed: false,
		segmentId: paragraph === null || paragraph === void 0 ? void 0 : paragraph.segmentId
	};
}
function getBlockRangeClipboardTargetRange(commandId, target, paragraph) {
	if ((target === null || target === void 0 ? void 0 : target.kind) !== "blockRange") return null;
	if (commandId === DocPasteCommand.id) return {
		startOffset: target.moveRange.endOffset,
		endOffset: target.moveRange.endOffset,
		collapsed: true,
		segmentId: paragraph === null || paragraph === void 0 ? void 0 : paragraph.segmentId
	};
	if (commandId === DocCopyCurrentParagraphCommand.id || commandId === DocCutCurrentParagraphCommand.id) return getBlockSelectionRange(target, paragraph ?? void 0);
	return null;
}
function ParagraphMenuBase({ popup, tableBlockOnly = false }) {
	var _doc$getBody, _paragraphObj$paragra;
	const [visible, setVisible] = (0, react.useState)(false);
	const [openMode, setOpenMode] = (0, react.useState)("pointer");
	const [anchorRect, setAnchorRect] = (0, react.useState)(null);
	const [dropRect, setDropRect] = (0, react.useState)(null);
	const [menuDirection, setMenuDirection] = (0, react.useState)("left");
	const targetRangeRef = (0, react.useRef)(null);
	const dragTargetOffsetRef = (0, react.useRef)(null);
	const dragRangeRef = (0, react.useRef)(null);
	const isDraggingRef = (0, react.useRef)(false);
	const openMenuRef = (0, react.useRef)(() => void 0);
	const hoverOpenSchedulerRef = (0, react.useRef)(createParagraphMenuHoverOpenScheduler(() => openMenuRef.current()));
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const themeService = (0, _univerjs_ui.useDependency)(_univerjs_core.ThemeService);
	const docSelectionManagerService = (0, _univerjs_ui.useDependency)(_univerjs_docs.DocSelectionManagerService);
	const direction = (0, _univerjs_ui.useObservable)(localeService.direction$, "ltr");
	const docClipboardService = (0, _univerjs_ui.useDependency)(IDocClipboardService);
	const docContentInsertService = (0, _univerjs_ui.useDependency)(_univerjs_docs.DocContentInsertService);
	const docBlockMoveValidatorService = (0, _univerjs_ui.useDependency)(_univerjs_docs.DocBlockMoveValidatorService);
	const layoutService = (0, _univerjs_ui.useDependency)(_univerjs_ui.ILayoutService);
	const iconManager = (0, _univerjs_ui.useDependency)(_univerjs_ui.IconManager);
	const anchorRef = (0, react.useRef)(null);
	const isMouseOver = (0, react.useRef)(false);
	const hideTimerRef = (0, react.useRef)(null);
	const handledSlashRequestNonceRef = (0, react.useRef)(0);
	const renderManagerService = (0, _univerjs_ui.useDependency)(_univerjs_engine_render.IRenderManagerService);
	const univerInstanceService = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService);
	const renderUnit = renderManagerService.getRenderUnitById(popup.unitId);
	const doc = univerInstanceService.getUnit(popup.unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
	const docParagraphMenuService = renderUnit === null || renderUnit === void 0 ? void 0 : renderUnit.with(DocParagraphMenuService);
	const docEventManagerService = renderUnit === null || renderUnit === void 0 ? void 0 : renderUnit.with(DocEventManagerService);
	const activeTarget = (0, _univerjs_ui.useObservable)(docParagraphMenuService === null || docParagraphMenuService === void 0 ? void 0 : docParagraphMenuService.activeTarget$);
	const slashMenuRequest = (0, _univerjs_ui.useObservable)(docParagraphMenuService === null || docParagraphMenuService === void 0 ? void 0 : docParagraphMenuService.slashMenuRequest$);
	const paragraph = (0, _univerjs_ui.useObservable)(docEventManagerService === null || docEventManagerService === void 0 ? void 0 : docEventManagerService.hoverParagraph$);
	const paragraphLeft = (0, _univerjs_ui.useObservable)(docEventManagerService === null || docEventManagerService === void 0 ? void 0 : docEventManagerService.hoverParagraphLeft$);
	const rawActiveTarget = activeTarget ?? (docParagraphMenuService === null || docParagraphMenuService === void 0 ? void 0 : docParagraphMenuService.activeTarget);
	const currentActiveTarget = tableBlockOnly ? (rawActiveTarget === null || rawActiveTarget === void 0 ? void 0 : rawActiveTarget.kind) === "table" ? rawActiveTarget : null : (rawActiveTarget === null || rawActiveTarget === void 0 ? void 0 : rawActiveTarget.kind) === "table" ? null : rawActiveTarget;
	const activeParagraphBound = (currentActiveTarget === null || currentActiveTarget === void 0 ? void 0 : currentActiveTarget.paragraph) ?? (docParagraphMenuService === null || docParagraphMenuService === void 0 ? void 0 : docParagraphMenuService.activeParagraph) ?? paragraph ?? paragraphLeft;
	const startIndex = activeParagraphBound === null || activeParagraphBound === void 0 ? void 0 : activeParagraphBound.startIndex;
	const dataStream = (doc === null || doc === void 0 || (_doc$getBody = doc.getBody()) === null || _doc$getBody === void 0 ? void 0 : _doc$getBody.dataStream) ?? "";
	const paragraphObj = (0, react.useMemo)(() => {
		var _doc$getBody2;
		return doc === null || doc === void 0 || (_doc$getBody2 = doc.getBody()) === null || _doc$getBody2 === void 0 || (_doc$getBody2 = _doc$getBody2.paragraphs) === null || _doc$getBody2 === void 0 ? void 0 : _doc$getBody2.find((p) => p.startIndex === startIndex);
	}, [doc, startIndex]);
	const isEmptyParagraph = (currentActiveTarget === null || currentActiveTarget === void 0 ? void 0 : currentActiveTarget.emptyMode) ?? isEmptyParagraphMenuTarget(dataStream, activeParagraphBound);
	const namedStyleType = paragraphObj === null || paragraphObj === void 0 || (_paragraphObj$paragra = paragraphObj.paragraphStyle) === null || _paragraphObj$paragra === void 0 ? void 0 : _paragraphObj$paragra.namedStyleType;
	const targetIconKey = (currentActiveTarget === null || currentActiveTarget === void 0 ? void 0 : currentActiveTarget.icon) ?? getParagraphMenuTriggerIconKey(namedStyleType);
	const TargetIcon = iconManager.get(targetIconKey);
	const paragraphMenuType = (0, react.useMemo)(() => getParagraphMenuType(currentActiveTarget, isEmptyParagraph), [currentActiveTarget, isEmptyParagraph]);
	const paragraphMenuActiveItemIds = (0, react.useMemo)(() => getParagraphMenuActiveItemIds(currentActiveTarget, namedStyleType), [currentActiveTarget, namedStyleType]);
	const paragraphMenuHiddenItemIds = (0, react.useMemo)(() => (currentActiveTarget === null || currentActiveTarget === void 0 ? void 0 : currentActiveTarget.kind) === "table" ? [] : getParagraphMenuHiddenItemIds(paragraphMenuType, currentActiveTarget, namedStyleType), [
		currentActiveTarget,
		namedStyleType,
		paragraphMenuType
	]);
	const anchorRect$ = (0, react.useMemo)(() => new rxjs.BehaviorSubject({
		left: 0,
		right: 0,
		top: 0,
		bottom: 0
	}), []);
	const updateAnchorRect = () => {
		var _anchorRef$current, _anchorRef$current2;
		const boundingRect = (_anchorRef$current = anchorRef.current) === null || _anchorRef$current === void 0 ? void 0 : _anchorRef$current.getBoundingClientRect();
		const left = ((boundingRect === null || boundingRect === void 0 ? void 0 : boundingRect.left) ?? 0) - 4;
		const right = (boundingRect === null || boundingRect === void 0 ? void 0 : boundingRect.right) ?? 0;
		const viewportWidth = ((_anchorRef$current2 = anchorRef.current) === null || _anchorRef$current2 === void 0 || (_anchorRef$current2 = _anchorRef$current2.ownerDocument.defaultView) === null || _anchorRef$current2 === void 0 ? void 0 : _anchorRef$current2.innerWidth) ?? window.innerWidth;
		setMenuDirection(getParagraphMenuPopupDirection(left, 212, 8, {
			anchorRight: right,
			direction,
			viewportWidth
		}));
		const nextAnchorRect = {
			left,
			right,
			top: (boundingRect === null || boundingRect === void 0 ? void 0 : boundingRect.top) ?? 0,
			bottom: (boundingRect === null || boundingRect === void 0 ? void 0 : boundingRect.bottom) ?? 0
		};
		setAnchorRect(nextAnchorRect);
		anchorRect$.next(nextAnchorRect);
	};
	const handleHideMenu = () => {
		setVisible(false);
		setOpenMode("pointer");
		targetRangeRef.current = null;
		setParagraphMenuInteractionActive(docParagraphMenuService, false);
	};
	const clearHideTimer = () => {
		if (hideTimerRef.current != null) {
			window.clearTimeout(hideTimerRef.current);
			hideTimerRef.current = null;
		}
	};
	const scheduleHideMenu = () => {
		clearHideTimer();
		hideTimerRef.current = window.setTimeout(() => {
			if (!isMouseOver.current && !isDraggingRef.current) handleHideMenu();
		}, PARAGRAPH_MENU_HOVER_HIDE_DELAY);
	};
	const handleOpenMenu = (mode = "pointer") => {
		clearHideTimer();
		const latestTarget = (docParagraphMenuService === null || docParagraphMenuService === void 0 ? void 0 : docParagraphMenuService.activeTarget) ?? activeTarget;
		setParagraphMenuInteractionActive(docParagraphMenuService, true);
		const targetRange = latestTarget ? {
			...latestTarget.menuRange,
			segmentId: activeParagraphBound === null || activeParagraphBound === void 0 ? void 0 : activeParagraphBound.segmentId
		} : getParagraphMenuTargetRange(activeParagraphBound);
		targetRangeRef.current = targetRange;
		updateAnchorRect();
		setOpenMode(mode);
		setVisible(true);
	};
	openMenuRef.current = handleOpenMenu;
	const handleOpenSlashMenu = (request) => {
		var _request$target$parag;
		clearHideTimer();
		targetRangeRef.current = {
			...request.target.menuRange,
			segmentId: ((_request$target$parag = request.target.paragraph) === null || _request$target$parag === void 0 ? void 0 : _request$target$parag.segmentId) ?? (activeParagraphBound === null || activeParagraphBound === void 0 ? void 0 : activeParagraphBound.segmentId)
		};
		setMenuDirection("vertical");
		setAnchorRect(request.anchorRect);
		anchorRect$.next(request.anchorRect);
		setOpenMode("slash");
		setVisible(true);
	};
	const scheduleOpenMenu = () => {
		clearHideTimer();
		hoverOpenSchedulerRef.current.schedule();
	};
	const cancelOpenMenu = () => {
		hoverOpenSchedulerRef.current.cancel();
	};
	const replaceSelection = (range) => {
		if (!range) return;
		docSelectionManagerService.replaceDocRanges([range], void 0, false);
	};
	const getCurrentTextRangesSnapshot = () => {
		return (docSelectionManagerService.getTextRanges() ?? []).map((range) => ({ ...range }));
	};
	const restoreTextRanges = (ranges) => {
		if (!ranges) return;
		docSelectionManagerService.replaceDocRanges(ranges, void 0, false);
	};
	const executeResolvedCommand = (option, targetRange) => {
		const resolved = getParagraphMenuResolvedCommand(option, targetRange);
		if (!resolved.commandId) return false;
		return commandService.executeCommand(resolved.commandId, resolved.params);
	};
	const unwrapActiveBlockRange = async () => {
		const latestTarget = (docParagraphMenuService === null || docParagraphMenuService === void 0 ? void 0 : docParagraphMenuService.activeTarget) ?? activeTarget;
		if (!latestTarget || latestTarget.kind !== "blockRange" || !latestTarget.blockRange || !(doc === null || doc === void 0 ? void 0 : doc.getBody())) return null;
		const previousBody = doc.getBody();
		const { body, range } = unwrapBlockRangeBody(previousBody, latestTarget.blockRange);
		const actions = buildUnwrapBlockRangeActions(previousBody, body, latestTarget.blockRange);
		const segmentId = (activeParagraphBound === null || activeParagraphBound === void 0 ? void 0 : activeParagraphBound.segmentId) ?? "";
		if (!await commandService.executeCommand(_univerjs_docs.RichTextEditingMutation.id, {
			unitId: popup.unitId,
			actions,
			textRanges: [{
				...range,
				segmentId
			}]
		})) return null;
		const nextRange = {
			...range,
			segmentId
		};
		replaceSelection(nextRange);
		targetRangeRef.current = nextRange;
		return nextRange;
	};
	const executeParagraphMenuOption = async (option) => {
		const latestTarget = (docParagraphMenuService === null || docParagraphMenuService === void 0 ? void 0 : docParagraphMenuService.activeTarget) ?? activeTarget;
		const targetRange = getTargetSelectionRange(latestTarget, activeParagraphBound ?? void 0);
		const blockRange = getBlockSelectionRange(latestTarget, activeParagraphBound ?? void 0);
		const formattingRange = getParagraphFormattingRange(latestTarget, activeParagraphBound ?? void 0);
		const commandId = option.commandId ?? option.id ?? (typeof option.label === "string" ? option.label : void 0);
		const commandParams = typeof option.params === "function" ? option.params() : option.params;
		if (!commandId) return;
		if (commandId === "doc.menu.paragraph-t.reset-colors") {
			if (formattingRange) replaceSelection(formattingRange);
			await commandService.executeCommand(SetInlineFormatTextColorCommand.id, { value: themeService.getColorFromTheme("gray.900") });
			await commandService.executeCommand(SetInlineFormatTextBackgroundColorCommand.id, { value: themeService.getColorFromTheme("primary.600") });
			finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
			return;
		}
		if (commandId === "doc.menu.paragraph-t.indent.increase" || commandId === "doc.menu.paragraph-t.indent.decrease") {
			var _paragraphObj$paragra2;
			if (formattingRange) replaceSelection(formattingRange);
			const currentIndent = (paragraphObj === null || paragraphObj === void 0 || (_paragraphObj$paragra2 = paragraphObj.paragraphStyle) === null || _paragraphObj$paragra2 === void 0 || (_paragraphObj$paragra2 = _paragraphObj$paragra2.indentFirstLine) === null || _paragraphObj$paragra2 === void 0 ? void 0 : _paragraphObj$paragra2.v) ?? 0;
			const delta = commandId === "doc.menu.paragraph-t.indent.increase" ? 20 : -20;
			await commandService.executeCommand(DocParagraphSettingCommand.id, { paragraph: { indentFirstLine: { v: Math.max(0, currentIndent + delta) } } });
			finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
			return;
		}
		if (commandId === "doc.menu.paragraph-t.insert-below.command") {
			if (!(latestTarget === null || latestTarget === void 0 ? void 0 : latestTarget.moveRange)) return;
			docContentInsertService.setInsertRange({
				unitId: popup.unitId,
				startOffset: latestTarget.moveRange.endOffset,
				endOffset: latestTarget.moveRange.endOffset,
				segmentId: activeParagraphBound === null || activeParagraphBound === void 0 ? void 0 : activeParagraphBound.segmentId
			});
			const wrappedCommandId = typeof (commandParams === null || commandParams === void 0 ? void 0 : commandParams.commandId) === "string" ? commandParams.commandId : void 0;
			if (wrappedCommandId) await commandService.executeCommand(wrappedCommandId);
			finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
			return;
		}
		if ((latestTarget === null || latestTarget === void 0 ? void 0 : latestTarget.kind) === "blockRange") {
			var _latestTarget$blockRa;
			const currentBlockCommandId = getParagraphMenuBlockRangeCommandId((_latestTarget$blockRa = latestTarget.blockRange) === null || _latestTarget$blockRa === void 0 ? void 0 : _latestTarget$blockRa.blockType);
			const clipboardRange = getBlockRangeClipboardTargetRange(commandId, latestTarget, activeParagraphBound);
			if (clipboardRange) {
				if (commandId === DocCopyCurrentParagraphCommand.id) await docClipboardService.copy(_univerjs_core.SliceBodyType.copy, [clipboardRange]);
				else if (commandId === DocCutCurrentParagraphCommand.id) await docClipboardService.cut([clipboardRange]);
				else {
					replaceSelection(clipboardRange);
					await executeResolvedCommand(option, clipboardRange);
				}
				finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
				return;
			}
			if (commandId === currentBlockCommandId) {
				await unwrapActiveBlockRange();
				finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
				return;
			}
			if (commandId === NormalTextHeadingCommand.id || commandId in HEADING_COMMAND_VALUES || commandId === BulletListCommand.id || commandId === OrderListCommand.id || commandId === CheckListCommand.id) {
				const nextRange = await unwrapActiveBlockRange();
				if (commandId !== NormalTextHeadingCommand.id) await executeResolvedCommand({
					...option,
					commandId,
					params: commandParams
				}, nextRange);
				finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
				return;
			}
			if (PARAGRAPH_MENU_BLOCK_RANGE_COMMAND_IDS.has(commandId)) {
				if (blockRange) replaceSelection(blockRange);
				await executeResolvedCommand({
					...option,
					commandId,
					params: commandParams
				}, blockRange);
				finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
				return;
			}
		}
		if (commandId === getParagraphMenuActiveHeadingCommandId(namedStyleType) && commandId !== NormalTextHeadingCommand.id) {
			if (targetRange) replaceSelection(targetRange);
			await executeResolvedCommand({ id: NormalTextHeadingCommand.id }, targetRange);
			finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
			return;
		}
		const previousTextRanges = shouldRestoreParagraphMenuSelectionAfterCommand(commandId) ? getCurrentTextRangesSnapshot() : null;
		if (commandId && shouldUseInsertBelowRange(commandId, option) && (latestTarget === null || latestTarget === void 0 ? void 0 : latestTarget.moveRange)) docContentInsertService.setInsertRange({
			unitId: popup.unitId,
			startOffset: latestTarget.moveRange.endOffset,
			endOffset: latestTarget.moveRange.endOffset,
			segmentId: (targetRange === null || targetRange === void 0 ? void 0 : targetRange.segmentId) ?? (activeParagraphBound === null || activeParagraphBound === void 0 ? void 0 : activeParagraphBound.segmentId) ?? ""
		});
		else if (PARAGRAPH_MENU_SELECTION_COMMAND_IDS.has(commandId) && !PARAGRAPH_MENU_SKIP_REPLACE_SELECTION_COMMAND_IDS.has(commandId)) replaceSelection(getParagraphMenuCommandTargetRange(commandId, targetRange, formattingRange));
		try {
			await executeResolvedCommand({
				...option,
				commandId,
				params: getParagraphMenuCommandParams(commandId, commandParams, latestTarget, popup.unitId)
			}, getParagraphMenuCommandTargetRange(commandId, targetRange, formattingRange));
		} finally {
			restoreTextRanges(previousTextRanges);
		}
		finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
	};
	const hoverBridgeStyle = visible && (menuDirection === "left" || menuDirection === "right") ? getParagraphMenuHoverBridgeStyle(anchorRect, menuDirection) : void 0;
	const resolvedParagraphMenuType = openMode === "slash" ? DOC_PARAGRAPH_T_INSERT_MENU_ID : paragraphMenuType;
	(0, react.useEffect)(() => () => {
		if (hideTimerRef.current != null) {
			window.clearTimeout(hideTimerRef.current);
			hideTimerRef.current = null;
		}
		hoverOpenSchedulerRef.current.cancel();
		setParagraphMenuInteractionActive(docParagraphMenuService, false);
	}, []);
	(0, react.useEffect)(() => {
		if (!slashMenuRequest || handledSlashRequestNonceRef.current >= slashMenuRequest.nonce) return;
		handledSlashRequestNonceRef.current = slashMenuRequest.nonce;
		hoverOpenSchedulerRef.current.cancel();
		clearHideTimer();
		isMouseOver.current = false;
		handleOpenSlashMenu(slashMenuRequest);
	}, [slashMenuRequest]);
	if (!currentActiveTarget) return null;
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)(react_jsx_runtime.Fragment, { children: [
		/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			"data-u-comp": "paragraph-menu",
			ref: anchorRef,
			className: (0, _univerjs_design.clsx)("univer-mr-1 univer-inline-flex univer-h-7 univer-cursor-pointer univer-items-center univer-gap-1 univer-rounded-md univer-border univer-border-gray-200 univer-bg-gray-0 univer-px-2 univer-py-0 univer-shadow-sm univer-transition-colors hover:univer-bg-gray-50 hover:univer-shadow-md dark:!univer-border-gray-700 dark:!univer-bg-gray-900 dark:hover:!univer-bg-gray-800", { "univer-bg-gray-100 univer-shadow-md dark:!univer-bg-gray-800": visible }),
			onMouseEnter: (e) => {
				var _popup$onPointerEnter;
				(_popup$onPointerEnter = popup.onPointerEnter) === null || _popup$onPointerEnter === void 0 || _popup$onPointerEnter.call(popup, e);
				setParagraphMenuInteractionActive(docParagraphMenuService, true);
				isMouseOver.current = true;
				scheduleOpenMenu();
			},
			onMouseLeave: () => {
				isMouseOver.current = false;
				setParagraphMenuInteractionActive(docParagraphMenuService, false);
				cancelOpenMenu();
				scheduleHideMenu();
			},
			onClick: (event) => {
				event.preventDefault();
				event.stopPropagation();
				isMouseOver.current = true;
				hoverOpenSchedulerRef.current.openNow();
			},
			children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(TargetIcon, { className: (0, _univerjs_design.clsx)(getParagraphMenuIconSizeClass(targetIconKey), "univer-shrink-0 univer-text-gray-700 dark:!univer-text-gray-0") }), (currentActiveTarget === null || currentActiveTarget === void 0 ? void 0 : currentActiveTarget.draggable) && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
				type: "button",
				className: "univer-group univer-flex univer-h-4 univer-w-2.5 univer-cursor-grab univer-items-center univer-justify-center univer-border-none univer-bg-transparent univer-p-0 active:univer-cursor-grabbing",
				"aria-label": localeService.t("docs-ui.doc.blockMenu.dragBlock"),
				title: localeService.t("docs-ui.doc.blockMenu.dragBlock"),
				onPointerDown: (event) => {
					var _event$currentTarget$, _event$currentTarget;
					const latestTarget = (docParagraphMenuService === null || docParagraphMenuService === void 0 ? void 0 : docParagraphMenuService.activeTarget) ?? activeTarget;
					const moveRange = latestTarget === null || latestTarget === void 0 ? void 0 : latestTarget.moveRange;
					if (!moveRange || !(latestTarget === null || latestTarget === void 0 ? void 0 : latestTarget.draggable)) return;
					event.preventDefault();
					event.stopPropagation();
					(_event$currentTarget$ = (_event$currentTarget = event.currentTarget).setPointerCapture) === null || _event$currentTarget$ === void 0 || _event$currentTarget$.call(_event$currentTarget, event.pointerId);
					clearHideTimer();
					isMouseOver.current = true;
					isDraggingRef.current = true;
					docParagraphMenuService === null || docParagraphMenuService === void 0 || docParagraphMenuService.setBlockMenuDragging(true);
					dragRangeRef.current = moveRange;
					dragTargetOffsetRef.current = null;
					setDropRect(null);
					const pointerId = event.pointerId;
					const handlePointerMove = (moveEvent) => {
						if (moveEvent.pointerId !== pointerId) return;
						moveEvent.preventDefault();
						const range = dragRangeRef.current;
						if (!range) return;
						const target = docParagraphMenuService === null || docParagraphMenuService === void 0 ? void 0 : docParagraphMenuService.getDropTargetFromClientPoint(moveEvent.clientX, moveEvent.clientY, range);
						dragTargetOffsetRef.current = (target === null || target === void 0 ? void 0 : target.targetOffset) ?? null;
						setDropRect((target === null || target === void 0 ? void 0 : target.rect) ?? null);
					};
					const finishDrag = (shouldDrop) => {
						window.removeEventListener("pointermove", handlePointerMove);
						window.removeEventListener("pointerup", handlePointerUp);
						window.removeEventListener("pointercancel", handlePointerCancel);
						window.removeEventListener("blur", handleWindowBlur);
						const range = dragRangeRef.current;
						const targetOffset = dragTargetOffsetRef.current;
						dragRangeRef.current = null;
						dragTargetOffsetRef.current = null;
						isDraggingRef.current = false;
						docParagraphMenuService === null || docParagraphMenuService === void 0 || docParagraphMenuService.setBlockMenuDragging(false);
						setDropRect(null);
						if (shouldDrop && range && targetOffset != null && shouldExecuteParagraphMenuMove(docBlockMoveValidatorService, {
							unitId: popup.unitId,
							sourceRange: range,
							targetOffset
						})) commandService.executeCommand(MoveDocBlockCommand.id, {
							unitId: popup.unitId,
							sourceRange: range,
							targetOffset
						});
					};
					const handlePointerUp = (upEvent) => {
						if (upEvent.pointerId !== pointerId) return;
						upEvent.preventDefault();
						finishDrag(true);
					};
					const handlePointerCancel = (cancelEvent) => {
						if (cancelEvent.pointerId !== pointerId) return;
						finishDrag(false);
					};
					const handleWindowBlur = () => {
						finishDrag(false);
					};
					window.addEventListener("pointermove", handlePointerMove);
					window.addEventListener("pointerup", handlePointerUp);
					window.addEventListener("pointercancel", handlePointerCancel);
					window.addEventListener("blur", handleWindowBlur, { once: true });
				},
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(DragHandleDotsIcon, {})
			})]
		}),
		visible && hoverBridgeStyle && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
			"aria-hidden": "true",
			className: "univer-fixed univer-z-[1019] univer-bg-transparent",
			style: hoverBridgeStyle,
			onMouseEnter: (e) => {
				var _popup$onPointerEnter2;
				(_popup$onPointerEnter2 = popup.onPointerEnter) === null || _popup$onPointerEnter2 === void 0 || _popup$onPointerEnter2.call(popup, e);
				setParagraphMenuInteractionActive(docParagraphMenuService, true);
				isMouseOver.current = true;
				clearHideTimer();
			},
			onMouseLeave: () => {
				isMouseOver.current = false;
				setParagraphMenuInteractionActive(docParagraphMenuService, false);
				scheduleHideMenu();
			}
		}),
		visible && /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_ui.RectPopup, {
			portal: true,
			anchorRect$,
			direction: menuDirection,
			children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("section", {
				onMouseEnter: (e) => {
					var _popup$onPointerEnter3;
					(_popup$onPointerEnter3 = popup.onPointerEnter) === null || _popup$onPointerEnter3 === void 0 || _popup$onPointerEnter3.call(popup, e);
					setParagraphMenuInteractionActive(docParagraphMenuService, true);
					isMouseOver.current = true;
					clearHideTimer();
				},
				onMouseLeave: () => {
					isMouseOver.current = false;
					setParagraphMenuInteractionActive(docParagraphMenuService, false);
					scheduleHideMenu();
				},
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_ui.ContextMenuPanel, {
					className: "univer-w-[212px]",
					menuType: resolvedParagraphMenuType,
					flowQuickGroups: true,
					activeItemIds: (currentActiveTarget === null || currentActiveTarget === void 0 ? void 0 : currentActiveTarget.kind) === "table" ? void 0 : paragraphMenuActiveItemIds,
					hiddenItemIds: openMode === "slash" || (currentActiveTarget === null || currentActiveTarget === void 0 ? void 0 : currentActiveTarget.kind) === "table" ? void 0 : paragraphMenuHiddenItemIds,
					autoFocus: openMode === "slash",
					autoFocusTarget: openMode === "slash" ? "container" : void 0,
					suppressHoverUntilPointerMove: openMode === "slash",
					onMenuPointerEnter: () => {
						setParagraphMenuInteractionActive(docParagraphMenuService, true);
						isMouseOver.current = true;
						clearHideTimer();
					},
					onMenuPointerLeave: () => {
						isMouseOver.current = false;
						setParagraphMenuInteractionActive(docParagraphMenuService, false);
						scheduleHideMenu();
					},
					onCancel: () => {
						finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
					},
					onOptionSelect: async (params) => {
						const targetRange = targetRangeRef.current ?? getParagraphMenuTargetRange(activeParagraphBound);
						const { commandId, params: commandParams } = getParagraphMenuCommand(params, targetRange);
						const latestTarget = (docParagraphMenuService === null || docParagraphMenuService === void 0 ? void 0 : docParagraphMenuService.activeTarget) ?? activeTarget;
						if ((latestTarget === null || latestTarget === void 0 ? void 0 : latestTarget.kind) === "table" && commandId && targetRange) {
							const tableRange = {
								...targetRange,
								segmentId: targetRange.segmentId ?? "",
								collapsed: false
							};
							const afterTableRange = {
								startOffset: latestTarget.moveRange.endOffset,
								endOffset: latestTarget.moveRange.endOffset,
								collapsed: true,
								segmentId: targetRange.segmentId ?? ""
							};
							if (commandId === DocCopyCommand.id || commandId === DocCopyCommand.name) {
								await docClipboardService.copy(_univerjs_core.SliceBodyType.copy, [tableRange]);
								finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
								return;
							}
							if (commandId === DocPasteCommand.id) {
								docSelectionManagerService.replaceDocRanges([afterTableRange], void 0, false);
								await commandService.executeCommand(DocPasteCommand.id);
								finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
								return;
							}
							if (commandId === DocTableDeleteTableCommand.id) docSelectionManagerService.replaceDocRanges([tableRange], void 0, false);
							else if (params.id === "doc.menu.insert-bellow" || commandId !== "doc.menu.insert-bellow") docSelectionManagerService.replaceDocRanges([afterTableRange], void 0, false);
							if (commandService && commandId) commandService.executeCommand(commandId, commandId === DocTableDeleteTableCommand.id ? {
								...commandParams,
								targetRange: tableRange
							} : commandParams);
							finishParagraphMenuCommand(docParagraphMenuService, layoutService, handleHideMenu);
							return;
						}
						await executeParagraphMenuOption(params);
					}
				})
			})
		}),
		dropRect && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
			className: "univer-pointer-events-none univer-fixed univer-z-[10000] univer-h-0.5 univer-rounded-full univer-bg-primary-600",
			style: {
				left: dropRect.left,
				top: dropRect.top,
				width: Math.max(60, dropRect.right - dropRect.left)
			}
		})
	] });
}
const ParagraphMenu = ({ popup }) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphMenuBase, { popup });
const TableBlockMenu = ({ popup }) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(ParagraphMenuBase, {
	popup,
	tableBlockOnly: true
});
function shouldUseInsertBelowRange(commandId, params) {
	if (params.id === "doc.menu.insert-bellow") return true;
	if (typeof params.id === "string" && params.id.toLowerCase().includes("below")) return true;
	const normalized = commandId.toLowerCase();
	if (normalized.includes("insert") && (normalized.includes("below") || normalized.includes("bellow"))) return true;
	if (normalized.includes("insert") && normalized.includes("image")) return true;
	if (normalized.includes("table") && normalized.includes("create")) return true;
	return normalized === "doc.command.create-table" || normalized === "doc.operation.create-table" || normalized === "doc.menu.paragraph-t.insert-below.command";
}
const DRAG_HANDLE_DOTS = [
	"top-left",
	"top-right",
	"middle-left",
	"middle-right",
	"bottom-left",
	"bottom-right"
];
function DragHandleDotsIcon() {
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
		className: "univer-grid univer-h-3.5 univer-w-2 univer-grid-cols-2 univer-place-items-center univer-gap-x-0.5 univer-gap-y-px",
		"aria-hidden": "true",
		children: DRAG_HANDLE_DOTS.map((key) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { className: "univer-size-0.5 univer-rounded-full univer-bg-gray-400 univer-transition-colors group-hover:univer-bg-gray-500 dark:!univer-bg-gray-500 dark:group-hover:!univer-bg-gray-300" }, key))
	});
}

//#endregion
//#region src/views/permission/DocObjectPermissionEntry.tsx
function DocObjectPermissionEntry({ unitId, id, objectType, segmentId = "" }) {
	const injector = (0, _univerjs_ui.useInjector)();
	const instances = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService);
	const permissions = (0, _univerjs_ui.useDependency)(_univerjs_core.ObjectPermissionService);
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	(0, _univerjs_ui.useObservable)(permissions.changed$, 0);
	(0, _univerjs_ui.useObservable)(localeService.currentLocale$);
	if (!unitId || !id) return null;
	const isParagraph = objectType === _univerjs_protocol.UnitObject.DocumentParagraph;
	const target = {
		unitId,
		objectId: isParagraph ? (0, _univerjs_docs.getDocumentParagraphPermissionObjectId)(segmentId, id) : (0, _univerjs_docs.getDocumentSectionPermissionObjectId)(segmentId, id),
		objectType
	};
	const exists = () => {
		var _instances$getUnit, _body$paragraphs, _body$sectionBreaks;
		const body = (_instances$getUnit = instances.getUnit(unitId)) === null || _instances$getUnit === void 0 || (_instances$getUnit = _instances$getUnit.getSelfOrHeaderFooterModel(segmentId)) === null || _instances$getUnit === void 0 ? void 0 : _instances$getUnit.getBody();
		return isParagraph ? (body === null || body === void 0 || (_body$paragraphs = body.paragraphs) === null || _body$paragraphs === void 0 ? void 0 : _body$paragraphs.some((paragraph) => paragraph.paragraphId === id)) === true : (body === null || body === void 0 || (_body$sectionBreaks = body.sectionBreaks) === null || _body$sectionBreaks === void 0 ? void 0 : _body$sectionBreaks.some((section) => section.sectionId === id)) === true;
	};
	if (!permissions.supports(target) || !exists()) return null;
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		className: "univer-mt-5",
		children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Separator, { className: "univer-mb-3" }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Button, {
			disabled: !permissions.canView(target),
			type: "button",
			variant: "text",
			className: "univer-w-full univer-justify-start",
			onClick: () => (0, _univerjs_ui.openObjectPermissionDialog)(injector, {
				target,
				name: localeService.t(isParagraph ? "docs-ui.objectPermission.paragraph" : "docs-ui.objectPermission.section"),
				commandId: _univerjs_docs.SetDocumentPermissionCommand.id,
				exists
			}),
			children: localeService.t("docs-ui.objectPermission.title")
		})]
	});
}

//#endregion
//#region src/views/section-setting/use-section-setting.ts
const DEFAULT_SECTION_COLUMN_GAP = 18;
function getCommonValue(values) {
	const first = values[0];
	return values.every((value) => value === first) ? first : void 0;
}
function getSectionColumnCount(section) {
	var _section$columnProper;
	return ((_section$columnProper = section.columnProperties) === null || _section$columnProper === void 0 ? void 0 : _section$columnProper.length) || 1;
}
function getSectionColumnGap(section) {
	var _section$columnProper2;
	return ((_section$columnProper2 = section.columnProperties) === null || _section$columnProper2 === void 0 || (_section$columnProper2 = _section$columnProper2[0]) === null || _section$columnProper2 === void 0 ? void 0 : _section$columnProper2.paddingEnd) ?? DEFAULT_SECTION_COLUMN_GAP;
}
function getSectionSettingValues(sections, documentStyle) {
	return {
		columnCount: getCommonValue(sections.map(getSectionColumnCount)),
		columnGap: getCommonValue(sections.map(getSectionColumnGap)),
		separatorType: getCommonValue(sections.map((section) => section.columnSeparatorType ?? _univerjs_core.ColumnSeparatorType.NONE)),
		sectionType: getCommonValue(sections.map((section) => section.sectionType ?? _univerjs_core.SectionType.SECTION_TYPE_UNSPECIFIED)),
		pageWidth: getCommonValue(sections.map((section) => {
			var _section$pageSize, _documentStyle$pageSi;
			return ((_section$pageSize = section.pageSize) === null || _section$pageSize === void 0 ? void 0 : _section$pageSize.width) ?? ((_documentStyle$pageSi = documentStyle.pageSize) === null || _documentStyle$pageSi === void 0 ? void 0 : _documentStyle$pageSi.width);
		})),
		pageHeight: getCommonValue(sections.map((section) => {
			var _section$pageSize2, _documentStyle$pageSi2;
			return ((_section$pageSize2 = section.pageSize) === null || _section$pageSize2 === void 0 ? void 0 : _section$pageSize2.height) ?? ((_documentStyle$pageSi2 = documentStyle.pageSize) === null || _documentStyle$pageSi2 === void 0 ? void 0 : _documentStyle$pageSi2.height);
		})),
		pageOrient: getCommonValue(sections.map((section) => section.pageOrient ?? documentStyle.pageOrient)),
		marginTop: getCommonValue(sections.map((section) => section.marginTop ?? documentStyle.marginTop ?? 0)),
		marginBottom: getCommonValue(sections.map((section) => section.marginBottom ?? documentStyle.marginBottom ?? 0)),
		marginLeft: getCommonValue(sections.map((section) => section.marginLeft ?? documentStyle.marginLeft ?? 0)),
		marginRight: getCommonValue(sections.map((section) => section.marginRight ?? documentStyle.marginRight ?? 0)),
		pageNumberStart: getCommonValue(sections.map((section) => section.pageNumberStart ?? documentStyle.pageNumberStart ?? 1))
	};
}
function createSectionColumnUpdates(sections, documentStyle, change) {
	return sections.map((section) => {
		const columnCount = "columnCount" in change ? change.columnCount : getSectionColumnCount(section);
		const columnGap = "columnGap" in change ? change.columnGap : getSectionColumnGap(section);
		return {
			sectionId: section.sectionId,
			config: { columnProperties: (0, _univerjs_docs.createSectionColumnProperties)(documentStyle, section, columnCount, columnGap) }
		};
	});
}
function createSectionOrientationUpdates(sections, documentStyle, pageOrient) {
	return sections.map((section) => {
		const currentOrient = section.pageOrient ?? documentStyle.pageOrient ?? _univerjs_core.PageOrientType.PORTRAIT;
		const currentPageSize = section.pageSize ?? documentStyle.pageSize;
		return {
			sectionId: section.sectionId,
			config: {
				pageOrient,
				...currentOrient !== pageOrient && (currentPageSize === null || currentPageSize === void 0 ? void 0 : currentPageSize.width) != null && currentPageSize.height != null ? { pageSize: {
					width: currentPageSize.height,
					height: currentPageSize.width
				} } : {}
			}
		};
	});
}
function getSelectedSections(documentDataModel, ranges) {
	const body = documentDataModel.getBody();
	if (!body || ranges.length === 0 || ranges.some((range) => Boolean(range.segmentId))) return [];
	const sections = (0, _univerjs_docs.getTopLevelSectionBreaks)(body);
	return sections.filter((section, index) => {
		const sectionStart = index === 0 ? 0 : sections[index - 1].startIndex + 1;
		return ranges.some((range) => {
			const rangeStart = Math.min(range.startOffset, range.endOffset);
			const rangeEnd = Math.max(range.startOffset, range.endOffset);
			return rangeStart <= section.startIndex && rangeEnd >= sectionStart;
		});
	});
}
function navigateToSection(controller, documentDataModel, sections, sectionId) {
	const index = sections.findIndex((section) => section.sectionId === sectionId);
	if (!documentDataModel || index < 0) return;
	controller.navigateToSectionEnd(documentDataModel.getUnitId(), index === 0 ? 0 : sections[index - 1].startIndex + 1, sections[index].startIndex);
}
function useSectionSetting() {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const instanceService = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService);
	const selectionManager = (0, _univerjs_ui.useDependency)(_univerjs_docs.DocSelectionManagerService);
	const controller = (0, _univerjs_ui.useDependency)(DocSectionSettingController);
	const documentDataModel = (0, _univerjs_ui.useObservable)((0, react.useMemo)(() => instanceService.getCurrentTypeOfUnit$(_univerjs_core.UniverInstanceType.UNIVER_DOC), [instanceService]));
	const resolveSections = () => (documentDataModel === null || documentDataModel === void 0 ? void 0 : documentDataModel.getDocumentStyle().documentFlavor) === _univerjs_core.DocumentFlavor.TRADITIONAL ? getSelectedSections(documentDataModel, selectionManager.getDocRanges()) : [];
	const sections = (0, _univerjs_ui.useObservable)(documentDataModel ? () => (0, rxjs.combineLatest)([selectionManager.textSelection$.pipe((0, rxjs.startWith)(void 0)), documentDataModel.change$.pipe((0, rxjs.startWith)(void 0))]).pipe((0, rxjs.map)(resolveSections)) : null, resolveSections(), false, [documentDataModel, selectionManager]);
	const documentStyle = (documentDataModel === null || documentDataModel === void 0 ? void 0 : documentDataModel.getDocumentStyle()) ?? {};
	const values = getSectionSettingValues(sections, documentStyle);
	const body = documentDataModel === null || documentDataModel === void 0 ? void 0 : documentDataModel.getBody();
	const allSections = (documentDataModel === null || documentDataModel === void 0 ? void 0 : documentDataModel.getDocumentStyle().documentFlavor) === _univerjs_core.DocumentFlavor.TRADITIONAL && body ? (0, _univerjs_docs.getTopLevelSectionBreaks)(body) : [];
	const update = (updates) => {
		if (!documentDataModel || updates.length === 0) return Promise.resolve(false);
		return commandService.executeCommand(_univerjs_docs.UpdateDocumentSectionCommand.id, {
			unitId: documentDataModel.getUnitId(),
			updates
		});
	};
	const updateAll = (config) => update(sections.map((section) => ({
		sectionId: section.sectionId,
		config
	})));
	return {
		unitId: documentDataModel === null || documentDataModel === void 0 ? void 0 : documentDataModel.getUnitId(),
		valid: Boolean(documentDataModel && sections.length > 0),
		selectedCount: sections.length,
		sectionOptions: allSections.map((section, index) => ({
			label: `#${index + 1}`,
			value: section.sectionId
		})),
		selectedSectionId: sections.length === 1 ? sections[0].sectionId : void 0,
		...values,
		selectSection(sectionId) {
			navigateToSection(controller, documentDataModel ?? void 0, allSections, sectionId);
		},
		setColumnCount(value) {
			const next = Math.max(1, Math.round(value));
			return update(documentDataModel ? createSectionColumnUpdates(sections, documentDataModel.getDocumentStyle(), { columnCount: next }) : []);
		},
		setColumnGap(value) {
			const next = Math.max(0, value);
			return update(documentDataModel ? createSectionColumnUpdates(sections, documentDataModel.getDocumentStyle(), { columnGap: next }) : []);
		},
		setSeparatorType(value) {
			return updateAll({ columnSeparatorType: value });
		},
		setSectionType(value) {
			return updateAll({ sectionType: value });
		},
		setPageWidth(value) {
			return update(sections.map((section) => ({
				sectionId: section.sectionId,
				config: { pageSize: {
					...documentStyle.pageSize,
					...section.pageSize,
					width: Math.max(1, value)
				} }
			})));
		},
		setPageHeight(value) {
			return update(sections.map((section) => ({
				sectionId: section.sectionId,
				config: { pageSize: {
					...documentStyle.pageSize,
					...section.pageSize,
					height: Math.max(1, value)
				} }
			})));
		},
		setPageOrient(value) {
			return update(createSectionOrientationUpdates(sections, documentStyle, value));
		},
		setMarginTop(value) {
			return updateAll({ marginTop: Math.max(0, value) });
		},
		setMarginBottom(value) {
			return updateAll({ marginBottom: Math.max(0, value) });
		},
		setMarginLeft(value) {
			return updateAll({ marginLeft: Math.max(0, value) });
		},
		setMarginRight(value) {
			return updateAll({ marginRight: Math.max(0, value) });
		},
		setPageNumberStart(value) {
			return updateAll({ pageNumberStart: Math.max(1, Math.round(value)) });
		}
	};
}

//#endregion
//#region src/views/section-setting/Setting.tsx
function SettingRow(props) {
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		className: "univer-grid univer-min-h-8 univer-items-center univer-gap-3",
		style: { gridTemplateColumns: "minmax(0, 1fr) minmax(140px, 160px)" },
		children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			className: "univer-min-w-0 univer-text-xs univer-leading-5 univer-text-gray-900 dark:!univer-text-gray-100",
			children: [props.label, props.unit]
		}), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
			className: "univer-w-full univer-min-w-0",
			children: props.children
		})]
	});
}
function SectionSetting() {
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const controller = (0, _univerjs_ui.useDependency)(DocSectionSettingController);
	const setting = useSectionSetting();
	(0, _univerjs_ui.useObservable)(localeService.currentLocale$);
	(0, react.useEffect)(() => {
		if (!setting.valid) controller.closePanel();
	}, [controller, setting.valid]);
	if (!setting.valid) return null;
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		className: "univer-box-border univer-w-full",
		children: [
			setting.selectedCount > 1 && /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
				className: "univer-mb-4 univer-text-xs univer-text-gray-600 dark:!univer-text-gray-300",
				children: localeService.t("docs-ui.doc.sectionSetting.selectedSections", `${setting.selectedCount}`)
			}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
				className: "univer-grid univer-gap-3",
				children: [
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.doc.slider.sectionSetting"),
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Select, {
							className: "univer-w-full",
							value: setting.selectedSectionId ?? "",
							options: [...setting.selectedSectionId == null ? [{
								label: localeService.t("docs-ui.doc.sectionSetting.multipleValues"),
								value: "",
								disabled: true
							}] : [], ...setting.sectionOptions],
							onChange: setting.selectSection
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.doc.sectionSetting.columnCount"),
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							"aria-label": localeService.t("docs-ui.doc.sectionSetting.columnCount"),
							className: "univer-w-full",
							min: 1,
							max: 12,
							step: 1,
							precision: 0,
							value: setting.columnCount,
							onChange: (value) => value != null && setting.setColumnCount(value)
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.doc.sectionSetting.columnGap"),
						unit: " (px)",
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							"aria-label": localeService.t("docs-ui.doc.sectionSetting.columnGap"),
							className: "univer-w-full",
							min: 0,
							max: 1e3,
							step: 1,
							precision: 1,
							value: setting.columnGap,
							onChange: (value) => value != null && setting.setColumnGap(value)
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.doc.sectionSetting.columnSeparator"),
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Select, {
							className: "univer-w-full",
							value: setting.separatorType == null ? "" : `${setting.separatorType}`,
							options: [
								...setting.separatorType == null ? [{
									label: localeService.t("docs-ui.doc.sectionSetting.multipleValues"),
									value: "",
									disabled: true
								}] : [],
								{
									label: localeService.t("docs-ui.doc.sectionSetting.none"),
									value: `${_univerjs_core.ColumnSeparatorType.NONE}`
								},
								{
									label: localeService.t("docs-ui.doc.sectionSetting.betweenColumns"),
									value: `${_univerjs_core.ColumnSeparatorType.BETWEEN_EACH_COLUMN}`
								}
							],
							onChange: (value) => setting.setSeparatorType(Number(value))
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.doc.sectionSetting.sectionStart"),
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Select, {
							className: "univer-w-full",
							value: setting.sectionType == null ? "" : `${setting.sectionType}`,
							options: [
								...setting.sectionType == null ? [{
									label: localeService.t("docs-ui.doc.sectionSetting.multipleValues"),
									value: "",
									disabled: true
								}] : [],
								{
									label: localeService.t("docs-ui.doc.sectionSetting.unspecified"),
									value: `${_univerjs_core.SectionType.SECTION_TYPE_UNSPECIFIED}`
								},
								{
									label: localeService.t("docs-ui.doc.sectionSetting.continuous"),
									value: `${_univerjs_core.SectionType.CONTINUOUS}`
								},
								{
									label: localeService.t("docs-ui.doc.sectionSetting.nextColumn"),
									value: `${_univerjs_core.SectionType.NEXT_COLUMN}`
								},
								{
									label: localeService.t("docs-ui.doc.sectionSetting.nextPage"),
									value: `${_univerjs_core.SectionType.NEXT_PAGE}`
								},
								{
									label: localeService.t("docs-ui.doc.sectionSetting.evenPage"),
									value: `${_univerjs_core.SectionType.EVEN_PAGE}`
								},
								{
									label: localeService.t("docs-ui.doc.sectionSetting.oddPage"),
									value: `${_univerjs_core.SectionType.ODD_PAGE}`
								}
							],
							onChange: (value) => setting.setSectionType(Number(value))
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
						className: "univer-pt-2 univer-text-sm univer-font-medium",
						children: localeService.t("docs-ui.doc.sectionSetting.pageSetup")
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.doc.sectionSetting.pageWidth"),
						unit: " (px)",
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							"aria-label": localeService.t("docs-ui.doc.sectionSetting.pageWidth"),
							className: "univer-w-full",
							min: 1,
							step: 1,
							precision: 1,
							value: setting.pageWidth,
							onChange: (value) => value != null && setting.setPageWidth(value)
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.doc.sectionSetting.pageHeight"),
						unit: " (px)",
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							"aria-label": localeService.t("docs-ui.doc.sectionSetting.pageHeight"),
							className: "univer-w-full",
							min: 1,
							step: 1,
							precision: 1,
							value: setting.pageHeight,
							onChange: (value) => value != null && setting.setPageHeight(value)
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.page-settings.orientation"),
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.Select, {
							className: "univer-w-full",
							value: setting.pageOrient == null ? "" : `${setting.pageOrient}`,
							options: [
								...setting.pageOrient == null ? [{
									label: localeService.t("docs-ui.doc.sectionSetting.multipleValues"),
									value: "",
									disabled: true
								}] : [],
								{
									label: localeService.t("docs-ui.page-settings.portrait"),
									value: `${_univerjs_core.PageOrientType.PORTRAIT}`
								},
								{
									label: localeService.t("docs-ui.page-settings.landscape"),
									value: `${_univerjs_core.PageOrientType.LANDSCAPE}`
								}
							],
							onChange: (value) => setting.setPageOrient(Number(value))
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.page-settings.top"),
						unit: " (px)",
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							"aria-label": localeService.t("docs-ui.page-settings.top"),
							className: "univer-w-full",
							min: 0,
							step: 1,
							precision: 1,
							value: setting.marginTop,
							onChange: (value) => value != null && setting.setMarginTop(value)
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.page-settings.bottom"),
						unit: " (px)",
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							"aria-label": localeService.t("docs-ui.page-settings.bottom"),
							className: "univer-w-full",
							min: 0,
							step: 1,
							precision: 1,
							value: setting.marginBottom,
							onChange: (value) => value != null && setting.setMarginBottom(value)
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.page-settings.left"),
						unit: " (px)",
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							"aria-label": localeService.t("docs-ui.page-settings.left"),
							className: "univer-w-full",
							min: 0,
							step: 1,
							precision: 1,
							value: setting.marginLeft,
							onChange: (value) => value != null && setting.setMarginLeft(value)
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.page-settings.right"),
						unit: " (px)",
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							"aria-label": localeService.t("docs-ui.page-settings.right"),
							className: "univer-w-full",
							min: 0,
							step: 1,
							precision: 1,
							value: setting.marginRight,
							onChange: (value) => value != null && setting.setMarginRight(value)
						})
					}),
					/* @__PURE__ */ (0, react_jsx_runtime.jsx)(SettingRow, {
						label: localeService.t("docs-ui.doc.sectionSetting.pageNumberStart"),
						children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
							"aria-label": localeService.t("docs-ui.doc.sectionSetting.pageNumberStart"),
							className: "univer-w-full",
							min: 1,
							step: 1,
							precision: 0,
							value: setting.pageNumberStart,
							onChange: (value) => value != null && setting.setPageNumberStart(value)
						})
					})
				]
			}),
			/* @__PURE__ */ (0, react_jsx_runtime.jsx)(DocObjectPermissionEntry, {
				unitId: setting.unitId,
				id: setting.selectedSectionId,
				objectType: _univerjs_protocol.UnitObject.DocumentSection
			})
		]
	});
}

//#endregion
//#region src/views/section-setting/index.tsx
function SectionSettingIndex() {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const [key, setKey] = (0, react.useState)(() => (0, _univerjs_core.generateRandomId)(4));
	(0, react.useEffect)(() => {
		const disposable = commandService.onCommandExecuted((info) => {
			if (info.id === _univerjs_docs.SetTextSelectionsOperation.id) {
				if (info.params.ranges.length > 0) setKey((0, _univerjs_core.generateRandomId)(4));
			} else if (info.id === _univerjs_docs.RichTextEditingMutation.id) setKey((0, _univerjs_core.generateRandomId)(4));
		});
		return () => disposable.dispose();
	}, [commandService]);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(SectionSetting, {}, key);
}

//#endregion
//#region src/views/table/create/TableCreate.tsx
const DocCreateTableConfirm = ({ handleRowColChange, tableCreateParams }) => {
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const [rowCount, setRowCount] = (0, react.useState)(3);
	const [colCount, setColCount] = (0, react.useState)(5);
	function handleInputChange(rowCount, colCount) {
		setRowCount(rowCount);
		setColCount(colCount);
		handleRowColChange(rowCount, colCount);
	}
	(0, react.useEffect)(() => {
		setRowCount(tableCreateParams.rowCount);
		setColCount(tableCreateParams.colCount);
	}, [tableCreateParams]);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
		className: "univer-flex univer-items-center univer-justify-between univer-gap-2",
		children: [/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			className: "univer-flex univer-items-center univer-gap-2",
			children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { children: localeService.t("docs-ui.toolbar.table.rowCount") }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
				className: "univer-w-28",
				min: 1,
				max: 20,
				precision: 0,
				value: rowCount,
				onChange: (val) => {
					handleInputChange(val, colCount);
				}
			})]
		}), /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			className: "univer-flex univer-items-center univer-gap-2",
			children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", { children: localeService.t("docs-ui.toolbar.table.colCount") }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_design.InputNumber, {
				className: "univer-w-28",
				min: 1,
				max: 20,
				precision: 0,
				value: colCount,
				onChange: (val) => {
					handleInputChange(rowCount, val);
				}
			})]
		})]
	});
};

//#endregion
//#region src/controllers/components.controller.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const paragraphSettingIndexKey = "doc_ui_paragraph-setting-panel";
let ComponentsController = class ComponentsController extends _univerjs_core.Disposable {
	constructor(_componentManager, _iconManager) {
		super();
		this._componentManager = _componentManager;
		this._iconManager = _iconManager;
		this._registerComponents();
		this._registerParts();
		this._registerIcons();
	}
	_registerParts() {
		const componentManager = this._componentManager;
		this.disposeWithMe(componentManager.register(COMPONENT_DOC_CREATE_TABLE_CONFIRM, DocCreateTableConfirm));
	}
	_registerIcons() {
		this.disposeWithMe(this._iconManager.register({
			AlignTextBothIcon: _univerjs_icons.AlignTextBothIcon,
			AddImageIcon: _univerjs_icons.AddImageIcon,
			BoldIcon: _univerjs_icons.BoldIcon,
			CopyDoubleIcon: _univerjs_icons.CopyDoubleIcon,
			ColumnIcon: _univerjs_icons.ColumnIcon,
			CutIcon: _univerjs_icons.CutIcon,
			DeleteColumnDoubleIcon: _univerjs_icons.DeleteColumnDoubleIcon,
			DeleteIcon: _univerjs_icons.DeleteIcon,
			DeleteRowDoubleIcon: _univerjs_icons.DeleteRowDoubleIcon,
			DeleteTableDoubleIcon: _univerjs_icons.DeleteTableDoubleIcon,
			DocSettingIcon: _univerjs_icons.DocSettingIcon,
			FontColorDoubleIcon: _univerjs_icons.FontColorDoubleIcon,
			GridIcon: _univerjs_icons.GridIcon,
			H1Icon: _univerjs_icons.H1Icon,
			H2Icon: _univerjs_icons.H2Icon,
			H3Icon: _univerjs_icons.H3Icon,
			H4Icon: _univerjs_icons.H4Icon,
			H5Icon: _univerjs_icons.H5Icon,
			HeaderFooterIcon: _univerjs_icons.HeaderFooterIcon,
			HorizontallyIcon: _univerjs_icons.HorizontallyIcon,
			InsertDoubleIcon: _univerjs_icons.InsertDoubleIcon,
			InsertRowAboveDoubleIcon: _univerjs_icons.InsertRowAboveDoubleIcon,
			InsertRowBelowDoubleIcon: _univerjs_icons.InsertRowBelowDoubleIcon,
			ItalicIcon: _univerjs_icons.ItalicIcon,
			KeyboardIcon: _univerjs_icons.KeyboardIcon,
			LeftInsertColumnDoubleIcon: _univerjs_icons.LeftInsertColumnDoubleIcon,
			LeftJustifyingIcon: _univerjs_icons.LeftJustifyingIcon,
			LineIndentDecreaseIcon: _univerjs_icons.LineIndentDecreaseIcon,
			LineIndentIncreaseIcon: _univerjs_icons.LineIndentIncreaseIcon,
			MenuIcon: _univerjs_icons.MenuIcon,
			TextTypeIcon: _univerjs_icons.TextTypeIcon,
			NoColorDoubleIcon: _univerjs_icons.NoColorDoubleIcon,
			OrderIcon: _univerjs_icons.OrderIcon,
			PaintBucketDoubleIcon: _univerjs_icons.PaintBucketDoubleIcon,
			PasteSpecialDoubleIcon: _univerjs_icons.PasteSpecialDoubleIcon,
			ReduceDoubleIcon: _univerjs_icons.ReduceDoubleIcon,
			ReduceIcon: _univerjs_icons.ReduceIcon,
			RightInsertColumnDoubleIcon: _univerjs_icons.RightInsertColumnDoubleIcon,
			RightJustifyingIcon: _univerjs_icons.RightJustifyingIcon,
			ShapeIcon: _univerjs_icons.ShapeIcon,
			SmileIcon: _univerjs_icons.SmileIcon,
			StrikethroughIcon: _univerjs_icons.StrikethroughIcon,
			SubscriptIcon: _univerjs_icons.SubscriptIcon,
			SuperscriptIcon: _univerjs_icons.SuperscriptIcon,
			SymbolsIcon: _univerjs_icons.SymbolsIcon,
			TitleTypeIcon,
			SubtitleTypeIcon,
			TodoListDoubleIcon: _univerjs_icons.TodoListDoubleIcon,
			UnderlineIcon: _univerjs_icons.UnderlineIcon,
			UnorderIcon: _univerjs_icons.UnorderIcon,
			DefaultTextColorIcon,
			HeaderTextColorIcon,
			MoreRightIcon: _univerjs_icons.MoreRightIcon,
			MoreLeftIcon: _univerjs_icons.MoreLeftIcon,
			"DocParagraphTextColorSwatchIcon.0": DocParagraphTextColorSwatchIcon0,
			"DocParagraphTextColorSwatchIcon.1": DocParagraphTextColorSwatchIcon1,
			"DocParagraphTextColorSwatchIcon.2": DocParagraphTextColorSwatchIcon2,
			"DocParagraphTextColorSwatchIcon.3": DocParagraphTextColorSwatchIcon3,
			"DocParagraphTextColorSwatchIcon.4": DocParagraphTextColorSwatchIcon4,
			"DocParagraphTextColorSwatchIcon.5": DocParagraphTextColorSwatchIcon5,
			"DocParagraphTextColorSwatchIcon.6": DocParagraphTextColorSwatchIcon6,
			"DocParagraphBackgroundColorSwatchIcon.0": DocParagraphBackgroundColorSwatchIcon0,
			"DocParagraphBackgroundColorSwatchIcon.1": DocParagraphBackgroundColorSwatchIcon1,
			"DocParagraphBackgroundColorSwatchIcon.2": DocParagraphBackgroundColorSwatchIcon2,
			"DocParagraphBackgroundColorSwatchIcon.3": DocParagraphBackgroundColorSwatchIcon3,
			"DocParagraphBackgroundColorSwatchIcon.4": DocParagraphBackgroundColorSwatchIcon4,
			"DocParagraphBackgroundColorSwatchIcon.5": DocParagraphBackgroundColorSwatchIcon5,
			"DocParagraphBackgroundColorSwatchIcon.6": DocParagraphBackgroundColorSwatchIcon6,
			"DocParagraphBackgroundColorSwatchIcon.7": DocParagraphBackgroundColorSwatchIcon7,
			"DocParagraphBackgroundColorSwatchIcon.8": DocParagraphBackgroundColorSwatchIcon8,
			"DocParagraphBackgroundColorSwatchIcon.9": DocParagraphBackgroundColorSwatchIcon9,
			"DocParagraphBackgroundColorSwatchIcon.10": DocParagraphBackgroundColorSwatchIcon10,
			"DocParagraphBackgroundColorSwatchIcon.11": DocParagraphBackgroundColorSwatchIcon11,
			"DocParagraphBackgroundColorSwatchIcon.12": DocParagraphBackgroundColorSwatchIcon12,
			"DocParagraphBackgroundColorSwatchIcon.13": DocParagraphBackgroundColorSwatchIcon13,
			"DocParagraphBackgroundColorSwatchIcon.14": DocParagraphBackgroundColorSwatchIcon14,
			"DocParagraphBackgroundColorSwatchIcon.15": DocParagraphBackgroundColorSwatchIcon15
		}));
	}
	_registerComponents() {
		if (!this._componentManager.get("COMPONENT_DOC_HEADER_FOOTER_PANEL")) this.disposeWithMe(this._componentManager.register(COMPONENT_DOC_HEADER_FOOTER_PANEL, DocHeaderFooterPanel));
		this.disposeWithMe(this._componentManager.register(paragraphSettingIndexKey, ParagraphSettingIndex));
		this.disposeWithMe(this._componentManager.register(DOC_SECTION_SETTING_COMPONENT, SectionSettingIndex));
		[
			[BULLET_LIST_TYPE_COMPONENT, BulletListTypePicker],
			[ORDER_LIST_TYPE_COMPONENT, OrderListTypePicker],
			[DOC_PARAGRAPH_MENU_COMPONENT_KEY, ParagraphMenu],
			[DOC_TABLE_BLOCK_MENU_COMPONENT_KEY, TableBlockMenu],
			[FLOAT_MENU_COMPONENT_KEY, FloatToolbar],
			[PAGE_SETTING_COMPONENT_ID, PageSettings],
			[DOC_LAYOUT_RECOVERY_COMPONENT, DocLayoutRecovery]
		].forEach(([key, comp]) => {
			this.disposeWithMe(this._componentManager.register(key, comp));
		});
	}
};
ComponentsController = __decorate([__decorateParam(0, (0, _univerjs_core.Inject)(_univerjs_ui.ComponentManager)), __decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_ui.IconManager))], ComponentsController);

//#endregion
//#region src/controllers/doc-auto-format.controller.ts
function hasQuickListType(text) {
	return Object.prototype.hasOwnProperty.call(_univerjs_core.QuickListTypeMap, text);
}
function hasQuickHeading(text) {
	return Object.prototype.hasOwnProperty.call(QUICK_HEADING_MAP, text);
}
let DocAutoFormatController = class DocAutoFormatController extends _univerjs_core.Disposable {
	constructor(_docAutoFormatService, _renderManagerService) {
		super();
		this._docAutoFormatService = _docAutoFormatService;
		this._renderManagerService = _renderManagerService;
		this._initListTabAutoFormat();
		this._initSpaceAutoFormat();
		this._initDefaultEnterFormat();
		this._initExitListAutoFormat();
	}
	_initListTabAutoFormat() {
		this.disposeWithMe(this._docAutoFormatService.registerAutoFormat({
			id: TabCommand.id,
			match: (context) => {
				const { selection, paragraphs, unit } = context;
				if (paragraphs.length === 1 && selection.startOffset === paragraphs[0].paragraphStart && paragraphs[0].bullet) {
					var _unit$getBody;
					const allParagraphs = (_unit$getBody = unit.getBody()) === null || _unit$getBody === void 0 ? void 0 : _unit$getBody.paragraphs;
					const bulletParagraphs = allParagraphs === null || allParagraphs === void 0 ? void 0 : allParagraphs.filter((p) => {
						var _p$bullet;
						return ((_p$bullet = p.bullet) === null || _p$bullet === void 0 ? void 0 : _p$bullet.listId) === paragraphs[0].bullet.listId;
					});
					if ((bulletParagraphs === null || bulletParagraphs === void 0 ? void 0 : bulletParagraphs.findIndex((p) => p.startIndex === paragraphs[0].startIndex)) === 0) return false;
					return true;
				} else if (paragraphs.length > 1 && paragraphs.some((p) => p.bullet)) return true;
				return false;
			},
			getMutations(context) {
				const params = context.commandParams;
				return [{
					id: ChangeListNestingLevelCommand.id,
					params: { type: (params === null || params === void 0 ? void 0 : params.shift) ? -1 : 1 }
				}];
			},
			priority: 100
		}));
		this.disposeWithMe(this._docAutoFormatService.registerAutoFormat({
			id: TabCommand.id,
			match: (context) => {
				const { selection, unit } = context;
				const { startNodePosition, endNodePosition } = selection;
				const renderObject = this._renderManagerService.getRenderUnitById(unit.getUnitId());
				const skeleton = renderObject === null || renderObject === void 0 ? void 0 : renderObject.with(_univerjs_docs.DocSkeletonManagerService).getSkeleton();
				if (skeleton == null) return false;
				if (startNodePosition && endNodePosition && isInSameTableCellData(skeleton, startNodePosition, endNodePosition)) return true;
				if (startNodePosition && !endNodePosition && startNodePosition.path.indexOf("cells") > -1) return true;
				return false;
			},
			getMutations(context) {
				const params = context.commandParams;
				return [{
					id: DocTableTabCommand.id,
					params: { shift: !!(params === null || params === void 0 ? void 0 : params.shift) }
				}];
			},
			priority: 99
		}));
	}
	_initSpaceAutoFormat() {
		this.disposeWithMe(this._docAutoFormatService.registerAutoFormat({
			id: AfterSpaceCommand.id,
			match: (context) => {
				var _unit$getBody2;
				const { selection, paragraphs, unit } = context;
				if (!selection.collapsed) return false;
				if (paragraphs.length !== 1) return false;
				if (!selection.collapsed) return false;
				const text = (_unit$getBody2 = unit.getBody()) === null || _unit$getBody2 === void 0 ? void 0 : _unit$getBody2.dataStream.slice(paragraphs[0].paragraphStart, selection.startOffset - 1);
				if (text && (hasQuickListType(text) || hasQuickHeading(text))) return true;
				return false;
			},
			getMutations(context) {
				var _unit$getBody3;
				const { paragraphs, unit, selection } = context;
				const text = (_unit$getBody3 = unit.getBody()) === null || _unit$getBody3 === void 0 ? void 0 : _unit$getBody3.dataStream.slice(paragraphs[0].paragraphStart, selection.startOffset - 1);
				if (text && hasQuickListType(text)) {
					const type = _univerjs_core.QuickListTypeMap[text];
					return [{
						id: QuickListCommand.id,
						params: {
							listType: type,
							paragraph: paragraphs[0]
						}
					}];
				}
				if (text && hasQuickHeading(text)) {
					const type = QUICK_HEADING_MAP[text];
					return [{
						id: QuickHeadingCommand.id,
						params: { value: type }
					}];
				}
				return [];
			}
		}));
	}
	_initExitListAutoFormat() {
		this.disposeWithMe(this._docAutoFormatService.registerAutoFormat({
			id: EnterCommand.id,
			match: (context) => {
				const { paragraphs } = context;
				if (paragraphs.length === 1 && paragraphs[0].bullet && paragraphs[0].paragraphStart === paragraphs[0].paragraphEnd) return true;
				return false;
			},
			getMutations: (context) => {
				const bullet = context.paragraphs[0].bullet;
				if (!bullet) return [];
				if (bullet.nestingLevel > 0) return [{
					id: ChangeListNestingLevelCommand.id,
					params: { type: -1 }
				}];
				return [{
					id: ListOperationCommand.id,
					params: { listType: context.paragraphs[0].bullet.listType }
				}];
			}
		}));
	}
	_initDefaultEnterFormat() {
		this.disposeWithMe(this._docAutoFormatService.registerAutoFormat({
			id: EnterCommand.id,
			match: () => {
				return true;
			},
			getMutations() {
				return [{ id: BreakLineCommand.id }];
			},
			priority: -9999
		}));
	}
};
DocAutoFormatController = __decorate([__decorateParam(0, (0, _univerjs_core.Inject)(DocAutoFormatService)), __decorateParam(1, _univerjs_engine_render.IRenderManagerService)], DocAutoFormatController);

//#endregion
//#region src/views/header-footer/text-bubble.ts
const COLLAB_CURSOR_LABEL_HEIGHT = 18;
const COLLAB_CURSOR_LABEL_MAX_WIDTH = 200;
const COLLAB_CURSOR_LABEL_TEXT_PADDING_LR = 6;
const COLLAB_CURSOR_LABEL_TEXT_PADDING_TB = 4;
function drawBubble(ctx, props) {
	let { radius, width, height } = props;
	radius = radius ?? 0;
	width = width ?? 30;
	height = height ?? 30;
	let bottomRight = 0;
	bottomRight = Math.min(radius, width / 2, height / 2);
	ctx.beginPath();
	ctx.moveTo(0, 0);
	ctx.lineTo(width, 0);
	ctx.lineTo(width, height - bottomRight);
	ctx.arc(width - bottomRight, height - bottomRight, bottomRight, 0, Math.PI / 2, false);
	ctx.lineTo(0, height);
	ctx.lineTo(0, 0);
	ctx.closePath();
	if (props.fill) {
		ctx.save();
		ctx.fillStyle = props.fill;
		if (props.fillRule === "evenodd") ctx.fill("evenodd");
		else ctx.fill();
		ctx.restore();
	}
}
/**
* Render a single collaborated cursor on the canvas.
*/
var TextBubbleShape = class TextBubbleShape extends _univerjs_engine_render.Shape {
	constructor(key, props) {
		super(key, props);
		_defineProperty(this, "color", void 0);
		_defineProperty(this, "text", void 0);
		this.color = props === null || props === void 0 ? void 0 : props.color;
		this.text = props === null || props === void 0 ? void 0 : props.text;
	}
	static drawWith(ctx, props) {
		const { text, color } = props;
		ctx.save();
		ctx.font = "13px Source Han Sans CN";
		const textWidth = ctx.measureText(text).width;
		const realInfoWidth = Math.min(textWidth + 2 * 6, 200);
		drawBubble(ctx, {
			height: 18,
			width: realInfoWidth,
			radius: 4,
			fill: color,
			evented: false
		});
		ctx.fillStyle = "rgba(58, 96, 247, 1)";
		const offsetX = 6;
		const offsetY = 18 - 4;
		const maxTextWidth = 200 - 2 * 6;
		if (textWidth > maxTextWidth) {
			let truncatedText = "";
			let currentWidth = 0;
			for (const element of text) {
				const charWidth = ctx.measureText(element).width;
				if (currentWidth + charWidth <= maxTextWidth - ctx.measureText("...").width) {
					truncatedText += element;
					currentWidth += charWidth;
				} else {
					truncatedText += "...";
					break;
				}
			}
			ctx.fillText(truncatedText, offsetX, offsetY);
		} else ctx.fillText(text, offsetX, offsetY);
		ctx.restore();
	}
	_draw(ctx) {
		TextBubbleShape.drawWith(ctx, this);
	}
};

//#endregion
//#region src/controllers/doc-header-footer.controller.ts
const HEADER_FOOTER_COVER_ALPHA = .5;
const HEADER_FOOTER_LABEL_ALPHA = .08;
let DocHeaderFooterController = class DocHeaderFooterController extends _univerjs_core.Disposable {
	constructor(_context, _commandService, _editorService, _instanceSrv, _renderManagerService, _docSkeletonManagerService, _docSelectionRenderService, _localeService, _themeService) {
		super();
		this._context = _context;
		this._commandService = _commandService;
		this._editorService = _editorService;
		this._instanceSrv = _instanceSrv;
		this._renderManagerService = _renderManagerService;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		this._docSelectionRenderService = _docSelectionRenderService;
		this._localeService = _localeService;
		this._themeService = _themeService;
		_defineProperty(this, "_loadedMap", /* @__PURE__ */ new WeakSet());
		_defineProperty(this, "_headerFooterColors", {
			primary: "",
			cover: "",
			label: ""
		});
		this._initialize();
	}
	_initialize() {
		this._initThemeColors();
		this._init();
		this._drawHeaderFooterLabel();
		this._listenSwitchMode();
	}
	_initThemeColors() {
		this.disposeWithMe(this._themeService.currentTheme$.subscribe(() => {
			const primary = this._themeService.getColorFromTheme("primary.600");
			this._headerFooterColors = {
				primary,
				cover: new _univerjs_core.ColorKit(this._themeService.getColorFromTheme("gray.0")).setAlpha(HEADER_FOOTER_COVER_ALPHA).toRgbString(),
				label: new _univerjs_core.ColorKit(primary).setAlpha(HEADER_FOOTER_LABEL_ALPHA).toRgbString()
			};
		}));
	}
	dispose() {
		super.dispose();
	}
	_listenSwitchMode() {
		this.disposeWithMe(this._commandService.onCommandExecuted((command) => {
			if (_univerjs_docs.RichTextEditingMutation.id === command.id) {
				const docDataModel = this._context.unit;
				const editArea = this._docSkeletonManagerService.getViewModel().getEditArea();
				const documentFlavor = docDataModel.getSnapshot().documentStyle.documentFlavor;
				if (editArea !== _univerjs_engine_render.DocumentEditArea.BODY && documentFlavor === _univerjs_core.DocumentFlavor.MODERN) this._commandService.executeCommand(CloseHeaderFooterCommand.id, { unitId: this._context.unitId });
			}
		}));
	}
	_init() {
		const { unitId } = this._context;
		const docObject = neoGetDocObject(this._context);
		if (docObject == null || docObject.document == null) return;
		if (!this._loadedMap.has(docObject.document)) {
			this._initialMain(unitId);
			this._loadedMap.add(docObject.document);
		}
	}
	_initialMain(unitId) {
		const { document } = neoGetDocObject(this._context);
		this.disposeWithMe(document.onDblclick$.subscribeEvent(async (evt) => {
			var _skeleton$getSkeleton;
			if (this._isEditorReadOnly(unitId)) return;
			if (!this._isTraditionalMode()) return;
			const { offsetX, offsetY } = evt;
			const { pageLayoutType = _univerjs_engine_render.PageLayoutType.VERTICAL, pageMarginLeft, pageMarginTop } = document.getOffsetConfig();
			const coord = this._getTransformCoordForDocumentOffset(offsetX, offsetY);
			if (coord == null) return;
			const viewModel = this._docSkeletonManagerService.getViewModel();
			const skeleton = this._docSkeletonManagerService.getSkeleton();
			const preEditArea = viewModel.getEditArea();
			const { editArea, pageNumber } = skeleton.findEditAreaByCoord(coord, pageLayoutType, pageMarginLeft, pageMarginTop);
			if (preEditArea === editArea && (editArea === _univerjs_engine_render.DocumentEditArea.BODY || this._docSelectionRenderService.getSegmentPage() === pageNumber)) return;
			viewModel.setEditArea(editArea);
			const { createType, headerFooterId, sectionId } = getHeaderFooterTarget(viewModel, editArea, pageNumber, (_skeleton$getSkeleton = skeleton.getSkeletonData()) === null || _skeleton$getSkeleton === void 0 ? void 0 : _skeleton$getSkeleton.pages[pageNumber]);
			if (editArea === _univerjs_engine_render.DocumentEditArea.BODY) {
				this._docSelectionRenderService.setSegment("");
				this._docSelectionRenderService.setSegmentPage(-1);
				this._docSelectionRenderService.setCursorManually(offsetX, offsetY);
			} else if (createType != null) {
				const segmentId = (0, _univerjs_core.generateRandomId)(6);
				this._docSelectionRenderService.setSegment(segmentId);
				this._docSelectionRenderService.setSegmentPage(pageNumber);
				await this._commandService.executeCommand(CoreHeaderFooterCommand.id, {
					unitId,
					createType,
					segmentId,
					sectionId
				});
			} else if (headerFooterId != null) {
				this._docSelectionRenderService.setSegment(headerFooterId);
				this._docSelectionRenderService.setSegmentPage(pageNumber);
				this._docSelectionRenderService.setCursorManually(offsetX, offsetY);
			}
		}));
	}
	_getTransformCoordForDocumentOffset(evtOffsetX, evtOffsetY) {
		const { document, scene } = neoGetDocObject(this._context);
		const { documentTransform } = document.getOffsetConfig();
		const activeViewport = scene.getViewports()[0];
		if (activeViewport == null) return;
		const originCoord = activeViewport.transformVector2SceneCoord(_univerjs_engine_render.Vector2.FromArray([evtOffsetX, evtOffsetY]));
		return documentTransform.clone().invert().applyPoint(originCoord);
	}
	_drawHeaderFooterLabel() {
		this.disposeWithMe(this._instanceSrv.getCurrentTypeOfUnit$(_univerjs_core.UniverInstanceType.UNIVER_DOC).subscribe((unit) => {
			if (unit == null) return;
			const unitId = unit.getUnitId();
			const currentRender = this._renderManagerService.getRenderUnitById(unitId);
			if (this._editorService.isEditor(unitId) || this._instanceSrv.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC) == null) return;
			if (currentRender == null) return;
			const { mainComponent } = currentRender;
			const docsComponent = mainComponent;
			this.disposeWithMe((0, _univerjs_core.toDisposable)(docsComponent.pageRender$.subscribe((config) => this._drawHeaderFooterPage(config, unitId))));
		}));
	}
	_drawHeaderFooterPage(config, unitId) {
		if (this._editorService.isEditor(unitId) || !this._isTraditionalMode()) return;
		const isEditBody = this._docSkeletonManagerService.getViewModel().getEditArea() === _univerjs_engine_render.DocumentEditArea.BODY;
		const { pageLeft, pageTop, ctx } = config;
		ctx.save();
		ctx.translate(pageLeft - .5, pageTop - .5);
		this._drawHeaderFooterCover(config, isEditBody);
		if (!isEditBody) this._drawHeaderFooterGuides(config);
		ctx.restore();
	}
	_drawHeaderFooterCover({ page, ctx }, isEditBody) {
		const { pageWidth, pageHeight, marginTop, marginBottom } = page;
		if (isEditBody) {
			_univerjs_engine_render.Rect.drawWith(ctx, {
				left: 0,
				top: 0,
				width: pageWidth,
				height: marginTop,
				fill: this._headerFooterColors.cover
			});
			ctx.save();
			ctx.translate(0, pageHeight - marginBottom);
			_univerjs_engine_render.Rect.drawWith(ctx, {
				left: 0,
				top: 0,
				width: pageWidth,
				height: marginBottom,
				fill: this._headerFooterColors.cover
			});
			ctx.restore();
			return;
		}
		ctx.save();
		ctx.translate(0, marginTop);
		_univerjs_engine_render.Rect.drawWith(ctx, {
			left: 0,
			top: marginTop,
			width: pageWidth,
			height: pageHeight - marginTop - marginBottom,
			fill: this._headerFooterColors.cover
		});
		ctx.restore();
	}
	_drawHeaderFooterGuides({ page, ctx }) {
		const { pageWidth, pageHeight, marginTop, marginBottom } = page;
		const headerPathConfig = {
			dataArray: [{
				command: "M",
				points: [0, marginTop]
			}, {
				command: "L",
				points: [pageWidth, marginTop]
			}],
			strokeWidth: 1,
			stroke: this._headerFooterColors.primary
		};
		const footerPathConfig = {
			dataArray: [{
				command: "M",
				points: [0, pageHeight - marginBottom]
			}, {
				command: "L",
				points: [pageWidth, pageHeight - marginBottom]
			}],
			strokeWidth: 1,
			stroke: this._headerFooterColors.primary
		};
		_univerjs_engine_render.Path.drawWith(ctx, headerPathConfig);
		_univerjs_engine_render.Path.drawWith(ctx, footerPathConfig);
		ctx.translate(0, marginTop + 1);
		TextBubbleShape.drawWith(ctx, {
			text: this._localeService.t("docs-ui.headerFooter.header"),
			color: this._headerFooterColors.label
		});
		ctx.translate(0, pageHeight - marginTop - marginBottom);
		TextBubbleShape.drawWith(ctx, {
			text: this._localeService.t("docs-ui.headerFooter.footer"),
			color: this._headerFooterColors.label
		});
	}
	_isEditorReadOnly(unitId) {
		const editor = this._editorService.getEditor(unitId);
		if (!editor) return false;
		return editor.isReadOnly();
	}
	_isTraditionalMode() {
		return this._context.unit.getSnapshot().documentStyle.documentFlavor === _univerjs_core.DocumentFlavor.TRADITIONAL;
	}
};
DocHeaderFooterController = __decorate([
	__decorateParam(1, _univerjs_core.ICommandService),
	__decorateParam(2, IEditorService),
	__decorateParam(3, _univerjs_core.IUniverInstanceService),
	__decorateParam(4, _univerjs_engine_render.IRenderManagerService),
	__decorateParam(5, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService)),
	__decorateParam(6, (0, _univerjs_core.Inject)(DocSelectionRenderService)),
	__decorateParam(7, (0, _univerjs_core.Inject)(_univerjs_core.LocaleService)),
	__decorateParam(8, (0, _univerjs_core.Inject)(_univerjs_core.ThemeService))
], DocHeaderFooterController);

//#endregion
//#region src/basics/table.ts
function findTableAfterLine(line, page) {
	const { ed } = line;
	const { skeTables } = page;
	let table = null;
	for (const t of skeTables.values()) if (t.st === ed + 1) {
		table = t;
		break;
	}
	return table;
}
function findTableBeforeLine(line, page) {
	const { st } = line;
	const { skeTables } = page;
	let table = null;
	for (const t of skeTables.values()) if (t.ed === st - 1) {
		table = t;
		break;
	}
	return table;
}
function firstLineInCell(cell) {
	return cell.sections[0].columns[0].lines[0];
}
function lastLineInCell(cell) {
	const lastColumn = (0, _univerjs_engine_render.getLastColumn)(cell);
	return lastColumn.lines[lastColumn.lines.length - 1];
}

//#endregion
//#region src/controllers/doc-move-cursor.controller.ts
let DocMoveCursorController = class DocMoveCursorController extends _univerjs_core.Disposable {
	constructor(_univerInstanceService, _renderManagerService, _textSelectionManagerService, _commandService) {
		super();
		this._univerInstanceService = _univerInstanceService;
		this._renderManagerService = _renderManagerService;
		this._textSelectionManagerService = _textSelectionManagerService;
		this._commandService = _commandService;
		_defineProperty(this, "_onInputSubscription", void 0);
		this._commandExecutedListener();
	}
	dispose() {
		var _this$_onInputSubscri;
		super.dispose();
		(_this$_onInputSubscri = this._onInputSubscription) === null || _this$_onInputSubscri === void 0 || _this$_onInputSubscri.unsubscribe();
	}
	_commandExecutedListener() {
		const updateCommandList = [MoveCursorOperation.id, MoveSelectionOperation.id];
		this.disposeWithMe(this._commandService.onCommandExecuted((command) => {
			if (!updateCommandList.includes(command.id)) return;
			const param = command.params;
			switch (command.id) {
				case MoveCursorOperation.id: return this._handleMoveCursor(param.direction, param.granularity ?? "character");
				case MoveSelectionOperation.id: return this._handleShiftMoveSelection(param.direction, param.granularity ?? "character");
				default: throw new Error("Unknown command");
			}
		}));
	}
	_handleShiftMoveSelection(direction, granularity = "character") {
		var _docDataModel$getSelf;
		const activeRange = this._textSelectionManagerService.getActiveTextRange();
		const allRanges = this._textSelectionManagerService.getTextRanges();
		const docDataModel = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null) return;
		const currentRender = this._renderManagerService.getRenderUnitById(docDataModel.getUnitId());
		const skeleton = currentRender === null || currentRender === void 0 ? void 0 : currentRender.with(_univerjs_docs.DocSkeletonManagerService).getSkeleton();
		const docObject = currentRender ? neoGetDocObject(currentRender) : null;
		if (activeRange == null || skeleton == null || docObject == null) return;
		const { startOffset, endOffset, style, collapsed, direction: rangeDirection, segmentId, startNodePosition, endNodePosition, segmentPage } = activeRange;
		const normalizedSegmentId = segmentId ?? "";
		const normalizedSegmentPage = segmentPage ?? -1;
		if (allRanges.length > 1) {
			let min = Number.POSITIVE_INFINITY;
			let max = Number.NEGATIVE_INFINITY;
			for (const range of allRanges) {
				min = Math.min(min, range.startOffset);
				max = Math.max(max, range.endOffset);
			}
			this._textSelectionManagerService.replaceDocRanges([{
				startOffset: direction === _univerjs_core.Direction.LEFT || direction === _univerjs_core.Direction.UP ? max : min,
				endOffset: direction === _univerjs_core.Direction.LEFT || direction === _univerjs_core.Direction.UP ? min : max,
				style
			}], void 0, false);
			return;
		}
		const anchorOffset = collapsed ? startOffset : rangeDirection === _univerjs_core.RANGE_DIRECTION.FORWARD ? startOffset : endOffset;
		let focusOffset = collapsed ? endOffset : rangeDirection === _univerjs_core.RANGE_DIRECTION.FORWARD ? endOffset : startOffset;
		const body = (_docDataModel$getSelf = docDataModel.getSelfOrHeaderFooterModel(normalizedSegmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
		if (body == null) return;
		const dataStreamLength = body.dataStream.length ?? Number.POSITIVE_INFINITY;
		const customRanges = docDataModel.getCustomRanges() ?? [];
		if (granularity !== "character") {
			const nextOffset = this._getCursorOffsetByGranularity(skeleton, focusOffset, direction, granularity, normalizedSegmentId, normalizedSegmentPage, dataStreamLength, body.dataStream);
			if (nextOffset == null || nextOffset === focusOffset) return;
			const normalizedNextOffset = this._normalizeRenderableCursorOffset(skeleton, body.dataStream, customRanges, nextOffset, direction, normalizedSegmentId, normalizedSegmentPage);
			if (normalizedNextOffset == null || normalizedNextOffset === focusOffset) return;
			this._textSelectionManagerService.replaceDocRanges([{
				startOffset: anchorOffset,
				endOffset: normalizedNextOffset,
				style
			}], void 0, false);
			this._scrollToFocusNodePosition(docDataModel.getUnitId(), normalizedNextOffset);
			return;
		}
		if (direction === _univerjs_core.Direction.LEFT || direction === _univerjs_core.Direction.RIGHT) {
			const preGlyph = skeleton.findNodeByCharIndex(focusOffset - 1, segmentId, segmentPage);
			const curGlyph = skeleton.findNodeByCharIndex(focusOffset, segmentId, segmentPage);
			focusOffset = direction === _univerjs_core.Direction.RIGHT ? focusOffset + ((curGlyph === null || curGlyph === void 0 ? void 0 : curGlyph.count) ?? 1) : focusOffset - ((preGlyph === null || preGlyph === void 0 ? void 0 : preGlyph.count) ?? 1);
			focusOffset = this._normalizeCursorOffset(body.dataStream, customRanges, focusOffset, direction);
			focusOffset = Math.min(dataStreamLength - 2, Math.max(0, focusOffset));
			this._textSelectionManagerService.replaceDocRanges([{
				startOffset: anchorOffset,
				endOffset: focusOffset,
				style
			}], void 0, false);
			this._scrollToFocusNodePosition(docDataModel.getUnitId(), focusOffset);
		} else {
			const focusGlyph = skeleton.findNodeByCharIndex(focusOffset, segmentId, segmentPage);
			const documentOffsetConfig = docObject.document.getOffsetConfig();
			const focusNodePosition = collapsed ? startNodePosition : rangeDirection === _univerjs_core.RANGE_DIRECTION.FORWARD ? endNodePosition : startNodePosition;
			const newPos = this._getTopOrBottomPosition(skeleton, focusGlyph, focusNodePosition, direction === _univerjs_core.Direction.DOWN, true);
			if (newPos == null) {
				const newFocusOffset = this._normalizeRenderableCursorOffset(skeleton, body.dataStream, customRanges, direction === _univerjs_core.Direction.UP ? 0 : dataStreamLength - 2, direction, normalizedSegmentId, normalizedSegmentPage);
				if (newFocusOffset == null || newFocusOffset === focusOffset) return;
				this._textSelectionManagerService.replaceDocRanges([{
					startOffset: anchorOffset,
					endOffset: newFocusOffset,
					style
				}], void 0, false);
				return;
			}
			const newActiveRange = new NodePositionConvertToCursor(documentOffsetConfig, skeleton).getRangePointData(newPos, newPos).cursorList[0];
			const newFocusOffset = this._normalizeRenderableCursorOffset(skeleton, body.dataStream, customRanges, newActiveRange.endOffset, direction, normalizedSegmentId, normalizedSegmentPage);
			if (newFocusOffset == null || newFocusOffset === focusOffset) return;
			this._textSelectionManagerService.replaceDocRanges([{
				startOffset: anchorOffset,
				endOffset: newFocusOffset,
				style
			}], void 0, false);
			this._scrollToFocusNodePosition(docDataModel.getUnitId(), newFocusOffset);
		}
	}
	_handleMoveCursor(direction, granularity = "character") {
		var _docDataModel$getSelf2;
		const activeRange = this._textSelectionManagerService.getActiveTextRange();
		const allRanges = this._textSelectionManagerService.getTextRanges();
		const docDataModel = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (docDataModel == null) return false;
		const currentRender = this._renderManagerService.getRenderUnitById(docDataModel.getUnitId());
		const skeleton = currentRender === null || currentRender === void 0 ? void 0 : currentRender.with(_univerjs_docs.DocSkeletonManagerService).getSkeleton();
		const docObject = currentRender ? neoGetDocObject(currentRender) : null;
		if (activeRange == null || skeleton == null || docObject == null || allRanges == null) return;
		const { startOffset, endOffset, style, collapsed, segmentId, startNodePosition, endNodePosition, segmentPage } = activeRange;
		const normalizedSegmentId = segmentId ?? "";
		const normalizedSegmentPage = segmentPage ?? -1;
		const body = (_docDataModel$getSelf2 = docDataModel.getSelfOrHeaderFooterModel(normalizedSegmentId)) === null || _docDataModel$getSelf2 === void 0 ? void 0 : _docDataModel$getSelf2.getBody();
		if (body == null) return;
		const dataStreamLength = body.dataStream.length ?? Number.POSITIVE_INFINITY;
		const customRanges = docDataModel.getCustomRanges() ?? [];
		if (granularity !== "character") {
			let cursorOffset;
			if (!activeRange.collapsed || allRanges.length > 1) {
				let min = Number.POSITIVE_INFINITY;
				let max = Number.NEGATIVE_INFINITY;
				for (const range of allRanges) {
					min = Math.min(min, range.startOffset);
					max = Math.max(max, range.endOffset);
				}
				cursorOffset = direction === _univerjs_core.Direction.LEFT || direction === _univerjs_core.Direction.UP ? min : max;
			} else cursorOffset = direction === _univerjs_core.Direction.LEFT || direction === _univerjs_core.Direction.UP ? startOffset : endOffset;
			let cursor = this._getCursorOffsetByGranularity(skeleton, cursorOffset, direction, granularity, normalizedSegmentId, normalizedSegmentPage, dataStreamLength, body.dataStream);
			if (cursor == null) return;
			cursor = this._normalizeRenderableCursorOffset(skeleton, body.dataStream, customRanges, cursor, direction, normalizedSegmentId, normalizedSegmentPage);
			if (cursor == null) return;
			this._textSelectionManagerService.replaceDocRanges([{
				startOffset: cursor,
				endOffset: cursor,
				style
			}], void 0, false);
			this._scrollToFocusNodePosition(docDataModel.getUnitId(), cursor);
			return;
		}
		if (direction === _univerjs_core.Direction.LEFT || direction === _univerjs_core.Direction.RIGHT) {
			let cursor;
			if (!activeRange.collapsed || allRanges.length > 1) {
				let min = Number.POSITIVE_INFINITY;
				let max = Number.NEGATIVE_INFINITY;
				for (const range of allRanges) {
					min = Math.min(min, range.startOffset);
					max = Math.max(max, range.endOffset);
				}
				cursor = direction === _univerjs_core.Direction.LEFT ? min : max;
			} else {
				const preSpan = skeleton.findNodeByCharIndex(startOffset - 1, segmentId, segmentPage);
				const curSpan = skeleton.findNodeByCharIndex(startOffset, segmentId, segmentPage);
				const nextGlyph = skeleton.findNodeByCharIndex(startOffset + 1, segmentId, segmentPage);
				if (direction === _univerjs_core.Direction.LEFT) cursor = Math.max(0, startOffset - ((preSpan === null || preSpan === void 0 ? void 0 : preSpan.count) ?? 1));
				else cursor = Math.min(dataStreamLength - 2, endOffset + curSpan.count + ((nextGlyph === null || nextGlyph === void 0 ? void 0 : nextGlyph.streamType) === _univerjs_core.DataStreamTreeTokenType.SECTION_BREAK ? 1 : 0));
			}
			const skipTokens = [...this._getCursorSkipTokens()];
			if (direction === _univerjs_core.Direction.LEFT) while (skipTokens.includes(body.dataStream[cursor])) cursor--;
			else while (skipTokens.includes(body.dataStream[cursor])) cursor++;
			cursor = this._normalizeCursorOffset(body.dataStream, customRanges, cursor, direction);
			const normalizedCursor = this._normalizeRenderableCursorOffset(skeleton, body.dataStream, customRanges, cursor, direction, normalizedSegmentId, normalizedSegmentPage);
			if (normalizedCursor == null) return;
			this._textSelectionManagerService.replaceDocRanges([{
				startOffset: normalizedCursor,
				endOffset: normalizedCursor,
				style
			}], void 0, false);
			this._scrollToFocusNodePosition(docDataModel.getUnitId(), normalizedCursor);
		} else {
			const startNode = skeleton.findNodeByCharIndex(startOffset, segmentId, segmentPage);
			const endNode = skeleton.findNodeByCharIndex(endOffset, segmentId, segmentPage);
			const documentOffsetConfig = docObject.document.getOffsetConfig();
			const newPos = this._getTopOrBottomPosition(skeleton, direction === _univerjs_core.Direction.UP ? startNode : collapsed ? startNode : endNode, direction === _univerjs_core.Direction.UP ? startNodePosition : collapsed ? startNodePosition : endNodePosition, direction === _univerjs_core.Direction.DOWN);
			if (newPos == null) {
				let cursor;
				if (collapsed) cursor = this._normalizeRenderableCursorOffset(skeleton, body.dataStream, customRanges, direction === _univerjs_core.Direction.UP ? 0 : dataStreamLength - 2, direction, normalizedSegmentId, normalizedSegmentPage);
				else cursor = this._normalizeRenderableCursorOffset(skeleton, body.dataStream, customRanges, direction === _univerjs_core.Direction.UP ? startOffset : endOffset, direction, normalizedSegmentId, normalizedSegmentPage);
				if (cursor == null) return;
				this._textSelectionManagerService.replaceDocRanges([{
					startOffset: cursor,
					endOffset: cursor,
					style
				}], void 0, false);
				return;
			}
			const newActiveRange = new NodePositionConvertToCursor(documentOffsetConfig, skeleton).getRangePointData(newPos, newPos).cursorList[0];
			const cursor = this._normalizeRenderableCursorOffset(skeleton, body.dataStream, customRanges, newActiveRange.endOffset, direction, normalizedSegmentId, normalizedSegmentPage);
			if (cursor == null) return;
			this._textSelectionManagerService.replaceDocRanges([{
				startOffset: cursor,
				endOffset: cursor,
				style
			}], void 0, false);
			this._scrollToFocusNodePosition(docDataModel.getUnitId(), cursor);
		}
	}
	_getCursorOffsetByGranularity(skeleton, focusOffset, direction, granularity, segmentId, segmentPage, dataStreamLength, dataStream = "") {
		switch (granularity) {
			case "document": return direction === _univerjs_core.Direction.LEFT || direction === _univerjs_core.Direction.UP ? 0 : dataStreamLength - 2;
			case "paragraph": return this._getParagraphBoundaryOffset(dataStream, focusOffset, direction);
			case "line": return this._getLineBoundaryOffset(skeleton, focusOffset, direction, segmentId, segmentPage, dataStreamLength);
			case "word": return this._getWordBoundaryOffset(skeleton, focusOffset, direction, segmentId, segmentPage, dataStreamLength);
			default: return null;
		}
	}
	_getParagraphBoundaryOffset(dataStream, focusOffset, direction) {
		if (direction !== _univerjs_core.Direction.UP && direction !== _univerjs_core.Direction.DOWN) return;
		if (dataStream.length === 0) return 0;
		const cursor = Math.min(Math.max(0, focusOffset), dataStream.length - 1);
		if (direction === _univerjs_core.Direction.UP) {
			const currentParagraphStart = this._getCurrentParagraphStartOffset(dataStream, cursor);
			if (cursor > currentParagraphStart) return currentParagraphStart;
			return this._getCurrentParagraphStartOffset(dataStream, Math.max(0, currentParagraphStart - 1));
		}
		const nextParagraphEnd = dataStream.indexOf(_univerjs_core.DataStreamTreeTokenType.PARAGRAPH, cursor);
		return nextParagraphEnd === -1 ? dataStream.length - 1 : nextParagraphEnd + 1;
	}
	_getCurrentParagraphStartOffset(dataStream, cursor) {
		for (let index = Math.min(cursor - 1, dataStream.length - 1); index >= 0; index--) {
			const token = dataStream[index];
			if (token === _univerjs_core.DataStreamTreeTokenType.PARAGRAPH || token === _univerjs_core.DataStreamTreeTokenType.SECTION_BREAK) return index + 1;
		}
		return 0;
	}
	_getWordBoundaryOffset(skeleton, focusOffset, direction, segmentId, segmentPage, dataStreamLength) {
		if (direction !== _univerjs_core.Direction.LEFT && direction !== _univerjs_core.Direction.RIGHT) return;
		const glyph = skeleton.findNodeByCharIndex(focusOffset, segmentId, segmentPage) ?? skeleton.findNodeByCharIndex(Math.max(0, focusOffset - 1), segmentId, segmentPage);
		if (glyph == null) return direction === _univerjs_core.Direction.LEFT ? 0 : dataStreamLength - 2;
		const paragraphInfo = getParagraphInfoByGlyph(glyph);
		if (paragraphInfo == null) return;
		const nodeIndex = Math.min(Math.max(0, focusOffset - paragraphInfo.st), paragraphInfo.content.length);
		const nextOffset = getNextWordBoundaryOffset(paragraphInfo.content, nodeIndex, paragraphInfo.st, direction);
		if (nextOffset == null) return;
		return Math.min(dataStreamLength - 2, Math.max(0, nextOffset));
	}
	_getLineBoundaryOffset(skeleton, focusOffset, direction, segmentId, segmentPage, dataStreamLength) {
		var _glyph$parent;
		if (direction !== _univerjs_core.Direction.LEFT && direction !== _univerjs_core.Direction.RIGHT) return;
		const glyph = skeleton.findNodeByCharIndex(focusOffset, segmentId, segmentPage) ?? skeleton.findNodeByCharIndex(Math.max(0, focusOffset - 1), segmentId, segmentPage);
		const line = glyph === null || glyph === void 0 || (_glyph$parent = glyph.parent) === null || _glyph$parent === void 0 ? void 0 : _glyph$parent.parent;
		if (line == null) return direction === _univerjs_core.Direction.LEFT ? 0 : dataStreamLength - 2;
		const boundaryGlyph = direction === _univerjs_core.Direction.LEFT ? this._getFirstCursorGlyphInLine(line) : this._getLastCursorGlyphInLine(line);
		const boundaryPosition = boundaryGlyph == null ? void 0 : skeleton.findPositionByGlyph(boundaryGlyph, segmentPage);
		if (boundaryPosition == null) return direction === _univerjs_core.Direction.LEFT ? 0 : dataStreamLength - 2;
		const cursor = skeleton.findCharIndexByPosition({
			...boundaryPosition,
			isBack: direction === _univerjs_core.Direction.LEFT
		});
		if (cursor == null) return;
		return Math.min(dataStreamLength - 2, Math.max(0, cursor));
	}
	_getFirstCursorGlyphInLine(line) {
		for (const divide of line.divides) for (const glyph of divide.glyphGroup) if (this._isCursorAddressableGlyph(glyph)) return glyph;
	}
	_getLastCursorGlyphInLine(line) {
		for (let divideIndex = line.divides.length - 1; divideIndex >= 0; divideIndex--) {
			const divide = line.divides[divideIndex];
			for (let glyphIndex = divide.glyphGroup.length - 1; glyphIndex >= 0; glyphIndex--) {
				const glyph = divide.glyphGroup[glyphIndex];
				if (this._isCursorAddressableGlyph(glyph)) return glyph;
			}
		}
	}
	_isCursorAddressableGlyph(glyph) {
		return glyph.count > 0 && !this._getCursorSkipTokens(true).includes(glyph.streamType);
	}
	_normalizeCursorOffset(dataStream, customRanges, cursor, direction) {
		cursor = Math.max(0, cursor);
		const skipTokens = this._getCursorSkipTokens();
		if (direction === _univerjs_core.Direction.LEFT || direction === _univerjs_core.Direction.UP) while (cursor > 0 && skipTokens.includes(dataStream[cursor])) cursor--;
		else while (cursor < dataStream.length - 1 && skipTokens.includes(dataStream[cursor])) cursor++;
		return this._normalizeWholeEntityRanges(customRanges, cursor, direction);
	}
	_normalizeRenderableCursorOffset(skeleton, dataStream, customRanges, cursor, direction, segmentId, segmentPage) {
		const boundedCursor = Math.min(Math.max(0, cursor), Math.max(0, dataStream.length - 1));
		const primaryCursor = this._findNearestRenderableCursorOffset(skeleton, dataStream, customRanges, boundedCursor, direction, segmentId, segmentPage);
		if (primaryCursor != null) return primaryCursor;
		const fallbackDirection = direction === _univerjs_core.Direction.LEFT || direction === _univerjs_core.Direction.UP ? _univerjs_core.Direction.DOWN : _univerjs_core.Direction.UP;
		return this._findNearestRenderableCursorOffset(skeleton, dataStream, customRanges, boundedCursor, fallbackDirection, segmentId, segmentPage);
	}
	_findNearestRenderableCursorOffset(skeleton, dataStream, customRanges, cursor, direction, segmentId, segmentPage) {
		const step = direction === _univerjs_core.Direction.LEFT || direction === _univerjs_core.Direction.UP ? -1 : 1;
		let current = Math.min(Math.max(0, cursor), Math.max(0, dataStream.length - 1));
		const visited = /* @__PURE__ */ new Set();
		while (current >= 0 && current < dataStream.length && !visited.has(current)) {
			visited.add(current);
			const normalizedCursor = this._normalizeCursorOffset(dataStream, customRanges, current, direction);
			if (normalizedCursor < 0 || normalizedCursor >= dataStream.length) return;
			if (this._isRenderableCursorOffset(skeleton, dataStream, normalizedCursor, segmentId, segmentPage)) return normalizedCursor;
			current = normalizedCursor + step;
		}
	}
	_isRenderableCursorOffset(skeleton, dataStream, cursor, segmentId, segmentPage) {
		if (cursor < 0 || cursor >= dataStream.length) return false;
		if (this._getNonRenderableCursorTokens().includes(dataStream[cursor])) return false;
		return skeleton.findNodePositionByCharIndex(cursor, true, segmentId, segmentPage) != null;
	}
	_normalizeWholeEntityRanges(customRanges, cursor, direction) {
		customRanges.filter((range) => range.wholeEntity && range.startIndex < cursor && range.endIndex >= cursor).forEach((range) => {
			if (direction === _univerjs_core.Direction.LEFT || direction === _univerjs_core.Direction.UP) cursor = Math.min(range.startIndex, cursor);
			else cursor = Math.max(range.endIndex + 1, cursor);
		});
		return cursor;
	}
	_getCursorSkipTokens(includeParagraph = false) {
		const tokens = [
			_univerjs_core.DataStreamTreeTokenType.TABLE_START,
			_univerjs_core.DataStreamTreeTokenType.TABLE_END,
			_univerjs_core.DataStreamTreeTokenType.TABLE_ROW_START,
			_univerjs_core.DataStreamTreeTokenType.TABLE_ROW_END,
			_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_START,
			_univerjs_core.DataStreamTreeTokenType.TABLE_CELL_END,
			_univerjs_core.DataStreamTreeTokenType.SECTION_BREAK,
			_univerjs_core.DataStreamTreeTokenType.BLOCK_START,
			_univerjs_core.DataStreamTreeTokenType.BLOCK_END
		];
		if (includeParagraph) tokens.push(_univerjs_core.DataStreamTreeTokenType.PARAGRAPH);
		return tokens;
	}
	_getNonRenderableCursorTokens() {
		return [
			...this._getCursorSkipTokens(),
			_univerjs_core.DataStreamTreeTokenType.COLUMN_GROUP_START,
			_univerjs_core.DataStreamTreeTokenType.COLUMN_GROUP_END,
			_univerjs_core.DataStreamTreeTokenType.COLUMN_START,
			_univerjs_core.DataStreamTreeTokenType.COLUMN_END
		];
	}
	_getTopOrBottomPosition(docSkeleton, glyph, nodePosition, direction, skipCellContent = false) {
		if (glyph == null || nodePosition == null) return;
		const offsetLeft = this._getGlyphLeftOffsetInLine(glyph);
		const target = this._getNextOrPrevLineTarget(glyph, direction, offsetLeft, skipCellContent);
		if (target == null) return;
		const position = this._matchPositionByLeftOffset(docSkeleton, target.line, target.offsetLeft, nodePosition);
		if (position == null) return;
		return {
			...position,
			isBack: true
		};
	}
	_getGlyphLeftOffsetInLine(glyph) {
		const divide = glyph.parent;
		if (divide == null) return Number.NEGATIVE_INFINITY;
		const divideLeft = divide.left;
		const { left } = glyph;
		return divideLeft + left;
	}
	_matchPositionByLeftOffset(docSkeleton, line, offsetLeft, nodePosition) {
		const nearestNode = { distance: Number.POSITIVE_INFINITY };
		const nearestEmptyParagraphNode = { distance: Number.POSITIVE_INFINITY };
		for (const divide of line.divides) {
			const divideLeft = divide.left;
			for (const glyph of divide.glyphGroup) {
				if (!this._isCursorAddressableGlyph(glyph)) {
					if (glyph.streamType === _univerjs_core.DataStreamTreeTokenType.PARAGRAPH && glyph.count > 0) {
						const { left } = glyph;
						const leftSide = divideLeft + left;
						const distance = Math.abs(offsetLeft - leftSide);
						if (distance < nearestEmptyParagraphNode.distance) {
							nearestEmptyParagraphNode.glyph = glyph;
							nearestEmptyParagraphNode.distance = distance;
						}
					}
					continue;
				}
				const { left } = glyph;
				const leftSide = divideLeft + left;
				const distance = Math.abs(offsetLeft - leftSide);
				if (distance < nearestNode.distance) {
					nearestNode.glyph = glyph;
					nearestNode.distance = distance;
				}
			}
		}
		const nearestGlyph = nearestNode.glyph ?? nearestEmptyParagraphNode.glyph;
		if (nearestGlyph == null) return;
		const { segmentPage } = nodePosition;
		return docSkeleton.findPositionByGlyph(nearestGlyph, segmentPage);
	}
	_getNextOrPrevLineTarget(glyph, direction, offsetLeft, skipCellContent = false) {
		const divide = glyph.parent;
		const line = divide === null || divide === void 0 ? void 0 : divide.parent;
		const column = line === null || line === void 0 ? void 0 : line.parent;
		const section = column === null || column === void 0 ? void 0 : column.parent;
		const page = section === null || section === void 0 ? void 0 : section.parent;
		if (divide == null || line == null || column == null || section == null || page == null) return;
		const currentLineIndex = column.lines.indexOf(line);
		if (currentLineIndex === -1) return;
		let newLine;
		if (this._isTableCellContentPage(page) && skipCellContent) {
			const tableCellTarget = this._getAdjacentTableCellLineTarget(page, direction, offsetLeft);
			if (tableCellTarget != null) return tableCellTarget;
		}
		if (direction === true) {
			newLine = column.lines[currentLineIndex + 1];
			const tableTarget = this._getTableLineTargetFromPageLine(line, page, direction, offsetLeft);
			if (tableTarget != null) return tableTarget;
		} else {
			newLine = column.lines[currentLineIndex - 1];
			const tableTarget = this._getTableLineTargetFromPageLine(line, page, direction, offsetLeft);
			if (tableTarget != null) return tableTarget;
		}
		const columnGroupTarget = this._getColumnGroupLineTargetFromBlockLine(newLine, page, direction, offsetLeft);
		if (columnGroupTarget != null) return columnGroupTarget;
		if (newLine != null) return {
			line: newLine,
			offsetLeft
		};
		const currentColumnIndex = section.columns.indexOf(column);
		if (currentColumnIndex === -1) return;
		if (direction === true) {
			var _section$columns;
			newLine = (_section$columns = section.columns[currentColumnIndex + 1]) === null || _section$columns === void 0 ? void 0 : _section$columns.lines[0];
		} else {
			var _section$columns2;
			const prevColumnLines = (_section$columns2 = section.columns) === null || _section$columns2 === void 0 || (_section$columns2 = _section$columns2[currentColumnIndex - 1]) === null || _section$columns2 === void 0 ? void 0 : _section$columns2.lines;
			newLine = prevColumnLines === null || prevColumnLines === void 0 ? void 0 : prevColumnLines[prevColumnLines.length - 1];
		}
		if (newLine != null) return {
			line: newLine,
			offsetLeft
		};
		const currentSectionIndex = page.sections.indexOf(section);
		if (currentSectionIndex === -1) return;
		if (direction === true) {
			var _page$sections;
			newLine = (_page$sections = page.sections[currentSectionIndex - 1]) === null || _page$sections === void 0 || (_page$sections = _page$sections.columns[0]) === null || _page$sections === void 0 ? void 0 : _page$sections.lines[0];
		} else {
			var _page$sections2;
			const prevColumns = (_page$sections2 = page.sections) === null || _page$sections2 === void 0 || (_page$sections2 = _page$sections2[currentSectionIndex - 1]) === null || _page$sections2 === void 0 ? void 0 : _page$sections2.columns;
			const column = prevColumns === null || prevColumns === void 0 ? void 0 : prevColumns[prevColumns.length - 1];
			const prevColumnLines = column === null || column === void 0 ? void 0 : column.lines;
			newLine = prevColumnLines === null || prevColumnLines === void 0 ? void 0 : prevColumnLines[prevColumnLines.length - 1];
		}
		if (newLine != null) return {
			line: newLine,
			offsetLeft
		};
		if (this._isColumnGroupContentPage(page)) return this._getColumnGroupExitLineTarget(page, direction, offsetLeft);
		if (this._isTableCellContentPage(page)) return this._getAdjacentTableCellLineTarget(page, direction, offsetLeft);
		const skeleton = page.parent;
		if ((skeleton === null || skeleton === void 0 ? void 0 : skeleton.pages) == null) return;
		const currentPageIndex = skeleton.pages.indexOf(page);
		if (currentPageIndex === -1) return;
		if (direction === true) {
			var _skeleton$pages;
			newLine = (_skeleton$pages = skeleton.pages[currentPageIndex + 1]) === null || _skeleton$pages === void 0 || (_skeleton$pages = _skeleton$pages.sections[0]) === null || _skeleton$pages === void 0 || (_skeleton$pages = _skeleton$pages.columns[0]) === null || _skeleton$pages === void 0 ? void 0 : _skeleton$pages.lines[0];
		} else {
			var _skeleton$pages2, _prevSections;
			const prevSections = (_skeleton$pages2 = skeleton.pages[currentPageIndex - 1]) === null || _skeleton$pages2 === void 0 ? void 0 : _skeleton$pages2.sections;
			if (prevSections == null) return;
			const prevColumns = (_prevSections = prevSections[prevSections.length - 1]) === null || _prevSections === void 0 ? void 0 : _prevSections.columns;
			const column = prevColumns[prevColumns.length - 1];
			const prevColumnLines = column === null || column === void 0 ? void 0 : column.lines;
			newLine = prevColumnLines[prevColumnLines.length - 1];
		}
		if (newLine != null) return {
			line: newLine,
			offsetLeft
		};
	}
	_getTableLineTargetFromPageLine(line, page, direction, offsetLeft) {
		const table = direction ? findTableAfterLine(line, page) : findTableBeforeLine(line, page);
		if (table == null) return;
		return this._getTableBoundaryLineTarget(table, direction, offsetLeft);
	}
	_getTableBoundaryLineTarget(table, direction, offsetLeft) {
		const rows = direction ? table.rows : [...table.rows].reverse();
		for (const row of rows) {
			if (row.isRepeatRow) continue;
			const target = this._getNearestTableCellLineTarget(row.cells, direction, offsetLeft);
			if (target != null) return target;
		}
	}
	_getAdjacentTableCellLineTarget(cell, direction, offsetLeft) {
		const row = cell.parent;
		const table = row === null || row === void 0 ? void 0 : row.parent;
		if (row == null || table == null) return;
		const hostOffsetLeft = this._getTableCellHostOffsetLeft(cell) + offsetLeft;
		const adjacentRow = this._findAdjacentTableRow(table, row, direction);
		if (adjacentRow != null) return this._getNearestTableCellLineTarget(adjacentRow.cells, direction, hostOffsetLeft);
		const line = this._findLineAroundTable(table, direction);
		if (line == null) return;
		return {
			line,
			offsetLeft: hostOffsetLeft
		};
	}
	_findAdjacentTableRow(table, row, direction) {
		const currentRowIndex = table.rows.indexOf(row);
		if (currentRowIndex === -1) return;
		const step = direction ? 1 : -1;
		let rowIndex = currentRowIndex + step;
		while (rowIndex >= 0 && rowIndex < table.rows.length) {
			const adjacentRow = table.rows[rowIndex];
			if (!adjacentRow.isRepeatRow) return adjacentRow;
			rowIndex += step;
		}
		return this._findAdjacentTableSliceRow(table, direction);
	}
	_findAdjacentTableSliceRow(table, direction) {
		if (!table.tableId.includes("#-#")) return;
		const [sourceTableId, sliceIndexText] = table.tableId.split("#-#");
		const sliceIndex = Number.parseInt(sliceIndexText);
		const tablePage = table.parent;
		const skeleton = tablePage === null || tablePage === void 0 ? void 0 : tablePage.parent;
		if (!Number.isFinite(sliceIndex) || (skeleton === null || skeleton === void 0 ? void 0 : skeleton.pages) == null) return;
		const nextTableId = `${sourceTableId}#-#${sliceIndex + (direction ? 1 : -1)}`;
		for (const page of skeleton.pages) {
			var _page$skeTables;
			const nextTable = (_page$skeTables = page.skeTables) === null || _page$skeTables === void 0 ? void 0 : _page$skeTables.get(nextTableId);
			const row = (nextTable == null ? [] : direction ? nextTable.rows : [...nextTable.rows].reverse()).find((item) => !item.isRepeatRow);
			if (row != null) return row;
		}
	}
	_getNearestTableCellLineTarget(cells, direction, hostOffsetLeft) {
		const candidates = cells.filter((cell) => !cell.isMergedCellCovered).map((cell) => ({
			cell,
			distance: this._getDistanceToTableCell(cell, hostOffsetLeft)
		})).sort((a, b) => a.distance - b.distance);
		for (const candidate of candidates) {
			const line = direction ? firstLineInCell(candidate.cell) : lastLineInCell(candidate.cell);
			if (line != null) return {
				line,
				offsetLeft: hostOffsetLeft - this._getTableCellHostOffsetLeft(candidate.cell)
			};
		}
	}
	_findLineAroundTable(table, direction) {
		var _tablePage$parent;
		const tablePage = table.parent;
		if (tablePage == null) return;
		const pages = ((_tablePage$parent = tablePage.parent) === null || _tablePage$parent === void 0 ? void 0 : _tablePage$parent.pages) ?? [tablePage];
		const lineMatcher = direction ? (line) => line.st === table.ed + 1 : (line) => line.ed === table.st - 1;
		for (const page of pages) for (const section of page.sections) for (const column of section.columns) {
			const line = (direction ? column.lines : [...column.lines].reverse()).find(lineMatcher);
			if (line != null) return line;
		}
	}
	_getDistanceToTableCell(cell, offsetLeft) {
		const left = this._getTableCellHostOffsetLeft(cell);
		const right = left + this._getTableCellContentWidth(cell);
		if (offsetLeft >= left && offsetLeft <= right) return 0;
		return Math.min(Math.abs(offsetLeft - left), Math.abs(offsetLeft - right));
	}
	_getTableCellHostOffsetLeft(cell) {
		const row = cell.parent;
		const table = row === null || row === void 0 ? void 0 : row.parent;
		return ((table === null || table === void 0 ? void 0 : table.left) ?? 0) + (cell.left ?? 0) + (cell.marginLeft ?? 0);
	}
	_getTableCellContentWidth(cell) {
		return Math.max(0, (cell.pageWidth ?? 0) - (cell.marginLeft ?? 0) - (cell.marginRight ?? 0));
	}
	_isTableCellContentPage(page) {
		var _table$rows;
		const row = page.parent;
		const table = row === null || row === void 0 ? void 0 : row.parent;
		return page.type === _univerjs_engine_render.DocumentSkeletonPageType.CELL && row != null && (table === null || table === void 0 || (_table$rows = table.rows) === null || _table$rows === void 0 ? void 0 : _table$rows.includes(row)) === true && row.cells.includes(page);
	}
	_getColumnGroupLineTargetFromBlockLine(line, page, direction, offsetLeft) {
		if (line == null || line.type !== _univerjs_engine_render.LineType.BLOCK) return;
		const columnGroup = this._findColumnGroupByBlockLine(page, line);
		if (columnGroup == null) return;
		return this._getColumnGroupContentLineTarget(columnGroup, direction, offsetLeft);
	}
	_getColumnGroupContentLineTarget(columnGroup, direction, offsetLeft) {
		const candidates = columnGroup.columns.map((column) => ({
			column,
			distance: this._getDistanceToColumnGroupColumn(column, offsetLeft)
		})).sort((a, b) => a.distance - b.distance);
		for (const candidate of candidates) {
			const line = this._getColumnGroupColumnBoundaryLine(candidate.column, direction);
			if (line != null) return {
				line,
				offsetLeft: offsetLeft - candidate.column.left
			};
		}
	}
	_getColumnGroupExitLineTarget(page, direction, offsetLeft) {
		const columnGroupColumn = page.parent;
		const columnGroup = columnGroupColumn === null || columnGroupColumn === void 0 ? void 0 : columnGroupColumn.parent;
		const hostPage = columnGroup === null || columnGroup === void 0 ? void 0 : columnGroup.parent;
		if (columnGroupColumn == null || columnGroup == null || hostPage == null) return;
		const blockLine = this._findColumnGroupBlockLine(columnGroup);
		const line = blockLine == null ? void 0 : this._getAdjacentLineAroundBlockLine(blockLine, direction);
		if (line == null) return;
		return {
			line,
			offsetLeft: columnGroupColumn.left + offsetLeft
		};
	}
	_findColumnGroupByBlockLine(page, line) {
		var _page$skeColumnGroups;
		for (const columnGroup of ((_page$skeColumnGroups = page.skeColumnGroups) === null || _page$skeColumnGroups === void 0 ? void 0 : _page$skeColumnGroups.values()) ?? []) if (line.paragraphIndex === columnGroup.ed || line.st <= columnGroup.ed && line.ed >= columnGroup.st) return columnGroup;
	}
	_findColumnGroupBlockLine(columnGroup) {
		const page = columnGroup.parent;
		if (page == null) return;
		for (const section of page.sections) for (const column of section.columns) for (const line of column.lines) if (line.type === _univerjs_engine_render.LineType.BLOCK && this._findColumnGroupByBlockLine(page, line) === columnGroup) return line;
	}
	_getAdjacentLineAroundBlockLine(line, direction) {
		var _section$columns4;
		const column = line.parent;
		const section = column === null || column === void 0 ? void 0 : column.parent;
		const page = section === null || section === void 0 ? void 0 : section.parent;
		if (column == null || section == null || page == null) return;
		const lineIndex = column.lines.indexOf(line);
		if (lineIndex === -1) return;
		const sameColumnLine = column.lines[direction ? lineIndex + 1 : lineIndex - 1];
		if (sameColumnLine != null) return sameColumnLine;
		const columnIndex = section.columns.indexOf(column);
		if (columnIndex === -1) return;
		if (direction) {
			var _section$columns3;
			return (_section$columns3 = section.columns[columnIndex + 1]) === null || _section$columns3 === void 0 ? void 0 : _section$columns3.lines[0];
		}
		const prevColumnLines = (_section$columns4 = section.columns[columnIndex - 1]) === null || _section$columns4 === void 0 ? void 0 : _section$columns4.lines;
		return prevColumnLines === null || prevColumnLines === void 0 ? void 0 : prevColumnLines[prevColumnLines.length - 1];
	}
	_getColumnGroupColumnBoundaryLine(column, direction) {
		const sections = direction ? column.page.sections : [...column.page.sections].reverse();
		for (const section of sections) {
			const columns = direction ? section.columns : [...section.columns].reverse();
			for (const skeletonColumn of columns) {
				const line = (direction ? skeletonColumn.lines : [...skeletonColumn.lines].reverse()).find((item) => item.type !== _univerjs_engine_render.LineType.BLOCK);
				if (line != null) return line;
			}
		}
	}
	_getDistanceToColumnGroupColumn(column, offsetLeft) {
		if (offsetLeft >= column.left && offsetLeft <= column.left + column.width) return 0;
		return Math.min(Math.abs(offsetLeft - column.left), Math.abs(offsetLeft - column.left - column.width));
	}
	_isColumnGroupContentPage(page) {
		var _parent$parent;
		const parent = page.parent;
		return (parent === null || parent === void 0 ? void 0 : parent.page) === page && ((_parent$parent = parent.parent) === null || _parent$parent === void 0 || (_parent$parent = _parent$parent.columns) === null || _parent$parent === void 0 ? void 0 : _parent$parent.includes(parent)) === true;
	}
	_scrollToFocusNodePosition(unitId, offset) {
		var _this$_renderManagerS;
		const backScrollController = (_this$_renderManagerS = this._renderManagerService.getRenderUnitById(unitId)) === null || _this$_renderManagerS === void 0 ? void 0 : _this$_renderManagerS.with(DocBackScrollRenderController);
		if (backScrollController == null) return;
		backScrollController.scrollToRange({
			startOffset: offset,
			endOffset: offset,
			collapsed: true
		});
	}
};
DocMoveCursorController = __decorate([
	__decorateParam(0, _univerjs_core.IUniverInstanceService),
	__decorateParam(1, _univerjs_engine_render.IRenderManagerService),
	__decorateParam(2, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(3, _univerjs_core.ICommandService)
], DocMoveCursorController);

//#endregion
//#region src/controllers/doc-table.controller.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
let DocTableController = class DocTableController extends _univerjs_core.Disposable {
	constructor(_commandService) {
		super();
		this._commandService = _commandService;
		this._initialize();
	}
	_initialize() {
		this._init();
		this._registerCommands();
	}
	_registerCommands() {
		[DocCreateTableOperation].forEach((command) => this.disposeWithMe(this._commandService.registerCommand(command)));
	}
	_init() {}
};
DocTableController = __decorate([__decorateParam(0, _univerjs_core.ICommandService)], DocTableController);

//#endregion
//#region src/controllers/render-controllers/doc-canvas-popup-layout-interaction.controller.ts
let DocCanvasPopupLayoutInteractionController = class DocCanvasPopupLayoutInteractionController extends _univerjs_core.Disposable {
	constructor(_context, _canvasPopupManagerService, _docLayoutInteractionService) {
		super();
		this._context = _context;
		this._canvasPopupManagerService = _canvasPopupManagerService;
		this._docLayoutInteractionService = _docLayoutInteractionService;
		_defineProperty(this, "_layoutInteraction", null);
		this.disposeWithMe(this._canvasPopupManagerService.popupUnits$.subscribe((popupUnits) => {
			if (popupUnits.has(this._context.unitId)) this._layoutInteraction ??= this._docLayoutInteractionService.beginInteraction();
			else this._endInteraction();
		}));
	}
	dispose() {
		this._endInteraction();
		super.dispose();
	}
	_endInteraction() {
		var _this$_layoutInteract;
		(_this$_layoutInteract = this._layoutInteraction) === null || _this$_layoutInteract === void 0 || _this$_layoutInteract.dispose();
		this._layoutInteraction = null;
	}
};
DocCanvasPopupLayoutInteractionController = __decorate([__decorateParam(1, (0, _univerjs_core.Inject)(DocCanvasPopManagerService)), __decorateParam(2, (0, _univerjs_core.Inject)(DocLayoutInteractionService))], DocCanvasPopupLayoutInteractionController);

//#endregion
//#region src/controllers/render-controllers/doc-checklist.render-controller.ts
let DocChecklistRenderController = class DocChecklistRenderController extends _univerjs_core.Disposable {
	constructor(_context, _docSkeletonManagerService, _commandService, _docEventManagerService, _textSelectionManagerService) {
		super();
		this._context = _context;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		this._commandService = _commandService;
		this._docEventManagerService = _docEventManagerService;
		this._textSelectionManagerService = _textSelectionManagerService;
		this._initPointerDownObserver();
		this._initHoverCursor();
	}
	_initPointerDownObserver() {
		this._docEventManagerService.clickBullets$.subscribe((paragraph) => {
			const textRanges = this._textSelectionManagerService.getTextRanges();
			this._commandService.executeCommand(ToggleCheckListCommand.id, {
				index: paragraph.paragraph.startIndex,
				segmentId: paragraph.segmentId,
				textRanges
			});
		});
	}
	_initHoverCursor() {
		this.disposeWithMe(this._docEventManagerService.hoverBullet$.subscribe((paragraph) => {
			if (paragraph) this._context.mainComponent.setCursor(_univerjs_engine_render.CURSOR_TYPE.POINTER);
			else this._context.mainComponent.setCursor(_univerjs_engine_render.CURSOR_TYPE.TEXT);
		}));
	}
	_getTransformCoordForDocumentOffset(document, viewport, evtOffsetX, evtOffsetY) {
		const { documentTransform } = document.getOffsetConfig();
		const originCoord = viewport.transformVector2SceneCoord(_univerjs_engine_render.Vector2.FromArray([evtOffsetX, evtOffsetY]));
		if (!originCoord) return;
		return documentTransform.clone().invert().applyPoint(originCoord);
	}
};
DocChecklistRenderController = __decorate([
	__decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService)),
	__decorateParam(2, _univerjs_core.ICommandService),
	__decorateParam(3, (0, _univerjs_core.Inject)(DocEventManagerService)),
	__decorateParam(4, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService))
], DocChecklistRenderController);

//#endregion
//#region src/controllers/render-controllers/doc-clipboard.controller.ts
let DocClipboardController = class DocClipboardController extends _univerjs_core.RxDisposable {
	constructor(_context, _commandService, _docClipboardService, _docSelectionRenderService, _contextService, _editorService) {
		super();
		this._context = _context;
		this._commandService = _commandService;
		this._docClipboardService = _docClipboardService;
		this._docSelectionRenderService = _docSelectionRenderService;
		this._contextService = _contextService;
		this._editorService = _editorService;
		this._init();
	}
	_init() {
		this._initLegacyPasteCommand();
	}
	_initLegacyPasteCommand() {
		var _this$_docSelectionRe;
		(_this$_docSelectionRe = this._docSelectionRenderService) === null || _this$_docSelectionRe === void 0 || _this$_docSelectionRe.onPaste$.pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe((config) => {
			var _clipboardEvent$clipb, _clipboardEvent$clipb2, _clipboardEvent$clipb3, _clipboardEvent$clipb4;
			if (!whenDocOrEditor(this._contextService)) return;
			config.event.preventDefault();
			const clipboardEvent = config.event;
			let htmlContent = (_clipboardEvent$clipb = clipboardEvent.clipboardData) === null || _clipboardEvent$clipb === void 0 ? void 0 : _clipboardEvent$clipb.getData(_univerjs_ui.HTML_CLIPBOARD_MIME_TYPE);
			const internalJson = (_clipboardEvent$clipb2 = clipboardEvent.clipboardData) === null || _clipboardEvent$clipb2 === void 0 ? void 0 : _clipboardEvent$clipb2.getData("application/x-doc-fragment+json");
			const textContent = (_clipboardEvent$clipb3 = clipboardEvent.clipboardData) === null || _clipboardEvent$clipb3 === void 0 ? void 0 : _clipboardEvent$clipb3.getData(_univerjs_ui.PLAIN_TEXT_CLIPBOARD_MIME_TYPE);
			const files = [...((_clipboardEvent$clipb4 = clipboardEvent.clipboardData) === null || _clipboardEvent$clipb4 === void 0 ? void 0 : _clipboardEvent$clipb4.items) || []].filter((item) => _univerjs_ui.imageMimeTypeSet.has(item.type)).map((item) => item.getAsFile()).filter((e) => !!e);
			if (!!this._editorService.getEditor(this._context.unitId) && (htmlContent ?? "").indexOf("</table>") > -1) htmlContent = "";
			this._docClipboardService.legacyPaste({
				html: htmlContent,
				internalJson,
				text: textContent,
				files
			});
		});
	}
};
DocClipboardController = __decorate([
	__decorateParam(1, _univerjs_core.ICommandService),
	__decorateParam(2, IDocClipboardService),
	__decorateParam(3, (0, _univerjs_core.Inject)(DocSelectionRenderService)),
	__decorateParam(4, _univerjs_core.IContextService),
	__decorateParam(5, IEditorService)
], DocClipboardController);

//#endregion
//#region src/controllers/render-controllers/doc-contextmenu.render-controller.ts
const SKIP_UNIT_IDS = [
	_univerjs_core.DEFAULT_EMPTY_DOCUMENT_VALUE,
	_univerjs_core.DOCS_FORMULA_BAR_EDITOR_UNIT_ID_KEY,
	_univerjs_core.DOCS_NORMAL_EDITOR_UNIT_ID_KEY
];
let DocContextMenuRenderController = class DocContextMenuRenderController extends _univerjs_core.Disposable {
	constructor(_context, _contextMenuService, _commandService, _docEventManagerService, _docSelectionManagerService, _contextService, _univerInstanceService) {
		super();
		this._context = _context;
		this._contextMenuService = _contextMenuService;
		this._commandService = _commandService;
		this._docEventManagerService = _docEventManagerService;
		this._docSelectionManagerService = _docSelectionManagerService;
		this._contextService = _contextService;
		this._univerInstanceService = _univerInstanceService;
		if (!SKIP_UNIT_IDS.includes(this._context.unitId)) {
			this._initPointerDown();
			this._initEditChange();
		}
	}
	_initPointerDown() {
		var _this$_context;
		const documentsSubscription = ((_this$_context = this._context) === null || _this$_context === void 0 || (_this$_context = _this$_context.mainComponent) === null || _this$_context === void 0 ? void 0 : _this$_context.onPointerDown$).subscribeEvent((event) => {
			if (event.button === 2) {
				var _model$getSelfOrHeade;
				if (this._docEventManagerService.isPointerOnNonChecklistBullet(event.offsetX, event.offsetY)) return;
				if (this._isSelectionInCodeBlock()) return;
				const bound = this._docEventManagerService.getParagraphByOffset(event.offsetX, event.offsetY);
				const model = this._univerInstanceService.getUnit(this._context.unitId);
				const segmentId = (bound === null || bound === void 0 ? void 0 : bound.segmentId) ?? "";
				const paragraph = bound && (model === null || model === void 0 || (_model$getSelfOrHeade = model.getSelfOrHeaderFooterModel(segmentId)) === null || _model$getSelfOrHeade === void 0 || (_model$getSelfOrHeade = _model$getSelfOrHeade.getBody()) === null || _model$getSelfOrHeade === void 0 || (_model$getSelfOrHeade = _model$getSelfOrHeade.paragraphs) === null || _model$getSelfOrHeade === void 0 ? void 0 : _model$getSelfOrHeade.find((item) => item.startIndex === bound.startIndex));
				this._docEventManagerService.contextMenuParagraph = (paragraph === null || paragraph === void 0 ? void 0 : paragraph.paragraphId) ? {
					unitId: this._context.unitId,
					segmentId,
					paragraphId: paragraph.paragraphId
				} : null;
				const position = this._contextService.getContextValue(_univerjs_core.FOCUSING_COMMON_DRAWINGS) ? _univerjs_ui.ContextMenuPosition.DRAWING : _univerjs_ui.ContextMenuPosition.MAIN_AREA;
				this._contextMenuService.triggerContextMenu(event, position);
			}
		});
		this.disposeWithMe(documentsSubscription);
	}
	_initEditChange() {
		this.disposeWithMe(this._commandService.onCommandExecuted((command) => {
			if (command.id === _univerjs_docs.RichTextEditingMutation.id) {
				if (this._contextMenuService.visible) this._contextMenuService.hideContextMenu();
			}
		}));
	}
	_isSelectionInCodeBlock() {
		var _documentDataModel$ge;
		const documentDataModel = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
		const blockRanges = (documentDataModel === null || documentDataModel === void 0 || (_documentDataModel$ge = documentDataModel.getBody()) === null || _documentDataModel$ge === void 0 || (_documentDataModel$ge = _documentDataModel$ge.blockRanges) === null || _documentDataModel$ge === void 0 ? void 0 : _documentDataModel$ge.filter((range) => range.blockType === _univerjs_core.DocumentBlockRangeType.CODE)) ?? [];
		const textRanges = this._docSelectionManagerService.getTextRanges() ?? [];
		if (!blockRanges.length || !textRanges.length) return false;
		return blockRanges.some((blockRange) => textRanges.some((range) => isRangeIntersectingBlock(range, blockRange.startIndex, blockRange.endIndex)));
	}
};
DocContextMenuRenderController = __decorate([
	__decorateParam(1, _univerjs_ui.IContextMenuService),
	__decorateParam(2, _univerjs_core.ICommandService),
	__decorateParam(3, (0, _univerjs_core.Inject)(DocEventManagerService)),
	__decorateParam(4, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(5, _univerjs_core.IContextService),
	__decorateParam(6, (0, _univerjs_core.Inject)(_univerjs_core.IUniverInstanceService))
], DocContextMenuRenderController);
function isRangeIntersectingBlock(range, startIndex, endIndex) {
	return Math.max(range.startOffset, startIndex) <= Math.min(range.endOffset, endIndex);
}

//#endregion
//#region src/controllers/render-controllers/doc-editor-bridge.controller.ts
let DocEditorBridgeController = class DocEditorBridgeController extends _univerjs_core.Disposable {
	constructor(_context, _univerInstanceService, _editorService, _commandService, _docSelectionRenderService, _docSkeletonManagerService, _renderManagerService) {
		super();
		this._context = _context;
		this._univerInstanceService = _univerInstanceService;
		this._editorService = _editorService;
		this._commandService = _commandService;
		this._docSelectionRenderService = _docSelectionRenderService;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		this._renderManagerService = _renderManagerService;
		_defineProperty(this, "_initialEditors", /* @__PURE__ */ new Set());
		this._initialize();
	}
	_initialize() {
		this._editorService.getAllEditor().forEach((editor) => {
			const unitId = editor.getEditorId();
			if (unitId !== this._context.unitId) return;
			if (this._initialEditors.has(unitId)) return;
			this._initialEditors.add(unitId);
			this._resize(unitId);
		});
		this._commandExecutedListener();
		this._initialBlur();
		this._initialFocus();
	}
	_resize(unitId) {
		if (unitId == null) return;
		const editor = this._editorService.getEditor(unitId);
		if ((editor === null || editor === void 0 ? void 0 : editor.cancelDefaultResizeListener) === true) return;
		const editorDataModel = this._univerInstanceService.getUnit(unitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
		if (!editorDataModel) return;
		const skeleton = this._docSkeletonManagerService.getSkeleton();
		if (editor == null || editor.render == null || skeleton == null || editorDataModel == null) return;
		skeleton.calculate();
		const { marginTop = 0, marginBottom = 0, marginLeft = 0, marginRight = 0 } = editorDataModel.getSnapshot().documentStyle;
		const { scene, mainComponent } = editor.render;
		let { actualHeight, actualWidth } = skeleton.getActualSize();
		actualHeight += marginTop + marginBottom;
		actualWidth += marginLeft + marginRight;
		const { width, height } = editor.getBoundingClientRect();
		const contentWidth = Math.max(actualWidth, width);
		const contentHeight = Math.max(actualHeight, height);
		scene.transformByState({
			width: contentWidth,
			height: contentHeight
		});
		mainComponent === null || mainComponent === void 0 || mainComponent.resize(contentWidth, contentHeight);
	}
	_initialBlur() {
		this.disposeWithMe(this._editorService.blur$.subscribe(() => {
			this._docSelectionRenderService.blur();
		}));
		this.disposeWithMe(this._docSelectionRenderService.onBlur$.subscribe(() => {
			const { unitId } = this._context;
			const editor = this._editorService.getEditor(unitId);
			const focusEditor = this._editorService.getFocusEditor();
			if (editor == null || editor.isSheetEditor() || focusEditor && focusEditor.getEditorId() === unitId) return;
			if (unitId.includes("range_selector") || unitId.includes("embedding_formula_editor")) return;
			this._editorService.blur();
		}));
	}
	_initialFocus() {
		const focusExcepts = [
			"univer-formula-search",
			"univer-formula-help",
			"formula-help-decorator",
			"univer-formula-help-param"
		];
		this.disposeWithMe((0, rxjs.fromEvent)(window, "mousedown").subscribe((event) => {
			if ((0, _univerjs_core.checkForSubstrings)(event.target.classList[0] || "", focusExcepts)) event.stopPropagation();
		}));
		const disposableCollection = new _univerjs_core.DisposableCollection();
		this.disposeWithMe(this._univerInstanceService.getCurrentTypeOfUnit$(_univerjs_core.UniverInstanceType.UNIVER_SHEET).subscribe((unit) => {
			disposableCollection.dispose();
			if (!unit) return;
			const unitId = unit.getUnitId();
			const render = this._renderManagerService.getRenderUnitById(unitId);
			const canvasEle = render === null || render === void 0 ? void 0 : render.engine.getCanvas().getCanvasEle();
			if (canvasEle == null) return;
			const disposable = (0, rxjs.fromEvent)(canvasEle, "mousedown").subscribe((evt) => {
				evt.stopPropagation();
			});
			disposableCollection.add(disposable);
		}));
		this.disposeWithMe(() => disposableCollection.dispose());
	}
	/**
	* Listen to document edits to refresh the size of the formula editor.
	*/
	_commandExecutedListener() {
		const updateCommandList = [_univerjs_docs.RichTextEditingMutation.id];
		this.disposeWithMe(this._commandService.onCommandExecuted((command) => {
			if (updateCommandList.includes(command.id)) {
				const { unitId } = command.params;
				if (this._editorService.isSheetEditor(unitId) || unitId !== this._context.unitId) return;
				const editorRenderConfig = this._editorService.getEditorRenderConfig(unitId);
				if (editorRenderConfig && !editorRenderConfig.scrollBar) this._resize(unitId);
			}
		}));
	}
};
DocEditorBridgeController = __decorate([
	__decorateParam(1, _univerjs_core.IUniverInstanceService),
	__decorateParam(2, IEditorService),
	__decorateParam(3, _univerjs_core.ICommandService),
	__decorateParam(4, (0, _univerjs_core.Inject)(DocSelectionRenderService)),
	__decorateParam(5, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService)),
	__decorateParam(6, _univerjs_engine_render.IRenderManagerService)
], DocEditorBridgeController);

//#endregion
//#region src/controllers/render-controllers/doc-ime-input.controller.ts
let DocIMEInputController = class DocIMEInputController extends _univerjs_core.Disposable {
	constructor(_context, _docSelectionRenderService, _docImeInputManagerService, _docSkeletonManagerService, _docStateEmitService, _docSelectionManagerService, _commandService) {
		super();
		this._context = _context;
		this._docSelectionRenderService = _docSelectionRenderService;
		this._docImeInputManagerService = _docImeInputManagerService;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		this._docStateEmitService = _docStateEmitService;
		this._docSelectionManagerService = _docSelectionManagerService;
		this._commandService = _commandService;
		_defineProperty(this, "_previousIMEContent", "");
		_defineProperty(this, "_isCompositionStart", true);
		_defineProperty(this, "_onStartSubscription", void 0);
		_defineProperty(this, "_onUpdateSubscription", void 0);
		_defineProperty(this, "_onEndSubscription", void 0);
		this._initialize();
	}
	dispose() {
		var _this$_onStartSubscri, _this$_onUpdateSubscr, _this$_onEndSubscript;
		(_this$_onStartSubscri = this._onStartSubscription) === null || _this$_onStartSubscri === void 0 || _this$_onStartSubscri.unsubscribe();
		(_this$_onUpdateSubscr = this._onUpdateSubscription) === null || _this$_onUpdateSubscr === void 0 || _this$_onUpdateSubscr.unsubscribe();
		(_this$_onEndSubscript = this._onEndSubscription) === null || _this$_onEndSubscript === void 0 || _this$_onEndSubscript.unsubscribe();
	}
	_initialize() {
		this._initialOnCompositionstart();
		this._initialOnCompositionUpdate();
		this._initialOnCompositionend();
	}
	_initialOnCompositionstart() {
		this._onStartSubscription = this._docSelectionRenderService.onCompositionstart$.subscribe((config) => {
			var _this$_docSelectionMa;
			if (config == null) return;
			this._resetIME();
			const { activeRange, rangeList = [] } = config;
			if (activeRange == null) return;
			this._docImeInputManagerService.setActiveRange(_univerjs_core.Tools.deepClone(activeRange));
			this._docImeInputManagerService.setPreviousDocRanges(_univerjs_core.Tools.deepClone([...rangeList, ...this._docSelectionRenderService.getAllRectRanges()]));
			this._docImeInputManagerService.setPreviousSelectionOptions(_univerjs_core.Tools.deepClone(((_this$_docSelectionMa = this._docSelectionManagerService.getSelectionInfo()) === null || _this$_docSelectionMa === void 0 ? void 0 : _this$_docSelectionMa.options) ?? null));
		});
	}
	_initialOnCompositionUpdate() {
		this._onUpdateSubscription = this._docSelectionRenderService.onCompositionupdate$.subscribe((config) => {
			this._updateContent(config, true);
		});
	}
	_initialOnCompositionend() {
		this._onEndSubscription = this._docSelectionRenderService.onCompositionend$.subscribe((config) => {
			this._updateContent(config, false);
		});
	}
	async _updateContent(config, isUpdate) {
		if (config == null) return;
		const unitId = this._context.unitId;
		const skeleton = this._docSkeletonManagerService.getSkeleton();
		const { event, activeRange } = config;
		if (skeleton == null || activeRange == null) return;
		const content = event.data;
		if (content === this._previousIMEContent && isUpdate) return;
		/**
		* IME composition is canceled when the composition content is the same as the previous one on composition end.
		* In this case, we need to finalize the composition and reset the IME state.
		*/
		if (!isUpdate && content === this._previousIMEContent) {
			this._finalizeComposition();
			this._resetIME();
			return;
		}
		await this._commandService.executeCommand(IMEInputCommand.id, {
			unitId,
			newText: content,
			oldTextLen: this._previousIMEContent.length,
			isCompositionStart: this._isCompositionStart,
			isCompositionEnd: !isUpdate
		});
		if (isUpdate) {
			if (this._isCompositionStart) this._isCompositionStart = false;
			this._previousIMEContent = content;
		} else this._resetIME();
	}
	_finalizeComposition() {
		const previousActiveRange = this._docImeInputManagerService.getActiveRange();
		if (previousActiveRange == null) return;
		this._docStateEmitService.emitStateChangeInfo({
			commandId: _univerjs_docs.RichTextEditingMutation.id,
			unitId: this._context.unitId,
			segmentId: previousActiveRange.segmentId,
			trigger: IMEInputCommand.id,
			redoState: {
				actions: [],
				textRanges: this._docImeInputManagerService.getPreviousDocRanges().length ? this._docImeInputManagerService.getPreviousDocRanges() : [previousActiveRange]
			},
			undoState: {
				actions: [],
				textRanges: this._docImeInputManagerService.getPreviousDocRanges().length ? this._docImeInputManagerService.getPreviousDocRanges() : [previousActiveRange],
				options: this._docImeInputManagerService.getPreviousSelectionOptions() ?? void 0
			},
			isCompositionEnd: true
		});
	}
	_resetIME() {
		this._previousIMEContent = "";
		this._isCompositionStart = true;
		this._docImeInputManagerService.clearUndoRedoMutationParamsCache();
		this._docImeInputManagerService.setActiveRange(null);
		this._docImeInputManagerService.setPreviousDocRanges([]);
		this._docImeInputManagerService.setPreviousSelectionOptions(null);
	}
};
DocIMEInputController = __decorate([
	__decorateParam(1, (0, _univerjs_core.Inject)(DocSelectionRenderService)),
	__decorateParam(2, (0, _univerjs_core.Inject)(DocIMEInputManagerService)),
	__decorateParam(3, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService)),
	__decorateParam(4, (0, _univerjs_core.Inject)(_univerjs_docs.DocStateEmitService)),
	__decorateParam(5, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(6, _univerjs_core.ICommandService)
], DocIMEInputController);

//#endregion
//#region src/controllers/render-controllers/doc-input.controller.ts
let DocInputController = class DocInputController extends _univerjs_core.Disposable {
	constructor(_context, _docSelectionRenderService, _docSkeletonManagerService, _commandService, _docMenuStyleService, _embedInteractionBoundaryService, _embedRuntimeFocusCoordinator) {
		super();
		this._context = _context;
		this._docSelectionRenderService = _docSelectionRenderService;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		this._commandService = _commandService;
		this._docMenuStyleService = _docMenuStyleService;
		this._embedRuntimeFocusCoordinator = _embedRuntimeFocusCoordinator;
		_defineProperty(this, "_onInputSubscription", void 0);
		this._init();
	}
	dispose() {
		var _this$_onInputSubscri;
		super.dispose();
		(_this$_onInputSubscri = this._onInputSubscription) === null || _this$_onInputSubscri === void 0 || _this$_onInputSubscri.unsubscribe();
	}
	_init() {
		this._initialNormalInput();
	}
	_initialNormalInput() {
		this._onInputSubscription = this._docSelectionRenderService.onInput$.subscribe(async (config) => {
			var _docDataModel$getSelf, _originBody$blockRang, _originBody$columnGro, _originBody$customBlo, _originBody$tables;
			if (config == null) return;
			const { unitId } = this._context;
			const { event, content = "", activeRange, rangeList = [] } = config;
			const e = event;
			if (e.defaultPrevented) return;
			if (!_univerjs_core.SHEET_EDITOR_UNITS.includes(unitId) && this._isEmbedChildInputActive(unitId, e)) return;
			const skeleton = this._docSkeletonManagerService.getSkeleton();
			if (e.data == null || skeleton == null || activeRange == null) return;
			const { segmentId } = activeRange;
			const originBody = (_docDataModel$getSelf = this._context.unit.getSelfOrHeaderFooterModel(segmentId)) === null || _docDataModel$getSelf === void 0 ? void 0 : _docDataModel$getSelf.getBody();
			if (!originBody) return;
			const defaultTextStyle = this._docMenuStyleService.getDefaultStyle();
			const cacheStyle = this._docMenuStyleService.getStyleCache();
			const curCustomRange = getCustomRangeAtPosition((originBody === null || originBody === void 0 ? void 0 : originBody.customRanges) ?? [], activeRange.endOffset, _univerjs_core.SHEET_EDITOR_UNITS.includes(unitId));
			const curTextRun = getTextRunAtPosition(originBody, activeRange.endOffset, defaultTextStyle, cacheStyle, _univerjs_core.SHEET_EDITOR_UNITS.includes(unitId));
			const curCustomDecorations = getCustomDecorationAtPosition((originBody === null || originBody === void 0 ? void 0 : originBody.customDecorations) ?? [], activeRange.endOffset);
			const insertBody = {
				dataStream: content,
				textRuns: curTextRun ? [{
					...curTextRun,
					st: 0,
					ed: content.length
				}] : [],
				customRanges: curCustomRange ? [{
					...curCustomRange,
					startIndex: 0,
					endIndex: content.length - 1
				}] : [],
				customDecorations: curCustomDecorations.map((customDecoration) => ({
					...customDecoration,
					startIndex: 0,
					endIndex: content.length - 1
				}))
			};
			if (!activeRange.collapsed && (Boolean((_originBody$blockRang = originBody.blockRanges) === null || _originBody$blockRang === void 0 ? void 0 : _originBody$blockRang.length) || Boolean((_originBody$columnGro = originBody.columnGroups) === null || _originBody$columnGro === void 0 ? void 0 : _originBody$columnGro.length) || Boolean((_originBody$customBlo = originBody.customBlocks) === null || _originBody$customBlo === void 0 ? void 0 : _originBody$customBlo.length) || Boolean((_originBody$tables = originBody.tables) === null || _originBody$tables === void 0 ? void 0 : _originBody$tables.length)) || rangeList.length > 1 || this._docSelectionRenderService.getAllRectRanges().length > 0) await this._commandService.executeCommand(ReplaceSelectionCommand.id, {
				unitId,
				body: insertBody,
				segmentId
			});
			else await this._commandService.executeCommand(_univerjs_docs.InsertTextCommand.id, {
				unitId,
				body: insertBody,
				range: activeRange,
				segmentId
			});
			if (content === " ") await this._commandService.executeCommand(AfterSpaceCommand.id);
		});
	}
	_isEmbedChildInputActive(unitId, event) {
		var _this$_embedRuntimeFo, _this$_embedRuntimeFo2, _this$_embedRuntimeFo3;
		if ((_this$_embedRuntimeFo = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo === void 0 ? void 0 : _this$_embedRuntimeFo.isChildUnitRuntimeEvent(unitId, event.target, event)) return false;
		if ((_this$_embedRuntimeFo2 = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo2 === void 0 ? void 0 : _this$_embedRuntimeFo2.isChildUnitInActiveSession(unitId)) return false;
		if ((_this$_embedRuntimeFo3 = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo3 === void 0 ? void 0 : _this$_embedRuntimeFo3.shouldSuppressHostInteraction(unitId, event.target, event)) return true;
		return false;
	}
};
DocInputController = __decorate([
	__decorateParam(1, (0, _univerjs_core.Inject)(DocSelectionRenderService)),
	__decorateParam(2, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService)),
	__decorateParam(3, _univerjs_core.ICommandService),
	__decorateParam(4, (0, _univerjs_core.Inject)(DocMenuStyleService)),
	__decorateParam(5, (0, _univerjs_core.Optional)(IDocEmbedInteractionBoundaryService)),
	__decorateParam(6, (0, _univerjs_core.Optional)(IDocEmbedRuntimeFocusCoordinator))
], DocInputController);

//#endregion
//#region src/controllers/render-controllers/doc-layout-progress.render-controller.ts
const DOC_LAYOUT_PROGRESS_DELAY_MS = 2e3;
const DOC_LAYOUT_PROGRESS_CANCEL_GRACE_MS = 1e3;
function resolveDocLayoutProgressPercent(progress) {
	if (progress.complete) return 100;
	const blockProgress = progress.totalBlockCount > 0 ? progress.processedBlockCount / progress.totalBlockCount : 0;
	const estimatedPageCount = Math.max(progress.estimatedPageCount, progress.pageCount);
	const pageProgress = progress.mode === "paginated" && estimatedPageCount > 0 ? progress.publishedPageCount / estimatedPageCount : 0;
	return Math.min(99, Math.max(0, Math.floor(Math.max(blockProgress, pageProgress) * 100)));
}
let DocLayoutProgressRenderController = class DocLayoutProgressRenderController extends _univerjs_core.RxDisposable {
	constructor(_context, _docSkeletonManagerService, _docLayoutProgressService) {
		super();
		this._context = _context;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		this._docLayoutProgressService = _docLayoutProgressService;
		_defineProperty(this, "_visible", false);
		_defineProperty(this, "_delayTimer", null);
		_defineProperty(this, "_cancelTimer", null);
		_defineProperty(this, "_latestProgress", null);
		_defineProperty(this, "_layoutStartedAt", null);
		_defineProperty(this, "_maxProgress", 0);
		this._docSkeletonManagerService.currentSkeleton$.pipe((0, rxjs.switchMap)((skeleton) => {
			this._reset();
			return (skeleton === null || skeleton === void 0 ? void 0 : skeleton.layoutProgress$) ?? rxjs.EMPTY;
		}), (0, rxjs.takeUntil)(this.dispose$)).subscribe((progress) => this._handleProgress(progress));
	}
	dispose() {
		this._reset();
		super.dispose();
	}
	_handleProgress(progress) {
		if (progress.cancelled) {
			this._latestProgress = null;
			this._scheduleCancelledClear();
			return;
		}
		this._clearCancelTimer();
		if (progress.complete) {
			this._reset();
			return;
		}
		this._latestProgress = progress;
		this._layoutStartedAt ??= Date.now();
		this._maxProgress = Math.max(this._maxProgress, resolveDocLayoutProgressPercent(progress));
		if (this._visible) this._docLayoutProgressService.setProgress(this._context.unitId, this._maxProgress);
		this._scheduleShow();
	}
	_scheduleShow() {
		if (this._visible || this._delayTimer != null || this._layoutStartedAt == null) return;
		const remainingDelay = Math.max(0, DOC_LAYOUT_PROGRESS_DELAY_MS - (Date.now() - this._layoutStartedAt));
		if (remainingDelay === 0) {
			this._show();
			return;
		}
		this._delayTimer = setTimeout(() => {
			this._delayTimer = null;
			this._show();
		}, remainingDelay);
	}
	_show() {
		if (this._visible || this._latestProgress == null || this._latestProgress.complete) return;
		this._visible = true;
		this._docLayoutProgressService.setProgress(this._context.unitId, this._maxProgress);
	}
	_scheduleCancelledClear() {
		if (this._cancelTimer != null) return;
		this._cancelTimer = setTimeout(() => {
			this._cancelTimer = null;
			if (this._latestProgress != null) return;
			this._reset();
		}, DOC_LAYOUT_PROGRESS_CANCEL_GRACE_MS);
	}
	_clearCancelTimer() {
		if (this._cancelTimer != null) {
			clearTimeout(this._cancelTimer);
			this._cancelTimer = null;
		}
	}
	_reset() {
		if (this._delayTimer != null) {
			clearTimeout(this._delayTimer);
			this._delayTimer = null;
		}
		this._clearCancelTimer();
		this._docLayoutProgressService.clearProgress(this._context.unitId);
		this._visible = false;
		this._latestProgress = null;
		this._layoutStartedAt = null;
		this._maxProgress = 0;
	}
};
DocLayoutProgressRenderController = __decorate([__decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService)), __decorateParam(2, (0, _univerjs_core.Inject)(DocLayoutProgressService))], DocLayoutProgressRenderController);

//#endregion
//#region src/controllers/render-controllers/doc-layout-recovery.render-controller.ts
function acquireDocLayoutRecoveryInteractionLock(canvas, shortcutService, blurSelection) {
	const previousPointerEvents = canvas.style.pointerEvents;
	const previousOpacity = canvas.style.opacity;
	const previousAriaBusy = canvas.getAttribute("aria-busy");
	const shortcutLock = shortcutService.forceDisable();
	blurSelection();
	canvas.style.pointerEvents = "none";
	canvas.style.opacity = "0.55";
	canvas.setAttribute("aria-busy", "true");
	return (0, _univerjs_core.toDisposable)(() => {
		shortcutLock.dispose();
		canvas.style.pointerEvents = previousPointerEvents;
		canvas.style.opacity = previousOpacity;
		if (previousAriaBusy == null) canvas.removeAttribute("aria-busy");
		else canvas.setAttribute("aria-busy", previousAriaBusy);
	});
}
let DocLayoutRecoveryRenderController = class DocLayoutRecoveryRenderController extends _univerjs_core.RxDisposable {
	constructor(_context, _layoutExecutorService, _docPopupManagerService, _docSelectionRenderService, _shortcutService) {
		super();
		this._context = _context;
		this._layoutExecutorService = _layoutExecutorService;
		this._docPopupManagerService = _docPopupManagerService;
		this._docSelectionRenderService = _docSelectionRenderService;
		this._shortcutService = _shortcutService;
		_defineProperty(this, "_popup", null);
		_defineProperty(this, "_interactionLock", null);
		_defineProperty(this, "_visible", false);
		this._layoutExecutorService.executorStatus$.pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe((status) => {
			if (status.state === _univerjs_docs.DocLayoutExecutorState.RECOVERING && status.recoveryUnitId === this._context.unitId) this._show();
			else this._hide();
		});
	}
	dispose() {
		this._hide();
		super.dispose();
	}
	_show() {
		if (this._visible) return;
		const canvas = this._context.engine.getCanvasElement();
		this._visible = true;
		try {
			this._interactionLock = acquireDocLayoutRecoveryInteractionLock(canvas, this._shortcutService, () => this._docSelectionRenderService.blur());
			this._popup = this._docPopupManagerService.attachPopupToRect(() => ({
				left: this._context.scene.width / 2,
				right: this._context.scene.width / 2,
				top: this._context.scene.height / 2,
				bottom: this._context.scene.height / 2
			}), {
				componentKey: DOC_LAYOUT_RECOVERY_COMPONENT,
				customActive: true,
				direction: "vertical-center",
				zIndex: 1021
			}, this._context.unitId);
		} catch {
			this._restoreInteraction();
		}
	}
	_hide() {
		var _this$_popup;
		if (!this._visible) return;
		(_this$_popup = this._popup) === null || _this$_popup === void 0 || _this$_popup.dispose();
		this._popup = null;
		this._restoreInteraction();
	}
	_restoreInteraction() {
		var _this$_interactionLoc;
		(_this$_interactionLoc = this._interactionLock) === null || _this$_interactionLoc === void 0 || _this$_interactionLoc.dispose();
		this._interactionLock = null;
		this._visible = false;
	}
};
DocLayoutRecoveryRenderController = __decorate([
	__decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_docs.DocLayoutExecutorService)),
	__decorateParam(2, (0, _univerjs_core.Inject)(DocCanvasPopManagerService)),
	__decorateParam(3, (0, _univerjs_core.Inject)(DocSelectionRenderService)),
	__decorateParam(4, _univerjs_ui.IShortcutService)
], DocLayoutRecoveryRenderController);

//#endregion
//#region src/controllers/render-controllers/doc-resize.render-controller.ts
function hasRenderableDocSkeleton(component) {
	if (typeof component !== "object" || component == null || !("getSkeleton" in component) || typeof component.getSkeleton !== "function") return false;
	const skeleton = component.getSkeleton();
	if (typeof skeleton !== "object" || skeleton == null || !("getSkeletonData" in skeleton) || typeof skeleton.getSkeletonData !== "function") return false;
	const skeletonData = skeleton.getSkeletonData();
	return typeof skeletonData === "object" && skeletonData != null && "pages" in skeletonData && Array.isArray(skeletonData.pages) && skeletonData.pages.length > 0;
}
let DocResizeRenderController = class DocResizeRenderController extends _univerjs_core.Disposable {
	constructor(_context, _docPageLayoutService, _textSelectionManagerService, _sidebarService) {
		super();
		this._context = _context;
		this._docPageLayoutService = _docPageLayoutService;
		this._textSelectionManagerService = _textSelectionManagerService;
		this._sidebarService = _sidebarService;
		const unitId = this._context.unitId;
		if ((0, _univerjs_core.isInternalEditorID)(unitId)) return this;
		this._initResize();
	}
	_initResize() {
		this.disposeWithMe((0, _univerjs_core.fromEventSubject)(this._context.engine.onTransformChange$).pipe((0, rxjs.throttleTime)(0, rxjs.animationFrameScheduler)).subscribe(() => this._refreshLayoutAndSelection()));
		this.disposeWithMe(this._sidebarService.sidebarOptions$.pipe((0, rxjs.observeOn)(rxjs.animationFrameScheduler)).subscribe(() => this._refreshLayoutAndSelection()));
	}
	_refreshLayoutAndSelection() {
		if (this._disposed) return;
		if (!hasRenderableDocSkeleton(this._context.mainComponent)) return;
		this._docPageLayoutService.calculatePagePosition();
		const unitId = this._context.unitId;
		this._textSelectionManagerService.refreshSelection({
			unitId,
			subUnitId: unitId
		});
	}
};
DocResizeRenderController = __decorate([
	__decorateParam(1, (0, _univerjs_core.Inject)(DocPageLayoutService)),
	__decorateParam(2, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(3, _univerjs_ui.ISidebarService)
], DocResizeRenderController);

//#endregion
//#region src/controllers/render-controllers/doc-selection-render.controller.ts
let DocSelectionRenderController = class DocSelectionRenderController extends _univerjs_core.Disposable {
	constructor(_context, _commandService, _editorService, _instanceSrv, _docSelectionRenderService, _docSkeletonManagerService, _docSelectionManagerService, _embedInteractionBoundaryService, _embedRuntimeFocusCoordinator) {
		super();
		this._context = _context;
		this._commandService = _commandService;
		this._editorService = _editorService;
		this._instanceSrv = _instanceSrv;
		this._docSelectionRenderService = _docSelectionRenderService;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		this._docSelectionManagerService = _docSelectionManagerService;
		this._embedRuntimeFocusCoordinator = _embedRuntimeFocusCoordinator;
		_defineProperty(this, "_loadedMap", /* @__PURE__ */ new WeakSet());
		_defineProperty(this, "_deferredEditorFocusTimer", null);
		this._initialize();
	}
	_initialize() {
		this._init();
		this._refreshListener();
		this._syncSelection();
		this._skeletonListener();
		this._commandExecutedListener();
	}
	_init() {
		const { unitId } = this._context;
		const docObject = neoGetDocObject(this._context);
		if (docObject == null || docObject.document == null) return;
		if (!this._loadedMap.has(docObject.document)) {
			this._initialMain(unitId);
			this._loadedMap.add(docObject.document);
		}
	}
	_refreshListener() {
		this.disposeWithMe(this._docSelectionManagerService.refreshSelection$.subscribe((params) => {
			if (params == null) return;
			const { unitId, docRanges, isEditing, options } = params;
			if (unitId !== this._context.unitId) return;
			this._docSelectionRenderService.replaceDocRanges(docRanges, isEditing, options);
		}));
	}
	_syncSelection() {
		this.disposeWithMe(this._docSelectionRenderService.textSelectionInner$.subscribe((params) => {
			if (params == null) return;
			if (!(0, _univerjs_core.isInternalEditorID)(this._context.unitId) && this._isEmbedChildInteractionActive(this._context.unitId)) return;
			this._docSelectionManagerService.__replaceTextRangesWithNoRefresh(params, {
				unitId: this._context.unitId,
				subUnitId: this._context.unitId
			});
		}));
	}
	_initialMain(unitId) {
		const { document, scene } = neoGetDocObject(this._context);
		this.disposeWithMe(document.onPointerEnter$.subscribeEvent(() => {
			if (this._isEditorReadOnly(unitId)) return;
			document.cursor = _univerjs_engine_render.CURSOR_TYPE.TEXT;
		}));
		this.disposeWithMe(document.onPointerLeave$.subscribeEvent(() => {
			document.cursor = _univerjs_engine_render.CURSOR_TYPE.DEFAULT;
			scene.resetCursor();
		}));
		this.disposeWithMe(document.onPointerDown$.subscribeEvent((evt, state) => {
			var _this$_editorService$;
			if (this._isEditorReadOnly(unitId)) return;
			if (this._isEmbedInteractionEvent(evt, unitId)) return;
			const docDataModel = this._instanceSrv.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
			if ((docDataModel === null || docDataModel === void 0 ? void 0 : docDataModel.getUnitId()) !== unitId) this._instanceSrv.setCurrentUnitForType(unitId);
			if (!((_this$_editorService$ = this._editorService.getEditorRenderConfig(unitId)) === null || _this$_editorService$ === void 0 ? void 0 : _this$_editorService$.preserveHostFocus)) this._instanceSrv.focusUnit(unitId);
			const skeleton = this._docSkeletonManagerService.getSkeleton();
			const { offsetX, offsetY } = evt;
			const coord = this._getTransformCoordForDocumentOffset(offsetX, offsetY);
			if (coord != null) {
				const { pageLayoutType = _univerjs_engine_render.PageLayoutType.VERTICAL, pageMarginLeft, pageMarginTop } = document.getOffsetConfig();
				const { editArea } = skeleton.findEditAreaByCoord(coord, pageLayoutType, pageMarginLeft, pageMarginTop);
				const viewModel = this._docSkeletonManagerService.getViewModel();
				const preEditArea = viewModel.getEditArea();
				if (preEditArea !== _univerjs_engine_render.DocumentEditArea.BODY && editArea !== _univerjs_engine_render.DocumentEditArea.BODY && editArea !== preEditArea) viewModel.setEditArea(editArea);
			}
			this._docSelectionRenderService.__onPointDown(evt);
			if (this._editorService.getEditor(unitId)) {
				/**
				* To accommodate focus switching between different editors.
				* Since the editor for Univer is canvas-based,
				* it primarily relies on focus and cannot use the focus event.
				* Our editor's focus monitoring is based on PointerDown.
				* The order of occurrence is such that PointerDown comes first.
				* Translate the above text into English.
				*/
				this._setEditorFocus(unitId);
				const { offsetX, offsetY } = evt;
				if (this._deferredEditorFocusTimer != null) clearTimeout(this._deferredEditorFocusTimer);
				this._deferredEditorFocusTimer = setTimeout(() => {
					this._deferredEditorFocusTimer = null;
					if (unitId === this._editorService.getFocusId() || this._docSelectionRenderService.isOnPointerEvent) return;
					this._setEditorFocus(unitId);
					this._docSelectionRenderService.setCursorManually(offsetX, offsetY);
				}, 0);
			}
			if (evt.button !== 2) state.stopPropagation();
		}));
		this.disposeWithMe(document.onDblclick$.subscribeEvent((evt) => {
			if (this._isEditorReadOnly(unitId)) return;
			if (this._isEmbedInteractionEvent(evt, unitId)) return;
			this._docSelectionRenderService.__handleDblClick(evt);
		}));
		this.disposeWithMe(document.onTripleClick$.subscribeEvent((evt) => {
			if (this._isEditorReadOnly(unitId)) return;
			if (this._isEmbedInteractionEvent(evt, unitId)) return;
			this._docSelectionRenderService.__handleTripleClick(evt);
		}));
	}
	dispose() {
		if (this._deferredEditorFocusTimer != null) {
			clearTimeout(this._deferredEditorFocusTimer);
			this._deferredEditorFocusTimer = null;
		}
		super.dispose();
	}
	_getTransformCoordForDocumentOffset(evtOffsetX, evtOffsetY) {
		const { document, scene } = neoGetDocObject(this._context);
		const { documentTransform } = document.getOffsetConfig();
		const activeViewport = scene.getViewports()[0];
		if (activeViewport == null) return;
		const originCoord = activeViewport.transformVector2SceneCoord(_univerjs_engine_render.Vector2.FromArray([evtOffsetX, evtOffsetY]));
		return documentTransform.clone().invert().applyPoint(originCoord);
	}
	_isEditorReadOnly(unitId) {
		const editor = this._editorService.getEditor(unitId);
		if (!editor) return false;
		return editor.isReadOnly();
	}
	_setEditorFocus(unitId) {
		this._editorService.focus(unitId);
	}
	_isEmbedInteractionEvent(evt, unitId) {
		var _this$_embedRuntimeFo, _this$_embedRuntimeFo2, _this$_embedRuntimeFo3;
		if ((0, _univerjs_core.isInternalEditorID)(unitId)) return false;
		const target = evt.target;
		if ((_this$_embedRuntimeFo = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo === void 0 ? void 0 : _this$_embedRuntimeFo.isChildUnitRuntimeEvent(unitId, target, evt)) return false;
		if ((_this$_embedRuntimeFo2 = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo2 === void 0 ? void 0 : _this$_embedRuntimeFo2.isChildUnitInActiveSession(unitId)) return false;
		if ((_this$_embedRuntimeFo3 = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo3 === void 0 ? void 0 : _this$_embedRuntimeFo3.shouldSuppressHostInteraction(unitId, target, evt)) return true;
		return isEmbedInteractionEvent(evt);
	}
	_isEmbedChildInteractionActive(unitId) {
		var _this$_embedRuntimeFo4;
		return ((_this$_embedRuntimeFo4 = this._embedRuntimeFocusCoordinator) === null || _this$_embedRuntimeFo4 === void 0 ? void 0 : _this$_embedRuntimeFo4.shouldSuppressHostInteraction(unitId)) === true;
	}
	_commandExecutedListener() {
		const updateCommandList = [SetDocZoomRatioOperation.id];
		this.disposeWithMe(this._commandService.onCommandExecuted((command) => {
			if (updateCommandList.includes(command.id)) {
				var _this$_docSelectionMa;
				const { unitId: documentId } = command.params;
				if (documentId !== ((_this$_docSelectionMa = this._docSelectionManagerService.__getCurrentSelection()) === null || _this$_docSelectionMa === void 0 ? void 0 : _this$_docSelectionMa.unitId)) return;
				if (this._isEmbedChildInteractionActive(documentId)) return;
				this._docSelectionManagerService.refreshSelection();
			}
		}));
	}
	_skeletonListener() {
		let selectionInitialized = false;
		this.disposeWithMe(this._docSkeletonManagerService.currentSkeleton$.subscribe((skeleton) => {
			if (!skeleton) return;
			const { unitId } = this._context;
			if (!(0, _univerjs_core.isInternalEditorID)(unitId)) {
				if (this._isEmbedChildInteractionActive(unitId)) return;
				if (selectionInitialized) return;
				selectionInitialized = true;
				this._docSelectionRenderService.focus();
				const offset = findFirstCursorOffset(this._context.unit.getSnapshot());
				const selectionTarget = {
					unitId,
					subUnitId: unitId
				};
				this._docSelectionManagerService.replaceSelectionInfoWithoutRefresh({
					textRanges: [{
						startOffset: offset,
						endOffset: offset,
						collapsed: true,
						isActive: true
					}],
					rectRanges: [],
					segmentId: "",
					segmentPage: -1,
					isEditing: false,
					style: _univerjs_engine_render.NORMAL_TEXT_SELECTION_PLUGIN_STYLE
				}, selectionTarget);
				this._docSelectionManagerService.refreshSelection(selectionTarget, false);
				if (this._docSelectionRenderService.getActiveTextRange() == null) this.disposeWithMe(skeleton.layoutProgress$.pipe((0, rxjs.filter)((progress) => (progress === null || progress === void 0 ? void 0 : progress.anchorReady) === true || (progress === null || progress === void 0 ? void 0 : progress.complete) === true), (0, rxjs.take)(1)).subscribe(() => this._docSelectionManagerService.refreshSelection(selectionTarget, false)));
			}
		}));
	}
};
DocSelectionRenderController = __decorate([
	__decorateParam(1, _univerjs_core.ICommandService),
	__decorateParam(2, IEditorService),
	__decorateParam(3, _univerjs_core.IUniverInstanceService),
	__decorateParam(4, (0, _univerjs_core.Inject)(DocSelectionRenderService)),
	__decorateParam(5, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService)),
	__decorateParam(6, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(7, (0, _univerjs_core.Optional)(IDocEmbedInteractionBoundaryService)),
	__decorateParam(8, (0, _univerjs_core.Optional)(IDocEmbedRuntimeFocusCoordinator))
], DocSelectionRenderController);
function isEmbedInteractionEvent(evt) {
	var _document$elementFrom;
	const target = evt.target;
	if (typeof Element !== "undefined" && target instanceof Element && target.closest(`[${"data-embed-interaction-boundary-owner"}]`) != null) return true;
	if (typeof document === "undefined") return false;
	const point = getEventClientPoint(evt, target);
	const clientX = point === null || point === void 0 ? void 0 : point.clientX;
	const clientY = point === null || point === void 0 ? void 0 : point.clientY;
	if (typeof clientX !== "number" || typeof clientY !== "number" || !Number.isFinite(clientX) || !Number.isFinite(clientY)) return false;
	if (typeof document.elementFromPoint !== "function") return false;
	return ((_document$elementFrom = document.elementFromPoint(clientX, clientY)) === null || _document$elementFrom === void 0 ? void 0 : _document$elementFrom.closest(`[${DOC_EMBED_INTERACTION_BOUNDARY_OWNER_ATTRIBUTE}]`)) != null;
}
function getEventClientPoint(evt, target) {
	if (Number.isFinite(evt.clientX) && Number.isFinite(evt.clientY)) return {
		clientX: evt.clientX,
		clientY: evt.clientY
	};
	if (typeof Element !== "undefined" && target instanceof Element && Number.isFinite(evt.offsetX) && Number.isFinite(evt.offsetY)) {
		const rect = target.getBoundingClientRect();
		return {
			clientX: rect.left + evt.offsetX,
			clientY: rect.top + evt.offsetY
		};
	}
	if (Number.isFinite(evt.x) && Number.isFinite(evt.y)) return {
		clientX: evt.x,
		clientY: evt.y
	};
}

//#endregion
//#region src/controllers/render-controllers/zoom.render-controller.ts
function shouldHandleDocWheelZoom(event, focusingDoc, _documentFlavor) {
	return focusingDoc && (event.ctrlKey || event.metaKey);
}
let DocZoomRenderController = class DocZoomRenderController extends _univerjs_core.Disposable {
	constructor(_context, _contextService, _docSkeletonManagerService, _univerInstanceService, _commandService, _textSelectionManagerService, _editorService, _docPageLayoutService, _renderManagerService, _docViewScaleService, _embedInteractionBoundaryService) {
		super();
		this._context = _context;
		this._contextService = _contextService;
		this._docSkeletonManagerService = _docSkeletonManagerService;
		this._univerInstanceService = _univerInstanceService;
		this._commandService = _commandService;
		this._textSelectionManagerService = _textSelectionManagerService;
		this._editorService = _editorService;
		this._docPageLayoutService = _docPageLayoutService;
		this._renderManagerService = _renderManagerService;
		this._docViewScaleService = _docViewScaleService;
		this._embedInteractionBoundaryService = _embedInteractionBoundaryService;
		_defineProperty(this, "_isSheetEditor", false);
		_defineProperty(this, "_initTimer", void 0);
		_defineProperty(this, "_updateTimer", void 0);
		this._initSkeletonListener();
		this._initCommandExecutedListener();
		this._isSheetEditor = this._context.unitId === _univerjs_core.DOCS_NORMAL_EDITOR_UNIT_ID_KEY;
		const currentSheet = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_SHEET);
		const sheetRenderer = currentSheet && this._renderManagerService.getRenderUnitById(currentSheet.getUnitId());
		this._initTimer = window.setTimeout(() => {
			const zoomRatio = sheetRenderer && this._isSheetEditor ? sheetRenderer.scene.scaleX : getDocEffectiveZoomRatio(this._context.unit);
			this.updateViewZoom(zoomRatio, true);
		}, 20);
		if (!(0, _univerjs_core.isInternalEditorID)(this._context.unitId)) this._initZoomEventListener();
	}
	dispose() {
		window.clearTimeout(this._initTimer);
		window.clearTimeout(this._updateTimer);
	}
	_initSkeletonListener() {
		this.disposeWithMe(this._docSkeletonManagerService.currentSkeleton$.subscribe((param) => {
			if (param == null) return;
			const documentModel = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
			if (!documentModel) return;
			this._updateTimer = window.setTimeout(() => {
				const currentSheet = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_SHEET);
				const sheetRenderer = currentSheet && this._renderManagerService.getRenderUnitById(currentSheet.getUnitId());
				const zoomRatio = !this._isSheetEditor ? getDocEffectiveZoomRatio(documentModel) : (sheetRenderer === null || sheetRenderer === void 0 ? void 0 : sheetRenderer.scene.scaleX) || 1;
				this.updateViewZoom(zoomRatio, false);
			});
		}));
	}
	_initCommandExecutedListener() {
		const updateCommandList = [SetDocZoomRatioOperation.id];
		this.disposeWithMe(this._commandService.onCommandExecuted((command) => {
			if (updateCommandList.includes(command.id) && command.params.unitId === this._context.unitId) {
				const documentModel = this._context.unit;
				const zoomRatio = getDocEffectiveZoomRatio(documentModel);
				this.updateViewZoom(zoomRatio);
			}
		}));
		this.disposeWithMe(this._commandService.beforeCommandExecuted((command) => {
			var _command$params;
			if (command.id === SwitchDocModeCommand.id || command.id === DocPageSetupCommand.id && ((_command$params = command.params) === null || _command$params === void 0 ? void 0 : _command$params.documentFlavor) === _univerjs_core.DocumentFlavor.MODERN) this._commandService.executeCommand(SetDocZoomRatioCommand.id, {
				zoomRatio: 1,
				documentId: this._context.unitId
			});
		}));
	}
	updateViewZoom(zoomRatio, needRefreshSelection = true) {
		var _this$_embedInteracti;
		const docObject = neoGetDocObject(this._context);
		const viewScale = this._docViewScaleService.getViewScale(zoomRatio);
		const viewScaleChanged = docObject.scene.scaleX !== viewScale || docObject.scene.scaleY !== viewScale;
		docObject.scene.scale(viewScale, viewScale);
		if (!this._editorService.isEditor(this._context.unitId)) this._docPageLayoutService.calculatePagePosition();
		if (needRefreshSelection && !this._editorService.isEditor(this._context.unitId) && !((_this$_embedInteracti = this._embedInteractionBoundaryService) === null || _this$_embedInteracti === void 0 ? void 0 : _this$_embedInteracti.hasRecentInteraction())) this._textSelectionManagerService.refreshSelection();
		if (needRefreshSelection && viewScaleChanged && !(0, _univerjs_core.isInternalEditorID)(this._context.unitId)) {
			var _docObject$scene$getT;
			(_docObject$scene$getT = docObject.scene.getTransformer()) === null || _docObject$scene$getT === void 0 || _docObject$scene$getT.clearSelectedObjects();
		}
		const createOptions = this._univerInstanceService.getUnitCreateOptions(this._context.unitId);
		if ((createOptions === null || createOptions === void 0 ? void 0 : createOptions.embeddedRender) === true || (createOptions === null || createOptions === void 0 ? void 0 : createOptions.skipAutoRender) === true) {
			this._context.scene.makeDirty();
			this._context.scene.render();
		}
	}
	_initZoomEventListener() {
		const scene = this._context.scene;
		this.disposeWithMe(scene.onMouseWheel$.subscribeEvent((e) => {
			const documentModel = this._univerInstanceService.getCurrentUnitOfType(_univerjs_core.UniverInstanceType.UNIVER_DOC);
			if (!documentModel) return;
			const { documentFlavor } = documentModel.getSnapshot().documentStyle;
			if (!shouldHandleDocWheelZoom(e, Boolean(this._contextService.getContextValue(_univerjs_core.FOCUSING_DOC)), documentFlavor)) return;
			const nextRatio = (0, _univerjs_engine_render.getNextWheelZoomRatio)(getDocEffectiveZoomRatio(documentModel), e);
			this._commandService.executeCommand(SetDocZoomRatioCommand.id, {
				zoomRatio: nextRatio,
				documentId: documentModel.getUnitId()
			});
			e.preventDefault();
		}));
	}
};
DocZoomRenderController = __decorate([
	__decorateParam(1, _univerjs_core.IContextService),
	__decorateParam(2, (0, _univerjs_core.Inject)(_univerjs_docs.DocSkeletonManagerService)),
	__decorateParam(3, _univerjs_core.IUniverInstanceService),
	__decorateParam(4, _univerjs_core.ICommandService),
	__decorateParam(5, (0, _univerjs_core.Inject)(_univerjs_docs.DocSelectionManagerService)),
	__decorateParam(6, IEditorService),
	__decorateParam(7, (0, _univerjs_core.Inject)(DocPageLayoutService)),
	__decorateParam(8, _univerjs_engine_render.IRenderManagerService),
	__decorateParam(9, (0, _univerjs_core.Inject)(DocViewScaleService)),
	__decorateParam(10, (0, _univerjs_core.Optional)(IDocEmbedInteractionBoundaryService))
], DocZoomRenderController);

//#endregion
//#region src/services/doc-ime-state-change-interceptor.service.ts
let DocIMEStateChangeInterceptorService = class DocIMEStateChangeInterceptorService {
	constructor(_renderManagerService) {
		this._renderManagerService = _renderManagerService;
	}
	transformChangeStateInfo(changeStateInfo) {
		var _this$_renderManagerS;
		const { isCompositionEnd, isSync, syncer } = changeStateInfo;
		if (!isCompositionEnd) return changeStateInfo;
		const imeInputManagerService = (_this$_renderManagerS = this._renderManagerService.getRenderUnitById(isSync ? syncer : changeStateInfo.unitId)) === null || _this$_renderManagerS === void 0 ? void 0 : _this$_renderManagerS.with(DocIMEInputManagerService);
		if (imeInputManagerService == null) return null;
		const historyParams = imeInputManagerService.fetchComposedUndoRedoMutationParams();
		if (historyParams == null) throw new Error("historyParams is null in RichTextEditingMutation");
		const { undoMutationParams, redoMutationParams, previousActiveRange, previousDocRanges, previousSelectionOptions } = historyParams;
		return {
			...changeStateInfo,
			redoState: {
				...changeStateInfo.redoState,
				actions: redoMutationParams.actions,
				textRanges: redoMutationParams.textRanges,
				options: redoMutationParams.options,
				isEditing: redoMutationParams.isEditing
			},
			undoState: {
				...changeStateInfo.undoState,
				actions: undoMutationParams.actions,
				textRanges: previousDocRanges.length ? previousDocRanges : [previousActiveRange],
				options: previousSelectionOptions ?? void 0
			}
		};
	}
};
DocIMEStateChangeInterceptorService = __decorate([__decorateParam(0, _univerjs_engine_render.IRenderManagerService)], DocIMEStateChangeInterceptorService);

//#endregion
//#region src/services/doc-print-interceptor.service.ts
const PRINTING_COMPONENT_COLLECT = (0, _univerjs_core.createInterceptorKey)("PRINTING_COMPONENT_COLLECT");
const PRINTING_DOM_COLLECT = (0, _univerjs_core.createInterceptorKey)("PRINTING_DOM_COLLECT");
var DocPrintInterceptorService = class extends _univerjs_core.Disposable {
	constructor() {
		super();
		_defineProperty(this, "_printComponentMap", /* @__PURE__ */ new Map());
		_defineProperty(this, "_printPreparationHandlers", /* @__PURE__ */ new Set());
		_defineProperty(this, "interceptor", new _univerjs_core.InterceptorManager({
			PRINTING_COMPONENT_COLLECT,
			PRINTING_DOM_COLLECT
		}));
		this.disposeWithMe(this.interceptor.intercept(this.interceptor.getInterceptPoints().PRINTING_COMPONENT_COLLECT, {
			priority: -1,
			handler: (_value) => _value
		}));
		this.disposeWithMe(this.interceptor.intercept(this.interceptor.getInterceptPoints().PRINTING_DOM_COLLECT, {
			priority: -1,
			handler: (_value) => _value
		}));
	}
	registerPrintComponent(componentKey, printingComponentKey) {
		this._printComponentMap.set(componentKey, printingComponentKey);
	}
	getPrintComponent(componentKey) {
		return this._printComponentMap.get(componentKey);
	}
	getPageDrawingIds(skeleton, pageIndex) {
		var _data$skeHeaders$get, _data$skeFooters$get;
		const data = skeleton.getSkeletonData();
		if (!data || data.pages.length <= 1 || pageIndex == null) return null;
		const drawingIds = /* @__PURE__ */ new Set();
		const page = data.pages[pageIndex];
		if (!page) return drawingIds;
		collectPageDrawingIds(page, drawingIds);
		const header = (_data$skeHeaders$get = data.skeHeaders.get(page.headerId)) === null || _data$skeHeaders$get === void 0 ? void 0 : _data$skeHeaders$get.get(page.pageWidth);
		const footer = (_data$skeFooters$get = data.skeFooters.get(page.footerId)) === null || _data$skeFooters$get === void 0 ? void 0 : _data$skeFooters$get.get(page.pageWidth);
		if (header) collectPageDrawingIds(header, drawingIds);
		if (footer) collectPageDrawingIds(footer, drawingIds);
		return drawingIds;
	}
	registerPrintPreparation(handler) {
		this._printPreparationHandlers.add(handler);
		return () => this._printPreparationHandlers.delete(handler);
	}
	async preparePrint(context) {
		await Promise.all([...this._printPreparationHandlers].map((handler) => handler(context)));
	}
	dispose() {
		this._printPreparationHandlers.clear();
		super.dispose();
	}
};
function collectPageDrawingIds(page, drawingIds) {
	for (const drawingId of page.skeDrawings.keys()) drawingIds.add(drawingId);
	for (const table of page.skeTables.values()) for (const row of table.rows) for (const cell of row.cells) collectPageDrawingIds(cell, drawingIds);
}

//#endregion
//#region src/services/docs-render.service.ts
const DOC_MAIN_CANVAS_ID = "univer-doc-main-canvas";
function getDocsCanvasBackgroundColor(documentFlavor, canvasColorService, editorBackgroundColor, isEditor) {
	return resolveDocRenderBackground({
		documentFlavor,
		canvasColorService,
		editorBackgroundColor,
		isEditor
	}).canvasElementBackgroundColor;
}
let DocsRenderService = class DocsRenderService extends _univerjs_core.RxDisposable {
	constructor(_instanceSrv, _renderManagerService) {
		super();
		this._instanceSrv = _instanceSrv;
		this._renderManagerService = _renderManagerService;
		this._init();
	}
	_init() {
		this._instanceSrv.getAllUnitsForType(_univerjs_core.UniverInstanceType.UNIVER_DOC).forEach((documentModel) => this._createRenderer(documentModel, this._instanceSrv.getUnitCreateOptions(documentModel.getUnitId()) ?? void 0));
		this._instanceSrv.getTypeOfUnitAdded$(_univerjs_core.UniverInstanceType.UNIVER_DOC).pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe((event) => this._createRenderer(event.unit, event.options));
		this._instanceSrv.getTypeOfUnitDisposed$(_univerjs_core.UniverInstanceType.UNIVER_DOC).pipe((0, rxjs.takeUntil)(this.dispose$)).subscribe((doc) => this._disposeRenderer(doc));
	}
	_createRenderer(doc, createUnitOptions) {
		if (createUnitOptions === null || createUnitOptions === void 0 ? void 0 : createUnitOptions.skipAutoRender) return;
		const unitId = doc.getUnitId();
		if (!this._renderManagerService.has(unitId)) {
			this._createRenderWithId(unitId, doc);
			return;
		}
		const renderer = this._renderManagerService.getRenderUnitById(unitId);
		if (renderer) this._syncRendererCanvas(renderer, doc);
	}
	_createRenderWithId(unitId, doc) {
		const renderer = this._renderManagerService.createRender(unitId);
		if (doc) this._syncRendererCanvas(renderer, doc);
	}
	_syncRendererCanvas(renderer, doc) {
		const unitId = doc.getUnitId();
		const documentFlavor = doc.getSnapshot().documentStyle.documentFlavor;
		const canvas = renderer.engine.getCanvas();
		canvas.setId(DOC_MAIN_CANVAS_ID);
		canvas.getContext().setId(DOC_MAIN_CANVAS_ID);
		canvas.getCanvasEle().style.backgroundColor = getDocsCanvasBackgroundColor(documentFlavor, renderer.engine.canvasColorService, void 0, (0, _univerjs_core.isInternalEditorID)(unitId));
	}
	_disposeRenderer(doc) {
		const unitId = doc.getUnitId();
		this._renderManagerService.removeRender(unitId);
	}
};
DocsRenderService = __decorate([__decorateParam(0, _univerjs_core.IUniverInstanceService), __decorateParam(1, _univerjs_engine_render.IRenderManagerService)], DocsRenderService);

//#endregion
//#region src/shortcuts/core-editing.shortcut.ts
const BreakLineShortcut = {
	id: EnterCommand.id,
	preconditions: whenDocAndEditorFocusedWithBreakLine,
	binding: _univerjs_ui.KeyCode.ENTER
};
const SoftBreakLineShortcut = {
	id: BreakLineCommand.id,
	preconditions: whenDocAndEditorFocusedWithBreakLine,
	binding: _univerjs_ui.KeyCode.ENTER | _univerjs_ui.MetaKeys.SHIFT
};
const DeleteLeftShortcut = {
	id: DeleteLeftCommand.id,
	preconditions: whenDocAndEditorFocused,
	binding: _univerjs_ui.KeyCode.BACKSPACE
};
const DeleteRightShortcut = {
	id: DeleteRightCommand.id,
	preconditions: whenDocAndEditorFocused,
	binding: _univerjs_ui.KeyCode.DELETE
};
const CloseHeaderFooterShortcut = {
	id: CloseHeaderFooterCommand.id,
	preconditions: whenDocAndEditorFocused,
	binding: _univerjs_ui.KeyCode.ESC
};

//#endregion
//#region src/shortcuts/cursor.shortcut.ts
function moveCursorShortcut(direction, granularity, binding, mac = binding, win = binding, linux = binding) {
	return {
		id: MoveCursorOperation.id,
		binding,
		mac,
		win,
		linux,
		preconditions: whenDocAndEditorFocused,
		staticParameters: {
			direction,
			granularity
		}
	};
}
function moveSelectionShortcut(direction, granularity, binding, mac = binding, win = binding, linux = binding) {
	return {
		id: MoveSelectionOperation.id,
		binding,
		mac,
		win,
		linux,
		preconditions: whenDocAndEditorFocused,
		staticParameters: {
			direction,
			granularity
		}
	};
}
const MoveCursorUpShortcut = {
	id: MoveCursorOperation.id,
	binding: _univerjs_ui.KeyCode.ARROW_UP,
	preconditions: whenDocAndEditorFocused,
	staticParameters: { direction: _univerjs_core.Direction.UP }
};
const MoveCursorDownShortcut = {
	id: MoveCursorOperation.id,
	binding: _univerjs_ui.KeyCode.ARROW_DOWN,
	preconditions: whenDocAndEditorFocused,
	staticParameters: { direction: _univerjs_core.Direction.DOWN }
};
const MoveCursorLeftShortcut = {
	id: MoveCursorOperation.id,
	binding: _univerjs_ui.KeyCode.ARROW_LEFT,
	preconditions: whenDocAndEditorFocused,
	staticParameters: { direction: _univerjs_core.Direction.LEFT }
};
const MoveCursorRightShortcut = {
	id: MoveCursorOperation.id,
	binding: _univerjs_ui.KeyCode.ARROW_RIGHT,
	preconditions: whenDocAndEditorFocused,
	staticParameters: { direction: _univerjs_core.Direction.RIGHT }
};
const MoveSelectionUpShortcut = {
	id: MoveSelectionOperation.id,
	binding: _univerjs_ui.KeyCode.ARROW_UP | _univerjs_ui.MetaKeys.SHIFT,
	preconditions: whenDocAndEditorFocused,
	staticParameters: { direction: _univerjs_core.Direction.UP }
};
const MoveSelectionDownShortcut = {
	id: MoveSelectionOperation.id,
	binding: _univerjs_ui.KeyCode.ARROW_DOWN | _univerjs_ui.MetaKeys.SHIFT,
	preconditions: whenDocAndEditorFocused,
	staticParameters: { direction: _univerjs_core.Direction.DOWN }
};
const MoveSelectionLeftShortcut = {
	id: MoveSelectionOperation.id,
	binding: _univerjs_ui.KeyCode.ARROW_LEFT | _univerjs_ui.MetaKeys.SHIFT,
	preconditions: whenDocAndEditorFocused,
	staticParameters: { direction: _univerjs_core.Direction.LEFT }
};
const MoveSelectionRightShortcut = {
	id: MoveSelectionOperation.id,
	binding: _univerjs_ui.KeyCode.ARROW_RIGHT | _univerjs_ui.MetaKeys.SHIFT,
	preconditions: whenDocAndEditorFocused,
	staticParameters: { direction: _univerjs_core.Direction.RIGHT }
};
const MoveCursorLineStartShortcut = moveCursorShortcut(_univerjs_core.Direction.LEFT, "line", _univerjs_ui.KeyCode.HOME, _univerjs_ui.KeyCode.ARROW_LEFT | _univerjs_ui.MetaKeys.CTRL_COMMAND);
const MoveCursorLineEndShortcut = moveCursorShortcut(_univerjs_core.Direction.RIGHT, "line", _univerjs_ui.KeyCode.END, _univerjs_ui.KeyCode.ARROW_RIGHT | _univerjs_ui.MetaKeys.CTRL_COMMAND);
const MoveSelectionLineStartShortcut = moveSelectionShortcut(_univerjs_core.Direction.LEFT, "line", _univerjs_ui.KeyCode.HOME | _univerjs_ui.MetaKeys.SHIFT, _univerjs_ui.KeyCode.ARROW_LEFT | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT);
const MoveSelectionLineEndShortcut = moveSelectionShortcut(_univerjs_core.Direction.RIGHT, "line", _univerjs_ui.KeyCode.END | _univerjs_ui.MetaKeys.SHIFT, _univerjs_ui.KeyCode.ARROW_RIGHT | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT);
const MoveCursorDocumentStartShortcut = moveCursorShortcut(_univerjs_core.Direction.UP, "document", _univerjs_ui.KeyCode.HOME | _univerjs_ui.MetaKeys.CTRL_COMMAND, _univerjs_ui.KeyCode.ARROW_UP | _univerjs_ui.MetaKeys.CTRL_COMMAND);
const MoveCursorDocumentEndShortcut = moveCursorShortcut(_univerjs_core.Direction.DOWN, "document", _univerjs_ui.KeyCode.END | _univerjs_ui.MetaKeys.CTRL_COMMAND, _univerjs_ui.KeyCode.ARROW_DOWN | _univerjs_ui.MetaKeys.CTRL_COMMAND);
const MoveSelectionDocumentStartShortcut = moveSelectionShortcut(_univerjs_core.Direction.UP, "document", _univerjs_ui.KeyCode.HOME | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT, _univerjs_ui.KeyCode.ARROW_UP | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT);
const MoveSelectionDocumentEndShortcut = moveSelectionShortcut(_univerjs_core.Direction.DOWN, "document", _univerjs_ui.KeyCode.END | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT, _univerjs_ui.KeyCode.ARROW_DOWN | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT);
const MoveCursorWordLeftShortcut = moveCursorShortcut(_univerjs_core.Direction.LEFT, "word", _univerjs_ui.KeyCode.ARROW_LEFT | _univerjs_ui.MetaKeys.CTRL_COMMAND, _univerjs_ui.KeyCode.ARROW_LEFT | _univerjs_ui.MetaKeys.ALT);
const MoveCursorWordRightShortcut = moveCursorShortcut(_univerjs_core.Direction.RIGHT, "word", _univerjs_ui.KeyCode.ARROW_RIGHT | _univerjs_ui.MetaKeys.CTRL_COMMAND, _univerjs_ui.KeyCode.ARROW_RIGHT | _univerjs_ui.MetaKeys.ALT);
const MoveSelectionWordLeftShortcut = moveSelectionShortcut(_univerjs_core.Direction.LEFT, "word", _univerjs_ui.KeyCode.ARROW_LEFT | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT, _univerjs_ui.KeyCode.ARROW_LEFT | _univerjs_ui.MetaKeys.ALT | _univerjs_ui.MetaKeys.SHIFT);
const MoveSelectionWordRightShortcut = moveSelectionShortcut(_univerjs_core.Direction.RIGHT, "word", _univerjs_ui.KeyCode.ARROW_RIGHT | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT, _univerjs_ui.KeyCode.ARROW_RIGHT | _univerjs_ui.MetaKeys.ALT | _univerjs_ui.MetaKeys.SHIFT);
const MoveCursorParagraphUpShortcut = moveCursorShortcut(_univerjs_core.Direction.UP, "paragraph", _univerjs_ui.KeyCode.ARROW_UP | _univerjs_ui.MetaKeys.CTRL_COMMAND, _univerjs_ui.KeyCode.ARROW_UP | _univerjs_ui.MetaKeys.ALT);
const MoveCursorParagraphDownShortcut = moveCursorShortcut(_univerjs_core.Direction.DOWN, "paragraph", _univerjs_ui.KeyCode.ARROW_DOWN | _univerjs_ui.MetaKeys.CTRL_COMMAND, _univerjs_ui.KeyCode.ARROW_DOWN | _univerjs_ui.MetaKeys.ALT);
const MoveSelectionParagraphUpShortcut = moveSelectionShortcut(_univerjs_core.Direction.UP, "paragraph", _univerjs_ui.KeyCode.ARROW_UP | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT, _univerjs_ui.KeyCode.ARROW_UP | _univerjs_ui.MetaKeys.ALT | _univerjs_ui.MetaKeys.SHIFT);
const MoveSelectionParagraphDownShortcut = moveSelectionShortcut(_univerjs_core.Direction.DOWN, "paragraph", _univerjs_ui.KeyCode.ARROW_DOWN | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT, _univerjs_ui.KeyCode.ARROW_DOWN | _univerjs_ui.MetaKeys.ALT | _univerjs_ui.MetaKeys.SHIFT);
const SelectAllShortcut = {
	id: DocSelectAllCommand.id,
	binding: _univerjs_ui.KeyCode.A | _univerjs_ui.MetaKeys.CTRL_COMMAND,
	preconditions: (contextService) => contextService.getContextValue(_univerjs_core.FOCUSING_UNIVER_EDITOR) && (contextService.getContextValue(_univerjs_core.FOCUSING_DOC) || contextService.getContextValue(_univerjs_core.EDITOR_ACTIVATED))
};

//#endregion
//#region src/shortcuts/heading.shortcut.ts
function headingShortcut(id, key) {
	const binding = key | _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.ALT;
	return {
		id,
		binding,
		mac: binding,
		win: binding,
		linux: binding,
		preconditions: whenDocAndEditorFocused
	};
}
const NormalTextHeadingShortcut = headingShortcut(NormalTextHeadingCommand.id, _univerjs_ui.KeyCode.Digit0);
const H1HeadingShortcut = headingShortcut(H1HeadingCommand.id, _univerjs_ui.KeyCode.Digit1);
const H2HeadingShortcut = headingShortcut(H2HeadingCommand.id, _univerjs_ui.KeyCode.Digit2);
const H3HeadingShortcut = headingShortcut(H3HeadingCommand.id, _univerjs_ui.KeyCode.Digit3);
const H4HeadingShortcut = headingShortcut(H4HeadingCommand.id, _univerjs_ui.KeyCode.Digit4);
const H5HeadingShortcut = headingShortcut(H5HeadingCommand.id, _univerjs_ui.KeyCode.Digit5);

//#endregion
//#region src/plugin.ts
let UniverDocsUIPlugin = class UniverDocsUIPlugin extends _univerjs_core.Plugin {
	constructor(_config = defaultPluginConfig, _injector, _renderManagerSrv, _commandService, _configService) {
		super();
		this._config = _config;
		this._injector = _injector;
		this._renderManagerSrv = _renderManagerSrv;
		this._commandService = _commandService;
		this._configService = _configService;
		const { menu, ...rest } = (0, _univerjs_core.merge)({}, defaultPluginConfig, this._config);
		if (menu) this._configService.setConfig("menu", menu, { merge: true });
		this._configService.setConfig(DOCS_UI_PLUGIN_CONFIG_KEY, rest);
		this._initDependencies(_injector);
		this._initializeShortcut();
		this._initCommand();
	}
	onReady() {
		this._initRenderBasics();
		this._initAutoFocus();
		(0, _univerjs_core.touchDependencies)(this._injector, [[DocsRenderService]]);
	}
	onRendered() {
		this._initRenderModules();
		(0, _univerjs_core.touchDependencies)(this._injector, [
			[DocAutoFormatController],
			[DocMoveCursorController],
			[DocParagraphSettingController],
			[DocSectionSettingController],
			[DocTableController],
			[ComponentsController],
			[DocUIController]
		]);
	}
	_initCommand() {
		[
			DeleteLeftCommand,
			DeleteRightCommand,
			SetInlineFormatBoldCommand,
			SetInlineFormatItalicCommand,
			SetInlineFormatUnderlineCommand,
			SetInlineFormatStrikethroughCommand,
			SetInlineFormatSubscriptCommand,
			SetInlineFormatSuperscriptCommand,
			SetInlineFormatFontSizeCommand,
			SetInlineFormatFontFamilyCommand,
			SetInlineFormatTextColorCommand,
			ResetInlineFormatTextColorCommand,
			SetInlineFormatTextFillCommand,
			ResetInlineFormatTextBackgroundColorCommand,
			SetInlineFormatTextBackgroundColorCommand,
			SetInlineFormatCommand,
			SetDocInputStyleCommand,
			BreakLineCommand,
			DeleteCustomBlockCommand,
			MoveDocBlockCommand,
			MergeTwoParagraphCommand,
			RemoveHorizontalLineCommand,
			SetDocZoomRatioOperation,
			OrderListCommand,
			BulletListCommand,
			ListOperationCommand,
			AlignLeftCommand,
			AlignCenterCommand,
			AlignRightCommand,
			AlignOperationCommand,
			AlignJustifyCommand,
			HorizontalLineCommand,
			CreateDocTableCommand,
			DocTableInsertRowCommand,
			DocTableInsertRowAboveCommand,
			DocTableInsertRowBellowCommand,
			DocTableInsertColumnCommand,
			DocTableInsertColumnLeftCommand,
			DocTableInsertColumnRightCommand,
			DocTableDeleteRowsCommand,
			DocTableDeleteColumnsCommand,
			DocTableDeleteTableCommand,
			CloseHeaderFooterCommand,
			DocTableTabCommand,
			TabCommand,
			AfterSpaceCommand,
			EnterCommand,
			ChangeListNestingLevelCommand,
			ChangeListTypeCommand,
			CheckListCommand,
			ToggleCheckListCommand,
			QuickListCommand,
			IMEInputCommand,
			SwitchDocModeCommand,
			DocParagraphSettingCommand,
			InnerPasteCommand,
			CutContentCommand,
			ReplaceSnapshotCommand,
			CoverContentCommand,
			SetDocZoomRatioCommand,
			DocSelectAllCommand,
			DocParagraphSettingPanelOperation,
			DocSectionSettingPanelOperation,
			InsertDocumentColumnBreakOperation,
			InsertDocumentSectionBreakOperation,
			MoveCursorOperation,
			MoveSelectionOperation,
			ReplaceTextRunsCommand,
			ReplaceSelectionCommand,
			InsertCustomRangeCommand,
			InsertSpecialCharacterCommand,
			SetParagraphNamedStyleCommand,
			QuickHeadingCommand,
			DocCopyCurrentParagraphCommand,
			DocCutCurrentParagraphCommand,
			H1HeadingCommand,
			H2HeadingCommand,
			H3HeadingCommand,
			H4HeadingCommand,
			H5HeadingCommand,
			NormalTextHeadingCommand,
			TitleHeadingCommand,
			SubtitleHeadingCommand,
			InsertBulletListBellowCommand,
			InsertOrderListBellowCommand,
			InsertCheckListBellowCommand,
			InsertHorizontalLineBellowCommand,
			DocPageSetupCommand,
			DocOpenPageSettingCommand
		].forEach((e) => {
			this.disposeWithMe(this._commandService.registerCommand(e));
		});
		[
			DocCopyCommand,
			DocCutCommand,
			DocPasteCommand,
			DeleteCurrentParagraphCommand
		].forEach((command) => this.disposeWithMe(this._commandService.registerMultipleCommand(command)));
	}
	_initializeShortcut() {
		[
			MoveCursorUpShortcut,
			MoveCursorDownShortcut,
			MoveCursorRightShortcut,
			MoveCursorLeftShortcut,
			MoveSelectionUpShortcut,
			MoveSelectionDownShortcut,
			MoveSelectionLeftShortcut,
			MoveSelectionRightShortcut,
			MoveCursorLineStartShortcut,
			MoveCursorLineEndShortcut,
			MoveSelectionLineStartShortcut,
			MoveSelectionLineEndShortcut,
			MoveCursorParagraphUpShortcut,
			MoveCursorParagraphDownShortcut,
			MoveSelectionParagraphUpShortcut,
			MoveSelectionParagraphDownShortcut,
			MoveCursorDocumentStartShortcut,
			MoveCursorDocumentEndShortcut,
			MoveSelectionDocumentStartShortcut,
			MoveSelectionDocumentEndShortcut,
			MoveCursorWordLeftShortcut,
			MoveCursorWordRightShortcut,
			MoveSelectionWordLeftShortcut,
			MoveSelectionWordRightShortcut,
			SelectAllShortcut,
			DeleteLeftShortcut,
			DeleteRightShortcut,
			CloseHeaderFooterShortcut,
			BreakLineShortcut,
			SoftBreakLineShortcut,
			ShiftTabShortCut,
			NormalTextHeadingShortcut,
			H1HeadingShortcut,
			H2HeadingShortcut,
			H3HeadingShortcut,
			H4HeadingShortcut,
			H5HeadingShortcut
		].forEach((shortcut) => {
			this._injector.get(_univerjs_ui.IShortcutService).registerShortcut(shortcut);
		});
	}
	_initDependencies(injector) {
		(0, _univerjs_core.mergeOverrideWithDependencies)([
			[DocPrintInterceptorService],
			[DocClipboardController],
			[DocEditorBridgeController],
			[ComponentsController],
			[DocUIController],
			[DocAutoFormatController],
			[DocTableController],
			[DocMoveCursorController],
			[DocParagraphSettingController],
			[DocSectionSettingController],
			[IEditorService, { useClass: EditorService }],
			[IDocClipboardService, { useClass: DocClipboardService }],
			[DocClipboardPasteAdapterService],
			[IDocClipboardPasteAdapterService, { useClass: DocClipboardPasteAdapterService }],
			[DocHtmlExportService],
			[DocCanvasPopManagerService],
			[DocLayoutProgressService],
			[DocsRenderService],
			[_univerjs_docs.IDocStateChangeInterceptorService, { useClass: DocIMEStateChangeInterceptorService }],
			[DocAutoFormatService],
			[DocMenuStyleService]
		], this._config.override).forEach((d) => injector.add(d));
	}
	_initAutoFocus() {
		const currentService = this._injector.get(_univerjs_core.IUniverInstanceService);
		const editorService = this._injector.get(IEditorService);
		this.disposeWithMe(currentService.getCurrentTypeOfUnit$(_univerjs_core.UniverInstanceType.UNIVER_DOC).subscribe((doc) => {
			if (!doc) return;
			const id = doc.getUnitId();
			if (!editorService.isEditor(id)) currentService.focusUnit(id);
		}));
	}
	_initRenderBasics() {
		[
			[_univerjs_docs.DocSkeletonManagerService],
			[DocSelectionRenderService],
			[_univerjs_docs.DocInterceptorService],
			[DocViewScaleService],
			[DocPageLayoutService],
			[DocLayoutInteractionService],
			[DocCanvasPopupLayoutInteractionController],
			[DocIMEInputManagerService],
			[DocLayoutProgressRenderController],
			[DocRenderController],
			[DocZoomRenderController],
			[DocBackScrollRenderController],
			[DocSelectionRenderController]
		].forEach((m) => {
			this._renderManagerSrv.registerRenderModule(_univerjs_core.UniverInstanceType.UNIVER_DOC, m);
		});
	}
	_initRenderModules() {
		[
			[DocEventManagerService],
			[DocFloatMenuService],
			[DocParagraphMenuService],
			[DocHeaderFooterController],
			[DocResizeRenderController],
			[DocParagraphPlaceholderRenderController],
			[DocContextMenuRenderController],
			[DocChecklistRenderController],
			[DocClipboardController],
			[DocInputController],
			[DocIMEInputController],
			[DocLayoutRecoveryRenderController],
			[DocEditorBridgeController]
		].forEach((m) => {
			this._renderManagerSrv.registerRenderModule(_univerjs_core.UniverInstanceType.UNIVER_DOC, m);
		});
	}
};
_defineProperty(UniverDocsUIPlugin, "pluginName", "DOC_UI_PLUGIN");
_defineProperty(UniverDocsUIPlugin, "packageName", name);
_defineProperty(UniverDocsUIPlugin, "version", version);
UniverDocsUIPlugin = __decorate([
	(0, _univerjs_core.DependentOn)(_univerjs_docs.UniverDocsPlugin, _univerjs_engine_render.UniverRenderEnginePlugin),
	__decorateParam(1, (0, _univerjs_core.Inject)(_univerjs_core.Injector)),
	__decorateParam(2, _univerjs_engine_render.IRenderManagerService),
	__decorateParam(3, _univerjs_core.ICommandService),
	__decorateParam(4, _univerjs_core.IConfigService)
], UniverDocsUIPlugin);

//#endregion
//#region src/services/clipboard/rtf-to-plain-text.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
const SKIPPED_DESTINATIONS = /* @__PURE__ */ new Set([
	"colortbl",
	"datastore",
	"filetbl",
	"fonttbl",
	"header",
	"headerf",
	"headerl",
	"headerr",
	"info",
	"listtable",
	"listoverridetable",
	"object",
	"pict",
	"stylesheet",
	"themedata"
]);
const SYMBOLS = {
	bullet: "•",
	emdash: "—",
	endash: "–",
	line: "\n",
	lquote: "‘",
	par: "\n",
	rdblquote: "”",
	rquote: "’",
	tab: "	",
	ldblquote: "“"
};
function convertClipboardRtfToPlainText(rtf) {
	if (!/^\s*\{\\rtf\d?/i.test(rtf)) return "";
	const states = [{
		skip: false,
		unicodeFallbackLength: 1
	}];
	let state = states[0];
	let output = "";
	let index = 0;
	let unicodeFallbackRemaining = 0;
	while (index < rtf.length) {
		const character = rtf[index];
		if (character === "{") {
			state = { ...state };
			states.push(state);
			index += 1;
			continue;
		}
		if (character === "}") {
			if (states.length > 1) {
				states.pop();
				state = states[states.length - 1];
			}
			unicodeFallbackRemaining = 0;
			index += 1;
			continue;
		}
		if (character !== "\\") {
			if (!state.skip && unicodeFallbackRemaining === 0 && character !== "\r" && character !== "\n") output += character;
			else if (unicodeFallbackRemaining > 0) unicodeFallbackRemaining -= 1;
			index += 1;
			continue;
		}
		const escaped = rtf[index + 1];
		if (escaped === "\\" || escaped === "{" || escaped === "}") {
			if (!state.skip && unicodeFallbackRemaining === 0) output += escaped;
			else if (unicodeFallbackRemaining > 0) unicodeFallbackRemaining -= 1;
			index += 2;
			continue;
		}
		if (escaped === "~" || escaped === "-" || escaped === "_") {
			if (!state.skip && unicodeFallbackRemaining === 0) output += escaped === "~" ? "\xA0" : escaped === "_" ? "‑" : "";
			else if (unicodeFallbackRemaining > 0) unicodeFallbackRemaining -= 1;
			index += 2;
			continue;
		}
		if (escaped === "*") {
			state.skip = true;
			index += 2;
			continue;
		}
		if (escaped === "'") {
			const hex = rtf.slice(index + 2, index + 4);
			if (!state.skip && unicodeFallbackRemaining === 0 && /^[0-9a-f]{2}$/i.test(hex)) output += decodeWindows1252(Number.parseInt(hex, 16));
			else if (unicodeFallbackRemaining > 0) unicodeFallbackRemaining -= 1;
			index += 4;
			continue;
		}
		const control = /^\\([a-z]+)(-?\d+)? ?/i.exec(rtf.slice(index));
		if (!control) {
			index += 2;
			continue;
		}
		const word = control[1].toLowerCase();
		const parameter = control[2] === void 0 ? void 0 : Number(control[2]);
		index += control[0].length;
		if (SKIPPED_DESTINATIONS.has(word)) {
			state.skip = true;
			continue;
		}
		if (word === "uc" && parameter !== void 0) {
			state.unicodeFallbackLength = Math.max(0, parameter);
			continue;
		}
		if (word === "u" && parameter !== void 0 && !state.skip) {
			output += String.fromCharCode(parameter < 0 ? parameter + 65536 : parameter);
			unicodeFallbackRemaining = state.unicodeFallbackLength;
			continue;
		}
		if (!state.skip && SYMBOLS[word]) output += SYMBOLS[word];
	}
	return output;
}
function decodeWindows1252(code) {
	return {
		128: "€",
		130: "‚",
		131: "ƒ",
		132: "„",
		133: "…",
		134: "†",
		135: "‡",
		136: "ˆ",
		137: "‰",
		138: "Š",
		139: "‹",
		140: "Œ",
		142: "Ž",
		145: "‘",
		146: "’",
		147: "“",
		148: "”",
		149: "•",
		150: "–",
		151: "—",
		152: "˜",
		153: "™",
		154: "š",
		155: "›",
		156: "œ",
		158: "ž",
		159: "Ÿ"
	}[code] ?? String.fromCharCode(code);
}

//#endregion
//#region src/views/mobile-rich-text-toolbar/MobileRichTextToolbar.tsx
const COLOR_TOKENS = [
	"gray.900",
	"red.500",
	"yellow.500",
	"green.500",
	"blue.500"
];
const mobileButtonClassName = `
  univer-flex univer-min-w-0 univer-items-center univer-justify-center univer-rounded-lg univer-text-lg
  univer-text-gray-800 active:univer-scale-95 active:univer-bg-primary-100
  dark:!univer-text-gray-100 dark:active:!univer-bg-gray-700
`;
const activeButtonClassName = "univer-bg-primary-100 univer-text-primary-600 dark:!univer-bg-gray-700 dark:!univer-text-primary-400";
function MobileRichTextToolbar({ editorId, className }) {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const editorService = (0, _univerjs_ui.useDependency)(IEditorService);
	const localeService = (0, _univerjs_ui.useDependency)(_univerjs_core.LocaleService);
	const themeService = (0, _univerjs_ui.useDependency)(_univerjs_core.ThemeService);
	const [, setRevision] = (0, react.useState)(0);
	const [colorsVisible, setColorsVisible] = (0, react.useState)(false);
	(0, _univerjs_ui.useObservable)(() => themeService.currentTheme$, void 0, false, [themeService]);
	const editor = editorService.getEditor(editorId);
	(0, react.useEffect)(() => {
		const frame = requestAnimationFrame(() => setRevision((value) => value + 1));
		return () => cancelAnimationFrame(frame);
	}, [editorId, editorService]);
	(0, react.useEffect)(() => {
		if (!editor) return void 0;
		const subscription = (0, rxjs.merge)(editor.input$, editor.selectionChange$).subscribe(() => setRevision((value) => value + 1));
		return () => subscription.unsubscribe();
	}, [editor]);
	const defaultTextColor = themeService.getColorFromTheme("gray.900");
	const state = resolveToolbarState(editor, defaultTextColor);
	const colors = COLOR_TOKENS.map((token) => themeService.getColorFromTheme(token));
	async function execute(commandId, params) {
		const activeEditor = editorService.getEditor(editorId);
		if (editorService.getFocusId() !== editorId) editorService.focus(editorId);
		else activeEditor === null || activeEditor === void 0 || activeEditor.focus();
		if (activeEditor && activeEditor.getSelectionRanges().length === 0) {
			var _activeEditor$getDocu;
			const end = Math.max(0, (((_activeEditor$getDocu = activeEditor.getDocumentData().body) === null || _activeEditor$getDocu === void 0 ? void 0 : _activeEditor$getDocu.dataStream.length) ?? 2) - 2);
			activeEditor.setSelectionRanges([{
				startOffset: end,
				endOffset: end
			}]);
		}
		await commandService.executeCommand(commandId, params);
		setRevision((value) => value + 1);
	}
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
		"data-u-comp": "mobile-rich-text-toolbar",
		className: (0, _univerjs_design.clsx)("univer-box-border univer-bg-gray-0 univer-shadow-[0_1px_0_rgba(0,0,0,0.06)] dark:!univer-bg-gray-800", className),
		onPointerDown: (event) => {
			event.preventDefault();
			event.stopPropagation();
		},
		children: !colorsVisible ? /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			className: "univer-grid univer-h-11 univer-grid-cols-7 univer-gap-1 univer-px-2",
			children: [
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(FormatButton, {
					label: localeService.t("docs-ui.toolbar.bold"),
					active: state.bold,
					icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.BoldIcon, {}),
					onClick: () => execute(SetInlineFormatBoldCommand.id)
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(FormatButton, {
					label: localeService.t("docs-ui.toolbar.italic"),
					active: state.italic,
					icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.ItalicIcon, {}),
					onClick: () => execute(SetInlineFormatItalicCommand.id)
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(FormatButton, {
					label: localeService.t("docs-ui.toolbar.underline"),
					active: state.underline,
					icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.UnderlineIcon, {}),
					onClick: () => execute(SetInlineFormatUnderlineCommand.id)
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(FormatButton, {
					label: localeService.t("docs-ui.toolbar.strikethrough"),
					active: state.strike,
					icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.StrikethroughIcon, {}),
					onClick: () => execute(SetInlineFormatStrikethroughCommand.id)
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
					type: "button",
					"aria-label": localeService.t("docs-ui.toolbar.textColor.main"),
					className: (0, _univerjs_design.clsx)(_univerjs_design.resetButtonClassName, mobileButtonClassName),
					onClick: () => setColorsVisible(true),
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.FontColorDoubleIcon, {
						className: "univer-size-5",
						extend: { colorChannel1: state.color }
					})
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(FormatButton, {
					label: localeService.t("docs-ui.toolbar.unorder"),
					active: state.bulletList,
					icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.UnorderIcon, {}),
					onClick: () => execute(BulletListCommand.id)
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)(FormatButton, {
					label: localeService.t("docs-ui.toolbar.order"),
					active: state.orderList,
					icon: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.OrderIcon, {}),
					onClick: () => execute(OrderListCommand.id)
				})
			]
		}) : /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			className: "univer-grid univer-h-11 univer-grid-cols-8 univer-items-center univer-gap-1 univer-px-2",
			children: [
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
					type: "button",
					"aria-label": localeService.t("docs-ui.toolbar.textColor.main"),
					className: (0, _univerjs_design.clsx)(_univerjs_design.resetButtonClassName, mobileButtonClassName, "univer-size-9"),
					onClick: () => setColorsVisible(false),
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.MoreLeftIcon, { className: "univer-size-5" })
				}),
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
					type: "button",
					"aria-label": localeService.t("docs-ui.toolbar.resetColor"),
					className: (0, _univerjs_design.clsx)(_univerjs_design.resetButtonClassName, "univer-flex univer-size-7 univer-items-center univer-justify-center univer-justify-self-center univer-rounded-md univer-bg-gray-100 univer-text-xs univer-text-gray-600 active:univer-scale-95 dark:!univer-bg-gray-700 dark:!univer-text-gray-200"),
					onClick: () => execute(ResetInlineFormatTextColorCommand.id),
					children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.NoColorDoubleIcon, { className: "univer-size-4" })
				}),
				colors.map((color) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
					type: "button",
					"aria-label": `${localeService.t("docs-ui.toolbar.textColor.main")} ${color}`,
					"aria-pressed": state.color.toLowerCase() === color.toLowerCase(),
					className: (0, _univerjs_design.clsx)(_univerjs_design.resetButtonClassName, "univer-size-7 univer-justify-self-center univer-rounded-md univer-border-2 univer-border-solid active:univer-scale-95", state.color.toLowerCase() === color.toLowerCase() ? "univer-border-primary-500" : "univer-border-transparent"),
					style: { backgroundColor: color },
					onClick: () => execute(SetInlineFormatTextColorCommand.id, { value: color })
				}, color)),
				/* @__PURE__ */ (0, react_jsx_runtime.jsxs)("label", {
					"aria-label": localeService.t("docs-ui.toolbar.textColor.main"),
					className: (0, _univerjs_design.clsx)(_univerjs_design.resetButtonClassName, "univer-relative univer-flex univer-size-7 univer-items-center univer-justify-center univer-justify-self-center univer-overflow-hidden univer-rounded-md univer-border-2 univer-border-solid univer-border-transparent active:univer-scale-95"),
					children: [/* @__PURE__ */ (0, react_jsx_runtime.jsx)(_univerjs_icons.ColorWheelMultiIcon, { className: "univer-size-6" }), /* @__PURE__ */ (0, react_jsx_runtime.jsx)("input", {
						"aria-label": localeService.t("docs-ui.toolbar.textColor.main"),
						className: "univer-absolute univer-inset-0 univer-size-full univer-cursor-pointer univer-opacity-0",
						type: "color",
						defaultValue: defaultTextColor,
						onChange: (event) => {
							execute(SetInlineFormatTextColorCommand.id, { value: event.target.value });
						}
					})]
				})
			]
		})
	});
}
function FormatButton(props) {
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("button", {
		type: "button",
		"aria-label": props.label,
		"aria-pressed": props.active,
		className: (0, _univerjs_design.clsx)(_univerjs_design.resetButtonClassName, mobileButtonClassName, props.active && activeButtonClassName),
		onClick: props.onClick,
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)("span", {
			className: "univer-flex univer-size-5 univer-items-center univer-justify-center",
			children: props.icon
		})
	});
}
function resolveToolbarState(editor, defaultTextColor) {
	var _style$ul, _style$st, _style$cl;
	const body = editor === null || editor === void 0 ? void 0 : editor.getDocumentData().body;
	const ranges = (editor === null || editor === void 0 ? void 0 : editor.getSelectionRanges()) ?? [];
	const range = ranges.find((item) => item.isActive) ?? ranges[0];
	if (!body || !range) return {
		bold: false,
		italic: false,
		underline: false,
		strike: false,
		color: defaultTextColor,
		bulletList: false,
		orderList: false
	};
	const style = getStyleInTextRange(body, range, {});
	const listTypes = _univerjs_core.BuildTextUtils.range.getParagraphsInRanges([range], body.paragraphs ?? [], body.dataStream).map((paragraph) => {
		var _paragraph$bullet;
		return (_paragraph$bullet = paragraph.bullet) === null || _paragraph$bullet === void 0 ? void 0 : _paragraph$bullet.listType;
	}).filter((listType) => typeof listType === "string");
	return {
		bold: style.bl === _univerjs_core.BooleanNumber.TRUE,
		italic: style.it === _univerjs_core.BooleanNumber.TRUE,
		underline: ((_style$ul = style.ul) === null || _style$ul === void 0 ? void 0 : _style$ul.s) === _univerjs_core.BooleanNumber.TRUE,
		strike: ((_style$st = style.st) === null || _style$st === void 0 ? void 0 : _style$st.s) === _univerjs_core.BooleanNumber.TRUE,
		color: ((_style$cl = style.cl) === null || _style$cl === void 0 ? void 0 : _style$cl.rgb) ?? defaultTextColor,
		bulletList: Boolean(listTypes.length && listTypes.every((listType) => listType.startsWith(_univerjs_core.PresetListType.BULLET_LIST))),
		orderList: Boolean(listTypes.length && listTypes.every((listType) => listType.startsWith(_univerjs_core.PresetListType.ORDER_LIST)))
	};
}

//#endregion
//#region src/views/rich-text-editor/hooks/editor-undo-redo-keyboard.ts
function isSheetFormulaEditor(editorUnitId, univerInstanceService) {
	var _documentModel$getBod;
	if (editorUnitId !== _univerjs_core.DOCS_NORMAL_EDITOR_UNIT_ID_KEY && editorUnitId !== _univerjs_core.DOCS_FORMULA_BAR_EDITOR_UNIT_ID_KEY) return false;
	const documentModel = univerInstanceService.getUnit(editorUnitId, _univerjs_core.UniverInstanceType.UNIVER_DOC);
	return (documentModel === null || documentModel === void 0 || (_documentModel$getBod = documentModel.getBody()) === null || _documentModel$getBod === void 0 || (_documentModel$getBod = _documentModel$getBod.dataStream) === null || _documentModel$getBod === void 0 ? void 0 : _documentModel$getBod.startsWith("=")) ?? false;
}
function executeEditorUndoRedoCommand(options) {
	var _univerInstanceServic;
	const { commandId, commandService, editorUnitId, univerInstanceService } = options;
	const shouldUseSheetEditorContext = editorUnitId === _univerjs_core.DOCS_NORMAL_EDITOR_UNIT_ID_KEY || editorUnitId === _univerjs_core.DOCS_FORMULA_BAR_EDITOR_UNIT_ID_KEY;
	if (isSheetFormulaEditor(editorUnitId, univerInstanceService)) return;
	const previousFocusedUnitId = ((_univerInstanceServic = univerInstanceService.getFocusedUnit()) === null || _univerInstanceServic === void 0 ? void 0 : _univerInstanceServic.getUnitId()) ?? null;
	const shouldRestoreFocus = !shouldUseSheetEditorContext && previousFocusedUnitId !== null && previousFocusedUnitId !== editorUnitId;
	if (!shouldUseSheetEditorContext && previousFocusedUnitId !== editorUnitId) univerInstanceService.focusUnit(editorUnitId);
	commandService.executeCommand(commandId).finally(() => {
		if (shouldRestoreFocus) univerInstanceService.focusUnit(previousFocusedUnitId);
	});
}
function createEditorUndoRedoKeyboardConfig(options) {
	const { commandService, editorUnitId, handler, keyCodes = [], univerInstanceService } = options;
	const ctrlCommand = _univerjs_ui.MetaKeys.CTRL_COMMAND;
	const ctrlCommandShift = _univerjs_ui.MetaKeys.CTRL_COMMAND | _univerjs_ui.MetaKeys.SHIFT;
	return {
		keyCodes: [
			{
				keyCode: _univerjs_ui.KeyCode.Z,
				metaKey: ctrlCommand
			},
			{
				keyCode: _univerjs_ui.KeyCode.Y,
				metaKey: ctrlCommand
			},
			{
				keyCode: _univerjs_ui.KeyCode.Z,
				metaKey: ctrlCommandShift
			},
			...keyCodes
		],
		handler: (keyCode, metaKey) => {
			switch (`${keyCode}:${metaKey ?? 0}`) {
				case `${_univerjs_ui.KeyCode.Z}:${ctrlCommand}`:
					executeEditorUndoRedoCommand({
						commandId: _univerjs_core.UndoCommand.id,
						commandService,
						editorUnitId,
						univerInstanceService
					});
					return;
				case `${_univerjs_ui.KeyCode.Y}:${ctrlCommand}`:
				case `${_univerjs_ui.KeyCode.Z}:${ctrlCommandShift}`:
					executeEditorUndoRedoCommand({
						commandId: _univerjs_core.RedoCommand.id,
						commandService,
						editorUnitId,
						univerInstanceService
					});
					return;
			}
			handler === null || handler === void 0 || handler(keyCode, metaKey);
		}
	};
}

//#endregion
//#region src/views/rich-text-editor/hooks/use-editor.ts
function useEditor(opts) {
	const { editorId, initialValue, container, preserveHostFocus, autoFocus: _autoFocus, isSingle, canvasStyle } = opts;
	const autoFocus = (0, react.useMemo)(() => _autoFocus ?? false, []);
	const [editor, setEditor] = (0, react.useState)();
	const editorService = (0, _univerjs_ui.useDependency)(IEditorService);
	(0, react.useLayoutEffect)(() => {
		if (container.current) {
			let focusFrame;
			const initialDoc = typeof initialValue === "string" ? _univerjs_core.RichTextBuilder.create().insertText(initialValue).getData() : _univerjs_core.Tools.deepClone(initialValue);
			const snapshot = {
				body: {
					dataStream: typeof initialValue === "string" ? `${initialValue}\r\n` : "\r\n",
					textRuns: [],
					customBlocks: [],
					customDecorations: [],
					customRanges: [],
					paragraphs: [{
						startIndex: 0,
						paragraphId: (0, _univerjs_core.createParagraphId)(/* @__PURE__ */ new Set())
					}]
				},
				...initialDoc,
				documentStyle: {
					...initialDoc === null || initialDoc === void 0 ? void 0 : initialDoc.documentStyle,
					pageSize: {
						width: !isSingle ? container.current.clientWidth : Infinity,
						height: Infinity
					}
				},
				id: editorId
			};
			const dispose = editorService.register({
				autofocus: true,
				canvasStyle,
				editorUnitId: editorId,
				initialSnapshot: snapshot,
				preserveHostFocus
			}, container.current);
			const editor = editorService.getEditor(editorId);
			setEditor(editor);
			if (autoFocus) {
				var _snapshot$body;
				editorService.focus(editorId);
				const end = (((_snapshot$body = snapshot.body) === null || _snapshot$body === void 0 ? void 0 : _snapshot$body.dataStream.length) ?? 2) - 2;
				focusFrame = requestAnimationFrame(() => {
					editor.setSelectionRanges([{
						startOffset: end,
						endOffset: end
					}]);
				});
			}
			return () => {
				if (focusFrame !== void 0) cancelAnimationFrame(focusFrame);
				dispose === null || dispose === void 0 || dispose.dispose();
			};
		}
	}, []);
	return editor;
}

//#endregion
//#region src/views/rich-text-editor/hooks/use-editor-click-outside.ts
const useEditorClickOutside = (editorId, containerRef, onClickOutside) => {
	const editorService = (0, _univerjs_ui.useDependency)(IEditorService);
	(0, react.useEffect)(() => {
		const handleClickOutside = (event) => {
			var _event$target;
			if (editorService.getFocusId() !== editorId) return;
			if (((_event$target = event.target) === null || _event$target === void 0 || (_event$target = _event$target.dataset) === null || _event$target === void 0 ? void 0 : _event$target.editorid) === editorId) return;
			if (containerRef.current && !containerRef.current.contains(event.target)) onClickOutside === null || onClickOutside === void 0 || onClickOutside();
		};
		const timer = setTimeout(() => {
			document.addEventListener("click", handleClickOutside);
		}, 100);
		return () => {
			document.removeEventListener("click", handleClickOutside);
			clearTimeout(timer);
		};
	}, [
		editorId,
		editorService,
		onClickOutside,
		containerRef
	]);
};

//#endregion
//#region src/views/rich-text-editor/hooks/use-is-focusing.ts
/**
* Copyright 2023-present DreamNum Co., Ltd.
*
* Licensed under the Apache License, Version 2.0 (the "License");
* you may not use this file except in compliance with the License.
* You may obtain a copy of the License at
*
*     http://www.apache.org/licenses/LICENSE-2.0
*
* Unless required by applicable law or agreed to in writing, software
* distributed under the License is distributed on an "AS IS" BASIS,
* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
* See the License for the specific language governing permissions and
* limitations under the License.
*/
function useIsFocusing(editorId) {
	const renderer = (0, _univerjs_ui.useDependency)(_univerjs_engine_render.IRenderManagerService).getRenderUnitById(editorId);
	const docSelectionRenderService = renderer === null || renderer === void 0 ? void 0 : renderer.with(DocSelectionRenderService);
	const selections = (0, _univerjs_ui.useObservable)(docSelectionRenderService === null || docSelectionRenderService === void 0 ? void 0 : docSelectionRenderService.textSelectionInner$);
	return Boolean(((docSelectionRenderService === null || docSelectionRenderService === void 0 ? void 0 : docSelectionRenderService.isFocusing) ?? false) && (selections === null || selections === void 0 ? void 0 : selections.textRanges.some((r) => r.collapsed)));
}

//#endregion
//#region src/views/rich-text-editor/hooks/use-keyboard-event.ts
function useKeyboardEvent(isNeed, config, editor) {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const shortcutService = (0, _univerjs_ui.useDependency)(_univerjs_ui.IShortcutService);
	const key = (0, react.useMemo)(() => (0, _univerjs_core.generateRandomId)(4), []);
	(0, react.useEffect)(() => {
		if (!editor || !isNeed || !config) return;
		const editorId = editor.getEditorId();
		const operationId = `sheet.operation.editor-${editorId}-keyboard-${key}`;
		const d = new _univerjs_core.DisposableCollection();
		d.add(commandService.registerCommand({
			id: operationId,
			type: _univerjs_core.CommandType.OPERATION,
			handler(_event, params) {
				const { keyCode, metaKey } = params;
				config.handler(keyCode, metaKey);
			}
		}));
		config.keyCodes.map((keyCode) => {
			return {
				id: operationId,
				binding: keyCode.metaKey ? keyCode.keyCode | keyCode.metaKey : keyCode.keyCode,
				eventPreconditions: keyCode.nativeTextEditorBehavior == null ? void 0 : (event) => event.target instanceof HTMLElement && event.target.id === `__editor_${editorId}`,
				nativeTextEditorBehavior: keyCode.nativeTextEditorBehavior,
				preconditions: () => true,
				priority: 901,
				staticParameters: {
					eventType: _univerjs_engine_render.DeviceInputEventType.Keyboard,
					keyCode: keyCode.keyCode,
					metaKey: keyCode.metaKey
				}
			};
		}).forEach((item) => {
			d.add(shortcutService.registerShortcut(item));
		});
		return () => {
			d.dispose();
		};
	}, [
		commandService,
		config,
		editor,
		isNeed,
		key,
		shortcutService
	]);
}

//#endregion
//#region src/views/rich-text-editor/hooks/use-left-and-right-arrow.ts
const useLeftAndRightArrow = (isNeed, selectingMode, editor, onMoveInEditor) => {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const shortcutService = (0, _univerjs_ui.useDependency)(_univerjs_ui.IShortcutService);
	const operationId = `doc.rich-text-editor.arrow.${(0, react.useId)()}`;
	const selectingModeRef = (0, react.useRef)(selectingMode);
	selectingModeRef.current = selectingMode;
	const onMoveInEditorRef = (0, react.useRef)(onMoveInEditor);
	onMoveInEditorRef.current = onMoveInEditor;
	(0, react.useEffect)(() => {
		if (!editor || !isNeed) return;
		const d = new _univerjs_core.DisposableCollection();
		const handleMoveInEditor = (keycode, metaKey) => {
			if (onMoveInEditorRef.current) {
				onMoveInEditorRef.current(keycode, metaKey);
				return;
			}
			let direction = _univerjs_core.Direction.LEFT;
			if (keycode === _univerjs_ui.KeyCode.ARROW_DOWN) direction = _univerjs_core.Direction.DOWN;
			else if (keycode === _univerjs_ui.KeyCode.ARROW_UP) direction = _univerjs_core.Direction.UP;
			else if (keycode === _univerjs_ui.KeyCode.ARROW_RIGHT) direction = _univerjs_core.Direction.RIGHT;
			if (metaKey === _univerjs_ui.MetaKeys.SHIFT) commandService.executeCommand(MoveSelectionOperation.id, { direction });
			else commandService.executeCommand(MoveCursorOperation.id, { direction });
		};
		d.add(commandService.registerCommand({
			id: operationId,
			type: _univerjs_core.CommandType.OPERATION,
			handler(_event, params) {
				const { keyCode, metaKey } = params;
				handleMoveInEditor(keyCode, metaKey);
			}
		}));
		[
			{ keyCode: _univerjs_ui.KeyCode.ARROW_DOWN },
			{ keyCode: _univerjs_ui.KeyCode.ARROW_LEFT },
			{ keyCode: _univerjs_ui.KeyCode.ARROW_RIGHT },
			{ keyCode: _univerjs_ui.KeyCode.ARROW_UP },
			{
				keyCode: _univerjs_ui.KeyCode.ARROW_DOWN,
				metaKey: _univerjs_ui.MetaKeys.SHIFT
			},
			{
				keyCode: _univerjs_ui.KeyCode.ARROW_LEFT,
				metaKey: _univerjs_ui.MetaKeys.SHIFT
			},
			{
				keyCode: _univerjs_ui.KeyCode.ARROW_RIGHT,
				metaKey: _univerjs_ui.MetaKeys.SHIFT
			},
			{
				keyCode: _univerjs_ui.KeyCode.ARROW_UP,
				metaKey: _univerjs_ui.MetaKeys.SHIFT
			}
		].map(({ keyCode, metaKey }) => {
			return {
				id: operationId,
				binding: metaKey ? keyCode | metaKey : keyCode,
				preconditions: () => editor.isFocus(),
				priority: 900,
				staticParameters: {
					eventType: _univerjs_engine_render.DeviceInputEventType.Keyboard,
					keyCode,
					metaKey
				}
			};
		}).forEach((item) => {
			d.add(shortcutService.registerShortcut(item));
		});
		return () => {
			d.dispose();
		};
	}, [
		commandService,
		editor,
		isNeed,
		operationId,
		shortcutService
	]);
};

//#endregion
//#region src/views/rich-text-editor/hooks/use-on-change.ts
function useOnChange(editor, onChange) {
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	(0, react.useEffect)(() => {
		if (!editor) return;
		const dispose = commandService.onCommandExecuted((command) => {
			if (command.id === _univerjs_docs.RichTextEditingMutation.id) {
				var _data$body;
				if (command.params.unitId !== editor.getEditorId()) return;
				const data = editor.getDocumentData();
				onChange(data, (0, _univerjs_core.getPlainText)(((_data$body = data.body) === null || _data$body === void 0 ? void 0 : _data$body.dataStream) ?? ""));
			}
		});
		return () => {
			dispose.dispose();
		};
	}, [
		editor,
		onChange,
		commandService
	]);
}

//#endregion
//#region src/views/rich-text-editor/hooks/use-resize.ts
const useResize = (editor, isSingle = true, autoScrollbar, autoScroll) => {
	const resize = (0, react.useCallback)(() => {
		if (editor) {
			var _editor$render$compon;
			const { scene, mainComponent } = editor.render;
			const docSkeletonManagerService = editor.render.with(_univerjs_docs.DocSkeletonManagerService);
			const { width, height } = editor.getBoundingClientRect();
			docSkeletonManagerService.getViewModel().getDataModel().updateDocumentDataPageSize(isSingle ? Infinity : width, Infinity);
			scene.transformByState({
				width,
				height
			});
			mainComponent === null || mainComponent === void 0 || mainComponent.resize(width, height);
			mainComponent === null || mainComponent === void 0 || mainComponent.translate(0, 0);
			(_editor$render$compon = editor.render.components.get("__Document_Render_Background__")) === null || _editor$render$compon === void 0 || _editor$render$compon.translate(0, 0);
			const selectionRanges = editor.getSelectionRanges();
			if (selectionRanges.length > 0) editor.setSelectionRanges(selectionRanges, false);
		}
	}, [editor, isSingle]);
	const checkScrollBar = (0, react.useMemo)(() => {
		return (0, _univerjs_core.debounce)(() => {
			if (!autoScrollbar) return;
			if (!editor || !autoScrollbar || editor.render.isDisposed()) return;
			const skeleton = editor.render.with(_univerjs_docs.DocSkeletonManagerService).getSkeleton();
			const { scene, mainComponent } = editor.render;
			const viewportMain = scene.getViewport("viewMain");
			const { actualWidth, actualHeight } = skeleton.getActualSize();
			const { width, height } = editor.getBoundingClientRect();
			let scrollBar = viewportMain === null || viewportMain === void 0 ? void 0 : viewportMain.getScrollBar();
			const contentWidth = Math.max(actualWidth, width);
			const contentHeight = Math.max(actualHeight, height);
			scene.transformByState({
				width: contentWidth,
				height: contentHeight
			});
			mainComponent === null || mainComponent === void 0 || mainComponent.resize(contentWidth, contentHeight);
			if (!isSingle) if (actualHeight > height) {
				if (scrollBar == null) {
					if (viewportMain) scrollBar = new _univerjs_engine_render.ScrollBar(viewportMain, {
						enableHorizontal: false,
						enableVertical: true,
						barSize: 8,
						minThumbSizeV: 8
					});
				} else viewportMain === null || viewportMain === void 0 || viewportMain.resetCanvasSizeAndUpdateScroll();
				autoScroll && (viewportMain === null || viewportMain === void 0 || viewportMain.scrollToBarPos({
					x: 0,
					y: Infinity
				}));
			} else {
				var _viewportMain$getScro;
				scrollBar = null;
				viewportMain === null || viewportMain === void 0 || viewportMain.scrollToBarPos({
					x: 0,
					y: 0
				});
				viewportMain === null || viewportMain === void 0 || (_viewportMain$getScro = viewportMain.getScrollBar()) === null || _viewportMain$getScro === void 0 || _viewportMain$getScro.dispose();
			}
			else if (actualWidth > width) {
				if (scrollBar == null) viewportMain && new _univerjs_engine_render.ScrollBar(viewportMain, {
					barSize: 8,
					enableVertical: false,
					enableHorizontal: true,
					minThumbSizeV: 8
				});
				else viewportMain === null || viewportMain === void 0 || viewportMain.resetCanvasSizeAndUpdateScroll();
				autoScroll && (viewportMain === null || viewportMain === void 0 || viewportMain.scrollToBarPos({
					x: Infinity,
					y: 0
				}));
			} else {
				var _viewportMain$getScro2;
				scrollBar = null;
				viewportMain === null || viewportMain === void 0 || viewportMain.scrollToBarPos({
					x: 0,
					y: 0
				});
				viewportMain === null || viewportMain === void 0 || (_viewportMain$getScro2 = viewportMain.getScrollBar()) === null || _viewportMain$getScro2 === void 0 || _viewportMain$getScro2.dispose();
			}
		}, 30);
	}, [
		editor,
		autoScrollbar,
		isSingle,
		autoScroll
	]);
	(0, react.useEffect)(() => {
		if (!autoScrollbar) return;
		if (editor) {
			const time = setTimeout(() => {
				resize();
				checkScrollBar();
			}, 500);
			return () => {
				clearTimeout(time);
			};
		}
	}, [
		editor,
		autoScrollbar,
		resize,
		checkScrollBar
	]);
	(0, react.useEffect)(() => {
		if (!autoScrollbar) return;
		if (editor) {
			const d = editor.input$.subscribe(() => {
				checkScrollBar();
			});
			return () => {
				d.unsubscribe();
			};
		}
	}, [
		editor,
		autoScrollbar,
		checkScrollBar
	]);
	return {
		resize,
		checkScrollBar
	};
};

//#endregion
//#region src/views/RichTextEditor.tsx
const RichTextEditor = (props) => {
	const { className, autoFocus, onFocusChange: _onFocusChange, initialValue, onClickOutside: _onClickOutside, preserveHostFocus, keyboardEventConfig, moveCursor = true, style, isSingle, editorId: propsEditorId, onHeightChange, onChange: _onChange, defaultHeight = 32, maxHeight = 32, icon, editorRef, placeholder, noStyle, canvasStyle } = props;
	(0, _univerjs_ui.useDependency)(IEditorService);
	const commandService = (0, _univerjs_ui.useDependency)(_univerjs_core.ICommandService);
	const univerInstanceService = (0, _univerjs_ui.useDependency)(_univerjs_core.IUniverInstanceService);
	const onFocusChange = (0, _univerjs_ui.useEvent)(_onFocusChange);
	const onClickOutside = (0, _univerjs_ui.useEvent)(_onClickOutside);
	const [height, setHeight] = (0, react.useState)(defaultHeight);
	const formulaEditorContainerRef = (0, react.useRef)(null);
	const editorId = (0, react.useMemo)(() => propsEditorId ?? (0, _univerjs_core.createInternalEditorID)(`RICH_TEXT_EDITOR-${(0, _univerjs_core.generateRandomId)(4)}`), [propsEditorId]);
	const editor = useEditor({
		editorId,
		initialValue,
		container: formulaEditorContainerRef,
		preserveHostFocus,
		autoFocus,
		isSingle,
		canvasStyle
	});
	const renderer = (0, _univerjs_ui.useDependency)(_univerjs_engine_render.IRenderManagerService).getRenderUnitById(editorId);
	const selectionIsFocusing = useIsFocusing(editorId);
	const docSelectionRenderService = renderer === null || renderer === void 0 ? void 0 : renderer.with(DocSelectionRenderService);
	const editorFocused = (0, _univerjs_ui.useObservable)(editor ? () => (0, rxjs.merge)(editor.focus$, editor.blur$).pipe((0, rxjs.map)(() => (docSelectionRenderService === null || docSelectionRenderService === void 0 ? void 0 : docSelectionRenderService.isFocusing) ?? false), (0, rxjs.startWith)((docSelectionRenderService === null || docSelectionRenderService === void 0 ? void 0 : docSelectionRenderService.isFocusing) ?? false)) : null, (docSelectionRenderService === null || docSelectionRenderService === void 0 ? void 0 : docSelectionRenderService.isFocusing) ?? false, false, [docSelectionRenderService, editor]);
	const isFocusing = selectionIsFocusing && editorFocused;
	const sheetEmbeddingRef = (0, react.useRef)(null);
	const resolveShowPlaceholder = () => {
		var _editor$getDocumentDa;
		return !_univerjs_core.BuildTextUtils.transform.getPlainText((editor === null || editor === void 0 || (_editor$getDocumentDa = editor.getDocumentData().body) === null || _editor$getDocumentDa === void 0 ? void 0 : _editor$getDocumentDa.dataStream) ?? "");
	};
	const showPlaceholder = (0, _univerjs_ui.useObservable)(editor ? () => editor.selectionChange$.pipe((0, rxjs.map)(resolveShowPlaceholder), (0, rxjs.startWith)(resolveShowPlaceholder())) : null, resolveShowPlaceholder(), false, [editor]);
	const { checkScrollBar } = useResize(editor, isSingle, true, true);
	(0, react.useLayoutEffect)(() => {
		if (!editorRef || !editor) return;
		if (typeof editorRef === "function") {
			editorRef(editor);
			return;
		}
		editorRef.current = editor;
	}, [editor]);
	const onChange = (0, _univerjs_ui.useEvent)((data) => {
		var _data$body;
		const docSkeleton = renderer === null || renderer === void 0 ? void 0 : renderer.with(_univerjs_docs.DocSkeletonManagerService);
		const size = docSkeleton === null || docSkeleton === void 0 ? void 0 : docSkeleton.getSkeleton().getActualSize();
		if (size) {
			onHeightChange === null || onHeightChange === void 0 || onHeightChange(size.actualHeight);
			setHeight(Math.max(defaultHeight, Math.min(size.actualHeight + 10, maxHeight)));
		}
		_onChange === null || _onChange === void 0 || _onChange(data, (0, _univerjs_core.getPlainText)(((_data$body = data.body) === null || _data$body === void 0 ? void 0 : _data$body.dataStream) ?? ""));
		checkScrollBar();
	});
	(0, react.useEffect)(() => {
		var _data$body2;
		const data = editor === null || editor === void 0 ? void 0 : editor.getDocumentData();
		onFocusChange === null || onFocusChange === void 0 || onFocusChange(isFocusing, (0, _univerjs_core.getPlainText)((data === null || data === void 0 || (_data$body2 = data.body) === null || _data$body2 === void 0 ? void 0 : _data$body2.dataStream) ?? ""));
	}, [isFocusing, onFocusChange]);
	useEditorClickOutside(editorId, sheetEmbeddingRef, onClickOutside);
	const resolvedKeyboardEventConfig = (0, react.useMemo)(() => createEditorUndoRedoKeyboardConfig({
		commandService,
		univerInstanceService,
		editorUnitId: editorId,
		keyCodes: keyboardEventConfig === null || keyboardEventConfig === void 0 ? void 0 : keyboardEventConfig.keyCodes,
		handler: keyboardEventConfig === null || keyboardEventConfig === void 0 ? void 0 : keyboardEventConfig.handler
	}), [
		commandService,
		editorId,
		keyboardEventConfig,
		univerInstanceService
	]);
	useLeftAndRightArrow(isFocusing && moveCursor, false, editor);
	useKeyboardEvent(isFocusing, resolvedKeyboardEventConfig, editor);
	useOnChange(editor, onChange);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
		className,
		style,
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsxs)("div", {
			className: (0, _univerjs_design.clsx)({
				"univer-border-primary-500": isFocusing && !noStyle,
				["univer-relative univer-box-border univer-flex univer-h-8 univer-w-full univer-items-center univer-justify-around univer-gap-2 univer-rounded-md univer-pb-0.5 univer-pl-1.5 univer-pr-2 univer-pt-1.5"]: !noStyle,
				"univer-size-full": noStyle,
				[_univerjs_design.borderClassName]: !noStyle
			}),
			style: { height },
			ref: sheetEmbeddingRef,
			children: [
				/* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					ref: formulaEditorContainerRef,
					className: "univer-relative univer-size-full",
					onMouseUp: () => editor === null || editor === void 0 ? void 0 : editor.focus()
				}),
				icon,
				!showPlaceholder ? null : /* @__PURE__ */ (0, react_jsx_runtime.jsx)("div", {
					className: "univer-absolute univer-left-[5px] univer-top-[5px] univer-text-sm univer-text-gray-500",
					children: placeholder
				})
			]
		})
	});
};

//#endregion
exports.AfterSpaceCommand = AfterSpaceCommand;
exports.AlignCenterCommand = AlignCenterCommand;
exports.AlignJustifyCommand = AlignJustifyCommand;
exports.AlignLeftCommand = AlignLeftCommand;
exports.AlignMenuItemFactory = AlignMenuItemFactory;
exports.AlignOperationCommand = AlignOperationCommand;
exports.AlignRightCommand = AlignRightCommand;
exports.BackgroundColorSelectorMenuItemFactory = BackgroundColorSelectorMenuItemFactory;
exports.BoldMenuItemFactory = BoldMenuItemFactory;
exports.BreakLineCommand = BreakLineCommand;
exports.BulletListCommand = BulletListCommand;
exports.BulletListTypePicker = BulletListTypePicker;
exports.ChangeListNestingLevelCommand = ChangeListNestingLevelCommand;
exports.ChangeListNestingLevelType = ChangeListNestingLevelType;
exports.ChangeListTypeCommand = ChangeListTypeCommand;
exports.CheckListCommand = CheckListCommand;
exports.CoverContentCommand = CoverContentCommand;
exports.CreateDocTableCommand = CreateDocTableCommand;
exports.CutContentCommand = CutContentCommand;
exports.DEFAULT_DOC_FIT_TO_WIDTH_OPTIONS = DEFAULT_DOC_FIT_TO_WIDTH_OPTIONS;
exports.DOCS_COMPONENT_BACKGROUND_LAYER_INDEX = DOCS_COMPONENT_BACKGROUND_LAYER_INDEX;
exports.DOCS_COMPONENT_DEFAULT_Z_INDEX = DOCS_COMPONENT_DEFAULT_Z_INDEX;
exports.DOCS_COMPONENT_HEADER_LAYER_INDEX = DOCS_COMPONENT_HEADER_LAYER_INDEX;
exports.DOCS_COMPONENT_MAIN_LAYER_INDEX = DOCS_COMPONENT_MAIN_LAYER_INDEX;
exports.DOCS_UI_PLUGIN_CONFIG_KEY = DOCS_UI_PLUGIN_CONFIG_KEY;
exports.DOCS_VIEW_KEY = DOCS_VIEW_KEY;
exports.DOC_CONTENT_INSERT_MENU_ID = DOC_CONTENT_INSERT_MENU_ID;
exports.DOC_CUSTOM_RANGE_CHROME_BACKGROUND_ALPHA = DOC_CUSTOM_RANGE_CHROME_BACKGROUND_ALPHA;
exports.DOC_CUSTOM_RANGE_CHROME_RADIUS = DOC_CUSTOM_RANGE_CHROME_RADIUS;
exports.DOC_EMBED_INTERACTION_BOUNDARY_OWNER_ATTRIBUTE = DOC_EMBED_INTERACTION_BOUNDARY_OWNER_ATTRIBUTE;
exports.DOC_PARAGRAPH_T_ALIGN_MENU_ID = DOC_PARAGRAPH_T_ALIGN_MENU_ID;
exports.DOC_PARAGRAPH_T_COLORS_MENU_ID = DOC_PARAGRAPH_T_COLORS_MENU_ID;
exports.DOC_PARAGRAPH_T_DIVIDER_MENU_ID = DOC_PARAGRAPH_T_DIVIDER_MENU_ID;
exports.DOC_PARAGRAPH_T_EDIT_MENU_ID = DOC_PARAGRAPH_T_EDIT_MENU_ID;
exports.DOC_PARAGRAPH_T_INSERT_BELOW_MENU_ID = DOC_PARAGRAPH_T_INSERT_BELOW_MENU_ID;
exports.DOC_PARAGRAPH_T_INSERT_MENU_ID = DOC_PARAGRAPH_T_INSERT_MENU_ID;
exports.DOC_TABLE_BLOCK_MENU_ID = DOC_TABLE_BLOCK_MENU_ID;
exports.DOC_VERTICAL_PADDING = DOC_VERTICAL_PADDING;
exports.DeleteCurrentParagraphCommand = DeleteCurrentParagraphCommand;
exports.DeleteCustomBlockCommand = DeleteCustomBlockCommand;
exports.DeleteLeftCommand = DeleteLeftCommand;
exports.DeleteRightCommand = DeleteRightCommand;
Object.defineProperty(exports, 'DocAutoFormatService', {
  enumerable: true,
  get: function () {
    return DocAutoFormatService;
  }
});
Object.defineProperty(exports, 'DocBackScrollRenderController', {
  enumerable: true,
  get: function () {
    return DocBackScrollRenderController;
  }
});
Object.defineProperty(exports, 'DocCanvasPopManagerService', {
  enumerable: true,
  get: function () {
    return DocCanvasPopManagerService;
  }
});
exports.DocClipboardPasteAdapterService = DocClipboardPasteAdapterService;
exports.DocCopyCommand = DocCopyCommand;
exports.DocCreateTableOperation = DocCreateTableOperation;
exports.DocCutCommand = DocCutCommand;
Object.defineProperty(exports, 'DocEventManagerService', {
  enumerable: true,
  get: function () {
    return DocEventManagerService;
  }
});
Object.defineProperty(exports, 'DocFloatMenuService', {
  enumerable: true,
  get: function () {
    return DocFloatMenuService;
  }
});
exports.DocHtmlExportService = DocHtmlExportService;
exports.DocIMEInputManagerService = DocIMEInputManagerService;
exports.DocLayoutInteractionService = DocLayoutInteractionService;
Object.defineProperty(exports, 'DocPageLayoutService', {
  enumerable: true,
  get: function () {
    return DocPageLayoutService;
  }
});
Object.defineProperty(exports, 'DocParagraphMenuService', {
  enumerable: true,
  get: function () {
    return DocParagraphMenuService;
  }
});
Object.defineProperty(exports, 'DocParagraphPlaceholderRenderController', {
  enumerable: true,
  get: function () {
    return DocParagraphPlaceholderRenderController;
  }
});
exports.DocPasteCommand = DocPasteCommand;
exports.DocPrintInterceptorService = DocPrintInterceptorService;
Object.defineProperty(exports, 'DocRenderController', {
  enumerable: true,
  get: function () {
    return DocRenderController;
  }
});
exports.DocSelectAllCommand = DocSelectAllCommand;
Object.defineProperty(exports, 'DocSelectionRenderService', {
  enumerable: true,
  get: function () {
    return DocSelectionRenderService;
  }
});
exports.DocTableDeleteColumnsCommand = DocTableDeleteColumnsCommand;
exports.DocTableDeleteRowsCommand = DocTableDeleteRowsCommand;
exports.DocTableDeleteTableCommand = DocTableDeleteTableCommand;
exports.DocTableInsertColumnCommand = DocTableInsertColumnCommand;
exports.DocTableInsertColumnLeftCommand = DocTableInsertColumnLeftCommand;
exports.DocTableInsertColumnRightCommand = DocTableInsertColumnRightCommand;
exports.DocTableInsertRowAboveCommand = DocTableInsertRowAboveCommand;
exports.DocTableInsertRowBellowCommand = DocTableInsertRowBellowCommand;
exports.DocTableInsertRowCommand = DocTableInsertRowCommand;
exports.DocTableTabCommand = DocTableTabCommand;
Object.defineProperty(exports, 'DocUIController', {
  enumerable: true,
  get: function () {
    return DocUIController;
  }
});
Object.defineProperty(exports, 'DocViewScaleService', {
  enumerable: true,
  get: function () {
    return DocViewScaleService;
  }
});
Object.defineProperty(exports, 'DocsRenderService', {
  enumerable: true,
  get: function () {
    return DocsRenderService;
  }
});
exports.DocsUIMenuSchema = menuSchema;
exports.EMPTY_PARAGRAPH_MENU_ID = EMPTY_PARAGRAPH_MENU_ID;
exports.Editor = Editor;
Object.defineProperty(exports, 'EditorService', {
  enumerable: true,
  get: function () {
    return EditorService;
  }
});
exports.EnterCommand = EnterCommand;
exports.FLOAT_TEXT_STYLE_MENU_ID = FLOAT_TEXT_STYLE_MENU_ID;
exports.FLOAT_TOOLBAR_MENU_POSITION = FLOAT_TOOLBAR_MENU_POSITION;
exports.FloatTextStyleMenuItemFactory = FloatTextStyleMenuItemFactory;
exports.FontFamilySelectorMenuItemFactory = FontFamilySelectorMenuItemFactory;
exports.FontSizeSelectorMenuItemFactory = FontSizeSelectorMenuItemFactory;
exports.HorizontalLineCommand = HorizontalLineCommand;
exports.IDocClipboardPasteAdapterService = IDocClipboardPasteAdapterService;
exports.IDocClipboardService = IDocClipboardService;
exports.IDocEmbedInteractionBoundaryService = IDocEmbedInteractionBoundaryService;
exports.IDocEmbedRuntimeFocusCoordinator = IDocEmbedRuntimeFocusCoordinator;
exports.IEditorService = IEditorService;
exports.IMEInputCommand = IMEInputCommand;
exports.INSERT_BELLOW_MENU_ID = INSERT_BELLOW_MENU_ID;
exports.InnerPasteCommand = InnerPasteCommand;
exports.InsertCustomRangeCommand = InsertCustomRangeCommand;
exports.ItalicMenuItemFactory = ItalicMenuItemFactory;
exports.ListOperationCommand = ListOperationCommand;
exports.MergeTwoParagraphCommand = MergeTwoParagraphCommand;
exports.MobileRichTextToolbar = MobileRichTextToolbar;
exports.MoveCursorOperation = MoveCursorOperation;
exports.MoveDocBlockCommand = MoveDocBlockCommand;
exports.MoveSelectionOperation = MoveSelectionOperation;
exports.NORMAL_TEXT_SELECTION_PLUGIN_NAME = NORMAL_TEXT_SELECTION_PLUGIN_NAME;
exports.NodePositionConvertToCursor = NodePositionConvertToCursor;
exports.NodePositionConvertToRectRange = NodePositionConvertToRectRange;
exports.OrderListCommand = OrderListCommand;
exports.OrderListTypePicker = OrderListTypePicker;
exports.ParagraphMenuInsertBelowSubmenuItemFactory = ParagraphMenuInsertBelowSubmenuItemFactory;
exports.PastePluginLark = LarkPastePlugin;
exports.PastePluginUniver = UniverPastePlugin;
exports.PastePluginWord = WordPastePlugin;
exports.QuickListCommand = QuickListCommand;
exports.RectRange = RectRange;
exports.ReplaceSelectionCommand = ReplaceSelectionCommand;
exports.ReplaceSnapshotCommand = ReplaceSnapshotCommand;
exports.ReplaceTextRunsCommand = ReplaceTextRunsCommand;
exports.ResetInlineFormatTextBackgroundColorCommand = ResetInlineFormatTextBackgroundColorCommand;
exports.ResetInlineFormatTextColorCommand = ResetInlineFormatTextColorCommand;
exports.RichTextEditor = RichTextEditor;
exports.SetDocInputStyleCommand = SetDocInputStyleCommand;
exports.SetDocZoomRatioCommand = SetDocZoomRatioCommand;
exports.SetDocZoomRatioOperation = SetDocZoomRatioOperation;
exports.SetInlineFormatBoldCommand = SetInlineFormatBoldCommand;
exports.SetInlineFormatCommand = SetInlineFormatCommand;
exports.SetInlineFormatFontFamilyCommand = SetInlineFormatFontFamilyCommand;
exports.SetInlineFormatFontSizeCommand = SetInlineFormatFontSizeCommand;
exports.SetInlineFormatItalicCommand = SetInlineFormatItalicCommand;
exports.SetInlineFormatStrikethroughCommand = SetInlineFormatStrikethroughCommand;
exports.SetInlineFormatSubscriptCommand = SetInlineFormatSubscriptCommand;
exports.SetInlineFormatSuperscriptCommand = SetInlineFormatSuperscriptCommand;
exports.SetInlineFormatTextBackgroundColorCommand = SetInlineFormatTextBackgroundColorCommand;
exports.SetInlineFormatTextColorCommand = SetInlineFormatTextColorCommand;
exports.SetInlineFormatTextFillCommand = SetInlineFormatTextFillCommand;
exports.SetInlineFormatUnderlineCommand = SetInlineFormatUnderlineCommand;
exports.SetParagraphNamedStyleCommand = SetParagraphNamedStyleCommand;
exports.StrikeThroughMenuItemFactory = StrikeThroughMenuItemFactory;
exports.SwitchDocModeCommand = SwitchDocModeCommand;
exports.TEXT_RANGE_LAYER_INDEX = TEXT_RANGE_LAYER_INDEX;
exports.TabCommand = TabCommand;
exports.TextColorSelectorMenuItemFactory = TextColorSelectorMenuItemFactory;
exports.TextRange = TextRange;
exports.ToggleCheckListCommand = ToggleCheckListCommand;
exports.UnderlineMenuItemFactory = UnderlineMenuItemFactory;
Object.defineProperty(exports, 'UniverDocsUIPlugin', {
  enumerable: true,
  get: function () {
    return UniverDocsUIPlugin;
  }
});
exports.VIEWPORT_KEY = VIEWPORT_KEY;
exports.addCustomDecorationBySelectionFactory = addCustomDecorationBySelectionFactory;
exports.addCustomDecorationFactory = addCustomDecorationFactory;
exports.buildMoveDocBlockActions = buildMoveDocBlockActions;
exports.calcDocFitToWidthScale = calcDocFitToWidthScale;
exports.calcDocRangePositions = calcDocRangePositions;
exports.collectDocsTableLikeEmbedChildUnitIds = collectDocsTableLikeEmbedChildUnitIds;
exports.convertBodyToHtml = convertBodyToHtml;
exports.convertClipboardHtmlToDocumentData = convertClipboardHtmlToDocumentData;
exports.convertClipboardRtfToPlainText = convertClipboardRtfToPlainText;
exports.convertPositionsToRectRanges = convertPositionsToRectRanges;
exports.createDefaultDocsTableLikeCustomBlockBleedViewport = createDefaultDocsTableLikeCustomBlockBleedViewport;
exports.createDocsCustomBlockSizeRefreshScheduler = createDocsCustomBlockSizeRefreshScheduler;
exports.createEditorUndoRedoKeyboardConfig = createEditorUndoRedoKeyboardConfig;
exports.deleteCustomDecorationFactory = deleteCustomDecorationFactory;
exports.disableMenuWhenHeaderFooterEditing = disableMenuWhenHeaderFooterEditing;
exports.drawDocCustomRangeChrome = drawDocCustomRangeChrome;
exports.executeEditorUndoRedoCommand = executeEditorUndoRedoCommand;
exports.genTableSource = genTableSource;
exports.generateParagraphs = _univerjs_docs.generateParagraphs;
exports.getAnchorBounding = getAnchorBounding;
exports.getCanvasOffsetByEngine = getCanvasOffsetByEngine;
exports.getCommandSkeleton = getCommandSkeleton;
exports.getCursorWhenDelete = getCursorWhenDelete;
exports.getCutActionsFromDocRanges = getCutActionsFromDocRanges;
exports.getDocBlockRangeMenuId = getDocBlockRangeMenuId;
exports.getDocObjectById = getDocObjectById;
exports.getEmptyTableCell = getEmptyTableCell;
exports.getEmptyTableRow = getEmptyTableRow;
exports.getLineBounding = getLineBounding;
exports.getListMarkerFallbackBound = getListMarkerFallbackBound;
exports.getListParagraphContextMenuHit = getListParagraphContextMenuHit;
exports.getOneTextSelectionRange = getOneTextSelectionRange;
exports.getStyleInTextRange = getStyleInTextRange;
exports.getTableColumn = getTableColumn;
exports.getTextRangeFromCharIndex = getTextRangeFromCharIndex;
exports.hasParagraphInTable = hasParagraphInTable;
exports.hideMenuWhenSelectionInBlockRange = hideMenuWhenSelectionInBlockRange;
exports.isInSameTableCell = isInSameTableCell;
exports.isTextRangeInAnyBlockRange = isTextRangeInAnyBlockRange;
exports.isValidRectRange = isValidRectRange;
exports.neoGetDocObject = neoGetDocObject;
exports.normalizeDocFitToWidthOptions = normalizeDocFitToWidthOptions;
exports.removeClipboardHtmlImages = removeClipboardHtmlImages;
exports.resolveDocCustomRangeChromeTheme = resolveDocCustomRangeChromeTheme;
exports.resolveDocFitBaseWidth = resolveDocFitBaseWidth;
exports.resolveDocViewScale = resolveDocViewScale;
exports.resolveDocsCustomBlockRenderViewport = resolveDocsCustomBlockRenderViewport;
exports.resolveDocsTableLikeCustomBlockBleedViewport = resolveDocsTableLikeCustomBlockBleedViewport;
exports.resolveDocsTableLikeCustomBlockContentHeight = resolveDocsTableLikeCustomBlockContentHeight;
exports.resolveDocsTableLikeCustomBlockContentWidth = resolveDocsTableLikeCustomBlockContentWidth;
exports.scrollDocsTableLikeCustomBlockLive = scrollDocsTableLikeCustomBlockLive;
exports.shouldRefreshDocsCustomBlockSizeForCommand = shouldRefreshDocsCustomBlockSizeForCommand;
exports.useEditor = useEditor;
exports.useEditorClickOutside = useEditorClickOutside;
exports.useIsFocusing = useIsFocusing;
exports.useKeyboardEvent = useKeyboardEvent;
exports.useLeftAndRightArrow = useLeftAndRightArrow;
exports.useOnChange = useOnChange;
exports.useResize = useResize;
exports.whenDocAndEditorFocused = whenDocAndEditorFocused;